# Pivot 工具库产品级差距分析与升级方案

> 文档版本：v1.0
> 
> 分析日期：2026-09-20
> 
> 分析对象：Pivot v0.1.154（当前工作区代码）
> 
> 目标：将“工具库”从 MCP 服务与工具缓存管理页，升级为具备连接器目录、授权、契约、发现、运行治理、可观测性和发布生命周期的一等产品能力。

## 1. 结论摘要

Pivot 已经具备较强的 Agent 工具运行底座，安全和执行治理甚至领先于许多只提供“工具列表”的早期产品：

- 已有内置工具、外部 MCP、数据库、报表目录、导入数据集、本机 SQLite/报表/浏览器连接器等多种来源。
- 已有工具策略、能力包、用户/单位共享、数据库表字段白名单、脱敏、超时、SSRF/DNS rebinding 防护、审批、沙箱、幂等账本、Trace、检查点和工作流节点契约。
- 已有工具自适应路由和 Embedding 目录索引，聊天、Agent、DAG、桌面连接器均能复用工具执行能力。
- 已有 MCP Streamable HTTP 的基础客户端、SSE 分块读取、会话恢复和 conformance 测试；专项验证的 29 个相关测试全部通过。

但当前产品仍是“强运行时、弱产品生命周期”的形态。最大差距不在工具数量，而在以下六个产品级闭环尚未完整建立：

1. **工具身份和版本闭环**：工具缓存是服务级平面表，不是可发布、可回滚、可比较的工具版本目录。
2. **连接与凭据闭环**：API Key/数据库密码已有安全存储，但还没有 OAuth、连接账户、Token 刷新/撤销、多个账号选择和授权状态产品化。
3. **发现与上下文闭环**：已有语义路由，但缺少“工具包/用例/示例/前置条件/输入输出说明”驱动的渐进式工具发现；工具规模增大后仍会有上下文和选择噪声。
4. **调用策略闭环**：能力包治理、工具契约和审批逻辑分布在多层，直接测试入口、MCP RPC、聊天 Agent、工作流的执行前检查尚未完全收敛为同一条 Policy Enforcement Point。
5. **长任务与可靠性闭环**：已有 Agent 长任务运行时，但 MCP 直接调用仍以同步请求/响应为主，缺少统一任务句柄、取消、恢复、重试分类、熔断和幂等键。
6. **运营与供应链闭环**：调用日志、Trace 和治理指标已有基础，但缺少按工具版本、连接、凭据、策略决策和租户聚合的运营面板，以及签名、来源证明、测试、灰度和回滚。

建议定位：Pivot 不应简单复制一个第三方 MCP 市场，而应建设 **“企业内控的工具与连接器控制平面”**。短期优先把现有工具治理做成统一控制平面；中期补齐连接器账户、版本目录、渐进式发现和任务协议；长期再开放可签名的工具包市场和第三方开发者生态。

## 2. 对标范围与判断标准

本方案采用四类产品能力进行对照：

| 对标层 | 关注点 | 对 Pivot 的启示 |
|---|---|---|
| MCP 协议与 SDK | 工具 Schema、结构化输出、注解、协议版本、任务、授权、Trace | 解决跨客户端互操作和工具契约标准化 |
| OpenAI Agents / Responses | 工具调用、Guardrails、Handoff、Sessions、Tracing、Hosted/Remote MCP | 解决 Agent 运行时与工具治理的统一体验 |
| Anthropic / Claude MCP 生态 | MCP Connector、工具搜索、渐进式加载、工具描述质量 | 解决大规模工具集的上下文成本与发现问题 |
| n8n / LangGraph / Composio 类产品 | 凭据生命周期、连接账户、节点/工具复用、执行恢复、人工介入 | 解决连接器产品化、工作流可靠性和企业运营问题 |

参考依据：

- [OpenAI Agents SDK](https://openai.github.io/openai-agents-python/) 将工具、Agent 交接、Guardrails、Sessions、MCP 和 Tracing 作为生产运行时的一组能力；[Tracing 文档](https://openai.github.io/openai-agents-python/tracing/)明确记录模型生成、工具调用、交接、Guardrail 和自定义事件。
- [Anthropic Advanced Tool Use](https://www.anthropic.com/engineering/advanced-tool-use)指出，当工具数量变大时，工具定义和结果会大量消耗上下文，因此需要 Tool Search 和更丰富的使用示例，而不仅是 JSON Schema。
- [MCP 最新变更说明](https://modelcontextprotocol.io/specification/draft/changelog)强调工具列表确定性排序、Streamable HTTP 的无状态方向、`Mcp-Method`/`Mcp-Name` 路由头和 OpenTelemetry Trace Context；[MCP Tasks 扩展](https://tasks.extensions.modelcontextprotocol.io/specification/draft/tasks)定义了长任务的 `tasks/get`、`tasks/update`、`tasks/cancel` 生命周期。
- [MCP 工具 Schema 说明](https://ts.sdk.modelcontextprotocol.io/v2/api/index/%40modelcontextprotocol/client/)包含 `outputSchema`、标题、结构化输出等面向客户端和 UI 的契约信息。
- [Composio Sessions](https://docs.composio.dev/docs/how-composio-works)把用户、工具包、授权账户和工具执行绑定到持久 Session，并用少量元工具在运行时发现工具；[Connected Accounts](https://docs.composio.dev/reference/v3/api-reference/connected-accounts)覆盖连接创建、刷新、启停、更新、撤销和删除。
- [LangGraph durable execution](https://docs.langchain.com/oss/javascript/langgraph/thinking-in-langgraph)把检查点、重试、可恢复错误、人工输入和补偿分支作为生产工作流的一部分；[n8n 文档](https://docs.n8n.io/)则体现了凭据、工作流共享、AI Agent Tool 与集成目录的产品化组合。

## 3. 当前项目能力盘点

### 3.1 已有能力

| 能力域 | 当前实现与证据 | 判断 |
|---|---|---|
| 内置工具 | `server/services/agent-tools.js`、`builtin-mcp*.js`，覆盖模型、数据、文档、图表、报告、知识、会话、工作流节点等 | 能力面较完整，但定义分布较散 |
| 外部 MCP | `server/services/mcp-client.js`、`server/routes/mcp/`，支持服务配置、刷新、缓存、调用、诊断 | 已可用，协议代际和生命周期仍需升级 |
| 数据连接 | `server/services/database-mcp/` 支持 PostgreSQL、MySQL、SQL Server、SQLite、MongoDB，以及表/字段白名单和只读 SQL | 安全深度较好，连接账户抽象不足 |
| 本机能力 | `desktop/local-mcp-connector.js`、`agent-local-connector.js`、`local-browser-connector-tools.js` | 本机反向执行、浏览器隔离和授权已有特色 |
| 工具目录 | `agent-tool-catalog.js`、`capability-market.js`、`mcp-tool-presentation.js` | 已有目录投影、中文展示、能力包和精确白名单 |
| 语义发现 | `mcp-tool-catalog-index.js`、`semantic-router.js` | 有 Embedding + 规则 + 词法回退，但偏“路由实现”，还不是完整工具搜索产品 |
| 工具契约 | `agent-contracts.js`、`agent-tool-capabilities.js` | 已有风险、能力、幂等、缓存、并发、取消、超时和输入 Schema；外部工具契约不完整 |
| 治理 | `capability_packages`、工具策略页、共享权限、审计、审批、网络策略 | 基础强，但需要统一执行前策略入口 |
| 可靠性 | Agent runtime、检查点、租约、重试、SSE 读取、连接器重试 | Agent 任务强，MCP 单次调用的任务化和熔断不足 |
| 可观测性 | `agent_traces`、`agent_trace_spans`、`mcp_call_logs`、Prometheus/治理指标 | 有数据点，但调用链关联和运营查询维度不足 |
| 测试与合规 | MCP conformance、工具 Schema/安全/连接器/路由测试；专项 29/29 通过 | 工程质量好，但缺少工具质量基线、契约兼容和线上回放评测 |

### 3.2 当前关键数据模型

当前 MCP 相关核心表为：

- `mcp_servers`：服务地址、密钥、配置、范围、状态、最后错误和检查时间。
- `mcp_tool_cache`：`server_id`、`name`、`description`、`input_schema`、`cached_at`。
- `mcp_call_logs`：用户、服务、工具、来源、状态、耗时、输入/输出预览和错误。
- `mcp_database_connections`、`mcp_builtin_configs`：数据库与内置服务配置。
- `capability_packages`：能力包状态、范围和工具治理配置。
- Agent 运行侧已有 `agent_runs`、`agent_steps`、`agent_traces`、`agent_trace_spans`、审批、检查点和产物表。

这些表能支撑“当前可用什么”和“刚才是否调用过”，但不能完整回答产品级问题：

- 这个工具来自哪个版本、何时变更、是否与已发布工作流兼容？
- 当前展示的 Schema 是否就是执行时使用的 Schema？
- 这个调用使用哪个连接账户、哪个授权 Scope、哪次用户批准？
- 该工具过去 24 小时的成功率、P95、限流率、Schema 错误率是多少？
- 某个版本下线后，哪些工作流、技能、模板和计划任务仍在依赖它？

## 4. 产品级差距评估

评分含义：5 为产品级成熟，1 为明显缺失。评分不是代码质量评分，而是“面向规模化用户交付的闭环成熟度”。

| 维度 | 当前评分 | 差距 | 优先级 |
|---|---:|---|---|
| 工具数量与基础执行 | 4.0 | 能力丰富，执行入口多 | P1 |
| 工具 Schema 与输出契约 | 2.5 | MCP 输出 Schema/注解/运行时校验不完整 | P0 |
| 工具身份、版本和兼容性 | 1.5 | 缓存没有版本、快照、差异和回滚 | P0 |
| 连接与凭据管理 | 2.0 | API Key/数据库密钥有基础，OAuth/多账户/刷新撤销缺失 | P0 |
| 工具发现与上下文控制 | 2.5 | 有语义路由，缺少渐进式 Tool Search 和用例资产 | P1 |
| 权限、审批与策略统一性 | 3.5 | 治理较强，但多入口执行策略需要收口 | P0 |
| 长任务、取消、恢复和幂等 | 2.5 | Agent runtime 较强，MCP 调用未统一任务化 | P1 |
| 重试、熔断、限流和隔离 | 2.5 | 有超时/SSE 重连，缺少按连接/工具的运行保护 | P1 |
| Trace、日志、指标和成本 | 2.5 | 有 Trace 与日志，但字段和关联粒度不足 | P1 |
| UI/UX 与自助连接体验 | 2.5 | 管理能力完整，普通用户缺少任务导向和授权向导 | P1 |
| 工具包供应链与发布 | 1.5 | 技能有治理，工具没有完整 manifest/签名/灰度 | P2 |
| 工具质量评测与运营 | 2.0 | 有 Agent 评测基础，缺工具选择/参数/契约质量基线 | P1 |

综合判断：Pivot 已达到“企业 Agent 工具执行底座”水平，尚未达到“可让非技术用户持续接入和运营连接器的产品级工具平台”水平。

## 5. 具体差距与优化方案

### 5.1 P0：建立统一 Tool Contract 与 Catalog Release

#### 问题

当前 `mcp_tool_cache` 只保存输入侧最小元数据；`refreshMcpTools()` 采取先删除再写入的方式，无法保留旧版本、产生差异、回滚或追溯“某次运行到底使用了哪份 Schema”。外部工具的 `output_schema`、annotations、认证要求、能力声明、数据分类和使用示例也没有稳定落库。

`agent-contracts.js` 虽然提供了统一归一化，但对未声明的 MCP 工具仍会按名称和来源推断部分风险、网络、幂等和副作用属性。能力标识已经改为显式登记，这是正确方向；还需要把整个工具契约都改为“服务端接收、校验、冻结、版本化”。

#### 方案

新增三层对象：

1. `tool_catalog_releases`：一次 `tools/list` 的不可变快照。
2. `tool_catalog_items`：快照中的工具定义和归一化契约。
3. `tool_catalog_aliases`：展示名、旧工具名、中文名、搜索别名与迁移提示。

建议字段：

```text
tool_catalog_releases
- id, server_id, release_version, protocol_version
- source_etag, source_last_modified, source_digest
- fetched_at, published_at, status(active|stale|blocked|rolled_back)
- fetch_duration_ms, tool_count, conformance_status, error_summary

tool_catalog_items
- release_id, server_id, tool_name, full_name
- title, description, input_schema, output_schema
- annotations, capabilities, risk_level, side_effect
- idempotent, cacheable, cancellable, concurrency
- timeout_default_seconds, timeout_max_seconds
- auth_scopes, data_classification, examples, tags
- definition_digest, compatibility_level
- status(active|deprecated|blocked), deprecation_message
```

刷新流程改为：

```text
tools/list
  -> 协议/Schema/工具名校验
  -> 规范化与能力登记校验
  -> 计算 definition_digest
  -> 创建候选 release
  -> 与 active release 做差异检查
  -> 自动激活低风险兼容变更
  -> 高风险/破坏性变更进入审核或灰度
  -> 失败时保留旧 release 继续服务
```

必须增加的兼容性规则：

- 输入必填字段增加、字段类型收窄、工具删除、工具名变化视为破坏性变更。
- 可选字段增加、描述/示例/展示名变化视为兼容变更。
- 输出 Schema 变化需要标记 `output_breaking`，不能只比较输入 Schema。
- 对已发布工作流保存 `tool_name + release_id + definition_digest`；新版本激活后标记依赖项 `stale`，由用户重新确认发布。
- 工具测试、工作流预检、Agent 执行必须记录实际 `release_id` 和 `definition_digest`。

### 5.2 P0：把所有执行入口收敛到统一 PEP

当前工具可以从聊天 Agent、工作流、桌面连接器、`/mcp/tools/call`、`/mcp/rpc` 和单步测试进入。能力包和工具治理已存在，但产品级要求是所有入口都经过同一套策略判定，并且策略结果可解释、可审计、可重放。

新增 `server/services/tool-policy-engine.js`，提供唯一入口：

```js
const decision = await evaluateToolInvocation({
  actor, tenant, run, tool, release, input,
  connection, credential, source, traceContext
});

// decision: allow | deny | require_approval | require_reauth | task_required
// reasonCodes: capability_denied, scope_missing, stale_release,
//               high_risk, rate_limited, credential_expired, input_invalid ...
```

执行前顺序固定为：

1. 解析工具完整标识和 active/referenced release。
2. 校验租户、用户、单位、资源共享和工具包状态。
3. 校验 capability、工具级治理、网络策略、数据范围和凭据 Scope。
4. 校验输入 Schema、大小、敏感字段和任务预算。
5. 判断是否必须审批、是否必须异步任务化、是否允许重试。
6. 写入不可变 policy decision 事件。
7. 执行工具，并在输出侧校验 `output_schema` 和脱敏规则。

`/mcp/tools/call` 和 `/mcp/rpc` 不应继续直接调用 `executeBuiltInTool()`/`executeMcpTool()`；它们只负责协议适配，实际执行统一走 `tool-policy-engine`。手动单步测试也必须标记为 `source=manual_test`，不能绕过策略。

### 5.3 P0：连接器与凭据升级为独立产品对象

当前 `mcp_servers` 同时承载服务地址、工具来源、共享范围和 API Key；数据库凭据也直接挂在连接配置上。这种设计适合第一阶段，但不适合多个用户账号、OAuth、轮换和授权撤销。

新增对象：

```text
connector_definitions
- slug, display_name, icon, category, docs_url
- auth_type, supported_protocols, default_scopes
- data_residency, privacy_url, owner, version, status

connection_accounts
- id, connector_id, tenant_id, owner_user_id
- display_name, auth_state, provider_subject
- encrypted_secret_ref, scopes, expires_at, last_refresh_at
- last_error, created_at, revoked_at

tool_connection_bindings
- tool_id/release_id, connection_account_id
- allowed_scopes, field_policy, default_account, status
```

状态机：

```text
unconnected -> authorizing -> active -> expiring -> refresh_failed
                                      -> disabled -> revoked
```

产品要求：

- 用户看到的是“连接 Git/数据库/业务系统账号”，而不是手工填写一堆 Header 和地址。
- 凭据永远不进入工具 Schema、模型上下文、日志预览和工作流版本。
- 支持个人连接、单位共享连接、服务账号三种归属，但三者权限和审计主体不同。
- 每次调用记录 `connection_account_id`、授权 Scope 摘要和凭据状态，不记录密钥本身。
- 支持重新授权、轮换、撤销、禁用、测试连接、查看最近失败原因。
- 对 OAuth 接入使用授权码 + PKCE，校验 issuer、redirect URI 和 state；对 API Key 保留当前加密存储和脱敏策略。

### 5.4 P1：从“把工具全交给模型”升级为渐进式 Tool Search

当前 `semantic-router.js` 已经能对已授权工具做规则、词法和向量排序，这是很好的基础。但它是应用内部路由，不足以承担大规模工具库的完整发现：工具没有稳定的使用场景、示例、前置条件和可解释的搜索结果，也没有把“发现”和“执行”明确分开。

建议保留现有路由作为第一阶段候选召回，同时增加三个只读元工具：

- `tools.search`：按用户问题、标签、工具包和风险筛选，返回少量工具摘要及 `tool_ref`。
- `tools.describe`：按 `tool_ref` 返回完整输入/输出 Schema、示例、权限和连接要求。
- `tools.execute`：只接受已通过 Policy 的 `tool_ref`，内部转到统一执行器。

模型默认只看到：

- 低风险高频内置工具；
- 当前任务语义召回的前 5 个工具摘要；
- `tools.search` 和 `tools.describe` 元工具。

模型在真正需要时再加载完整 Schema。对单步工作流和用户明确勾选的工具，继续支持直接注入完整 Schema，保证确定性。

工具目录索引应从进程内 2048 条向量缓存升级为持久化、分租户、按 release 的索引：

- 词法索引：工具名、中文展示名、描述、标签、示例和领域词。
- 向量索引：`semantic_signature` + embedding model/version + release digest。
- 规则索引：任务类型、数据源、输出类型、风险、是否需要连接、是否支持工作流。
- 缓存策略：按照工具列表 `ttlMs`/版本指纹失效，确定性排序，避免无意义刷新和上下文抖动。

每次路由返回可解释摘要：命中了哪个示例/标签/语义片段，为什么没有选择另一个工具，以及是否因权限、连接或风险被过滤。

### 5.5 P1：支持长任务、取消、重试和幂等

Agent runtime 已有持久化运行、租约、检查点和审批恢复；但外部 MCP 直接调用主要仍是一个 HTTP 请求。对于导入、报表生成、批量写入、浏览器长操作等工具，需要统一任务化。

统一调用结果：

```json
{
  "resultType": "task",
  "task": {
    "id": "task_xxx",
    "status": "working",
    "pollAfterMs": 1000,
    "expiresAt": "2026-09-20T12:00:00Z"
  }
}
```

服务端内部实现可先复用现有 `agent_runs`/`agent_steps` 和队列，不必立即重写执行器：

- 同步短工具继续直接返回。
- 超过阈值、明确声明 `long_running`、需要人工输入或调用方要求后台运行时，返回任务句柄。
- 提供 `GET /api/tools/tasks/:id`、`POST .../cancel`、`POST .../input`。
- 任务句柄必须高熵、绑定租户和用户；每次查询都重新鉴权。
- 重试按错误类型分级：连接断开/429/5xx 可退避重试；Schema/权限/业务拒绝不重试；写操作必须有幂等键或明确禁止重试。
- 连接级和工具级增加并发舱壁、速率限制、熔断、半开探测和最大排队时间。

### 5.6 P1：可观测性从“日志预览”升级为工具运营数据

`mcp_call_logs` 当前存储截断后的输入/输出预览，优点是安全且轻量，但不足以支撑线上诊断和成本运营。建议保留预览，同时增加不可变调用事件和 Trace 关联：

```text
tool_invocation_events
- trace_id, span_id, parent_span_id
- run_id, step_id, session_id, tenant_id, actor_id
- tool_id, release_id, server_id, connection_account_id
- policy_decision, approval_id, source
- request_digest, input_ref, output_ref, output_digest
- attempt, queue_ms, network_ms, execution_ms, total_ms
- status, error_class, error_code, retryable, cancelled
- input_tokens, output_tokens, estimated_cost
- created_at
```

指标最少包括：

- 工具调用量、成功率、错误率、取消率、审批通过率。
- P50/P95/P99 延迟，排队时间和下游网络时间。
- Schema 校验失败、权限拒绝、凭据过期、429、5xx、超时、熔断次数。
- 工具发现命中率、工具选择后实际调用率、错误重选率、无工具回答率。
- 按租户、工具包、工具版本、连接账户、模型、工作流和来源分组。
- 数据脱敏命中数、输出截断数、超预算数。

Trace 采用 W3C `traceparent`/`tracestate`，并在 MCP `_meta` 或标准头中透传；同时保留 Pivot 自己的 `run_id` 和 `step_id`，避免只能依赖第三方 Trace 系统。

### 5.7 P1：工具质量评测和发布门禁

现有测试证明安全和基础契约较好，但工具库需要独立质量基线。每个工具包发布前至少自动检查：

1. 工具名、描述、输入/输出 Schema、标题、示例完整性。
2. Schema 能否生成有效样例；样例调用是否得到预期结构化输出。
3. 只读/副作用/幂等/网络/风险/审批标记是否与行为一致。
4. 超时、取消、限流、429、5xx、断流、重复调用和大结果处理。
5. 权限拒绝、过期凭据、越权资源、敏感字段和 SSRF 回归。
6. 工具选择评测：给定 20~50 条真实任务，测 Top-1、Top-3、误选率和拒绝率。
7. 破坏性 Schema 变化是否正确阻断依赖工作流发布。

新增 `tool_eval_suites`、`tool_eval_cases`、`tool_eval_runs`、`tool_eval_results`，或与现有 Agent 评测表建立统一的 `subject_type=tool_release` 维度。发布门禁建议：

- P0 安全回归全部通过；
- Schema/调用契约通过率 100%；
- 工具选择 Top-3 召回率达到业务设定阈值；
- P95 延迟和错误率不劣化超过阈值；
- 破坏性变更必须有迁移说明和依赖清单；
- 高风险工具必须通过人工审核或灰度。

### 5.8 P2：工具包供应链、签名和市场能力

现有 Skill 已有签名、来源、发布和 rollout 相关能力，工具库应复用同一治理理念，而不是继续把外部 MCP 当作一个 URL + API Key。

建议定义 `pivot.toolkit.json`：

```json
{
  "schemaVersion": "1.0",
  "slug": "acme.erp",
  "version": "2.3.0",
  "displayName": "ERP 业务工具",
  "publisher": "acme",
  "protocol": { "type": "mcp", "versions": ["2025-11-25", "2026-07-28"] },
  "tools": ["orders.search", "orders.create"],
  "requiredCapabilities": ["network.request"],
  "requiredScopes": ["orders.read"],
  "dataClassification": "internal",
  "privacyUrl": "https://example.invalid/privacy",
  "healthcheck": true,
  "signature": "..."
}
```

供应链流程：导入 -> 静态扫描 -> conformance -> 工具评测 -> 签名验证 -> 沙箱 -> 管理员审核 -> 灰度 -> 发布 -> 监控 -> 回滚。

## 6. 目标架构

```text
                    ┌────────────────────────────┐
                    │ 工具库产品 UI / 管理 API     │
                    │ 目录、连接、策略、测试、运营 │
                    └─────────────┬──────────────┘
                                  │
                    ┌─────────────▼──────────────┐
                    │ Tool Control Plane           │
                    │ Catalog / Release / Policy   │
                    │ Connection / Evaluation      │
                    └──────┬──────────┬───────────┘
                           │          │
             ┌─────────────▼───┐  ┌──▼────────────────┐
             │ Discovery Plane   │  │ Execution Plane   │
             │ lexical/vector    │  │ PEP → budget     │
             │ tools.search      │  │ approval → task  │
             │ tools.describe    │  │ retry/circuit    │
             └─────────────┬────┘  └──┬────────────────┘
                           │          │
       ┌───────────────────▼──────────▼────────────────────┐
       │ Built-in | External MCP | DB | Reports | Desktop   │
       │ Browser | Workflow API | Skills/Toolkit adapters   │
       └───────────────────┬────────────────────────────────┘
                           │
                    ┌──────▼──────┐
                    │ Trace / Audit │
                    │ Metrics / CAS │
                    └─────────────┘
```

设计原则：

- Catalog 是事实源，模型看到的工具列表是 Catalog 的投影。
- Release 是不可变的，运行必须绑定 release，而不是只绑定工具名。
- Connection 是凭据和授权事实源，服务配置不再直接等同于用户账户。
- Policy 是唯一执行前门，协议、聊天、工作流、桌面和测试都只能适配它。
- Discovery 只负责召回和解释，不拥有执行权限。
- Execution 负责超时、重试、任务、取消、幂等和结果校验。
- Trace/Audit 记录“谁在什么策略决定下使用了哪个版本和哪个连接”。

## 7. 产品体验改造

当前工具库按“工具服务 / 数据连接 / 连接与凭据”分 Tab，适合管理者，但不够适合普通用户。建议改成四个一级视图：

### 7.1 工具目录

首页默认显示任务导向的工具包卡片：数据分析、文档处理、浏览器自动化、通知、业务系统等。每张卡显示：

- 能解决什么问题；
- 可用工具数量和风险等级；
- 是否已连接、是否需要授权；
- 最近成功率/P95；
- 可用范围（个人、单位、当前设备）；
- “试用”“连接”“查看工具”“查看运行记录”操作。

增加搜索、标签、风险、执行位置、是否需要审批、连接状态、是否支持工作流等筛选。

### 7.2 工具详情

详情页按“用途 -> 示例 -> 所需连接 -> 权限与风险 -> 输入 -> 输出 -> 版本 -> 调用记录”排序。技术字段如完整内部名、JSON Schema、协议版本放在高级区，但不能缺失。

### 7.3 连接中心

连接中心显示所有账户和设备：状态、归属、Scope、过期时间、最后刷新、最后错误、可使用的工具包。用户能一键重新授权、撤销、禁用和测试。

### 7.4 运行与治理

增加工具运营页：健康矩阵、失败排行、慢调用排行、凭据过期、Schema 变更、版本依赖、策略拒绝、审批等待和最近调用。管理员可从一条失败调用直接跳到 release diff、连接状态和 Trace。

### 7.5 工作流与聊天的统一反馈

- 聊天中工具未连接：展示“需要连接 XXX”按钮，而不是只返回工具不可用。
- 工具需要审批：展示具体动作、目标、数据范围和风险；审批后继续原任务。
- 工具长任务：显示任务进度、取消、重试和结果产物。
- 工具版本过期：显示“工作流依赖旧版本，重新发布前不会自动切换”。
- 调用失败：区分用户可修复（授权/输入）、系统可重试（网络/限流）和需要管理员处理（服务/契约）。

## 8. 分阶段实施路线

### 阶段 0：基线与收口（1 周）

目标：不改变用户能力，先建立事实。

- 盘点所有工具定义、执行入口、来源、风险、审批、Schema 和测试。
- 输出工具契约完整性报告：缺 `output_schema`、缺能力、缺副作用声明、缺示例、缺测试的工具清单。
- 为所有执行入口增加 `source`、`trace_id`、`run_id`、`step_id`。
- 把现有直接调用路径接入临时统一 Policy Adapter；旧路径进入告警而不是静默绕过。
- 增加 P0 安全回归和 10~20 条真实任务工具选择基线。

验收：所有工具调用都能查到 actor、tool、source、policy decision；旧入口绕过数为 0 或有明确迁移告警。

### 阶段 1：契约与 Release（2~3 周）

- 创建 release/item/alias 表和迁移。
- 改造 `refreshMcpTools()` 为候选快照、差异计算、激活/回滚。
- 默认校验工具名、输入 Schema、输出 Schema、annotations 和大小限制。
- 运行时记录 release digest；工作流依赖做 stale 检测。
- 保留 `mcp_tool_cache` 作为兼容读模型，逐步改为 active release 的投影。
- 通过 conformance 和契约测试支持标准 MCP 双版本。

验收：刷新失败不影响旧版本；Schema 破坏性变化可阻断发布；任意运行可还原当时工具定义。

### 阶段 2：统一 PEP 与连接中心（3~5 周）

- 实现 `tool-policy-engine`，收口聊天、工作流、MCP RPC、单步测试和桌面连接器。
- 新增 connector definition、connection account、binding 数据模型。
- API Key/数据库连接先迁移到 Connection Account 兼容层，不强迫一次性重做所有 UI。
- 加入连接状态机、测试连接、撤销、轮换、过期告警。
- 对高风险和写工具强制 policy decision + approval 记录。

验收：同一个工具从不同入口得到相同的策略结果；凭据不出现在 Schema、模型上下文和日志；过期连接不会静默失败。

### 阶段 3：渐进式发现与任务执行（4~6 周）

- 建立持久化工具索引和 release 级向量/词法/规则索引。
- 增加 `tools.search`、`tools.describe`，按任务动态加载完整 Schema。
- MCP/内部工具增加 task adapter、取消、输入请求、进度和结果引用。
- 增加连接级限流、工具级熔断、重试分类、幂等键和并发舱壁。
- 聊天、工作流和桌面统一显示任务状态。

验收：工具目录达到 100~500 个时，模型上下文中的工具 Schema 仍受预算控制；长任务可跨请求恢复；网络抖动不造成重复副作用。

### 阶段 4：运营、评测和供应链（持续迭代）

- 工具评测集、发布门禁、灰度和回滚。
- OpenTelemetry/W3C Trace Context 全链路关联。
- 工具运营指标、租户成本和失败诊断页。
- `pivot.toolkit.json`、签名、来源证明、隐私/文档/支持信息。
- 受控工具包市场：内置、组织私有、审核后的第三方三层目录。

## 9. 关键 API 建议

```text
GET    /api/tools/catalog?query=&category=&risk=&connectionStatus=
GET    /api/tools/catalog/:toolRef
GET    /api/tools/catalog/:toolRef/releases
POST   /api/tools/servers/:id/refresh
POST   /api/tools/servers/:id/releases/:releaseId/activate
POST   /api/tools/servers/:id/releases/:releaseId/rollback
GET    /api/tools/servers/:id/releases/:releaseId/diff

GET    /api/connectors
POST   /api/connectors/:slug/authorize
GET    /api/connection-accounts
POST   /api/connection-accounts/:id/refresh
POST   /api/connection-accounts/:id/revoke
POST   /api/connection-accounts/:id/test

POST   /api/tools/search
POST   /api/tools/describe
POST   /api/tools/invoke
GET    /api/tools/tasks/:id
POST   /api/tools/tasks/:id/cancel
POST   /api/tools/tasks/:id/input

GET    /api/tools/operations
GET    /api/tools/operations/:id/trace
GET    /api/tools/health
GET    /api/tools/evaluations
POST   /api/tools/evaluations/run
```

`toolRef` 不使用易变的工具显示名，建议为：

```text
{ toolkitSlug, serverId, toolName, releaseId, definitionDigest }
```

## 10. 量化验收指标

### 安全与治理

- 所有工具执行入口统一经过 PEP：100%。
- 高风险/写工具无 policy decision 或审批记录：0。
- 敏感凭据出现在模型上下文、日志和工具 Schema：0。
- 外部工具 Schema 校验覆盖率：100%。
- 共享数据库工具越权、表/字段白名单绕过、SSRF 回归：0。

### 可靠性

- 工具目录刷新失败时旧版本可继续服务：100%。
- 工具版本变更可差异比较、回滚和追溯：100%。
- 只读工具重试不产生重复副作用；写工具无幂等键时默认不自动重试。
- 长任务取消成功率、恢复成功率和任务过期清理率纳入 SLO。
- 关键工具按工具包统计成功率、P95、429、5xx、超时和熔断。

### 发现质量

- Top-1/Top-3 工具选择准确率按业务任务集持续评测。
- 工具搜索平均加载 Schema 数和 Token 成本有明确上限。
- 无授权/无连接/高风险工具不会进入可执行候选，但可以返回可解释的连接或审批 CTA。

### 产品体验

- 新用户从工具目录到完成首次授权/测试调用不超过 5 个主要交互步骤。
- 连接过期、工具停用、Schema 破坏性升级、调用失败均能给出可行动提示。
- 管理员可在一个页面看到版本、依赖、健康、策略和调用 Trace。

## 11. 风险与取舍

### 不建议立即做的事情

- 不建议先扩充大量第三方 MCP 数量；目录和凭据生命周期未补齐前，工具数量只会放大治理成本。
- 不建议把所有工具一次性改造成 `tools.search` 元工具；应保留显式白名单和工作流固定绑定，逐步灰度。
- 不建议立刻替换现有 Agent runtime；应优先把 MCP 任务适配到现有运行、租约和检查点能力。
- 不建议把第三方工具的风险完全相信 MCP annotations；annotations 是声明性提示，最终风险必须由 Pivot Policy 和连接范围决定。

### 主要实施风险

| 风险 | 表现 | 缓解 |
|---|---|---|
| Release 迁移影响历史工作流 | 工具名仍在但 Schema 已变化 | 保存 release/digest，迁移期保留兼容投影，发布前做 stale 检查 |
| 统一 PEP 引入延迟 | 每次调用增加多次数据库查询 | 请求级 policy snapshot、短 TTL 缓存、异步审计但不异步放行 |
| OAuth 接入复杂 | 多供应商刷新和撤销差异大 | 先抽象 provider adapter，优先支持 1~2 个高价值连接器 |
| 动态 Tool Search 误选 | 语义召回把相似工具混在一起 | 权限过滤先于召回，规则/标签作为硬约束，低置信度只展示不执行 |
| 日志过度记录敏感数据 | 诊断变好但数据泄露风险上升 | digest/ref/CAS、字段级脱敏、默认不存原文、权限分级查看 |
| 协议版本快速变化 | 客户端兼容和会话语义变动 | 双栈适配、conformance 矩阵、协议能力协商、不要把 session 当业务状态 |

## 12. 最终建议

按投入产出排序，建议立即启动以下五件事：

1. **统一执行前策略入口**：先解决“不同入口是否同一套安全规则”。
2. **工具 Release 快照和 Schema 差异**：先解决“运行时到底使用哪一版工具”。
3. **连接账户抽象**：先解决“谁授权、授权了什么、如何刷新和撤销”。
4. **渐进式工具搜索**：先解决“工具多了以后模型和用户如何找到正确工具”。
5. **工具运营与评测**：先解决“怎么证明工具可靠、选择正确、升级不会回归”。

完成这五项后，Pivot 的工具库将从“可以接工具”升级为“可以安全地管理、发现、运行、评测和持续交付工具”。这条路线能最大化复用现有 Agent runtime、能力包、工作流、Trace、桌面连接器和安全治理投资，同时避免因一次性重构造成现有工具和工作流不可用。

## 附录 A：本次代码检查证据

- 工具目录聚合与契约投影：`server/services/agent-tool-catalog.js`。
- 能力登记与工具前缀映射：`server/services/agent-tool-capabilities.js`、`server/services/agent-capability-registry.js`。
- 工具契约归一化：`server/services/agent-contracts.js`。
- 外部 MCP 客户端、缓存刷新、调用和日志：`server/services/mcp-client.js`。
- MCP 路由、工具列表、工具调用和 RPC：`server/routes/mcp/index.js`、`server/routes/mcp/configuration-routes.js`、`server/routes/mcp/management-routes.js`。
- 工具语义索引：`server/services/mcp-tool-catalog-index.js`。
- 工具路由：`server/services/semantic-router.js`、`server/services/chat-route-config.js`。
- 工具库 UI：`client/chat/partials/workspaces/mcp.html`、`client/chat/mcp-workbench-main.js`、`client/chat/mcp-workbench-common.js`。
- MCP 相关数据模型：`server/db/schema/tables-agent.js`、`server/db/schema/indexes.js`。
- 相关专项测试：`tests/agent-capability-catalog.test.js`、`tests/tool-library-hardening.test.js`、`tests/mcp-tool-presentation.test.js`、`tests/mcp-client-long-connection.test.js`、`tests/chat-adaptive-routing.test.js`、`tests/desktop-local-mcp-connector.test.js`、`tests/agent-tool-scheduler.test.js`。

## 附录 B：实施时的 Definition of Done

一个工具包只有同时满足以下条件，才算“产品级可发布”：

- 有唯一 slug、版本、发布者、文档、隐私说明和支持责任人。
- 所有工具有稳定名称、标题、描述、输入 Schema、输出 Schema、示例和风险契约。
- 已通过协议、Schema、安全、超时、取消、错误和数据脱敏测试。
- 所需连接、Scope、数据范围和审批条件可在 UI 中解释。
- 已产生不可变 release，并完成依赖工作流检查。
- 已经过灰度调用，健康指标达到阈值。
- 执行可追溯到用户、租户、连接账户、策略决定、Trace 和工具版本。
- 发现失败、连接失效、版本回滚和工具停用都有用户可理解的处理路径。

## 附录 C：实施完成记录（2026-09-20）

本方案的开发任务已在当前工作区完成，原有工具和工作流保留兼容投影，不要求一次性迁移既有 MCP 服务。

发布版本：`v0.1.157`；完整发布说明见 [v0.1.157 工具库产品化控制面与受控连接发布记录](docs/releases/v0.1.157-工具库产品化控制面与受控连接.md)。

| 方案能力 | 已落地实现 |
|---|---|
| 工具契约与 Release | 新增不可变目录版本、目录项、别名和兼容性差异；破坏性变更进入审核，激活后才投影回兼容缓存；工作流发布冻结 release/digest 并在目录更新后标记 stale。 |
| 统一策略执行点 | 聊天、Agent、DAG、MCP RPC、OpenAI 兼容入口、手动测试和数据分析调用均接入统一 PEP；输入、输出、连接、能力、审批、版本与策略决定均可审计。 |
| 连接器与凭据 | 新增连接器、连接账户与工具绑定模型；OAuth 2.0 授权码 + PKCE、刷新、撤销、过期状态、实际授权跳转和只读连接测试均已实现，令牌仅以加密形式保留在服务端。 |
| 渐进式发现 | 增加 `tools.search`、`tools.describe`、`tools.execute`，并将目录向量、别名、标签与示例按 release 持久化；目录 UI 可搜索并按需展示冻结的输入/输出契约。 |
| 任务与可靠性 | 新增持久化工具任务、取消与输入接口；按工具/连接实施并发舱壁、速率限制、熔断、半开探测及仅对安全调用的重试。 |
| 可观测性与运营 | 调用事件关联 actor、连接、release、策略、Trace 与安全摘要；工具库新增运行成功率、延迟、失败与审批概览。 |
| 评测与供应链 | 新增工具评测集、用例、运行与 Top-1/Top-3 指标；提供 `pivot.toolkit` manifest Schema、签名密钥、验证、审核和评测门禁。 |
| 协议兼容 | MCP HTTP 同时覆盖既有会话模式与 2026 无状态路由头，并传播 W3C Trace Context。 |

验收证据：`npm run check` 通过；最终 `npm run test:all` 通过 **662/662**（含 PostgreSQL 隔离迁移、工具目录控制面、OAuth/PKCE、MCP 无状态协议、熔断重试、工具包签名和现有回归组）。
