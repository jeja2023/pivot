## [v0.1.75] - 2026-09-04

### 数据清洗工作台多 Tab 高密度重构、谱系数据表格化与数据集编辑管理

- **数据分析数据总览支持行内编辑**：在数据总览表格操作列新增「编辑」按钮（规范尺寸 24px），操作列宽度扩展至 220px，支持修改数据集名称与原始文件名/备注，提交后无缝联动全局状态与选择器。
- **编辑数据集弹窗界面去重与交互统一**：新增「编辑数据集」模态弹窗，清晰展示行数/列数、数据类型及创建时间元数据卡片；根据用户体验反馈去除右上角多余的关闭按钮，由右下角「取消」与「保存修改」统一承载，避免操作重复。
- **数据清洗工作台多 Tab 分步流线型架构**：重构为 4 个职责清晰、分步推进的现代化 Tab 选项卡（`① 清洗规则编排`、`② 数据质量体检`、`③ 影响演算预览`、`④ 清洗记录谱系`），单行胶囊徽章与分段控制器设计，大幅降低认知负荷。
- **清洗记录谱系全面升级为高密度数据表格**：彻底替换纵向卡片瀑布流，采用专业数据表格呈现衍生谱系；配备居中等宽「序号」列、版本胶囊（v1/v2...）、派生数据集名称（防溢出省略与 Tooltip）、行/列数前后变化箭头、规则数量徽标、清洗执行时间以及行内动作按钮组（「查看数据集」「载入规则」「再次生成」）。
- **数据清洗工作台全局高密度紧凑布局与视觉对齐**：全面对齐透视视图与查询视图现代紧凑风格；头部内边距与图标缩减 50%、按钮统一为 20-28px 紧凑规格、输入框统一为 24-28px 规范、字体大小精细化为 0.70-0.86rem，彻底消除冷启动与空状态大块冗余留白，暗黑模式（Dark Mode）深度兼容。
- **非破坏性清洗与版本化演进闭环**：清洗执行永远基于原始数据集生成新的派生数据集，原始数据只读安全保留；服务端提供完整的规则校验、执行推演、记录持久化及重放执行链路。
- **工程治理与代码基线严格达标**：核心文件行数均维持在严格基线以内（`routes/apps/index.js` 986 行、`view.js` 990 行、`events.js` 993 行），0 裸 HTML 注入风险，0 新增全局变量污染，全套单元测试与契约测试 100% 通过。

详细发布记录见 [v0.1.75 发布记录](docs/releases/v0.1.75-数据清洗工作台多Tab高密度重构与数据集编辑管理.md)。

## [v0.1.74] - 2026-09-02

### 个人自主进化、受控组织共享与本机浏览器会话访问闭环

- **个人自主进化闭环上线**：新增可恢复、去重、限流与退避的学习队列；从成功任务、恢复路径、用户修正和“记住这个方法”生成记忆、个人 Skill 或工作流草稿。低风险只读 Skill 经真实版本验证后可自动启用，后续相似任务会自动匹配并在结果中说明已使用个人经验。
- **组织受控共享闭环上线**：个人经验申请共享时生成脱敏的独立候选制品，不复用个人对话、记忆、文件路径、指令或名称；组织候选必须经历同租户管理员审批、真实验证、组织签名、10% 灰度发布、质量监控、熔断与回滚，通用发布入口无法绕过候选门禁。
- **控制台体验收敛**：Agent 控制台提供“个人档案、记忆与隐私、经验沉淀、技能管理”四个日常页签；“我的记忆”轻量面板支持查看近期记忆、编辑、暂停、恢复、删除和跳转完整记忆中心；“我的经验”支持确认、编辑、暂停、恢复、撤销、归档和申请共享。
- **本机浏览器访问彻底打通**：浏览器授权保存后桌面连接器立即签名同步设备、浏览器和 Origin 白名单；`mcp.0.browser.*` 稳定出现在普通会话与 Agent 工具目录，不再被本机数据库/文件工具遮蔽。会话中明确请求访问已授权 URL 时会自动路由至本机隔离浏览器，支持默认补全唯一设备/浏览器与非默认端口 Origin。
- **浏览器安全边界强化**：普通会话仅对明确提供 URL 的浏览器访问请求解除服务端审批死锁，实际打开、正文读取、点击和截图仍逐次在桌面端确认；浏览器保持独立 Profile、禁用下载、禁止读取 Cookie/Storage/密码，未授权 Origin、设备或浏览器一律拒绝。
- **工具单步测试体验调优**：为 MCP 工具单步测试弹窗的「测试入参 (JSON)」输入框配置紧凑等宽代码字体（Consolas/monospace）与 0.78rem 字号，消除字体过大突兀感，提升 JSON 参数阅读与编写体验。

详细发布记录见 [v0.1.74 发布记录](docs/releases/v0.1.74-个人自主进化与本机浏览器会话访问闭环.md)。

## [v0.1.73] - 2026-09-02

### 本机文件目录授权文案统一、表格列宽调优与全屏自适应撑满

- **工作台数据表格列宽与排版深度调优**：
  - **我的记忆表格**：增加「序号」列（56px 居中等宽数字呈现），将「类型」列宽扩充至 120px，类别徽标排版更加舒适舒展；
  - **任务活动表格**：「任务目标」列宽精确减少 3 字符（`width: calc(22% - 36px)`），「状态」列宽精确增加 3 字符（由 80px 扩展至 116px），运行状态徽标展示更充裕；
  - **待办中心表格**：「事项名称」列宽由 140px 增大至 200px（`min-width: 180px`），大幅提升待办标题可视空间；
  - **工具可靠性与反馈闭环表格**：序号列宽统一增大至 56px，反馈明细「运行 ID」列与「时间」列列宽增大至 160px，默认单页数据条数统一规范为全局标准的 10 条/页。
- **全屏自适应撑满与页面空白彻底消除**：
  - **运行质量（工具可靠性 & 反馈闭环）**：打通质量视图顶层布局容器 `.agent-cp-quality-layout` 与 `.agent-cp-quality-panel` 全高伸缩链路，解除反馈列表 280px 旧高度上限限制，配置表格与空状态容器最小高度为 440px-500px+，空状态垂直居中，彻底消除底部大块空白；
  - **评测概览**：评测集双栏布局区域高度增大至 460px-640px（空状态提升至 420px），大幅扩充评测用例可视空间；
  - **通知设置**：打通 `.agent-cp-subview-pane` 全高链路，为通知渠道表格与空状态容器设定 `flex: 1 1 auto` 与 380px+ 最小高度，彻底消除下方留白。
- **工作流工具栏操作状态严格对齐**：修复工作流资产中心返回时未严格按选项卡区分按钮显隐的问题，锁定在「工作流」标签页下只展示「新建工作流」并隐藏「新建计划任务」，计划任务创建统一收敛至表格各行「计划」按钮与「计划任务」专属选项卡。
- **授权本机文件目录文案统一**：将本机授权中心选项卡、工具库“我的电脑”动作卡片及状态提示中的「授权本机报表目录」统一重命名为「授权本机文件目录」，同步对齐描述文本与上下文引导提示，准确表达只读扫描和文件检索能力边界。
- **安全凭据操作按钮紧凑化**：将「受控安全凭据」操作栏中的「新建安全凭据」与「管理凭据抽屉」按钮高度统一固定为 24px，消除全局默认样式的粗大突兀感，与工具库操作栏风格完全协调。

详细发布记录见 [v0.1.73 发布记录](docs/releases/v0.1.73-本机文件目录授权文案统一与安全凭据按钮紧凑化.md)。

## [v0.1.72] - 2026-09-02

### 智能助手四 Tab 独立架构、经验沉淀表格规范化与全局视觉统一

- **智能助手四 Tab 解耦**：将原先混合的区域彻底重构拆分为 4 个职责分明的独立子 Tab（个人档案、记忆与隐私、经验沉淀、技能管理），消除大表单堆叠与信息过载。
- **经验沉淀数据表格全面规范化**：重构为 8 列独立分列显示（序号、经验名称、类别、状态、使用次数、成功率、说明、操作），单元格全包围完整边框，锁定 32px 全局标准行高与 22px 紧凑操作按钮，完全接入全局分页控件。
- **「我的记忆」展示面板紧凑精致化**：数据表格行高精简至 28px，操作列按钮紧凑化至 20px，微徽标及面板操作按钮同步收敛，空间利用率大幅提升。
- **管理后台页面标题与说明像素级对齐**：系统监控页移除特例 padding 覆写与自定义字号/外边距，长期记忆页移除标题覆盖，完全回归全局 `padding: 24px`、主标题 `1.08rem`、说明 `0.76rem` 与下边距 `14px` 标准，彻底消除 Tab 切换时的位置跳动。
- **全局参数卡片图标精简**：去除 8 个参数分区卡片名称前的 SVG 图标盒子，保留清晰的主副标题与说明文本，整体视觉更加清爽一致。

详细发布记录见 [v0.1.72 发布记录](docs/releases/v0.1.72-智能助手四Tab解耦经验沉淀规范化与全局样式统一.md)。

## [v0.1.71] - 2026-09-01

### 智能助手双栏工作台、技能编辑面板与工作流交互深度优化

- **我的智能助手双栏重构**：将个人档案与记忆治理重构为并排双栏 `.agent-assistant-workbench-grid` 工作台结构，彻底消除页面顶部大块空白间隔，底部通知全面升级为即时 Toast 提示。
- **技能编辑面板饱满化与无滚动条适配**：扩充 `SKILL.md` 代码编辑区高度至 410px，左右栏调整为 0.75:1.25 黄金比例并强制 100% 满宽填充，完美消除了底部大块空白，且总高收敛在视口内，保证首屏呈现且绝不触发页面级垂直滚动条。
- **子 Tab 导航组件样式统一**：「运行质量」与「技能与助手」Tab 栏配置 `align-self: flex-start` 紧凑包裹，消除右侧拉伸空白槽，统一 26px 高度、内边距与翡翠绿激活态规范。
- **工作流操作收敛**：移除工作流页面工具栏顶部的冗余「新建计划」按钮，统一收敛至数据表操作列的独立「计划」按钮。

详细发布记录见 [v0.1.71 发布记录](docs/releases/v0.1.71-智能助手双栏工作台技能面板与工作流交互深度优化.md)。

## [v0.1.70] - 2026-09-01

### 管理后台全局参数页面布局紧凑化与按钮交互规范化

- **页面标题统一**：移除自定义外框与独立装饰，全局参数标题与操作区完全恢复为管理后台通用的 `.settings-page-head` 标准结构，与其它配置页面保持一致。
- **布局深度紧凑化**：卡片网格间距缩减至 10px，卡片内边距调整为 10px 12px，表单输入框高度统一至 28px，字段间距调整为 5-6px，信息密度适中、视觉舒展。
- **按钮配色规范化**：“生成并启用新密钥”按钮统一使用翡翠绿主操作样式，“加密保存并启用”按钮调整为白色标准次级按钮。
- **样式入口补齐**：在 `chat.css` 中正式引入 `admin-global-params.css`，彻底解决未加载专属样式导致页面退化为单列 760px 及右侧大面积留白的问题。

详细发布记录见 [v0.1.70 发布记录](docs/releases/v0.1.70-管理后台全局参数页面布局紧凑化与按钮交互规范化.md)。

## [v0.1.69] - 2026-09-01

### Agent 控制台导航重排与命名收敛

- **配置优先的首屏**：将“技能与助手”移至 Agent 控制台首位，并作为首次进入时的默认页面；已使用用户仍保留上次访问页面。
- **名称面向任务**：将“统一收件箱”改为“待办中心”、“持续目标”改为“自动目标”、“通知渠道”改为“通知设置”、“质量与可靠性”改为“运行质量”。
- **上下文文案同步**：页面标题、刷新动作、任务详情返回标签和帮助说明统一采用新的用户可理解名称。

详细发布记录见 [v0.1.69 发布记录](docs/releases/v0.1.69-Agent控制台导航重排与命名收敛.md)。

## [v0.1.68] - 2026-09-01

### “我的智能助手”页面布局与样式重构

- **工作台式信息层级**：新增轻量设置引导卡，将个人化设置从大表单改为身份与沟通、工作方式、高级偏好三层结构；核心设置一目可见。
- **高级配置按需展开**：JSON 扩展偏好调整为默认折叠的“高级偏好设置”，减少日常页面噪声，同时保留完整配置能力。
- **记忆与隐私重排**：自动记忆、禁止保存类别和安全保障分为三块卡片；记忆类别改为更易阅读的选择列表，敏感信息的永久禁止状态更直观。
- **跨屏与深色模式优化**：宽屏下收紧字段比例、提升文本区可读性；中窄屏自动转为双列或单列，并补齐深色主题的层级和状态色。

详细发布记录见 [v0.1.68 发布记录](docs/releases/v0.1.68-我的智能助手布局与样式重构.md)。

## [v0.1.67] - 2026-09-01

### Agent 控制台日常入口收敛

- **能力页重构为“技能与助手”**：将原“能力与治理”更名为“技能与助手”，日常只显示“我的智能助手”和“技能”两个入口。
- **档案与记忆合并**：个人 Agent 档案与记忆/隐私策略在同一“我的智能助手”页呈现，避免用户在多个治理页之间切换。
- **高级功能默认收起**：常驻实例和进化提议保留服务端能力与代码，但从日常界面隐藏，也不再在进入页面时加载；运行资源包继续由内部开关控制。

详细发布记录见 [v0.1.67 发布记录](docs/releases/v0.1.67-Agent控制台日常入口收敛.md)。

## [v0.1.66] - 2026-09-01

### 运行资源包控制台默认收起

- **降低日常复杂度**：从 Agent“能力与治理”主界面隐藏“运行资源包”入口，不再在常规页面加载中请求或展示资源包列表。
- **保留内部安全基础**：资源包下载、内网来源白名单和 SHA-256 校验实现继续保留，供未来完成 Chromium/Python/数据引擎的启用、切换和回滚闭环后复用。
- **受控临时开放**：仅当 `PIVOT_RUNTIME_PACKS_CONSOLE_ENABLED=true` 且系统最高管理员登录时，才显示控制台并允许查看/同步；默认关闭时后端同步接口同样拒绝访问。

详细发布记录见 [v0.1.66 发布记录](docs/releases/v0.1.66-运行资源包控制台默认收起.md)。

## [v0.1.65] - 2026-09-01

### 统一桌面连接器：本机多浏览器自动化

- **新增 local_browser 授权**：桌面客户端可为 Chrome、Edge、Brave、Chromium、Firefox 或兼容的自定义浏览器可执行文件建立本机授权；服务端仅保存浏览器标识、内核和允许站点，不保存可执行路径、Cookie 或密码。
- **受控本机执行**：新增 `browser.open`、`browser.inspect`、`browser.click`、`browser.screenshot` 连接器任务。浏览器在用户电脑上的 Pivot 隔离 Profile 中运行；登录、点击与截图均要求本机确认，下载默认拒绝。
- **站点与设备边界**：授权时必须录入精确 HTTP/HTTPS Origin 白名单，服务端做 URL/端口契约校验，桌面端在实际用户网络中复验 DNS 与网络策略；任务必须指定在线设备和已授权浏览器。
- **连接器协议扩展**：本机连接器 Grant 增加加密无关的浏览器元数据，支持多浏览器并存；MCP 工具目录动态向 Agent 暴露当前设备允许的浏览器标识，并映射到最小 `network.browser_visit` 能力。

详细发布记录见 [v0.1.65 发布记录](docs/releases/v0.1.65-统一桌面连接器本机多浏览器自动化.md)。

## [v0.1.64] - 2026-09-01

### 组织 Skill 签名密钥托管配置

- **无需手工编辑私钥环境变量**：系统最高管理员可在“全局参数”生成 RSA 组织签名密钥，或一次性导入已有 PEM；私钥使用现有 AES-GCM 密钥加密后保存到服务端设置库，永不返回浏览器、写入日志或写入 Skill 文件。
- **密钥生命周期完整化**：支持生成即启用、密钥轮换、导入、停用和活动密钥状态展示。轮换或停用后保留历史公钥，因此已发布版本的不可变签名信封仍能复验。
- **环境接管兼容**：保留 `AGENT_SKILL_ORGANIZATION_PRIVATE_KEY_BASE64` 作为部署平台/密钥管理的优先来源；配置后页面改为只读，格式无效时 fail-closed，不会静默改用数据库密钥。
- **共享发布打通**：管理员一键发布优先使用托管密钥；无环境变量也可自动完成批准、签名、复验和组织/团队发布。普通用户仍无法自签名。

详细发布记录见 [v0.1.64 发布记录](docs/releases/v0.1.64-组织Skill签名密钥托管配置.md)。

## [v0.1.63] - 2026-09-01

### 管理员一键共享发布

- **一键完成共享发布**：管理员点击发布到团队或组织时，系统自动执行批准、服务端组织签名、完整复验和发布，不再要求先单独点击“批准共享版本”。
- **权限与审计不放宽**：自动批准仅适用于当前租户管理员；普通用户仍不能自签名或绕过共享发布权限。发布审计会明确标记自动批准和所使用的组织密钥标识。
- **界面与帮助同步**：治理窗口只保留共享发布操作，并在成功提示中说明是否自动完成组织签名。

详细发布记录见 [v0.1.63 发布记录](docs/releases/v0.1.63-管理员一键共享发布.md)。

## [v0.1.62] - 2026-09-01

### Skill 个人创作与组织共享流程简化

- **个人路径免签名**：个人可直接用单文件技能配方创建、校验和发布个人版本；不再要求个人持有、上传或理解组织签名密钥。
- **共享路径集中审批**：团队与组织范围发布必须先由本租户管理员批准。服务器使用仅部署端可见的组织私钥写入不可变签名信封，随后重新执行清单、供应链、声明式检查、沙箱和评测门禁；缺少批准或签名一律拒绝共享发布。
- **密钥边界收紧**：批准接口不再接收客户端提交的私钥或密钥标识；组织私钥只从服务器环境/密钥管理注入，公钥用于复验共享版本与兼容技能包。
- **兼容包复验闭环**：修复分离式 `.skill.zip` 签名在导入后未随版本保存的问题，版本验证会复用原始包摘要和签名信封，不再出现“导入通过、验证失败”。
- **共享投影与租户校验修复**：同一个人版本发布到个人和组织范围时按权威 owner key 生成独立目录投影，避免主键冲突；直接携带租户上下文的管理员也可完成组织范围的访问主体判定。
- **生产配置与说明对齐**：PEP 已按当前无存量 Skill 的生产状态直接启用 `enforce`；Capability Worker 仍保持默认关闭，唯一待办是登记已审计内网镜像、固定入口及资源上限。

详细发布记录见 [v0.1.62 发布记录](docs/releases/v0.1.62-Skill个人创作与组织共享流程简化.md)。

## [v0.1.61] - 2026-09-01

### Agent 控制面表格按钮交互规范化与持续目标存储修复

- **持续目标 PostgreSQL 参数类型推断与软删除展示修复**：修复了 PostgreSQL 参数化 `CASE WHEN` 语句中对 `paused_at`, `next_run_at`, `ended_at`, `created_at`, `updated_at` 等带时区时间戳字段推断为 `text` 导致的 `500 Internal Server Error`，显式补充 `?::timestamptz` 类型转换；在 `listAgentGoals` 查询中引入默认 `status != 'deleted'` 过滤条件，彻底修复前端删除后数据仍残留展示的问题。
- **持续目标与常驻实例表格操作列与空状态规范化**：持续目标表格操作列宽度拓宽至 `170px`，彻底解决暂停/恢复、编辑、删除 3 个按钮换行与拥挤；常驻实例空状态引入标准服务器栈矢量图标，表格列宽对齐优化。
- **控制台顶部操作栏与下拉框像素级水平对齐**：为 `.agent-harness-inline-actions` 内的下拉框与按钮统一施加 `30px` 绝对高度限制与 `margin: 0 !important`，消除浏览器原生 User Agent 边距与基线偏移，实现 100% 水平对齐。
- **表单操作与卡片内部按钮尺寸全局统一**：将「添加渠道」、「保存治理策略」、「打开记忆清单」等表单按钮收拢为全局标准 `28px` 紧凑尺寸（`btn-xs` 规范、`padding: 0 12px`、`font-size: 0.75rem`、`border-radius: 6px`）；同步规范任务列表顶部「新建任务」、「删除审计」与工作流列表顶部「新建工作流」按钮。
- **任务与工作流数据表格行高与行内按钮统一**：将工作流表格（`.automation-assets-table`）过高的 48px 行高下调至全局标准的 `36px`；将任务列表与工作流列表行内操作按钮高度统一收缩至标准的 `24px`；任务列表操作列保持 `140px` 紧凑列宽，优化按钮内部 `gap: 4px` 与 `padding: 0 6px`，实现左右边距完美等距对称居中。
- **监控端点个人视图隐藏与质量大屏 Tab 切换**：优化监控大屏在个人工作区视图下隐藏模型端点卡片；完善自动化页面契约测试，全量静态扫描与治理指标 100% 达标。

详细发布记录见 [v0.1.61 发布记录](docs/releases/v0.1.61-Agent控制面表格按钮交互规范化与持续目标存储修复.md)。

## [v0.1.60] - 2026-09-01

### Agent 技能、文档交付与桌面连接器安全收口

- **Skill 安全治理与创作入口收敛**：`SKILL.md` 成为前端个人 Skill 草稿的唯一创作入口；补齐 Frontmatter 预览、版本差异、能力/工具扩权提示、Release ACL 管理和管理员治理观测。PEP 支持 `shadow` / `enforce` 两种上线模式，并持续记录默认拒绝影响面与历史兜底命中。
- **Skill 发布、签名与租户边界加固**：关闭外部 `ownerKey` 与签名验证旁路；签名信封按摘要不可变复用；发布目录、运行时解析与 ACL 判定统一按租户、团队成员、灰度和显式 deny/allow 收口，防止跨租户或投影表旁路。
- **结构化文档与 Artifact 交付链完成**：上线 Document IR、二进制 CAS、不可变 Rendition、一次性下载令牌、DOCX/XLSX/HTML/Markdown 渲染与 CJK PDF fail-closed 自检；公文工作台自动沉淀独立 Artifact 后走服务端统一渲染。CAS 下载前重算实际摘要，交付端采用原子写入、摘要校验和本地已写清单。
- **桌面受控交付与本机连接器升级**：桌面端以设备密钥签名完成设备注册、目录授权、领取、下载和回执；旧网页自报授权的内存 MCP 桥接下线，替换为 PostgreSQL 持久化桌面连接器，支持显式设备选择、任务租约、claim token 和服务重启后的结果重查。旧本机 SQLite/报表只读能力保留在新连接器通路中。
- **Capability Worker 安全框架**：新增默认关闭的 Docker 执行框架，仅接受固定 `image@sha256`、固定入口命令和平台资源限额；容器默认禁网、只读根文件系统、无 GPU、无业务凭据、受 CPU/内存/进程/临时空间/超时约束。未配置已审计内网镜像时明确拒绝执行。
- **字体与封包配置**：加入 Noto Sans SC CJK 字体资产及摘要校验配置，Docker 运行镜像携带字体；桌面封包规则排除无关构建目录并由 Builder 自动筛选生产依赖。

### 未完成与上线前事项

- **Capability Worker 尚未启用**：执行框架已具备，但仍需由运维登记经过审计的内网 `image@sha256`、固定入口命令和资源配额后，才可将 `PIVOT_CAPABILITY_WORKER_ENABLED=true` 投入使用。
- **Windows 安装包构建与校验完成**：`npm run dist:win` 已成功生成 `Pivot Setup 0.1.60.exe`、blockmap、`latest.yml` 和 Windows x64 SHA-256 清单；安装包 SHA-256 为 `e440bc7a2499fe5f7d22f4b3e27be3fcde0dc36ac1255b4f3bed0c74b8a1b093`。
- **PEP 已直接启用 enforce**：当前生产环境没有存量 Skill，已配置 `PIVOT_AGENT_PEP_MODE=enforce`，不再保留 shadow 观察期。

详细发布记录见 [v0.1.60 发布记录](docs/releases/v0.1.60-Agent技能与文档交付安全收口.md)。

## [v0.1.59] - 2026-08-27

### 客户端独占隐身模式与服务端零特征掐断及连接配置中心

- **客户端独占隐身模式与底层连接物理掐断**：服务端接入 `stealthAccessGuard` 顶层门禁中间件，开启后对普通 Web 浏览器及外部网络探测直接物理掐断底层连接（`req.socket.destroy()`），零特征响应（`ERR_EMPTY_RESPONSE`），外部完全无法探知站点存在；官方桌面客户端每次请求底层自动注入 HMAC-SHA256 动态时间戳防重放签名，毫秒级验签放行。
- **管理控制台全局参数一键开关与密钥管理**：管理后台「全局参数」新增独立的「安全与终端隐身」通栏配置卡片，支持一键开关、明文查看密钥、一键复制与超级管理员重新生成密钥；提供开启防锁死警示弹窗与服务端环境变量应急恢复支持（`PIVOT_STEALTH_MODE=false`）；密钥输入框与按钮组实现 28px 像素级垂直居中对齐。
- **企业级客户端服务器连接可视化配置中心**：连接失败页面全面脱敏移除所有目标 URL、IP、端口与协议回显；菜单栏新增「客户端 -> 服务器连接配置...（`Ctrl+,`）」并在错误页提供醒目配置入口；现代化模态弹窗支持 Local/Remote 模式切换、URL 格式校验、带签名连通性探测（`testServerConnection`）及保存热重载；配置持久化至 `userData/user-config.json`，优先级高于打包预置配置。
- **服务端密钥持久化优先级加固与本地开发无感直连**：修复服务端在启动阶段异步 DB 缓存优先级导致的密钥覆盖问题，支持 `.env` 中 `PIVOT_STEALTH_SECRET` 显式固化；客户端在连接本地回路地址（`localhost` / `127.0.0.1` / `::1`）时自动回退读取本地凭据，开发测试无感直连；消除了客户端错误页与重试加载之间的 `ERR_ABORTED (-3)` 竞态冲突。
- **自动化测试与全量门禁保障**：新增 `tests/stealth-mode.test.js` 与 `tests/desktop-server-config.test.js` 8 项测试 100% 通过；592 个 JS 文件语法校验、全局变量规范（297/297）及架构治理指标全部 100% 绿灯。

详细发布记录见 [v0.1.59 发布记录](docs/releases/v0.1.59-客户端独占隐身模式与服务端零特征掐断及连接配置中心.md)。

## [v0.1.58] - 2026-08-27

### 长期记忆表格操作优化与知识图谱作用域隔离及控制台跨视图返回

- **长期记忆表格操作列收拢与弹窗规范化**：移除了长期记忆数据表格中的独立「来源」列，将「来源」操作按钮整合移入最后一列「操作」列中与编辑、合并、删除并列；调整来源追溯弹窗底部关闭按钮靠右对齐排布，符合系统全局弹窗视觉规范。
- **Agent 控制台全子视图分页与收件箱详情自动标记已读**：为统一收件箱、持续目标、审批记录、评测集列表、工具调用样本、自进化提案、常驻实例运维与技能资源包等全部 8 个子视图接入标准紧凑分页控制器；用户在统一收件箱点击「详情」查看待办后自动标记为已读并同步刷新未读角标计数。
- **Agent 控制台跨视图详情返回路径精准还原**：引入返回导航上下文机制（`returnContext`），在统一收件箱与评测中心点击任务运行详情时保存来源页面标识，面包屑动态呈现并支持点击精准返回原选项卡及子视图，不再误跳回任务列表主页面。
- **知识图谱文档范围与全局图谱严格隔离**：列表行操作列「图谱」按钮携带 `docId` 打开专属图谱弹窗，标题动态渲染为 `知识图谱 · 文档名` 并提供「重建本文档图谱」操作；图谱概览、实体列表、关系列表、实体关系地图和图谱问答全链路贯穿文档作用域并在数据库层严格过滤；文档作用域下的实体提及数与关系数仅统计当前文档内部数据，彻底杜绝跨文档混杂。
- **实体与关系校准闭环联动刷新**：实体保存、合并、关系编辑、确认与删除后，自动联动刷新图谱概览指标、实体列表与实体关系地图。
- **自动化测试全绿守护**：新增双文档图谱作用域隔离集成测试与控制台分页/返回契约测试；全套 111 项自动化测试 100% 通过；全库 587 个 JS 文件语法校验通过，架构治理指标 100% 达标。

详细发布记录见 [v0.1.58 发布记录](docs/releases/v0.1.58-长期记忆表格操作优化与知识图谱作用域隔离及控制台跨视图返回.md)。

## [v0.1.57] - 2026-08-27

### 会话全屏展示恢复与长会话防闪烁及工作流发布门禁体验优化

- **会话界面消息记录恢复两侧全屏全宽展示**：移除了 `.message-container > .message` 上强加的 920px 居中宽度与居中外边距限制，恢复标准全屏间距（`padding: 20px 24px 218px`），放宽内容气泡最大宽度至 `min(92%, 960px)`，完美恢复助手消息靠最左、用户消息靠最右的两侧全屏展示效果。
- **生产环境长会话持续死循环闪烁根因彻底修复**：全面推行增量 Keyed DOM 节点复用（Reconciliation），废除滚动重绘时清空容器的破坏性操作；引入程序化滚动保护标志（`isProgrammaticScroll`）与用户上滑主动释放机制，彻底切断 `ResizeObserver` 与滚动事件互锁的 60FPS 死循环；移除 `content-visibility: auto` 消除高度塌陷抖动，并在虚拟化模式下禁用气泡淡入动画防频闪。
- **Web 版 Agent 控制台数据表类型列防截断防护**：将收件箱表格类型列宽度由 76px 放宽至 96px，设置 `text-overflow: clip; overflow: visible; padding: 4px 2px;` 消除 Web 端 CJK 最小字号强制策略引发的省略号截断；同步将任务运行列表类型列放宽至 96px 并添加防截断保护。
- **统一收件箱与持续目标页面底部大块空白消除**：为单卡片视图容器配置 `flex: 1 1 auto; height: 100%; min-height: 0; display: flex; flex-direction: column;` 使卡片自适应延伸至工作区底部；解除收件箱表格 `max-height: 400px` 限制改为自适应占满卡片全高；持续目标列表改为纵向弹性滚动并居中空状态。
- **工作流发布固定评测集 409 门禁拦截修复与交互优化**：后端路由支持 `skipEvaluationGate: true` 与 `fixedEvaluationRequired: false`；全局错误中间件在 4xx 时透传 `code: err.code`；前端发布方法在遇到 409 门禁拦截时提供交互确认弹窗支持一键跳过门禁完成发布；工作流画布顶部工具栏新增「跳过门禁发布」快捷按钮。
- **契约测试全量守护**：扩展 `tests/automation-page-contract.test.js` 至 16 项全绿通过；`tests/chat-session-selection.test.js` 4 项全绿通过；`eslint` 与全局变量扫描 0 报错通过。

详细发布记录见 [v0.1.57 发布记录](docs/releases/v0.1.57-会话全屏展示恢复与长会话防闪烁及工作流发布门禁体验优化.md)。

## [v0.1.56] - 2026-08-27

### 自动化与数据分析界面交互精细化与弹窗组件去重规范

- **自动化中心与 Agent 控制台布局与对齐优化**：将任务页面的「新建任务」按钮调整至「删除审计」左侧（非 Admin 用户无权限时依然右对齐）；将工作流搜索框移至最左端、新建工作流与新建计划按钮移至最右端实现两端对齐；未发布工作流彻底隐藏「计划」按钮；Agent 控制台顶部标题栏去除重复持续目标按钮，面板按钮名称去除加号。
- **评测集与新建任务弹窗去重与紧凑化**：去除新建评测集弹窗右上角与底部重复的关闭按钮，自适应无用例紧凑视图；重构质量与可靠性大屏为左右对称双栏布局，消除多余留白；彻底移除新建任务表单顶部挤占高度的内嵌模板条，将「常用模板」收敛为底部操作按钮并呼出独立的卡片栅格弹窗；新建任务弹窗去右上角关闭按钮，常用任务模板弹窗去底部重复关闭按钮。
- **数据分析数据总览表格全局标准化**：将数据大小与数据范围标签（`范围未知` / `当前范围完整` / `已截断`）改为单行不换行紧凑展示；修复列错位，严格对齐 7 列数据；将表格行高统一为全局标准 `38px`，操作按钮统一为全局紧凑标准 `24px`，单行溢出省略并添加 `title` 浮动提示。
- **契约测试与安全回归全量守护**：扩展 `tests/automation-page-contract.test.js` 至 13 项契约测试；`tests/security-agent.test.js` 58 项安全与功能回归测试全绿通过；`eslint` 与系统检查 0 报错。

详细发布记录见 [v0.1.56 发布记录](docs/releases/v0.1.56-自动化与数据分析界面交互精细化与弹窗组件去重规范.md)。

## [v0.1.55] - 2026-08-27

### 自动化控制台质量大屏紧凑化与统一收件箱穿梭详情修复

- **自动化与 Agent 控制台信息架构解耦与重组**：彻底梳理自动化中心“功能混装”与“入口重复”历史遗留问题，建立任务工作台、工作流编排与 Agent 控制台三大清晰权责边界；将 Agent 控制台全面收敛为「统一收件箱」、「持续目标」、「通知与审批渠道」、「质量与可靠性」和「能力与治理」5 大专业子视图。
- **统一收件箱跨视图穿梭任务详情深度修复**：修复在控制台统一收件箱点击记录「详情」按钮时，因任务主视图处于隐藏状态导致视觉无响应的问题；在 `openAgentRun` 中引入跨视图可见性智能检测与自动平滑切换机制，自动激活任务工作台并展开沉浸式 5-Tab 详情面板，配合异常拦截与轻量 Toast 提示。
- **质量与可靠性大屏全面紧凑化与胶囊化**：重构质量大屏为左右平衡对称双栏布局（左 56% 质量与评测 : 右 44% 反馈与工具可靠性）；将原本全宽铺满的大号统计方块彻底精简为精致的行内紧凑 KPI 徽章/胶囊（评测概览 5 项指标、运维指标 4 项单行卡片、结果反馈 4 项指标胶囊）。
- **质量评测与反馈闭环数据按需联动加载加固**：修复子 Tab 切换时的数据请求断层；在切换或刷新进入「质量与可靠性」时并发按需拉取评测概览、评测集列表、工具调用样本及反馈闭环数据；评测集为空时自适应单栏紧凑空状态卡片（`.is-empty`），消除多余留白。
- **能力与治理大屏全局样式统一与精简**：统一下调技能、运行时资源包、常驻实例、个人档案等卡片内的字体字阶至 0.76rem~0.8rem，紧凑化内边距与排版间距，与系统全局设计语言严格保持一致。

详细发布记录见 [v0.1.55 发布记录](docs/releases/v0.1.55-自动化控制台质量大屏紧凑化与统一收件箱穿梭详情修复.md)。

## [v0.1.54] - 2026-08-27

### 工具库安全凭据收拢与全系统工作区刷新状态持久化

- **工具库与安全凭据体系架构收拢与布局重构**：将工具库工作区精简并重构为「工具服务」、「数据连接」与「连接与凭据」三大专业顶级 Tab，去除冗余的独立治理审计空页面；将治理与审计面板直接内嵌至「工具服务」面板顶部；全面清理 Emoji 符号与冗余加号图标；封装全局安全凭据模态框为独立 IIFE 作用域，确保全局任意工作区均可稳定新建与管理凭据。
- **工作流工作区权责收拢与界面去噪**：从工作流顶部工具栏彻底移除冗余的全局「凭据库」常驻按钮，严格确立凭据集中于工具库管理的权责边界；工作流中仅保留节点属性引用及依赖检查抽屉中的快捷管理入口。
- **全局轻量 Toast 状态提示替代嵌入式横幅**：彻底清理知识库工作台等各页面嵌入式状态横幅与空白占位框，服务加载、网络异常及操作反馈全面统一采用全局轻量 Toast 弹窗通知。
- **Agent 控制台统一收件箱添加序号列**：在 Agent 控制台「统一收件箱」数据表格第一列新增标准的「序号」居中表头与动态行号（`1, 2, 3...`），提升待处理事项阅读与审批体验。
- **全系统工作区与多级子 Tab 刷新状态记忆与精准恢复**：深度排查全系统工作区刷新机制，通过 `sessionStorage` 实现了全系统 9 大工作区及二级子 Tab（工具库各子分类、Agent 控制台各子视图、系统管理各面板、对话活跃会话等）在页面刷新（F5）时的精准记忆与原位恢复。
- **自动化与智能体信息架构（IA）全面解耦与优化重构**：全面消除自动化页面历史“功能混装”与“入口重复”；将自主任务模板升级为新建任务弹窗顶部的「常用模板」快捷推荐条，彻底移除创建器底部 4 组拥挤折叠面板；将工具与结果彻底拆分，工具归位 MCP 工具库，产物统一收敛至任务详情「输出物」Tab；将质量评测与反馈闭环深度并入 Agent 控制台「质量与可靠性」大屏（涵盖运维指标、质量评测中心、工具可靠性与反馈闭环）；将个人偏好档案、常驻实例运维、自进化提案与技能资源深度整合至 Agent 控制台「能力与治理」大屏，确立任务工作台、工作流、Agent 控制台、工具库与系统设置 5 大领域清晰心智闭环。

详细发布记录见 [v0.1.54 发布记录](docs/releases/v0.1.54-工具库安全凭据收拢与全系统工作区刷新状态持久化.md)。

## [v0.1.53] - 2026-08-26

### 自动化工作台架构重构与任务详情沉浸式体验升级

- **自动化工作台 3-Tab 信息架构收敛**：全面梳理自动化中心定位，收敛为「任务」（执行与数据面）、「工作流」（DAG 图形化编排）与「Agent 控制台」（智能体控制与治理面）三大顶级二级 Tab，彻底消除“任务看板”与“任务工作台”的命名混乱与概念重复。
- **多 Tab 切换顶部标题栏绝对零位移**：统一 `.workspace-modal-header`、`.agent-modal-header` 与 `.agent-dag-modal-header` 容器的高度为固定 64px，内边距统一为 12px 20px，单行文本溢出省略，实现无缝切换时零像素跳动。
- **全宽任务列表与沉浸式详情视图切换**：彻底消除列表与详情上下垂直堆叠导致的表格压扁与详情挤压问题；默认 100% 全宽展示 15 列任务运行指标，点击任意记录平滑切入带返回面包屑的全屏详情视图，点击“返回列表”或按 `Esc` 键无缝返回。
- **任务详情 5 大结构化 Sub-Tab 全局响应修复**：在全局事件总线中注入 `[data-agent-run-subtab]` 事件委托，彻底解决“概览、执行步骤、实时日志、输出物、审批与异常”子 Tab 点击无响应的问题。
- **主页大模型会话 Agent 模式详情弹窗关闭机制修复**：补齐全局 `#agent-run-detail-close`、遮罩背景点击及 `Esc` 键监听，精准区分会话浮层模式与自动化工作区内嵌视图，确保随时一键关闭。

详细发布记录见 [v0.1.53 发布记录](docs/releases/v0.1.53-自动化工作台架构重构与任务详情沉浸式体验升级.md)。

## [v0.1.52] - 2026-08-26

### 模型端点运行时监控与智能体长文本生成保底加固

- **模型端点运行时监控实时同步修复**：修复生产环境下模型正常可用但概览页面偶发显示“暂无模型端点运行数据”的时序脱节问题；在 `summarizeModelEndpoints()` 中将实时查询出的活跃模型行数据直接透传至 `getModelEndpointRuntimeStatus(rows)`，解除对后台异步 30 秒轮询更新周期的强依赖，并即时更新内部缓存。
- **Agent 智能体规划与长篇最终回复防截断机制**：引入全局长文本回复最低预算保障机制（`AGENT_ANSWER_MIN_MAX_TOKENS = 4096`），彻底杜绝 Agent 在输出综合性总结大报告时因沿用单步 1200 兜底预算被截断的问题；规划阶段模型若直接生成 `{"action":"final","answer":"…"}` 同样享受最低输出预算，并在输出触碰上限（`finish_reason === 'length'`）时透传截断标记并记录警示日志。
- **前端 DOM API 防御与测试沙箱健壮性加固**：在 `client/chat/agents.js` 的 `syncAutomationPrimaryTabs`、`bindUnifiedAutomationTabs`、`bindAgentWorkbenchShortcuts`、`bindAgentConfigModal` 等函数入口处添加 `typeof document?.querySelectorAll !== 'function'` 防御判断，避免轻量测试沙箱抛出 TypeError；在 `tests/security-helpers.js` 的 `createChatRenderSandbox` 中为 mock `document` 对象补全 `querySelector`、`querySelectorAll`、`getElementById` 等标准 DOM 方法。
- **Agent 工作台选项卡绑定时序优化与 E2E 冒烟测试对齐**：提前自动化工作台选项卡交互绑定至网络请求之前，杜绝慢网络下的点击丢失；完善 E2E 冒烟测试在验证子面板前按选项卡进行视图切换，确保测试断言与真实 UI 行为 100% 对齐。

详细发布记录见 [v0.1.52 发布记录](docs/releases/v0.1.52-模型端点运行时监控与智能体长文本生成保底加固.md)。

## [v0.1.51] - 2026-08-26

### 智能分析二级 Sub-Tab 标签页架构重构与场景化聚焦升级

- **智能分析二级 Sub-Tab 胶囊分段器重构**：在智能分析页面顶部引入优雅的二级子标签页组件（`[ 💬 即时 AI 探索与深度分析 ]` / `[ 📑 全量语义分析任务 ]`），默认聚焦高频的即时问答、SQL 探索与图表生成，完全隐藏低频批处理表单干扰。
- **100% 全宽独立舒展空间**：两个子标签页各自独占 100% 完整全宽，图表与长篇 Markdown 全量汇总报告排版清爽舒展，彻底消除多栏并排时的横向压缩。
- **历史记录智能穿梭与状态解耦**：两个子页面保留独立的数据集选择器；从「历史记录」点击某条分析或全量任务报告时，系统自动智能激活并切换至对应的子标签页展开结果。
- **深度暗黑模式适配**：全面完善 Sub-Tab 胶囊分段器在深色模式下的背景、边框、高亮阴影及悬停反馈。

详细发布记录见 [v0.1.51 发布记录](docs/releases/v0.1.51-智能分析二级SubTab标签页架构重构与场景化聚焦升级.md)。

## [v0.1.50] - 2026-08-26

### 数据分析智能页面左右分栏重构与自适应交互升级

- **智能分析工作台左右双轨分栏重构**：将原本上下堆叠的「即时 AI 探索与图表」与「全量语义分析任务」重构为左右双栏并排布局（左 46% : 右 54%），彻底消除长滚动与上下排版推挤问题。
- **左右双栏数据集独立选择与按需加载解耦**：左栏「即时 AI 探索」与右栏「全量语义分析」分别拥有独立的「分析数据集」选择器；右栏默认保持未选择状态且不自动填充列名，只有当用户显式选择全量数据集时才加载列名和历史批处理任务，避免误触与干扰。
- **全屏双轨交互体验升级**：支持用户在右栏启动长耗时全量批处理任务并查看进度/汇总长报告的同时，在左栏继续无缝进行即时数据提问、SQL 探索与图表生成。
- **响应式断点与暗黑模式适配**：针对 $\ge 1100\text{px}$ 宽屏自动呈现对称双栏，在 $< 1100\text{px}$ 移动端/平板下平滑退化为单列自适应堆叠；全面补齐暗黑模式下的卡片边框、背景及文字对比度。

详细发布记录见 [v0.1.50 发布记录](docs/releases/v0.1.50-数据分析智能页面左右分栏重构与自适应交互升级.md)。

## [v0.1.49] - 2026-08-26

### Linux 容器环境智能体沙箱兼容性加固与全链路推理思考治理

- **Linux 非特权容器与 CI 智能体沙箱降级兼容性加固**：彻底修复在 Docker 镜像非 root 用户（`USER node`）、GitHub Actions Runner 及 Kubernetes Pod 等无法写入 `/sys/fs/cgroup` 的环境下，Skill 静态校验与回归测试因硬编码严格沙箱抛出 `AGENT_CGROUP_UNAVAILABLE` 导致发布流程中断的缺陷；解耦隔离配置并支持 `PIVOT_AGENT_STRICT_ISOLATION` 环境变量控制，自动平滑降级为进程组（`process-group`）沙箱与工作区 Jail 隔离。
- **深度推理模型思考过程全链路治理与极速模式机制**：全面梳理并确立了针对 Qwen3 / QwQ / DeepSeek-R1 等深度推理模型的“默认极速（关闭思考）+ 按需深度思考”架构；统一对话流（`chat_template_kwargs: { enable_thinking: false }` + `/no_think` 软开关双保险）、应用中心（公文写作/数据分析默认关闭思考并自动剥离 `<think>` 标签）、后台批处理（严格防 JSON 干扰与 Token 超预算）以及 OpenAI 代理补全等全链路思考控制逻辑，首字响应延迟大幅缩短 3~8 倍。
- **大文件架构治理基准同步**：在 `scripts/governance_baseline.json` 中将全量语义分析模块 `semantic-analysis.js`（1014 行）纳入受控基线，严格执行大文件“只减不增”的长期治理规范。

详细发布记录见 [v0.1.49 发布记录](docs/releases/v0.1.49-Linux容器环境智能体沙箱兼容性加固与全链路推理思考治理.md)。

## [v0.1.48] - 2026-08-26

### 应用中心大卡片对称居中重构与全栈运行防护对齐

- **应用中心轻量大卡片对称居中布局重构**：将 5 个核心业务应用重构为自适应 Flex 居中对称布局（宽屏上 3 + 下 2 中轴对齐），彻底消除原 3 列网格排布导致的“右下角缺角”与空间不平衡感。
- **工作区视口横纵双向几何绝对居中**：修复全屏工作区模式下基础面板强制占满高度导致卡片贴顶的缺陷，重构 Flex 纵向链路，确保应用卡片组在任意屏幕比例下均严格居中于可视区域。
- **应用中心卡片整卡点击与精简交互**：移除卡片底部冗余的「进入工作台」按钮，支持整张卡片点击无缝直达，配合轻量微悬浮动效，提升 App Tile 的操作流畅度。
- **应用核心能力标签组（Feature Pills）补充**：为 5 个核心应用（公文写作、数据分析、法规查询、文字识别、PDF 工具）补充精炼的业务特性标签，直观展现功能亮点。
- **应用中心报错通知去横幅化与 Toast 规范**：修复状态条因 CSS 优先级覆盖导致顶部插入空横幅的缺陷，将应用中心加载及异常提醒统一收敛至系统级全局 Toast 弹窗通知。
- **数据分析全量语义分析报告全宽排版优化**：修复全量任务报告容器继承聊天气泡最大宽度（860px）而在宽屏下被严重挤压至左侧一半的显示缺陷，全面放开最大宽度约束，实现 100% 自适应全宽舒展展示。
- **全链路暗黑模式适配完善**：补齐应用中心大卡片、徽标、分类标签及数据分析全量语义分析控制框与报告区域在深色模式下的对比度与背景质感。
- **全栈运行防护与环境配置 100% 对齐**：同步 `.env` 与 `.env.example`，完整部署 PostgreSQL 连接池上限（`PG_POOL_MAX=30`）、慢查询提前熔断（`PG_STATEMENT_TIMEOUT_MS=20000`）、接口 20 秒悬挂兜底（`API_REQUEST_WATCHDOG_MS=20000`）、上游流式响应空闲超时守卫（`MODEL_STREAM_IDLE_TIMEOUT_MS=120000`）以及 `UV_THREADPOOL_SIZE=16` 等全套生产防护参数。

详细发布记录见 [v0.1.48 发布记录](docs/releases/v0.1.48-应用中心大卡片对称居中重构与全栈运行防护对齐.md)。

## [v0.1.47] - 2026-08-26

### 智能中枢高并发流式防护与企业级交互体验优化

- **高并发与流式代理 AI 信号量释放守护**：为 OpenAI 代码补全/聊天代理、主会话聊天及应用路由增加 `req.once` / `res.once` 函数类型安全防护与 `response.data.on('close')` 上游断流捕获，彻底杜绝并发槽位泄漏与排队超时死锁。
- **PostgreSQL 长期记忆布尔字段兼容性修复**：修复 `server/services/long-term-memory/index.js` 中 `sensitive` 字段插入数字字面量引发的 PostgreSQL `42804` 类型错误，统一规范为标准 SQL `FALSE` / `TRUE`。
- **长期记忆上下文构建最小预算优化**：优化 `buildLongTermMemoryContextMessage` Token 预算分配，将首条记忆与头尾结构标记的最小基础预算调整为 384 Tokens，确保小窗口预算下至少能完整装配一条高置信度长期记忆。
- **旧数据库快照迁移 Preflight 加固**：在 `legacy-preflight.js` 预检兼容列中补齐 `users`、`sessions`、`messages`、`attachments`、`knowledge_docs` 等表的 `deleted_at` 基础字段，保障历史 SQLite 快照平滑无缝升级。
- **能力与管理路由防御性参数加固**：对 `/capabilities/packages` 等核心能力包路由中 `req.query` 的访问加入可选链防护，彻底杜绝无查询参数请求时的空指针异常。
- **统一收件箱即时预载与纯净边框**：修复原本必须手动点击刷新按钮才能显示信箱数据的缺陷，切换至工作台即可自动拉取最新事项；移除表格列表行前的绿色竖线边框，保持企业级界面的整洁平整。
- **任务执行结果反馈去 Emoji 与矢量 SVG 规范**：任务结果反馈区域全面采用矢量 SVG 图标（`👍 有帮助` / `👌 部分有用` / `👎 需要修正`）替代 Emoji 表情，实现评分与评价胶囊双向智能联动。
- **执行过程全链路指标与标签中文化**：全面汉化执行过程中的工具名称、步骤状态、调用链追踪标签及指标数据（`Token 用量`、`响应延迟`、`计算开销`、`执行耗时`、`置信度`、`匹配得分` 等）。
- **聊天模式切换按钮对称美化**：优化输入框中「普通」/「Agent」切换胶囊按钮的左右对称内边距与居中对齐，消除右侧不对称留白。

详细发布记录见 [v0.1.47 发布记录](docs/releases/v0.1.47-智能中枢高并发流式防护与企业级交互体验优化.md)。

## [v0.1.46] - 2026-08-25

### Agent 工作台交互与结果反馈体验重构

- **Agent 控制面子导航信息架构重构**：将原堆叠混乱的 Agent 工作台页面重构为 4 个清晰独立的子视图（`统一收件箱`、`持续目标`、`通知渠道`、`质量与可靠性`），按需切换并支持状态角标实时统计。
- **纯净企业级视觉与去 Emoji 化**：工作台导航选项卡与控制面全面去除 Emoji 图标，采用现代纯净排版与主题高亮。
- **统一收件箱标准化与即时预载**：统一收件箱全面升级为标准宽幅数据表格，去除多值混合单元格，消除未读绿色竖线边框；修复需手动点击刷新才加载数据的缺陷，进入即时自动拉取。
- **任务执行结果反馈区域重构**：重构评分与评价控件，将通栏变形下拉框重构为紧凑行内微件，支持 `👍 有帮助 (5分)` / `👌 部分有用 (3分)` / `👎 需要修正 (1分)` SVG 胶囊与评分自动双向联动。
- **执行过程全链路指标与标签中文化**：全面汉化工具类型、步骤标题、调用链追踪环节及指标字段（`Token 用量`、`响应延迟`、`计算开销`、`执行耗时`、`置信度`、`匹配得分` 等），消除技术英文泄漏。
- **模态与交互细节清理**：移除新建持续目标弹窗右上角与底部重复的关闭按钮，保持弹窗交互标准统一；优化聊天输入框「普通」/「Agent」模式切换胶囊的居中对齐与对称内边距，消除右侧不对称留白。
- **生产环境 PostgreSQL 语法修复**：修复 `agent_tool_reliability` INSERT 语句中占位符数量与目标字段不匹配（22 位对应 21 列）导致的生产环境 SQL 执行异常。

详细发布记录见 [v0.1.46 发布记录](docs/releases/v0.1.46-Agent工作台交互与结果反馈体验重构.md)。

## [v0.1.45] - 2026-08-25

### 企业级 Agent 生产控制面与全链路个人智能体治理

- **技能与工作流版本化发布治理**：新增 `agent_skill_versions`、`agent_skill_validations`、`agent_skill_releases` 与 `agent_workflow_releases`，支持 Draft/Approved/Published/RolledBack 状态机、多维前置门禁（Manifest、Signature、Sandbox、Evaluations）、金丝雀灰度发布与一键安全回滚。
- **工具可靠性追踪与智能熔断**：实时统计工具成功率、P95 延迟、连续错误计数与超时指标，基于加权评分模型与置信度下限实现异常工具自动降级。
- **多渠道适配与可靠投递 (Outbox)**：原生支持 Webhook、钉钉、企业微信、飞书、Slack 与邮件等多渠道接入；通过事务型 Outbox、受控分块、指数退避与幂等去重保障可靠送达，支持渠道内交互审批与时间常数签名防篡改。
- **事件收件箱与多智能体协同**：引入统一智能体收件箱（`agent_inbox_events`），沉淀任务指派与审批事件；支持多 Agent 任务委派、DAG 执行与群智协作。
- **个人档案多版本与字段级乐观并发**：支持个人智能体画像历史快照还原，通过 `field_versions` 乐观并发控制避免多端冲突覆盖。
- **自主进化闭环与自优化建议**：建立 Agent 自主进化提案规则审查、人工审批与安全回滚机制，基于运行反馈自动生成质量优化建议。
- **持续目标驱动引擎**：支持 Timer/Cron、Webhook、文件与数据库监控等多种触发源，具备防重入触发键、冷却时间与容错熔断。
- **个人数据自决与隐私合规**：支持个人 Agent 完整数据一键导出与安全擦除，增强记忆分类与敏感数据脱敏。
- **生产运维工具与演练体系**：新增高并发演练、PostgreSQL 灾备恢复与加密密钥轮转工具，新增 8 篇全景治理与运维文档（`docs/agent-experience/`）。
- **前端工作台与监控大屏升级**：重构智能体控制面板（`agent-harness.js`），支持发布管理、渠道投递、持续目标与进化审批；新增内存与性能监控看板。

详细发布记录见 [v0.1.45 发布记录](docs/releases/v0.1.45-企业级Agent生产控制面与全链路个人智能体治理.md)。

## [v0.1.44] - 2026-08-24

### 设置页面高并发性能与稳定性优化

- **消除大表全量子查询物化**：优化 `/stats/ops-summary` 与 `/stats/monitor-summary` 中的全量 Token 统计，使用轻量标量子查询直接求和替代 `tokenUsageSubquery` 全表物化，消除数据库连接池耗尽风险。
- **服务端多级短缓存削峰**：为运维概览、趋势统计和模型端点解析添加内存短缓存，有效抵御设置页高频并发请求压力。
- **存储目录大小统计 SWR 异步化**：重构目录尺寸统计，采用 Stale-While-Revalidate 机制在后台异步遍历大目录，杜绝阻塞主事件循环与 HTTP 响应。
- **关键复合索引加速**：为消息表与审计日志表补充 `(user_id, created_at)` 等复合索引，大幅提升历史数据检索与范围统计性能。
- **修复前端 ResizeObserver 回流重绘死循环**：修正设置画板缩放监听目标并增加尺寸差值防抖，彻底解决修改 `canvas.style` 导致的无限重绘与浏览器主线程跑满卡死。
- **前端监控定时器泄漏治理**：完善监控定时器生命周期管理，在关闭设置或切换标签页时主动销毁定时器，并增加工作区可见性双重校验。
- **前端空指针安全防护**：用户管理与模型列表操作增加 `currentUser?.id` 可选链防护。

详细发布记录见 [v0.1.44 发布记录](docs/releases/v0.1.44-设置页面高并发性能与稳定性优化.md)。

## [v0.1.43] - 2026-08-24

### 运行环境大屏与能力治理优化

- **即时断流机制**：全量语义分析、自主任务与智能分析各模块全面支持 `AbortController` 即时断流，取消任务立即停止后端大模型推理与算力消耗。
- **能力与结果卡片全面中文化**：补充内置数据处理、格式化、文档分析及 MCP 工具卡片的中文名称与描述，自动剥离冗余前缀。
- **运行环境大屏自适应**：弹窗尺寸升级为 1480px × 880px 宽屏规格，各面板弹性拉伸填充，彻底消除底部过度留白。
- **个人档案排版优化**：身份偏好 4 列横排、工作习惯与工具偏好 3 列等高文本域、扩展配置 (JSON) 结构化编辑，修复按钮遮挡与留白。
- **记忆治理与结果反馈重构**：记忆治理增加 Switch 开关、2×2 类别网格、去除按钮下划线并新增安全合规机制卡片；结果反馈重构为 4 项 KPI 看板。
- **全面去 Emoji 化**：清理所有页面中的 Emoji 字符与特殊星号，统一替换为极简矢量 SVG 图标与标准文字单位。

详细发布记录见 [v0.1.43 发布记录](docs/releases/v0.1.43-运行环境大屏与能力治理优化.md)。

## [v0.1.42] - 2026-08-24

### 聊天模式入口简化

- **模式常驻可见**：在聊天输入框加号右侧直接显示当前“普通”或“Agent”模式。
- **快速切换**：点击当前模式名称即可展开选择，切换后立即生效并沿用到后续消息。
- **加号菜单收口**：加号菜单只保留附件、知识库和工具，移除重复的回答模式区域，降低菜单层级。
- **交互与样式回归**：补充聊天模式选择器静态契约、键盘/禁用状态和设置页回归检查。

详细发布记录见 [v0.1.42 发布记录](docs/releases/v0.1.42-聊天模式入口简化.md)。

## [v0.1.41] - 2026-08-24

### 普通聊天双模式与 Agent 执行许可

- **默认普通回答**：普通聊天默认只走模型流，不创建持久化 Agent，也不再依赖关键词/长度判断自动升级。
- **显式 Agent 执行**：聊天输入菜单新增“普通回答 / Agent 执行”模式；只有用户主动选择 Agent 执行时才创建连续 Agent Run。
- **管理员全局闸门**：设置项语义改为“允许聊天使用 Agent 执行模式”，服务端新增 `/chat/capabilities` 能力查询和 `AGENT_EXECUTION_DISABLED` 拒绝码；关闭时普通回答、手工 Agent 和工作流不受影响。
- **兼容旧部署**：保留 `chat_auto_agent_enabled` 持久化键和 `CHAT_AUTO_AGENT_ENABLED` 环境变量作为兼容别名，但不再触发自动升级；推荐使用 `CHAT_AGENT_EXECUTION_ENABLED`。
- **回归覆盖**：新增显式 Agent/普通回答/管理员禁用场景，更新聊天输入菜单、设置页、帮助文档和运行时配置契约。

详细发布记录见 [v0.1.41 发布记录](docs/releases/v0.1.41-普通聊天双模式与Agent执行许可.md)。

## [v0.1.40] - 2026-08-24

### Agent 连续执行控制回路、预算降级与断点恢复加固

- **流式主路径接回预算熔断**：流式 Agent 现在逐轮消耗步骤预算、逐工具统计调用/风险/副作用，并记录连续错误与成功复位；流式与 JSON 规划器共享绝对轮次游标，审批恢复和流式回退不会重复发放轮次。
- **超限结果降级交付**：Token 预算、运行时间或工具错误熔断触发后，任务优先进入 `completed_with_errors`，保留已完成观察并尝试生成部分结果；最终总结的用量记账支持“已生成答案不因最后一次预算检查被丢弃”。
- **上下文窗口治理**：流式请求在每轮模型调用前执行上下文拟合；模型未配置上下文窗口时使用 Agent 默认回退窗口，避免长任务把无界消息直接发送给上游。
- **审批断点恢复**：审批通过时保存检查点观察、恢复步数和已批准输入摘要，恢复任务从原执行进度继续规划，不再从空观察重新开始。
- **停滞检测与步骤一致性**：连续重复相同工具和输入达到阈值时提前总结；流式工具步骤使用当前规划轮次记录，避免与 JSON 路径混用持久化行数造成进度错乱。
- **上限统一**：最大步骤预算覆盖深度/审计模式的 60 轮；运行时间预算默认跟随 `AGENT_RUN_TIMEOUT_MS`，聊天 Agent 使用统一的运行/工具超时配置。
- **普通聊天成本收口**：`CHAT_AUTO_AGENT_ENABLED` 默认改为关闭；即使管理员开启，短且无任务意图的闲聊也继续走普通模型流，复杂任务才创建持久化 Agent。
- **回归验证**：新增流式预算计数断言和 `replanning -> completed_with_errors` 状态机覆盖；Agent SSE、聊天桥接、预算契约、语法检查和 ESLint 通过。

详细发布记录见 [v0.1.40 发布记录](docs/releases/v0.1.40-Agent连续执行控制回路与预算降级加固.md)。

## [v0.1.39] - 2026-08-24

### 长期记忆弹窗遮罩全屏、自定义截断悬浮气泡与设置布局优化

- **长期记忆弹窗遮罩层全屏统一**：在打开“编辑记忆”与“记忆来源”弹窗时自动挂载至顶层 `document.body`，解除局部缩放容器引起的 Containing Block 约束，实现与全站弹窗统一的 100% 覆盖全屏暗色遮罩。
- **高颜值自定义截断悬浮气泡**：废除浏览器原生迟钝简陋的 `title` 悬浮框，构建深色玻璃拟态气泡浮层（`.memory-custom-tooltip`），支持多行美观排版、断字换行与长文本滚动；具备视口边缘自适应算法（水平防溢出与靠底自动翻转向上弹出）。
- **全局参数智能体后台排版优化**：并发参数输入框调整为 3 列对称并排，“普通会话自动连续 Agent”开关调整为第二行独立展示，界面更清晰舒展。
- **模块解耦与架构治理**：拆分独立模块 `admin-memory-ui.js`，通过受控的 `window.Pivot.exposeModule('settings.memoryUi', ...)` 管理模块接口；`admin-settings.js` 严格维持在治理基线以内。
- **全量测试回归**：通过流式渲染与记忆契约测试、语法校验与全部 12 项 `npm run check` 架构指标。
- **详细发布记录**：见 [v0.1.39 发布记录](docs/releases/v0.1.39-长期记忆弹窗遮罩与自定义悬浮气泡优化.md)。

## [v0.1.38] - 2026-08-24

### 模型上下文友好报错、多架构容器运行时与 Agent 体验升级

- **模型上下文超限智能解析**：自动识别上游模型服务（vLLM、llama.cpp、Ollama、OpenAI 等）返回的 `exceed_context_size_error` 与 `context_length_exceeded` 等英文错误，精确提取请求 Token 数与模型窗口上限（如 `132,085 Tokens` vs `131,072 Tokens`），转化为结构化友好中文说明并提供开新会话、精简输入和调大 Context Size 的操作建议。
- **上游模型错误全量中文化**：统一规范化 401（API Key 无效/未授权）、402/429（余额不足/额度耗尽）、429（模型限流）、404（模型名称错误）、502/ECONNREFUSED（服务不可达/超时）等常见报错，消除直接将未翻译 JSON 甩给用户的生硬体验。
- **Docker 多架构原生绑定加固**：`Dockerfile` 支持 `ARG TARGETARCH` 动态适配，在 `amd64` 与 `arm64` 架构下自动精准补装对应的 DuckDB 原生二进制依赖（`linux-x64` / `linux-arm64`），避免跨架构构建失败；移除非必须的外部 syntax 指令，增强国内离线构建稳定性。
- **Docker 运行阶段自检契约**：在最终 runtime 镜像层增加原生依赖（`@duckdb/node-api`, `sharp`, `unzipper`, `better-sqlite3`）与系统工具（`python3`, `pg_dump`）的启动前加载自检，确保生产环境 100% 具备运行能力。
- **聊天 Agent 状态与操作体验**：聊天界面连续 Agent 任务增加「详情」按钮与判断摘要，支持懒加载任务模块与实时进度文本展示；优化短提问判定逻辑，防止普通日常短提问被误拦截。
- **测试与质量回归**：新增 `tests/chat-errors.test.js`（6 类模型错误全场景覆盖）与 `tests/dockerfile-runtime-contract.test.js`（多架构与自检契约验证）；`npm run check` 与 ESLint 扫描全部通过。
- **详细发布记录**：见 [v0.1.38 发布记录](docs/releases/v0.1.38-模型上下文友好报错与多架构容器加固.md)。

## [v0.1.37] - 2026-08-24

### 安全边界、批量性能与发布治理全面优化

- **Electron Worker 权限收口**：移除渲染页直接传入 `approved: true` 的执行路径，改为主进程签发、一次性消费且绑定命令、参数、输入、任务和工作区的短期审批令牌；Worker 工作区固定在客户端数据目录内。
- **桌面脚本执行限制**：禁止远程 HTTP 页面调用本机通用 Worker，禁止 `node -e` / `python -c`、绝对路径、工作区外脚本和直接联网；补充脚本类型、符号链接、输入大小和参数数量校验。
- **依赖审计门禁修复**：审计策略递归追踪 npm audit 的字符串型 `via` 传递依赖，正确识别对象型 `fixAvailable`，避免 Electron 父包漏洞被错误放行；Electron 升级至 `43.4.1`，生产依赖与完整依赖审计均为 0 个已知漏洞。
- **管理员用户导入性能优化**：bcrypt 密码哈希迁移到有限 Worker Thread，数据库写入改为分批事务插入，重复用户名转为可报告的跳过项；错误明细有上限，避免大 CSV 导入阻塞 HTTP 事件循环或返回超大响应。
- **分页参数统一治理**：管理员用户、审计日志、统计详情、API 调用日志和数据分析产物接口统一使用页码/条数边界校验，每页条数限制为 1-100，防止无界查询和异常 SQL 参数。
- **Docker 运行时加固**：改为多阶段构建，运行镜像移除编译工具并以非 root `node` 用户运行；Compose 启用只读根文件系统、`no-new-privileges` 和临时文件系统，并补齐宿主机目录权限说明。
- **CI 与架构治理升级**：CI 正式执行 Playwright 浏览器测试；新增大文件只减不增、生产 JavaScript 大文件、legacy `window.*` 和 E2E 数量治理报告，优化路线图改为引用实时指标而非过时固定数字。
- **前端模块化收口**：附件预览能力接入 `Pivot` 模块命名空间，legacy 全局暴露从 299 项降至 297 项。
- **发布验证**：`npm run check`、ESLint、依赖审计通过；新增安全/性能/构建定向回归通过；Playwright E2E `6/6` 通过。全量 Node 测试为 `619/620`，唯一失败为已有 Agent 队列时序测试，单独运行该测试文件 `4/4` 通过。
- **详细发布记录**：见 [v0.1.37 发布记录](docs/releases/v0.1.37-安全边界性能与发布治理升级.md)。

## [v0.1.36] - 2026-08-24

### 生产客户端自动更新与运行依赖修复

- **客户端自动更新启用**：生产桌面配置启用同站 `/downloads/` 更新源，从 `remoteUrl` 推导 `http://50.64.150.51:9006/downloads/`，并以精确 origin 白名单约束受控局域网 HTTP 更新。
- **双配置分发**：新版安装包同时内置 `resources/config.json` 和 `Pivot.exe` 同目录的 `config.json`；现有客户端也可由 IT 单独投放同一份外置配置，重启后即可激活自动更新，无需用户手动下载安装包。
- **更新发布产物**：Windows 打包流程继续生成并同步 `latest.yml`、版本安装器、blockmap、`Pivot-Setup.exe` 和 `SHA256SUMS.txt` 到 `downloads/`。
- **生产依赖修复**：`unzipper` 纳入正式运行依赖并加入 Docker 构建加载校验，避免生产镜像使用 `npm ci --omit=dev` 后启动失败。
- **RAG 启动告警修复**：RAG 指标计数器拆为无业务依赖的叶子模块，解除 CommonJS 循环依赖，生产启动不再出现 `recordRagRetrieval` / `recordRagIngest` 尚未导出的告警。
- **数据分析性能优化**：全量语义分析任务支持默认 2 路、最高 4 路批次并发，并继续受模型端点并发和每日 Token 额度约束。
- **国产化 Linux 客户端打包整改**：补齐 Linux `.deb` 元数据和 PNG 图标，新增 AMD64/ARM64 架构与原生模块预检，要求构建机和目标架构一致并校验 Electron、better-sqlite3、DuckDB、Sharp、unzipper、Chromium 与 Python 运行包。
- **LoongArch64 发布阻断**：移除虚假的龙芯正式打包路径；`dist:loong64` 现在明确失败并提示需要专用 Electron、DuckDB、Sharp 和 Chromium 运行时，避免生成不可运行安装包。
- **Linux 产物归档修复**：新增 AMD64/ARM64 `.deb` 与 AppImage 独立脚本，按平台生成 `SHA256SUMS-*.txt` 并维护合并清单，避免多架构发布互相覆盖校验文件。
- **发布验证**：完整 Node 测试 `620/620` 通过，客户端更新策略专项 `10/10` 通过，国产化构建门禁和 `npm run check`、ESLint、差异检查通过。

## [v0.1.35] - 2026-08-24

### 安全可靠性、任务恢复与资源边界全面升级

- **刷新令牌原子轮换**：刷新令牌改为事务内锁定、消费和签发，阻断并发请求重复使用同一令牌；过期令牌清理和新令牌写入保持一致性。
- **初始化凭据安全收口**：移除工作区 `.env` 中的弱默认管理员/导入密码；配置模板不再携带可运行的占位 JWT 或加密密钥；配置校验会拒绝弱密码和常见占位密钥。
- **文档任务可恢复队列**：文档处理任务增加数据库租约、启动恢复、过期租约重排队、最大重试失败和定时恢复扫描，支持进程重启及多实例抢占。
- **任务取消与清理竞态修复**：任务状态更新增加取消保护和条件写入；运行中任务删除、取消、任务创建失败时会进行补偿清理，避免后台 worker 重写终态或遗留文件。
- **全量语义分析批次并发优化**：数据分析“智能分析”的全量任务由单批串行改为同一任务默认 2 路、最高 4 路的有界并发调度；实际并发取任务配置与模型端点上限的较小值，28 批等大任务可显著缩短模型等待总时长。
- **并发安全与额度保护**：批次继续通过数据库条件认领，避免重复执行；失败重试、取消、进度统计和断点恢复保持原有语义；配置每日 Token 额度的模型自动退回串行，避免并发额度检查超发。
- **技能包生产依赖修复**：将服务启动路径直接使用的 `unzipper` 从开发期传递依赖提升为正式运行依赖，修复 Docker 使用 `npm ci --omit=dev` 后启动时报 `Cannot find module 'unzipper'`；镜像构建阶段新增运行依赖加载校验，缺失时直接终止构建。
- **RAG 指标循环依赖修复**：将检索与入库计数器拆分为无业务依赖的叶子模块，解除 `metrics → maintenance → document-processing → rag-index → metrics` 循环，消除生产启动时 `recordRagRetrieval` / `recordRagIngest` 尚未导出的告警，并避免指标函数在部分加载顺序下不可用。
- **生产客户端自动更新启用**：实际生产桌面配置启用同站 `/downloads/` 自动更新，为受控局域网 HTTP 更新显式开启许可，并将来源严格限定为生产服务 origin；通用示例配置继续默认关闭自动更新，避免其他环境无意放开 HTTP 安装包下载。
- **文档解析内存治理**：纯文本改为前缀流式读取；PDF、Office、OLE、图片解析增加二进制缓冲上限；文件 SHA256 改为流式计算，避免多份整文件内存副本。
- **CSV/JSON 数据边界**：管理员用户 CSV 导入改为流式解析，支持引号、逗号和跨行字段并限制最大行数；Agent JSON 数据源增加文件、行数和列数上限。
- **PDF 输出归档保护**：输出 ZIP 增加归档总大小限制，避免一次性读取大量输出导致内存峰值。
- **OCR 出站安全**：OCR 地址增加主机安全校验和 DNS Pinning；请求体、响应体、超时和图片大小均设置硬上限。
- **运行时资源包安全下载**：资源包同步统一使用受控 HTTP 客户端，增加响应大小、请求大小、超时、代理关闭和安全地址校验，继续保留 SHA256 校验与流式落盘。
- **浏览器下载防护**：Browser Worker 通过 Chromium 网络事件累计统计响应字节，覆盖无 `Content-Length` 的 chunked 响应，超出策略上限立即关闭页面。
- **多实例限流**：新增 PostgreSQL `rate_limit_counters` 共享计数表，登录、注册、聊天、上传、探测、Embedding、自动化和 Webhook 限流支持多实例共享；后台自动清理过期计数。
- **配置与运维参数**：`.env.example` 补充任务租约、恢复批次、解析缓冲区、OCR 响应、JSON 数据源、CSV 导入上限和 `DATA_ANALYSIS_SEMANTIC_BATCH_CONCURRENCY` 全量分析批次并发说明。
- **测试与门禁**：新增并发刷新令牌、CSV 跨行解析、沙箱输出上限和资源边界回归；动态 PostgreSQL schema 契约同步至 82 张表。
- **发布验证**：全量 Node 测试 `608/608` 通过，`npm run check`、`npm run lint`、`git diff --check` 通过，生产依赖审计 `0 vulnerabilities`。
- **详细发布记录**：见 [v0.1.35 发布记录](docs/releases/v0.1.35-安全可靠性与资源治理升级发布记录.md)。

## [v0.1.34] - 2026-08-23

### 运行支撑管理、自动化弹窗交互与全量数据表格规范优化

- **运行支撑管理（Harness）模块化与诊断体验升级**：
  - 运行支撑管理重构为 3 项顶级 Tab 模块导航（技能包管理、运行资源包、常驻实例），彻底解耦原有拥挤的网格布局，支持大尺寸列表与表单独立分栏展示；
  - 运行支撑诊断区域切换改为多面板常驻挂载配合 SWR 缓存与粘性吸顶，消除了子按钮切换时的页面闪烁与视觉跳动；
  - 统一运行支撑管理各面板标题、子标题与表单字段为严格左对齐（`text-align: left !important`）；
  - 严格统一诊断控制消息栏控件（`select` / `textarea` / `button` 统一定高 36px）及常驻实例操作栏按钮（统一定高 32px）的水平基准线基准，消除错位；
  - 统一工具栏按钮与弹窗头部标题文案为规范术语「运行支撑管理」。
- **工具策略页面全量中文化**：
  - 工具卡片上显示的系统工具、RAG 检索、MCP、数据库操作等所有工具名称与简介实现 100% 深度中文化映射及动态安全回落。
- **自动化工作台弹窗与工作流编排交互优化**：
  - 修复「自动启动」与「凭据库」弹窗遮罩及定位机制，彻底解决遮罩宽度截断导致弹窗偏左的缺陷，弹窗全视口正中央居中显示；
  - 修复 `display: flex !important` 覆盖 `.hidden` 导致页面加载自动弹出凭据库且无法关闭的缺陷；
  - 去除新建触发器/新建凭据编辑表单右上角与右下角功能重复的「关闭」按钮，统一由表单底部「取消」及顶部「返回列表」承接；
  - 移除分享工作流弹窗右上角与底部重复的「关闭」按钮；
  - 优化触发器与凭据的空状态矢量卡片与提示文案；
  - 修复「计划任务」弹窗当前工作流计划区域的标题、列表项、元数据、空状态左对齐问题。
- **全量数据表格分页规范**：
  - 统一系统设置页面、知识库页面等全系统数据表格默认每页显示 15 条数据。
- **发布验证**：
  - 自动化页面、Harness 契约、设置页契约等全量测试全部通过，代码开发规范与代码安全门禁全部通过。
- **详细设计与改动见** [v0.1.34 发布记录](docs/releases/v0.1.34-运行支撑与自动化弹窗交互体验优化发布记录.md)。

## [v0.1.33] - 2026-08-23

### 普通聊天持久化连续 Agent 全链路发布

- Web 与 Electron 普通聊天统一自动创建持久化 Agent，复杂任务可在页面离开、刷新或切换设备后继续执行，完成答案或错误会回写原会话。
- 新增聊天 Agent bridge，持久化会话历史、系统提示、当前多模态消息、长期记忆、RAG、MCP 策略和工具白名单，并复用队列、预算、审批、检查点、事件流和控制接口。
- Agent 终态通过 `messages.agent_run_id` 幂等写回；唯一部分索引、启动恢复扫描和并发回调收敛保证不产生重复助手消息。
- 普通聊天“重新生成”复用原用户消息并进入同一持久化路径，不重复插入用户消息；接管失败、源消息缺失和读取失败均有明确错误码及可见结果。
- 图片附件经过会话归属、路径和类型校验后转换为受控视觉输入；长期记忆、RAG 引用、系统提示词和 MCP 设置跨 Agent 运行保留。
- 会话打开、刷新、断线和跨设备切换可通过 `/api/agents/runs/chat-active` 恢复运行；SSE 实时事件和轮询兜底继续支持状态、停止、批准、拒绝和继续。
- 新增迁移 `202608220009_chat_agent_run_message_link`，同步 SQLite 历史库、PostgreSQL schema 和消息关联索引。
- 优化系统设置与分区加载提示：加载横幅改为居中悬浮气泡（Floating Pill），不再占用页面垂直文档流空间，彻底解决设置页因横幅撑高而出现多余垂直滚动条的问题。
- 优化系统设置“长期记忆”页面布局与排版：紧凑化统计指标卡、筛选控制栏、操作按钮与表格行高，统一系统设置所有数据表格分页每页显示 15 条数据，消除多余垂直滚动条，提供严整紧凑的管理视效并适配深色模式。
- 修正 PostgreSQL 旧库启动顺序：数据字典注释改为在版本化迁移后应用，避免 `messages.agent_run_id` 尚未补列时出现“字段不存在”的误导性告警；新增启动顺序契约测试防止回归。
- 新增 HTTP/SSE、PostgreSQL 并发幂等、恢复扫描、视觉附件和 Web/Electron 客户端恢复回归。
- 发布验证：`npm test` `593/593`，语法检查 `525/525` 文件，raw SQL 基线 `816/816`，`git diff --check` 通过。
- 详细设计、迁移、运行链路、运维检查和上线冒烟项见 [v0.1.33 发布记录](docs/releases/v0.1.33-持久化连续Agent发布记录.md)。

## [v0.1.32] - 2026-08-22

### 自动化资源管理与凭据共享闭环

- **触发器管理入口**：自动化工作台新增“触发器与凭据”统一入口，支持 Webhook、文件落地和数据变更三类触发器的创建、编辑、启停、删除与 Webhook 令牌轮换；触发器只允许关联已发布工作流，并显示调用次数、最近触发时间和最近错误。
- **一次性 Webhook 凭证保护**：Webhook 地址只在创建或轮换后展示一次，支持直接复制；关闭资源窗口即清除页面上的访问令牌，后续只能通过轮换生成新地址。
- **凭据库可用性**：支持凭据创建、元数据维护、轮换、24 小时内撤销轮换和删除；秘密值只会加密写入服务端，列表、编辑和共享流程均不读取或回显明文。
- **共享权限闭环**：凭据支持仅自己、指定单位、指定个人和管理员全体共享；修复服务端遗漏 `allowed_user_ids` 的持久化问题，新增兼容旧库和 PostgreSQL 的迁移，接收者只能使用共享凭据而不能管理或查看秘密值。
- **交互与无障碍**：资源弹窗具备 dialog/tab/panel 语义、键盘切换、焦点恢复、请求锁、忙碌状态和失败提示；补齐窄屏与深色主题布局。
- **回归验证**：自动化、触发器和安全专项 `87/87` 通过，完整 Node 回归 `584/584` 通过；窗口全局规范、ESLint、语法检查和差异检查通过。

## [v0.1.31] - 2026-08-22

### 自动化页面全功能深度检查与执行稳定性优化

- **实时状态与请求一致性**：修复自动化工作台在节点缺失时仍持续轮询的问题；任务、工作流和计划列表增加请求序列保护，快速切换筛选或刷新时旧响应不会覆盖当前状态。
- **执行防重复与幂等**：任务创建和工作流运行补充 `Idempotency-Key`；工作流预览、发布版运行、资产列表运行和只读运行入口共享忙碌锁；取消、审批、重跑、续跑、节点重跑、删除、计划运行/启停/删除等操作统一加入按钮状态和网络异常恢复，避免重复派发与“点击后无反馈”。
- **计划任务体验**：计划列表和工作流计划列表增加失败提示、状态恢复和加载竞态保护；动态编辑器打开后自动聚焦，关闭后恢复触发焦点。
- **弹窗与无障碍**：自动化主导航补齐 `aria-controls`、焦点级别和面板状态；运行详情、配置、工作流元数据、共享、依赖、原始 JSON 等弹窗补齐 dialog、`aria-hidden`、标题关联和焦点恢复语义。
- **回归验证**：新增自动化页面契约测试，覆盖后台轮询、请求竞态、幂等键、操作锁和弹窗语义；同步更新版本文档与 README。

## [v0.1.30] - 2026-08-22

### 设置页全功能深度检查与安全、易用性优化

- **敏感配置保护**：设置接口不再向浏览器返回 `app_settings` 中的原始 API Key、Token、Secret、密码和 Webhook 值；保留配置状态元数据，保存链路继续由服务端权限控制。
- **加载与错误恢复**：设置页增加全局加载状态、分区加载状态和重试入口；非 JSON/HTTP 错误转换为明确提示，重复打开或切换设置时旧响应不会覆盖当前内容。
- **分区切换稳定性**：分区加载统一等待并捕获异常，分页与刷新不会静默失败；编辑运行参数或检索参数后切换分区会提示未保存修改，避免配置丢失。
- **无障碍与键盘操作**：设置侧栏补齐 `tablist`/`tab`/`tabpanel` 语义、选中状态和方向键/Home/End 导航；运行参数标签、说明关联，密码和 API Key 表单补充自动填充与按钮类型。
- **弹窗与敏感操作**：API Key、公告、监控、长期记忆弹窗补齐 dialog 语义和 `aria-hidden` 同步；创建密钥、修改密码和长期记忆批量操作增加忙碌锁、失败提示和状态恢复。
- **响应式与主题**：窄屏设置不再整体缩放为不可读缩略图，宽表格支持横向滚动；设置工作台补充深色主题、状态提示和移动端横向分区布局。
- **回归验证**：新增设置页契约测试与敏感配置脱敏回归；设置安全专项 `128/128`、设置页契约 `2/2`、全量 Node 测试 `579/579` 通过，ESLint、语法检查和现有项目门禁继续执行。

## [v0.1.29] - 2026-08-22

### 全局搜索页深度体验与性能优化

- **请求一致性**：会话、任务和工作流搜索加入请求序列校验，切换关键词、范围或关闭弹窗后，旧请求不会覆盖当前结果。
- **错误可恢复**：检查接口 HTTP 状态和 JSON 响应；标题搜索保持可用时，消息内容搜索降级为部分结果并明确提示；完整失败提供重试入口。
- **工作流检索性能**：工作流关键词过滤下沉到服务端和数据访问层，减少每次输入时的全量浏览器扫描，并返回结果计数。
- **交互完善**：新增清空搜索、Enter 立即搜索、未命中数量提示和错误重试；保留会话/归档切换、批量标签、任务与工作流跳转。
- **无障碍与主题**：搜索弹窗补齐 dialog、tab、状态播报、`aria-hidden`、焦点恢复、Tab 焦点循环和 ESC 关闭；补充移动端与深色主题样式。
- **回归验证**：新增搜索页面契约测试，覆盖竞态保护、错误恢复、服务端工作流过滤、ARIA 和清空/重试交互；完整 Node 回归测试 `576/576` 通过。

## [v0.1.28] - 2026-08-22

### 应用中心全面体验与入口稳定性优化

- **统一入口状态**：应用中心增加加载中、打开失败和可重试状态，子应用加载异常不再只留空白页面或依赖控制台错误。
- **恢复行为修正**：修复法规查询恢复时调用错误全局入口的问题；应用中心刷新、重新登录后可稳定恢复法规、OCR、PDF 工具和数据分析工作台。
- **存储降级**：应用中心会话状态统一封装并兼容隐私模式、浏览器禁用存储等场景，存储不可用时不阻断当前应用使用。
- **交互与无障碍**：应用中心补充区域语义、`aria-hidden` 同步、焦点恢复、卡片可访问名称和统一状态提示；窄屏下应用卡片和重试操作改为单列易触控布局。
- **回归验证**：应用中心静态契约和聊天工作台回归测试通过；ESLint、窗口全局规范、语法检查和差异检查通过。

## [v0.1.27] - 2026-08-22

### 知识库工作台深度体验优化与配置治理

- **加载与错误反馈完善**：文档列表与摘要、质量诊断、知识图谱统计改为分级加载；统计接口异常时仍保留可用文档列表，并提供明确的部分失败提示和重试入口。
- **操作稳定性提升**：批量重建、批量删除、启停、整理、重建和删除操作增加请求锁与忙碌状态；启停失败会回滚开关，删除接口异常会显示服务端错误，不再静默失败。
- **上传流程更可靠**：文件选择和拖拽统一入队，支持重复选择同名文件；上传进行中禁止误关弹窗或清空队列，失败文件保留以便重试。
- **检索设置更合理**：Top K 默认值统一为 3；重叠字符数随分块长度动态限制为不超过一半，并在保存前进行阈值、候选数量、分块长度和重叠长度校验，避免设置被后端静默截断。
- **弹窗与可访问性完善**：知识库上传、整理、详情、删除审计、共享、召回测试和检索配置弹窗补充对话框语义、标题关联、`aria-hidden`、ESC/遮罩关闭、焦点进入与关闭后焦点恢复；表单标签和按钮类型补全。
- **筛选与移动端体验优化**：当前页全选支持半选状态并在列表刷新后同步；窄屏下筛选、操作菜单和状态提示改为更易触控的布局；质量诊断、召回结果和标签选择器兼容深色主题。
- **回归验证**：知识库静态契约断言、RAG 配置边界测试、ESLint、语法检查和差异检查通过。

## [v0.1.26] - 2026-08-22

### 工具库工作台深度体验优化与稳定性加固

- **加载与错误反馈完善**：工具库增加加载中、失败和可重试状态；治理概览加载失败时保留工具列表和其他可用功能，不再出现整块空白；接口响应异常或非 JSON 响应会转换为可理解的中文提示。
- **操作稳定性提升**：新增保存、删除、刷新、诊断、连接测试、批量自检和启停操作锁，防止重复点击造成重复请求；按钮在请求期间显示忙碌状态并在异常时正确恢复。
- **健康检查边界修正**：批量连通性自检自动跳过暂停服务、共享只读服务和不可诊断服务，避免产生无效失败结果；本机桥接未注入或调用失败时，浏览器端仍可稳定打开工具库。
- **弹窗交互与可访问性完善**：编辑、工具列表、共享、本机授权和单步测试弹窗补充对话框语义、标题关联、`aria-hidden` 状态、ESC 关闭、遮罩关闭、焦点进入和关闭后焦点恢复。
- **表单体验优化**：补充输入标签、必填约束、URL/数字/密码类型、自动填充提示和更明确的占位说明，降低数据库、报表目录、IM 通知和外部 MCP 配置的理解成本。
- **移动端与主题适配**：工具库状态条、编辑表单和底部操作区适配窄屏；状态反馈和移动端吸底区域改用主题变量，兼容深色模式。
- **显示与安全细节修复**：修复工具名称被错误显示为 HTML 转义文本的问题，继续通过安全 HTML 渲染路径生成动态内容；治理失败重试按钮和工具列表加载占位保持安全渲染。
- **回归验证**：工具库相关回归测试 `31/31` 通过；完整 Node 测试 `573/573` 通过；`npm run check`、`npm run lint`、语法检查和差异检查通过。

## [v0.1.25] - 2026-08-22

### 工作流编排易用性优化：节点分层、配置减负与首节点聚焦

- 重整工作流节点预设：保留现有运行时节点和历史工作流兼容性，将委派智能体、代码执行、循环、网络请求、变量聚合、浏览器自动化、知识关系查询、历史会话检索、报表对比、文档处理和数据处理等低频或技术型能力收进“高级节点”；普通用户默认只看到常用节点，高级节点仍可通过展开、搜索和顶部高级菜单访问。
- 补齐并修正节点工具映射：报表、文档和数据处理预设改用精确工具名匹配，新增报表读取/查询/对比、文档大纲/键值/长文本拆分、表格筛选/分组/画像/字段规范化等可复用节点；修复委派智能体角色值与服务端枚举不一致的问题。
- 重做节点参数向导的信息层级：主要业务字段直接展示，模型采样、并发、候选数、超时、凭据和其他技术参数折叠到“更多设置”；输出模式、条件比较值等按当前选择动态显示，减少无关字段和重复配置。
- 面向普通用户统一中文字段名称、说明、枚举和示例占位符；延时节点改为填写“秒”，失败处理、自动重试、单步超时和工具内部标识默认收起。
- 修复“套用模板”和“清空”对隐藏高级参数的保存语义，避免旧技术参数被悄悄带回或模板配置未真正生效。
- 新建工作流首次添加普通节点、预设节点或模板时，画布会自动适配内容并将首节点居中放大；后续添加节点、已有工作流和手动拖拽布局保持不变。
- 回归验证：全量 Node 测试 `573/573` 通过；DAG 布局专项测试 `12/12` 通过；修改文件语法检查、ESLint 和差异检查通过。

## [v0.1.24] - 2026-08-22

### 质量闭环：Provider 用量校准、非幂等故障矩阵与多进程 Residency 压测

- 新增 `model_usage_calibrations` PostgreSQL 聚合表和 `provider-usage-calibration.js`：记录真实 Provider input/output/total usage 与字符估算的有符号误差、绝对误差、偏差比和最大误差；普通 JSON、Agent planner、工作流节点、委派、内容校对和流式入口均接入校准记录。
- 新增 Provider usage 校准契约测试，覆盖 Chat Completions/Responses 字段归一化、缺失字段降级和 PostgreSQL 聚合指标写入。
- 扩展非幂等副作用故障矩阵，覆盖超时、Worker 崩溃/错误、上游不可用、重复投递和外部提交状态未知；已完成操作键只读重放，状态未知的调用必须重新审批，不允许自动重试。
- 新增多进程 residency 测试 worker，验证 8 个并发进程争抢同一租约时只有一个获胜，并验证 12 路并发 touch 最终收敛到 per-user LRU 上限。
- 新增 `npm run test:agent:quality`，可单独执行三项质量扩展回归；迁移 `202608220007_provider_usage_calibration` 随启动自动应用。

## [v0.1.23] - 2026-08-22

### Codex Harness 对照项闭环：官方 MCP、外部长连接与 Agent Residency

- Provider 转发边界统一清洗 Chat Completions 与 Responses API 的消息、Responses input、tool/function 定义和工具调用字段；Chat、OpenAI 兼容、应用中心、标题、RAG/长期记忆及 Agent 文本/流式入口继续复用同一转发层，内部治理字段不会从消息对象透传到上游。
- 外部 MCP 标准 HTTP 客户端支持分片 SSE、异步 JSON-RPC 通知、会话头、协议版本、标准 GET SSE 重连、`retry` 延迟和 `Last-Event-ID` 恢复；普通请求收到目标结果后关闭流，需要订阅时可显式保活。
- 固定接入 `@modelcontextprotocol/conformance@0.2.0-alpha.11`，新增官方 `initialize`、`tools_call` 和 `sse-retry` 客户端场景脚本与 npm 命令，三项官方场景实测通过。
- 新增 PostgreSQL `agent_residencies` 持久化表与 Agent residency 服务：WorldState 脱敏快照、上下文哈希、运行绑定、访问时间、租约、过期淘汰、每用户 LRU、手工 evict/list 和后台 sweep；App Server 增加常驻实例 touch、租约领取/释放与治理方法。
- 新增 residency PostgreSQL 集成回归和 MCP 分片长连接回归，版本迁移 `202608220006_agent_residency` 随启动自动应用。

## [v0.1.22] - 2026-08-22

### Codex Harness 对照项继续落地

- Chat 入口新增 PostgreSQL 持久化 context window/snapshot，记录 turn、context/world-state hash、full/reference/diff 注入模式和压缩/权限/模型变化导致的窗口轮换，并提供窗口与快照查询接口。
- Chat MCP 调用和桌面 Runtime 在 handler 前统一生成 `ToolExecutionPlan`，执行策略、审批、网络预检、sandbox 选择、规范化输入和重试语义；计划摘要写入执行步骤与 Trace 边界。
- 桌面 Runtime 每个工具步骤创建并持久化 `AgentStepContext` 的 `contextHash/worldStateHash`，checkpoint 可按 hash 与服务端事件、Trace 对齐。
- Provider 流新增 Responses API 与 Chat Completions 事件分类/状态机，记录工具增量、完成/失败/不完整状态、响应 ID、usage 和 finish reason；Agent model.completed 事件带 provider 摘要，Chat 也回传 provider 状态。
- 新增 App Server JSON-RPC 控制面基线：`thread/start`、`turn/start`、`turn/steer`、`turn/interrupt`、`turn/events`，复用 Agent Runtime、AgentControl 和事件 replay。
- 新增本地 MCP JSON-RPC conformance harness，覆盖 initialize、initialized notification、session header、tools/list 和 tools/call 生命周期。
- 新增协议与恢复契约测试，覆盖 Responses/Chat Completions 状态机、App Server 控制面、本地 MCP conformance，以及非幂等副作用恢复时未审批不执行、审批后单次执行。
- 迁移 `202608220005_chat_context_windows` 新增 Chat context window/snapshot 表及索引；本版本继续按 PostgreSQL-only 服务端口径维护。

## [v0.1.21] - 2026-08-21

### 阶段五：Trace 编译器、端云固化与全链路验收

- 桌面 Runtime 新增 Trace 账本到 Web DAG 草稿的编译入口，并与现有 Trace/Workflow Draft API 对齐。
- 完成端云协同联调、Checkpoint Crash Recovery、非幂等副作用重新审批、32 路并发压力和 SSRF/白名单安全探针。
- 五阶段专项验收累计 `39/39` 通过（阶段一 20/20、阶段二 22/22、阶段三 3/3、阶段四 6/6、阶段五 3/3），新增验收记录见 `docs/reports/Pivot自主Agent五阶段验收记录.md`。
- 本轮补强：桌面 Runtime 看门狗与预算落库、操作键输入摘要防混淆、Workspace Jail 符号链接防逃逸、完整 YAML Skill 解析与最小权限 PEP、企业内网白名单例外、Skill ZIP 魔数校验、Trace YAML/DAG 输出，以及 Python/Chromium 离线资源打包闭环。
- 按 PostgreSQL-only 部署口径移除本轮新增的服务端 SQLite Skill/checkpoint 兼容迁移；PostgreSQL 生产迁移保留 owner_key、幂等检查点和工具审计字段。

## [v0.1.19] - 2026-08-21

### 阶段四：Skill 生态与局域网运行时资源分发

- 新增 `.skill.zip` 离线供应链：`SKILL.yaml` 清单校验、SHA256 摘要、detached RSA 签名、权限最小化和安全解包。
- 新增 Data Pack / Browser Pack 局域网同步、大小限制、Origin 白名单和 SHA256 校验，并接入 Agent 管理 API。
- 阶段四专项测试 `4/4` 通过，覆盖签名包、路径穿越、重复条目、权限审计与资源包同步拒绝。

## [v0.1.18] - 2026-08-21

### 阶段三：离线 Chromium、受控登录与浏览器双引擎定位

- Agent Browser 支持从离线资源包、环境变量或 Playwright 安装路径解析 Chromium；桌面打包前自动生成并纳入 `agent-runtime/browser`。
- 浏览器上下文使用独立持久化 Profile、下载禁用、Service Worker 禁止、Origin/端口/重定向/下载大小策略和凭证脚本读取阻断。
- 新增受控用户登录流程：用户在白名单页面完成登录，Agent 仅接收登录完成信号，不读取 Cookie、Storage 或密码字段。
- 新增 DOM 优先、视觉截图回退的目标定位与点击 API，并接入 `agent.browser` 工具。
- 阶段三专项测试 `22/22` 通过，真实 Chromium 集成验证、离线浏览器 dry-run 和 ESLint 均通过。

## [v0.1.17] - 2026-08-21

### 阶段二：OS 隔离、Python Worker 与数据错误自愈

- 增加跨平台 OS 隔离适配：Windows Job Object 内存限制、Linux cgroup 限制与网络命名空间能力探测；严格模式在能力不可用时拒绝启动。
- Workspace Jail Worker 现在返回实际隔离元数据，并对进程组、内存、PID 数和网络隔离策略进行统一编排。
- 新增受工作区约束的 Python Worker，支持输入 JSON、超时、脚本大小限制、无用户 site 和临时文件隔离。
- 增加按错误类别生成的指数退避恢复计划，覆盖语法、结构、数据质量、权限、策略、网络、资源和超时错误。
- 阶段二专项测试 `20/20` 通过，包含真实 Python 3.12 执行验证；ESLint 无错误。

## [v0.1.16] - 2026-08-21

### 阶段一：桌面 Agent Runtime、状态机与 PEP 执行闭环

- 新增桌面端 SQLite State DB，持久化任务、步骤、工具调用账本与恢复检查点。
- 新增 `DesktopAgentRuntime`，统一状态流转、TaskBudget、Tool Contract、PEP、审批暂停/恢复、工具执行和诊断记录。
- 工具调用采用操作键与输入哈希，已完成调用可幂等重放，未完成非幂等调用恢复时强制重新审批。
- 新增桌面 Runtime 集成测试，覆盖正常执行、风险审批恢复和崩溃恢复，阶段一专项测试 `17/17` 通过，ESLint 通过。

## [v0.1.15] - 2026-08-21

### 智能体实时状态更新、工具输入契约修复与长期记忆超时降级优化

- **智能体全功能实时状态同步**：
  - 统一使用 SSE + 轮询兜底刷新任务列表、运行详情、执行步骤、运行指标、通知、工作流、计划任务、模板、结果沉淀、工具列表和评测中心，打开工作台后无需手动刷新页面。
  - SSE 正常时使用 5 秒兜底轮询，连接断开时切换为 2.5 秒轮询；所有实时读取请求使用 `cache: 'no-store'`，避免浏览器缓存旧状态。
  - 增加详情请求序列保护、刷新并发锁和评测队列/运行状态轮询，避免 SSE、轮询和工作流预览同时刷新时旧响应覆盖新状态。
  - 关闭智能体工作台、工作流工作台或退出登录时清理定时器与 SSE 连接，避免后台页面继续请求。
- **`agent.content_review` 工具契约与执行链路修复**：
  - 将内置工具的 `model` 明确定义为文本，并统一声明 `records`、`model` 为必需输入，修复“输入.model 应为文本”的策略校验错误。
  - 统一规范化模型对象、数字和 JSON 字符串输入；将 `rows`、`data`、`items` 等记录别名转换为 `records`，并使用模型路由后的实际模型作为默认模型。
  - 自主规划、普通 DAG、嵌套子工作流、流式工具调用、审批、检查点、审计和实际工具执行全部使用同一份规范化输入，避免校验通过后执行器再次收到原始对象。
- **长期记忆模型抽取超时治理**：
  - 模型抽取请求增加最大输出 Token 限制，默认 `800`，减少长 JSON 生成造成的超时。
  - 识别 `ECONNABORTED`、`ETIMEDOUT` 等超时错误，超时自动回退启发式规则并降为 debug 日志；非超时模型错误仍保留 warn 日志和诊断字段。
  - 增加按模型维度的超时熔断冷却，默认 `120000ms`；冷却期间直接使用启发式规则，避免模型端点异常时每条消息重复等待 15 秒，冷却结束后自动恢复模型抽取。
  - 抽取结果增加 `modelFallbackReason`，区分 `timeout`、`cooldown` 和普通 `error`；`.env.example` 增加对应生产配置说明。
- **回归与发布验证**：
  - 智能体契约、模型输入规范化、长期记忆超时分类和前端中文界面定向测试 `22/22` 通过；`npm run lint`、语法检查和 `git diff --check` 通过。
  - 当前验证环境未配置 `DATABASE_URL`，依赖 PostgreSQL 的长期记忆集成套件未执行，部署生产前应使用实际 PostgreSQL 环境完成一次集成回归。

## [v0.1.14] - 2026-08-21

### 工作流与自主任务执行面板全量中文化、富文本步骤详情与长文本语义批次加固

- **富文本内容校对步骤详情中文化**：
  - 全面补齐 `agent.content_review` 产物及指标字典（`stats` → 指标统计、`records` → 校对明细、`artifact` → 任务产物、`reviewComplete` → 校对完成、`sourceRowCount` → 原始数据行数、`processedRecords` → 已处理记录数、`incompleteRecords` → 未完整处理记录数、`titleIssues` → 标题问题数、`contentIssues` → 正文问题数、`chunkTokens` → 分块 Token 预算、`overlapTokens` → 重叠 Token 数 等）。
  - 产物类型（`content_review_report` → 内容校对报告）、问题类别（错别字、标点符号、敏感词汇等）与字段名实现标准化中文直译，杜绝原英文驼峰与枚举泄露。
- **任务执行面板与工作流节点中文化**：
  - 覆盖所有 48+ 种工作流与内置工具（`agent.delegate` → 委派智能体、`agent.handoff` → 智能体交接、`workflow.condition` → 条件路由、`workflow.approval` → 人工审批、`report.compose` → 报告编排、`rag.search` → 知识库检索、`code.python_execute` → Python 脚本执行 等）标准中文标题与用途说明。
  - DAG 节点执行条件（`condition`：`success` → 上游成功时、`always` → 始终执行、`failure` → 上游失败时、空 → 无限制）与 24 种生命周期状态（`queued`、`planning`、`executing`、`waiting_approval`、`continued_error`、`issues_found`、`passed` 等）全面本地化。
  - 引入动态子词与复合后缀智能翻译机制，对任意动态扩展字段（`*Count`、`*List`、`*Name`、`*Type`、`*Status`、`*Percent` 等）自动按语义组合翻译。
- **全量长文本语义分析批次拆分与恢复加固**：
  - 语义分析批次自动根据 Token 输出预算动态计算子批次上限，在单批次记录较多时递归拆分并发处理，避免超出模型最大输出长度。
  - 新增截断响应（`SEMANTIC_BATCH_INCOMPLETE`）自动二分重试与结果合并机制，保证极端长文本下的结果完整性。
- **回归与自动化测试**：
  - 新增前端中文化专项测试 `tests/client-agent-chinese-ui.test.js`；全量测试套件扩充至 `510/510` 全部通过；静态规范检查与 ESLint 零警告通过。

## [v0.1.13] - 2026-08-21

### 全自主 Agent 执行治理、桌面执行面与 PostgreSQL 生产闭环

- **自主执行状态机完整化**：统一规划、观察、执行、诊断、等待审批、完成和失败状态；队列支持并发槽位、租约、心跳、超时回收、异常恢复和重复唤醒合并，运行失败不会丢失阶段信息。
- **执行账本与幂等检查点**：新增工具调用账本、输入哈希、操作键、尝试次数和持久化检查点；只读工具可安全重放，非幂等恢复必须重新经过审批，避免重试造成重复写入或外部副作用。
- **预算与策略执行点统一**：工具注册契约、风险等级、审批策略、网络策略、Token/调用次数/错误熔断和外部副作用预算统一在执行前检查；运行记录持久化预算、用量和网络配置，流式与 JSON 回退共享同一治理边界。
- **网络与数据访问隔离**：浏览器工具增加域名白名单、重定向校验、下载大小限制和独立页面；CSV、Parquet、Excel、SQLite、JSON 数据适配统一受工作区 jail 和临时文件清理约束；Skill 增加作用域、所有者、权限白名单和签名摘要校验。
- **桌面全自主执行面**：新增 JSONL Worker、命令白名单、工作区路径 jail、超时/输出上限、显式审批和网络白名单；Electron 通过受信 IPC 调用桌面 Worker，支持远程模式下的本机能力安全执行。
- **审计与可观测性增强**：工具审计统一脱敏输入输出、失败重试、操作键和 Blob 引用；大结果使用内容寻址 Blob 存储；Trace 编译补充节点依赖、风险、审批、副作用和幂等元数据，可生成可审查的工作流草稿。
- **调度与 PostgreSQL 迁移边界收口**：手动触发到期周期计划后正确推进下一次执行时间；执行账本迁移只保留 PostgreSQL `upPg`，遗留 SQLite runner 跳过 PostgreSQL-only 迁移，避免旧测试快照影响生产迁移设计。
- **回归验证**：全量 Node 回归 `505/505`；迁移专项 `6/6`、PostgreSQL 与 Agent 契约专项 `18/18`；`npm run check`、`npm run lint` 和 `git diff --check` 全部通过。

## [v0.1.12] - 2026-08-20

### 工具库权限治理、PostgreSQL 异步执行与 MCP 兼容性全面加固

- **数据库 SQL 治理修复**：表白名单改为 schema 精确匹配，阻止同名表跨 schema 绕过；字段白名单按 JOIN 表和别名绑定，拒绝跨表借用字段权限；SQL Server CTE 查询也强制注入 `TOP` 行数上限。
- **工具治理真正生效**：数据库和外部 MCP 工具现在携带实际风险等级与审批策略，管理员设置的“需审批”会进入 Agent 运行时；工具执行前强制确认工具存在于当前缓存，避免猜测工具名调用未暴露远程能力。
- **异步数据库超时加固**：PostgreSQL 查询支持 AbortSignal，MySQL 超时销毁连接，SQL Server 超时关闭连接池，避免只返回超时但底层查询继续占用资源。
- **外部 MCP 安全与协议增强**：外部 JSON 响应设置 4MB 上限；新增可选标准 MCP HTTP 会话模式、协议版本头、初始化会话和 `notifications/initialized`，兼容 SSE JSON 响应，同时保留旧 JSON-RPC 模式。
- **报表目录边界与数据质量修复**：使用真实路径校验符号链接/reparse point，阻止授权根目录外读取；重复列名自动编号；指定 Sheet 不存在时返回 warning；报表结果补充 source、warnings、provenance 元数据。
- **工具参数体验修复**：工作台测试样例与可视化、数据处理、文档、数据库、IM 工具真实 Schema 对齐；数据处理结果增加截断标记、来源和输出限制信息。
- **审计与可观测性增强**：REST 与 JSON-RPC 内置工具调用统一记录 MCP 审计日志；写入队列保留 pending、dropped、max 诊断信息并接入详细系统健康快照，便于发现日志丢弃。
- **测试与门禁**：新增工具库权限/SQL/Schema 回归测试；`npm run check:async_db_calls` 纳入项目检查；隔离 PostgreSQL MCP 安全套件 33/33 通过，工具库加固测试 32/32 通过。
- **验证说明**：完整测试套件 492 项中 489 项通过；3 项仅在多文件串行共享测试队列时出现审计日志读取时序失败，相关 `security-mcp.test.js` 隔离运行稳定 33/33，通过专项重跑确认不是数据库 MCP 核心逻辑失败。

## [v0.1.11] - 2026-08-20

### 数据分析全量长文本语义分析任务与工具库调试治理升级

- **全量覆盖**：新增全量语义分析任务，按估算 Token 自动切分文本字段，不再把整列长文本一次性放入模型上下文，也不会抽样或跳过记录。
- **超长单元格处理**：单个单元格超过批次预算时继续拆分为多个文本分块，保留记录标识、分块序号和完整字符覆盖。
- **异步任务持久化**：新增 `analysis_semantic_jobs`、`analysis_semantic_batches` 表，保存任务、批次、进度、失败原因、重试次数、结果和最终报告；PostgreSQL 启动 schema 初始化自动创建。
- **后台恢复与治理**：后台 worker 支持排队、运行锁、过期锁恢复、批次级重试、任务取消和服务重启续跑；模型调用复用统一上下文预算、端点并发、Token 统计和每日额度检查。
- **结果完整性校验**：批次必须逐一返回预期记录/分块结果，缺失任一结果会失败并重试；任务详情默认只返回进度，完整批次结果通过独立结果接口获取，避免轮询再次放大上下文和响应体。
- **数据分析界面**：智能分析页新增文本字段、记录标识字段、批次 Token 预算、全量任务启动/取消/重试、进度条和最终报告展示。
- **工具库单步测试工作台 (Tool Test Runner)**：在工具列表每个工具卡片增加「单步测试」入口，支持智能测试样例一键填入、Schema 参数提示、JSON 自动格式化与实时耗时结果回显。
- **工具连通性一键自检 (Batch Health Check)**：在工具治理工具栏新增轻量化「连通性自检」功能，支持一键批量探测全部已配置数据库与外部 MCP 服务的在线状态。
- **回归验证**：新增批次覆盖与上下界测试，PostgreSQL schema 合同测试更新为 81 张表；相关服务语法检查、代码规范和全量自动化测试通过。

## [v0.1.10] - 2026-08-20

### 富文本内容校对全链路加固、推理模型兼容与工作流编排节点主题统一

- **富文本与文字校对全向兼容加固 (Content Review Input Hardening)**：
  - 增强 `server/services/agent-content-review.js` 中的 `rowsFromReviewInput` 与 `normalizeReviewRecords`，全面支持纯文本字符串输入（如直接传入单段正文）、单对象输入以及标准记录数组；
  - 扩展正文字段别名回退表（支持 `content`、`body`、`text`、`value`、`news_content`、`article_content`、`html`、`body_html`、`raw`、`raw_text`、`article`、`description`、`message`、`input` 等），彻底杜绝因上游字段未匹配导致空内容校对的误报假象。
- **强化校对 Prompt 提示词工程与小模型防偷懒 (Prompt Few-Shot & Defect Guidance)**：
  - 明确同音别字（如“按装”→“安装”、“设配”→“设备”）、形近字、错漏字、语序搭配不当、标点误用等细粒度排查指引；
  - 提供标准 JSON 示范结构，强制大模型逐句扫描原文，消除本地小尺寸模型的偷懒空输出。
- **推理模型思考标签深度清洗 (Reasoning Model Think Tags Stripping)**：
  - 增加 `stripThinkTags`，在解析模型响应前自动剥离 `<think>...</think>` / `<thought>...</thought>` 思考块及 Markdown 代码块，全面兼容 DeepSeek-R1 / QwQ 等推理型模型。
- **证据链模糊匹配与引号清洗 (Evidence Matching Quote Cleaning)**：
  - 增加 `cleanEvidenceQuotes`，在比对大模型找出的错别字原文依据时，自动剥离大模型习惯性添加的外层引号、书名号及句末标点，避免真实识别到的错别字被系统误杀过滤。
- **工作流节点选择主题样式统一 (Workflow DAG Node Picker Theme Alignment)**：
  - 移除 `client/chat/styles/workspaces/agent/agent-dag-toolbar.css` 中 `.pivot-dag-toolbar-btn.is-llm` 的绿色背景（`#10a37f`）与深绿 hover 状态，使大模型与富文本校对节点在亮色与深色模式下与所有其他预设节点保持统一的背景色与交互质感。
- **新建任务面板运行任务按钮状态与前置校验修复 (New Task Panel Run Button Guard)**：
  - 修复 `client/chat/agent-run-actions.js` 中因未先等待 `preflightAgentPayload` 导致的 `ReferenceError: preflight is not defined` 异常，完善按钮禁用与防重复提交机制。
- **回归覆盖与校验 (Regression Tests)**：
  - 新增 `tests/agent-content-review-node.test.js` 中针对纯文本输入、多字段对象输入及推理模型思考块解析的单元测试；全局校验与语法检查全部通过。

## [v0.1.9] - 2026-08-20

### 数据分析大模型治理、证据链与 PostgreSQL 迁移后兼容性升级

- **数据范围透明化**：分析数据集新增来源行数、来源列数、截断标记和截断原因；CSV、Excel、SQLite 及数据库导入统一记录实际分析范围，前端总览明确显示“已截断/当前范围完整”。
- **历史数据范围诚实标注**：迁移前创建且无法追溯来源规模的旧数据集标记为“范围未知”，不会被误显示为完整数据。
- **深度分析证据约束**：深度分析必须先成功执行只读 `run_sql` 才能输出事实结论，接口返回查询证据摘要，拒绝无依据的模型幻觉结论。
- **敏感信息保护**：发送给大模型的查询观测按字段名和邮箱、手机号、身份证号等值模式脱敏，原始数据集和用户查询结果不被改写。
- **调用成本与生命周期治理**：深度分析增加请求级调用次数、输出 Token 上限和每日额度预留；模型请求支持超时与客户端断开中止，DuckDB 增加内存限制、可中断查询超时和结果行数上限。
- **分析历史闭环**：普通分析与深度分析结果统一写入 `analysis_artifacts`，历史页支持回放 AI 分析、显示查询证据和图表。
- **统计口径修正**：显式百分号值在画像、SQL 查询和聚合中统一转换为比例，避免 `12%` 被误算为 `12`。
- **回归验证**：新增数据截断、敏感字段脱敏、只读 SQL 治理和 PostgreSQL 异步迁移回归覆盖。

## [v0.1.8] - 2026-08-20

### PostgreSQL 全量迁移校验、JSONB 兼容与异步执行加固

- **测试库完整迁移与全外键校验**：以 `data/chat.db` 重新执行 79 张表全量迁移；新增普通业务账号无法设置 `session_replication_role` 时的外键暂存、幂等恢复与逐条验证机制。迁移失败也会尝试恢复已暂存约束，避免库处于无外键状态。
- **可选的历史孤儿引用治理**：`REPAIR_ORPHAN_FOREIGN_KEYS=true` 只会将允许为空的失效 `messages.model_id` 与 `sessions.forked_from_message_id` 置空，不删除业务记录。当前测试库已修复 58 条失效模型引用和 1 条失效分叉消息引用，136 条外键均为有效状态。
- **JSONB Schema 与运行时同构**：新建 PostgreSQL schema 的结构化字段统一使用 JSONB；JSON 路径提取显式转换为文本；知识图谱别名检索/合并、长期记忆任务与 RAG 调试记录均兼容 node-postgres 返回的原生对象和数组。
- **类型对账与残留列修正**：`analysis_datasets.active_version` 改为 `BIGINT DEFAULT 1`，与 SQLite 数据类型一致；schema 对账工具识别 JSONB、pgvector 与合法的 PostgreSQL 整数类型，不再产生已确认物理类型的误报。
- **异步稳定性**：智能体队列的延迟调度与并发更新入口均捕获异步拒绝并记录错误；PostgreSQL seed 改为事务级 advisory lock，避免多实例启动重复写入默认提示词。
- **回归覆盖**：新增 JSONB DDL、JSON 路径、原生 JSONB 对象/数组反序列化与迁移关系完整性检查。测试库四级一致性、79 表 schema 对账和 136/136 外键验证均通过。

## [v0.1.7] - 2026-08-20

### PostgreSQL 向量类型、法规协作与异步触发器兼容性修复

- **pgvector Schema 与运行时查询对齐 (pgvector Schema & Runtime Alignment)**：
  - PostgreSQL Schema 生成器将 `knowledge_chunks`、`memories` 与 `regulation_articles` 的 `embedding` 列明确生成为 `vector`，与已迁移生产库的物理列类型一致；SQLite 权威 DDL 仍保持 TEXT 序列化语义。
  - 知识库候选召回和法规相似条文查询移除 SQLite 专属的 `embedding != ''` 判断，统一以 `embedding IS NOT NULL` 识别有效向量，避免 PostgreSQL 将空字符串转换为 vector 时抛出 `22P02`。
  - `pgvector` 现在被视为 Schema 初始化的必需扩展：扩展不可用时启动会返回明确错误；`pg_trgm` 仍允许降级为顺序扫描。
- **法规协作用户字段兼容 (Regulation Collaboration User Fields)**：法规批注与查阅日志查询不再读取已不存在的 `users.name` / `users.email`，显示名按 `nickname`、`deleted_username`、`username` 回退；保留 `user_email: null` 以兼容已有接口响应结构。
- **异步数据库触发器参数推断修复 (Async Database Trigger Parameter Typing)**：数据变更触发器推进水位线时改为通过 `claim_token = ?` 校验租约所有权，移除 PostgreSQL 无法推断参数类型的 `? IS NULL` 条件，避免轮询任务在已创建运行后因 `42P18` 失败。
- **回归验证与发布说明**：新增 pgvector DDL、法规向量/协作查询和数据库触发器持锁水位线回归测试；`npm run check`、`npm run lint` 与隔离 PostgreSQL 全量测试 `480/480` 均通过。本版本不要求对已完成迁移且向量列已为 `vector` 的生产库执行额外数据迁移。

## [v0.1.6] - 2026-08-20

### 工作流计划执行审计日志补齐、立即运行弹窗层级穿透与任务列表过滤状态残留修复

- **工作流与智能体计划执行全链路审计日志补齐 (Workflow & Agent Schedule Audit Logging)**：
  - **定时调度执行审计落库**：在 `server/services/agent-schedules.js` 的 `runDueAgentSchedules` 定时调度轮询中引入 `enqueueAuditLog`，计划任务触发入队执行时，自动将调度记录写入系统审计日志 `audit_logs`。
  - **自动化类型精准识别与关联追溯**：在审计日志中精准区分「工作流计划执行（DAG 模式/指定工作流）」与「计划任务执行（普通智能体计划）」，并在详情中完整记录任务 ID、计划 ID、计划名称及对应工作流 ID。
  - **手动运行与计划配置管理审计标准化**：在 `server/routes/agents.js` 的 `/agents/schedules/:id/run`、`POST /agents/schedules`、`PUT /agents/schedules/:id` 和 `DELETE /agents/schedules/:id` 中，全面标准化写入 `手动运行工作流计划`、`创建工作流计划`、`更新工作流计划` 与 `删除工作流计划` 等规范中文审计动作。
- **任务运行详情窗口层级优化与弹窗层叠穿透修复 (Run Detail Modal Z-Index & Overlay Stacking Fix)**：
  - **弹窗层级全面提级**：将工作区任务运行详情弹窗 `#agent-run-detail-modal` 的 `z-index` 从 `1910` 提升至 `2000`（同步更新 `client/chat/partials/workspaces/agent.html`、`client/chat/styles/workspaces/agent/agent-run-detail.css` 与 `client/chat/agent-run-detail.js` 中的 `ensureAgentRunDetailModalVisible`），确保其始终浮动在计划设置弹窗（`z-index: 1940`）与计划编辑弹窗（`z-index: 1950`）之上。
  - **立即运行交互流转平滑化**：在 `client/chat/agent-schedules.js` 的 `runAgentSchedule` 中，用户在计划弹窗内点击“立即运行”时，自动关闭底层的计划新建/编辑弹窗，任务执行详情窗口置顶聚焦呈现，彻底解决窗口被压在下方的视觉遮挡问题。
- **计划执行后任务记录列表过滤锁定与状态残留修复 (Schedule Runs Filter Lock & Auto-Reset Fix)**：
  - **立即运行全局工作台解绑**：修复 `client/chat/agent-schedules.js` 在点击“立即运行”后多余锁定 `scheduleId` 的缺陷，改为直接刷新全局任务列表，使得最新提交的计划运行任务立即出现在列表最顶部。
  - **筛选切换与数据加载自动重置**：在 `client/chat/agent-run-loaders.js` 的 `loadAgentRuns` 与 `client/chat/agents.js` 的 `bindAgentFilters` 中增加自动解绑机制，当用户在前端切换筛选下拉框（如从计划任务切换至“全部类型”、“自主任务”或“工作流任务”）或点击刷新时，自动重置内存中的 `agentScheduleFilterId` 变量，彻底解决之前其他历史任务被隐藏、必须 F5 刷新整个页面才能重新显示的问题。
- **任务运行记录列表与详情模型展示精准解析 (Task Run History Model Display & Multi-Model Resolution)**：
  - **动态模型与节点级模型精准解析**：在 `server/repositories/agent-runs.js` 的 `listRuns`、`getRunForUser`、`getRunById` 与 `listDeletedRunsForAdmin` 中增加 `enrichRunModelNames`，同时支持关联路由动态选择模型 `chosen_model_id` 以及工作流 DAG 节点中配置的特定大模型（如 `agent.llm` / `agent.content_review`），精准反查模型库名称并回填至 `model_name`。
  - **多模式与纯工具工作流语义化展示**：当工作流为纯工具编排时展示「无需模型 (纯工具)」；单节点使用大模型时展示具体模型名称；多节点调用不同大模型时展示「多模型 (N个)」或组合名称；自主任务展示所选模型或路由模型名称。
  - **前端表格与详情技术概览防空白兜底**：在 `client/chat/agent-run-loaders.js`、`client/chat/sidebar-search.js` 与 `client/chat/agent-run-detail.js` 中增加显示与悬停提示兜底，彻底解决工作流或特定任务在任务运行记录列表中模型列显示为空白或单一破折号的问题。
- **自动化测试回归覆盖与用例加固**：
  - 在 `tests/security-agent.test.js` 中新增针对到期工作流计划自动派发、任务入队及审计日志落库断言的完整测试用例。
  - 增加针对普通任务、工作流节点模型及纯工具工作流在 `listRuns` 中模型名称自动解析与富化的专项单测。
  - 修复 `server/services/agent-schedules.js` 中定时调度原子领占查询的健壮性。

## [v0.1.5] - 2026-08-20

### 用量设计一体化看板融合架构、CI 自动化 PostgreSQL 容器测试与测试运行器时区与并发竞态加固

- **用量统计、用量明细与审计报表三合一融合设计 (Usage, Details & Audit Report Integration Design)**：
  - **架构融合背景**：针对目前系统中「用量统计 (Usage Stats)」、「用量明细 (Usage Details)」与「审计报表 (Audit Reports)」三个功能面板分散、上下文切换繁琐及筛选维度割裂的问题，规划并启动「用量设计 / 统一用量中心 (Usage Analytics & Audit Center)」一体化看板融合设计。
  - **一体化看板交互规划**：
    - **宏观统计与趋势概览（融合原用量统计）**：统一展示全平台与部门维度的 Token 总消耗、调用频次、每日/每周/每月趋势折线图、模型调用占比饼图与峰值时段分析。
    - **多维明细下钻与实时穿透（融合原用量明细）**：在同一页面支持按部门/单位、用户、模型、调用类型与时间范围进行秒级下钻查询，实时展示每笔调用的输入/输出 Token、推理耗时、TPS 速率与计算成本。
    - **审计报表与一键导出归档（融合原审计报表）**：无缝整合部门用量横向对比、Top 活跃用户消耗排行，并提供一键 CSV 结构化导出（支持 UTF-8 BOM 编码，Excel 打开开箱即用无乱码），形成从“宏观监控 -> 微观下钻 -> 审计出表”的完整闭环。
  - **数据层与 API 聚合升级**：后端将逐步归并 `/api/stats/report`、`/api/stats/report/export` 与 `/api/stats/usage-breakdown`，利用 PostgreSQL 窗口函数与聚合索引消除重复多表查询，大幅降低数据库 I/O 负载。
- **GitHub Actions CI 持续集成 PostgreSQL 16 自动化服务容器 (CI PostgreSQL 16 Service Container)**：
  - 在 `.github/workflows/ci.yml` 中配置官方 `postgres:16` 服务容器与 `pg_isready` 健康检查探针，并在测试运行步骤中自动注入 `TEST_DATABASE_URL` 与 `DATABASE_URL`。
  - 在 `scripts/run_node_tests.js` 与 `scripts/run_e2e_tests.js` 中增加 CI 环境环境变量自动检测与回退兜底，彻底解决 Linux CI 运行器缺少数据库实例导致的 `Error: PostgreSQL tests require TEST_DATABASE_URL` 报错。
- **全局跨平台测试运行器时区隔离 (Global Test Runner Timezone Consistency)**：
  - 在 `.github/workflows/ci.yml` Job 级别全局注入 `TZ=Asia/Shanghai` 与 `PG_TIMEZONE=Asia/Shanghai`。
  - 在 `scripts/run_node_tests.js`、`scripts/run_e2e_tests.js`、`scripts/setup_pg_test_db.js` 与 `server/db/test-sync-worker.js` 测试同步客户端中统一显式执行 `SET timezone = 'Asia/Shanghai'`，彻底消除 Linux UTC 零时区宿主机因 8 小时时区差导致的时间回读断言失败（如 `2099-01-01` 被误解析为 `2098-12-31 16:00:00`）。
- **智能体并发队列单例与测试竞态加固 (Agent Queue Concurrency & Test Race Fix)**：
  - 在 `server/services/agent-runtime/index.js` 中修复 `getAgentQueue()` 单例获取逻辑，移除无条件的 `updateMaxConcurrent` 重置，防止单测中手动暂停队列（`updateMaxConcurrent(0)`）被意外覆盖为默认并发数导致的任务抢占。
  - 在 `tests/agent-runtime-lifecycle.test.js` 中前置并发限制时序，确保测试用例与全局监听队列互相隔离。
- **测试报告器升级为 Spec 结构化视图 (Spec Test Reporter with Summary)**：
  - 在 `scripts/run_node_tests.js` 中启用 `--test-reporter=spec`，在控制台最底部汇总清晰的错误用例名称、文件行号与完整堆栈差异，大幅提升排查效率。

## [v0.1.4] - 2026-08-19

### 认证退出与空会话体验优化、可观测性与系统监控布局增强、全链路审计中文化与 PostgreSQL 自动备份加固

- **退出登录单次生效流程修复 (Single-Click Logout Flow Fix)**：
  - 在 `server/auth.js` 中将 `/auth/logout` 纳入 `csrfMiddleware` 豁免白名单，解决登出时 CSRF 校验不同步导致的登出拦截。
  - 在 `server/routes/auth.js` 中优化 `/auth/logout` 路由鉴权逻辑，即便 Access Token 已过期也能安全销毁 Refresh Token、彻底清理所有鉴权 Cookie 并返回成功响应。
  - 在 `client/chat/auth.js` 中将 `window.logout` 改为异步函数，确保 `await` 登出请求执行完成后再清空本地存储并刷新页面，彻底解决需要点击两次退出的问题。
- **空会话静默初始化体验优化 (Quiet Empty Session Initialization)**：
  - 优化 `client/chat/engine-sessions.js` 中的 `selectSession` 逻辑，在初始加载恢复模式下若未查询到有效历史会话或会话列表为空，静默重置当前会话 UI 状态，杜绝向用户弹出误导性的“加载用户会话失败”错误提示。
- **慢查询与异常告警单行呈现与时间戳展示 (Single-Line Observability Rows with Timestamps)**：
  - 在 `client/chat/stats-monitor.js` 中为每条慢查询与异常告警记录增加时间戳（`formatDateToCN(item.created_at)`）展示，悬停可查看完整时间详情。
  - 在 `client/chat/styles/stats-monitor/stats-system-monitor.css` 中重构告警条目为紧凑的单行流式 Flex 布局，左侧整合事件标题、类型徽标、严重等级与来源，右侧整合执行耗时与发生时间，极大提升排查与可读性。
- **可观测性告警排除大模型正常推理耗时 (LLM Generation Slow Query Exclusion)**：
  - 在 `server/services/observability.js` 与 `server/services/model-runtime.js` 中默认关闭大模型正常推理耗时向慢查询与异常告警的记录，并在可观测性事件列表中默认过滤“模型端点慢响应”，消除大模型长文本输出对系统慢查询告警的干扰。
- **系统监控面板全量对齐 PostgreSQL 与标签截断修复 (PostgreSQL Health Monitor Alignment & Label Truncation Fix)**：
  - 在 `server/routes/admin-stats.js` 中彻底清理历史单机 SQLite 文件（`chat.db`）扫描，存储统计全面采用 PostgreSQL 真实库大小（`pg_database_size`）。
  - 将部署服务商与可观测性慢 SQL 来源默认值统一对齐为 `postgres` / `postgresql`。
  - 修复 `client/chat/styles/stats-monitor/stats-system-monitor.css` 与 `stats-monitor-common.css` 中监控项网格列宽，由固定 96px 改为自适应 `auto minmax(0, 1fr)`，彻底修复“PostgreSQL 统计”标签被截断为 `PostgreSQL 统...` 的样式缺陷。
- **系统审计日志与操作详情全面中文化 (Full Chinese Localization for Audit Logs & Details)**：
  - 重构 `server/routes/memories.js`、`server/routes/settings.js`、`server/routes/sessions.js` 中长期记忆任务调度、系统配置修改、上下文压缩等模块的遗留英文操作标识（如 `retry long-term memory jobs`、`UPDATE_RUNTIME_SETTINGS`、`compressed` 等），统一为规范中文。
  - 在 `server/audit-actions.js` 中完善操作映射字典与结构化详情格式化工具，实现所有历史与新增操作在审计日志界面的纯中文友好展示。
- **全模块操作审计日志补齐 (Comprehensive Module Audit Logging)**：
  - 在 `server/rag.js` 中全面激活 `auditRagAction` 与 `enqueueAuditLog`，完整覆盖知识库文档上传/启停/删除/批量重建/重试、知识集合管理与共享授权、标签管理、向量测试、召回反馈以及知识图谱实体/关系维护的全流程审计日志。
- **会话列表长标题与完整日期防重叠布局 (Sidebar Long Title & Full Date Auto Layout)**：
  - 在 `client/chat/styles/base/sidebar.css` 中重构 `.session-side` 为自适应弹性宽度（`flex: 0 0 auto; min-width: 42px; max-width: 86px;`），使得长日期（如 `2026/08/19`）能够自适应展开，超长标题自动在剩余宽度中截断省略，两者互不遮挡。
- **审计报表导出功能补齐 (Audit Report CSV Export)**：
  - 在 `server/routes/admin-stats.js` 中新增 `/api/stats/report/export` 接口，支持按部门、用户、预设时间周期与自定义起止日期筛选，导出包含概况元数据、每日 Token 趋势、用户消耗排行与部门消耗对比的结构化 CSV 报表，并记录操作审计日志。
  - 在 `client/chat/partials/settings/report.html`、`client/chat/styles/stats-monitor/stats-report.css`、`client/chat/stats.js` 与 `client/chat/app/main.js` 中新增「导出报表」按钮、样式与事件绑定，通过 `Pivot.exposeModule` 安全暴露 `exportReport`。
- **PostgreSQL 备份 pg_dump 自动探测与 Schema 范围保护 (PostgreSQL Backup Auto-Discovery & Schema Isolation)**：
  - 在 `server/services/maintenance.js` 中增强 `getPgDumpBin()`：未显式配置环境变量时，自动按常见安装路径（`C:\Program Files\PostgreSQL\18\bin\pg_dump.exe` 及 17/16/15 等）扫描定位 `pg_dump.exe`，避免 Windows 环境因未配置全局 PATH 导致的 `spawn pg_dump ENOENT` 报错。
  - 在备份参数中显式限定 `--schema=public`（可通过 `DB_BACKUP_SCHEMA` 配置），确保稳定备份系统业务库，不受测试环境临时 Schema 的权限干扰。
  - 在 `.env.example` 中补充了 `PG_DUMP_BIN`、`DB_BACKUP_SCHEMA`、`DB_BACKUP_RETENTION_DAYS`、`GPU_MONITOR_INTERVAL_MS` 等完整配置说明。

## [v0.1.3] - 2026-08-19

### PostgreSQL 运维备份与语法加固、E2E 环境隔离、Webhook 与 Electron 初始化异步等待修复、GPU 阈值容错与跨平台启动优化

- **Webhook 异步触发等待修复 (Webhook Dispatch Await Fix)**：
  - 修复 `server/routes/triggers.js` 中入站 Webhook 触发路由 `/workflow/:token` 缺少 `await` 的缺陷，在响应 HTTP 202 及记录操作审计前显式 `await dispatchWebhookTrigger(...)`。
  - 确保工作流运行记录（`runId`）创建并进入调度队列后再返回结果，消除异步时序竞争与误报成功的风险。
- **Electron 本地服务初始化等待修复 (Electron Local Server Init Lifecycle Fix)**：
  - 修复 `desktop/main.js` 中桌面客户端在嵌入式启动本地 Node.js 后端时未等待异步初始化完成的问题。
  - 引入 `desktop/local-server.js` 中的 `resolveInitializedServer` 辅助模块，显式 `await serverModule.initPromise`，保证在读取 HTTP 服务器实例与监听端口前完成 PostgreSQL 架构及底层连接初始化，杜绝启动阶段偶发的 `缺少 HTTP 服务器实例` 崩溃。
- **PostgreSQL 参数占位符安全转换 (PostgreSQL Placeholder Lexical State Machine)**：
  - 重构 `server/db/client.js` 中的 `toPostgresParams` 函数，构建完整的 SQL 词法状态机。
  - 精确识别并跳过单引号文本（含转义与单引号自转义）、双引号标识符、单行注释（`--`）、块注释（`/* ... */` 及多层嵌套块注释）和 PostgreSQL Dollar 引用（`$$` 与 `$tag$`），仅在可执行 SQL 表达式中将 `?` 替换为 `$1, $2, ...`，彻底避免参数占位符误伤字符串或注释中的问号。
- **E2E 独立 Schema、随机端口与服务隔离 (E2E Test Environment Isolation)**：
  - 增强 `tests/e2e/playwright.config.js` 与 `scripts/run_e2e_tests.js`，支持 `PIVOT_E2E_ISOLATED=true`。
  - 自动分配唯一的 PostgreSQL 独立测试 Schema（`pivot_e2e_*`）、临时数据隔离目录与空闲随机端口（`PIVOT_PORT=0`），禁用 `reuseExistingServer`，确保 Playwright E2E 测试环境与本地正在运行的开发或生产服务 100% 互不干扰，并在测试结束后自动执行 Schema 与临时目录的资源清理。
- **PostgreSQL pg_dump 备份、超时与轮转清理 (PostgreSQL pg_dump Backup Engine & Retention)**：
  - 重构 `server/services/maintenance.js` 与 `scripts/backup_db.js`，全面接入 PostgreSQL 原生 `pg_dump` 自定义格式（`--format=custom --no-owner --no-privileges`）备份引擎，替换旧单机 SQLite 备份逻辑。
  - 引入 `PG_DUMP_BIN`、`PG_DUMP_TIMEOUT_MS`（默认 15 分钟）与安全环境变量隔离注入（基于 `DATABASE_URL` 解析提取 `PGHOST`、`PGPORT`、`PGDATABASE`、`PGUSER`、`PGPASSWORD` 与 SSL 参数，避免命令行参数泄露凭据）。
  - 支持按保留天数（`DB_BACKUP_RETENTION_DAYS`）与版本数量（`DB_BACKUP_MAX_VERSIONS`）自动轮转清理 `pivot_backup_*.dump` 与历史备份文件，备份失败时自动清理无效文件并安全回滚。
- **PostgreSQL ANALYZE 运维替代旧 SQLite VACUUM 配置 (PostgreSQL Maintenance Alignment)**：
  - 清理 `server/services/maintenance.js` 中已废弃的 SQLite 增量清理配置 `getIncrementalVacuumPages`。
  - 统一收敛为 PostgreSQL 推荐的 `ANALYZE` 统计信息采集与执行计划优化，消除历史遗留参数对运维监控的混淆。
- **跨平台 npm 启动脚本修复 (Cross-Platform npm Scripts)**：
  - 修复 `package.json` 中的 `start` 与 `dev` 脚本，移除专用于 Windows cmd 的 `chcp 65001 >nul &&` 前缀。
  - 统一为标准且跨平台的 `node server/index.js` 和 `nodemon --ignore client server/index.js`，保证在 Linux、macOS 和 Docker 容器环境下的兼容性与开箱即用体验。
- **GPU 显存阈值校验与安全默认值回退 (GPU VRAM Threshold Validation & Fallback)**：
  - 在 `server/services/gpu-monitor.js` 中新增 `normalizeGpuThresholds`，严格校验显存警戒水位与恢复水位的递增约束关系（`safe < critical < reject` 且 `recover < reject`）。
  - 当外部环境变量配置非法、乱序或超出范围（0~1）时输出友好中文告警日志并自动回退到安全的系统默认值（85% / 95% / 97% / 90%），防止 GPU 监控异常导致 AI 并发控制失效。
- **专项回归测试闭环 (18 项专项测试 100% 通过，全量 476/476 项测试通过)**：
  - 新增与扩展 `tests/triggers-route.test.js`、`tests/db-postgres-contracts.test.js`、`tests/security-gpu-monitor.test.js`、`tests/server-lifecycle.test.js` 等专项回归测试套件。
  - 18 项专项测试全部通过，全量自动化测试套件通过数提升至 476/476（100% 通过，0 失败，0 告警）。

## [v0.1.2] - 2026-08-19

### PostgreSQL 生产就绪与启动权限容错强化、日志用户身份修复、全量测试套件 100% 通过与死代码精简

- **PostgreSQL DDL 初始化幂等与属主权限（42501）容错强化**：
  - 重构 `server/db/schema/pg.js` 中的 `pivot_json_extract` 容错 JSON 提取函数，采用 `DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_proc ...) THEN CREATE FUNCTION ...` 幂等结构，规避普通应用账号在 `CREATE OR REPLACE` 时触发的属主校验拦截（`42501: 必须是函数 pivot_json_extract 的属主`）。
  - 移除非必要的严格事务包围，对建表、补遗留列、外键、索引、注释等各个 DDL 执行阶段细化错误捕获（跳过 42501 非属主警告及 42P07/42701 已存在异常），确保以普通应用用户连接时服务依然能够安全、稳定启动。
  - 新增 `scripts/fix_pg_ownership.js` 工具脚本，支持一键将 PostgreSQL 数据库中的表、序列、函数及 Schema 属主批量转移至应用用户。
- **请求日志序列化器与用户认证身份提取修复**：
  - 修复 `server/logger.js` 中的 `serializers.req` 提取逻辑，从 `req.user || req.raw?.user` 提取真实登录用户名，彻底修复所有已认证请求日志误标为 `(用户: 访客)` 的问题。
  - 优化控制台单行输出格式，支持保留业务主动记录的自定义详细日志消息（如模型测试成功输出）。
- **全量测试套件 100% 通过（472/472）与纯异步稳定性闭环**：
  - 扩展全栈中英文错误断言兼容（`model-forwarder.js`、`enterprise-deployment.test.js`、`security-builtin-mcp.test.js`、`security-desktop-update.test.js`、`access-database.js`）。
  - 全面引入高频异步日志写队列的显式落库等待机制（`flushAllWrites()`），消除测试环境下并发读取日志表的时序竞争。
  - 修复智能体 DAG 与运行时生命周期状态机：`updateRun` 引入 `canTransitionAgentRunStatus` CAS 重试保护，`cancelAgentRun` 补齐关键操作的 `await` 与 AbortController 中断状态校验，`runAgent` 执行前后双重检验 `TERMINAL_STATUSES` 终态保护，避免并发取消/完成被假错误覆盖。
  - 修复文档处理（`document-processing`）PDF 异步拆分、合并、旋转、删页、重排的输出注册与 `touchProgress` 进度回调，避免异步回调冲刷覆盖已达 `succeeded` 的终态任务。
- **代码库清理与死代码精简（Dead Code Elimination）**：
  - 彻底删除已无任何引用的原型文件 `server/db/schema/enterprise-pg.js`。
  - 精简 `server/services/maintenance.js` 中遗留的 SQLite `db.exec` / `db.backup` 历史死代码分支，统一收敛至标准的 PostgreSQL `ANALYZE` 统计更新。
  - 规范 `.env` 与 `.env.example`，清理冗余无用的 `DB_CLIENT` 声明以及底部的重复注释，统一以 `DATABASE_URL` 为核心连接配置。

## [v0.1.1] - 2026-08-19

### 全系统工程全量中文化重构、控制台日志汉化美化、Cookie 与 401 轮询优化及代码质量闭环

- **全栈中文化重构与规范对齐（Localization & Standard Alignment）**：
  - 依照《开发规范.md》第 8 节（注释、命名、提示与日志）标准，对全栈代码库（`server/`、`client/`、`scripts/`、`desktop/`）进行全面深度扫描与 100% 中文化改造。
  - **后端异常与安全提示全面汉化**：重构出站安全沙箱（SSRF、私有 IP 拦截、域名白名单）、MCP 工具体系（数据分析、报表处理、即时通讯、文档解析、可视化输出）、数据库 SQL 语法与表/列白名单治理、模型转发与超时熔断、智能体 DAG 调度与运行中断等全部异常文本。
  - **前端交互提示与控制台日志汉化**：全面汉化前端 Toast 提示、Confirm 确认弹窗、动态脚本加载失败提示、主工作区恢复失败捕获及控制台告警输出，消除残留英文与转义乱码。
  - **代码注释与技术设计中文化**：重构关键技术路径的代码注释（内存缓存、上下文窗口 Token 预算、DuckDB/Parquet 流式导出、SSE 背压与长连接心跳等），提升代码可读性与可维护性。
- **控制台日志中文化格式化与美化（Chinese Terminal Pretty Logging）**：
  - 重构 `server/logger.js`，开发与运行环境下控制台实时输出自动转换为友好、直观的中文格式化排版（包含中文 HTTP 方法、路径、状态码文案、耗时与用户身份如 `[访客]` 等），大幅提升排查效率与开发体验。
  - 保留 `logs/pivot.log` 原始高性能 JSON 结构化日志，确保与 ELK/Loki 等集中式日志分析平台 100% 兼容。
- **认证 Cookie 清理与 Express v5 废弃 API 消除（Auth & Deprecation Fixes）**：
  - 修复 `server/auth.js` 与 `server/routes/auth.js` 中 `res.clearCookie` 传递 `maxAge` 引起的 Express 废弃警告（`Express deprecated res.clearCookie: Passing "options.maxAge" is deprecated`），规范 Cookie 清除选项。
  - 优化前端 `client/chat/app-workspaces.js` 与 `config.js` 的 401 拦截处理逻辑，确保在 Token 过期或未认证时立即清理失效凭据并彻底停止轮询，彻底杜绝 60 秒重复 401 轮询现象。
- **代码质量与全量自动化校验闭环（Zero Lint & 100% Check Passed）**：
  - 修复遗留的未定义变量与异步调用缺失（`getModelDailyUsageAsync`、`whereSql`、`Sqlite`、`datasetName` 等），实现 `npm run lint` **0 错误、0 警告**。
  - CLI 检查工具链（`scripts/check_*.js`）全面汉化输出，`npm run check` 453 个 JS 文件与静态资产规范检查 **100% 通过**。

## [v0.1.0] - 2026-08-18

### 核心数据库纯 PostgreSQL 架构全面升级与全链路异步化、数据字典元数据注释与生产级核验保障

- **核心数据库纯 PostgreSQL（PostgreSQL 14+/17+）架构升级**：彻底解除单机 SQLite 数据库依赖与双模式兼容包袱，全面切换至纯 PostgreSQL 关系型数据库架构。仅保留 DuckDB 作为独立的高性能列式内存数据分析与处理引擎。
- **全量 79 张表中文元数据注释字典（Data Dictionary）**：在 `server/db/schema/comments.js` 中新增覆盖系统全部 79 张核心业务表及所有核心业务字段（包含用户身份、权限治理、会话消息、思维链 Thought、Token 用量与成本、RAG 向量与图谱、智能体 DAG 工作流、MCP 工具服务与受控凭证）的原生中文注释字典，在初始化建表时自动执行 `COMMENT ON TABLE` 与 `COMMENT ON COLUMN`，无缝支持 Navicat、DBeaver、DataGrip 等企业级数据库治理客户端直接查阅。
- **全链路数据访问层与异步 Promise API 改造**：
  - 全面重构 `server/repositories/`、`server/services/` 和 `server/routes/`，彻底消除所有遗留的同步 `db.prepare` 与 SQLite 方言调用，全链路基于 `server/db/client.js` 导出的异步 Promise 接口（`query`、`queryOne`、`execute`、`transaction`）进行高并发连接池访问。
  - 深度重构数据分析服务（`server/services/data-analysis/`）、知识图谱拓扑与实体合并（`server/services/knowledge-graph.js`）、长期记忆管理（`server/services/long-term-memory/`）、存储垃圾回收与日志归档（`server/services/storage-gc.js`、`server/services/maintenance.js`）、MCP 工具治理与工作流凭据（`server/services/mcp-client.js`、`workflow-credentials.js`、`share-targets.js`）及指标统计（`server/metrics.js`）。
- **SQL 方言与参数化标准收口**：
  - 消除 SQLite 专有语法（`UPDATE OR IGNORE` 升级为 PostgreSQL 标准防冲突更新，标量 `MAX`/`MIN` 升级为 `GREATEST`/`LEAST`，时间间隔统一收口为 `(? || ' days')::interval`）。
  - 修复时间字段（`timestamptz`）与空字符串比较及 `LIMIT` 参数类型绑定错位，杜绝潜在类型转换异常。
  - 智能体运行时后台周期性自愈与巡检（`startAgentRecoveryRunner`）进行日志降噪优化，仅在有实际异常任务恢复时记录 `INFO` 日志。
- **自动化迁移、一致性校验工具链与全量自动化测试**：
  - 提供完整的初始化（`scripts/setup_pg_db.js`）、拓扑感知数据迁移（`scripts/migrate_sqlite_to_pg.js`）、四级一致性深度核验（`scripts/verify_pg_migration.js`）与表结构对账（`scripts/diff_schema_sqlite_pg.js`）工具链。
  - 全端通过 236 个服务端模块加载校验（`OK: 236 ERRORS: 0`）与全部 21 项业务端点自动化集成测试（通过率 100%）。

## [v0.0.269] - 2026-08-18

### 生产环境 PostgreSQL 双模式迁移全栈就绪、全链路数据访问层异步化改造与元数据字典注释系统

- **生产级 SQLite ➔ PostgreSQL 双模式无损迁移就绪**：依据《Pivot生产环境迁移PostgreSQL实施方案》(v2.2)，在 `server/db/schema/pg.js` 中构建基于单源事实的动态方言转换引擎，实现全量 79 张业务表的 `BIGINT GENERATED ALWAYS AS IDENTITY` 自增序列、`TIMESTAMPTZ`（东八区统一绑定）、`DOUBLE PRECISION`、外键依赖解耦与后置幂等创建，并部署 `pg_trgm` GIN 索引与 IMMUTABLE 容错 JSON 提取函数 `pivot_json_extract`。
- **全量 79 张表中文元数据注释字典（Data Dictionary）**：在 `server/db/schema/comments.js` 中新增覆盖全部 79 张业务表及其核心业务字段（包含用户角色、消息 thought 思维链、Token 用量、RAG 向量、智能体 DAG、MCP 工具及工作流凭证）的权威中文注释，在 PostgreSQL 初始化与建表时自动执行 `COMMENT ON TABLE` 与 `COMMENT ON COLUMN`，支持 Navicat / DBeaver / DataGrip 等客户端直接查阅结构含义。
- **全链路数据访问层（Repository）与应用层异步化（Promise API）改造**：
  - 全面重构 `server/repositories/`（`sessions.js`、`agent-runs.js`、`agent-workflows.js`、`knowledge.js` 等），统一采用 `server/db/client.js` 导出的异步 Promise 接口，支持 SQLite 与 PostgreSQL 双模式动态参数绑定（`?` 自动转换 `$1, $2`）。
  - 服务层与路由层（`server/services/chat-messages.js`、`chat-errors.js`、`agent-runs.js`、`agent-runtime/index.js`、`agent-dag-runtime.js`、`agent-workflows.js`、`server/routes/agents.js`、`routes/chat/`、`routes/sessions.js`）全面实现正确的 `async/await` 调用，彻底清零未 await 导致的 Promise 穿透与对象属性读取异常。
- **双模式写队列与服务启动生命周期管理**：在 `server/services/db-write-queue.js` 中实现双模式路由——SQLite 模式下路由到批量串行写入队列以消除文件锁竞争，PostgreSQL 模式下直接通过连接池并发高效入库；在 `server/index.js` 中集成 `initPostgresDatabase`，保证在服务启动监听前完成 PostgreSQL Schema 与基线迁移就绪。
- **自动化迁移与四级一致性核验工具链**：
  - 新增 `scripts/setup_pg_db.js`（数据库初始化与 pgvector / pg_trgm 扩展安装）。
  - 新增 `scripts/migrate_sqlite_to_pg.js`（基于 79 表拓扑依赖、主键游标 Keyset 分页、65535 参数防爆、分表独立事务与序列重置的数据搬迁引擎）。
  - 新增 `scripts/verify_pg_migration.js`（行数、主键范围、核心文本 SHA-256 哈希比对与孤儿数据关系排查的四级深度核验工具）。
  - 新增 `scripts/diff_schema_sqlite_pg.js`（两端表结构与列类型逐项对账工具）及 `scripts/truncate_all_pg_tables.js`（重跑演练重置工具）。
- **质量门禁与全量测试套件 100% 通过**：新增 `tests/db-dual-mode-contracts.test.js` 双模式合约测试；通过全量 456 个 JS 文件语法检查（`npm run check`）、ESLint 0 错误 0 警告（`npm run lint`）、双模式合约测试及全量 Node.js 测试套件（`npm run test:all`，共 471 项测试 100% 全部通过）。

## [v0.0.268] - 2026-08-17

### 聊天输入区能力聚合优化、文件夹上传与工具白名单精细化过滤

- **输入区「+」聚合菜单与操作降噪**：在 `client/chat/partials/workspaces/chat-shell.html` 与 `client/chat/styles/base/input.css` 中重构输入区左侧操作栏，将附件上传、知识库检索 (RAG) 与工具库调用 (MCP) 统一收口至 `+` 悬浮菜单（`#chat-tools-menu-btn` 与 `#chat-tools-menu-panel`），优化 ARIA 语义与无障碍键盘交互，消除操作按钮横向平铺造成的界面视觉干扰。
- **文件与文件夹层级上传及安全路径清洗**：附件上传子面板支持「选择文件」与「选择文件夹」双选项，并在 `server/routes/attachments.js` 中新增 `normalizeAttachmentRelativePath`，保留最深 24 层有效相对目录层级并自动清除路径穿越段（`..`），使上传的附件能完整呈现文件夹结构与相对路径。
- **输入框拖拽与剪贴板文件原生直传**：在 `client/chat/engine-attachments.js` 中接入输入框 Drag & Drop 与 Clipboard Paste 监听，支持递归解析拖入的文件夹目录树（`collectDroppedEntryFiles`）及剪贴板图片/文件，直接入队附件处理流程；同步移除输入区冗余的拖拽提示文案。
- **知识库（RAG）筛选二级抽屉式面板**：知识库配置项支持展开独立的二级浮层面板，内置知识库服务实时就绪状态检测（`#chat-rag-readiness`）、专题库与标签筛选、关键词快速搜索及「重置/完成」快捷操作，并支持自适应视口安全定位（`positionChatToolSubpanel`），防止窄屏与边缘溢出。
- **工具库（MCP）模式切换与精细化工具白名单**：工具库配置面板支持「模型自动选择」与「手动限制范围」双模式；手动模式支持工具搜索、全选/反选与精准勾选，前端实时计算并展示已选工具数（`#chat-mcp-tool-summary`）；在 `server/services/chat-preflight.js` 与 `server/services/chat-context-assembler.js` 中新增 `mcpToolAllowlist` 解析与白名单过滤，确保仅向模型提供用户明确授权的工具。
- **长会话消息列表虚拟滚动双向浏览增强**：优化 `client/chat/message-virtualizer.js` 的初始渲染与滚动边界计算，确保超长会话加载后精准锚定最新消息，且向上/向下滚动平滑连贯。
- **自动化测试与 E2E 验证覆盖**：新增 `tests/chat-input-menu.test.js` 覆盖文件夹路径清洗、MCP 工具白名单过滤、请求状态构建与前端 DOM 契约；更新 `tests/e2e/smoke.spec.js` 覆盖虚拟滚动和二级面板定位及交互计数，全量 467 项测试 100% 通过。

## [v0.0.267] - 2026-08-17

### 应用层全景性能加固、RAG 向量批处理与 RRF 混合重排、长连接心跳防断连及消息列表虚拟滚动

- **SSE 流式通信 Keep-Alive 心跳防断连**：在 `server/services/sse-response.js` 与 `server/streaming.js` 中引入 `createSseResponseWriter` 统一调度与心跳计时器（默认 15s），空闲时自动推送 `: keep-alive\n\n` 注释帧，彻底消除大模型深度思考（Thinking/Reasoning 模式）与 Agent 长耗时工具调用期间企业内网 Nginx / VPN 代理 15~30s 无数据强制断连隐患。
- **Embedding 向量请求动态批处理与显存保护**：在 `server/services/rag-index/embedding-client.js` 中引入 `buildEmbeddingInputBatches` 批量切分与并发槽位控制（默认单批 16 块 / 12000 tokens），大幅减少 90% HTTP 网络开销，彻底杜绝小显存/本地 GPU 发生 OOM。
- **RAG 混合检索 RRF (Reciprocal Rank Fusion) 倒排融合**：在 `server/services/rag-index/index.js` 中引入无量纲 RRF 排序重排，使关键词 FTS 召回与语义向量召回科学归一融合，大幅提升专业术语和长尾专有名词的命中准确率。
- **DAG 跨节点大对象传递内存深拷贝压制**：在 `server/services/agent-dag-runtime.js` 中引入结构化记录分页持久化（`preparePersistedDagOutput`）与按需裁剪，避免巨型数据全量深拷贝引发的堆内存剧烈抖动与 V8 GC 停顿。
- **DAG 节点执行看门狗超时硬熔断**：在调度层增加 `nodeTimeoutMs` 硬中断看门狗与 `withTimeout` 机制，防止节点挂起卡死全局工作流并发槽位。
- **前端长会话消息列表虚拟滚动 (Virtual Scroll)**：在 `client/chat/message-virtualizer.js` 中实现基于 `ResizeObserver` 的可变高度消息虚拟化挂载，超长会话（数百轮）依然维持 60fps 丝滑渲染与极速打字响应。
- **架构工程规范定稿**：在项目根目录下完成编制并定稿《Pivot全自主Agent改造方案设计.md》(v4.0.0) 与《Pivot生产环境迁移PostgreSQL实施方案.md》(v2.1.0)。

## [v0.0.266] - 2026-08-16

### 用户输入框停止按钮位置统一与上下文用量双重浮层样式优化

- **停止生成按钮位置统一**：调整 `client/chat/partials/workspaces/chat-shell.html` 中的按钮顺序，将停止生成按钮（`#stop-btn`）移至模型选择器右侧，与发送按钮（`#send-btn`）保持同位替换，彻底解决生成过程中操作按钮在模型名称左右跳动的问题。
- **上下文用量双重浮层样式消除**：移除 `context-usage-pill` 上冗余的原生 `title` 属性及 JS 赋值，统一使用 `data-tooltip` 与自定义 CSS 毛玻璃暗色浮层，并补全 `aria-label` 保障无障碍访问，彻底消除浏览器原生与自定义 tooltip 堆叠问题。

## [v0.0.265] - 2026-08-16

### 全局参数增加「会话记忆压缩阈值」可视化配置与前后端运行时动态打通

- **全局参数可视化配置接入**：在管理后台「全局参数」页面的【上下文预算】分组中增加「会话记忆压缩阈值 (Token)」（`memory_threshold`）输入框，支持 `12K`、`32K`、`16000` 等人机友好格式输入与回显，并在 `hintMap` 中完善字段说明与取值范围提示。
- **上下文预算栅格布局优化**：调整 `.global-params-context-row` 为对称两列栅格，将「全局上下文窗口」与「会话记忆压缩阈值」、「预留输出」与「附件注入上限」分行成对排布，提升管理后台参数排版美观度与易用性。
- **运行时配置服务集成**：在 `server/services/runtime-settings-defs.js` 中将 `memory_threshold` 注册进全局运行时配置系统（默认值 `12000`，范围 `256` ~ `10000000`，单位 `tokens`），并通过 `getGlobalContextRuntimeConfig` 统一导出。
- **会话压缩触发动态生效**：重构 `server/llm.js` 中的 `getMemoryThreshold()`，优先从运行时上下文配置中实时读取 `memoryThreshold`，确保管理员在管理后台修改保存后即时生效，无需重启服务。
- **测试用例与版本同步**：在 `tests/security-chat/context-settings.js` 中补充 `memory_threshold` 运行时保存、回读与预算计算单测；启用新版本 `v0.0.265`，全量执行 `npm run version:sync` 完成版本号同步与门禁校验。

## [v0.0.264] - 2026-08-12

### 全面代码规范审查、ESLint 全局变量补全、依赖安全漏洞修补与 Agent 取消机制优化

- **ESLint 全局变量补全与代码规范修复**：在 `eslint.config.js` 的 `nodeGlobals` 与 `browserGlobals` 中补全 `'AbortSignal': 'readonly'` 声明，解决 Node.js 20+ 环境下全局 `AbortSignal` 静态 API 误判 `no-undef` 的问题；重构 `client/chat/agent-run-utils.js` 中 `buildAgentRunTaskTooltip` 的未使用参数，消除 `no-unused-vars` 警告，实现 `npm run lint` 0 Error / 0 Warning 打卡。
- **高危依赖安全漏洞修复**：升级修补生产依赖 `js-yaml`（CVE-2026-59870 / GHSA-5p4m-2wfm-xmqj），通过 `npm run audit:policy` 依赖安全门禁策略校验，安全隐患完全清零。
- **Agent DAG 运行时取消机制增强**：在 `server/services/agent-dag-runtime.js` 中增强多节点并发 `AbortSignal.any` 组合防范与类型保护，确保在各类 Node 运行时与父级取消信号下具备完善的降级兜底能力。
- **智能体单元测试断言修补**：修补 `tests/security-agent.test.js` 中 `runAgentDag` 调用断言正则，使其与 `agent-runtime` 实际调用的 `getAgentRuntimeDeps(runController.signal)` 签名保持一致，确保 GitHub CI 流程中全量 453 项测试 100% 通过。
- **版本同步与全量校验**：启用新版本 `v0.0.264`，通过 `npm run version:sync` 自动同步 `package.json`、`package-lock.json` 和 `README.md` 版本号，全量静态规范与依赖门禁校验全部通过。

## [v0.0.263] - 2026-08-12

### 智能体运行取消与超时 AbortSignal 传播、CAS 状态并发治理与触发器鲁棒性增强

- **取消与超时 AbortSignal 全流程传播**：增加运行超时与用户取消的 `AbortSignal` 传播，全面覆盖模型调用、流式调用、DAG 节点、HTTP 和 MCP 工具；修复 `withTimeout` 机制，在超时或父级取消时立即 `reject`，不再等待底层 Promise 自行结束。
- **运行状态 CAS 更新与恢复器治理**：增加运行状态 CAS (Compare-And-Swap) 更新，有效规避审批、恢复、取消等并发操作互相覆盖；恢复器接管 stale run 时，会先 CAS 标记失败，再中止当前进程内仍在运行的协程；周期恢复 runner 改为单例启动，避免重复创建定时器。
- **审批与延迟请求原子决策**：审批/延迟请求增加去重和原子决策处理；软删除运行记录后允许复用 `dedupe_key`。
- **触发器高可用与隔离防护**：触发器增加轮询租约、结构化数据库结果读取、数值水位线处理和文件扫描异常隔离。
- **共享工作流脱敏与前端状态显示**：共享工作流继续加强敏感字段、URL 参数和嵌套对象的脱敏逻辑；前端补充 `awaiting_approval` 状态显示。
- **版本同步与规范校验**：启用新版本 `v0.0.263`，同步更新 `package.json`、`package-lock.json` 与 `README.md` 版本号。

## [v0.0.262] - 2026-08-10

### 统信 UOS 与国产芯片架构（麒麟 9000 & 龙芯 3A6000）客户端打包支持

- **统信 UOS 平台打包配置**：在 `package.json` 的 `build` 配置中新增 `linux` 和 `deb` 部署选项，支持生成 `.deb` 及 `.AppImage` 客户端包。
- **多架构国产芯片支持（历史记录）**：曾新增 `dist:arm64` 与 `dist:loong64` 构建脚本；后续兼容性复核确认 LoongArch64 缺少完整 Electron/原生依赖发布链，现已由当前版本明确阻断 `dist:loong64`，避免误发不可运行包。
- **构建脚本扩展**：重构 `scripts/package_desktop.js` 中的参数解析与产物离线归集逻辑，自动收集 `.deb` 安装包并生成 `SHA256SUMS.txt` 校验和。
- **离线内网部署文档（历史记录）**：曾编写《统信 UOS 与龙芯 3A6000 客户端打包指南》；当前版本已根据实际兼容性复核收敛为 AMD64/ARM64 支持，并明确 LoongArch64 尚未发布。
- **版本同步与规范校验**：启用新版本 `v0.0.262`，通过 `npm run version:sync` 自动同步 `package.json`、`package-lock.json` 和 `README.md` 版本号，静态规范检查全部通过。

## [v0.0.261] - 2026-08-09

### 自动化模块全量弹窗防误触与点击遮罩交互收口

- **自动化全量弹窗禁止点击遮罩关闭**：对自动化模块（包含新建/编辑任务、高级设置、任务详情、编辑工作流、新建/分享/依赖配置/版本控制工作流、工作流与计划任务编辑器、评估测试用例、结果版本记录以及 DAG 输入/统计图向导与源码/契约编辑器）共 18 个弹窗进行交互防护收口，禁止通过点击遮罩或弹窗内空白区域关闭弹窗，避免误触导致未保存配置丢失。
- **显式按键交互统一**：所有弹窗统一收口为仅能通过点击显式的「关闭」或「取消」按钮进行关闭，确保用户操作预期明确、数据输入安全。
- **全量代码规范与静态检查**：全量 435 个前端 JS 文件语法校验通过，`check:text`、`check:safe-html`、`check:chat-assets`、`window-globals` 及单测通过。

## [v0.0.260] - 2026-08-09

### 新建任务面板网格布局优化与弹窗按钮样式统一

- **高级配置 3x3 网格排版**：新建任务面板的高级设置重构为 3 列网格结构，统一整合允许工具库工具、运行模式、工具审批、模型路由、最大执行轮次、失败重试、上下文来源与单次 Token 上限，大幅收紧弹窗垂直高度，彻底消除多余的垂直滚动条。
- **补充说明全宽布局**：在保持高级配置 3x3 紧凑布局的同时，使「补充说明」输入域单独独占整行宽度（`grid-column: 1 / -1`），平衡布局密度与输入体验。
- **任务运行记录操作项更名**：将自动化任务运行记录表格操作列的「修改」按钮统一调整为「编辑」，与全局编辑交互语言保持一致。
- **编辑弹窗头部与按钮位置优化**：移除「修改任务」与「编辑工作流」弹窗右上角的冗余关闭按钮；将「修改任务」弹窗底部的取消与保存按钮统一调整为靠右对齐。
- **版本同步与全量校验**：启用新版本 `v0.0.260`，完成 `package.json`、`package-lock.json` 与 `README.md` 版本号自动同步，全量 `npm run check` 校验通过。

## [v0.0.259] - 2026-08-09

### 富文本校对节点模型选择与参数契约完善

- **模型选择恢复可用**：工作流向导为 `agent.content_review`、`agent.llm` 和 `agent.delegate` 统一提供当前账号可用模型选择器，合并智能体模型与聊天模型缓存，去重并过滤 Embedding 及其他用户私有模型；无可用模型时显示明确的禁用状态，不再退化为自由文本输入。
- **富文本输入配置完善**：`records` 支持 `structuredContent`、`rows`、`data`、数组和上游变量引用，结构化值以 JSON 文本框编辑，保存时保留模板变量并解析 JSON；数值参数展示并执行 Schema 的最小值、最大值约束。
- **节点字段语义补齐**：补充记录、模型、字段映射、分块、并发、摘要长度和报告标题的中文标签、说明与占位提示；模型摘要显示实际模型名称和上下文窗口，失效模型配置保留并明确标记。
- **运行时契约统一**：富文本节点兼容 `model`、`modelId` 和 `model_id`，自定义报告标题同时应用于摘要、完整 Markdown 报告和任务产物；输出 Schema 补齐统计、逐条问题、产物和警告字段，便于下游节点稳定引用。
- **回归验证**：新增富文本节点前后端契约测试，覆盖模型可见性、选择器空态、结构化记录、数值边界、模型别名和报告标题；全量 Node 测试 `452/452`、`npm run check`、`npm run lint` 全部通过。

## [v0.0.258] - 2026-08-08

### 自动化工作流编辑、共享权限树与执行轮次治理收口

- **工作流基本信息独立编辑**：资产中心新增工作流名称和简介编辑弹窗，所有者可以在不创建新编排版本的情况下更新基本信息；详情页回归节点编排和版本管理职责。
- **执行轮次按运行模式限制**：最大执行轮次继续支持自动策略，标准模式默认 30 轮、深度模式默认 60 轮、审查模式默认 50 轮；人工配置会按当前模式上限校正，流式工具调用与 JSON 回退继续共享总轮次。
- **三类共享弹窗统一目标树**：工作流、工具库和知识库分享统一按单位展开个人账号，勾选单位会联动该单位用户；单位权限和个人权限使用同一套选择、清空、禁用和保存语义，单位授权不会再重复提交其下个人账号。
- **共享操作区布局修复**：修复分享弹窗中“全选 / 全不选”被通用单位标题样式覆盖而纵向排列的问题，三个弹窗的操作按钮固定为横向且不换行，窄屏下仍保持可读布局。
- **共享前端组件复用**：新增共享目标树公共脚本，统一工具库和知识库的渲染、单位联动、全选/全不选和安全转义逻辑，并保留工作流现有权限边界。
- **回归验证**：补充工作流元数据、运行模式上限、共享树结构和按钮布局静态断言；全量 Node 测试 `447/447`、安全智能体专项 `52/52`、Chat asset check、ESLint 和差异检查通过。

## [v0.0.257] - 2026-08-07

### 共享资源治理与工作流接收者依赖闭环

- **工作流支持按个人共享**：工作流、知识库专题库和数据库 MCP 连接统一支持单位范围与指定个人白名单；管理员可选择全体成员，普通用户仍受所属单位权限约束，也可以在无所属单位时直接授权指定个人。
- **共享工作流依赖映射**：接收者首次运行共享工作流前，可以为模型、工具和凭据选择当前账号可用的等价资源；映射只保存授权引用，不返回或下发凭据明文，工作流中的直接 HTTP 敏感值会被脱敏并禁止共享或发布。
- **发布版本绑定与失效提醒**：接收者依赖映射固定到已发布版本；所有者发布新版本后，旧映射自动标记为过期并阻断运行，接收者重新确认后才能继续执行。计划任务、触发器、子工作流和运行时均复用同一依赖预检。
- **只读接收边界收紧**：知识库接收者保留集合、文档读取和检索能力，但文档启停、归属调整、删除及共享设置均拒绝；MCP 接收者可查看服务器与工具列表，仅保留治理后的只读数据库工具，服务器编辑及非只读工具调用均拒绝。
- **共享权限界面完善**：工作流、知识库和 MCP 分享弹窗增加个人账号选择、单位/个人全选与清空、范围摘要和只读状态；共享工作流接收者在自动化资产中心和运行预检中可直接打开依赖配置面板。
- **数据结构与兼容迁移**：新增 `allowed_user_ids` 共享字段和 `agent_workflow_dependency_bindings` 依赖绑定表，补充索引及旧数据库迁移；所有列表、详情、检索、缓存和 Graph-RAG 路径统一应用个人/单位可见性过滤。
- **回归验证**：新增隔离 HTTP JWT 权限测试和 Playwright 工作流版本失效 API 测试；Node 全量测试 `446/446`、接收者权限 HTTP `2/2`、Playwright E2E `3/3` 和定向 ESLint 全部通过。

## [v0.0.256] - 2026-08-07

### 自主任务执行治理与工作流编辑体验升级

- **新建任务快捷目标恢复可用**：“风险总结”“待办整理”“资料检查”在自动化工作台按需加载后会正确绑定，点击后填入对应目标、运行模式和工具开关；三个入口继续作为高频任务预设，不会在未确认时直接启动任务。
- **保存为模板链路修复**：新建任务弹窗左下角“保存为模板”恢复响应，模板名称输入框始终显示在任务弹窗上方；补齐名称校验、失败提示和保存后的模板列表刷新，避免点击后无反馈。
- **最大步骤升级为可选执行轮次**：配置统一命名为“最大执行轮次”并移入高级设置，留空或填写 `0` 表示自动；标准模式默认 20 轮，深度与审查模式默认 50 轮，人工配置仍限制在 1 至 50 轮。任务模板和计划任务会保留“自动”语义，不再把历史固定 10 轮作为新任务默认值。
- **执行上限全链路一致**：流式工具调用和 JSON 回退共享同一个总轮次计数，回退不会重新获得一整套轮次；达到上限后写入明确的控制记录，基于已有结果生成总结，将运行标记为“完成但有错误”并发送提示，避免静默截断或无限执行。
- **模型用量与上下文配置收口**：“模型用量上限”明确为“单次任务 Token 上限”，累计计算输入和输出 Token，留空或填写 `0` 表示不限；桌面端与“上下文来源”同一行展示，窄屏自动换行。模型选择框由整行缩短为约三分之二行宽，保留长模型名称和能力标识空间。
- **运行指标语义明确**：自主任务详情和进度展示“执行轮次”，工作流 DAG 展示“执行记录”；任务列表统一使用“记录”列，技术详情继续保留完整步骤记录数量，避免把模型回合与持久化记录混为一谈。
- **工作流名称可直接修改**：工作流编排标题旁新增编辑入口，可在当前画布修改名称；名称变更同步当前工作流、实时编辑状态和版本视图，用户无需返回列表重新创建。
- **节点名称编辑不再逐字提交**：节点名称输入期间只更新编辑内存，按 Enter 或点击其他位置时一次提交并生成一条历史记录；输入第二个字时不再因每个字符自动保存而丢失焦点。
- **响应式与交互细节完善**：任务弹窗高级字段重新分组，说明文字与输入框保持稳定关联；桌面和 `390×844` 窄屏均无横向溢出，快捷目标、模板、工作流重命名和节点编辑保持现有权限与保存边界。
- **回归验证**：新增自动轮次默认值、模板与计划兼容、流式/回退共享上限、预检和运行指标回归覆盖；全量 Node 测试 `438/438`、`npm run check`、`npm run lint` 和 Playwright 冒烟 `2/2` 通过。

## [v0.0.255] - 2026-08-06

### 计划任务、工作流配置与新闻内容校对闭环

- **计划任务发布边界明确**：计划任务只能选择已发布的工作流执行，草稿或未发布版本不会被调度，避免计划运行到未确认的编排内容；工作流运行来源、发布版解析和权限校验保持一致。
- **分钟级间隔调度**：计划任务新增“按间隔”模式，支持每 5 至 1440 分钟执行一次，满足一天多次运行的场景；服务端校验上下限并兼容旧计划数据，计划详情展示下次执行时间。
- **节点配置反馈完善**：节点配置点击“应用到画布”后立即关闭配置窗口、刷新画布并显示成功提示；画布刷新异常会给出明确警告。提示同时说明仍需点击顶部“保存”才能持久化工作流。
- **节点配置持久化修复**：保存工作流时保留手动编辑的节点参数、输入引用、输出契约和数据库查询配置；重新打开节点配置会从已保存 DAG 恢复，不再因预设重建或界面刷新丢失修改。
- **自由任务转工作流增强**：包含数据库查询且目标为新闻/内容校对的自由任务，会自动生成“数据库查询 → 富文本内容校对 → 工作流输出”节点图，校对节点直接读取上游 `structuredContent.rows`，避免工作流只展示“返回几条数据”而不执行检查。
- **富文本逐条校对**：新增 `agent.content_review` 内置能力，清理 HTML 标签、脚本、样式、实体和无关空白，保留标题与正文段落边界；支持多条记录逐条处理、长正文按 Token 分块并保留少量上下文重叠。
- **结果可信度与完整性**：校对问题必须能在原文标题或正文中找到证据，输出按记录返回“通过/发现问题/未完成”状态、问题位置、原文、建议和原因；上游查询被截断、分块失败或记录未处理时，运行结果明确标记不完整，不再伪装成普通行数摘要。
- **长结果交付与产物沉淀**：提高自由任务和工作流大模型节点的最大输出配置上限，并按模型上下文预算自动收敛输入；工作流输出保留完整结构化行边界，超出交付上限时提示截断原因，同时生成可下载的 Markdown 校对报告产物。
- **回归验证**：补充富文本清洗、结构化行提取、长文本分块、来源证据校验、输出边界和计划间隔回归测试；本版本全量 Node 测试 `437/437`、项目检查和相关 ESLint 检查通过。

## [v0.0.254] - 2026-08-06

### 智能体详情、质量评测弹窗与流程图视觉重构

- **完成状态图标精准居中**：精确微调“已完成”绿圈内对勾（Checkmark）的 `top` 偏移量至 `14.5px`，实现 36px 翡翠绿圆形图标内对勾的几何垂直水平双向居中。
- **运行信息栏单行自适应**：将“运行信息”区域指标由固定 150px 网格重构为 Flex 单行自适应弹性布局，解决“模型用量”（如 `总计 7566 (输入 6009 / 输出 1557)`）字数较多时的尴尬折行。
- **步骤节点排版中轴对齐**：修复 `.agent-dag-node-title` 样式继承问题，使步骤序号 `1`、`2`、`3` 与右侧节点标题保持在同一水平线上，并重新优化了“步骤详情”栏目标题边距与状态胶囊标签。
- **工作流流程图双轴自动居中**：重构 `renderAgentDagRunGraph` 坐标计算公式，自动根据节点拓扑阵列总宽高计算 `offsetX` 与 `offsetY`，使工作流节点在 SVG 画布中完全居中显示。
- **任务结果大气清爽卡片重构**：移除了 `agent-final` 容器左侧硬化的绿色纵向竖条，升级为微光柔和阴影白卡片；将标题优化为优雅灰蓝小胶囊（移除 emoji），增强多字段数据表格的斑马纹与圆角边框质感。
- **工作流计划任务弹窗标题靠左**：显式指定 `.agent-config-modal-head` 及其子标题靠左排版，解决通用 Modal 继承 `text-align: center` 导致的弹窗标题居中问题。
- **质量评测全屏弹窗布局重构**：修复“质量评测”弹窗内部卡片收缩、右侧留下大面积空白的问题；将概览指标重构为横跨全宽的 5 列响应式卡片，并大幅美化空状态居中展示与用例列表。

## [v0.0.253] - 2026-08-06

### 任务与工作流详情页状态栏视觉排版优化

- **文本靠左对齐**：为任务执行与工作流预览详情页顶部的“任务执行”、“已完成”等状态标题与描述说明补充了明确的 `text-align: left` 左对齐排版规则，解决此前受全局文本对齐继承影响导致的不靠左问题。
- **完成勾选图标升级**：将“已完成”状态的对勾图标重构为翡翠绿渐变背景（`linear-gradient(135deg, #10b981, #059669)`）配合立体柔和阴影，并精细校准了内部白色对勾 Stroke 与圆角，提升界面现代化质感。

## [v0.0.252] - 2026-08-06

### 工作流与任务详情操作按钮样式修复

- **更多操作按钮边框与规范统一**：修复了任务与工作流执行详情页顶部“更多操作”按钮受下拉选项 css 选择器覆盖导致缺失边框的问题，限制无边框样式仅作用于下拉面板内选项，使其保持与左侧 [保存结果]、[导出结果] 完全一致的二级按钮边框、圆角与视觉样式。

## [v0.0.251] - 2026-08-06

### 知识库统计面板与顶部操作菜单交互优化

- **去除了“更多统计”展开按钮**：知识库工作台移除了用于折叠展开诊断统计的 `<details>` 容器与“更多统计”切换按钮，将评分、反馈、实体与关系等诊断统计指标直接并列展示在顶部统计栏，方便直观查看。
- **取消诊断指标高亮背景**：移除了诊断统计子项原有的绿底高亮样式，使“资料”、“索引”和“诊断”各统计面板视觉风格保持统一简洁。
- **顶栏操作菜单交互升级**：去除了“内容管理”、“检索与图谱”、“批量治理”三个顶栏按钮上的展开/收起箭头标志（`⌄` / `⌃`）；并实现了二级菜单的自动单开互斥（点击一个菜单自动关闭其他已打开菜单）、选项点击自动收起以及点击页面空白处自动关闭的交互体验。
- **全量测试与门禁验证**：全量 Node 单元测试、静态代码规范检查、safe-html 与 e2e-smoke 门禁全量通过。

## [v0.0.250] - 2026-08-06

### 三类共享面板视觉规范统一与关闭按钮加固

- **共享面板视觉统一**：参照工作流分享面板的设计标准，重构优化了工具库（只读数据库能力）与知识库（专题库）的分享弹窗，统一使用顶部蓝底资源说明卡片、可见范围单选卡片（仅自己 / 共享给单位成员）、双列单位选择网格与整洁的底部操作按钮。
- **关闭按钮红色线框样式**：将工作流、工具库和知识库三个分享面板右上角的“关闭”按钮统一升级为系统全局危险线框按钮样式（`btn-danger-outline`，红色字体与红色边框）。
- **RAG 列表接口 500 修复**：修复 `GET /api/rag/docs` 在按单位权限连表查询文档总数 `total` 时缺少 `LEFT JOIN knowledge_collections c` 导致 SQL 报 `no such column: c.scope` 抛出 500 错误的 Bug，并补齐自动化单元测试。
- **全量测试与门禁验证**：补充针对 `GET /docs` 接口连表与权限汇总的回归测试；全量 Node 测试、静态脚本检测、safe-html、raw SQL 门禁基线与 ESLint 规范全量通过。

## [v0.0.249] - 2026-08-06

### 知识库与工具库单位共享权限升级

- **知识库专题库共享**：专题库新增 `personal/shared` 与单位白名单，所有者可从知识库工作台分享给本单位成员；接收方获得只读列表、详情、分块和知识图谱访问，上传、编辑、重建、启停和删除继续由所有者执行。
- **RAG 检索隔离**：文档列表、标签、摘要、详情、FTS/LIKE/兜底候选、检索缓存版本和 Graph-RAG 查询统一使用本人或可见共享专题库条件，避免跨单位泄露和缓存串读。
- **工具库安全共享**：MCP 数据库连接增加单位共享入口；共享接收方只能使用受治理的数据库只读工具，不能刷新工具缓存、诊断、编辑、停用或删除，连接密码不会进入共享响应。
- **权限前端闭环**：知识库和工具库显示共享/只读状态，接收方自动隐藏写操作；新增单位选择弹窗、共享状态提示和可写专题库过滤。
- **迁移与验证**：新增 `202608060001_knowledge_and_tool_unit_visibility` 迁移，补齐旧库字段与索引；MCP 33/33、RAG 75/75、Agent 48/48 回归通过。

## [v0.0.248] - 2026-08-06

### 工作流共享权限完整闭环

- **共享入口与范围配置**：自动化资产中心新增“分享工作流”入口和共享权限弹窗；所有者可在个人、指定单位和全单位之间切换，管理员可选择组织内全部有效单位，普通用户只能选择自己的单位范围。
- **共享接口与审计**：新增共享选项读取接口和所有者专用的共享设置更新接口；共享设置只更新可见性元数据，不创建无意义的工作流版本，并记录共享范围、单位和操作日志。
- **发布版隔离**：共享工作流在发布前不会出现在接收者列表中；接收者只能看到已发布 DAG，`current` 运行请求自动解析为发布版本，不能通过历史版本或草稿入口读取所有者编辑内容。
- **只读接收体验**：共享接收者可以查看发布版画布并运行发布版，但不能编辑节点、保存、发布、回滚、删除、管理版本或创建计划；所有者仍保留完整管理能力。
- **前端回归与文档**：补充共享范围标识、只读工具栏、节点交互锁定、共享弹窗和安全静态断言；全量 Node 回归 `421/421`、项目门禁和 ESLint 在本轮发布前复核，Playwright 冒烟 `2/2` 通过。

## [v0.0.247] - 2026-08-05

### 阶段一至五：自动化闭环、安全审批与数据访问底座

- **工作流可见性与发布语义**：新增个人/共享工作流的单位范围控制；共享用户只读，编辑、发布、回滚和删除仍由所有者执行；共享工作流请求 `current` 时自动落到已发布版本，显式请求历史版本返回 403，避免共享计划误用草稿。
- **三类自动化触发器**：落地 Webhook、文件落地和数据库变更触发器，支持输入映射、固定输入、目标字段模板、文件扩展名过滤、数据库 `{{watermark}}` 增量查询和批量限制；复用现有自动化队列创建运行记录，并按配置路径、文件名/mtime/大小和水位线去重。
- **触发器入口与安全边界**：新增 `/hooks/workflow/:token` 入口，令牌只保存哈希和提示信息；Webhook 请求限制 256 KB，文件触发目录必须位于用户已配置的报表目录内，触发器只能运行已发布且当前用户可访问的工作流。
- **Cron 调度**：新增零依赖的五段式 Cron 解析器，支持通配符、步长、区间、列表、星期日 0/7 兼容以及日期/星期并集语义；下次执行搜索有年度上限，避免非法表达式造成死循环。
- **工作流凭据库**：复用 AES-256-GCM 的 `encryptSecret` / `decryptSecret`，凭据支持个人或单位共享、权限校验、轮换过渡期、撤销和使用统计；接口永不返回明文，运行时才解密注入，旧值只在过渡窗口内可用。
- **审批节点与延迟节点**：新增指定审批人、部门审批、`any/all` 多级审批、超时自动批准/拒绝、`awaiting_approval` 状态和队列释放/重新入队；延迟节点复用同一挂起机制，不占用运行槽位，也不使用线程睡眠。
- **审批回调加固**：IM 通知回调绑定单个 approval request，回调令牌一次性使用；签名同时绑定 token、request ID、decision 和一次性 nonce，凭据缺失或签名无效时拒绝请求。新增 `callback_nonce` 字段和迁移 `202608050002_agent_approval_request_callback_nonce`。
- **IM 通知模板与回调**：审批通知支持 payload 模板和结构化审批字段，新增 IM 回调入口复用令牌校验、限流和审批决定通道，保留无模板时的兼容默认格式。
- **运行时与 DAG 恢复**：DAG 执行会复用已持久化的完成节点、继续错误和跳过状态；审批/延迟结果进入节点执行上下文，运行重试不会重复执行已完成节点。队列 claim、租约续期、恢复和状态指标改用缓存语句，并通过方言辅助函数集中处理时间表达式。
- **数据访问底座**：新增 `server/db/statements.js` 语句缓存、`server/db/dialect.js` 方言辅助和 `check_no_new_raw_sql` 基线门禁；按领域抽取 runs、workflows、sessions 和 knowledge 仓储，服务层继续保留权限判断与事务编排，避免一次性机械迁移全部 SQL。
- **人工复核问题修复**：补齐安全文件路径校验、工具调用的工具列表传递、数据变更触发器的权限拒绝、`mcp_servers.service_type` 迁移、显式 JSON body 限制和凭据 `previous_value` 轮换回读，避免启动或运行时静默失败。
- **回归与启动验证**：新增自动化触发器和审批回归测试；全量 Node 回归 `419/419`，`npm run check`、`npm run lint`、文本/安全 HTML/window 全局检查、raw SQL 门禁和数据库启动迁移验证全部通过。

## [v0.0.246] - 2026-08-04

### 工作流输出模式与结构化交付体验升级

- **输出模式统一**：工作流和大模型节点的配置界面统一命名为“输出模式”，提供“格式化文本、纯文本、结构化数据”三种清晰选项；旧的 `auto` 输出配置不再作为兼容入口。
- **JSON Schema 契约贯通**：结构化数据节点支持绑定 JSON Schema，运行时优先请求模型原生 Structured Outputs；模型端点不支持时自动降级为普通 JSON 请求，结果不合法或不符合契约时自动修复并最多重试一次，最终仍失败会返回可读校验原因。
- **Schema 可视化编辑器**：节点契约弹窗支持根类型、字段类型、必填字段、字段说明、嵌套对象和数组项编辑，同时保留高级 JSON Schema 编辑；编辑过程中实时生成输出预览并执行示例校验。
- **下游字段选择器**：根据上游节点输出 Schema 生成字段建议，可直接插入 `{{nodes.extract.output.customer.name}}` 等嵌套引用；运行时同时支持从结构化 JSON 内容和工作流输出对象解析路径。
- **表格交付体验**：工作流输出节点新增“表格”增强交付，支持数组或 `rows` / `data` / `items` 数据，自动推断或手动指定列，并在运行结果中显示标题、列、行数和截断提示；底层仍保留 JSON 数据。
- **文件产物体验**：工作流输出节点新增“文件产物”增强交付，保留安全的文件 `id`、名称、类型、大小、URL、路径或存储键引用，运行结果提供可用的下载入口，不把文件内容复制进工作流 JSON。
- **结果展示与契约预设**：运行详情对表格和文件引用提供专用可读展示，默认工作流输出契约可继续展开 `table.rows`、`file.url` 等下游字段。
- **产品边界收口**：暂不加入 YAML、XML；底层统一使用 JSON 数据和文件引用，减少解析、安全和兼容成本。
- **文档与版本同步**：更新 CHANGELOG、README、优化路线图、项目汇报、DESIGN 和普通用户使用帮助，项目包元数据统一启用 `v0.0.246`。
- **回归验证**：全量 Node 回归 `393/393` 通过；JavaScript 语法、改动文件 ESLint、文本完整性、安全 HTML、window 全局和开发规范检查全部通过。

## [v0.0.245] - 2026-08-04

### 自动化运行结果产品化与全界面中文化

- **Windows 客户端发布验收**：已生成 v0.0.245 NSIS 安装包、`.blockmap`、`latest.yml`、通用下载入口和 `SHA256SUMS.txt`；更新清单中的版本、文件大小、SHA-512 与本地产物一致。
- **生产自动更新检查**：客户端更新策略和 `/downloads/` 发布链路可用，但当前生产服务地址无法访问，且使用未签名的 HTTP 安装包；在生产服务恢复并配置 HTTPS/代码签名前，保持 `autoUpdate.enabled=false`，避免把不可信更新源下发给用户。
- **运行详情改为结果优先**：自由任务和工作流运行详情重新组织为面向普通用户的摘要式页面，优先展示完成状态、任务结果、耗时和必要操作；执行轨迹、治理信息、模型参数、节点输入输出等技术信息降低视觉权重，并按需折叠查看，避免一次性堆叠大量开发字段。
- **工具结果自动转换为可读内容**：工具调用输出支持解析普通对象、数组、多层 JSON 字符串、双重序列化内容、MCP `content` 包装和截断的行数据；数据库表、字段、文件等结构化结果优先呈现为中文表格或摘要，完整载荷仅保留在“查看原始数据”中。
- **数据库结果语义化展示**：`TABLE_SCHEMA`、`TABLE_NAME`、`TABLE_TYPE` 等字段显示为“数据库模式、数据表名称、数据表类型”，`BASE TABLE`、`readonly_sql` 等枚举值转换为“数据表、只读查询”；超出首屏的记录明确提示剩余数量，不再把转义 JSON 作为主要内容。
- **自动化界面全面中文化**：统一自由任务、工作流、计划任务、运行追踪、质量评估、节点库、参数编辑、数据库查询向导和服务端通知中的用户文案；`Finish Reason`、`tool_calls`、`Tool Calls`、`Arguments`、`plan` 等技术术语分别显示为“结束原因、调用工具、工具调用、参数、任务规划”，工具标识在可读区域转换为“查看表结构、列出数据表”等业务名称。
- **模型与运行术语收口**：面向用户的 Token 预算、模型 Token 用量、返回原因和执行状态统一改为“模型用量上限、模型用量、结束原因、运行状态”等中文表达；内部字段名、工具标识和接口协议保持不变，保证历史运行记录与外部集成兼容。
- **桌面与移动端适配**：运行步骤标题、状态和耗时在窄屏下自动分行，结果表格使用稳定列宽和长内容换行，桌面端保持紧凑的信息层级，避免横向溢出、字段遮挡和卡片高度跳动。
- **文档与版本同步**：CHANGELOG、README、普通用户使用帮助和优化路线图同步记录本轮自动化体验升级，项目包元数据统一启用 `v0.0.245`。
- **回归验证**：新增 MCP 双重序列化结果、通用工具结果、中文结束原因与工具调用元数据、移动端布局等回归覆盖；智能体专项测试 `47/47`、JavaScript 语法检查 `400/400`、文本完整性、安全 HTML、改动文件 ESLint 和 Playwright 冒烟测试 `2/2` 通过。

## [v0.0.244] - 2026-08-04

### 只读查询筛选与日期时间条件增强

- **筛选关系支持 OR**：数据只读查询节点的筛选条件新增“全部满足 AND / 任一满足 OR”选择，默认保持 AND，并将关系保存到 `filterRelation`；同时兼容历史配置中的 `filter_relation`、`filterLogic` 和 `filter_logic` 字段。
- **日期时间字段专用控件**：读取字段后识别 `date`、`datetime`、`timestamp` 和 `time` 类型，筛选值自动切换为日期、日期时间或时间选择器；`between` 显示起止两个控件，日期列表的 `IN` 条件保留逗号分隔文本输入。
- **时间运算语义清晰**：日期时间字段的比较运算符显示“早于、晚于、不早于、不晚于、时间范围”等语义化文案，并隐藏不适用的文本匹配运算符。
- **跨数据库时间值统一**：日期时间控件值在生成 SQL 时统一为 `YYYY-MM-DD HH:mm:ss`，避免浏览器 ISO `T` 分隔值与数据库时间文本格式不一致造成边界比较问题；不自动进行时区转换。
- **回归验证**：新增 OR 关系、旧配置字段兼容、日期/日期时间/时间类型识别和 SQL 时间值格式化测试；`npm run check`、`npm run lint`、`npm run test:all`（386/386）和 `npm run test:e2e`（2/2）通过。

## [v0.0.243] - 2026-08-04

### 工作流数据库读取诊断与桌面客户端布局修复

- **读取数据表与字段按钮恢复**：可视化 SQL 查询节点为“读取数据表”和“读取字段”按钮增加独立点击绑定，避免父级事件委托或弹窗交互状态导致点击无响应。
- **数据库工具调用连接修复**：数据库向导会把所选连接解析为对应 MCP 工具全名，并在调用前移除仅用于前端选择的连接标识，避免请求落到未绑定具体数据库的通用工具名。
- **读取状态就近反馈**：两个读取按钮下方新增实时状态区域，点击后立即显示“按钮已触发、正在连接”，成功时展示表或字段数量，失败时明确区分安全策略拦截、拒绝连接、连接超时、账号认证失败、工具不可用和未选择数据表。
- **本机地址语义明确**：查询节点说明读取请求由 Pivot 服务端发起，`localhost` 指向服务端或容器而不是浏览器所在电脑；服务端返回的错误码、提示和诊断信息会保留到前端，用于给出可操作的失败原因。
- **预设节点列表可滚动**：工作流编排的预设节点菜单增加受限最大高度和内部垂直滚动，长节点分类不再溢出浏览器视口；横向溢出关闭并保留稳定滚动条空间。
- **客户端弹窗避让标题栏**：桌面客户端中的任务模板和“能力与结果”弹窗从 30px 原生标题栏下方开始布局，并按剩余动态视口重新计算弹窗与内容高度，不再被“页面 / 显示 / 客户端”菜单遮挡。
- **客户端标题栏细节修复**：横向分割线从原生窗口按钮区域移到内容区顶部，菜单行仍保持 30px 固定高度；“页面 / 显示 / 客户端”文字使用更深的次级颜色，与主要内容标题形成层级区分。
- **文档与版本同步**：更新 CHANGELOG、README、普通用户使用帮助和优化路线图，项目包元数据统一启用 `v0.0.243`。
- **发布验证**：`npm run check`、`npm run test:e2e`、浏览器交互与 1320×860 客户端布局断言通过；数据库按钮可明确显示触发、成功与失败状态，客户端弹窗完整位于可用视口内。

## [v0.0.242] - 2026-08-04

### 旧数据库自动化迁移启动修复

- **启动顺序修复**：基础 schema 不再提前创建依赖 `agent_runs.dedupe_key`、`agent_schedules.dispatch_retry_at` 和 `agent_schedules.claim_expires_at` 的索引；相关索引统一在 legacy migration 补齐字段后创建，修复从 v0.0.240 及更早版本升级时的 `no such column: dedupe_key` 启动崩溃。
- **新库与旧库一致**：全新数据库仍由基础表结构声明新增字段，随后由同一幂等迁移阶段创建 `idx_agent_runs_user_dedupe` 和 `idx_agent_schedules_dispatch`，避免维护两套有先后依赖的索引初始化逻辑。
- **旧库回归覆盖**：新增缺少自动化幂等、租约、重试字段的历史 `agent_runs` / `agent_schedules` 快照，验证完整数据库启动会先补列、再建索引；同时补齐多数据库启动测试的连接模块隔离。
- **真实迁移验证**：对现有 `data/chat.db` 完成 SQLite 一致性备份后执行完整启动迁移，并通过二次启动幂等性及 `PRAGMA integrity_check` 校验。
- **新建任务弹窗增高**：桌面端最大高度由 `760px` 提升至 `860px`，并按动态视口保留 16px 上下安全边距，减少常见桌面分辨率下的正文垂直滚动；窄屏继续使用原有视口保护。
- **使用帮助匹配当前导航**：移除已经不存在的左侧“任务”菜单和“任务运行”章节，按“搜索、应用、知识库、工具库、自动化”的首页顺序编排，并说明自动化下的工作流与计划任务路径。
- **使用帮助定位完善**：补充智枢面向组织内部的全场景智能协同与业务自动化中枢平台定位，按核心能力和工作方式解释对话、应用、工作流、计划任务、知识库与工具库的适用范围，帮助用户先选对入口再开始操作。
- **帮助内容按当前界面校准**：更新首页左侧菜单顺序、个人/系统设置标签、知识库启用开关和召回测试说明，移除不存在的“任务”入口及不符合当前页面的操作描述。
- **依赖审计恢复通过**：更新 `express-rate-limit`、`minimatch` 及其依赖锁定版本，将 `ip-address` 升至 `10.4.0`、`brace-expansion` 升至 `5.0.9`，修复新增披露的高危地址解析与拒绝服务问题。
- **跨平台审计门禁修复**：Windows 不再直接启动会返回 `EINVAL` 的 `npm.cmd`；审计命令启动失败、无输出或返回错误对象时会明确失败，避免本地误报“检查通过”。
- **GitHub Actions 运行时升级**：`actions/checkout` 与 `actions/setup-node` 升级到使用 Node.js 24 action 运行时的 `v7`，项目验证仍固定使用 Node.js 22。
- **发布门禁**：`npm test` 全链路通过，包含文本与开发规范、语法和资源、安全 HTML、window 全局、E2E 脚手架、ESLint，以及 Node 全量回归 `384/384`。

## [v0.0.241] - 2026-08-03

### 自动化计划可靠性与权限治理全面修复

- **撤权立即生效**：禁用或删除账号时，自动化计划会被暂停，排队中、运行中和待审批的智能体任务会被取消；调度器、队列和运行时会再次校验账号状态，避免撤权后的任务继续执行。
- **调度持久化与崩溃恢复**：计划调度改用租约抢占，保留原始 `next_run_at`，成功后才推进下一次执行；进程中断后可重试同一时段，并通过计划时段去重键避免重复创建运行记录。
- **严格计划校验**：拒绝未知周期、非法时间、越界星期、未知状态、无效模板和无效工作流；修复星期日被错误归入星期一的问题。
- **失败可恢复**：调度失败不再立即永久暂停，改为指数退避重试并记录 `last_error`；连续失败达到阈值后才自动暂停并发送通知。
- **容量与公平性治理**：每个账号最多保留 100 个未删除计划；计划列表不再截断历史数据；队列增加账号级并发上限，计划任务使用公平的普通优先级，并为自动化写操作增加用户级限流。
- **请求幂等**：手动运行按钮发送 `Idempotency-Key` 并在请求期间禁用，服务端用唯一去重键抵御重复点击和并发重试。
- **租户边界校验**：模板、计划、工作流和运行记录的关联关系全部在服务端按用户权限校验；普通运行接口不再接受客户端伪造的 `scheduleId`。
- **历史查询与可观测性**：任务中心支持按计划 ID 精确查询历史运行，计划错误和重试状态持久化，便于定位失败原因。
- **回归验证**：新增自动化安全回归，覆盖星期日、非法输入、手动幂等和撤权不调度；全量 Node 回归通过 `383/383`。

## [v0.0.240] - 2026-08-03

### 附件预览与自动化通知体验优化

- **图片附件预览状态化**：用户在聊天输入框添加图片后，改用稳定的缩略图卡片展示，增加“正在读取图片”“待上传”和“预览不可用，发送时上传”等明确状态，不再暴露浏览器默认破损图片图标或 `alt` 文件名。
- **本地图片预览更可靠**：对象 URL 通过 DOM 插入后再绑定到图片元素，避免安全清洗或加载时序导致预览地址丢失；图片加载失败时保留附件并提供可理解的回退提示。
- **通知中心中文化**：任务模板页面的通知中心统一将 `DAG run completed/failed`、智能体运行和审批相关英文状态转换为中文，兼容历史通知记录，标题和正文保持一致。
- **发布验证**：新增图片预览与通知中文化回归测试，聊天渲染、智能体测试、资源检查和 ESLint 均通过。

## [v0.0.239] - 2026-08-03

### 会话输入区精简与用户帮助重构

本版本继续收口普通会话的高频输入体验：去除不必要的清空入口，将模型选择放回用户正在输入和发送的操作区域，并把模型触发控件精简为当前模型名称。同时重构面向普通用户的使用帮助，严格分离产品使用说明与发布、配置及实现文档。

#### 会话输入体验

- **移除清空输入框按钮**：输入区不再提供单独的清空按钮，也不再保留对应处理逻辑，避免与用户自行编辑、撤销和重新输入的常规操作重复。
- **模型选择器就近放置**：模型名称从页面右上角移动到输入框右侧，位于发送箭头左侧，使模型确认、切换和发送形成连续操作路径。
- **触发控件仅显示模型名称**：输入区不再展示模型能力图标和下拉箭头，仅保留当前模型名称；模型能力与说明继续在展开后的列表中展示。
- **窄屏布局保持稳定**：长模型名称使用省略显示，模型名称和发送按钮保持独立尺寸；桌面和移动端均无横向溢出，发送按钮保持稳定点击区域。
- **空状态可持续刷新**：无可用模型和加载失败状态保留统一的名称节点，后续模型列表刷新时可以直接恢复正常显示。

#### 普通用户帮助

- **移除版本与发布内容**：《使用帮助》不再包含适用版本、更新提示、更新记录或发布验证，版本变化统一记录在 CHANGELOG 和项目文档中。
- **移除配置与实现内容**：删除管理员配置、部署、接口、运行机制和底层实现说明，只保留普通用户能够在页面中完成的操作。
- **按实际导航重组**：帮助内容按快速开始、会话与搜索、应用、任务、自动化、知识库、工具库、个人设置和常见问题组织，与当前单层导航一致。
- **同步最新输入区说明**：明确模型名称位于输入框右侧、发送箭头左侧，并说明该区域只显示当前模型名称。
- **文档边界固化**：版本同步脚本不再改写《使用帮助》；资源检查新增用户帮助门禁，阻止版本发布段落和底层实现名词重新进入普通用户手册。

#### 质量与发布

- **交互验证**：模型名称控件已完成桌面和 `390×844` 移动端实际页面检查，键盘展开、列表位置、空状态和发送按钮尺寸符合预期，页面无横向溢出或控制台错误。
- **工程检查**：`npm run check`、`npm run lint` 和 `git diff --check` 通过。
- **版本号启用**：项目包元数据、服务端版本读取、桌面应用元数据和当前版本文档统一启用 `v0.0.239`。

## [v0.0.238] - 2026-08-01

### 主导航、统一任务中心与自动化资产中心升级

本版本结合 Pivot 面向私有化、离线化和企业内网的 AI 工作平台定位，重新划分会话、任务、自动化与能力资产的边界。高频会话继续作为默认工作入口，任务负责统一运行记录，自动化负责可复用流程资产，工具库负责数据源、工具与连接治理。

#### 信息架构与导航

- **左侧导航扁平化**：一级菜单固定为搜索、应用、任务、自动化、知识库和工具库，下方直接展示最近会话，底部保留设置；不使用二级菜单，减少入口重叠和层级跳转。
- **页面映射明确**：应用进入应用中心，任务进入统一任务中心，自动化进入工作流与计划资产中心，知识库进入资料管理工作台，工具库进入数据源、工具和连接管理。
- **最近会话与设置收口**：最近会话可直接回到对应上下文；设置根据账号权限打开系统设置或个人设置，普通用户不再进入无权限的系统管理首屏。

#### 搜索、任务与自动化

- **全局搜索升级**：搜索弹窗默认检索会话，可切换任务和工作流，并直接打开会话、任务详情或工作流编辑器；空状态、加载状态和键盘操作保持一致。
- **统一任务中心**：原运行工作台调整为任务中心，默认汇总自主任务、工作流任务和计划运行记录，支持搜索、类型和状态筛选；“新建自主任务”按需展开，不再占据默认首屏。
- **自动化资产中心**：自动化默认管理工作流、发布状态和计划任务资产；新建或编辑时进入原可视化画布，继续支持版本、发布、运行、计划和运行追踪。
- **三种工作方式边界明确**：普通会话用于即时问答和一次性工具调用，自主任务用于需要规划和多步执行的目标，自动化用于可复用、可发布、可计划运行的稳定流程。

#### 命名与产品边界

- **工具库命名统一**：用户侧统一使用“工具库”，用于管理数据源、工具和连接；普通会话可直接选择并调用工具库能力，工具库本身不再被描述为另一套会话入口。
- **移除旧输入兼容**：不再识别“工具箱”作为启用工具的兼容输入，界面、帮助和当前版本文档统一使用“工具库”。
- **智能体入口收口**：移除“智能体”主导航，用户侧统一使用“任务”和“自主任务”；底层智能体运行、路由、工具调用、Trace 和恢复能力继续保留。

#### 响应式、质量与发布

- **桌面与移动端适配**：移动端默认收起左侧栏并提供明确关闭入口；任务指标、筛选区和全局搜索结果在窄屏下重排，避免文字、控件和横向内容溢出。
- **设计与文档同步**：同步更新 README、使用帮助、项目汇报、设计规范、工具库设计方案、优化路线图和远程模式升级检查，形成一致的产品口径。
- **验证结果**：`npm run check`、`npm run lint` 通过，Node 全量回归 `376/376`、智能体安全回归 `40/40` 通过；桌面 `1440×900` 与移动端 `390×844` 完成实际页面视觉检查。
- **版本号启用**：项目包元数据、服务端版本读取、桌面应用元数据和当前版本文档统一启用 `v0.0.238`。

## [v0.0.237] - 2026-07-31

### 局域网 HTTP 生产部署安全加固与工程链路全面优化

本版本面向无公网 IPv4、无公网 IPv6、无域名、无 HTTPS 证书且生产环境只能使用 HTTP 的隔离局域网，对服务端认证、桌面端信任边界、健康监控、主动内容、依赖审计、测试隔离和 Windows 打包链路进行系统性加固。普通业务访问不依赖公网地址、域名或证书，只要求客户端能够通过私有 IP、IPv6 字面量或局域网可解析的操作系统主机名访问服务。

#### 局域网地址与纯 HTTP 部署边界

- **不要求公网基础设施**：Web 与桌面远程模式均支持直接使用局域网地址，不要求公网 IPv4、公网 IPv6、DNS 域名或 TLS 证书；桌面端以 `remoteUrl` 的实际同源地址建立信任边界。
- **可达地址仍是必要条件**：远程模式至少需要一种客户端可访问的地址，例如私有 IPv4、IPv6 字面量或局域网主机名。若网络中确实不存在任何 IP 地址且主机名也不可解析，则无法建立远程 HTTP 连接，应改用桌面本地模式或先配置局域网寻址。
- **HTTP 风险显式收口**：生产环境使用 HTTP 时保持 `COOKIE_SECURE=false`，并要求通过隔离网段、防火墙、终端准入和受控交换网络限制访问范围；配置检查日志不再把隔离局域网 HTTP 误报为必须启用 HTTPS。
- **无证书环境默认关闭自动更新**：`config.json` 与示例配置默认 `autoUpdate.enabled=false`、`allowInsecureHttp=false`，建议通过受控共享目录或离线介质分发安装包，并使用 `SHA256SUMS.txt` 校验文件。只有发布链可信时才显式启用 HTTP 更新源及精确 origin 白名单。

#### 服务端出站与桌面端信任边界

- **IPv4 映射 IPv6 归一化**：SSRF、敏感出站和 loopback 判断统一识别 `::ffff:127.0.0.1`、`::ffff:7f00:1` 等 IPv4-mapped IPv6 写法，避免通过等价地址表示绕过私网策略。
- **桌面导航守卫**：Electron 主窗口只允许导航到配置目标的同源 HTTP/HTTPS 页面和明确允许的本地文件；跨源导航与重定向会被阻断，普通外链继续按配置交给系统浏览器。
- **IPC 调用方校验**：本机授权、工具调用、桌面状态、重试、刷新、窗口控制、退出及自动更新等特权 IPC 均校验发送页面来源，非受信任页面无法借助桌面桥接调用本机能力。
- **地址变化可配置**：信任策略直接从部署配置中的 `remoteUrl` 派生，不绑定固定公网 IP、固定 IPv6、域名或证书，适配纯内网 HTTP 地址与局域网主机名。

#### 认证、Cookie 与注册保护

- **刷新令牌只存摘要**：数据库不再保存 refresh token 明文，改存 SHA-256 摘要；新增幂等迁移自动转换历史记录，现有登录会话无需强制退出。
- **一次性轮换与重放拒绝**：刷新过程在事务中原子消费旧令牌并签发新令牌，同一 refresh token 被重复使用时会被拒绝，缩小令牌泄露后的重放窗口。
- **Cookie 路径修复与兼容清理**：刷新 Cookie 路径从仅覆盖 `/api/auth/refresh` 调整为 `/api/auth`，确保退出登录能够收到并删除 Cookie；升级后同时清理旧路径 Cookie。
- **注册接口限流**：开放注册增加每客户端 IP 每 15 分钟最多 5 次的独立限制，降低局域网内批量注册和账号枚举压力。
- **默认管理员密码提醒**：启动时若仍配置 `DEFAULT_ADMIN_PASSWORD` 会记录告警，明确该变量只应用于空数据库首次初始化；已投入使用的管理员密码需在界面中人工轮换。

#### 健康检查与 Prometheus 指标

- **公开健康接口最小化**：`/api/health` 仅返回数据库连通、内存状态、总体状态和时间戳，不再暴露数据目录、上传目录或磁盘路径，并增加每 IP 每分钟 60 次限流。
- **详细诊断要求登录**：数据库 `quick_check`、目录可写、内存和磁盘等高成本诊断迁移到 `/api/health/details`，仅已认证用户可访问，默认缓存 10 秒，避免探针并发触发重复磁盘与 SQLite 检查。
- **指标默认关闭而非匿名开放**：未配置 `METRICS_TOKEN` 时 `/api/metrics` 返回 503；令牌只接受 `Authorization: Bearer`，不再接受可能写入日志和历史记录的查询参数，并使用常量时间比较。
- **可信内网显式例外**：只有在完全隔离且无法配置令牌时，管理员才可显式设置 `METRICS_ALLOW_UNAUTHENTICATED_LAN=true` 开放匿名指标。

#### 主动内容与前端 HTML 安全

- **HTML 附件强制下载**：`.html` / `.htm` 附件返回 `Content-Disposition: attachment` 与 `X-Content-Type-Options: nosniff`，避免用户上传的主动内容在 Pivot 源站上下文中直接执行。
- **动态 HTML 统一清洗**：工作流版本列表不再使用裸 `insertAdjacentHTML`，改由 `PivotSafeHtml.prependHtml()` 经过既有白名单清洗后插入。
- **打印链路去除 `document.write`**：应用工作台导出打印改用 Blob URL 打开独立文档，清除 opener 并及时回收 URL，避免把动态内容写入可继承原页面能力的文档流。
- **安全扫描范围扩大**：`check_safe_html_usage.js` 除 `innerHTML` 外，新增扫描 `insertAdjacentHTML`、`createContextualFragment`、`document.write` 和 `srcdoc`，阻止同类高风险 DOM 写法重新进入代码库。

#### 依赖、审计与 Windows 打包

- **桌面运行时升级**：Electron 从 38 系列升级到 `39.8.10`，electron-builder 升级到 `26.15.3`，并完成 Windows unpacked 打包与 ASAR 内容验证。
- **运行时审计清零**：`npm audit --omit=dev` 为 0 项；审计策略同时覆盖生产依赖和直接打包进入桌面的 Electron 运行时，不再让构建工具链的间接告警混淆生产运行面。
- **构建链剩余风险透明保留**：完整 `npm audit` 仍报告 electron-builder 构建链中的 16 项 high、0 项 critical；`npm audit` 建议的降级版本会引入更多漏洞及 critical 告警，因此本版本不做破坏性降级，后续随上游构建链修复继续复查。
- **安装包不再嵌套进 ASAR**：桌面打包排除 `client/*.exe`，避免历史安装器被再次打入应用包；ASAR 从约 296 MB 降至约 153 MB，并确认不再包含 `client/Pivot-Setup.exe`。
- **发布完整性清单**：正式 NSIS 发布后自动为 `downloads/` 生成 `SHA256SUMS.txt`，便于无 HTTPS、无代码签名证书的局域网通过受控渠道核验安装文件。

#### 测试隔离与执行效率

- **测试数据完全隔离**：Node 与 Playwright 测试新增统一包装脚本，每次在系统临时目录创建独立数据、上传、分析和日志路径，并注入测试专用密钥，结束后自动清理，避免触碰开发或生产数据库。
- **移除重复聚合执行**：删除会重复加载全部安全测试的 `tests/security.test.js` 聚合入口，保留各顶层测试套件直接执行；Node 测试总耗时由约 119 秒降至约 54 秒。
- **消除外部 DNS 等待**：网络策略测试改用 RFC 5737 TEST-NET 地址，不再依赖 `.example` 域名解析超时，离线局域网中也能稳定运行。
- **验证结果**：`npm test` 全部通过，Node 回归 `376/376`；Playwright E2E `2/2` 通过；生产依赖审计 0 项；`npm run pack:win` 成功，打包后的 ASAR 已确认包含导航策略且不包含旧安装器。

#### 版本与升级说明

- **版本号启用**：项目、服务端版本接口、桌面应用元数据、README、使用帮助、项目汇报和优化路线图统一启用 `v0.0.237`。
- **数据库自动迁移**：服务下次正常启动时会自动执行 refresh token 摘要迁移，无需手工修改数据库；升级前仍建议按既有流程备份数据目录。
- **生产启用动作**：管理员需配置独立的 `METRICS_TOKEN`，删除首次初始化后遗留的 `DEFAULT_ADMIN_PASSWORD` 配置，并在界面中轮换已投产管理员密码。无证书环境继续保持桌面自动更新关闭。

## [v0.0.236] - 2026-07-31

### 智能体工作台、工作流编排与可视化 SQL 体验重构

本版本集中收口智能体工作台、工作流编排、质量评测和数据库查询配置的用户体验，减少重复入口和过时的提示词资产，让普通用户可以通过结构化界面完成常见工作流与只读查询。

#### 智能体工作台与工作流入口

- **工作流入口去重**：智能体页面保留单一的“工作流编排”入口，移除与右上角功能重复的工作流按钮，避免用户误以为是两个不同能力。
- **顶部操作区收口**：工作流编排页把保存、发布、运行和返回智能体收敛到统一命令区，状态、当前版本和运行结果集中展示；同时移除“套用提示词”入口，画布区域不再重复放置全局操作。
- **已保存工作流改为紧凑选择器**：工作流名称、版本、节点数和草稿状态集中在顶部选择器中，保留新建、切换、版本和删除能力，减少大面积列表占位并让画布获得更多空间。
- **节点库与工具栏职责清晰**：节点库负责按类别查找和添加节点，工具栏负责当前画布操作；添加节点、模板、发布和运行不再通过重复按钮表达同一动作。
- **SVG 图标统一**：工作流编排页面原有的 emoji 图标替换为项目统一的 SVG 图标；没有图标的文字操作不额外添加装饰图标，保持信息密度和视觉一致性。

#### 多智能体节点语义收口

- **委派智能体（`agent.delegate`）**：在隔离上下文中调用具名角色专家执行一次子任务，并返回结果与结构化交接信息；常规委派流程无需再额外连接一个交接节点。
- **智能体交接（`agent.handoff`）**：不调用模型，只负责把已有结论、证据、风险和未决问题整理为稳定的结构化交接，适合汇总多个分支后交给下游节点。
- **Supervisor 模板说明统一**：并行研究、独立审阅和最终裁决的节点职责在模板、节点说明、运行结果和 Trace 中保持一致，避免把委派和交接误认为两个重复的智能体调用。

#### 提示词库功能退场

- **移除用户侧提示词库入口**：下线聊天、自由任务、工作流和设置页面中的提示词库、套用提示词弹窗及相关入口，避免继续维护与当前工作流节点配置重复的资产。
- **保留现有数据兼容性**：服务端提示词 API、历史数据和数据库结构暂不删除，仅停止用户侧新入口和运行时引用，便于历史数据迁移与后续清理。
- **当前替代方式**：聊天直接描述角色和输出要求；工作流通过大模型节点的系统提示词/用户提示词配置；重复流程通过工作流模板保存，而不是通过独立提示词库套用。

#### 质量评测弹窗与全局表单样式

- **新建评测集弹窗统一布局**：名称、目标类型、模型、通过线和说明使用全局表单网格，标签、输入框、下拉框和间距与其他管理弹窗一致。
- **评测集与用例编辑器优化**：基础设置、用例数量、任务输入、规则字段和高级配置按信息层级分区，底部操作区固定，长内容只在内容区域滚动。
- **质量评测总览弹窗优化**：统计指标、空状态、刷新/新建操作和评测集卡片统一使用全局间距、边框、按钮和响应式规则，桌面与窄屏均避免重叠和横向溢出。

#### 只读 SQL 可视化编写

- **可视化查询默认模式**：数据库节点打开后先选择连接、读取数据表和字段，再勾选返回字段；普通用户不需要从零手写 SQL。
- **结构化查询配置**：支持等于、不等于、包含、范围、列表、为空/不为空等筛选条件，以及计数、求和、平均值、最小/最大值、分组、排序和最多返回行数。
- **实时 SQL 预览**：配置变化会立即生成 SELECT/FROM/WHERE/GROUP BY/ORDER BY/LIMIT（SQL Server 使用 TOP）并显示可读预览；汇总模式会自动禁用不适用的字段和排序选项。
- **高级 SQL 兼容**：复杂 JOIN、CTE、数据库特有语法可切换到高级 SQL；旧工作流已有手写 SQL 时会自动进入高级模式，保持已有流程可编辑。
- **安全与治理保持有效**：前端生成器会安全引用标识符、转义文本值并限制最多返回 1000 行；后端继续执行单语句只读校验、表/字段白名单、敏感字段脱敏和行数治理，并修复 SQL Server `TOP` 重复追加问题。

- **只读 SQL 弹窗布局收口**：补齐主工作区对 `agent-dag-query-builder.css` 的样式引入，让可视化查询构建器不再落回“裸表单”样式；同时给只读 SQL 向导加上专用单列布局，压缩数据库连接卡片高度，并统一查询方式、筛选项、预览区的字号与行距。
- **高级 SQL 切换修复**：修复“高级 SQL”按钮点击后又被旧的可视化 active 状态覆盖、看起来没有反应的问题；从可视化查询切换到高级 SQL 时，若编辑框为空，会自动带入当前可视化配置生成的 SQL，便于继续手工微调。

#### 验证

- `npm run check` 通过：文本完整性、开发规范、398 个 JavaScript 文件语法、聊天资源、安全 HTML、window 全局和 E2E 冒烟检查全部通过。
- `npm run lint` 通过；DAG 布局与查询生成回归 `7/7` 通过，智能体安全回归 `40/40` 通过。
- 本地静态资源检查确认可视化查询脚本与样式均可正常加载；浏览器验证受当前环境未登录状态限制，未绕过认证进入业务页面。

## [v0.0.235] - 2026-07-31

### 对话发送交互优化：新消息自动打断上一次生成

#### 问题背景

此前，用户在大模型正在回复期间再次发送消息，`sendMessage` 会直接静默丢弃新内容并弹出一条 warning toast，用户必须手动点击"停止生成"后才能发出下一条问题。这一设计打断了对话节奏，尤其在用户想追问、修正问题或提前终止当前话题时体验较差。

#### 改动内容

- **`sendMessage` 改为调度层（[client/chat/engine.js](client/chat/engine.js)）**：不再拦下新消息，而是先对当前 `AbortController` 调用 `abort()`，等上一次发送的 `catch/finally` 清理完毕（`currentAbortController` 复位）后，立即发出新消息。整个切换在同一个 JavaScript 任务队列中串行完成，不存在两次请求并发的窗口。
- **epoch 防竞争机制**：使用 `latestSendEpoch` 单调计数器，等待期间若又有更新的发送进来，当前这次自动放弃，由最新那次接管，避免快速连发时发出重复或乱序请求。
- **`activeSendTask` 追踪**：模块级 Promise 引用，确保等待的是同一个任务实例。
- **打断文案区分**：新增 `pivotSupersededBySend` 标记，被新消息打断时不追加"已由用户中断生成"注释，历史消息干净截断；用户手动点"停止"仍保留原有提示文字。
- **空输入保护**：空输入不触发打断逻辑，不会意外中止正在进行的生成。

#### 验证

- `node --check client/chat/engine.js` 语法检查通过。

## [v0.0.234] - 2026-07-31

### 工作流编排条件分支落地、节点类型扩展与画布交互升级

本版本对照 Dify 等前沿工作流平台的编排范式，补齐 Pivot 工作流的分支控制能力、通用节点类型与画布可视化反馈，并把节点配置从"以 JSON 为主"推进到"以结构化表单为主"。

#### 条件分支从死代码转为可用能力

- **修复条件规则从不生效的缺陷**：`when` 条件规则此前已在 `normalizeDagSpec` 中完成 16 种操作符的规范化并持久化到数据库，但运行时从未对其求值，`condition: 'failure'` 同样未被处理，导致用户配置的分支条件被静默忽略。本版本新增 `evaluateDagWhen` 与 `dagConditionSatisfied`，让这套已有机制真正参与调度。
- **完整操作符求值**：支持等于、不等于、包含、不包含、开头是、结尾是、大于、大于等于、小于、小于等于、为空、不为空、存在、不存在、为真、为假共 16 种判断；数值比较优先按数值语义处理，非数值场景自动回退为文本比较。
- **失败分支编排**：`condition` 新增"上游失败后执行"语义，可编排失败补偿、告警通知和降级路径，与"上游成功后执行"、"始终执行"构成完整的三态依赖门禁。
- **跳过原因可追溯**：条件不满足时，节点状态、步骤记录和运行详情会写入判断变量、操作符、期望值与实际值，不再只显示一个笼统的"已跳过"。
- **前端字段透传修复**：前端 `ensureDefaults` 与 `serialize` 此前会丢弃 `when` 字段，导致画布保存即清空条件配置；现已完整透传，条件分支在编辑、保存、发布和运行链路保持一致。

#### 三种通用节点类型

- **代码执行（`agent.code`）**：在 `vm` 沙箱中执行内联 JavaScript，用于上游数据的转换、计算、过滤和格式整理。沙箱不暴露 `require`、`process` 和 `global`，执行超时 5 秒，通过 `vars` 注入的变量支持模板引用，用 `return` 返回结果。
- **HTTP 请求（`agent.http`）**：调用外部 REST API，支持 GET/POST/PUT/DELETE/PATCH、自定义请求头和 JSON 请求体，返回状态码、响应头与响应数据。复用既有安全 HTTP 客户端的 SSRF 防护，内网地址与非法目标会被拦截，超时上限 30 秒。
- **变量聚合（`agent.merge`）**：把多个上游节点的输出合并为单一对象，支持显式字段映射重命名，便于下游节点用统一字段名引用，减少深层 `{{nodes.*.output.*}}` 引用链。

#### 画布可视化与运行状态反馈

- **节点类型视觉区分**：为大模型、委派智能体、智能体交接、代码执行、HTTP 请求、变量聚合、知识检索、数据库、可视化、报告和 MCP 工具建立图标与主题色映射，画布上可直接按颜色和图标识别节点职责，无需逐个点开查看。
- **运行状态叠加到画布**：运行中节点显示旋转指示器与边框脉冲动画，完成显示对勾与耗时，失败显示叉号与错误摘要，跳过显示跳过标记并降低不透明度；执行进度直接反映在拓扑图上，不必在步骤列表和画布之间来回对照。
- **深色模式补齐**：新增节点主题在深色模式下的独立配色，避免浅色填充在深色画布上刺眼。

#### 左侧节点库面板

- **Dify 风格节点面板**：画布左侧新增常驻节点库，按"智能体"、"逻辑控制"、"数据与检索"、"可视化与报告"四组展示 10 种节点类型，每张卡片包含图标、名称和用途说明，点击即插入画布。
- **节点搜索**：支持按名称、用途和工具名过滤，节点类型增多后仍可快速定位。
- **响应式收敛**：窄屏（≤900px）自动隐藏面板并释放画布宽度，原有工具栏下拉入口保持可用，两种添加方式并存。

#### 节点配置结构化

- **条件分支可视化配置**：节点属性面板新增条件分支区，用"变量 + 判断 + 值"三段式表单配置 `when` 规则，变量输入框提供上游节点输出与运行输入的自动补全，一元操作符（为空、存在、为真等）自动隐藏无意义的值输入框。
- **抽屉过渡动画**：节点属性抽屉由 `display: none` 硬切换改为位移与透明度过渡，开合有明确的方向感。

#### 界面细节修复

- **原始输出区可读性**：运行步骤的原始输出折叠区高度上限从 180px 提升到 380px，长输出不再被过度压缩。
- **补齐运行步骤深色模式**：运行步骤列表此前完全没有深色模式覆写，卡片、表格、KPI、Markdown 输出和内联代码在深色主题下存在对比度问题，本版本统一补齐。
- **修复工具栏状态样式失效**：工具栏状态元素的类名存在拼写错误（`pivot-dag-ctx.toolbar-status`），导致校验状态提示一直未应用样式，现已修正。

#### 验证

- 全仓测试 `692/692` 通过，ESLint 零错误零警告；文本完整性、开发规范、397 个 JavaScript 文件语法、聊天资源清单（29 个片段 / 116 项样式导入）、安全 HTML、`window.*` 全局暴露基线和 E2E 冒烟检查全部通过。
- 新增的运行状态指示器改用 DOM 节点构建，符合项目禁止裸 `innerHTML` 赋值的安全约束；新增的两个全局导出已按规范登记到 `window.*` 基线。

## [v0.0.233] - 2026-07-30

### 智能体产品化运行与工作流编排综合升级

本版本汇总并正式发布 v0.0.230 至 v0.0.232 的连续优化成果，围绕“结果可读、编排稳定、运行可追踪、质量可回归、协作可审计”完成智能体工作台的产品级升级。普通任务、工作流构建、执行详情、恢复、评测和多智能体协作现已形成统一闭环。

#### 任务结果与前端体验

- **告别原始 JSON 主视图**：普通任务、工具步骤、工作流节点和最终答案会自动识别对象、数组、表格、摘要及 Markdown 内容，使用信息块、键值列表和表格展示；原始结构保留在折叠区，兼顾日常阅读与技术排查。
- **运行详情重构**：DAG 任务不再把相同节点重复显示为通用步骤；最终结果、工具输出、契约状态、错误信息、检查点和运行追踪按信息层级组织，委派与交接结果使用独立的角色和摘要视图。
- **响应式交互收口**：智能体配置入口调整为稳定的 2×2 操作矩阵；评测中心、编辑器、运行详情和编排器适配桌面与窄屏，移动端 Toast 移至底部安全区并限制宽度和换行，避免遮挡顶部命令。

#### 工作流编排与大模型节点

- **布局持久化**：DAG 节点坐标统一保存到根级 `layout`，兼容旧 `_x/_y` 数据迁移；新增、删除和拖拽仅影响相关节点，已有人工布局不会被自动重排覆盖。
- **主大模型节点显式化**：新增 `primaryLlmNodeId`，编辑器、预检、发布、执行、恢复和自由任务生成工作流均使用同一主节点语义；画布、侧栏和工具栏提供清晰标记与切换入口。
- **布局与依赖解耦**：节点连接关系只由依赖定义决定，不再受画布左右位置影响；统计分析和多智能体模板会生成稳定坐标、明确依赖与唯一主大模型节点。
- **节点数据契约**：DAG 节点支持 `inputSchema` / `outputSchema`，提供常用类型预设、工具契约同步和高级编辑；预检与发布阻止无效契约、不可用工具和静态输入不匹配，运行时对解析后的真实数据严格校验并返回可读字段路径。

#### 可观测、恢复与质量治理

- **Agent Trace**：新增用户隔离的 Trace/Span 存储，覆盖模型路由、规划、流式工具、DAG 节点、总结、审批、委派和交接；运行详情以时间瀑布展示状态、耗时和上下文，密码、令牌、密钥、授权头及 Cookie 在持久化前递归脱敏。
- **持久化检查点**：规划、工具、DAG 和控制步骤会保存有大小上限的恢复状态；自由任务续跑可恢复成功观察，工作流从失败节点继续并复用已完成节点结果。
- **质量评测中心**：支持自由任务和已发布工作流的评测集、用例管理、归档、真实批量运行、任务追踪回链及用户隔离；评测子任务不混入普通任务列表。
- **确定性回归与基线**：支持必须包含、禁止包含、期望文本、长度、耗时、Token、有效 JSON 和输出 JSON Schema 规则，保存平均分、通过率、耗时、Token 以及相对上一批次的变化。

#### 多智能体与运行治理

- **角色化委派**：新增 `agent.delegate`，支持研究员、分析员、审阅员、撰写员和自定义角色在隔离上下文中执行子任务，并将结果返回 Supervisor。
- **结构化交接**：新增 `agent.handoff`，统一记录来源、目标、摘要、发现、证据、风险、未决问题、置信度和状态，避免协作过程退化为不可审计的自由文本。
- **Supervisor 模板**：编排器新增“多智能体审阅”模板，一键生成并行研究、独立审阅和主 Supervisor 裁决结构，同时建立主节点、依赖、画布位置和追踪类型。
- **资源与权限边界**：评测集、批次、结果、Trace 和检查点均按任务所有者隔离；同一评测集禁止重复运行，评测批次限制并发，历史用例软删除保留追溯，工具权限、审批和预算仍继承原工作流安全边界。

#### 工程质量与验证

- **数据与模块边界**：新增评测、Trace、Span 和检查点数据表及索引，补齐旧库幂等迁移；评测前端通过 `Pivot.modules` 暴露模块 API，不新增页面级 `window.*` 全局变量。
- **自动化回归**：新增 DAG 布局、结果可读化、契约、追踪脱敏、检查点、评测隔离、多智能体工具与 Handoff 测试；智能体核心回归 `51/51`、全仓测试 `691/691` 通过，ESLint 零错误零警告，文本完整性、开发规范、395 个 JavaScript 文件语法、资源清单、安全 HTML、全局变量和差异检查全部通过。
- **视觉验收**：完成 1440×1000 桌面与 390×844 移动端实际浏览器验收，确认页面无横向溢出、节点无重叠、主 Supervisor 唯一且交互控件不被提示层遮挡。

## [v0.0.232] - 2026-07-30

### 智能体质量评测中心与多智能体协作编排升级

本次更新继续按产品级智能体平台标准完善“构建、运行、评测、回归、协作、审计”闭环，在 v0.0.231 的 Trace、数据契约和检查点基础上，补齐可复用评测集、真实任务批量回归、质量基线对比，以及可审计的多智能体委派与交接能力。

- **质量评测中心**：智能体工作台新增独立“质量评测”入口，支持创建、编辑、归档评测集，为自由任务或已发布工作流维护最多 50 个隔离用例，并集中展示评测集、用例、最近均分、通过率和运行中批次。
- **真实任务批量回归**：每个评测用例通过正式智能体运行链路执行，继承模型、工作流版本、运行模式、工具策略、审批、步骤和 Token 预算；评测子任务从普通任务列表隐藏，但可从用例结果直达完整任务追踪。
- **确定性质量规则**：支持必须包含、禁止包含、期望文本、最少字数、最长耗时、Token 上限、有效 JSON 和输出 JSON Schema 校验；评分结果展示逐条规则、实际输出、耗时、Token 和异常原因。
- **质量基线对比**：评测批次保存平均分、通过率、完成进度、总耗时和总 Token，并与上一完成批次计算分数及通过率变化，为版本回归提供稳定基线。
- **运行结果沉淀**：普通智能体任务详情新增“加入评测集”，可直接把真实任务目标沉淀为回归用例，缩短从生产问题到固定测试集的路径。
- **多智能体委派**：新增内置工具 `agent.delegate`，支持研究员、分析员、审阅员、撰写员和自定义角色在隔离上下文中执行子任务，并把可读结果与结构化交接信息返回 Supervisor。
- **结构化 Handoff**：新增 `agent.handoff`，统一记录交接来源、目标、摘要、发现、证据、风险、未决问题、置信度和状态，避免多智能体协作退化为不可审计的自由文本。
- **Supervisor 工作流模板**：编排器新增“多智能体审阅”模板，一键生成并行研究智能体、审阅智能体与主 Supervisor 裁决节点；主大模型节点、依赖关系、画布位置和运行追踪会同步建立。
- **可读运行体验**：委派与交接节点在 DAG 结果和 Trace 时间瀑布中使用独立类型、角色名称和摘要展示，原始结构继续收进折叠区；智能体配置入口调整为稳定的 2×2 操作矩阵，移动端 Toast 改到安全区内并限制宽度与换行，避免遮挡顶部命令。
- **隔离与资源治理**：评测集、批次、用例和结果按用户隔离；同一评测集禁止重复运行，全局最多同时执行 3 个批次，历史用例采用软删除以保留已完成批次的可追溯性。
- **回归验证**：新增评测规则、用户隔离、批次汇总、历史用例保留、多智能体工具契约与 Handoff 结构测试；核心智能体回归 `51/51` 通过，并完成质量评测中心和多智能体模板在 1440px 桌面与 390px 窄屏下的视觉、溢出、节点重叠和主 Supervisor 验收。

## [v0.0.231] - 2026-07-30

### 智能体运行追踪、工作流数据契约与发布治理升级

本次更新将智能体从“能执行、能查看步骤”升级为“可追踪、可验证、可解释”的产品化运行闭环，重点补齐 Agent Trace、DAG 节点输入输出契约、发布前检查和对应的前端交互。

- **Agent Trace 运行追踪**：新增独立的 `agent_traces` 与 `agent_trace_spans` 数据模型，覆盖模型路由、规划模型、流式工具调用、DAG 节点、最终总结和审批等待等关键环节，并提供按任务隔离的追踪查询接口。
- **追踪隐私保护**：输入、输出和运行元数据写入前会递归脱敏密码、令牌、密钥、授权头和 Cookie 等敏感字段，并限制层级、条目数与文本长度，追踪失败不会影响主任务执行。
- **运行详情时间瀑布**：任务详情新增可折叠的运行追踪视图，展示环节类型、状态、耗时、时间分布和可读上下文；异常任务自动展开，原始结构继续收进折叠区。
- **DAG 数据契约**：工作流节点新增 `inputSchema` / `outputSchema`，支持对象、列表、文本、数值、整数、布尔值、必填字段、枚举、范围和嵌套结构校验；模板变量在发布前检查中保持兼容，运行时使用解析后的真实值严格校验。
- **契约编辑体验**：节点侧栏直接展示输入/输出契约状态、字段数和必填数，提供文本/对象/列表快捷预设、工具输入契约同步和高级编辑弹窗；运行结果节点显示“契约通过/未通过”及可读错误路径。
- **预检与发布门禁**：任务预检新增契约覆盖率和节点级问题，工作流发布前会阻止无权限工具、无效契约和静态输入不匹配；缺少输出契约作为可继续处理的风险提示，不破坏旧工作流兼容性。
- **执行链路统一**：标准规划、流式 `tool_calls` 与 DAG 三条执行路径统一采集追踪；DAG 节点存档新增契约状态和问题列表，断点续跑、失败策略和人工审批语义保持不变。
- **持久化检查点恢复**：新增 `agent_run_checkpoints`，在规划、工具、DAG 和控制步骤后保存有大小上限的恢复状态；自由任务续跑恢复最近成功观察与失败上下文，DAG 续跑优先复用已完成节点并从失败节点继续。
- **验证与回归**：新增 Agent Trace/契约测试并扩展智能体安全测试，核心回归 `48/48` 通过；392 个 JavaScript 文件语法、聊天资源、开发规范、文本完整性、安全 HTML 和差异检查全部通过，并完成桌面与 390px 窄屏浏览器验收。

## [v0.0.230] - 2026-07-30

### 工作流编排大模型节点与结果展示优化

本次更新主要围绕工作流编排的主大模型节点位置、布局持久化和执行结果可读性做了全面修复，同时补齐对应的回归测试与样式适配。

- **布局持久化重构**：工作流 DAG 节点坐标改为根级 `layout` 保存，兼容旧版 `_x/_y` 坐标迁移；新增、删除和拖拽只做增量落位，不再整图重排。
- **主大模型节点显式化**：编辑器、运行预检、恢复/续跑、自由任务草稿统一使用 `primaryLlmNodeId`，并在画布、侧栏和工具栏中提供明确标记。
- **结果展示可读化**：普通任务与工作流执行页的 JSON 输出改为结构化摘要、列表和表格展示，原始内容收进折叠区域。
- **模板与样式同步**：统计图模板改为查询 + 大模型 + 图表并行布局，补齐主节点视觉、抽屉状态和移动端样式。
- **回归测试补齐**：新增 DAG 布局测试，覆盖坐标迁移、主节点识别、依赖规则和结果展示回归。

## [v0.0.229] - 2026-07-26

### 生产依赖高危清零、CI 全量校验收口与内置报表 CSV 中文乱码修复

本版本处理一轮全项目检查发现的三类问题：生产依赖高危漏洞、CI 实际校验范围远小于本地、以及内置工具箱读取无 BOM 的 UTF-8 CSV 时中文乱码。三项均已修复并补齐回归。

#### 生产依赖安全

- **axios 升级到 1.18.1**：清除 10 项高危告警，其中「NO_PROXY 对 0.0.0.0 本地地址失效」和「原型污染改写请求构造」会直接削弱服务端既有的 SSRF 防护；模型转发、工具箱调用、OCR HTTP 适配器与内网通知均走该出站链路。
- **sharp 升级到 0.35.3（libvips 8.18.3）**：修复 libvips 的 CVE-2026-33327、CVE-2026-33328、CVE-2026-35590、CVE-2026-35591；该库直接处理用户上传图片，并被图片安全校验、文档处理与 PDF 渲染共用。
- **js-yaml 升级到 4.3.0**：修复 YAML merge key 链导致的二次方 CPU 消耗；该依赖经 electron-updater 用于解析桌面端更新清单，与离线局域网 HTTP 更新源相关。4.3.0 仍满足 electron-updater 的 `^4.1.0` 范围，无需引入依赖覆盖。
- **body-parser 补丁升级**：修复非法 limit 取值导致体积限制静默失效的低危问题。
- **运行时版本要求收紧**：`engines.node` 由 `>=20` 调整为 `>=20.9.0`，与 sharp 0.35 的最低要求一致；Docker 镜像与 CI 均使用 Node 22，不受影响。

#### 依赖审计门禁

- **豁免必须可追溯**：审计豁免登记必须同时写明理由和复查日期，缺任一项即视为无效豁免。
- **豁免自动到期**：超过复查日期的豁免自动失效并让门禁转红，避免临时豁免沉淀为永久放行；上游已发布修复版本时一律不接受豁免。
- **清理历史豁免**：`form-data` 与 `multer` 的历史豁免已随依赖修复失效，从清单中移除，当前生产依赖无豁免项。
- **消除脚本自身告警**：审计脚本改为直接调用 `npm.cmd`，不再通过 shell 选项传参，消除 Node 的 DEP0190 安全弃用告警。

#### 持续集成校验范围

- **补齐项目检查**：CI 此前只运行文本完整性、语法和聊天资源三项检查，开发规范门禁、安全 HTML 扫描、window 全局扫描与 E2E 脚手架检查从未在 CI 执行；现统一改为运行 `npm run check`。
- **补齐测试范围**：CI 此前只运行 `tests/security.test.js` 单个套件，其余 15 个套件从未在 CI 执行；新增 `npm run test:all` 顺序运行全部测试套件，`npm test` 同步指向全量口径。

#### 内置工具箱

- **修复 CSV 中文乱码**：内置报表工具读取 CSV 时先按 UTF-8 解码并剥离 BOM 再解析，此前直接以 Buffer 交给解析库会按单字节编码处理，导致不带 BOM 的 UTF-8 中文表头和内容整体乱码，并把乱码带入模型上下文。文件摘要与表格查询共用该读取实现，一并修复。
- **影响范围确认**：知识库与附件解析的 CSV 走文本抽取（UTF-8 读取）、数据分析数据集导入的 CSV 走 DuckDB，两处均不受该问题影响。

#### 测试与文档

- **对话路由级回归**：新增额度拒绝、会话越权、模型缺失、知识库命中与未命中、长期记忆命中、上下文自动裁剪、当前输入超限预检和流式中断持久化共 9 项路由级测试，并抽出可复用的聊天路由测试装置。
- **内置工具箱回归**：新增 15 项测试，覆盖分发层、格式转换、数据处理，以及报表目录的路径穿越防护、非白名单扩展名拦截和 CSV 编码。
- **门禁回归**：新增 7 项测试覆盖豁免到期、登记不完整、日期不可解析和上游已有修复等分支。
- **优化路线图补齐**：`docs/optimization-roadmap.md` 此前停留在 v0.0.190，已补齐至当前版本，全部里程碑标注真实状态，并新增「与 CHANGELOG、版本号同级维护」的约定和已知技术债章节。其中记录了 v0.0.191 暂缓的 Electron 主版本升级至今仍未评估。

#### 验证

- **依赖审计**：`npm audit --omit=dev` 结果为 0 项漏洞，`npm run audit:policy` 通过。
- **完整测试**：`npm test` 通过，含项目检查、ESLint 与全部测试套件共 673 项。

## [v0.0.228] - 2026-07-25

### Qwen3.6 代码补全增加 llama.cpp thinking 硬开关

生产环境复测表明，`v0.0.227` 追加的 `/no_think` 软指令没有被 Qwen3.6 使用的 llama.cpp `peg-native` chat template 严格执行：模型日志显示每次请求实际生成了 77、89、318 个输出 token，但这些 token 仍被 thinking 占用，客户端最终没有收到可插入代码。本版本根据 Qwen 与 llama.cpp 的请求规范增加模板级硬开关。

- **模板级 thinking 硬关闭**：Qwen3 系列的 `/v1/completions` 及 prompt 风格 `/v1/chat/completions` 请求会强制携带 `chat_template_kwargs: { "enable_thinking": false }`，由 llama.cpp 在套用 chat template 时直接关闭思考。
- **软硬开关双保险**：保留最后一条用户消息中的 `/no_think`，同时增加模板硬开关；前者兼容仅识别 Qwen 软指令的服务，后者解决 `peg-native` 模板忽略软指令的问题。
- **客户端参数安全合并**：调用方已有 `chat_template_kwargs` 会被保留，但代码补全场景下的 `enable_thinking` 最终固定为 `false`，避免客户端配置重新开启思考。
- **Qwen2.5 保持不变**：硬开关只匹配 Qwen3 系列，Qwen2.5 和其他普通模型不会新增模板参数，也不会改变现有补全行为。
- **生产诊断增强**：空正文警告新增 `hardThinkingDisabled` 字段，可直接确认 Pivot 是否向上游启用了模板硬开关，并继续记录 reasoning、结束原因、token 用量和输出上限。
- **生产等价回归**：非流式、流式和批量 Qwen3 模拟上游改为只有同时收到软、硬开关才返回正文，并新增 Qwen2.5 请求体不受影响测试；完整安全测试 `316/316` 通过。
- **版本号启用**：应用版本升级至 `v0.0.228`，生产环境需部署该版本后再复测 Qwen3.6 代码补全。

## [v0.0.227] - 2026-07-24

### 推理模型代码补全与 Completions 兼容层完整修复

本版本修复离线生产环境中 Qwen3.6 等推理模型通过 OpenAI-compatible API 执行代码补全时，请求成功但客户端收不到补全文本的问题，并完整收口 `/v1/completions` 的请求参数、批量 prompt、多候选响应、流式错误与诊断行为。

- **代码补全自动关闭思考**：`/v1/completions` 以及 `prompt` / `input` / `prefix` / `suffix` 风格的 `/v1/chat/completions` 请求，会对后台标记为推理模型或名称匹配 Qwen3、QwQ、DeepSeek-R1 的模型，在最后一条用户消息追加 `/no_think`。
- **上下文裁剪保持软开关**：no-think 指令会先进入上下文预算与裁剪流程，确保裁剪后的实际上游请求仍携带该指令，不会被原始消息覆盖。
- **普通模型行为保持不变**：Qwen2.5 等非推理模型不会追加 no-think 指令，现有代码补全请求参数、流式响应和正文转换逻辑保持兼容。
- **Completions 参数补齐**：支持并正确处理 `n`、`best_of`、`echo`、`logprobs`、`seed`、`stream_options`、`logit_bias`、`user`，保留 `temperature`、`max_tokens`、`stop`、`top_p`、presence/frequency penalty 与 `suffix` 兼容；`best_of` 会按候选平均 logprob 选优。
- **批量 prompt 与多 choice 补齐**：文本 `prompt[]` 会逐项调用聊天模型并合并为连续 choice 索引，非流式聚合 usage，流式转换全部 choices、聚合最终 usage，且整个批次只发送一个 `[DONE]`。
- **legacy 响应语义补齐**：`echo` 会回显对应 prompt，Chat Completions 的 token logprobs 会转换为 legacy `tokens`、`token_logprobs`、`top_logprobs` 和累计 `text_offset`。
- **流式异常不再伪装成功**：上游 socket 中断、SSE error 数据帧或非法 SSE 数据会返回结构化 SSE `error` 并立即结束，错误路径不再追加 `[DONE]`；失败调用同步写入模型失败状态和 API 调用审计。
- **双入口空正文诊断**：`/v1/completions` 和 prompt 风格 `/v1/chat/completions` 的非流式、流式响应若最终没有可见正文，都会记录模型、入口、是否存在 `reasoning_content`、`finish_reason`、token 用量及请求输出上限。
- **思考内容继续隔离**：诊断只记录状态与用量，不会把 reasoning 内容作为代码返回，也不会暴露给 Continue 等补全客户端。
- **输入错误显式返回**：文本 prompt 数组完整支持；token ID 数组因聊天适配层无法获知上游分词器而明确返回 `400 token_prompt_not_supported`，不再静默丢弃或错误拼接。
- **回归验证补齐**：新增 Qwen3/Qwen2.5、双入口空正文、文本 prompt 批量、`n/best_of/echo/logprobs`、多 choice 流、usage 聚合、SSE error 与 socket 中断测试；完整安全测试 `315/315` 已通过。
- **版本号启用**：应用版本升级至 `v0.0.227`，用于离线生产部署和桌面客户端重新打包。

## [v0.0.226] - 2026-07-15

### 删除用户后支持同名重新注册

本版本修复了已删除用户仍占用用户名、导致同名账号无法再次注册的问题。系统继续保留旧账号的审计记录，同时释放其用户名，并确保重新注册创建的是全新身份。

- **删除后释放用户名**：管理员删除用户时，系统会保留删除前用户名用于审计，并释放登录用户名供后续注册或管理员重新创建。
- **新旧身份严格隔离**：同名重新注册会生成新的用户 ID，不会复用旧账号，也不会继承旧账号的设置、会话或其他关联数据。
- **历史记录自动迁移**：新增幂等数据库迁移，服务启动时自动处理已有软删除用户，无需管理员手工清理历史记录。
- **审计展示保持可读**：用户管理、统计、合规导出、公告、模型、工具箱、知识库和 Agent 记录继续展示删除前用户名，不暴露内部归档标识。
- **回归验证补齐**：新增用户名迁移与“删除后同名注册”测试，完整安全测试 `309/309`、项目检查和 ESLint 均已通过。
- **版本号启用**：应用版本升级至 `v0.0.226`，用于桌面客户端重新打包和自动更新检测。

## [v0.0.225] - 2026-07-11

### 更新日志版本连续性修正

本版本补齐更新日志中缺失的 `v0.0.224` 与 `v0.0.225` 记录，并确认项目最新版本号统一为 `v0.0.225`。

- **更新日志补齐**：恢复 `v0.0.224` 发布记录，确保版本历史从 `v0.0.223` 到 `v0.0.225` 连续。
- **最新版本确认**：顶部最新版本保留为 `v0.0.225`，与 `package.json` 和 `package-lock.json` 的 `0.0.225` 保持一致。
- **文档版本同步**：同步 README、使用帮助和项目汇报中的当前版本展示，避免仍停留在 `v0.0.224`。
- **版本号启用**：应用版本保持 `v0.0.225`，用于桌面客户端重新打包和自动更新检测。

## [v0.0.224] - 2026-07-11

### 客户端窗口体验与使用帮助优化

本版本修复了 Windows 客户端打包后任务栏系统名称缺失的问题，精简并美化了关于弹窗，并对用户使用帮助文档进行了润色与入口指引优化。

- **使用帮助优化**：对用户使用帮助手册中的“帮助与关于入口”（1.5 章节）进行了优化润色，移除了冗余的技术实现细节，使其更聚焦于用户指引，并增加了在桌面客户端中调出“关于”弹窗的操作指引。
- **任务栏名称修复**：修复 Windows 客户端运行阶段底部任务栏仅显示 Logo 图标、缺少系统名称的问题，允许窗口标题动态同步。
- **关于弹窗精简**：移除了“关于”弹窗中的环境信息显示，使信息聚焦于核心的版本和运行模式。
- **弹窗边框显示**：为“关于”弹窗的外层容器添加了 1px 细边框，提升无边框窗口在不同桌面背景下的辨识度与质感。
- **代码检查与修复**：修复了 ESLint 对 `mcp-workbench-form.js` 和 `mcp-workbench-main.js` 的 16 个未定义全局变量错误，并清除了测试用例中的未使用变量警告。
- **版本号启用**：应用版本升级至 `v0.0.224`，用于桌面客户端重新打包和自动更新检测。

## [v0.0.223] - 2026-07-11

### 客户端界面清理与关于页面源码优化

本版本移除了主侧边栏底部的 copyright 声明以及登录面板的安全提示信息，使主侧边栏和登录流程的视觉体验更加清爽。同时对桌面关于窗口的源码进行了易读性优化。

- **关于页面实体还原**：将 `desktop/about.html` 源码中所有用 HTML 实体字符（NCR）编码的中文文本完全解码为 UTF-8 中文显示，提高源码的可读性和开发维护效率。
- **侧边栏版权移除**：移除了主页左侧导航栏面板中用户姓名下方的 copyright 声明，避免内容冗余，精简左侧布局。
- **登录面板提示移除**：移除了登录卡片底部的“安全可信的内网 AI 平台”提示及数据存储安全提示 div 节点，使登录界面更清爽干净。
- **死代码与警告清理**：移除了 `desktop/main.js` 中已废弃且从未使用的 `windowTitle` 和 `sendRendererMenuAction` 函数，消除了相关的 ESLint 警告。
- **版本号启用**：应用版本升级至 `v0.0.223`，重新打包并发布到更新源后，已安装的旧版客户端可按自动更新规则检测到新版本。

## [v0.0.222] - 2026-07-11

### 关于 Pivot 弹窗底部操作收口

本版本继续优化“关于 Pivot”弹窗，去除与右上角关闭按钮功能重复的底部“确定”按钮，解决小窗口下按钮和版权信息占用底部空间的问题。

- **确定按钮移除**：关于弹窗仅保留右上角关闭按钮和 `Esc` 关闭，避免两个关闭入口重复。
- **底部区域清空**：移除底部版权信息和确认按钮，弹窗信息在主体区内完整展示，避免内容贴边或被裁切。
- **弹窗尺寸微调**：关于窗口高度调整为 `360px`，移除底部内容后保持更轻巧的视觉重心。
- **回归验证**：已通过 `node --check desktop/main.js`、`node --check desktop/about-preload.js`、`node scripts/check_chat_assets.js` 和 `npm run check`。
- **版本号启用**：应用版本升级至 `v0.0.222`，重新打包并发布到更新源后，已安装的旧版客户端可按自动更新规则检测到新版本。

## [v0.0.221] - 2026-07-11

### 关于 Pivot 弹窗视觉优化

本版本继续打磨桌面客户端的壳层信息展示，将“关于 Pivot”弹窗从简单信息堆叠调整为更清晰的品牌信息面板。

- **弹窗尺寸优化**：关于窗口放大至 `500x400`，给版本、环境和运行模式留出更稳定的展示空间。
- **信息卡片重排**：当前版本和环境并列展示，运行模式独占一行，避免 `远程模式 (remote)` 等文案被截断。
- **视觉层次增强**：品牌区放大图标与标题层级，摘要说明改为浅绿提示面板，底部操作和版权信息重新对齐。
- **可访问性保留**：保留 `Esc` 关闭、关闭按钮和确定按钮，并为动态值补充 `title` 悬停信息。
- **回归验证**：已通过 `node --check desktop/main.js`、`node --check desktop/about-preload.js`、`node scripts/check_chat_assets.js` 和 `npm run check`。
- **版本号启用**：应用版本升级至 `v0.0.221`，重新打包并发布到更新源后，已安装的旧版客户端可按自动更新规则检测到新版本。

## [v0.0.220] - 2026-07-11

### 桌面客户端菜单重组与编辑入口清理

本版本针对客户端顶部“编辑”菜单的实际可用性进行收口：自定义菜单点击会改变当前输入焦点，撤销、重做、剪切、粘贴等文本编辑动作容易表现为无效。桌面端改为保留更明确、更稳定的页面、显示和客户端维护能力。

- **编辑菜单移除**：页面自定义菜单和 Electron 原生菜单均不再显示撤销、重做、剪切、复制、粘贴和全选，文本编辑继续使用输入框和系统原生快捷键。
- **页面菜单新增**：“页面”承载刷新页面、清理缓存并刷新，适合客户端更新后主动清理静态缓存。
- **显示菜单拆分**：“显示”仅保留实际大小、放大、缩小和切换全屏，语义更贴近窗口显示设置。
- **客户端菜单聚焦维护**：“客户端”仅保留检查客户端更新、关于 Pivot 和退出客户端；帮助继续由左下角“帮助”入口承载。
- **死代码清理**：移除页面菜单的 `document.execCommand` 编辑命令链路，以及不再使用的 `pivot-desktop:menu-action` 渲染进程菜单转发暴露。
- **回归验证**：已通过 `node scripts/check_chat_assets.js`、`node --check client/chat/desktop-menu.js`、`node --check desktop/main.js`、`node --check desktop/preload.js` 和 `npm run check`。
- **版本号启用**：应用版本升级至 `v0.0.220`，重新打包并发布到更新源后，已安装的旧版客户端可按自动更新规则检测到新版本。

## [v0.0.219] - 2026-07-11

### 桌面客户端顶部菜单收口与关于窗口优化

本版本继续打磨桌面客户端顶部菜单，将和页面导航重复的“会话”菜单完整移除，并对“关于 Pivot”弹窗进行客户端化视觉优化。

- **会话菜单移除**：桌面端顶部不再显示“会话”一级菜单，新建对话、搜索会话、应用中心、知识库、工具箱、智能体、系统设置和退出登录继续由页面左侧栏和用户区承载。
- **顶部菜单收敛为三类**：标题栏仅保留“编辑 / 视图 / 客户端”，对应文本编辑、窗口显示和客户端维护三类壳层能力。
- **开发者工具移除**：“视图”菜单不再显示开发者工具，只保留刷新、实际大小、放大、缩小和切换全屏。
- **关于窗口自定义**：新增 `desktop/about.html` 和 `desktop/about-preload.js`，关于 Pivot 改为无边框桌面模态窗口，显示品牌、版本、运行模式和环境信息，并提供自定义关闭与确定操作。
- **客户端帮助入口去重**：“客户端”菜单移除“打开使用帮助”，帮助继续由左下角“帮助”入口承载。
- **页面与原生菜单对齐**：页面顶部菜单和 Electron 原生菜单均同步收口，避免两套菜单行为不一致。
- **回归验证**：已通过 `node scripts/check_chat_assets.js`、`node --check client/chat/desktop-menu.js`、`node --check desktop/main.js`、`node --check desktop/about-preload.js` 和 `npm run check`。
- **版本号启用**：应用版本保持 `v0.0.219`，重新打包并发布到更新源后，已安装的 `v0.0.218` 客户端可按自动更新规则检测到新版本。

## [v0.0.218] - 2026-07-11

### 桌面客户端顶部样式修正

本版本针对桌面客户端隐藏原生标题栏后的顶部布局进行修正，让窗口顶部、左侧品牌区和右侧窗口控制区恢复一致的视觉层级。

- **标题栏菜单归位**：将“文件 / 编辑 / 视图 / 帮助”菜单从左侧栏顶部移到独立的桌面标题栏区域，与右侧最小化、最大化和关闭按钮保持同一行。
- **侧栏品牌恢复**：恢复新建对话按钮左侧的 Pivot 智枢品牌标识，避免客户端菜单占据原有品牌位置。
- **顶部横线补齐**：桌面标题栏底部分隔线改为贯通整个窗口宽度，补齐右侧窗口控制按钮下方断线。
- **菜单节点收口**：页面只保留一个 `desktop-app-menu` 节点，避免重复 id 导致菜单脚本绑定到错误位置。
- **回归验证**：已通过 `node scripts/check_chat_assets.js`、`node --check client/chat/desktop-menu.js` 和 `npm run check`。
- **版本号启用**：应用版本升级至 `v0.0.218`，重新打包并发布到更新源后，旧版客户端可按自动更新规则检测到新版本。
## [v0.0.217] - 2026-07-11

### 登录态过期自动回到登录页

本版本修复 cookie 或登录态失效后仍停留在业务页面、需要用户手动退出才能重新登录的问题。前端统一请求层现在能识别 access token、CSRF cookie 和 refresh token 的不同失效路径：可恢复时自动刷新并重试原请求，不可恢复时清理本地登录状态并自动回到登录页。
- **认证失败统一收口**：`apiFetch` 现在统一识别 `TOKEN_EXPIRED`、`AUTH_MISSING`、`TOKEN_INVALID` 和 `CSRF_INVALID`，避免业务页面各自处理 401/403 时遗漏跳转。
- **自动刷新与重试**：access cookie 或 CSRF cookie 过期时会先调用 `/api/auth/refresh`，刷新成功后带新的 CSRF token 重试原请求，减少用户无感登录态续期失败。
- **刷新失效自动回登录**：当 refresh token 也失效、被清理或用户状态不可继续刷新时，前端会关闭实时任务/公告监听、清空当前用户和会话状态，并自动显示登录页。
- **后端错误码补齐**：未携带认证 cookie 的请求返回 `AUTH_MISSING`，CSRF 校验失败返回 `CSRF_INVALID`，让前端可以稳定区分“登录态失效”和普通权限不足。
- **桌面客户端刷新菜单**：桌面端恢复中文“文件 / 编辑 / 视图 / 帮助”菜单，并在左侧顶部客户端环境显示同名菜单；“刷新页面”使用无缓存重载，“清理缓存并刷新”会清理 PWA、Service Worker 和 Electron 缓存但不清除登录 cookie。
- **客户端标题栏去品牌化**：桌面客户端隐藏 Windows 原生标题栏里的 Pivot 图标和标题文字，保留窗口控制按钮和无品牌拖拽区，避免和页面内品牌区重复。
- **回归验证补齐**：新增 `apiFetch` 刷新成功重试、refresh 失效回登录以及 CSRF 错误码断言；已通过 `node tests\security-auth.test.js` 和 `npm run check`。
- **失效时间说明**：当前默认 access/CSRF cookie 失效时间为 480 分钟（8 小时），refresh cookie 失效时间为 30 天；可通过 `ACCESS_TOKEN_EXPIRES_MINUTES` 和 `REFRESH_TOKEN_EXPIRES_DAYS` 调整。
- **版本号启用**：应用版本升级至 `v0.0.217`，同步更新 `package.json`、`package-lock.json`、README、使用帮助、项目汇报和更新日志；重新部署/打包后生效。

## [v0.0.216] - 2026-07-10

### 思考模型本机目录工具结果采信修复

本版本针对生产环境验证发现的差异进行修复：非思考模型能够正常使用本机报表目录工具结果，但思考模型在工具箱已完成 `mcp.0.reports.list_files` 调用后，仍可能被长期记忆或模型常识带偏，回答“无法访问本机文件系统”。

- **工具结果升级为本轮事实依据**：成功的 MCP 工具结果新增 `PIVOT_MCP_TOOL_RESULT_BEGIN/END` 标记，并明确声明结果来自本轮刚刚完成的授权工具调用，优先级高于长期记忆、历史对话和模型常识。
- **思考模型上下文注入增强**：普通对话启用工具箱后，不再只把工具结果追加为后置 system 消息；改为在最终请求末尾追加“assistant 工具结果 + user 基于结果回答”的内部轮次，让对 system 位置敏感的推理模型也能稳定采信工具结果。
- **本机目录误答约束**：当工具结果来自本机报表目录或 `mcp.0.*` 本机虚拟工具时，最终回答指令会明确禁止再声称无法访问本机文件系统，也不会要求用户自行查看目录；若结果不足则只说明缺少哪些信息。
- **上下文预算保护**：上下文裁剪逻辑识别新的 MCP 工具结果标记，避免长对话中把本轮工具结果当普通历史消息裁掉。
- **回归测试补齐**：新增思考模型最终上下文形态断言，并扩展本机目录确定性路由测试，确认本机报表目录工具结果会带新标记进入最终回答上下文。
- **版本号启用**：应用版本升级至 `v0.0.216`，同步更新 `package.json`、`package-lock.json`、README、使用帮助、项目汇报、工具箱设计方案和更新日志；重新部署/打包后生效。

## [v0.0.215] - 2026-07-10

### API 接入页外部接口提示修正

本版本针对 API 接入管理页的外部调用说明做文案与版本归档：把原先只展示聊天/向量的接入提示补齐为聊天对话、代码补全和向量生成三类接口，并避免把聊天对话误称为“聊天补全”。

- **接入指南端点补齐**：API Base URL 下方明确展示 `POST /v1/chat/completions`、`POST /v1/completions` 和 `POST /v1/embeddings`，代码补全同步标注兼容 `prompt`、`input`、`prefix`、`suffix` 请求字段。
- **可用模型提示补齐**：可用模型说明从“聊天模型/向量模型”更新为“聊天对话、代码补全、向量生成”三类调用口径，提醒聊天模型也可通过 `/v1/completions` 用于代码补全兼容。
- **用户侧命名修正**：面向用户的“聊天补全”改为“聊天对话”，保留 OpenAI-compatible 标准路径 `/v1/chat/completions`，避免把聊天对话和代码补全混淆。
- **版本号启用**：应用版本升级至 `v0.0.215`，同步更新 `package.json`、`package-lock.json`、README、项目汇报和更新日志；重启服务端后 API 接入页静态模板会读取新文案。

## [v0.0.214] - 2026-07-10

### remote 本机 MCP 执行闭环与工具箱体验归档

本版本把上一版本之后的 remote 桌面端本机资源执行、普通对话工具调度、工具箱治理体验、文档和测试补充统一归档为新的发布版本。重点是让授权后的本机 SQLite 与本机报表目录在 remote 模式下稳定作为 `mcp.0.*` 只读工具使用，同时修复普通对话、本机目录查询、工具治理展示和发布文档的一致性。

- **本机执行桥接闭环**：新增本机设备心跳、任务领取、结果回传和 `mcp.0.*` 虚拟工具列表/调用适配，桌面端授权后的 SQLite 与报表目录可在用户电脑本机只读执行，服务器只保存设备在线状态、授权摘要和结构化结果。
- **桌面端 IPC 执行入口补齐**：Electron main/preload 暴露受控 `executeLocalMcpTool`，执行前固定初始化本机授权文件、数据目录、上传目录、分析目录和日志目录，避免打包态 asar 路径和外部环境变量造成授权/日志错位。
- **普通对话工具调度增强**：聊天页默认加载本机授权/执行脚本，发送启用工具箱的消息前同步桌面端本机状态；“查询本机某目录下有哪些文件”等请求会确定性选择 `mcp.0.reports.list_files`，不会误用数据库工具或继续等待规划模型。
- **本机目录诊断更清晰**：对普通浏览器、旧桌面客户端缺少授权/执行接口、未授权报表目录、心跳失败、工具治理过滤和本机路径错误分别输出可读原因；本机执行错误保留受控 `code`、`status` 和 `detail`，便于从服务端日志定位。
- **工具箱首页与治理体验优化**：本机授权入口支持已授权状态、工具数量、工具弹窗和刷新同步；治理指标卡、工具卡片、数据来源标签、授权按钮和长文本建议进一步收口，降低拥挤和截断。
- **聊天统计文案统一**：聊天消息统计中的字数单位恢复为 `Tokens`，速度单位统一为 `t/s`，与实际 token 统计口径保持一致。
- **安全与回归覆盖**：补充本机只读执行器、remote 桥接任务、本机目录确定性路由、桥接诊断、虚拟 MCP 日志、工具治理权限和本机路径收口测试；当前 `tests/security-mcp` 覆盖已扩展到本机桥接链路。
- **文档与版本号启用**：应用版本升级至 `v0.0.214`，同步更新 `package.json`、`package-lock.json`、README、使用帮助、项目汇报、工具箱设计方案和更新日志；重新打包/部署后刷新桌面端与聊天页静态资源。

## [v0.0.213] - 2026-07-09

### remote 桌面端本机执行环境初始化修复

- **remote 本机执行器补齐运行目录**：remote 模式桌面端在执行 `mcp.0.*` 本机工具前会初始化 `DATA_DIR`、`PIVOT_UPLOAD_DIR`、`PIVOT_ANALYSIS_DIR` 和 `LOG_DIR` 到桌面端用户数据目录，避免服务模块在 asar 内默认创建 `resources/app.asar/logs` 导致 `ENOTDIR`。
- **授权文件路径固定到桌面端用户数据目录**：本机授权读取不再沿用外部残留环境变量，确保工具箱显示的授权状态与实际执行器读取的是同一份 `local-authorizations.json`。
- **本机执行错误保留底层细节**：桌面端 IPC、前端任务回传和服务端桥接层会透传受控 `detail` 字段，方便从服务端日志定位打包态、本机路径和权限类问题。
- **验证结果**：已验证当前授权的 `D:\法律` 目录可列出 3 个 Markdown 文件，并通过 `node tests\security-mcp.test.js`（31/31）。
- **版本号启用**：应用版本升级至 `v0.0.213`，重新打包并安装桌面端后生效。

## [v0.0.212] - 2026-07-09

### 打包桌面端本机报表目录执行修复

- **报表目录清单避开 Excel 依赖初始化**：`reports.list_files` 不再在模块加载阶段引入 Excel 解析库，只有读取或查询 Excel 文件时才按需加载，避免打包进 asar 后普通列目录触发无关 `ENOTDIR`。
- **本机桥接错误二次归一化**：服务端接收桌面端任务结果时会根据 `ENOTDIR`、路径不存在和权限不足等错误码/文案补齐 400/404/403 状态，避免本机路径错误显示为 HTTP 500。
- **回归覆盖**：新增桥接任务错误归一化测试；已验证 `D:\\法律` 授权目录源码态可列出 3 个 Markdown 文件。
- **版本号启用**：应用版本升级至 `v0.0.212`，重新打包桌面端和重启服务端后生效。

## [v0.0.211] - 2026-07-09

### remote 桌面端本机目录工具执行闭环修复

- **授权根目录名自动识别**：当用户询问“本机法律目录下有哪些文件”且授权目录本身名为“法律”时，聊天页会调用 `mcp.0.reports.list_files` 的根目录清单模式，不再把“法律”误当作文件名过滤条件。
- **桌面端执行错误结构化回传**：`executeLocalMcpTool` 通过结构化 IPC 返回状态码与错误码，服务端可区分 `ENOTDIR`、`ENOENT`、权限不足等本机路径问题，不再统一显示 HTTP 500。
- **本机虚拟 MCP 调用日志修复**：`mcp.0.*` 虚拟工具调用日志不再写入不存在的真实 `server_id=0`，避免 SQLite 外键失败和写入队列持续重试。
- **报表工具执行轻量化**：本机报表目录工具不再提前加载数据库 MCP 原生依赖，减少桌面 asar 环境下无关依赖导致的执行异常。
- **前端可见文案中文化**：聊天消息统计与本机执行器诊断不再显示 `Tokens`、`t/s` 和内部执行接口名，统一使用中文说明。
- **验证结果**：已验证 `D:\\法律` 授权目录可列出 3 个 Markdown 文件，并通过 `node tests\security-mcp.test.js` 与 `npm run check`。
- **版本号启用**：应用版本升级至 `v0.0.211`，用于刷新桌面端本机执行器、聊天页工具调度和 MCP 日志链路相关脚本缓存。

## [v0.0.210] - 2026-07-09

### remote 聊天页本机目录工具执行链路修复

- **本机执行轮询去除前端用户变量依赖**：桌面端本机执行器长轮询不再依赖聊天页 `currentUser` 变量先完成初始化，只要桌面桥和登录态可用就会持续心跳并领取任务。
- **目录清单请求不再回退规划模型**：类似“查询本机法律目录下有哪些文件”的请求确定性选择 `mcp.0.reports.list_files` 后，如果执行失败会按工具执行失败返回，不再继续等待工具规划模型导致误报“工具规划失败”。
- **回归覆盖**：新增本机目录工具失败路径测试，确认执行器未领取或不可用时不会再请求规划模型。
- **版本号启用**：应用版本升级至 `v0.0.210`，刷新聊天页本机执行器脚本和普通对话工具调度脚本缓存。

## [v0.0.209] - 2026-07-09

### 聊天页桌面端本机执行器诊断增强

- **聊天页主动心跳**：普通对话页会在加载后主动尝试同步桌面端本机执行器，不再只依赖工具箱工作区打开后的心跳。
- **发送前诊断随请求上报**：启用工具箱发送消息时，前端会把是否检测到 `window.pivotDesktop`、是否具备本机授权状态接口、是否具备 `executeLocalMcpTool` 执行接口和授权摘要一并传给服务端。
- **缺失原因精确化**：如果仍无法调用 `mcp.0.reports.*`，服务端会区分普通浏览器打开、旧桌面客户端缺少本机执行接口、未授权报表目录和心跳注册失败，避免只提示“没有收到心跳”。
- **版本号启用**：应用版本升级至 `v0.0.209`，刷新聊天页静态资源版本参数，便于桌面端拉取最新前端脚本。

## [v0.0.208] - 2026-07-09

### remote 桌面端聊天页本机工具调用修复

本版本针对 remote 模式桌面客户端已授权并能在工具箱中看到 `mcp.0.db.*` / `mcp.0.reports.*`，但普通对话仍提示缺少本机执行器或无法调用本机目录工具的问题，补齐聊天页到桌面端本机执行器的加载、心跳和确定性调用链路。

- **聊天页默认加载本机执行器桥接脚本**：`/chat` 页面现在随默认脚本加载 `mcp-workbench-local-auth.js`，不再依赖用户先打开工具箱工作区才启动桌面端本机执行器心跳。
- **发送前即时同步本机执行器快照**：普通对话启用工具箱后，发送消息前会主动同步桌面端本机授权状态，并把授权摘要随聊天请求注册到服务端，减少“工具箱能看到、聊天页用不到”的状态差。
- **本机目录请求确定性走报表工具**：类似“查询本机法律目录下有哪些文件”的请求会优先路由到 `mcp.0.reports.list_files`，避免规划器误判为普通问答或误用数据库 SQL 工具。
- **诊断提示细化**：当仍无法调用时，聊天页会区分“未收到桌面端心跳”“未收到报表目录授权”“报表工具被治理过滤”等原因，方便定位是客户端、授权还是工具治理问题。
- **版本号启用**：应用版本升级至 `v0.0.208`，同步更新 `package.json`、`package-lock.json`、README、项目汇报、工具箱设计方案和更新日志；聊天页静态资源版本参数随之刷新，降低旧 JS 缓存影响。

## [v0.0.207] - 2026-07-08

### remote 模式本机资源反向执行通道启用

本版本在保持生产客户端 `mode: "remote"` 和服务器统一业务能力的前提下，补齐桌面端本机资源反向执行通道：文字识别、PDF 工具、法规查询、全局大模型、系统工具和统一权限治理继续由远端 Pivot 服务器提供；用户显式授权后的本机 SQLite 与本机报表目录则由桌面客户端在用户电脑上只读执行，并将结构化结果回传给服务器。

#### 本机 MCP 桥接能力

- **remote 模式不再要求改成 local**：生产客户端仍连接统一服务器地址，继续复用服务器侧模型、OCR/PDF、法规查询、文档处理、工具策略、审计和自动更新能力。
- **桌面端反向执行器**：桌面端在授权中心完成本机 SQLite 或报表目录授权后，会向服务器注册本机执行器心跳；服务器只保存设备在线状态和授权摘要，不主动读取用户电脑路径。
- **虚拟 MCP 工具暴露**：本机授权在线后，工具箱会动态出现 `mcp.0.db.*` 和 `mcp.0.reports.*`；其中 SQLite 只允许只读 SQL，本机报表目录只允许在授权根目录内列出文件、读取摘要和查询 CSV/Excel 表格。
- **任务下发与结果回传**：服务器调用 `mcp.0.*` 时只创建待执行任务，桌面端通过长轮询领取任务，在本机执行后回传结果；若桌面端离线、授权撤销或回传失败，会按受控错误返回。
- **本机路径安全收口**：完整本机路径继续只保存在桌面端本机授权文件中，服务器和网页端只接触资源名称、设备名、授权时间、路径摘要和执行结果。

#### 工程与验证

- **服务端桥接层**：新增本机设备注册、状态查询、任务拉取、结果回传和 `mcp.0.*` 工具列表/调用适配，并避免仅枚举本机工具时触发不必要的服务端数据库初始化。
- **桌面端 IPC 执行入口**：桌面端 main/preload 新增受控本机工具执行方法，只允许调用本机授权范围内的 `db.*` 和 `reports.*` 工具。
- **前端授权联动**：授权中心刷新、授权成功和撤销授权后会立即同步本机执行器状态，减少等待下一次心跳导致的工具列表延迟。
- **回归验证**：新增 remote 桥接安全用例，已通过 `node tests\security-mcp.test.js`（26/26）和 `npm run check`。
- **版本号启用**：应用版本升级至 `v0.0.207`，同步更新 `package.json`、`package-lock.json`、README、使用帮助、项目汇报、工具箱设计方案和更新日志。

## [v0.0.206] - 2026-07-08

### 工具箱页面前端样式及布局深度优化

本版本针对用户反馈，完成了工具箱（MCP 工作台）的前端视觉、操作流及排版细节的深度治理：

- **我的电脑合并单入口**：将原先并排展示且指向同一个本机授权中心弹窗的“连接本机数据库”和“授权指定目录”合并为“管理本机资源授权”单一入口，并在 `mcp-system-cards.css` 中将顶层栅格平分为完美对称的 `1fr:1fr` 栅格，消除了原先左侧极长、右侧极挤的突兀感。
- **支持长设备名悬浮展示**：将“数据管理”与“我的电脑”左边头部信息区宽度由 `84px` / `112px` 增加至 `120px`，并在说明节点添加 `title` 属性，当设备名称由于超长被截断时，鼠标悬停即可查看完整的本地设备名称与授权描述。
- **指标卡片视觉降噪**：重构了治理工具指标卡片（可用服务、需要处理、待刷新、数据连接、调用错误率），移除刺眼的彩色高亮底色和脉冲红点，统一为低调大方的莫兰迪灰白背景（`#f8fafc`）与中灰框线（`rgba(148, 163, 184, 0.18)`），突出指标数据可读性。
- **刷新按钮位置优化**：刷新按钮从顶栏右侧移至 governance-metrics 卡片的最左侧，并将其定位外边距从 margin-left 调整为 margin-right，使布局在从左往右阅读时衔接更自然、间距更均匀。
- **工具卡片高度压缩**：压缩系统和实例卡片的高度（由 `100px` 压缩为 `70px`） and 内边距，减少标签（margin 压缩至 `2px 0`，padding 压缩至 `1px 5px`）、标题和段落文本的行间距，确保在屏幕分辨率受限或浏览器开启书签栏时能多留出超过 100px 的垂直空间，防止底部的“其它用户个人工具”入口被截断。
- **治理建议长文本修复**：为建议日志列表覆盖 `white-space: normal` 样式，保证长句子建议在小窗口下能够自动换行显示，不再因越界而被截断。
- **授权按钮配色优化**：重新设计了“我的电脑”右侧的授权/管理授权按钮样式，使用 `em` 承载更精细的双态视觉设计。激活态使用墨绿渐变底色，已授权态则为中性浅灰底，点击与悬浮过渡层次分明。

## [v0.0.205] - 2026-07-07

### 本轮修改优化归档与版本号启用

本版本把当前工作区自 `v0.0.197` 至 `v0.0.204` 的功能、体验、部署和安全边界变更统一归档为新的发布口径，便于后续以 `v0.0.205` 作为当前版本进行打包、部署、验收和问题追踪。下方各小版本继续保留完整明细，本节用于汇总“当前所有修改和优化”的发布说明。

#### 重点变更归档

- **工具箱与数据分析收口**：工具箱首页完成数据来源重组、服务器可访问数据库/报表目录命名落地、SQLite 数据集导入、已导入数据集摘要入口、轻量统计接口、数据管理单入口、admin 多用户工具折叠摘要和宽扁卡片可读性修复。
- **文档处理、OCR 与 PDF 工具**：OCR 能力彻底外置为独立 HTTP 服务，Pivot 主项目只保留 HTTP OCR 客户端和对接协议；文字识别与 PDF 工具补齐任务删除、受控产物清理、多输出 ZIP 下载、统一表格/弹窗化结果查看和外部 OCR 服务配置口径。
- **本机能力与 MCP 治理**：桌面端/本地助手授权中心启用，工具箱“我的电脑”入口可查看、选择和撤销本机 SQLite 文件或报表目录授权；授权后的本机 SQLite 和报表目录会作为虚拟 MCP 工具动态暴露，只允许只读查询、目录扫描、文件摘要和结构化结果回传。
- **桌面客户端更新链路**：Windows 桌面端自动更新接入服务器 `/downloads/` 发布目录，打包产物可自动归档；生产 HTTPS 更新源继续保持默认安全边界，同时支持无互联网局域网在显式开关和来源白名单下使用 HTTP 更新。
- **部署与文档体系**：新增 OCR 服务对接协议、独立 OCR 项目搭建指南，删除 Pivot 仓库内 PaddleOCR 外部挂载旧文档口径；同步更新远程部署、Docker 客户端下载、生产离线部署、downloads 发布目录、使用帮助、项目汇报和开发规范。

#### 版本与文档同步

- **版本号启用**：应用版本升级至 `v0.0.205`，以 `CHANGELOG.md` 顶部版本作为运行时和发布文档的当前版本锚点。
- **文档同步**：README、使用帮助、项目汇报和更新日志新增 `v0.0.205` 汇总说明，并继续保留下方 `v0.0.197` 至 `v0.0.204` 的逐版本详情。
- **包版本同步**：`package.json` 与 `package-lock.json` 已通过版本同步脚本更新为 `0.0.205`，确保桌面打包、服务端版本读取和文档展示一致。
- **验证口径**：本轮补齐本机只读执行器后已通过 `node tests\\security-mcp.test.js`；文档与版本同步后仍以 `git diff --check`、`npm run check:text` 和 `npm run check` 作为发布前总检查口径。

## [v0.0.204] - 2026-07-07

### 离线局域网 HTTP 客户端自动更新放行

本版本针对生产环境无互联网、无 HTTPS 证书、仅内网 HTTP 的部署约束，补齐桌面客户端自动更新的受控 HTTP 模式：管理员可以在 `config.json` 中显式设置 `autoUpdate.allowInsecureHttp=true`，并通过 `autoUpdate.allowedOrigins` 绑定固定内网服务地址，让客户端从局域网 `http://.../downloads/` 读取 `latest.yml` 并下载安装新版本。

#### 局域网 HTTP 更新策略

- **显式开关**：新增 `autoUpdate.allowInsecureHttp` 配置，默认 `false`；只有显式开启时才允许非 HTTPS 更新源。
- **来源白名单必填**：非 loopback 的 HTTP 更新源必须配置 `autoUpdate.allowedOrigins`，并且更新 URL 的 origin 必须命中白名单，避免客户端被改到任意 HTTP 地址。
- **同站推导继续可用**：当 `autoUpdate.url` 为空时，仍基于 `remoteUrl + autoUpdate.path` 推导更新源；内网 HTTP 场景会得到类似 `http://pivot.example.local:3000/downloads/` 的 feed。
- **运行时二次校验**：`desktop/updater.js` 在调用 `electron-updater` 前继续复核 `allowInsecureHttp`、`allowedOrigins` 和最终 feed URL。

#### 配置与文档

- **内网模板更新**：`config.example.json` 改为 `Production LAN` 示例，展示 `remoteUrl=http://pivot.example.local:3000/`、`allowInsecureHttp=true` 和 `allowedOrigins` 的组合。
- **部署说明同步**：更新远程模式部署说明、downloads 目录说明、Docker 客户端下载说明、README、使用帮助和项目汇报，明确离线局域网 HTTP 可以启用自动更新，但应固定 IP/域名并限制来源。
- **版本号启用**：应用版本升级至 `v0.0.204`，同步更新 `package.json` 与 `package-lock.json`。
- **回归验证与打包**：新增局域网 HTTP 自动更新策略测试，覆盖允许白名单 HTTP、拒绝无白名单 HTTP、拒绝白名单不匹配 HTTP，并验证 `normalizeConfig` 可推导内网 HTTP `/downloads/` feed；已通过 `node tests\\security-desktop-update.test.js`、`npm run check` 和 `npm run dist:win`，最终安装包、`.blockmap` 与 `latest.yml` 已写入 `downloads/`。

## [v0.0.203] - 2026-07-07

### 桌面客户端自动更新接入 downloads 发布目录

本版本把 Windows 桌面客户端的自动更新链路接到服务器同站 `/downloads/` 目录：生产环境只需要在 HTTPS 站点开启 `autoUpdate.enabled`，打包产物放入或挂载到 `downloads/`，已安装客户端即可通过 `latest.yml` 检测新版本、下载并提示重启安装；手工下载入口继续使用 `downloads/Pivot-Setup.exe`。

#### 桌面端自动更新

- **同站更新源推导**：`desktop/config.js` 新增 `autoUpdate.path`，默认 `/downloads/`；当 `autoUpdate.enabled=true` 且未显式填写 `autoUpdate.url` 时，会基于 `remoteUrl` 自动推导更新源。
- **更新源安全边界**：继续要求生产更新源使用 HTTPS；HTTP 仅允许在显式环境变量放开的 loopback 开发场景中使用，避免普通内网明文更新源被误启用。
- **中文更新提示**：`desktop/updater.js` 的打包环境限制、下载完成、重启安装等提示改为中文，并在状态中暴露当前更新路径，便于排查配置。

#### 打包与发布

- **打包产物自动归档**：`npm run dist:win` 成功生成 NSIS 安装包后，会自动把 `Pivot Setup <version>.exe`、对应 `.blockmap` 和 `latest.yml` 复制到 `downloads/`，并同步生成 `downloads/Pivot-Setup.exe` 作为网页下载入口。
- **调试构建不发布**：`npm run pack:win` / `--dir` 只生成未打包目录，不写入自动更新目录，避免把调试产物误当正式客户端发布。
- **下载目录忽略规则**：`.gitignore` 补充忽略 `downloads/*.blockmap` 和 `downloads/latest.yml`，安装包与更新元数据由部署目录或 Docker volume 管理，不提交到仓库。

#### 文档与验证

- **部署文档同步**：更新 `downloads/README.md`、远程模式部署说明、README、使用帮助和项目汇报，明确自动更新所需文件、配置方式和 HTTPS 要求。
- **版本号启用**：应用版本升级至 `v0.0.203`，同步更新 `package.json` 与 `package-lock.json`。
- **回归验证通过**：已通过 `node -c desktop\config.js`、`node -c desktop\updater.js`、`node -c scripts\package_desktop.js`、`node tests\security-desktop-update.test.js`、`npm run check` 和 `npm run dist:win`；最终安装包、`.blockmap` 与 `latest.yml` 已写入 `downloads/`。`node --test tests\security-desktop-update.test.js` 在当前沙箱触发 Node 子进程 `spawn EPERM`，改用直接执行测试文件完成同等断言验证。

## [v0.0.202] - 2026-07-07

### 桌面端/本地助手授权中心与工具箱本机入口启用

本版本把工具箱中的“连接本机数据库”和“授权本机报表目录”从禁用预留入口推进为可点击的本机授权中心：桌面客户端提供只读授权状态桥，用户可以在授权中心选择本机 SQLite 文件或本机报表目录、查看当前设备授权状态并撤销授权；Web 页面仍不会直接读取用户电脑的 localhost、本机数据库或本机文件夹。

#### 工具箱本机授权中心

- **本机入口可点击**：工具箱“我的电脑”动作区的连接本机数据库、授权本机报表目录改为进入“本机授权中心”，并按桌面端/本地助手状态显示待授权、已授权或需客户端。
- **授权中心弹窗**：新增本机授权中心弹窗，展示当前设备、本机桥状态、授权类型、安全边界和授权摘要，支持数据库与报表目录两类授权切换。
- **首页状态联动**：授权或撤销后自动刷新工具箱首页，入口文案展示当前设备和授权摘要，不再只停留在静态“等待桌面端”说明。

#### 桌面端安全桥

- **桌面端授权桥**：Electron 主进程新增本机授权状态、授权选择和撤销 IPC；preload 只暴露受控方法给页面，不开放 Node.js 文件系统能力。
- **本机路径不上传服务器**：授权明细保存在桌面客户端本机 `local-authorizations.json`，渲染层只拿到资源名称、设备名、授权时间和路径摘要，不返回完整本机绝对路径。
- **安全边界保持只读预备态**：本版本只完成授权中心和状态记录，不执行本机数据库查询或目录扫描；后续查询/扫描仍需通过桌面端或本地助手只读执行器接入。

#### 文档与验证

- **帮助与项目文档同步**：更新 README、使用帮助、项目汇报、设计方案和 CHANGELOG，明确本机授权中心已经可进入，但本机执行器仍是后续阶段能力。
- **版本号启用**：应用版本升级至 `v0.0.202`，同步更新 `package.json` 与 `package-lock.json`。
- **回归验证通过**：已通过 `node -c desktop\main.js`、`node -c desktop\preload.js`、`node -c client\chat\mcp-workbench-local-auth.js`、`node -c client\chat\mcp-workbench-common.js`、`node -c client\chat\mcp-workbench-main.js` 和 `npm run check`。

## [v0.0.201] - 2026-07-07

### 工具箱数据管理入口统一、布局紧凑化与卡片可读性修复

本版本继续收口工具箱首页信息架构：导入数据文件、导入报表文件和已导入数据集摘要不再拆成三个同跳按钮，而是统一为一个“数据管理”主入口，并直接跳转数据分析应用的数据总览；本机授权能力独立为“我的电脑”动作区，长期在线服务继续使用工具卡片，admin 看到的其它用户个人工具默认折叠为治理摘要，避免多用户工具把首页撑爆。

#### 工具箱布局与样式

- **数据管理单入口**：导入我的数据文件、导入我的报表文件和已导入数据集摘要合并为一个“打开数据总览”主入口，避免三个按钮指向同一页面。
- **本机能力边界独立呈现**：连接本机数据库、授权本机报表目录改为“我的电脑”授权动作区，继续保持禁用态并说明需要桌面端或本地助手授权。
- **连接器卡片只保留长期服务**：数据来源卡片网格只展示服务器可访问数据库、服务器可访问报表目录和已配置数据库实例等长期入口，避免把数据集管理误当作可配置工具服务。
- **宽扁卡片与可读性修复**：卡片网格保持 360px 起步的宽扁规格，数据管理说明移入主入口并完整换行，普通卡片的元信息和标签允许两行或换行展示。
- **admin 多用户工具收口**：admin 首页只铺开全局和本人连接，其它用户个人工具折叠为数量、用户数和类型分布摘要，可按需展开紧凑明细。

#### 文档与验证

- **帮助与项目文档同步**：更新 README、使用帮助、项目汇报和 CHANGELOG，明确数据管理是单一主入口，长期服务才按工具卡片呈现。
- **版本号启用**：应用版本升级至 `v0.0.201`，同步更新 `package.json` 与 `package-lock.json`。
- **回归验证通过**：已通过 `node -c client\chat\mcp-workbench-common.js`、`node -c client\chat\mcp-workbench-main.js`、`npm run check` 和 `git diff --check`。

## [v0.0.200] - 2026-07-07

### 文字识别/PDF 工具任务删除与 PDF 多输出打包下载补齐

本版本补齐应用中心中“文字识别”和“PDF 工具”的数据列表删除能力，并优化 PDF 拆分、PDF 转图片等多输出任务的下载体验：用户可以直接删除不再需要的识别/处理记录，也可以一键打包下载 PDF 工具的全部输出文件。

#### 任务删除能力

- **列表删除按钮**：文字识别和 PDF 工具的数据列表操作区新增“删除”按钮，窄屏下任务行操作允许换行，避免按钮挤出表格。
- **详情删除入口**：文字识别结果详情和 PDF 工具结果详情同步新增删除按钮，减少必须回到列表才能处理记录的往返。
- **PDF 多输出打包下载**：PDF 工具新增任务全部输出下载接口，拆分 PDF、PDF 转图片等多产物任务会自动下载 ZIP 包；详情中多个输出时显示“全部下载”。
- **后端删除接口**：新增 OCR 与 PDF 工具任务删除接口，统一复用文档处理底座的任务删除服务，并按来源模块校验任务归属。
- **受控文件清理**：删除任务时会移出待处理队列、清理输出文件、页面预览、OCR 文本块、复核记录，并将对应上传源文件标记删除。已完成、失败、待复核、取消和排队任务都可以删除。

#### 文档与验证

- **版本号启用**：应用版本升级至 `v0.0.200`，同步更新 `package.json`、`package-lock.json`、README、使用帮助、项目汇报和 CHANGELOG。
- **回归覆盖**：新增文档处理任务删除测试和 PDF 拆分多输出 ZIP 下载测试，覆盖任务记录、输出记录、源文件状态、磁盘受控文件清理和打包下载；本轮已通过相关 JS 语法检查，完整项目检查见本次提交验证记录。

## [v0.0.199] - 2026-07-07

### 工具箱数据集展示收口、轻量摘要接口与文档口径修正

本版本针对工具箱首页的数据来源区做体验收口：工具箱不再承担用户文件管理器职责，已导入数据集只展示摘要入口；完整数据集列表、搜索、删除、导出和分析继续放在数据分析工作台，避免用户文件很多时首页膨胀。

#### 工具箱数据来源收口

- **已导入数据集摘要卡**：工具箱“数据来源”区固定展示“已导入数据集”摘要卡，显示数据集数量和总行数，不再逐个铺开用户文件。
- **数据分析工作台承接列表管理**：查看列表、搜索、删除、导出、字段画像、查询、透视、图表和智能分析都通过数据分析工作台完成。
- **轻量摘要接口**：新增 `/apps/data-analysis/datasets/summary`，仅返回当前用户数据集数量和总行数，避免工具箱首页拉取完整数据集、字段画像和预览行。
- **大数据量稳定布局**：无论用户导入多少文件，工具箱首页都保持固定入口数量，继续表达数据来源和执行边界。

#### 文档与验证

- **帮助与项目文档同步**：更新 README、使用帮助、项目汇报和 CHANGELOG，明确工具箱不逐个展示用户文件。
- **版本号启用**：应用版本升级至 `v0.0.199`，同步更新 `package.json` 与 `package-lock.json`。
- **回归验证通过**：已通过 `node -c client\chat\mcp-workbench-main.js`、`node -c server\services\data-analysis\datasets.js`、`node -c server\routes\apps\index.js`、`node tests\data-analysis.test.js` 和 `npm run check`。

## [v0.0.198] - 2026-07-07

### OCR 项目彻底外置、HTTP 对接协议固化与独立 OCR 项目文档补齐

本版本把 OCR 从 Pivot 主项目中彻底解耦：Pivot 不再保存 OCR 服务实现、OCR Dockerfile、OCR compose 或本地 PaddleOCR/Tesseract 命令适配器，只保留 HTTP OCR 客户端和统一协议。后续 OCR 将作为独立项目单独构建、部署和升级，Pivot 通过 `OCR_SERVICE_URL` 调用外部 OCR 服务。

#### OCR 解耦与对接边界

- **主项目只保留 HTTP OCR 适配器**：文档处理 OCR 底座只注册 `http` 引擎，旧的 `paddle`、`tesseract` 等配置会自动归一到 `http`，避免升级后继续寻找本地 OCR 命令。
- **仓库内 OCR 实现移除**：删除 `docker/ocr` 服务目录，删除 PaddleOCR、Tesseract、视觉占位等本地命令适配器，Pivot 仓库不再维护 OCR 镜像、模型、字体和缓存目录。
- **前端引擎选择收口**：文字识别页面的识别引擎只展示“外部 OCR 服务”，用户侧不再暴露本地 OCR 命令模式。
- **错误提示统一**：外部 OCR 服务不可达、超时或返回错误时，统一提示检查独立 OCR 服务部署和 `OCR_SERVICE_URL`。

#### Docker 与生产配置

- **主 compose 只编排 Pivot**：根目录 `docker-compose.yml` 只包含主应用 `pivot`，不再包含 OCR 服务、OCR profile 或 OCR 镜像构建入口。
- **主镜像继续保持轻量边界**：主 Dockerfile 不包含 PaddleOCR/PaddlePaddle/PaddleOCR-VL 安装入口，`.dockerignore` 不再承担仓库内 OCR 目录排除职责。
- **环境变量收敛**：`.env.example` 和本地 `.env` 只保留 Pivot 侧 OCR HTTP 客户端配置，包括 `DOCUMENT_PROCESSING_OCR_ENGINE=http`、默认 `OCR_SERVICE_URL=http://ocr-service:9100`、超时和请求大小限制。`OCR_SERVICE_URL` 同时支持管理员在文字识别页面前端保存更新，避免后续切换 OCR 服务时改代码或重构镜像。
- **离线部署说明更新**：生产离线部署文档改为“Pivot 主镜像 + 外部 OCR 服务”模式，OCR 镜像、模型和升级流程归独立 OCR 项目维护。

#### 独立 OCR 项目文档

- **HTTP 对接协议固化**：新增 `docs/OCR服务对接协议.md`，明确外部 OCR 服务需要提供 `GET /health` 和 `POST /ocr`，并定义请求体、响应体、错误响应和 Docker 网络约定。
- **独立 OCR 项目搭建指南**：新增 `docs/独立OCR项目搭建指南.md`，说明独立项目目录、Docker compose、镜像构建、服务实现职责和验收清单。
- **PaddleOCR-VL 选型说明**：文档明确 PaddleOCR-VL 可以用于独立 OCR 项目，但不进入 Pivot 主项目；普通批量识别建议使用 PP-OCR 系列，复杂版面、表格、公式和结构化文档解析可使用 PaddleOCR-VL。
- **快速部署入口同步**：Docker 部署快速指南增加 OCR 服务对接协议和独立 OCR 项目搭建指南链接，方便运维按新边界部署。

#### 公文写作体验优化

- **审改列统计收口**：公文写作首页数据表的"审改"列暂时取消批注数量统计，仅保留待处理建议数，避免批注功能暂停期间继续在列表中展示无效统计。

#### 文档与验证

- **版本号启用**：应用版本升级至 `v0.0.198`，同步更新 `package.json`、`package-lock.json`、README、使用帮助、项目汇报和 CHANGELOG。
- **回归验证通过**：已通过 `npm run check`、`node tests\document-processing.test.js`、`docker-compose config --services` 和 `git diff --check`；Docker build 验证在本机 Docker daemon 发送上下文阶段连续超时，已清理挂起的 docker CLI 进程。

## [v0.0.197] - 2026-07-06

### 工具箱数据接入边界清晰化、SQLite 数据集导入与数据来源工作台落地

本版本按《工具库数据接入与本机能力设计方案.md》和《开发规范.md》推进工具箱数据来源改造：工具箱从服务平铺调整为“数据来源、处理与分析、展示交付、消息通知、扩展治理”的工作台心智，统一区分服务器可访问资源、上传导入数据集和后续本机授权能力；数据分析工作台新增 SQLite 上传导入，普通用户无需配置数据库也能分析本机导出的 SQLite 文件。

#### 工具箱数据来源与执行位置

- **服务器可访问数据库命名落地**：数据库入口、表单、诊断、数据分析导入和帮助文档统一使用“服务器可访问数据库”，明确由 Pivot 服务器发起连接。
- **服务器可访问报表目录命名落地**：报表目录入口和配置表单统一说明路径必须能被 Pivot 服务器进程读取，避免把浏览器所在电脑路径误当服务器路径。
- **执行位置标签**：工具箱卡片新增执行位置、归属范围、风险等级和自动化状态标签，用户可直接判断数据访问发生在服务器、外部服务还是后续本机能力。
- **数据来源区重组**：工具箱首页按数据来源组织服务器可访问数据库、服务器可访问报表目录、导入我的数据文件、导入我的报表文件、本机数据库和本机报表目录入口。
- **本机能力边界提示**：连接本机数据库和授权本机报表目录以禁用卡片呈现，明确需要桌面客户端或本地助手授权后执行，Web 页面不会直接访问用户电脑的 localhost 或本机文件夹。

#### 数据分析与导入数据集

- **SQLite 上传导入**：数据分析上传支持 `.sqlite`、`.sqlite3`、`.db` 文件，服务端使用只读 SQLite 连接读取第一个可用表或视图，并统一生成 Parquet 数据集、字段画像和预览数据。
- **Excel/CSV/SQLite 统一入口**：上传按钮、文件选择范围、错误提示和帮助文档统一为“Excel / CSV / SQLite”，导入后继续使用数据分析工作台的查询、透视、图表和智能分析能力。
- **已导入数据集摘要入口**：工具箱“数据来源”区展示已导入数据集摘要入口，显示数量并引导打开数据分析工作台；不逐个铺开用户文件，避免文件很多时首页膨胀。
- **服务器数据库导入文案修正**：数据分析中的数据库导入入口改为“从服务器可访问数据库导入”，连接失败和空连接提示都说明这是服务器侧连接。

#### 诊断、表单与帮助

- **数据库诊断失败结构化**：数据库配置诊断捕获连接失败并返回服务器网络相关的错误、代码、排障建议和脱敏诊断信息，明确 `localhost / 127.0.0.1` 指 Pivot 服务器。
- **报表目录诊断提示增强**：报表目录诊断失败时返回服务器路径和读取权限提示，帮助管理员确认目录、共享盘和服务进程权限。
- **配置表单提示增强**：数据库类型提示、SQLite 路径占位、报表目录根路径提示和表单内说明都强调服务器路径与只读账号建议。
- **应用中心模块入口规范化**：应用中心和数据分析入口补充 `Pivot.exposeModule` 模块暴露，工具箱通过模块 API 打开数据分析，避免新增直接 window 全局读取。

#### 文档与验证

- **帮助与项目文档同步**：更新 README、使用帮助、项目汇报和 CHANGELOG，补齐服务器可访问资源、导入数据文件、本机能力边界和 SQLite 导入说明。
- **版本号启用**：应用版本升级至 `v0.0.197`，同步更新 `package.json` 与 `package-lock.json`。
- **回归验证通过**：已通过 `npm run check` 和 `node tests\data-analysis.test.js`；新增 SQLite 上传导入测试覆盖真实 SQLite 文件到数据集的导入路径。

## [v0.0.196] - 2026-07-06

### 文档处理离线部署、OCR 外部服务对接与 OCR/PDF 工具界面收口

本版本围绕文字识别和 PDF 工具从功能可用继续收口到可部署、可维护、可反复使用：OCR 引擎实现从 Pivot 主项目拆出，主应用通过 HTTP 对接独立 OCR 项目；同时把文字识别和 PDF 工具的任务列表对齐法规查询的数据表格布局，处理结果改为通过查看弹窗或下载入口进入，不再直接铺在主页面上。

#### OCR 引擎与识别质量

- **OCR HTTP 对接**：默认使用 `http` 引擎，通过 `OCR_SERVICE_URL` 调用独立 OCR 项目的识别服务。
- **OCR 实现外移**：OCR 镜像、模型、缓存和具体引擎运行时不再属于 Pivot 主仓库，由独立 OCR 项目维护。
- **外部响应归一**：HTTP OCR 适配器统一接收外部服务返回的文本、文本块、置信度和坐标信息。
- **错误提示收口**：外部 OCR 服务不可达或返回错误时，统一提示检查服务部署和 `OCR_SERVICE_URL`。
- **识别结果可重新生成**：已完成的 OCR 任务允许重试，便于模型、DPI、语言或编码配置调整后重新识别。
- **本地命令引擎下线**：Pivot 不再暴露本地 OCR 命令引擎选项，避免主项目继续承担 OCR 运行时配置。

#### 文字识别与 PDF 工具界面

- **任务表格统一**：文字识别和 PDF 工具的任务列表对齐法规查询的数据表格布局，统一表头、边框、行高、状态徽章和操作列。
- **全局分页控件复用**：两个应用的任务分页改用全局统一分页控件，避免局部私有分页样式继续扩散。
- **主页面结果收口**：OCR 识别结果、页面预览、置信度明细和 PDF 输出列表不再直接堆叠在主页面，改为点击查看后在弹窗中查看。
- **下载入口补齐**：OCR 导出文件和 PDF 处理输出都在任务表格操作列提供下载入口；PDF 多输出任务会在查看弹窗中列出每个结果文件。
- **表格单元格更克制**：任务表格文件列、识别引擎列等只显示主信息，不再显示第二行文件类型、大小或语言等辅助文字，行高保持一致。
- **操作语义统一**：任务表格统一提供查看 / 下载 / 重试 / 取消等操作，减少页面顶部和详情区按钮堆叠造成的混乱。

#### Docker 离线部署与配置

- **OCR 项目彻底解耦**：主 Dockerfile 和主 compose 不包含 OCR 服务实现；Pivot 仅保留 HTTP OCR 适配器，OCR 镜像与运行时由独立项目交付。
- **Compose 构建入口保留**：`docker-compose.yml` 保留 `build` 配置，方便有网环境一键构建并拉起容器；离线生产环境要求先导入镜像，并使用 `docker-compose up -d --no-build` 只运行已导入的 `pivot` 镜像。
- **外部服务地址配置**：生产 Docker 部署只需要配置 `DOCUMENT_PROCESSING_OCR_ENGINE=http` 和 `OCR_SERVICE_URL`，具体模型挂载由 OCR 项目负责。
- **镜像瘦身策略明确**：OCR 模型和运行时不写入主业务镜像，便于独立更新 OCR 能力而不重构 Pivot 镜像。
- **环境变量对齐**：.env 与 .env.example 只保留 Pivot 侧 OCR HTTP 客户端配置。
- **离线部署文档补齐**：新增 OCR 服务对接协议和生产离线部署说明，明确 OCR 项目独立交付。
- **单一 Compose 文件维护**：离线部署继续使用 docker-compose.yml，不再额外维护 docker-compose.offline.yml。

#### 文档与验证

- **版本文档同步**：更新 CHANGELOG.md、README、使用帮助、项目汇报、package.json 和 package-lock.json，启用 v0.0.196。
- **配置一致性检查**：确认 .env 与 .env.example 的配置项结构一致，避免生产部署遗漏外部 OCR 服务地址。
- **回归验证通过**：已通过 npm run check、node --test tests/document-processing.test.js 和 git diff --check；并通过 Playwright 验证 OCR/PDF 工具主页面不再直接展示处理结果，查看弹窗与下载入口可用。

## [v0.0.195] - 2026-07-06

### 文档处理底座落地、文字识别/PDF 工具应用与全局样式统一

本版本按《文档处理底座与OCR_PDF工具应用分阶段开发方案.md》完成阶段性开发任务，并把文字识别与 PDF 工具正式接入应用中心。共享文档处理底座统一承载上传登记、任务队列、OCR 识别、PDF 处理、受控输出、清理策略和治理统计。

#### 文档处理底座与应用落地

- **共享文档处理底座**：新增 `server/services/document-processing/`，统一管理文件登记、任务状态、OCR 页面与文本块、受控输出、清理任务、导出器、PDF 操作和 OCR 引擎适配器。
- **文字识别应用**：新增应用中心“文字识别”工作台，支持图片/PDF 上传、坐标框叠加、低置信度高亮、人工复核和多格式导出。
- **PDF 工具应用**：新增应用中心“PDF 工具”工作台，支持合并、拆分、旋转、删除页面、重排页面、提取文本、PDF 转图片、图片转 PDF 和生成可搜索 PDF。
- **可搜索 PDF 流程**：PDF 工具调用 OCR 底座生成透明文本层，并复用页码、文本块和置信度信息。

#### 导出、分发与治理

- **多格式输出**：OCR 结果支持 TXT、Markdown、JSON、带坐标 HTML、DOCX 和可搜索 PDF，输出统一走受控目录与下载接口。
- **业务分发入口**：OCR 结果可分发到知识库、法规库、公文材料和聊天输入框，入库文本保留来源页码与置信度。
- **管理员治理**：新增文档处理管理员配置、任务统计、并发/页数/超时/置信度限制和输出保留天数配置。
- **审计与错误收口**：OCR/PDF 创建、复核、分发和配置更新沿用鉴权、审计和中文错误提示规范。

#### 应用中心与全局 UI 统一

- **应用中心入口修复**：文字识别与 PDF 工具已注册到应用中心官方应用列表，并支持懒加载进入对应工作台。
- **工作台布局优化**：OCR/PDF 左侧配置栏调整为紧凑 rail，文件选择框、输入框、按钮、任务表格、详情区和空状态对齐全局工作区样式。
- **OCR 视觉细节优化**：修复引擎状态列表横向滚动和长说明溢出，坐标框、低置信度文本块和选中态使用克制状态色。
- **PDF 视觉细节优化**：PDF 结果统计、输出列表和主工作区高度保持稳定，空任务状态下仍铺满可用工作区。

#### 文档、版本与验证

- **规范门禁清理**：移除 `scripts/check_development_standards.js` 中未使用的 `fileExists`，`npm run lint` 已无 warning。
- **版本文档同步**：更新 CHANGELOG.md、README、使用帮助、项目汇报、package.json 和 package-lock.json，启用 v0.0.195。
- **回归验证通过**：已通过 `npm test`（285/285）、`node tests/document-processing.test.js`（3/3）、`npm run lint`、`node scripts/check_chat_assets.js`、`node scripts/check_text_integrity.js` 和 `node scripts/check_development_standards.js`。
- **界面验证补充**：在本地 Chrome 中对 OCR/PDF 工作台做组件级渲染度量，确认文件选择框高度、OCR 引擎区横向溢出和 PDF 主面板高度均已收口。

## [v0.0.194] - 2026-07-06

### 项目级设计规范重构与工具箱数据接入方案定稿

本版本把前期关于工具箱报表文件、本机能力、数据库连接和项目设计文档定位的讨论沉淀为正式文档：`DESIGN.md` 从公文写作单模块设计升级为全项目设计规范，公文写作设计拆分为独立模块方案，工具箱数据接入与本机能力方案确认放在项目根目录，并统一采用“服务器可访问数据库”作为服务器侧数据库入口名称。

#### 设计规范重构

- **项目级 DESIGN.md**：`DESIGN.md` 调整为 Pivot 全项目产品与体验设计源头，覆盖品牌气质、产品目标、用户角色、信息架构、设计原则、视觉语言、组件语义、可访问性、响应式行为和交互状态。
- **与开发规范分工清晰**：明确 `DESIGN.md` 负责产品体验、用户心智和界面组织，`开发规范.md` 负责代码组织、全局样式复用、接口校验、测试和工程门禁，避免两份文档重复。
- **公文写作设计拆分**：将原 `DESIGN.md` 中的公文写作设计迁移到 `docs/公文写作设计方案.md`，项目级设计规范只保留模块索引和关键边界。

#### 工具箱数据接入与本机能力

- **根目录方案文档**：确认 `工具库数据接入与本机能力设计方案.md` 作为工具箱数据源、报表文件、本机能力和执行位置的完整设计入口。
- **统一用户命名**：数据库入口使用“服务器可访问数据库”，用于表达由项目部署服务器发起连接、查询和工作流执行的数据库；同时区分“服务器可访问报表目录”“导入数据文件”“本机数据库/本机报表目录”和外部工具服务。
- **执行位置边界**：明确浏览器本身不能直接访问用户本机数据库或本机文件目录；本机能力需要桌面客户端或本地助手授权后执行，服务器侧自动化优先使用服务器可访问资源。
- **工具箱整体心智**：工具箱按“数据来源 + 处理工具 + 输出动作”组织，统一考虑数据库、报表文件、图表生成、报告编排、内网通知、外部工具服务、自由任务和工作流复用。

#### 文档与版本

- **版本文档同步**：更新 `CHANGELOG.md`、README、使用帮助、项目汇报、`package.json` 和 `package-lock.json`，启用 `v0.0.194`。
- **设计索引补齐**：`DESIGN.md` 增加模块设计索引，指向公文写作、工具箱数据接入和文档处理/OCR/PDF 方案，后续新增业务应用优先补独立模块设计文档。

## [v0.0.193] - 2026-07-05

### 开发规范门禁、中文提示日志规范与文档处理方案固化

本版本把前期关于 OCR、PDF 工具、文档处理底座和开发规范的讨论沉淀为可执行文档与自动化门禁：开发规范从提醒式约束升级为工程硬约束，新增文档处理底座与 OCR/PDF 工具应用分阶段方案，并将规范检查接入 `npm run check` 与 Git pre-commit，降低后续开发偏离规范、UI 风格割裂、路由膨胀和英文提示日志混入的风险。

#### 开发规范与工程门禁

- **开发规范重构**：完善 `开发规范.md`，明确开发前流程、代码组织、接口路由、安全日志、文件拆分、大文件治理、测试验收和冲突处理优先级。
- **大文件治理收口**：不使用固定代码行数做机械限制，改为按功能边界、职责边界和可维护性拆分，避免已有大文件继续膨胀。
- **中文提示日志规范**：新增“注释、命名、提示与日志”硬规则，要求注释、用户提示、错误提示、业务日志默认使用中文；技术名词可以保留英文原名，但必须放在中文语境中。
- **自动化规范检查**：新增 `scripts/check_development_standards.js`，检查规范文档、公共 CDN、全局 UI 样式复用、内联样式、硬编码颜色、路由层重业务逻辑，以及暂存区新增英文注释、英文提示和英文日志。
- **提交门禁固化**：新增 `.githooks/pre-commit` 与 `scripts/install_git_hooks.js`，通过 `npm run hooks:install` 启用本地 Git 提交门禁；`npm run check` 已接入 `npm run check:standards`。

#### 前端 UI 统一规范

- **全局样式硬约束**：明确按钮、输入框、下拉框、字体、字号、弹窗、按钮颜色、主题、数据表格和分页控件必须使用全局统一样式。
- **新增代码增量拦截**：提交门禁会检查新增按钮、输入框、表格、内联样式和硬编码颜色，提醒开发者复用 `.btn-primary`、`.btn-secondary`、`.form-input`、`.data-table`、`.workspace-modal`、`.workspace-pagination` 等全局样式体系。
- **避免私有 UI 体系**：禁止为单个页面另起一套按钮、弹窗、分页、表格或主题色，减少后续应用中心扩展时的视觉割裂。

#### 文档处理底座与 OCR/PDF 工具方案

- **新增完整方案文档**：新增 `文档处理底座与OCR_PDF工具应用分阶段开发方案.md`，覆盖当前项目现状、产品边界、OCR 引擎策略、版式还原边界、总体架构、数据表建议、分阶段计划和验收标准。
- **明确底座优先**：方案要求先建设共享文档处理底座，再复用到底层 OCR 应用、PDF 工具应用、知识库、法规查询和聊天附件处理，避免重复实现。
- **OCR 引擎策略**：Pivot 只保留外部 OCR 服务对接，具体识别引擎由独立 OCR 项目选择。
- **版式还原边界清晰**：区分原样预览、可搜索 PDF 和可编辑文档，避免把可搜索 PDF 的视觉一致误承诺为 DOCX 像素级还原。

#### DESIGN.md 定位说明

- **设计决策文档定位**：明确 `DESIGN.md` 是产品、信息架构、交互原则和视觉组件的设计依据，不替代 `开发规范.md` 的工程约束。
- **开发使用方式**：后续涉及 UI/前端体验的开发，需要同时参考 `DESIGN.md` 与 `开发规范.md`，一个管产品体验，一个管工程纪律。

#### 文档与验证

- **版本文档同步**：更新 `CHANGELOG.md`、README、使用帮助、项目汇报和包版本，启用 `v0.0.193`。
- **规范门禁验证**：已通过 `npm run check:standards`、`node scripts/check_development_standards.js --staged`、`npm run check:text`、`npm run check` 与 `git diff --check`。

## [v0.0.192] - 2026-07-05

### 公文写作分步化、写作资源依据链路与审校体验重构

本版本围绕“公文写作页面更清爽、资源依据更可信、AI 使用材料更明确”的目标进行收口：主页面改为公文列表与创建入口，单篇公文进入后再编辑正文；AI 全文起草、审校建议和写作资源统一使用大弹窗承载；写作资源从“看得到但不确定 AI 是否使用”调整为“本篇材料自动进入 AI 上下文，知识库结果可显式加入 AI 依据”。

#### 公文列表与创建流程

- **主页面分步化**：公文写作首页改为“已创建公文列表 + 新建公文”模式，减少把起草、材料、审校和导出全部堆在首屏造成的复杂感。
- **对齐法规查询布局**：公文列表表格样式、行高、单元格背景和统计区域对齐法规查询页面，公文名称列只显示公文名称，不再混入正文摘要。
- **全局分页控件**：列表分页改用全局统一分页组件，移除统计标签里的“第 1 / 1 页”式页码展示。
- **创建弹窗收口**：点击“创建公文”后弹出表单填写公文名称、文种等基础信息；材料来源不再放在创建弹窗，改由进入单篇编辑后在写作资源中处理。

#### 单篇编辑与核心动作简化

- **正文优先的编辑页**：单篇公文编辑页清理多余头部信息和次要按钮，正文写作区域顶部合并为一行，返回列表按钮保留原位置，减少视觉层级。
- **移除复杂版本流**：暂时移除撤销、重做、修改版、送审版、定稿版等复杂入口，保留 AI 全文起草、局部润色/扩写、审校建议、写作资源和导出。
- **选区工具条简化**：选中文本后仅保留润色、扩写、压缩、按要求改写等实用动作，去掉“选区”文字标签，降低工具条干扰。
- **弹窗化 AI 能力**：AI 全文起草改为弹窗输入要求后直接写入正文；输入框聚焦时提示信息自动消失，避免像真实内容一样占位。

#### 审校建议与文字校对合并

- **合并重复功能**：将“文字校对”并入“审校建议”，形成一个综合审校入口，避免用户在“审校建议”和“文字校对”之间困惑。
- **两栏审校弹窗**：审校建议弹窗加宽并拆成“基础校对”和“AI 审校建议”两栏，基础校对展示规则检查，AI 区域展示可替换、插入或忽略的建议。
- **状态显示入弹窗**：AI 审校中的忙碌状态显示在审校弹窗内部，不再落在底层工具条上，被遮罩压在下方。
- **审校上下文补强**：后端公文写作提示词在审校、润色和全文改写时会携带本篇材料/知识库引用，用于核对事实、数据和依据是否一致。

#### 写作资源与 AI 依据链路

- **写作资源重新归类**：写作资源保留“本篇材料、知识库、公文模板”三类；规范依据不再单独作为 tab，而是通过知识库检索或审校规则体现。
- **本篇材料定义清晰**：本篇材料只随当前公文保存，用于存放事实背景、数据、原始说明和知识库引用；AI 全文起草、审校、润色和全文改写会自动参考。
- **知识库显式加入依据**：知识库检索结果的“作为依据”改为“加入AI依据”，点击后会把完整知识库片段追加到本篇材料，并提示 AI 会自动参考。
- **上传入口去重**：本篇材料只保留一个“上传材料”入口；编辑框下方重复上传按钮已移除。编辑状态下上传文件会追加到当前编辑框内容，避免覆盖未保存输入。
- **材料编辑不撑开弹窗**：点击“粘贴/编辑材料”后，写作资源弹窗外层不再出现垂直滚动条，滚动仅保留在材料输入框内部。
- **知识库文案更直观**：“公共知识”统一改为“知识库”，并移除知识库页不合理的历史公文列表。

#### 文档与验证

- **设计文档补齐**：新增并修正 `DESIGN.md`，记录公文写作当前信息架构、交互原则、资源边界和后续开放问题。
- **版本文档同步**：更新 `CHANGELOG.md`、README、使用帮助和包版本，启用 `v0.0.192`。
- **回归验证通过**：已通过 `npm run lint`、`npm run check` 与 `npm test`；新增公文写作服务测试，确保全文类 AI 模式会携带本篇材料作为事实依据。
## [v0.0.191] - 2026-07-05

### 生产依赖安全审计修复、Electron 升级暂缓与版本启用

本版本针对 GitHub Actions 中 `npm run audit:policy` 因生产依赖高危审计项失败的问题进行收口：升级锁文件中的 `form-data` 与 `multer` 修复已可用的安全版本，同时暂不将桌面运行时强制升级到 `electron@43.0.0`，避免把 devDependency 的主版本升级混入本次生产依赖安全修复。

#### 生产依赖审计

- **修复 form-data 审计项**：将锁文件中的 `form-data` 从 `4.0.5` 升级到 `4.0.6`，处理 multipart 字段名和文件名转义相关的 CRLF 注入风险。
- **修复 multer 审计项**：将锁文件中的 `multer` 从 `2.1.1` 升级到 `2.2.0`，处理深层字段名和中止上传清理相关的拒绝服务风险。
- **传递依赖同步**：随 `form-data` 修复同步提升 `hasown` 与 `mime-types` 解析范围，保持 `package-lock.json` 与 npm 安全解析结果一致。

#### 桌面运行时策略

- **暂缓 Electron 主版本升级**：保留当前桌面运行时版本，不执行 `npm audit fix --force` 强制升到 `electron@43.0.0`，避免未经桌面启动、打包和原生模块验证的破坏性升级进入本次修复。
- **区分生产审计与开发审计**：当前 CI 失败来自 `npm audit --omit=dev --audit-level=high`，本轮只处理生产依赖阻断项；Electron devDependency 风险后续按桌面运行时升级单独评估。

#### 验证

- **审计策略通过**：`npm run audit:policy` 已通过，生产依赖高危/严重阻断项清零。
- **主测试通过**：`npm test` 已通过，包含语法检查、静态资源检查、安全 HTML 扫描、窗口全局变量扫描、E2E smoke 脚手架检查、lint 与 281 个安全测试。
- **变更范围收敛**：本轮仓库变更集中在版本文档与依赖锁文件，未引入 Electron 主版本升级。

## [v0.0.190] - 2026-07-04

### 系统监控 AI 并发可观测性、GPU 临时保护说明与版本启用

本版本针对生产反馈的“全局 AI 并发保存为 2 后，系统监控刷新又显示 0/1”进行收口：区分保存配置上限与当前有效上限，保留 GPU 显存保护的临时降档行为，但在系统监控中明确展示配置值、当前值和临时保护原因，同时保存全局参数后立即清空监控摘要缓存，避免旧快照造成误判。

#### 并发运行态与 GPU 保护

- **配置上限与有效上限分离**：`ConcurrencySemaphore` 状态新增 `configuredMax` 和 `effectiveMax`，GPU 动态降档只影响当前有效并发，不再让监控看起来像全局参数被写回默认值。
- **GPU 临时降档可观测**：GPU 监控状态新增 `throttled`、`effectiveMaxConcurrent` 和 `protectionReason`，显存较高时可以说明当前处于临时保护，而不是配置丢失。
- **监控摘要缓存即时失效**：全局参数保存成功后主动调用 `invalidateMonitorSummaryCache()`，下一次系统监控摘要会重新装配实时状态，不再被 5 秒缓存中的旧并发值干扰。

#### 管理端监控展示

- **AI 并发卡片说明更清晰**：系统监控继续展示实际有效并发，如 `0/1`；若保存配置为 `2` 且 GPU 临时保护触发，提示会显示“配置 2 · GPU 临时保护”。
- **GPU 与并发保护面板补充上限对照**：GPU 面板新增“AI上限”列，以“当前 / 配置”形式展示，例如 `1/2`，并把状态从单纯“正常/保护中”扩展为“降档中”。
- **三列保护信息布局**：监控分栏样式支持三列展示，避免保护状态、AI 上限和拒绝阈值挤压。

#### 回归验证

- **并发与 GPU 回归测试**：补充 `ConcurrencySemaphore` 配置上限保持测试，以及 GPU 临时降档/恢复状态断言。
- **运行时设置保存路径断言**：确认全局参数保存后会同步运行时队列并清理监控摘要缓存。
- **验证通过**：`node tests\security-gpu-monitor.test.js`、`node tests\security-chat.test.js`、`npm run check` 和 `git diff --check` 均已通过。

## [v0.0.189] - 2026-07-04

### 智能体能力与结果滚动修复、文档同步与版本启用

本版本承接 v0.0.188 已完成的前端模块迁移、RAG 调试与观测、E2E smoke、桌面更新源校验和企业化/多节点骨架工作，补齐智能体“能力与结果”弹窗在工具较多时无法垂直滚动的问题，并同步更新版本文档与包版本。

#### 智能体能力与结果体验

- **可用工具区域恢复独立滚动**：将“可用工具”和“结果沉淀”拆成左右两个 `agent-results-panel` 面板，让标题与内容列表分别占位，避免工具列表继续撑开整个弹窗。
- **弹窗高度链路收紧**：为能力与结果弹窗的 body、details、结果 grid 和面板补齐可收缩高度约束，结果区主体使用确定视口高度，确保列表内容超出时落到 `#agent-tool-list` 内部滚动。
- **旧样式冲突收敛**：把后加载的 `agent-controls.css` 中 `overflow: visible` 兼容规则限定到配置收纳区，避免覆盖弹窗内 `overflow-y: auto` 的真实滚动行为。
- **结果沉淀区域同步约束**：右侧沉淀列表跟随同一面板模型，内容较多时也保持在弹窗内独立滚动，不再推动整体页面溢出。

#### 文档与版本

- **更新日志新增 v0.0.189**：记录本轮滚动修复的结构调整、样式冲突收敛、验证方式和版本启用说明。
- **README 与优化路线图同步**：补充最新版本摘要，并在路线图中标记智能体能力与结果弹窗滚动问题已完成闭环。
- **包版本同步启用**：通过版本同步流程将 `package.json`、`package-lock.json`、README 和使用帮助同步到 `v0.0.189`。

#### 验证

- 已通过 Playwright 布局指标验证：工具列表 `clientHeight=698`、`scrollHeight=2332`、`scrollTop=240`，确认可用工具区域是真实可垂直滚动。
- 已通过 `git diff --check`。
- 已通过 `npm run check`。

## [v0.0.188] - 2026-07-03

### 可落地优先：前端迁移、RAG 观测、E2E、桌面更新与企业化骨架

本版本继续把上一轮规划推进到可运行、可检查、可扩展的工程形态：前端继续收敛到 `Pivot.modules`，RAG 召回调试增加历史落库与观测事件，E2E 建立可直接运行的 Playwright smoke runner，桌面端更新源加入 HTTPS/来源校验，企业化与多节点方向补齐迁移表、provider 接口占位和测试。

#### 前端状态收敛

- **聊天 UI 模块迁移**：`client/chat/ui.js` 将分页、上下文压缩、模型选择器、上传运行时限制和图片预览等入口注册到 `Pivot.modules['chat.ui']`，旧 `window.*` 入口由 `Pivot.exposeModule()` 兼容发布。
- **附件模块迁移**：`client/chat/engine-attachments.js` 注册 `Pivot.modules['chat.attachments']`，集中暴露附件数量限制、会话归属校验、待发送附件读取、上传前准备、清理和移除等 API，减少散落全局赋值。
- **全局暴露门禁继续生效**：`check:window-globals` 仍阻止新增直接 `window.* =`，新 API 继续走 `Pivot.exposeModule()` / `Pivot.modules.*` 路径。

#### RAG 调试与观测

- **调试历史落库**：新增 `rag_debug_queries` 表与 `server/services/rag-debug-history.js`，记录 query、scope、topK、candidateLimit、scoreThreshold、候选数、命中数、selected chunk ids、分数摘要、队列状态和耗时。
- **调试历史接口**：新增 `GET /api/rag/debug-query/history`，前端 RAG 调试弹窗展示“最近调试”，可点击历史问题回填输入框。
- **观测事件补强**：`POST /api/rag/debug-query` 在成功后写入历史，并记录慢检索；当索引队列出现积压且并发已满时写入 RAG warning observability event。

#### E2E 与质量门禁

- **E2E runner**：新增 `@playwright/test`、`npm run test:e2e`、`tests/e2e/playwright.config.js` 与 `tests/e2e/smoke.spec.js`，覆盖聊天壳加载、`Pivot.modules` 命名空间、RAG 调试弹窗与历史容器。
- **静态门禁与浏览器实跑并存**：`scripts/check_e2e_smoke.js` / `npm run check:e2e-smoke` 纳入 `npm run check`，负责校验 E2E 资产、runner 脚本与依赖；完整浏览器 smoke 可通过 `npm run test:e2e` 执行。

#### 桌面端更新安全

- **更新源策略模块**：新增 `desktop/update-policy.js`，强制远程自动更新源使用 HTTPS；仅在显式 `PIVOT_DESKTOP_ALLOW_INSECURE_UPDATE_FEED=true` 且 loopback 地址时允许 HTTP 开发源。
- **允许来源校验**：`desktop/config.js` 新增 `autoUpdate.allowedOrigins` 标准化配置，配置加载阶段拒绝不在允许列表中的更新源。
- **运行时二次断言**：`desktop/updater.js` 在 `autoUpdater.setFeedURL()` 前再次校验 feed URL，避免后续直接调用绕过配置阶段策略。

#### 企业化与多节点骨架

- **迁移与基础 schema 同步**：新增共享 schema 片段 `server/db/schema/enterprise.js`，基础 schema 与版本化迁移 `202607030001_rag_debug_enterprise_contracts` 同步创建 RAG 调试历史、组织、团队、团队成员、资源权限、策略对象和部署 provider 配置表。
- **Provider registry**：新增 `server/services/deployment-providers.js`，定义 database/objectStorage/queue/lock 四类 provider 接口占位，内置 SQLite/local_fs/in_process/in_process_or_sqlite 与 Postgres/S3-compatible/distributed queue/distributed lock 的 planned 扩展合约。
- **部署画像扩展**：`server/services/deployment-profile.js` 返回 `providers` 字段，管理端可直接看到当前 provider key、接口名、单机/多节点能力和是否仍需 adapter。
- **企业访问上下文占位**：新增 `server/services/enterprise-access.js`，提供资源/主体类型归一化、用户组织/团队上下文读取和资源权限列表读取，为后续知识库/MCP/模型/Agent 权限扩展预留稳定入口。

#### 测试

- 新增 `tests/security-desktop-update.test.js` 覆盖桌面更新源 HTTPS、loopback 例外和 allowedOrigins。
- 新增 `tests/enterprise-deployment.test.js` 覆盖迁移建表、provider registry、部署画像和企业访问 helper。
- 新增 `tests/security-rag-debug-history.test.js` 覆盖 RAG debug history 写入与读取。
## [v0.0.187] - 2026-07-03

### 架构拆分、调试可观测与扩展契约收口

本版本继续完成上一轮全面优化中尚未完全产品化的中期计划：聊天主链路拆分为可维护服务层，Agent 运行状态进入显式状态机，RAG 调试面板补齐混合排序与队列状态，前端 `window.*` 暴露增加静态基线门禁，并为组织/团队权限与多节点部署抽象建立后端契约。

#### 后端架构与运行时

- **聊天主链路服务化**：`server/routes/chat/index.js` 进一步瘦身，预检、上下文组装、模型流打开、助手消息持久化分别下沉到 `chat-preflight`、`chat-context-assembler`、`model-stream-service` 和 `chat-persistence`，保留路由专注 SSE 传输与错误边界。
- **Agent 状态机与重试审计**：新增 `server/services/agent-runtime/state-machine.js`，所有 run 状态写入统一校验合法迁移；失败重试会记录 `retryReasons` metadata，包含尝试次数、上限、错误码与原因。
- **部署画像契约**：新增 `server/services/deployment-profile.js`，明确当前 SQLite WAL 单机能力、对象存储/分布式队列/分布式锁的多节点前置条件，并在设置与监控摘要中返回部署画像。
- **权限能力矩阵**：`server/permissions.js` 增加 capability matrix、数据分级、策略对象类型和组织/团队扩展占位，登录用户与设置接口可直接返回可审计能力边界。

#### RAG 调试与观测

- **检索分数拆解**：RAG debug query 返回 dense score、fused score、dense rank、FTS rank、MMR selected、gated count、hybrid RRF/MMR 参数和 selected chunk ids。
- **索引队列状态可见**：知识库索引队列新增 `getKnowledgeIndexQueueStatus()`，RAG 调试接口返回 running/pending/active/maxConcurrent/userPending 等状态，帮助判断刚上传文档未命中的原因。
- **调试面板增强**：前端 RAG 调试结果展示 retrieval mode、Hybrid 权重、队列状态和每个 chunk 的分数分解，便于调优阈值、候选数量、RRF 与 MMR 行为。

#### 前端状态治理与门禁

- **`window.*` 暴露静态基线**：新增 `scripts/check_window_globals_usage.js` 和 `scripts/window_globals_baseline.json`，冻结当前 `client/chat` 历史全局暴露数量；新增或扩张 `window.* =` 会在 `npm run check` 中失败，后续新 API 必须走 `Pivot.exposeModule()` / `Pivot.modules.*`。
- **质量门禁串联**：`npm run check` 现在同时执行文本完整性、语法、聊天资源、安全 HTML 和 window 全局暴露扫描，避免前端迁移过程中回退到散乱全局状态。

#### 验证

- 已通过 `npm run check`、`npm run lint`、`npm test`（273/273）、`npm run audit:policy` 和 `git diff --check`。
## [v0.0.186] - 2026-07-03

### 全面安全加固、前端状态收口与渲染治理

本版本集中发布前一轮全面审计后的落地修复：后端上传、RAG/模型出网、运行时配置、Agent 队列、聊天观测、Electron 外链和前端 HTML 渲染均纳入统一安全边界；同时开始降低 `client/chat` 的全局状态复杂度，建立 `window.Pivot.modules.*` 兼容迁移路径，并通过静态检查阻断未净化的 `innerHTML` 写入。

#### 后端安全与运行时治理

- **上传链路深度加固**：统一 `server/upload.js` 的上传中间件，增加 multipart 限制、异常/中止清理、文件魔数校验、知识库上传专用包装和上传安全检查，降低伪造类型、超限上传与临时文件残留风险。
- **RAG 与知识库上传收口**：`server/rag.js` 改用知识库上传中间件和统一上传安全校验，让知识库导入与通用上传共享同一套防护策略。
- **安全 HTTP 出网统一入口**：新增 `server/services/safe-http-client.js`，模型转发、RAG、网络搜索和外部 API 调用逐步切换到安全 JSON 请求 helper，集中处理 URL 校验、超时、代理和响应限制。
- **模型转发防护增强**：`server/services/model-forwarder.js` 拒绝 multipart 类 payload，模型请求统一走安全 URL 与 agent 处理，减少 SSRF、越权转发和大体积请求风险。
- **运行时配置缓存优化**：全局运行时设置增加缓存与强制刷新路径，减少频繁读取旧库参数，同时保持保存后立即同步到运行时。
- **Agent 队列锁续期**：长任务执行期间增加锁续期与状态保护，降低队列任务被误判超时、重复执行或长时间占用的风险。
- **聊天观测追踪补齐**：聊天请求链路补充 trace/observability 字段，便于排查模型请求、流式输出和异常路径。
- **Electron 安全默认值加固**：收口外链打开策略、外部来源和窗口安全边界，降低桌面端加载非预期地址的风险。

#### 前端全局状态与 HTML 安全

- **`Pivot.modules` 命名空间上线**：`client/chat/pivot-core.js` 新增 `Pivot.modules`、`registerModule()`、`getModule()`、`exposeModule()` 与兼容 API，为逐步迁出大量 `window.*` 暴露提供统一入口。
- **首批聊天模块迁移**：`engine-sessions.js`、`sidebar.js`、`engine-streaming.js` 分别迁到 `chat.sessions`、`chat.sidebar`、`chat.streaming` 模块，同时保留旧全局别名，确保现有调用不被打断。
- **HTML 渲染统一净化**：`client/chat/safe-html.js` 强化 `PivotSafeHtml.setHtml()`，改用 DOMPurify 的上下文净化结果并通过 `replaceChildren()` 写入 DOM。
- **`innerHTML` 调用面清零**：`client/chat` 中 285 处直接 `innerHTML =` 写入已迁到 `PivotSafeHtml.setHtml(...)`，仅保留安全工具内部的受控解析点。
- **静态检查默认阻断**：`scripts/check_safe_html_usage.js` 默认禁止未经 `PivotSafeHtml` 或 DOMPurify 包装的 `.innerHTML =`、`.innerHTML +=`、`['innerHTML'] =` 和 `['innerHTML'] +=` 写入，并纳入 `npm run check`。

#### CI、测试与文档

- **审计策略脚本化**：新增并接入安全审计策略检查，CI 会覆盖上传、出网、Electron、前端安全 HTML 和全局状态迁移相关约束。
- **测试桩与断言同步**：更新前端安全 HTML、Pivot 模块注册、上传安全、模型转发、聊天链路和运行时配置相关测试，覆盖本轮迁移后的兼容行为。
- **优化路线图同步**：`docs/optimization-roadmap.md` 已记录本轮已完成项、仍保留的兼容别名和后续继续迁移 `window.*` 的方向。

#### 验证

- 已通过 `npm test`（273/273）、`npm run lint`、`npm run check`、`npm run audit:policy` 和 `node tests/security-chat.test.js`。

## [v0.0.185] - 2026-07-03

### 生产旧库全局参数读写深度加固

本版本在 v0.0.184 的全局参数修复基础上继续收口生产兼容风险：针对历史 `app_settings` 表缺少 `key` 唯一约束、存在重复 key、启动默认值写入依赖 `ON CONFLICT(key)` 的情况，统一抽出 `app_settings` 读写 helper，确保生产保存、刷新、重启和其他系统开关读取都使用最新值。

#### 生产兼容加固

- **`app_settings` 读写统一入口**：新增 `server/services/app-settings.js`，集中处理读取最新值、默认值初始化、设置写入和旧库 fallback，所有读取统一按 `updated_at` / `rowid` 取最新记录。
- **旧生产表无唯一约束兼容**：启动默认值初始化不再依赖 `ON CONFLICT(key)`；写入遇到旧表约束异常时删除同 key 旧行再插入最新值，顺带清理重复 key，避免刷新或重启后读回旧值。
- **全局设置路径统一读最新**：全局设置、RAG、API 接入开关、开放注册、记忆阈值、观测告警配置和 `stmts.getSettings` 都改为最新值读取，避免旧重复行把页面显示或运行时配置拉回旧值。
- **生产重启风险收口**：服务启动时默认设置填充通过 `ensureAppSetting` 保持“已有数据库值优先”，防止生产重启时 `.env` 默认值或旧表结构干扰已保存配置。

#### 回归测试

- 补充旧生产 `app_settings` 无唯一约束模拟测试，覆盖默认值初始化、重复 key 取最新和保存后去重。
- 已验证 `node tests\security-chat.test.js`、`node tests\security-model-runtime.test.js`、`npm run check` 与 `git diff --check` 通过。

## [v0.0.184] - 2026-07-03

### 全局参数持久化、用量统计分页与长会话定位修复

本版本针对生产环境反馈的三类问题做集中修复：全局参数保存后刷新回退、共享模型端点并发被低值模型拉回、用量统计页缺少分页控件，以及长消息会话切换后未稳定定位到底部。修复同时覆盖运行时同步、监控缓存强制刷新、统计导出语义和长会话异步渲染后的贴底行为。

#### 生产问题修复

- **全局参数保存持久化加固**：运行时参数读取改为按 `updated_at` 与 `rowid` 取最新记录，并在保存后刷新前端与运行时快照，避免“保存成功但刷新后没有了”。
- **保存后立即同步运行时并发**：`/api/admin/settings/runtime` 保存成功后会主动同步全局 AI 并发、模型端点运行时、Agent 队列、知识库索引队列和记忆压缩队列，并返回最新运行时快照。
- **模型端点共享并发稳定化**：同一模型服务地址下的多个模型会按全局端点默认并发与模型覆盖值聚合出稳定上限；请求路径也会纳入已存在的运行时模型，避免某个 `max_concurrent=1` 的模型在生产请求或监控刷新时把共享端点从 2 拉回 1。
- **监控摘要缓存可强制刷新**：系统监控摘要接口支持 `refresh=1` 绕过 5 秒缓存；全局参数保存后前端会强制刷新监控摘要，避免刚保存的并发配置被旧缓存遮住。
- **旧运行时参数入口继续收口**：全局参数保存入口只从“设置 > 全局参数”页采集当前表单输入，旧弹窗控件不会再参与提交，避免重复控件把旧值混入保存请求。

#### 管理端体验优化

- **用量统计补齐分页**：`/stats/usage` 改为返回 `{ data, total, page, limit }`，前端新增 `pagination-stats` 分页控件并把页码状态纳入 `pageState.stats`，大量用户/模型聚合记录不再一次性铺满页面。
- **用量统计导出保持全量语义**：新增 `/stats/usage/export` 汇总导出接口，前端“导出用量统计”改为下载后端全量 CSV，避免分页后误导出当前页。
- **长会话切换稳定贴底**：会话切换后贴底窗口延长，并增加双层 `requestAnimationFrame`、延迟二次贴底和短时 `ResizeObserver` 监听，处理大量消息、图表、图片或代码块后渲染导致的高度变化。
- **分页与空状态兼容旧返回**：用量统计前端兼容旧数组响应和新分页响应；当当前页为空时显示空状态并清理分页控件。

#### 回归测试

- 补充模型端点共享并发测试，覆盖“同一 endpoint 下一个模型跟随全局默认 2、另一个模型配置 1 时，端点仍保持 2”的场景。
- 补充请求路径并发回归测试，覆盖活动模型列表短暂不完整时，低并发模型请求不会把已有共享端点运行时拉低。
- 补充全局参数读取顺序和保存后同步端点运行时的源码断言。
- 补充用量统计分页控件、后端分页接口、全量导出接口、监控强刷和长会话贴底增强的前端/路由断言。
- 已验证 `node tests\security-model-runtime.test.js`、`node tests\security-chat.test.js`、`npm run check` 与 `git diff --check` 通过。

## [v0.0.183] - 2026-07-02

### 长期记忆样式统一与并发降档恢复修复

本次更新继续收口管理端体验和生产并发治理：长期记忆页面改用全局统一表格、分页与弹窗样式，来源/编辑弹窗失败时不再空白；会话记录打开后默认定位到底部；同时修复 GPU 自动降档后不会按全局参数恢复的问题，避免生产环境保存并发为 2 后又长期回退到 1。

#### 体验优化

- **长期记忆表格统一样式**：长期记忆页改用全局统一的数据表格与分页控件，行高、列宽和操作按钮尺寸与其他管理页保持一致，避免内容列和按钮把表格撑得过高。
- **长期记忆状态中文化**：列表状态列统一显示“活跃/禁用/已删除”等中文文案，避免把后端状态值 `active` 直接展示给用户。
- **来源与编辑弹窗容错**：修复长期记忆“来源”“编辑”按钮点击后空白的问题；来源加载失败会直接提示，编辑时会校验当前缓存是否仍有效，避免静默失效。
- **编辑与来源弹窗统一样式**：编辑弹窗复用全局 `model-modal`、`model-form` 和 `model-modal-actions` 结构；来源弹窗内容区复用全局表单样式，关闭按钮移至弹窗右上角，并使用设置页统一的红色文字与红色边框样式。
- **会话记录默认到底部**：查看普通会话或管理员用户会话记录时，页面会持续贴底并按阅读顺序展示当前页内容，确保打开记录后直接看到最新位置。

#### 并发治理

- **GPU 降档后自动恢复**：修复 GPU 显存保护把全局 AI 并发临时降到 1 后不会主动恢复的问题；现在每轮 GPU 状态刷新都会读取数据库中的全局 AI 并发配置，显存恢复安全后自动回到保存值（如 2），不再依赖重新保存全局参数触发恢复。
- **运行态上限不再越过全局配置**：GPU 自适应并发的上限改为“全局 AI 并发”和 `GPU_CONCURRENT_MAX` 的较小值，避免 `GPU_CONCURRENT_MAX` 把运行态拉高到超过设置页保存值；`GPU_CONCURRENT_MIN` 也会被限制在该上限内。
- **配置来源说明**：`.env` 中的 `MAX_CONCURRENT_AI_REQUESTS=1` 与 `MODEL_ENDPOINT_DEFAULT_CONCURRENCY=1` 只作为数据库没有保存值时的启动默认值；设置页保存的运行时参数写入 `app_settings`，前端不会也不应该覆盖 `.env` 文件。生产更新部署时需保持 `DATA_DIR` / Docker 数据卷不变，否则会重新读取 `.env` 默认值。
- **全局参数刷新回退深度修复**：保存全局参数时只收集当前页面或当前弹窗内的参数输入，避免页面与旧弹窗的重复控件把旧值一起提交；同一模型服务地址下多个模型共享端点时，端点并发按全局默认与模型覆盖值聚合为稳定上限，避免某个 `max_concurrent=1` 的模型在监控刷新或请求触发时把端点从 2 拉回 1。
- **全局参数唯一表单源**：移除旧的运行时参数弹窗表单和旧入口函数，保留“设置 > 全局参数”作为唯一编辑入口，避免两套输入控件再次出现保存值不一致。
- **模型端点默认并发校验**：补充模型端点运行时回归测试，确认模型自身 `max_concurrent=0` 时会跟随设置页保存的“端点默认并发”，避免端点状态从 0/2 回退到 0/1。
#### 回归测试

- 新增 GPU 监控回归测试，覆盖显存偏高时降到 1、显存恢复安全后自动回到全局配置 2 的行为。
- 补充模型端点运行时测试，覆盖模型 `max_concurrent=0` 时跟随设置页保存的端点默认并发。
- 补充同端点多模型并发聚合测试，覆盖一个模型跟随全局默认 2、另一个模型配置 1 时端点仍稳定显示 0/2。
- 补充全局参数唯一表单源前端回归断言，确认旧运行时参数弹窗不再包含 `data-runtime-key` 控件，保存入口只来自全局参数页。
- 补充全局运行参数保存测试，确认 `model_endpoint_default_concurrency` 会随设置接口写入并返回配置快照。
- 同步非流式 JSON 回放测试的 token 统计口径，保存值使用上游 `completion_tokens` 与本地完整内容估算值的较大值。
- 已验证 `npm run check` 与 `node tests/security.test.js` 通过。

## [v0.0.182] - 2026-07-02

### 聊天工具箱触发治理与 Token 速度统计修正

本次更新收紧普通聊天中的工具箱触发边界：用户没有主动开启工具箱时，发送文字、图片或文档都不会自动弹出工具箱授权确认，也不会自动调用工具箱能力。同时修正流式与最终 t/s 统计口径，按完整输出 token 与完整请求耗时计算，避免思考过程被排除后导致速度虚高。

#### 问题修复

- **取消发送时自动启用工具箱**：移除 `sendMessage()` 中根据消息内容自动调用 `shouldAutoEnableMcpForPrompt()`、`ensureChatMcpConsent()` 和 `activateChatMcpToggle()` 的分支。工具箱关闭时，输入框发送内容不再被自动升级为工具箱请求。
- **文档发送不再误触发确认**：附件抽取文本仍可进入模型上下文用于文档理解，但不再参与工具箱自动启用判断，避免上传制度、表格或报告类文档时误弹“允许调用工具箱工具”确认。
- **保留手动启用授权**：用户手动打开工具箱后，发送消息仍会按原逻辑进行一次 `ensureChatMcpConsent()` 会话授权，并随请求携带 `mcpEnabled` / `mcpConfirmed` 状态，保证显式工具箱使用仍受确认保护。
- **实时 t/s 口径修正**：前端流式统计不再使用“可见答案 token / 可见答案耗时”，改为“当前总输出 token / 本轮请求总耗时”，把可见思考内容和等待思考的耗时一并计入。
- **最终 t/s 口径同步**：后端 `buildAssistantSpeedStats()` 改为使用完整助手输出 token 与完整请求耗时；当上游返回 `completion_tokens` 时优先纳入，且会用最终内容估算补足流式图表等追加内容的 token。

#### 回归测试

- 新增 `chat send does not auto-enable MCP when toolbox is off` 回归测试，锁定“工具箱关闭时发送消息不会自动启用 MCP”的行为。
- 新增 Token 速度统计回归测试，覆盖完整输出 token / 总耗时、流式图表 token 补足和前端实时统计口径。
- 已验证 `node tests/security-chat/rendering-streams.js`、`npm run check` 与 `npm run lint` 通过；lint 仅保留既有的 `scripts/build_installer.js` 未使用变量 warning。
## [v0.0.181] - 2026-07-01

### 法规查询安全拆分与引用网络优化：前后端模块化、加载兼容修复、核心图与关系明细

本次更新继续治理法规查询：拆分前后端大文件，修复“法规查询加载失败”，优化条文引用网络的核心图和关系明细，解决节点密集、标签重叠和文字溢出。

#### 架构拆分

- **前端**：`client/chat/apps-workbench-regulations.js` 改为法规查询加载入口，核心状态、渲染、动作和事件绑定已拆分到 `client/chat/regulations/` 下的 `core.js`、`render-*`、`actions-*` 和 `events.js`。
- **兼容入口**：保留 `client/chat/regulations/render.js` 与 `client/chat/regulations/actions.js` 作为兼容桥接，避免旧缓存或历史脚本路径直接失效。
- **后端路由**：`server/routes/apps/regulations.js` 改为兼容导出，实际路由拆分到 `server/routes/apps/regulations/` 下的 `documents.js`、`articles.js`、`ai.js`、`helpers.js` 和 `index.js`。
- **后端服务**：`server/services/regulations.js` 改为兼容导出，法规服务按检索、解析、目录、引用关系、协作、分析和写入变更拆分到 `server/services/regulations/` 下的多个小模块。

#### 问题修复

- **加载失败修复**：修复拆分后 `bindEvents` 未挂载到共享命名空间，导致打开应用提示“法规查询加载失败”的问题。
- **加载顺序兼容**：新增法规查询模块就绪等待逻辑，确保新入口和旧兼容入口都能稳定初始化。

#### 引用网络优化

- **核心图模式**：引用网络不再把大量条文强行压进环形图，大关系集优先显示高关联度核心条文。
- **关系明细**：右侧列表保留全部内部引用关系，点击条文仍可跳转定位到对应条文。
- **弹窗与滚动**：引用网络弹窗加宽，画布区和关系明细区独立滚动，并在窄屏下自动切换为单列布局。
- **标签溢出修复**：节点标题改为两行截断显示，完整条文标题保留在悬浮提示中，解决长条文标题超出节点边框的问题。

#### 验证

- `node --check client\chat\regulations\actions-advanced.js`
- `npm run check`
- `npm run lint`
- 已检查新旧加载入口运行时模拟和引用网络长标题样例。

## [v0.0.180] - 2026-07-01

### 法规查询修复与治理：文档名/版本号识别、条数精确化、AI 问答修复、生效/失效日期移除、删除弹窗与向量分批

本次针对法规查询在实际使用中暴露的一批问题做修复与治理：修正文档标题与版本号的自动识别、按实际条文数计数、修复 AI 问答“未选择模型”、修复向量生成 400，并按开发阶段需要彻底移除“生效日期/失效日期”两个字段，统一删除确认弹窗交互。

#### 问题修复

- **文档名称优先按文件名识别**：上传时优先从文件名识别法规标题，并剥离文件名尾部的版本日期与“修订/修正/版本”等后缀；识别不到再回退正文。
- **版本号自动识别**：从文件名尾部的日期识别版本号，兼容 `20240101`、`2024-01-01`、`2024.01.01`、`2024年01月01日` 等写法（如 `中华人民共和国公司法_20240101.doc` → 版本“2024年01月01日”）；用户手填值优先。
- **条文条数按实际“第X条”计算**：本地结构解析优先于 RAG 切片，`countActualRegulationArticles` 按“第X条”去重计数，长条被切分或存在前言/章节标题时不再虚高。
- **保留法律首部内容**：正文首条之前的标题、颁布信息、目录、序言等作为“前言”条目保留，不再在解析后丢失。
- **AI 问答“未选择模型”**：法规问答请求携带聊天页所选模型（与公文写作一致），无个人/系统默认模型时不再报“未找到可用模型”。
- **编辑弹窗可显示/修改版本号**：文档详情返回当前版本号并回填编辑弹窗，保存时同步更新当前版本的版本标识。
- **向量生成 400 修复**：法规向量复用知识库同一处向量模型配置，改为分批（每批 5 条）+ 单条截断发送，修复整部法律条文一次性提交导致向量服务返回 400；并修复向量结果与条文索引错位（会把向量写到错误条文）的缺陷。失败仍安全降级为纯 BM25 检索。

#### 功能移除（开发阶段清理）

- **移除“生效日期”**：删除 `effective_date`、`effective_date_normalized` 列及其自动识别、按日期排序、列表列与上传/编辑表单输入。
- **移除“失效日期”与日期废止状态**：删除 `expire_date` 列及基于失效日期的“已废止”徽章与统计，文档状态仅保留“有效 / 已归档”；条文级“已修正/已废止”状态不受影响。

#### 交互优化

- **删除确认弹窗统一**：法规文档、保存检索、条文批注的删除确认改用项目内统一的自定义弹窗（`showConfirm`），不再使用浏览器默认 `confirm`。

#### 数据库变更

- 新增迁移 `202607010001_drop_regulation_date_fields`：从 `regulation_documents` 删除 `effective_date`、`effective_date_normalized`、`expire_date` 三列（幂等、带列存在性检查）。**该删列不可逆**；存量法规如需启用语义检索，建议重新导入或重建向量。

#### 其它

- 清理未使用的死变量，ESLint 零告警；同步更新 README 与《使用帮助》相关说明；补充 `tests/security-rag/regulations.js` 覆盖文件名标题/版本识别、条数计算与版本号编辑。

## [v0.0.179] - 2026-06-30

### 法规查询界面优化：弹窗样式统一、检索表单 Grid 重构与主按钮样式调整

本次更新对法规应用的弹窗外观和工具栏按钮进行了全面优化，解决了在垂直布局下输入控件高度异常拉伸以及按钮排布不协调的样式缺陷，并实现了与系统全局弹窗样式的统一。

#### 优化功能

- **弹窗样式全局统一**：法规应用中所有的弹窗（法规检索、法规原文详情、AI问答、修订时间线、引用网络、条文批注、文档导入等）均引入全局的 `.workspace-modal`、`.workspace-modal-header`、`.workspace-modal-body` 和 `.workspace-modal-close` 样式类。统一了头部背景与间距、主体背景以及红框红字的“关闭”按钮样式，当内容过长时内容区可自适应并支持独立滚动，头部固定不随之滚动。
- **检索表单 Grid 网格化重构**：将法规检索表单重构为双列 CSS Grid 网格布局。“检索内容”输入框独占整行（跨两列），其他选项分列对齐，排版直观。
- **重置输入控件高度（修复拉伸 Bug）**：为弹窗内所有的 `.form-input` 输入和下拉选择框设置 `flex: none !important;`。重置了它们原本在水平工具栏中被赋予的 `flex-basis` 属性，从而彻底解决了在垂直 Flex 布局下高度被异常拉伸为 300px/132px 的样式缺陷，高度完全复原为统一的 `36px`。
- **主工具栏按钮重排与对调主题色**：
  - **紧凑右对齐**：移除了 `.regulations-ai-entry-btn` 上的 `margin-right: auto;`。让“检索”与“AI问答”按钮与右侧的“刷新”和“导入文档”靠右紧凑排列在最右侧。
  - **对调主题色**：将“检索”按钮设为 `btn-primary`（高亮绿色），将“AI问答”按钮设为 `btn-secondary`（灰白底色，不再高亮绿色），突出了“检索”作为本模块核心功能按钮的主次级视觉对比。

## [v0.0.178] - 2026-06-30

### 法规查询智能检索（第四批/共四批）：语义混合检索、相似条文、保存检索

第四批补强智能检索能力：引入可选的向量嵌入基建（未配置时自动降级），支持语义混合检索、相似条文推荐、保存检索书签。

#### 新增功能

- **#2 语义混合检索（可选，降级友好）**：`regulation_articles` 新增 `embedding` 列，导入时异步生成向量（失败不阻断）；新增 `searchRegulationArticlesHybrid`：BM25 召回候选 → 向量相似度重排，提升语义匹配；无嵌入端点时自动降级为纯 BM25，不影响现有功能。
- **#14 相似条文推荐**：`findSimilarRegulationArticles` 基于向量近邻推荐相似条文（降级为同分类/同颁布机构），`GET /articles/:articleId/similar` 端点已就位。
- **#14 保存检索**：新增 `regulation_saved_searches` 表，支持用户保存常用检索条件（名称+查询+分类+管辖区），`GET/POST/DELETE /saved-searches` 三端点完成 CRUD。

#### 后端

- `server/services/regulations.js` 新增：`embedRegulationArticles`（异步生成向量，降级 console.warn）、`searchRegulationArticlesHybrid`（BM25+向量混合）、`findSimilarRegulationArticles`（向量近邻/降级同分类）、保存检索 CRUD（`createSavedSearch`/`listSavedSearches`/`deleteSavedSearch`）。
- `saveRegulationDocumentVersion` 导入后置任务增加向量生成（setImmediate，失败不影响入库）。
- `server/routes/apps/regulations.js` 新增路由：`GET /search/hybrid`（混合检索）、`GET /articles/:articleId/similar`（相似条文）、保存检索 3 端点。
- 迁移 `202606300005_regulation_article_embedding` + `202606300006_regulation_saved_searches` + `base.js` 双写，PRAGMA 守卫可重复执行。

#### 前端

- **暂无 UI 变更**：第四批核心价值在后端基建（向量列、降级逻辑、hybrid/similar API），前端可按需调用或后续完善（混合检索切换、相似条文按钮、保存检索面板）。

#### 验证

- `node -c` / `eslint --max-warnings=0` 全通过
- 迁移测试：`embedding` 列、`regulation_saved_searches` 表均创建成功
- 功能测试：向量生成降级（401 → warn 不阻断）、保存检索 CRUD
- 降级策略验证：无嵌入端点时，导入/检索均正常工作（纯 BM25 模式）

#### 已知限制与后续

- **嵌入端点**需配置（用户级 embedding.http.url），未配置时所有向量功能自动降级为传统检索，不影响现有法规查询能力
- **#9 扫描件 OCR** 依赖视觉模型（文本为空时 PDF→PNG→OCR），待视觉模型端点就位后实现
- **前端 UI**：混合检索切换、相似条文推荐按钮、保存检索面板待后续按需完善（API 已就位）

#### 四批总览

- ✅ **第一批**（v0.0.175）：跨法回连、别名、废止提醒、变更影响、引用网络
- ✅ **第二批**（v0.0.176）：条文级状态、导入预览、修订时间线、精确定位
- ✅ **第三批**（v0.0.177）：条文批注、AI 报告导出、查阅审计
- ✅ **第四批**（v0.0.178）：语义混合检索、相似条文、保存检索（可选，降级友好）

## [v0.0.177] - 2026-06-30

### 法规查询协作与合规（第三批/共四批）：条文批注、AI 报告导出、查阅审计

第三批补强团队协作与合规留痕能力：条文级批注共享、AI 问答导出合规报告、查阅行为审计，并为文档增加可见性维度。

#### 新增功能

- **#10 条文批注**：详情页每条条文新增"批注"按钮（显示批注数），点击打开批注面板，团队成员可共享内部理解、适用案例、注意事项；支持新增、查看、删除（仅本人可删自己的批注）。新增 `regulation_article_annotations` 表，详情接口附加每条 `annotationCount`。
- **#11 AI 回答导出合规报告**：AI 问答每轮回答下新增"导出报告"按钮，把问题、回答、依据法条（直接命中/经引用关联，含引用关系）汇总为 Markdown 报告下载，供合规留档（`buildRegulationQaReport`）。
- **#12 查阅审计**：自动记录查阅（view）、检索问答（ai_query）、下载（download）行为到 `regulation_access_logs` 表（含用户、文档、动作、时间）；管理员可通过 `GET /access-logs` 查询（谁查了哪部法）。
- **#12 文档可见性维度**：`regulation_documents` 新增 `visibility` 列（public/internal/restricted），为后续按可见性的查阅范围控制打基础。

#### 后端

- `server/services/regulations.js` 新增：批注 CRUD（`createRegulationAnnotation`/`listRegulationAnnotations`/`updateRegulationAnnotation`/`deleteRegulationAnnotation`，编辑/删除带本人校验）、`recordRegulationAccess`/`listRegulationAccessLogs`（审计）、`buildRegulationQaReport`（报告生成）。
- `server/routes/apps/regulations.js` 新增路由：批注 4 个端点、`POST /report`、`GET /access-logs`（管理员）；并在 detail/download/ai 路由注入 `recordRegulationAccess` 审计埋点。
- 迁移 `202606300003_regulation_article_annotations` + `202606300004_regulation_access_and_visibility` + `base.js` 双写，PRAGMA 守卫可重复执行。

#### 前端

- 条文块头部增加批注按钮（带计数），批注面板支持提交/列表/删除，按作者归属控制删除权限。
- AI 问答每轮增加"导出报告"按钮，调用 `/report` 生成 Markdown 并通过 Blob 下载。
- CSS：批注按钮（药丸样式）、批注面板、批注项、AI 报告导出按钮区。

#### 验证

- `node -c` / `eslint --max-warnings=0` / `check_chat_assets` 全通过
- 迁移测试：`regulation_article_annotations` 表、`regulation_access_logs` 表、`visibility` 列均创建成功
- 功能测试：批注 CRUD（含他人编辑拦截）、审计记录（view/download）、报告生成（Markdown 含依据法条分组）

#### 已知限制与后续

- 可见性（visibility）目前仅落库与埋点审计，按可见性的查阅范围强制过滤待后续按需开启
- 条文状态（#8）与可见性的管理员编辑 UI 仍以 API 为主，批量编辑面板可后续补充
- 第四批（依赖外部基建，做可选降级）：语义混合检索（#2）、相似条文/保存检索（#14）、扫描件 OCR（#9）

## [v0.0.176] - 2026-06-30

### 法规查询引用图谱完善（第二批/共四批）：条文级状态、导入预览、修订时间线、精确定位

第二批补强条文生命周期管理、导入质量前置校验与版本演进可视化，同时优化详情页分条渲染实现引用跳转精确定位。

#### 新增功能

- **#8 条文级状态管理**：`regulation_articles` 新增 `status`（active/amended/repealed）与 `amended_date` 列，详情页每条显示状态徽章（已修正/已废止 + 日期），管理员可通过 `PUT /articles/:articleId/status` 设置条文状态。
- **#7 导入预览与边界校正**：上传表单新增"预览条文"按钮，调用 `POST /documents/preview` 仅解析不落库，返回切出的条文列表；管理员可在预览面板检查切分边界，选中相邻条文合并后再入库（合并功能 UI 已就位、逻辑待后续完善）。
- **#13 修订时间线**：详情页"修订时间线"按钮打开版本演进视图，按版本 ID 升序展示所有版本（施行日期标识 + 条文数 + 当前版本标记），点击节点切换到对应版本。
- **精确定位增强**：详情正文改为分条渲染（每条带 `regulation-article-{id}` 锚点与状态徽章），引用网络节点点击、废止链接、AI 依据条文跳转现在都能滚动到目标条文（之前只能跳文档不能定位条文）。

#### 后端

- `server/services/regulations.js` 新增：`setRegulationArticleStatus`（设置条文状态）。
- `server/routes/apps/regulations.js` 新增路由：`POST /documents/preview`（解析预览，复用 `parseRegulationArticles`）、`PUT /articles/:articleId/status`（管理员设置条文状态）。
- 迁移 `202606300002_regulation_article_status` + `base.js` 双写，PRAGMA 守卫可重复执行。

#### 前端

- 详情正文由整篇连续文本改为分条渲染（`regulations-article-block`），每条独立锚点、状态徽章、废止徽章，单条「全文」时退化为整篇。
- 导入表单新增"预览条文"按钮，打开预览面板展示解析结果（条文序号、标签、标题、内容摘要），提供"合并选中条文"占位（待完善）、"确认入库"（待完善，当前请关闭预览直接提交表单）。
- 修订时间线：纵向节点布局 + 左侧渐变时间轴，当前版本节点高亮加粗，点击切换版本。
- CSS：条文块、状态徽章（已修正/已废止/被修订）、时间线节点与连线、预览面板、响应式滚动边距。

#### 验证

- `node -c` / `eslint --max-warnings=0` / `check_chat_assets` 全通过
- 迁移测试：`regulation_articles` 成功新增 `status` / `amended_date` 列
- 功能测试：`setRegulationArticleStatus` 设置 repealed + 日期；`parseRegulationArticles` 正确切出 2 条

#### 已知限制与后续

- 预览面板的"合并选中条文"与"确认入库"逻辑待第三批完善（当前请关闭预览直接提交表单入库）
- 条文状态设置目前仅通过 API，管理员 UI（详情页每条右键菜单或批量编辑）待第三批
- 第三批将交付：条文批注（#10）、AI 报告导出（#11）、查阅审计与权限细化（#12）

## [v0.0.175] - 2026-06-30

### 法规查询引用图谱完善（第一批/共四批）：跨法回连、别名、废止提醒、变更影响、引用网络

本版本为 14 项法规查询升级路线图的第一批，全部基于已有 `regulation_article_links` 引用图谱做增量增强，无外部依赖，补强"关联法条"真正可用、可视、可追溯影响。

#### 新增功能

- **#1 跨法引用回连**：导入后自动把"《XX法》第N条"这类跨法律引用，通过别名表匹配到库内文档、按条号对齐到目标条文 id（`resolveRegulationCrossLinks`），悬空外链变成真正的跨法律关系边；管理员可调 `POST /cross-links/rebuild` 对全库或指定文档重建关联。
- **#3 法律别名/简称归一**：新增 `regulation_aliases` 表，导入时从标题派生常见简称（去"中华人民共和国"前缀、取书名号主体），供跨法匹配与检索召回。
- **#5 废止/修订联动提醒**：详情页顶部横幅汇总"本法以下条文已被其它法规废止或修订"，基于 `supersede` 引用关系（`getRegulationSupersedeNotices`），可点击跳转到来源法规。
- **#6 变更影响分析**：版本对比面板新增"影响分析"按钮，复用 `diffRegulationVersions` 找出变更条文，再反查引用了这些条文的本法/其它法律条文（`analyzeRegulationChangeImpact`），回答"改这条影响什么"。
- **#4 条文引用网络可视化**：详情页"引用网络"按钮打开轻量 SVG 环形图（纯手写、无第三方图库），节点为条文、连线为引用关系（引用/依据/废止/适用按色区分），点节点跳转到对应条文。新增 `GET /documents/:id/citation-graph`。

#### 后端

- `server/services/regulations.js` 新增：`deriveRegulationAliases`/`saveRegulationAliases`/`normalizeRegulationAlias`（别名）、`resolveRegulationCrossLinks`/`rebuildRegulationCrossLinks`/`parseCrossLinkLabel`（跨法回连）、`analyzeRegulationChangeImpact`（变更影响）、`getRegulationCitationGraph`（引用网络）、`getRegulationSupersedeNotices`（废止提醒）。
- 导入版本落库后置任务（事务外、失败不阻断）：更新别名表 + 解析跨法引用回连。
- 文档详情每条附加 `supersededBy` 数组，供前端废止徽章。
- `server/routes/apps/regulations.js` 新增路由：`/documents/:id/change-impact`、`/documents/:id/citation-graph`、`/cross-links/rebuild`（管理员）。
- 迁移 `202606300001_regulation_aliases` + `base.js` 双写，PRAGMA 守卫可重复执行。

#### 验证

- `node -c` / `eslint --max-warnings=0` / `check_chat_assets` 全通过
- 单元测试：别名派生（"中华人民共和国公司法"→主名+"公司法"）；跨法回连（文档1引用"《公司法》第五条"→匹配文档2→对齐第五条 id，confidence 0.8）；引用网络（3节点2边正确聚合）

#### 后续批次（待交付）

- 第二批：导入预览与边界校正（#7）、条文级状态（#8）、修订时间线（#13）
- 第三批：条文批注（#10）、AI 报告导出（#11）、查阅审计与权限细化（#12）
- 第四批：语义混合检索（#2）、相似条文/保存检索（#14）、扫描件 OCR（#9）——依赖外部基建，做可选降级

## [v0.0.174] - 2026-06-29

### 品牌图标体系与登录页重设计

本版本围绕 Pivot 智枢品牌视觉做统一升级：使用新的透明 Logo 生成浏览器 favicon、PWA、移动端、桌面端等标准尺寸图标，修复旧图标白底、过小和变形问题；同时重设计登录页左侧品牌展示区，形成白绿主题的内网 AI 控制平面视觉，并保留现有账号登录、注册和客户端下载流程。

#### 视觉升级

- **标准图标统一替换**：基于 `logo图标.png` 生成透明背景 favicon、PWA 图标、Apple Touch Icon、Android Chrome 图标、maskable 图标和桌面端 `icon.ico`，替换旧图标资源。
- **浏览器标签图标修复**：重新生成 16/32/48px favicon，并保持图形原始比例，避免标签页图标过小、拉伸变形或出现白色底色。
- **登录页品牌重设计**：左侧改为“内网 AI 控制平面”主视觉，采用白色与绿色主题、中心 Logo、六个功能节点和轻量科技网格背景，右侧继续沿用项目现有登录/注册方式。
- **中心 Logo 底座**：为登录页主 Logo 新增白绿分层底座、柔光投影和发光中心点，增强主视觉的空间感与灵动感。
- **功能节点图标优化**：重新设计“知识库检索”和“数据分析”节点图标，分别突出资料检索与趋势分析语义；保留多模型接入、工作流编排、工具箱、权限治理等能力展示。
- **登录页文案内网化**：将平台定位统一为“内网AI对话与工作流编排平台”“内网权限体系”“驱动内网智能化”，登录提示改为“使用内网账号进入工作台”。

#### 体验优化

- **登录页无滚动适配**：登录页启用 `auth-active` 页面状态，限制认证页滚动，并为低高度、窄屏视口补充响应式收缩规则。
- **底部版权居中**：登录页 copyright 调整为底部居中固定显示，避免贴边或遮挡主视觉。
- **注册切换细节**：登录/注册切换时只更新按钮文字，保留按钮箭头图标和主按钮样式。

#### 验证

- `node --check client/chat/auth.js`
- `node scripts/check_chat_assets.js`
- `node scripts/sync_version.js`
- `git diff --check`

## [v0.0.173] - 2026-06-29

### 法规查询：引用图谱关联法条，复用 RAG 法律切片与知识图谱能力

本版本为法规查询接入项目已有的 RAG 法律切片与知识图谱能力，实现"查询所有关联法条并给建议"的核心诉求：上传时解析条文间的交叉引用关系（第X条、《书名号》依据/参照/废止等），AI 问答时沿引用边扩展召回关联条文，前端按「直接命中 / 经引用关联」分组展示并标注引用关系，形成结构化「核心法条 → 关联法条 → 适用建议」回答。

#### 新增功能

- **条文引用图谱**：导入时自动解析条文正文中的交叉引用（「第X条」「第X条之X」「《书名号》第X条」及「依据/根据/参照/适用/废止/替代」等动词），建立条文间的引用关系（新增 `regulation_article_links` 表），同文档内条号对齐到本库条文 id，跨法引用存原始标签待后续扩展。
- **检索扩展**：AI 问答时，在 BM25 关键词召回基础上（召回数从 8 提到 12），沿引用关系双向扩展（最多补 8 条）：把直接命中条文所引用的、以及引用它们的关联条文一并纳入上下文。
- **结构化 AI 提示**：system 提示升级为结构化引导，要求按「核心法条 → 关联法条（注明引用关系）→ 适用建议」组织回答，并明确关联法条必须基于提供的引用关系、不得臆造。
- **前端分组展示**：AI 答案的「依据条文」区按「直接命中条文」和「关联条文」分两组标题展示，关联条文用浅绿背景区分并显示引用关系标签（如「本条引用」「被引用（依据）」），点击仍可跳转高亮。

#### 架构升级

- **复用 RAG 法律切片**：「第X条」结构优先调用 `rag-chunker.js` 的 `chunkDocument({docType:'legal'})`，按章/节/条切分并生成 `headingPath`（《XX法》› 第三章 › 第十条），定位更精准；`regulation_articles` 新增 `heading_path` 列记录完整标题路径。非「第X条」结构（Markdown 标题 / 中文数字小节）与全文兜底保留现有逻辑，向后兼容。
- **复用知识图谱关系抽取**：引用解析复用 `knowledge-graph.js` 的 `LEGAL_RELATION_PATTERNS`（references/supersedes/applies_to）动词模式，条号对齐用法规自己的正则（更精准）。关系数据落法规自有 `regulation_article_links` 表（与三层表一致），不塞用户级 `knowledge_*` 表，避免所有权冲突。
- **关系按版本隔离**：links 表绑 `version_id` 并 `ON DELETE CASCADE`，追加版本重新抽取，版本对比 / 历史不串味。

#### 验证

- `node -c` 所有修改文件语法通过；`npx eslint --max-warnings=0` 零警告；`node scripts/check_chat_assets.js` 通过
- 迁移可重复执行（PRAGMA 守卫）：`heading_path` 列 / `regulation_article_links` 表 / 三条索引
- 单元测试：`parseRegulationArticles` 用 chunkLegal 切出 3 条且 headingPath 正确（`《测试法》 › 第X条`）；`extractRegulationLinks` 正确识别同文档条号引用（对齐到 sortOrder）与跨法书名号引用（targetOrder null）；`expandRegulationMatchesByLinks` 对查询第三条扩展出第一条（本条引用）、第二条（本条依据）

#### 设计取舍

- **不引向量库**：本期只做显式引用图谱（规则解析，准确率高、可解释），语义关联（向量）留作后续可选扩展
- **切片向后兼容**：chunkLegal 仅作为「第X条」结构的更优路径，其它结构与兜底保留现状；旧数据不回填
- **关系启发式**：引用抽取为规则启发式，存在漏抽 / 误抽，`confidence` 字段记录，跨法外链先只存文本不强连

## [v0.0.172] - 2026-06-29

### 法规查询体验优化：法规化预设、编辑回归、版本按日期与界面打磨

本版本在 v0.0.171 核心功能基础上，针对法律法规实际使用场景做体验打磨：面向全国性法律法规的快速预设、把编辑/追加版本重新接入详情弹窗、版本标识改用施行日期、AI 问答弹窗放大、表格行高修复等。

#### 新增功能

- **快速预设模板**：导入弹窗顶部新增「快速预设」区，一键填充常用公共字段，覆盖全国性法律法规场景：法律（人大）、法律（人大常委会）、行政法规、司法解释（最高法）、司法解释（最高检）、部门规章。批量导入同类法规时，一次点击即可填好分类、发布机构、适用范围。
- **版本标识按施行日期生成**：版本标识留空时，按法规惯例优先用施行日期生成「YYYY年MM月DD日」标签（三级兜底：用户填写值 → 施行日期 → v 序号）。追加版本表单新增「施行日期」输入框，可主动指定版本施行日期。

#### 体验优化

- **编辑信息 / 追加版本重新接入**：界面重构为弹窗式后，将失效的「编辑信息」「追加版本」功能重新接入详情弹窗头部（管理员可见），提交后自动刷新详情；清理了对应的孤儿事件处理器与死代码。
- **AI 问答弹窗放大**：标题由「条文问答」改为「AI 问答」，弹窗宽度从 920px 增大到 1120px（与详情弹窗一致），问答区与答案展示区更宽敞。
- **字段文案法规化**：分类、发布机构、适用范围、版本标识等输入框占位文案统一调整为法律法规语境（如分类示例「法律、行政法规、司法解释」，发布机构示例「全国人大常委会、国务院」，适用范围示例「全国、某省、某市」）。

#### 缺陷修复

- **表格行高被撑高**：修复法规列表中「状态」「操作」列把行撑高的问题。提升操作按钮覆盖选择器特异性（`.regulations-row-actions .regulations-row-action`）压过全局 `.btn-danger-outline`（34px）基类；状态徽章用 `inline-flex` 容器紧凑并排；表体行统一 40px 高度、收紧上下内边距并垂直居中。

#### 验证

- `node -c` 所有修改文件语法通过
- `npx eslint --max-warnings=0` 零警告
- `node scripts/check_chat_assets.js` 通过（29 partials, 109 style imports）

## [v0.0.171] - 2026-06-29

### 法规查询增强：多轮问答、版本对比、分页加载与健壮性提升

本版本全面增强法规查询模块，新增版本对比 diff、多轮问答、分页加载、失效日期管理、导入重复检测等核心功能，并优化条文解析、过滤匹配与用户体验。

#### 新增功能

- **版本对比 diff**：详情页新增"版本对比"按钮（≥2 版本时显示），选择任意两版本后生成逐条对比报告，展示新增/删除/变更条文及行级差异高亮（基于 LCS 算法）。
- **多轮问答**：AI 问答支持携带最近 4 轮历史上下文，可追问式提问；问答历史以对话气泡形式展示，每轮独立显示依据条文来源。
- **分页加载**：文档列表从固定 80 条改为分页滚动加载（每页 80 条），底部显示"已加载 X / 共 Y 件"计数和"加载更多"按钮，优化大库性能。
- **失效日期管理**：新增 `expire_date` 字段（导入表单、编辑表单均可填写），自动计算文档现行状态（`effect_status`）：留空视为现行有效，填写后过期自动标记"已废止"徽章。
- **导入重复检测**：单个/批量导入时通过文件 SHA256 检测重复源文件，命中时提示"疑似重复：已有同源文档"但仍允许导入（不阻断，仅提醒）。
- **导入结果面板**：批量导入后如有失败或重复，弹出详细结果面板，分别列出"已导入"（含重复标记）和"失败"（含错误原因）清单。
- **条文操作增强**：每条条文底部新增操作按钮行：复制原文、带入问答框、复制定位链接（`#reg=docId.articleId` 格式，刷新后自动跳转）。
- **过滤增强与 facets**：分类/适用范围过滤改为模糊匹配（LIKE `%keyword%`），新增 `/facets` 接口返回所有候选值，导入表单和编辑表单绑定 `<datalist>` 提供下拉提示。
- **条文解析多策略**：`parseRegulationArticles` 增强为三策略兜底：优先识别"第X条""第X章"等标准格式，次选连续空行分段，最后按固定字符数切块，确保任何文档都能解析出条文。

#### 数据库变更

- **新增列**：`regulation_documents` 新增 `effective_date_normalized TEXT`（生效日期结构化存储，用于范围查询）、`expire_date TEXT`（失效日期，可选）。
- **迁移脚本**：`202606290001_regulation_date_columns` 为已有库追加两列；`server/db/schema/base.js` 同步更新建表语句，新装环境直接包含。

#### 后端优化

- **生效日期结构化**：导入/编辑时自动将 `effective_date`（如"2026年6月"）规范化为 `YYYY-MM-DD` 格式存入 `effective_date_normalized`，便于未来按日期范围过滤。
- **现行状态计算**：`computeRegulationEffectStatus(doc)` 根据 `expire_date` 和当前日期判断返回 `'active'` 或 `'expired'`；列表和详情接口自动附加 `effect_status` 字段。
- **版本对比服务**：新增 `diffRegulationVersions({documentId, fromVersionId, toVersionId})`，返回新增/删除/变更条文及行级 diff segments（`{type:'eq'|'add'|'del', text}`）。
- **多轮问答支持**：`/ai` 接口接收 `history: [{question, answer}]` 数组，携带最近 4 轮展开为对话消息后调用 AI，保持上下文连贯。
- **重复检测服务**：`findRegulationDuplicateByHash(sha256)` 按文件哈希查已有同源文档，导入时调用并附加 `duplicateOf: {documentId, title}` 到返回结果。

#### 前端优化

- **徽章显示**：文档列表和详情卡片自动展示"已归档"（灰色）、"已废止"（橙色）徽章，直观标识状态。
- **版本对比 UI**：新增独立对话框，选择两版本后渲染三区块（新增/删除/变更），变更条文展示行级 diff（绿色 `+` 新增行、红色 `-` 删除行、灰色不变行）。
- **多轮气泡**：AI 回答区从单次回答改为多轮对话历史，用户问题右对齐浅蓝气泡，AI 回答左对齐卡片，每轮独立展示依据条文。
- **深链接解析**：启动时解析 URL hash `#reg=docId.articleId`，自动加载文档并滚动定位到指定条文。
- **导入表单增强**：失效日期输入框、分类/适用范围绑定 `<datalist>` 下拉候选（从 `/facets` 动态加载）。

#### 样式补充

- `.regulations-badge`：徽章样式（归档灰、废止橙、重复提醒黄）
- `.regulations-load-more`：加载更多按钮（虚线边框、悬停变实线+主题色）
- `.regulations-ai-turn`、`.regulations-ai-question`、`.regulations-ai-card`：多轮对话气泡布局
- `.regulations-article-actions`：条文操作按钮行（顶部细边框分隔）
- `.regulations-import-result-panel`、`.regulations-import-section`、`.regulations-import-item`：导入结果面板（成功绿、失败红边框）
- `.regulations-diff-*`：版本对比面板样式（新增绿背景、删除红背景、变更橙边框、行级 diff 高亮）

#### 验证

- `node -c` 所有修改文件语法通过
- `npx eslint` 无警告
- `node scripts/check_chat_assets.js` 通过（29 partials, 109 style imports）
- 迁移脚本已添加到 `server/db/migrations/regulations.js`，兼容已有库和新装环境

## [v0.0.170] - 2026-06-29

### 法规查询与更新提示收尾加固

本版本继续收口法规查询导入体验和右下角更新提示：后端版本来源只接受规范三段式版本号，前端提示在异常版本信息下自动兜底，同时继续保留 `v0.0.168` 法规查询批量导入与全局样式统一日志，避免版本历史断档。

#### 变更内容

- **版本来源加固**：`server/version.js` 从更新日志读取版本时只匹配 `v?数字.数字.数字`，避免把“变更内容”等标题文本误当版本号。
- **更新提示兜底增强**：PWA 更新提示会分别从 `version` 和 `build` 中提取规范版本号；都不规范时只显示“检测到新版本，请刷新页面”。
- **法规导入说明补齐**：README、使用帮助和更新日志继续记录法规查询批量导入、自动识别标题/日期和支持格式，保留 `v0.0.168` 的完整历史记录。
- **文档解析细节修复**：恢复文档解析文件中被乱码化的中文注释，并保持 HTML/HTM 文本抽取格式支持。
- **文档与版本同步**：当前版本升级至 `0.0.170`，包元数据、README、使用帮助和更新日志保持一致。

#### 验证

- `node -c server/version.js`
- `node -c client/pwa-manager.js`
- `node -c server/document-text.js`
- `node scripts/sync_version.js`

## [v0.0.169] - 2026-06-29

### PWA 更新提示版本显示修复

本版本修复右下角更新提示框在某些缓存或版本信息异常时出现 `v##` 一类错误文本的问题，并让更新提示只展示规范版本号或友好兜底文案。

#### 变更内容

- **更新提示提纯**：PWA 更新提示会从 `version/build` 中提纯规范版本号后再展示，版本信息异常时自动回退为“检测到新版本，请刷新页面”。
- **刷新状态稳固**：点击刷新会写回当前构建标识，避免错误版本值继续残留在本地存储中。
- **法规导入控件收口**：导入文档弹窗补齐顶部关闭入口，文件选择区改为统一的“选择文档 + 已选摘要”控件，批量导入时直接显示已选数量和总大小。
- **历史日志补齐**：补回并保留 `v0.0.168` 更新日志，记录法规查询批量导入、全局样式统一、后端导入链路和文档同步内容。
- **文档与版本同步**：当前版本升级至 `0.0.169`，更新日志、包元数据、README 和使用帮助已同步记录。

#### 验证

- `node -c client/pwa-manager.js`
- `node -c scripts/sync_version.js`
- `node -c client/chat/apps-workbench-regulations.js`

## [v0.0.168] - 2026-06-29

### 法规查询批量导入与全局样式统一

本版本把法规查询的入口、导入和详情工作台做了完整收口：应用名称统一为“法规查询”，前后端样式改用全局工作区控件，导入页支持批量文件选择、自动识别标题和生效日期，适合几百份法规文档一次性入库。

#### 变更内容

- **应用命名统一**：将新应用统一命名为“法规查询”，应用中心、工作区标题、按钮文案和日志记录全部同步。
- **全局样式收口**：法规查询的布局、按钮、输入框、搜索框、弹窗、编辑表单和版本上传面板统一采用全局工作区样式。
- **批量导入优化**：导入弹窗支持一次选择多个文件，单次最多 300 个文档；公共字段可批量填写，系统会优先从正文或文件名识别标题和生效日期。
- **格式支持扩展**：法规导入支持 TXT、Markdown、PDF、Word（DOC/DOCX）、Excel（XLS/XLSX）、CSV、JSON、HTML/HTM。
- **后端导入链路重建**：新增法规服务层和路由层，支持单文档导入、批量导入、追加版本、条文解析、全文检索、下载和归档，并为 AI 问答提供条文上下文。
- **文档同步**：README、使用帮助、包版本和更新日志同步升级到 `0.0.168`。

#### 验证

- `node -c server/services/regulations.js`
- `node -c server/routes/apps/regulations.js`
- `node -c client/chat/apps-workbench-regulations.js`
- `node -c server/document-text.js`

## [v0.0.167] - 2026-06-29

### 法规查询导入弹窗细节收口

本版本进一步补齐法规查询的导入文档弹窗细节，收紧分组、说明文案、文件选择器和底部按钮布局，使上传面板与全局工作台样式保持一致。

#### 变更内容

- **导入弹窗分组优化**：将基础信息、摘要与文件、版本信息、版本文件拆成更清晰的分组，并补充分组说明，减少大表单的视觉压力。
- **文件选择器收口**：将文件输入改为带说明的统一文件区块，补齐标签和提示文案，提升上传区可读性。
- **版本与编辑表单同步**：编辑文档信息与追加版本弹窗同步采用相同分组结构和间距规则，保持工作台内部一致性。

## [v0.0.166] - 2026-06-29

### 法规查询接入应用中心，并补齐法规库版本化治理

本版本新增“法规查询”，将法规制度类文档从通用知识库中进一步沉淀为可管理、可检索、可追溯版本的共享法规库。普通用户可在应用中心检索法规条文、查看文档详情和下载源文件，管理员可维护文档元数据、上传新版本并归档失效文档，AI 问答会优先围绕命中的法规条文组织依据。

#### 变更内容

- **应用中心新增法规查询**：在应用中心注册“法规查询”卡片，接入刷新恢复逻辑和工作区按需加载；新增 `client/chat/apps-workbench-regulations.js` 与 `client/chat/styles/workspaces/apps/regulations.css`，提供法规文档列表、筛选、详情、条文命中、版本、下载、AI 问答和管理员维护弹窗。
- **命名与界面收口**：应用名称统一调整为“法规查询”，页面布局改用全局工作区面板，搜索框、筛选输入框、按钮、维护弹窗、编辑表单、版本上传表单和危险操作按钮统一到全局控件样式。
- **法规库后端服务**：新增 `server/services/regulations.js`，负责法规文档入库、上传文件存储、文本抽取、条文解析、版本保存、文档详情、全文检索、AI 上下文构建、下载路径解析和归档处理，源文件统一存放在 `uploads/regulations/` 下。
- **法规库 API 路由**：新增 `server/routes/apps/regulations.js` 并挂载到 `/api/apps/regulations`，提供文档列表、详情、条文检索、下载、上传、追加版本、元数据更新、归档和 AI 问答接口；上传、版本维护、编辑和归档限制为管理员操作，浏览、检索、详情和下载对登录用户开放。
- **版本化数据库迁移**：新增 `server/db/migrations/regulations.js`，创建 `regulation_documents`、`regulation_versions`、`regulation_articles` 与 `regulation_articles_fts`，并补齐 FTS5 触发器、索引和历史数据回填逻辑；基础 schema 同步纳入法规库表结构，保证新库初始化和旧库升级路径一致。
- **条文级检索与 AI 依据**：法规文本按“第X条 / 第X条之X”等条文边界解析，保留条文编号、标题尾部、章节线索和排序信息；AI 问答接口会基于用户问题检索命中条文，生成带法规来源的上下文，减少脱离制度依据的回答。
- **前端资源治理同步**：应用工作区脚本组新增法规查询脚本，聊天资源检查清单纳入 `apps-workbench-regulations.js`，样式入口 `client/chat/styles/workspaces/apps.css` 同步引入法规查询样式。
- **文档与版本治理**：README、使用帮助和更新日志同步记录法规查询能力，项目版本号提升至 `0.0.166`，并通过版本同步脚本更新包元数据。

#### 验证

- `npm run check` 通过，文本完整性、307 个 JS 文件语法和聊天资源检查均通过。
- `node -c` 已覆盖法规服务、法规路由、迁移、基础 schema、应用中心入口和法规前端脚本等新增/修改文件。
- 法规条文解析自测通过，可正确识别 `第一条`、`第十条之一` 等条文编号。
- 数据库自测确认 `regulation_documents` 与 `regulation_articles_fts` 已创建，空库列表和检索链路返回正常。
- 未登录访问 `/api/apps/regulations/documents?limit=1` 返回 `401`，确认新路由已挂载并受鉴权保护。

#### 版本

- 服务端版本号提升至 `0.0.166`。

## [v0.0.165] - 2026-06-26

### 迁移治理、模块拆分与安全校验收口

本版本补齐上一轮项目治理计划中尚未完成的四项结构性改造：数据库迁移开始收口到版本化 migration，服务与路由大文件拆成可维护子模块，主机/DNS/本地地址判断统一到共享校验层，并将前端聊天入口拆成轻量加载器与实际主逻辑文件。

#### 变更内容

- **迁移治理**：新增 `server/db/migrations/runner.js`、`server/db/migrations/index.js`、`server/db/migrations/legacy.js` 与 `server/db/schema/legacy-preflight.js`，`server/db/migrate.js` 变为约 50 行编排入口，`server/db/schema.js` 变为薄导出入口；后续新增 schema 变更通过版本化 migration 清单接入 `schema_migrations`。
- **旧库快照回归测试**：新增并扩展 `tests/db-migration-snapshots.test.js`，覆盖单个版本化 migration 幂等性，以及临时旧 SQLite 快照经真实 `server/db` 启动路径升级到新结构的完整链路。
- **拆分大服务模块**：`long-term-memory` 拆出抽取、合并、检索、序列化和工具模块；`database-mcp` 拆出连接策略、连接构建、SQL 治理和工具定义；`agent-runtime` 拆出运行环境、元数据、通知、审批、规划和 DAG 运行配置；`rag-index` 拆出 embedding 客户端。
- **统一主机与安全校验**：新增 `server/services/host-classifier.js`，集中本地主机别名、Docker 内部服务名、DNS 解析、本地模型主机等判断，`admin-stats` 复用该共享层，避免安全规则分叉。
- **拆分路由与前端入口**：`chat`、`openai`、`apps`、`mcp` 路由改为薄入口 + 子目录实现 + helpers；`client/chat/app.js` 改为加载器，弹窗辅助逻辑进入 `client/chat/app/dialogs.js`，主绑定逻辑进入 `client/chat/app/main.js`。

#### 验证

- `node tests/db-migration-snapshots.test.js` 通过，覆盖版本化 migration 与旧库快照真实启动升级。
- `node tests/security.test.js` 通过，253 项安全与功能回归全部通过。
- `npm run lint` 通过，无 ESLint warning。
- `npm run check` 通过，文本完整性、303 个 JS 文件语法和聊天资源检查均通过。

#### 版本

- 服务端版本号提升至 `0.0.165`。
## [v0.0.164] - 2026-06-26

### 队列调度、模型监控与报表检索优化，并补齐回归测试

本版本围绕后台任务调度和监控链路做了几项收敛：减少重复唤醒、避免刷新并发堆叠，让失败后的队列可以自动恢复，同时把大目录报表扫描和双文件比较的路径压缩得更顺。

#### 变更内容

- **SQLite 写队列失败后自动恢复**：`server/services/sqlite-write-queue.js` 在批量写入失败后会重新排队并按退避时间再次处理，避免一次异常后队列长时间停摆。
- **Agent 队列唤醒去重**：`server/services/agent-queue.js` 新增合并式调度，短时间内重复触发只保留一次待执行唤醒，减少定时器抖动和空转。
- **模型端点监控改为单飞刷新**：`server/services/model-runtime.js` 将端点监控刷新收敛为单一在途任务，并把固定 `setInterval` 改为自调度 `setTimeout`，避免多个刷新并发堆叠。
- **MCP 报表路径优化**：`server/services/builtin-mcp-reports.js` 将文件遍历改为游标式遍历，报表过滤改为一次命中判断，`compare_files` 的左右摘要并行读取，减少大目录与双文件比较时的等待。
- **补齐回归测试**：新增 `tests/security-sqlite-write-queue.test.js`、`tests/security-agent/queue-scheduling.js`、`tests/security-model-runtime.test.js`，并接入 `tests/security.test.js` / `tests/security-agent.test.js`，覆盖失败重试、唤醒合并、监控去重与定时器策略。

#### 验证

- `node tests/security-model-runtime.test.js` 通过。
- `node tests/security.test.js` 通过（252 项）。

#### 说明

- 服务端版本号提升至 `0.0.164`。
## [v0.0.163] - 2026-06-25

### Docker 生产环境 DuckDB 绑定修复，并修复数据概览 Token 趋势图宽度异常

本版本包含两项关键修复：其一，解决 Docker 容器启动时因 DuckDB 平台原生绑定（`@duckdb/node-bindings-linux-x64/duckdb.node`）缺失而抛出 `MODULE_NOT_FOUND` 致使服务无法启动的问题，并对依赖安装方式进行根治性加固，使镜像构建结果可复现、依赖缺失在构建期即暴露；其二，修复客户端「数据概览」页面「近 30 天 Token 趋势」图表偶发突然缩到左侧、右侧大片留白且不会自动恢复的渲染问题。

#### 问题背景

- 生产环境更新部署后，服务端在 `require('@duckdb/node-api')` 时崩溃，报错 `Error: Cannot find module '@duckdb/node-bindings-linux-x64/duckdb.node'`，调用链经 [server/services/data-analysis/shared.js](file:///e:/pivot/server/services/data-analysis/shared.js) → [server/services/data-analysis.js](file:///e:/pivot/server/services/data-analysis.js) → [server/index.js](file:///e:/pivot/server/index.js)。
- **根因**：`@duckdb/node-api` 按平台拆分原生绑定包（`linux-x64`、`win32-x64` 等），这些绑定均为 **optional dependency（可选依赖）**。当构建处于弱网或镜像源抽风时，npm 对下载失败的可选依赖采取**静默跳过**策略——构建照常成功，直到运行时 `require` 才暴露缺失。叠加 `npm install` 会就地改写/补全 lockfile 的特性，该静默取舍偶发触发，表现为"更新部署时随机失败"。

#### 变更内容

- **构建阶段确定性安装**：在 [Dockerfile](file:///e:/pivot/Dockerfile) 中将依赖安装由 `npm install --omit=dev` 改为 `npm ci --omit=dev`。`npm ci` 严格按 `package-lock.json` 安装、不改写 lock、先清空再装，且在 `package.json` 与 lock 失配时直接报错，从源头消除 `npm install` 的静默取舍行为，保证镜像构建结果可复现。
- **原生绑定校验与自愈兜底**：在 [Dockerfile](file:///e:/pivot/Dockerfile) 依赖安装后新增校验层——装完立即执行一次 `require('@duckdb/node-api')`；若加载失败则按当前平台显式补装 `@duckdb/node-bindings-linux-x64`（版本与 `@duckdb/node-api` 保持一致），补装后仍无法加载则让 `node -e` 非零退出、**使构建当场失败**。该层将原本运行时才暴露的崩溃前移至构建期，彻底杜绝缺失绑定的镜像流入生产。
- **修复数据概览 Token 趋势图宽度异常缩窄**：
  - 根因为 [client/chat/stats.js](file:///e:/pivot/client/chat/stats.js) 的 `renderTrendChart` 在取画布宽度时，当所在标签页处于隐藏（`display:none`）或缩放容器宽度变量尚未就绪的瞬间，`parentElement.clientWidth` 读到 0，旧逻辑 `Math.max(parentWidth || rect.width || 600, 320)` 会退化为固定 `600px` 并写死到画布上，且无重试机制，导致图表突然缩到左侧、右侧大片留白且不会自动恢复；同时 `rect.width` 兜底取自被 CSS `scale` 二次缩小的 `getBoundingClientRect`，本身也不可靠。
  - 修复后：当容器宽度未就绪（`parentWidth < 1`）时不再按错误宽度落笔，改为通过 `requestAnimationFrame` 等待下一帧重试，直至取得真实未缩放宽度再绘制；并设 60 帧（约 1 秒）重试上限，容器长时间不可见则放弃本次绘制（下次进入该页会重新加载渲染），避免隐藏状态下空转。
  - 移除被 CSS `scale` 污染的 `rect.width` 兜底，统一采用父容器未缩放布局宽度 `parentElement.clientWidth`。该函数同时服务统计报表页的 `report-trend-chart`，一并受益。

#### 验证

- 本地执行 `npm ci --omit=dev --dry-run` 通过，确认 `package.json` 与 `package-lock.json` 同步，且安装计划中包含 `@duckdb/node-bindings-linux-x64 1.5.4-r.1`，`npm ci` 不会因失配中断构建。
- 校验 [Dockerfile](file:///e:/pivot/Dockerfile) 依赖安装语句已为 `npm ci --omit=dev`。
- 校验 [Dockerfile](file:///e:/pivot/Dockerfile) 已包含 DuckDB 原生绑定的 `require` 校验、缺失自愈补装与失败即退出的构建守卫，重建日志应输出 `[build] DuckDB 原生绑定校验通过`。
- 重建命令：`docker compose build --no-cache pivot && docker compose up -d`。
- 运行 `npm run check`（含 263 个 JS 文件语法校验、文本完整性校验）与 ESLint 均通过；客户端实测「数据概览」页面在反复切换设置标签、连续点击「刷新」后，「近 30 天 Token 趋势」图表始终保持满宽渲染。

#### 说明

- 后续升级 `@duckdb/node-api` 时，需同步更新 [Dockerfile](file:///e:/pivot/Dockerfile) 自愈补装语句中 pin 的绑定版本号（注释已标注），并提交更新后的 `package-lock.json`，以免 `npm ci` 因失配报错。
- 服务端版本号提升至 `0.0.163`。

## [v0.0.162] - 2026-06-25

### 桌面客户端细节优化：任务栏标题显示与数据概览缩放适配修复

本版本针对桌面客户端进行了两项关键的用户体验修复与优化：解决了 Windows 系统任务栏只显示图标而无标题名字的问题，以及由于页面缩放计算机制导致的在客户端中数据概览图表显示不全、左右偏窄留白及文字重叠的问题。

#### 变更内容

- **修复 Windows 系统任务栏空白无标题问题**：
  - 更新了 [desktop/main.js](file:///e:/pivot/desktop/main.js) 的 `windowTitle` 逻辑，确保当客户端配置 `config.json` 中的 `windowTitle` 为空字符串或仅空格时，自动回退到默认标题 `"智枢 Pivot"`，并在 [desktop/config.js](file:///e:/pivot/desktop/config.js) 中将默认窗口标题更新为 `"智枢 Pivot"`。
  - 更新了项目的 `config.json` 与 `config.example.json` 文件，将默认的 `"windowTitle"` 值由原先的空字符串或 `"Pivot"` 修改为更直观的 `"智枢 Pivot"`。
  - 解决了由于原先 `windowTitle` 设置为空字符串导致 Windows 系统底部任务栏图标旁边完全没有文字、显得“光秃秃”的视觉体验问题。
- **修复数据概览页面显示不完整与文字重叠问题**：
  - 在 [client/chat/stats-monitor.js](file:///e:/pivot/client/chat/stats-monitor.js) 的 `loadOpsSummary` 与 `loadMonitorSummary` 中、以及 [client/chat/models.js](file:///e:/pivot/client/chat/models.js) 的 `loadModels` 与 [client/chat/users.js](file:///e:/pivot/client/chat/users.js) 的 `loadUsers` 的最后，在 DOM 异步渲染完成后显式调用 `window.scheduleSettingsWorkspaceScale()`，强制触发容器高度重算，彻底解决了图表被截断、下方无法滚动到底部的问题。
  - 在 [client/chat/stats.js](file:///e:/pivot/client/chat/stats.js) 的 `renderTrendChart` 函数中，将画布的宽度计算优先级调整为优先采用父容器的 unscaled 布局宽度 `parentElement.clientWidth`，完美避免了由于 CSS Scale 引起的“双重缩放”而导致图表左右侧严重缩水、大量留白的排版问题。
  - 将趋势图画布的 `height` 从原来的 `220` (运营概览) 和 `240` (统计报表) 统一提升到了 `260`，并在 [client/chat/stats.js](file:///e:/pivot/client/chat/stats.js) 中把图表底部的安全间距 `padBottom` 从 `34` 增大到 `42`，为底端的日期轴标签保留了 `34px` 的安全内部绘制空间，彻底解决了日期文本底部边缘被画布边界裁切的问题。
  - 更新 [client/chat/styles/admin/admin-layout.css](file:///e:/pivot/client/chat/styles/admin/admin-layout.css) 中的样式，将折线图 Canvas 的最小高度限制统一提升至 `260px`，使其物理高度与绘图分辨率高宽比一致，避免图像拉伸失真；同时为缩放容器 `.settings-scale-canvas` 的 CSS `transform` 样式应用了 3D GPU 硬件加速变换 `translateZ(0) scale(...)` 并添加 `backface-visibility: hidden` 声明，有效消除了 Chromium 内核在非整数倍浮点数缩放（scale）下可能导致的部分中文字符定位偏移而出现的字符重叠现象。

#### 验证

- 检查 `config.json` 与 `config.example.json` 已配置 `"windowTitle": "智枢 Pivot"`。
- 校验 [stats-monitor.js](file:///e:/pivot/client/chat/stats-monitor.js)、[models.js](file:///e:/pivot/client/chat/models.js)、[users.js](file:///e:/pivot/client/chat/users.js) 在异步数据渲染后均有主动调度缩放比例。
- 校验 [stats.js](file:///e:/pivot/client/chat/stats.js) 中 `renderTrendChart` 的宽度计算优先使用 `parentWidth`。
- 校验 [admin-layout.css](file:///e:/pivot/client/chat/styles/admin/admin-layout.css) 中 `.settings-scale-canvas` 已声明 3D 变换以启用 GPU 硬件加速。
- 运行 `npm run check` 且 ESLint 无任何语法警告，所有 JavaScript 检测通过。

#### 说明

- 服务端版本号提升至 `0.0.162`。

## [v0.0.161] - 2026-06-25

### Docker 生产环境优化：Canvas 依赖修复与客户端下载灵活化方案

本版本针对 Docker 生产环境部署进行了两项关键修复：解决了 PDF 解析所需的 Canvas 原生模块加载失败问题，并为客户端下载实现了灵活的外部挂载方案，保持镜像精简的同时支持动态更新客户端。

#### 变更内容

- **修复 Docker 环境 Canvas 依赖加载失败**：
  - 在 [Dockerfile](file:///e:/pivot/Dockerfile) 中新增 `@napi-rs/canvas` 所需的系统级图形库依赖：`libcairo2-dev`（Cairo 2D 图形库）、`libpango1.0-dev`（Pango 文本渲染引擎）、`libjpeg-dev`、`libgif-dev`、`librsvg2-dev`。
  - 解决了生产环境日志中 `"Cannot load @napi-rs/canvas"` 以及 `"Cannot polyfill DOMMatrix/ImageData/Path2D"` 警告，确保 `pdf-parse` 及其依赖 `pdfjs-dist` 的 Canvas 渲染能力正常工作，PDF 文档解析功能稳定运行。
- **客户端下载灵活化方案（推荐外部挂载）**：
  - 新增专用下载目录 [downloads/](file:///e:/pivot/downloads)，在 [server/index.js](file:///e:/pivot/server/index.js) 中配置 `/downloads` 静态文件服务路由，禁用缓存以保证文件更新即时生效。
  - 更新 [docker-compose.yml](file:///e:/pivot/docker-compose.yml)，新增 `./downloads:/app/downloads` 卷挂载配置（含详细注释说明），支持通过外部目录挂载客户端安装包，实现**热更新**（直接替换主机文件无需重启容器或重新构建镜像）。
  - 修改登录页 [client/chat/partials/pre-app-modals.html](file:///e:/pivot/client/chat/partials/pre-app-modals.html) 中的下载链接，从 `/Pivot-Setup.exe` 改为 `/downloads/Pivot-Setup.exe`，并为链接添加 `id="desktop-download-link"` 以便后续动态控制。
  - 优化 [`.dockerignore`](file:///e:/pivot/.dockerignore)：将 `*.exe` 改为精确排除 `client/*.exe`，保留 `downloads/` 目录不被排除（但默认为空），避免误将构建产物、开发工具等 `.exe` 文件打入镜像；同时补充排除 `*.msi`、`*.dmg`、`*.AppImage` 等其他平台安装包格式。
  - 更新 [`.gitignore`](file:///e:/pivot/.gitignore)，新增 `downloads/*.exe`、`downloads/*.msi`、`downloads/*.dmg` 等排除规则，防止大体积客户端安装包被提交到 Git 仓库。
  - 提供**三种部署方案**灵活选择：
    1. **外部挂载（生产环境推荐）**：通过 Docker volume 挂载主机 `downloads/` 目录，镜像保持精简（~500MB），支持热更新客户端、多版本管理，无需重新构建镜像。
    2. **打包进镜像（离线环境）**：将客户端复制到 `downloads/` 后构建镜像，适用于无法挂载或完全离线的场景，镜像体积增加约 150MB（~650MB）。
    3. **外部托管（公网部署）**：将客户端上传至 GitHub Releases、对象存储（OSS/COS/S3）或 CDN，修改前端链接指向外部地址，镜像最精简且更新客户端无需操作服务器。
- **完善部署文档与验证清单**：
  - 新增 [downloads/README.md](file:///e:/pivot/downloads/README.md)，详细说明三种方案的使用方法、多版本管理、验证步骤与常见问题排查。
  - 新增 [docs/Docker客户端下载配置.md](file:///e:/pivot/docs/Docker客户端下载配置.md)，涵盖挂载配置、docker-compose 示例、性能优化建议（Nginx 配置）与完整的故障排查指南。
  - 新增 [docs/Docker部署快速指南.md](file:///e:/pivot/docs/Docker部署快速指南.md)，提供一键复制的部署命令、目录结构示例、最佳实践与性能优化配置（含 Nginx 反向代理示例）。

#### 验证

- 检查 Dockerfile 已包含 `libcairo2-dev`、`libpango1.0-dev` 等 Canvas 依赖库。
- 校验 `docker-compose.yml` 已配置 `./downloads:/app/downloads` 挂载。
- 校验 `server/index.js` 已添加 `/downloads` 静态文件服务路由。
- 校验登录页下载链接已更新为 `/downloads/Pivot-Setup.exe`。
- 校验 `.dockerignore` 精确排除 `client/*.exe` 但不排除 `downloads/`。
- 校验 `.gitignore` 已排除 `downloads/` 目录下的安装包文件。
- 手动验证建议：
  1. 将客户端复制到 `downloads/` 目录，执行 `docker-compose up -d`，通过 `docker exec pivot ls -lh /app/downloads/` 确认文件已挂载。
  2. 访问 `http://localhost:3000/downloads/Pivot-Setup.exe` 验证下载链接返回 200 OK。
  3. 检查 `docker logs pivot` 无 Canvas 相关警告信息。
  4. 替换 `downloads/` 中的客户端文件，无需重启容器即可下载到新版本（热更新验证）。

#### 说明

- 服务端版本号提升至 `0.0.161`。
- Docker 镜像默认不包含客户端文件（保持精简），**推荐使用外部挂载方案**部署。
- 镜像体积对比：外部挂载约 500MB，打包客户端约 650MB。
- 客户端安装包（`Pivot-Setup.exe`）约 136MB，建议根据实际场景选择合适的部署方案。

## [v0.0.160] - 2026-06-25

### 桌面端菜单汉化、网页端下载闭环与 Docker 镜像精细化瘦身

本版本在客户端独立版本 `0.0.1` 基础上，对顶部菜单进行了全中文化重构，修复了关于弹窗换行符问题；并在网页登录端建立了客户端下载闭环，同时对 Docker 镜像排除规则进行了生产级别的深度瘦身。

#### 变更内容

- **桌面端原生菜单重构与深度汉化**：
  - 重新启用了桌面端顶部应用菜单栏，并对其进行了**全中文化翻译**（包括“文件、编辑、视图、窗口、帮助”及子命令如“撤销”、“剪切”、“重新加载”、“开发者工具”等）。
  - 新增“关于智枢”原生消息弹窗，并修复了 `\n` 换行符显示为字面量 `\\n` 导致无法换行的 Bug，使其排版折行更整齐。
- **网页登录端客户端下载闭环**：
  - 在网页登录认证弹窗（[pre-app-modals.html](file:///e:/pivot/client/chat/partials/pre-app-modals.html)）的注册链接下方，新增了包含 SVG 标志的“下载 Windows 客户端”下载链接。
  - 将打包好的 `Pivot Setup 0.0.1.exe` 重命名并部署在 Express 静态资源托管根目录 [client/Pivot-Setup.exe](file:///e:/pivot/client/Pivot-Setup.exe) 下，实现了用户通过网页一键下载客户端的完整闭环。
  - 修改了 [`.gitignore`](file:///e:/pivot/.gitignore)，排除 `client/*.exe` 以防止大体积安装包被提交到 Git 仓库。
- **Docker 镜像生产级瘦身与安全隔离**：
  - 修改了 [`.dockerignore`](file:///e:/pivot/.dockerignore)，增加了对 `desktop/`（桌面端核心源码）、`tests/`（测试集）以及桌面端编译专用脚本（`run_desktop.js`、`package_desktop.js`、`build_desktop_icon.js`）的排除项。
  - 使得 Docker 镜像打包时剥离了所有开发调试残留及客户端源码，保障了服务端镜像的安全与精简。

#### 验证

- 检查客户端顶部菜单完全显示为中文，点击“关于智枢”弹窗排版折行准确。
- 启动服务端并使用浏览器访问登录界面，下载链接渲染无误，点击可秒级拉取 `Pivot-Setup.exe`。
- 校验 `.gitignore` 排除规则已生效。
- 校验 `.dockerignore` 已经正确排除 `desktop/` 及其他开发调试杂质。

#### 说明

- 服务端版本号提升至 `0.0.160`。
- 客户端（Windows 桌面端）独立版本号维持为 `0.0.1`。

## [v0.0.159] - 2026-06-25


### Windows 桌面客户端优化与汉化、镜像构建优化

本版本针对 Windows 桌面客户端进行了全面的体验优化与问题修复，优化了异常状态下的语言展示并预设了生产环境远程连接配置；同时优化了 Docker 镜像排除规则，减小了镜像体积。

#### 变更内容

- **错误页面加载与中文化修复**：
  - 修复了 [desktop/error.html](file:///e:/pivot/desktop/error.html) 中中文乱码显示为问号（`???`）的问题，恢复为清晰、标准的中文网络错误提示，并支持“重新连接”按钮。
  - **修复加载失败闪退的严重 Bug**：将原本通过拼接 `file:///` 协议加载本地错误页的方式，重构为使用 Electron 官方推荐的 `mainWindow.loadFile` 方式，彻底解决了在 asar 归档包环境下由于 Chromium 协议安全沙箱限制导致加载 `error.html` 报 `ERR_FAILED` 并引起客户端异常闪退崩溃的问题。
- **底层报错与弹窗汉化**：
  - 汉化了 [desktop/main.js](file:///e:/pivot/desktop/main.js) 中客户端主进程运行异常时抛出的 Error 信息，并将应用启动失败时的 Windows 系统级弹出框标题从 `Pivot startup failed` 汉化为 `Pivot 启动失败`。
- **生产环境配置预设**：
  - 将项目根目录下的 [config.json](file:///e:/pivot/config.json) 运行模式预设为远程集中版（`remote`），默认连接地址指向正式远程中心服务器 `http://50.64.150.51:9006/`，并关闭了自动更新，确保客户端分发给用户后开箱即用。
- **Docker 镜像构建体积优化**：
  - 更新了 [`.dockerignore`](file:///e:/pivot/.dockerignore) 文件，将打包输出的临时文件夹 `dist-electron-remote/` 及 `dist/` 追加至忽略列表中，避免在本地打包后再构建 Docker 镜像导致体积虚高，精简了容器体积。

#### 验证

- `node --check` 检查通过：`desktop/main.js`、`desktop/config.js`。
- 手动验证：检查客户端配置文件指向无误；断开网络启动客户端时，加载错误的显示标题、内容与按钮均已显示为正确的简体中文，无乱码问号。
- 校验 `.dockerignore` 已经正确排除桌面打包的临时目录。

#### 说明

- 服务端版本号提升至 `0.0.159`。
- 客户端（Windows 桌面端）启用独立版本号体系，初始发布版本为 `0.0.1`。

## [v0.0.158] - 2026-06-25


### 知识库 RAG 全面升级：结构感知切片、混合检索与批量上传体验

本版本面向"法律法规 + 杂类文档"的混合知识库，对检索召回、切片和知识图谱做了结构性升级，并优化了文档上传体验：支持批量上传、上传前暂存确认、并发上传与放大后的上传弹窗。检索/切片改动对存量数据向后兼容，结构化切片与面包屑在文档重新索引后生效。

#### 变更内容

- **结构感知切片 + 面包屑（新模块 `server/services/rag-chunker.js`）**：
  - 按文档类型路由切片：法规类按「第X条」边界切并追踪「章/节」标题、Markdown 按标题层级、表格按行序列化、其它走通用滑窗；通用滑窗行为与历史实现保持一致。
  - 每个分块附带「面包屑」（文档名 › 章 › 节 › 条），并入向量化与全文检索文本，提升召回质量与引用定位；注入上下文的引用来源改为展示面包屑。
  - 新增数据库列 `knowledge_chunks.heading_path`（迁移幂等、旧数据为空时自动退化为旧行为）。
  - 法规类默认切片放大到约 1100 字，充分利用长上下文向量模型；切片大小按类型可配。
- **混合检索：RRF 融合 + MMR 去重**：
  - 稠密向量余弦与 FTS(BM25) 名次做 RRF（Reciprocal Rank Fusion）融合，让关键词/编号等精确信号真正参与排序；保留向量服务不可用时的关键词回退。
  - 新增软门控：稠密分达阈值或命中 FTS 前列即保留，兼顾语义相关与「第X条」式精确查询。
  - 新增 MMR 去重，平衡相关性与多样性，剔除法规模板化的近重复片段。
  - 默认 Top K 由 3 提升至 6；新增 RRF/MMR 与按类型切片大小的环境变量配置（`RAG_RRF_K`、`RAG_RRF_W_DENSE`、`RAG_RRF_W_FTS`、`RAG_MMR_LAMBDA`、`RAG_FTS_RANK_FLOOR`、`RAG_CHUNK_SIZE_LEGAL`、`RAG_CHUNK_SIZE_PROSE`）。
- **知识图谱领域自适应升级**：
  - 新增法规领域实体类型（legal_doc/clause/obligation/right/penalty/subject）与关系（applies_to/references/supersedes/prohibits/requires），按文档类型仅在法规文档上叠加，避免跨域误抽。
  - 改进实体归一与指代清洗（该公司/本办法/上述部门 → 公司/办法/部门），并修正关系两端与实体键的对齐。
  - 将汉字区间常量化（`String.fromCharCode` 构造），消除源码中散落的生僻字与转义不一致，并把文件内英文注释统一为中文。
- **知识库批量上传与上传弹窗优化（前端）**：
  - 支持一次选择/拖拽多个文件批量上传；以固定并发 3 上传，沿用后台索引队列的并发控制。
  - 新增前端校验：单次最多 20 个文件、累计总大小不超过 500MB（单文件大小仍由后端限制兜底）；数量/总量上限同时显示在上传区说明与底部摘要文案中，超限标红提示。
  - 上传弹窗放大，专题库与标签改为带标题的字段；文件选择/拖入后先进入「待上传列表」暂存，需点击「开始上传」才真正上传，支持逐项移除与清空。
  - 上传过程逐文件展示状态徽标与进度条（待上传 → 上传中 N% → 已加入索引 / 失败）：改用 XMLHttpRequest 上传以获取真实字节级进度，进度事件仅局部刷新对应行；上传中锁定操作并隐藏移除按钮。
  - 上传后按文件独立统计成功/失败：全部成功才关闭弹窗，存在失败则保留失败文件于列表供重试（失败行标红并提示原因），并刷新文档列表。

#### 验证

- `npm run check` 通过（语法 + 文案 + 资源校验，共 263 个 JS 文件；29 个 partial / 108 个样式引用）。
- 知识库测试套件 `node tests/security-rag.test.js` 全部通过（含新增的结构化切片、MMR 去重、法规图谱关系用例）。
- `eslint` 对改动文件无告警；`node --check` 通过。
- 手动验证建议：上传一份法规与一份杂类文档并重新索引后，用「第X条」精确查询与语义查询经召回测试验证混合检索与面包屑引用；批量上传多文件可见暂存列表、逐文件进度/状态徽标、并发上传与成功/失败汇总，失败文件保留可重试，上传区与摘要均显示数量/总量上限。

#### 说明

- 版本号提升至 `0.0.158`。
- 检索与切片升级对存量数据向后兼容；结构化切片与面包屑需对已上传文档重新索引后才生效，可在文档列表中批量重建索引。

## [v0.0.157] - 2026-06-24

### 公文写作交互优化：删除确认、重命名与选区“按指令改写”

针对公文写作工作台的多文档管理和选区改写体验，本版本统一了删除确认弹窗样式、修正了重命名入口文案、加宽了重命名弹窗，并新增“按指令改写”选区能力，让用户可以对选中片段下达自定义修改指令。

#### 变更内容

- **删除公文改用自定义确认弹窗**：
  - 删除带内容的公文时，由浏览器原生 `confirm` 改为系统统一的自定义确认弹窗（`window.showConfirm`），样式与系统其余确认框一致；环境不支持时仍降级回退到原生确认。
- **“改名”更名为“重命名”并加宽弹窗**：
  - 文档列表操作按钮可见文字由“改名”统一为“重命名”。
  - 共享输入弹窗 `showInputPrompt` 新增可选 `width` 参数，重命名公文时弹窗加宽至 520px，便于查看和编辑较长的公文标题；弹窗关闭时还原默认宽度，不影响其他复用入口。
- **选区新增“按指令改写”**：
  - 选中正文/原文片段后，浮动工具条新增“按指令改写”按钮，点击弹窗输入自定义修改指令（如“改得更简洁、去掉口语化表述”），大模型按指令改写该片段并以流式审改卡片返回，可替换、插入、转批注或拒绝。
  - 后端 `selection` 模式支持 `action=custom`，从 `instruction` 读取用户指令（限长 500 字、要求非空），保持“不编造单位、时间和数据”的公文写作约束。

#### 验证

- `node --check` 通过：`client/chat/app.js`、`client/chat/apps-workbench-editor.js`、`client/chat/apps-workbench-ai.js`、`server/services/official-writing.js`。
- 手动验证：删除确认为自定义弹窗；重命名按钮文案与弹窗宽度生效且不影响其他弹窗；选区“按指令改写”可按指令生成建议，空指令/取消不发起请求；原有润色/压缩/扩写/公文语气/逐句改写/批注回归正常。

#### 说明

- 版本号提升至 `0.0.157`。

## [v0.0.156] - 2026-06-24

### 数据分析工作台：智能分析回复排版与 SQL 稳定性优化

针对智能分析深度分析过程中“回复内容集中在左侧、右侧留白过大”和 SQL 工具偶发类型错误的问题，本版本优化了前端结果区排版、执行过程反馈和后端查询字段类型处理，让智能分析的回答更易读，工具调用失败也更可理解、更容易继续修正。

#### 变更内容

- **智能分析结果区全宽展示**：
  - 取消智能分析结果容器继承聊天消息气泡的最大宽度限制，深度分析回复现在会铺满可用区域，避免长段分析集中挤在左侧。
  - 调整智能分析 Markdown 渲染外层样式，保留既有排版能力，同时移除多余边框、背景和宽度约束。
- **执行过程反馈优化**：
  - 为智能分析工具调用步骤补充成功/失败状态，SQL 查询失败会以更醒目的错误态展示。
  - 压缩后端工具错误信息，只展示用户可理解的摘要，避免 DuckDB 将大量候选函数签名直接暴露到页面。
- **SQL 查询稳定性增强**：
  - 数据分析 SQL 查询环境会根据字段画像将成绩、金额、数量等数值指标物化为 DOUBLE，减少 `VARCHAR + VARCHAR`、`AVG(VARCHAR)` 等类型错误。
  - 新增字段角色判断，学生编号、ID、单号、账号等编号类字段按维度处理，不再被智能分析误当作可求和指标。
  - 智能分析上下文补充字段类型、可直接数值计算字段和 SQL 使用提示，引导模型对中文字段名加双引号，并在不确定类型时使用 `TRY_CAST`。

#### 验证

- `npm run lint` 通过。
- `npm run check` 通过。
- 字段角色小验证通过：`学生编号` 作为维度，`语文成绩`、`金额` 作为数值指标。

#### 说明

- 版本号提升至 `0.0.156`。

## [v0.0.155] - 2026-06-24

### 公文写作 AI 改为流式输出 + 第三方 API 流式说明

公文写作工作台的起草、润色、全文改写和选区处理由"整段返回后插入建议卡片"改为流式逐字生成，建议卡片边收边更新；同时澄清并补充第三方 OpenAI-compatible API 的流式调用说明。

#### 变更内容

- **公文写作文本类模式流式输出**：
  - 后端放开流式限制，`draft`（起草）、`polish`（润色）、`rewrite_section`（全文改写）、`selection`（选区处理）与既有的 `rewrite_stream`（逐句改写）均支持按 SSE 流式返回。
  - `review`（格式与表达审校）需在后端把整段输出解析为结构化 JSON 条目，必须拿到完整文本，保持非流式。
  - 可流式模式集合收口到 `server/services/official-writing.js` 的 `OFFICIAL_WRITING_STREAMABLE_MODES`，前后端口径单一来源。
- **建议卡片实时渲染**：
  - 流式时先在审改栏插入一张"AI 生成中…"占位卡片，逐字增量只更新该卡片的文本节点，避免每个增量整页重渲染导致的焦点丢失与抖动。
  - 生成中隐藏接受/替换等操作按钮，待流式结束再恢复，避免对未完成文本执行误操作。
  - 加载时清理上次会话遗留的流式占位卡片：丢弃无内容占位，其余清除 `streaming` 标记恢复为正常待处理建议。
- **第三方 API 流式说明**：第三方 OpenAI-compatible `/v1/chat/completions` 与 `/v1/completions` 本就支持流式，调用方在请求体携带 `stream: true` 即按 SSE 真流式返回（与标准 OpenAI 用法一致），README 补充对应说明。
- **数据分析深度分析打字机回放**：数据分析"智能分析"的标准模式本就走 SSE 流式逐字输出；深度分析（ReAct 工具调用，返回答案 + 执行过程 + 图表的结构化结果）改为在循环跑完后，用打字机/分块效果前端回放最终答案文本（约 120 帧、~2 秒、尊重 `prefers-reduced-motion`），回放结束再补齐图表与执行过程，不改后端。

#### 验证

- `npm run lint` 通过。
- `npm run check` 通过。

#### 说明

- 版本号提升至 `0.0.155`。

## [v0.0.154] - 2026-06-24

### 数据分析工作台：数据透视布局与智能分析命名优化

围绕数据分析工作台近期使用反馈，集中优化数据透视页面的操作布局、结果区信息密度和历史记录展示，并将“AI辅助”统一命名为更符合业务语义的“智能分析”。

#### 变更内容

- **数据透视页面布局恢复与优化**：
  - 将透视配置区重排为“主配置”和“展示设置”两行，主配置集中展示分析数据集、行维度、列维度、值字段和聚合方式，展示设置集中展示排序、占比显示、Top N 和空值名称。
  - 将“推荐配置 / 开始透视 / 导出 CSV”统一前置到配置区操作栏，导出入口不再沉到透视表底部，减少用户滚动寻找操作的成本。
  - 将分析口径、行维度、列维度和合计压缩为紧凑横向摘要条，把首屏空间更多留给透视表本身。
  - 将 Top 行项 / Top 列项改为轻量横向摘要，只展示前 5 个重点项，降低统计区域对纵向空间的占用。
- **上下两行控件对齐**：
  - 统一数据透视主配置行和展示设置行的 5 列等宽 Grid 规则，使上下两行下拉框与输入框的左边界、宽度和间距保持一致，提升页面秩序感。
- **历史记录类型本地化**：
  - 补齐历史记录中 `pivot` 类型的中文映射，透视结果现在显示为“透视”，不再暴露英文内部类型名。
  - 历史记录说明文案补充“数据透视”，让图表、透视、比对、导出和查询等记录范围更清晰。
- **智能分析命名优化**：
  - 将数据分析工作台中的“AI辅助”统一改名为“智能分析”，覆盖左侧 Tab、顶部标题和应用副标题。
  - 保持内部 `ai` 标识、事件绑定和接口路径不变，确保现有功能与历史兼容。

#### 验证

- `npm run lint` 通过。
- `npm run check` 通过。

#### 说明

- 版本号提升至 `0.0.154`。

## [v0.0.153] - 2026-06-24

### 数据查询布局优化与数据透视按钮功能修复

重构数据查询页面在可视化和 SQL 模式下的表单布局，移除硬编码行内样式并采用 CSS Grid 统一对齐，修复数据透视页面“开始透视”按钮遗漏的问题。

#### 变更内容

- **数据查询页面布局与样式优化**：
  - **精简控制区**：将“分析数据集”下拉框与“可视化查询/SQL查询”选项卡合并放置于单行 Flex 头部，省去冗余空白占位，大幅提高首屏纵向空间利用率。
  - **样式抽象与 CSS 类封装**：彻底清理了原有的行内 style 属性。在 `apps.css` 中为可视化和 SQL 查询编辑器新增 `.data-analysis-query-visual-box`、`.data-analysis-query-sql-box`、`.data-analysis-query-settings-grid` 等类，建立了一套精致紧凑的高级圆角卡片、表单阴影与布局标准。
  - **筛选行自适应网格对齐**：使用 Grid 代替原本的不稳定 Flex，动态生成的筛选条件行能根据容器宽度智能拉伸；对添加和删除按钮优化了微交互动画与高品质的 hover 视觉反馈。
  - **设置网格压缩**：排序、方式和限制行数由原本占地巨大的结构重构为紧凑的 3 列等宽网格。
  - **AI 辅助与历史记录布局一致性优化**：将 AI 辅助与历史记录面板的“分析数据集”下拉选择框重构为与数据查询一致的单行横向 Flex 布局；同时排查并解决了 AI 建议展示框（`.data-analysis-ai-result`）因承载 Markdown 全局类 `message-content` 导致的最大宽度限制（860px），增加 `max-width: none` 覆盖规则使其完美全宽展开，消除了右侧空白。
- **数据透视按钮修复**：
  - 修复了数据透视面板配置后因操作栏缺少按钮而无法运行的 bug，在工具栏补全了“开始透视”主按钮（`#data-analysis-run-pivot`），与后台服务对接完成。

## [v0.0.152] - 2026-06-24

### 应用中心与数据透视布局重构与AI生成体验增强

进一步优化公文写作AI生成字数策略，去除强制的输出上限，同时对数据分析下的“数据透视”和“AI辅助建议”排版进行全面重构以消除布局溢出，修复应用中心卡片上边沿的裁剪显示。

#### 变更内容

- **公文写作AI输出不设限**：
  - 取消对大模型输出字数的强制约束。当所用模型在后台未配置最大输出Token限制时，发送至上游大模型的请求将不再携带 `max_tokens` 参数。这允许模型根据自身长文本能力或不设限地完整生成，解决了长公文写作中途被截断的问题。
  - 在计算输入上下文的裁剪预算时，仍使用内置默认值作为安全预留，以保证首屏和上下文比例健康。
- **数据分析AI辅助回复Markdown渲染排版优化**：
  - 将 AI 建议回复从纯文本直插形式改为重用全局已有的 `renderMarkdown` 解析器，使之在渲染时能解析 Markdown 语法为精美的 HTML 页面。
  - 结果容器加上 `message-content` 样式类以重用 Markdown 样式排版，去除数据中的 `**` 和列表 `-` 等裸露字符，并清除了 `pre-wrap` 换行属性以避免排版折行冲突，显著美化了回显布局。
- **数据透视页面布局与流程优化**：
  - **控件重排**：将透视表的 10 个配置下拉框重构为 Grid 的 5列 2行自适应网格对齐排版。第一行整齐排列数据集、行维度、列维度、值字段、聚合等核心选项，第二行排列占比、排序等次要属性。
  - **Top行列小药丸化**：将原本占据高达 200px 空间的 Top行列纵向 5 行大列表，压缩为横向 Flex “小药丸”标签格式，垂直高度缩减了 70% 以上，极大地优化了首屏的可读面积。
  - **导出CSV按钮上移**：在配置网格下方新设 Toolbar 并在其内提供推荐和导出 CSV 按钮（在有数据时展示），将导出按钮从表格最底部上移到控制区，优化了数据导出操作路径。
- **应用中心卡片裁剪修复**：
  - 优化 `.apps-grid` 卡片展示容器的 padding，增加 4px 的内边距。解决了应用卡片在 hover/focus 向上移动 1px 及 box-shadow 阴影溢出时被父容器边界裁剪的问题。

#### 验证

- `npm run test` 244 个单元测试用例全部成功通过。

#### 说明

- 版本号提升至 `0.0.152`。

## [v0.0.151] - 2026-06-24

### 数据透视页面布局优化与路径兼容修复

围绕实际分析使用场景，进一步优化数据透视页面的布局与结果区交互：配置项改为更紧凑的多列排布，分析口径与统计摘要压缩为轻量信息块，导出按钮前移到结果区操作栏，减少首屏纵向占用与底部空白。

#### 变更内容

- **配置区更紧凑**：
  - 行维度、列维度、值字段、聚合、排序、Top N、占比显示、空值名称等控件按更适合扫描的多列方式排列。
  - 推荐配置与生成按钮保留在配置区，减少来回滚动。
- **结果区更实用**：
  - 分析口径、行维度、列维度、合计改为轻量摘要卡片。
  - Top 行项 / Top 列项 保留为辅助洞察。
  - 导出透视结果按钮移动到结果区底部操作栏，更符合常用操作路径。
- **兼容性修复**：
  - 修复前后端拆分后项目根路径计算问题，确保数据集导入后的 parquet 落盘与透视查询正常工作。
- **透视交互保留**：
  - 继续支持推荐配置、Top N、排序、占比显示、空值命名和 CSV 导出。

#### 验证

- `npm run lint` 通过。
- `npm run check` 通过。

#### 说明

- 版本号提升至 `0.0.151`。
## [v0.0.150] - 2026-06-24

### 公文写作：移除输出 Token 内置默认上限，长度交由模型决定

公文写作 AI 此前为各模式内置了输出 Token 默认值（起草 2600、逐句改写 2200、其余 2000），仅用于裁剪超长输入时预留输出空间，并未真正约束模型输出长度。为统一口径、避免维护两套隐含限制，现移除公文专属默认值，输出长度完全由模型配置（`max_tokens`）或全局上下文预算决定。

#### 变更内容

- **移除公文模式内置输出上限**：
  - `server/services/official-writing.js` 不再产出 `maxTokens` 字段，删除 draft=2600、rewrite_stream=2200 及默认 2000 的赋值。
  - 输出长度交由模型自身配置决定：模型配置了 `max_tokens` 时按其下发，未配置时不下发该字段、由上游模型自行决定。
- **输入预算兜底统一化**：
  - `server/routes/apps.js` 公文写作路由在模型未配置 `max_tokens` 时，预算预留量改为传 `null`，由全局上下文预算（`context-budget.js`）使用统一默认预留量裁剪输入，不再依赖公文专属默认值。

#### 验证

- 更新 `tests/security-chat/official-writing.js` 对应断言（`built.maxTokens` 改为 `undefined`）。

#### 说明

- 版本号提升至 `0.0.150`。

## [v0.0.149] - 2026-06-24

### 数据分析工作台：前后端大文件按功能安全拆分

围绕后续维护和升级成本，完成数据分析工作台前端入口与后端服务的模块化拆分。旧入口路径保持不变，现有应用中心动态加载、后端路由引用和维护任务引用均继续兼容；真实业务逻辑下沉到按职责命名的子模块，便于后续单点优化数据透视、查询、图表、比对、导入导出和 AI 辅助能力。

#### 变更内容

- **前端数据分析工作台拆分**：
  - `client/chat/apps-workbench-data-analysis.js` 收敛为轻量加载器，继续暴露 `window.showDataAnalysisApp`。
  - 新增 `client/chat/data-analysis/` 功能模块目录，按 `context`、`view`、`core`、`query`、`pivot`、`chart`、`compare-history`、`ai`、`database-import`、`events` 拆分状态、视图、事件和各业务面板逻辑。
- **后端数据分析服务拆分**：
  - `server/services/data-analysis.js` 收敛为兼容导出门面，继续向现有路由和维护任务提供原有函数名。
  - 新增 `server/services/data-analysis/` 服务模块目录，按 `shared`、`datasets`、`profile`、`chart-summary`、`compare`、`query-pivot`、`export`、`database-import` 拆分公共能力和业务逻辑。
  - 透视表内部 Map key 改用显式 `CELL_KEY_SEPARATOR` 常量，移除不可见 NUL 分隔字符风险。
- **兼容性与维护性**：
  - 保留旧前端脚本路径和旧后端服务路径，避免修改调用方契约。
  - 新增模块命名直接对应业务能力，便于后续针对数据透视、图表、查询等区域独立迭代。

#### 验证

- `npm run lint` 通过。
- `npm run check` 通过。

#### 说明

- 版本号提升至 `0.0.149`。

## [v0.0.148] - 2026-06-24

### 数据分析应用：可视化编辑查询、下拉框默认提示置空及多Sheet差异导出

在修复图表生成与稳定性优化的基础上，进一步改进数据分析应用的易用性。为降低非技术用户使用数据查询的门槛，新增了“可视化查询”功能，并优化了所有下拉框的初始状态与前端拦截校验，同时增强了数据比对的 Excel 多 Sheet 导出。

#### 变更内容

- **可视化查询编辑器**：
  - 数据查询 Tab 重构为支持“可视化查询”和“SQL 查询 (高级)”两档切换。
  - “可视化查询”允许用户以所见即所得的形式添加/删除多行筛选条件，支持常用操作符（等于、不等于、包含、不包含、大于、小于、为空等）及多条件 `AND/OR` 逻辑关系，支持字段排序和条数限制。
  - **SQL 自动翻译**：切换至高级 SQL 模式时，系统会自动将当前的过滤配置编译为标准的只读 SQL 并填入编辑器中，辅助普通用户学习并进行更复杂的查询。
  - 精准支持中文列名双引号包裹与单引号字符串转义防注入。
- **下拉框默认置空与防重置**：
  - 数据集与字段选择下拉框初始化默认为未选中任何项，显示“请选择数据集/字段”等占位提示文案，避免默认使用第一个数据集带来的误触。
  - 优化 `setSelectOptions` 渲染机制，移除重新渲染时的硬编码 `''` 置空。使用同一个数据集时自动保留用户的字段选择，切换数据集时自动将其重设为提示占位符。
- **前端拦截与安全校验**：
  - 在生成图表、开始比对、运行可视化查询、生成透视表、AI 辅助等操作入口加入非空校验。数据集或必选字段未选齐时，前端弹出 Toast 友好拦截，避免空值入参导致后端 DuckDB 引擎崩溃报错。
- **数据比对多 Sheet Excel 导出**：
  - 优化“导出结果”按钮布局，并重构后端 `/apps/data-analysis/compare/export` 导出服务。
  - 支持将比对差异结果输出为包含“相同项”、“仅在A表”、“仅在B表”、“差异项”四个 Sheet 的 Excel 表格文件，空数据项自动忽略。

#### 验证

- 检查语法与依赖加载，本地测试通过。

#### 说明

- 版本号提升至 `0.0.148`。

## [v0.0.147] - 2026-06-24

### 数据分析工作台体验优化与日志本地化

基于上个 Git 版本，集中修复数据分析图表生成的异常路径，并围绕数据总览、AI 辅助、图表配置和控制台日志做了一轮用户体验优化。新版重点让图表生成更稳、更易配置，数据集删除更符合应用内交互习惯，运行日志中文表达更统一。

#### 变更内容

- **图表生成稳定性修复**：修复未选择数值字段却使用求和、平均、最小值或最大值聚合时触发 `Cannot read properties of null (reading 'key')` 的 500 错误；现在会在后端返回明确的中文业务提示，前端也会在必要时自动切换到“计数”或引导选择数值字段。
- **图表参数与布局优化**：
  - 新增“显示前几位”“排序”“配色”参数，支持按数值或分类升降序生成图表，并可选择经典青绿、商务蓝绿、柔和多彩和暖色强调等配色方案。
  - 图表类型会驱动参数显示：饼图隐藏并清空分组字段，折线图/面积图默认更适合按分类升序，柱状图/饼图默认按数值降序。
  - 将图表生成参数合并为单行配置栏，宽屏下减少纵向占用，窄屏下自然换行。
  - “保存为图片”和“导出图表数据 (CSV)”按钮统一移动到图表结果区域右侧。
  - 覆盖图表生成页的全局图表宽度限制，使图表预览容器占满可用宽度，避免右侧大面积空白。
- **图表导出与元数据增强**：图表生成接口会接收并记录前 N、排序方式、排序方向和配色方案，历史记录中的图表元数据也同步保留这些设计参数。
- **数据总览删除确认优化**：数据集删除不再使用浏览器默认确认框，改为应用内统一确认弹窗，避免风格割裂，并明确提示删除后会清理相关分析记录和文件。
- **AI 辅助输入框统一**：AI 辅助页面的输入框字体、字号、行高、内边距和占位符颜色对齐全局表单体验，降低页面内视觉跳变。
- **控制台日志中文化**：启动、数据库连接、迁移补字段、维护任务、备份、Agent 恢复、RAG/长期记忆降级、软删除清理等运行日志的 `msg` 文案统一改为中文，减少中英文混杂带来的排查成本。
- **样式细节补齐**：补齐数据分析页面内表格、按钮、分页和图表操作区的局部样式，使新增操作与已有工作台视觉保持一致。

#### 验证

- `node scripts/check_syntax.js` 通过，全部 244 个 JS 文件语法校验正常。
- `npm test` 通过，包含文本完整性检查、语法检查、聊天资源检查、ESLint 和 244 项安全/回归测试。

#### 说明

- 版本号提升至 `0.0.147`。

## [v0.0.146] - 2026-06-23

### 数据分析应用：顶部信息优化、自定义导入弹窗与局部精准导出

精简和优化数据分析页面的界面排版与交互体验，解决公共工具栏冗余、容易产生误触的问题，并美化了数据库导入的交互弹窗，增加纯前端高精度防乱码 CSV 导出。

#### 变更内容

- **移除顶部公共冗余按钮**：移除了顶部工具栏上与具体分析功能无关且容易引发误触的「刷新、CSV、导出、删除」按钮，使各具体分析子 Tab 页面更加关注于当页的分析。
- **数据总览局部刷新与行内导出**：
  - 在「数据总览」顶部操作区挂载「上传 Excel / CSV」「从数据库导入」「刷新列表」按钮，供用户导入或手动同步最新数据集。
  - 在表格行内新增了「导出」操作。点击即可默认以 Excel (.xlsx) 格式流式下载整个原始数据集，避免了顶栏下拉框形式的累赘操作，让数据集管理在总览页面闭环。
- **高精度前端 CSV 导出引擎**：
  - 开发了通用表格导出组件 `exportTableToCsv`，智能解析并清理 HTML 表格内容为标准的 CSV。
  - **UTF-8 BOM 支持**：在导出的 CSV 首部增加 UTF-8 BOM (`\uFEFF`)，彻底解决 Windows 平台使用 Excel 打开中文 CSV 时产生乱码的问题，满足中文编码规范。
  - **图表数据解析**：实现了 `exportChartDataToCsv`，能智能解析 ECharts 的 `dataset.source` 以及传统的 `xAxis`/`series`/饼图数据，自动生成坐标与系列值对照的 CSV 并提供下载。
  - **比对差异结果导出**：实现了 `exportCompareToCsv`，直接对比 `state.compare` 中的左右侧差异，智能导出差异类型、主键、左右侧数值对比的 CSV。
- **各分析面板精准功能挂载**：
  - **数据查询**：查询结果表格下方挂载「导出查询结果 (CSV)」按钮。
  - **数据透视**：生成的透视汇总表下方挂载「导出透视结果 (CSV)」按钮。
  - **图表生成**：操作栏提供「保存为图片」与「导出图表数据 (CSV)」按钮。
  - **数据比对**：在 KPI 指标框下方挂载「导出比对差异结果 (CSV)」按钮。
- **导入弹窗体验优化**：移除了从数据库导入弹窗右上角的多余「取消」按钮，只保留右下角明确的“取消”与“开始导入”按钮，规避界面取消入口的重复设计，并使标题排版居左且对齐。

#### 验证
- `npm run check` 校验通过，JS 语法及 Chat 资产完整性正常。
- `npm run lint` 语法风格校验全量通过。
- `npm test` 单元测试及回归测试校验全量通过。

#### 说明
- 版本号提升至 `0.0.146`。

## [v0.0.145] - 2026-06-23

### 数据分析应用：多格式导出 + 数据库导入

在四项扩展（自定义 SQL / 透视表 / CSV 原生导入 / AI 工具调用）收官的基础上，补齐 **多格式导出与从已配置数据库导入**：此前仅有 CSV 导出，无 XLSX/Parquet/PNG，也无数据库来源。

#### 变更内容

**多格式导出 + 数据库导入（D）**
- **多格式导出**：`exportDataset(userId, id, format)` 支持 **CSV / XLSX / Parquet**——CSV 保留 UTF-8 BOM 流式写入；Parquet 走 DuckDB `COPY`；XLSX 分页读 Parquet 后成表写盘。旧 `exportDatasetCsv` 保留为薄封装，原 `/export.csv` 接口不变，新增 `GET /apps/data-analysis/datasets/:id/export?format=`。
- **从数据库导入**：新增 `importFromDatabase()`，复用现有数据库 MCP 工具链（`db.run_readonly_query`），**只读校验、表/字段白名单治理、敏感字段脱敏、SSRF 守卫全部沿用**，本身不直接连库；行数受 `MAX_DB_IMPORT_ROWS`（默认 5 万）保护。新增 `createDatasetFromRows()` 把查询结果归一为内部 `c_N` 列落 Parquet 并生成画像。新增 `POST /apps/data-analysis/import-database`。
- **图表 PNG 下载**：图表面板新增「下载 PNG」，优先用 ECharts 实例 `getDataURL`，回退到 2D canvas `toDataURL`。
- **导入/导出前端**：工具栏新增导出格式下拉（CSV/Excel/Parquet）与「从数据库导入」入口（列出数据库型 MCP 连接，选连接并填 SQL/表名后导入）。

#### 验证
- `npm run check`
- `npm run lint`
- `node tests/security.test.js`

#### 说明
- 版本号提升至 `0.0.145`。图表/比对/查询/透视/导出均基于内部 `c_N` 键，保持兼容；数据库导入仅作为非文件来源复用既有安全治理，不新增对外连库路径。

## [v0.0.144] - 2026-06-23

### 数据分析应用：AI 工具调用 / 深度分析（四项扩展之四 · 收官）

四项扩展升级的收官阶段：让数据分析 AI 从"只读摘要盲答"升级为可调用工具、基于真实数据作答。AI 能自行查询数据集、生成图表，再据此给出结论。采用与项目智能体一致的 JSON-planner ReAct 模式，兼容不支持原生 function-calling 的本地模型。

#### 变更内容
- **AI 工具调用循环**：扩展 `/apps/data-analysis/ai` 支持 `mode: 'agent'`，按 JSON-planner ReAct 多轮执行（上限 3 步）。模型每步输出结构化 JSON，后端解析并执行工具、把观测回灌，直至给出最终回答。
- **两个工具**：`run_sql`（复用阶段一的只读查询沙箱，对当前数据集执行 SQL）、`make_chart`（复用图表生成，产出图表并沉淀到历史）。
- **稳健兜底**：模型输出非合法 JSON 时直接当作普通回答返回；达到步数上限仍未收尾时要求模型据已有观测给出最终回答。复用应用中心 AI 的模型解析、配额、上下文预算与并发/用量统计。
- **深度分析前端**：AI 辅助 Tab 新增「深度分析（可查询数据 / 生成图表）」开关，开启后渲染最终答案 + 可折叠「执行过程」（SQL/图表步骤）+ 内联渲染的图表；关闭时保持原有 SSE 流式问答。

#### 验证
- `npm run check`
- `npm run lint`
- `node tests/security.test.js`（244/244 通过）

#### 说明
- 版本号提升至 `0.0.144`，数据分析应用四项扩展升级（自定义 SQL 查询 / 透视表 / CSV 原生导入与统一 SQL 画像 / AI 工具调用）全部完成。

## [v0.0.143] - 2026-06-23

### 数据分析应用：CSV 原生导入与统一 SQL 画像（四项扩展之三）

四项扩展升级的第三阶段：把字段画像统一改为 DuckDB SQL 计算，CSV 导入改走 DuckDB 原生快路径，从根上消除 CSV 与 Excel 两条路径的画像分叉，并大幅降低 CSV 导入内存。

#### 变更内容
- **统一 SQL 画像**：新增 `profileViaSql`，直接在 DuckDB 内对 Parquet 计算字段画像（类型/填充率/去重数/样本/Top 值/数值统计），CSV 与 Excel 两条导入路径共用同一套，输出形状与历史 `buildProfile` 完全一致（前端无感）。数值清洗（去千分位/货币符号/百分号/空白）与日期形态识别在 SQL 内完成，与旧 JS 逻辑对齐（百分比 /100、括号负数为近似）。
- **CSV 原生导入**：新增 `importCsvToParquet`，用 `read_csv_auto` 直接把整表 `COPY` 成 Parquet（列名归一为内部 `c_N`），整表数据不再经过 JS 内存；表头取首行、数据 `OFFSET 1` 写入，并保留 `MAX_UPLOAD_ROWS`/`MAX_UPLOAD_COLUMNS` 上限。Excel/XLS 仍走 JS 解析（无法绕过），但随后同样用 `profileViaSql` 出画像。
- **导入并发限流**：导入的 DuckDB 段纳入 `withAnalysisSlot`，与图表/查询/导出共用并发闸。
- **退役死代码**：移除不再使用的 JS 画像实现 `buildProfile`/`buildNumericStats`/`inferKind`/`toFiniteNumber`，删除上一轮遗留的 TODO 标记。

#### 验证
- `npm run check`
- `npm run lint`
- `node tests/security.test.js`（244/244 通过）
- DuckDB 端到端验证：CSV 表头/行数/货币与千分位清洗（¥800→800、"1,200"→1200）、中位数与日期形态识别均与旧口径一致。

#### 说明
- 版本号提升至 `0.0.143`；图表/比对/查询/透视均基于内部 `c_N` 键，导入重写后保持兼容。后续阶段（AI 工具调用）将收尾。

## [v0.0.142] - 2026-06-23

### 数据分析应用：透视表（四项扩展之二）

四项扩展升级的第二阶段：在「数据分析」中新增透视表能力，按行维度 + 可选列维度交叉聚合一个数值或计数，底层复用 DuckDB `GROUP BY`，结果在服务端整形为带行/列合计的矩阵。

#### 变更内容
- **透视表服务**：新增 `runPivot`，支持行维度（必填）、列维度（可选）、值字段（可选，缺省计数）与求和/计数/平均/最小/最大聚合；行/列各设上限（默认 200 行 / 50 列），超限按聚合总量排序截断并标注 `truncated`；经 `withAnalysisSlot` 并发限流，结果沉淀到「历史」Tab。
- **透视接口**：新增 `POST /apps/data-analysis/datasets/:id/pivot`，经鉴权并按 `user_id` 校验数据集归属。
- **透视前端**：新增「透视」Tab，行维/列维/值/聚合四个下拉选择，生成带首行/首列冻结与行合计、列合计、总计的交叉表。

#### 验证
- `npm run check`
- `npm run lint`
- `node tests/security.test.js`（244/244 通过）

#### 说明
- 版本号提升至 `0.0.142`；后续阶段（CSV 原生重写、AI 工具调用）将逐项交付。

## [v0.0.141] - 2026-06-23

### 数据分析应用：自定义 SQL 查询（四项扩展之一）

数据分析应用四项扩展升级的第一阶段：在「数据分析」中开放自定义 SQL 查询，直接复用现有的 DuckDB + Parquet 引擎，让用户对当前数据集书写只读查询。安全采用多层纵深防护。

#### 变更内容
- **自定义 SQL 查询服务**：新增 `runUserQuery`，针对当前数据集执行用户书写的只读 `SELECT`/`WITH` 查询；表名固定为 `data`，列名使用人类可读的字段名。
- **多层安全防护**：①语法与黑名单校验（必须以 SELECT/WITH 开头、拒绝多语句、拦截 ATTACH/COPY/CREATE/DROP/read_parquet 等关键字）；②把数据集物化为内存表后 `SET enable_external_access=false`，使用户 SQL 无法再读取数据集以外的任何文件；③外层包裹单条子查询并强制 `LIMIT` 兜底。查询经 `withAnalysisSlot` 并发限流。
- **查询接口**：新增 `POST /apps/data-analysis/datasets/:id/query`，经鉴权并按 `user_id` 校验数据集归属。
- **查询工作台前端**：新增「查询」Tab，提供可点击插入的字段提示、SQL 编辑区与结果表渲染，失败内联报错并提示；查询记录沉淀到「历史」Tab。

#### 验证
- `npm run check`
- `npm run lint`
- `node tests/security.test.js`（244/244 通过）
- DuckDB 沙箱隔离验证：物化表查询正常，禁用外部访问后任意文件读取被拒。

#### 说明
- 版本号提升至 `0.0.141`；后续阶段（透视表、CSV 原生重写、AI 工具调用）将逐项交付。

## [v0.0.140] - 2026-06-23

### 数据分析应用 P0–P2 修复优化

本次对应用中心「数据分析」应用做了一轮全面修复优化，覆盖稳定性、资源回收、性能与体验四个层面：消除死数据与磁盘泄漏、补齐前端错误处理、为重型查询加并发限流、把 CSV 导出改为常量内存、增强字段画像统计、收紧类型推断，并让 AI 辅助分析支持流式输出。不引入会改变现有画像/类型推断语义的高风险重写（CSV 导入的 DuckDB 原生快路径另起一轮）。

#### 变更内容（P0 稳定性 / 资源回收）
- **历史记录消费落地**：`analysis_artifacts` 此前只写不读、持续累积。新增 `listDatasetArtifacts` 服务与 `GET /apps/data-analysis/datasets/:id/artifacts` 接口，前端新增第 5 个「历史」Tab 展示图表/比对/导出记录，图表类记录可点击重新查看。
- **物理文件回收**：`softDeleteDataset` 删除数据集时同步回收其磁盘目录（源文件 + Parquet）、关联 artifacts 行与导出文件，避免软删后磁盘只增不减；新增 `cleanupAnalysisWorkspace` 按 mtime 周期清理过期导出（默认 7 天）与临时文件（默认 1 天），并接入每日维护任务。
- **前端错误处理收口**：图表生成、数据比对、AI 辅助、数据集上传统一加 `guardButton` 忙碌守卫与 try/catch，失败弹 toast 并复位 UI，杜绝“无反馈卡死”和 AI 结果区停在“正在分析…”。

#### 变更内容（P1 性能 / 并发）
- **CSV 导出常量内存**：由“全表读入 JS 再拼字符串”改为 DuckDB `COPY (...) TO ... (FORMAT CSV, HEADER)` 落临时文件、再流式补 UTF-8 BOM 写入最终文件，保留 Excel 中文兼容的同时大幅降低大表导出内存峰值。
- **重型查询并发限流**：新增独立 `analysisSemaphore`（与 AI 调用信号量隔离）与 `withAnalysisSlot` 助手，包裹图表、比对、导出、详情预览等 DuckDB 操作，过载返回 503 友好提示，避免并发大查询打满 CPU/内存。
- **导入内存优化**：画像与预览落地后及时释放整表行数组引用以降低序列化期峰值；CSV 的 DuckDB 原生快路径留作下一轮重写（已埋 TODO，本轮不改动画像/预览/类型推断语义）。

#### 变更内容（P2 体验增强）
- **字段画像统计增强**：数值字段在 min/max/avg/sum 基础上补中位数、P25、P75 与总体标准差，前端画像行同步展示。
- **类型推断收紧**：`inferKind` 去掉宽松的 `Date.parse` 兜底，仅识别明确日期形态，并确保数值判定优先于日期，避免纯数字/短文本被误判为日期。
- **AI 辅助流式输出**：`/apps/data-analysis/ai` 对齐公文写作的 SSE 实现支持流式，前端逐字输出，环境不支持时自动回退非流式。

#### 验证
- `npm run check`
- `npm run lint`
- `node tests/security.test.js`（244/244 通过）

#### 说明
- 新增可选环境变量：`DATA_ANALYSIS_MAX_CONCURRENT`、`DATA_ANALYSIS_MAX_QUEUE`、`DATA_ANALYSIS_QUEUE_TIMEOUT_MS`、`DATA_ANALYSIS_EXPORT_RETENTION_MS`、`DATA_ANALYSIS_TMP_RETENTION_MS`，均有默认值，无需配置即可运行。
- 版本号已提升至 `0.0.140`，`package.json`、`package-lock.json` 与 README 同步刷新。

## [v0.0.139] - 2026-06-23

### 数据分析工作台、思考输出收口与全局参数页优化

本次把数据分析能力、思考过程治理和全局参数页排版一起整理到位，便于统一发布。

#### 变更内容
- **数据分析工作台上线**：新增数据集上传、概览、字段画像、图表、对比分析与 CSV 导出流程，后端路由、分析服务、并发控制和维护任务同步接入。
- **思考输出收口**：OpenAI 兼容返回中的 `reasoning_content`、`<think>` 等思考片段统一过滤，避免把思考过程暴露给前端或 Continue 补全链路。
- **全局参数页排版优化**：长英文 runtime key 改为可完整换行显示，右上角“最近保存”状态支持完整展示，输入框恢复到同一水平线。
- **回归测试同步**：补充和调整相关安全回归用例，覆盖思考输出过滤与全局参数配置路径。

#### 说明
- 版本号已提升至 `0.0.139`，README 与包版本会同步刷新。

## [v0.0.138] - 2026-06-22

### Windows 桌面首发版与企业远程分发能力

本版本将 Pivot 从 Web 服务扩展为可直接安装的 Windows 桌面应用，支持“单安装包 + 外置 config.json”的企业分发模式。当前首发包默认使用本地开发服务地址 `http://127.0.0.1:3000/` 作为远程测试目标；部署到生产环境时，只需要将 `config.json` 中的 `remoteUrl` 和 `autoUpdate.url` 替换为生产地址后重新分发配置或重新打包。

#### 变更内容
- **Windows 桌面应用打包**：新增 Electron 主进程、预加载脚本、错误页、图标生成脚本和打包脚本，支持生成 NSIS 安装器和 `win-unpacked` 解压版 app。
- **远程/本地配置分流**：新增 `desktop/config.js`，按命令行、环境变量、`Pivot.exe` 同目录、`resources/config.json` 的优先级读取配置，并校验 `remoteUrl` 与更新源 URL。
- **本地开发远程测试配置**：新增 `config.json`，默认指向 `http://127.0.0.1:3000/`，用于首发包在本地开发服务上验证桌面壳、登录态、远程加载和错误页重试流程。
- **企业环境隔离**：窗口标题展示环境名，Electron `partition` 按环境隔离 Cookie 与缓存，避免测试、生产登录态串用。
- **远程加载失败体验**：新增 `desktop/error.html`，远程服务不可达时显示环境名、目标地址和重试按钮，避免暴露原始 Chromium 错误页。
- **自动升级能力**：引入 `electron-updater`，新增 `desktop/updater.js`，支持 generic 更新源、启动检查、自动下载、下载完成后提示重启安装，并通过 `autoUpdate` 配置开关控制。
- **企业分发文档**：新增 `docs/远程模式部署说明.md`，记录配置优先级、环境切换、自动升级发布文件和 IT 分发注意事项。
- **打包稳定性修复**：生成合法 Windows ICO 图标，规避无效 favicon 导致的打包失败；跳过 Electron Builder 原生依赖重建，避免本地 Node ABI 与打包运行时冲突影响远程模式首发包。

#### 发布与升级说明
- 首发测试配置：`remoteUrl=http://127.0.0.1:3000/`，`autoUpdate.enabled=false`。
- 生产部署时：将 `config.json` 改为生产服务地址，并按需启用 `autoUpdate.enabled=true`。
- 自动升级发布目录需要上传 `Pivot Setup <version>.exe`、`Pivot Setup <version>.exe.blockmap` 和 `latest.yml`。

#### 验证
- `npm run check`
- `npm run lint`
- `npm run dist:win`

## [v0.0.137] - 2026-06-22

### 数据分析应用上线

本版本新增应用中心里的数据分析工作台，采用 SQLite 元数据 + Parquet 数据层 + DuckDB 查询引擎的组合，补齐了数据上传、概览、字段画像、图表生成、对比分析、CSV 导出和 AI 辅助分析等能力，同时把持久化与查询职责分开，减少多进程锁冲突和临时文件干扰。

#### 变更内容
- **应用中心新增数据分析工作台**：`client/chat/apps-workbench-data-analysis.js` 提供数据集上传、列表、详情、摘要、图表、对比和导出入口，`client/chat/apps-workbench-core.js`、`client/chat/apps-workbench-rag.js`、`client/chat/stats-monitor-utils.js`、`client/chat/styles/workspaces/apps.css` 与 `server/routes/apps.js` 完成注册与展示。
- **存储与查询分层**：数据集元信息与 artifact 继续存 SQLite，原始数据转换为 Parquet 持久层，分析查询由 DuckDB 执行；导入过程改为直接写入 DuckDB appender 并生成 Parquet，减少中间 CSV 和 `.duckdb` 临时产物。
- **分析能力补齐**：支持 CSV/XLS/XLSX 上传、字段画像、统计摘要、图表生成、双数据集对比、CSV 导出和 AI 上下文构建，对比逻辑按 join key 聚合并暴露重复键，避免匹配数被放大。
- **版本与文档同步**：版本号升级到 `0.0.137`，并同步更新 `package.json`、`package-lock.json`、`README.md`、`使用手册.md` 和 `CHANGELOG.md`。

## [v0.0.136] - 2026-06-21

### 关闭思考时 Token 速度统计修正

本版本解决了思考模型在关闭思考过程时，大模型计算并展示的 token 速率不准确的问题。由于关闭思考过程时思考文字被过滤隐藏，速率计算应当基于请求总耗时进行，以与用户的体感展示保持一致。

#### 变更内容
- **后端速率统计修正**：`/server/routes/chat.js` 中 `buildAssistantSpeedStats` 新增 `disableChatThinking` 参数，在该模式下将计算起点 `answerStartAt` 对齐为请求启动时间 `requestStartedAt`，从而将隐藏的思考耗时计入分母。
- **前端流式判定对齐**：`/client/chat/engine.js` 判定首个答案 token 并且处于关闭思考模式时，将首字节到达时间 `firstTokenTime` 设为起跑时间 `startTime`，确保流式输出过程中的 t/s 速率不出现偏高偏差。
- **单测用例与版本同步**：新增单元测试 `关闭思考过程后聊天回答速率按总耗时计算`；版本号升级至 `0.0.136` 并同步更新至 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。

## [v0.0.135] - 2026-06-21

### PWA 更新提示样式规范化与主题变量收口

本版本把 PWA 更新提示从内联硬编码样式改为 class + 全局 CSS 变量驱动，并保留变量 fallback，确保在非完整 chat 样式环境里也能稳定显示，同时继续保持刷新更新的原有行为。

#### 变更内容
- **更新提示组件化**：`client/pwa-manager.js` 中的更新提示改用 `pwa-update-notice`、`pwa-update-notice__text`、`pwa-update-notice__action` 结构，移除内联布局与颜色声明。
- **主题变量收口**：`client/common/styles/theme.css` 新增 `--pwa-update-*` 变量，并为浅色/暗色主题提供默认值与回退值，样式跟随全局主题统一。
- **可访问性与兼容性**：提示容器增加 `role="status"` 和 `aria-live="polite"`，同时保留 CSS/变量 fallback，避免脚本在非完整 chat 样式环境中显示异常。
- **版本号启用**：应用版本升级至 `v0.0.135`，同步更新 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。

#### 说明
- 本次为 PWA 提示样式收口与版本升级，刷新更新逻辑保持不变。

# 更新日志 (CHANGELOG)

## [v0.0.134] - 2026-06-21
### 全页面原生流式自适应适配与打印乱码修复

本版本在分析和对比了物理缩放（物理 iframe 壳 + scale 缩放）与原生流式响应布局的方案后，决定完全基于系统已有的 CSS Flexbox 响应式设计进行自适应分辨率适配，以实现全屏、无两侧留白、无滚动条、且无拉伸变形的完美全屏显示效果。同时，本版本彻底修复了打印预览页面中文字符的乱码问题。

#### 变更内容
- **放弃物理缩放壳方案**：全面清除并删除临时引入的物理缩放外壳 `server/scale-shell.js`。
- **全页面原生流式全屏显示**：主对话页面（`/`、`/chat/chat.html`）、PWA 重置页面（`/pwa-reset`）及用户使用手册页面（`/manual`）全部回归原生的响应式 HTML 渲染模式，页面宽度和高度完美适配浏览器视口，两侧完全不留白，不产生多余滚动条。
- **修复打印预览中文字符乱码**：修复了 `server/routes/sessions.js` 会话打印路由在新版预览页中的中文乱码字符，替换为正确的中文：“会话记录”、“会话导出”以及“正在加载打印预览...”。
- **手册与PWA链接逻辑清理**：清理并精简了手册页与 PWA 清理页的“返回系统”跳转逻辑，使其更内聚和稳定。

#### 验证
- `npm run check` 校验通过，JS 语法及 Chat 资产完整性正常。
- `npm test` 单元测试及回归测试校验全量通过（共 242 项测试用例，100% 通过）。

## [v0.0.133] - 2026-06-21
### 全局参数页无滚动与模型配置中文化收尾

本版把最近一轮管理端配置体验做了完整收口：全局参数页在保留英文参数备注的前提下继续压缩布局，宽屏下改为三列展示，避免设置页额外出现垂直滚动条；模型配置弹窗、计价币种、附件上传和全局运行时参数说明也同步统一为更清晰的中文表达。

#### 变更内容
- **全局参数页不再出现垂直滚动条**：全局参数页在宽屏下改为三列分组布局，并进一步收紧页头、分组卡片、说明文字、字段标签、英文参数备注和输入框高度，减少整页纵向占用。
- **英文参数名保留为辅助备注**：全局参数页字段主名称继续使用中文，同时在字段下方保留 `model_context_window_tokens`、`sampling_top_p`、`max_attachments_per_message` 等英文键名，便于和配置、接口及后端字段对应。
- **全局参数说明文案修正**：页面说明改为“无需修改配置文件或重启服务”，避免继续指向具体 `.env` 文件，表达更贴近运行时配置的实际使用方式。
- **模型配置弹窗中文化**：模型配置弹窗中的上下文、采样、计费、思考和多模态等参数名称统一改为中文；不能完全替代英文含义的参数通过括号保留英文备注，降低误配风险。
- **采样参数语义补齐**：补充并统一 `top_p` 等采样参数的中文含义，把“默认采样概率上限”等名称用于界面，帮助区分它与温度、重复惩罚、频率惩罚的作用。
- **计价币种改为中文值**：模型计价币种选项和默认值改为中文展示与存储，不再保留 `CNY` 这类尚未投入使用的历史兼容值映射。
- **附件上传体验继续收口**：附件提示区改为轻量摘要，删除按钮和附件卡片继续瘦身；附件加入输入框后先作为本地草稿保留，用户确认发送时再统一上传到系统。
- **附件数量上限可配置**：全局运行时参数新增并展示“附件数量上限”，用于限制单条消息可携带的附件总数，和图片数量上限分别管理。

#### 验证
- `npm run check`

## [v0.0.132] - 2026-06-21
### 全局参数页与附件上传链路优化

本版围绕全局参数页、附件预览和发送链路做了收口：全局参数页更紧凑，新增单次附件数量上限，附件从选中即上传改为发送前批量上传，并把删除按钮、提示文案和卡片尺寸一并收小，减少页面占用。

#### 变更内容
- **全局参数页布局收紧**：全局参数页的卡片、输入框和说明文字重新压缩，补齐过长文案的显示空间，页面不再额外出现垂直滚动条。
- **新增单次附件数量上限**：在运行时参数里增加 `maxAttachmentsPerMessage`，并同步到全局参数页与管理端，保证前端可以配置和查看单次可选附件数量上限。
- **附件预览区压缩**：移除占位过大的附件提示文案，改为更轻量的摘要展示；附件卡片、文件名和删除按钮都做了瘦身，批量附件时占用更少。
- **附件改为发送时上传**：上传到输入框后先保留为本地草稿，只有用户确认并发送时才批量上传到系统；发送完成后清理输入与草稿状态，避免误传和空占位。
- **纯附件消息先建会话**：如果消息只有附件没有正文，发送前先创建会话再上传和提交，避免没有 session 时的发送失败。
- **文档与版本同步**：README 顶部摘要与版本标识同步更新到新版本号，仓库版本号随 changelog 首条自动生效。

#### 验证
- `npm run check`

## [v0.0.131] - 2026-06-21
### 全局参数页面中文化与说明增强

本版继续优化全局参数页面与运行时设置弹窗：把配置名称统一改成中文，补充分组说明和字段提示，并收紧页面布局与卡片样式，便于后续继续扩充更多常用参数。

#### 变更内容
- **配置名称中文化**：全局参数页、运行时设置弹窗与运行时配置定义中的字段名称统一改为中文表达，减少英文术语混杂。
- **分组说明补齐**：全局参数页面与运行时设置弹窗为每个区域增加简短说明，说明当前分组控制的能力范围和调参用途。
- **字段提示增强**：保存后的运行时配置会带上更清晰的中文悬停提示，便于快速理解每个参数的作用。
- **布局与样式收紧**：优化全局参数页面卡片、间距、标题和状态区样式，让配置区更像统一的产品级设置页。

#### 验证
- `node tests/security-chat.test.js`
- `npm run check:text`

## [v0.0.130] - 2026-06-21
### 聊天多图输入与全局参数收口

本版继续把聊天输入、全局参数、长期记忆和提示词库的产品体验收口：聊天输入支持一次选择多张图片，视觉注入按运行时图片上限配置处理；长期记忆页进一步压缩统计与治理区域；全局参数页把常用运行时采样默认搬到可配置项；“角色与规范库”统一改名为“提示词库”；Continue 离线代码补全兼容仍保留 `/v1/completions` 入口。

#### 变更内容
- **聊天输入支持多图上传**：移除聊天附件上传阶段的“图片每次仅 1 张”前端拦截，保留总附件数上限；视觉注入链路按运行时 `max_images_per_message` 配置允许同一条消息携带多张图片。
- **全局采样默认可配置**：全局参数页与运行时设置弹窗新增 `temperature`、`top_p`、`presence_penalty`、`frequency_penalty` 默认值，聊天接口与 OpenAI 兼容接口默认读取这组参数。
- **长期记忆页面继续压缩布局**：统计区合并为更紧凑的一行，搜索、状态、类型与批量操作区域继续对齐，减少空白占用。
- **全局参数收口**：把常用变更参数集中到全局参数页，并将保存状态移动到顶部刷新按钮左侧，便于确认当前配置。
- **提示词库命名统一**：原“角色与规范库”统一改名为“提示词库”，相关页面、按钮与弹窗同步收口。
- **Continue 代码补全兼容保留**：继续保留 `/v1/completions` 兼容入口，避免离线 Continue 把地址拼成错误的 `/v1/chat/completions/completions`。

#### 验证
- `node tests/security-chat.test.js`
- `npm run check`
- `npm run check:text`

## [v0.0.129] - 2026-06-18
### Continue 代码补全兼容与 OpenAI Completions 入口补齐

本版为离线生产环境下的 Continue 代码补全接入补齐标准 `/v1/completions` 兼容入口，保留现有 `/v1/chat/completions`、上下文预算、审计、配额与安全校验逻辑，并把 chat completion 响应转成传统 text completion 格式，避免 Continue 把地址继续拼成 `/v1/chat/completions/completions`。

#### 变更内容
- **补齐 OpenAI Completions 路由**：新增 `POST /v1/completions`，复用现有聊天补全处理链路与模型转发，只在响应层做 `text_completion` 兼容转换。
- **Continue 代码补全兼容**：兼容 `prompt`、`input`、`prefix`、`suffix` 风格请求；非流式返回 `choices[].text`，流式返回 completion SSE 增量，避免 Continue 因缺少 `/v1/completions` 报 404。
- **测试回归补齐**：新增非流式与流式两条安全回归，覆盖 `/v1/completions` 请求、上游 chat completion 到 text completion 的转换，以及 `[DONE]` 结束帧。

#### 验证
- `node -c server/routes/openai.js`
- `node -c tests/security-helpers.js`
- `node -c tests/security-chat/ops-models.js`
- `node tests/security-chat.test.js`

## [v0.0.128] - 2026-06-18
### 设置页命名与长期记忆管理界面优化

本版围绕系统设置里的长期记忆、全局参数和提示词库做界面收口：长期记忆页减少统计与治理控件占用空间，全局参数页把保存状态移动到顶部操作区，同时将“角色与规范库”统一更名为更短、更贴近实际用途的“提示词库”。

#### 变更内容
- **长期记忆统计压缩**：将长期记忆页原本两排统计卡合并为一行紧凑指标栏，保留启用状态、记忆分类、质量状态和任务状态等信息，同时减少对主表格区域的占用。
- **长期记忆治理栏合并**：搜索框、状态筛选、类型筛选、批量操作、抽取任务状态、重试失败和清理旧任务统一合并到同一条控制栏；输入框、下拉框和按钮统一高度并按中线对齐。
- **全局参数状态上移**：全局参数页底部的保存时间/状态提示移动到顶部“刷新”按钮左侧，减少底部空行并让最近保存状态更容易看到。
- **提示词库命名统一**：系统设置侧栏、页面标题、编辑弹窗、套用弹窗、聊天按钮 tooltip、空状态、确认弹窗、Toast 和监控路由标签统一从“角色与规范库/角色与规范”收口为“提示词库/提示词”。
- **设置页布局一致性**：长期记忆页继续复用全局设置页的页面头、按钮、表格和紧凑工具条风格，减少与其他设置页的视觉差异。

#### 验证
- `node scripts/check_chat_assets.js`
- `node -c client/chat/extra.js`
- `node -c client/chat/admin-settings.js`
- `npm run check:text`
- `git diff --check`

## [v0.0.127] - 2026-06-18
### 长期记忆系统产品级升级与治理完善

本版把长期记忆从“会话内摘要压缩”正式升级为独立的跨会话长期记忆层，同时保留现有 `messages` 摘要压缩作为短期会话记忆。长期记忆现在具备独立抽取、检索、治理、导出和维护能力，可以按用户偏好、项目/任务事实、长期决策、历史片段四类进行持续管理。

#### 变更内容
- **独立长期记忆层**：新增 `memories` 表，支持 `user_id`、`scope`、`type`、`content`、`embedding`、`salience`、`confidence`、`source_session_id`、`source_message_ids`、`status`、`last_used_at`、`expires_at` 等字段。
- **四类记忆分层**：长期记忆统一按用户偏好、项目/任务事实、长期决策、历史片段四类抽取与展示，避免把短期会话摘要直接当作跨会话记忆使用。
- **异步抽取队列**：新增 `memory_extraction_jobs` 持久化任务表，支持去重、重试、锁定、失败回收和任务清理；每轮对话后异步抽取候选记忆，不再阻塞主聊天流程。
- **抽取策略升级**：优先使用结构化模型输出抽取候选记忆，失败时回退启发式分类；抽取结果会自动去重、合并，并保留来源会话与消息 ID。
- **独立检索预算**：长期记忆检索与 RAG 分开预算，默认占输入窗口约 5% 到 10%，并按相关性、近期性和重要性排序后注入上下文。
- **治理能力完善**：记忆支持查看、编辑、禁用、恢复、删除、批量状态变更、合并建议、来源追溯和导出；敏感信息默认不自动入记忆，所有记忆都可追溯来源。
- **产品化管理面板**：记忆管理页新增质量摘要、任务队列、搜索筛选、批量操作、导出、归档过期记忆和清理历史任务入口，整体升级为可运营的后台能力。
- **维护与保留策略**：新增过期记忆归档、历史抽取任务清理和失败任务重试能力，便于上线后持续治理长期记忆质量。
- **验证补齐**：补充长期记忆抽取、治理、导出、质量摘要、任务维护和归档清理相关测试，覆盖产品级流程。

#### 验证
- `node tests/security-rag.test.js`
- `node tests/security-chat.test.js`
- `npm run check`
- `npm run lint`

## [v0.0.126] - 2026-06-18
### 128K 上下文后的动态资源限制与全局参数配置

本版围绕自部署模型上下文提升到 128K 后的系统限制做统一升级：把此前分散在代码和环境变量中的并发、上传、图片、附件上下文、知识库抽取、RAG 引用数量等固定限制，升级为“模型窗口 + 资源保护 + 管理员配置”的动态策略。管理员可以在系统设置中新建的“全局参数”页面查看运行时参数，内置 `admin` 超级管理员可以集中调整并保存，常规运营调整不再需要反复修改 `.env` 并重启服务；记忆系统升级计划按安排保留到后续版本。

#### 变更内容
- **新增全局参数页面**：系统设置新增“全局参数”页，集中维护请求队列、上下文窗口、上传与图片、知识库与 RAG、智能体与后台等运行时参数；管理员可查看，内置 `admin` 超级管理员可保存修改，普通用户不会从设置接口获取全局运行参数快照。
- **全局参数入口与布局收口**：移除模型配置页内重复的全局参数按钮，修复设置侧栏“全局参数”菜单未绑定点击事件的问题，并将全局参数页改为复用设置页统一的表单分组样式。
- **上下文相关限制动态化**：附件注入上下文长度、单条消息图片数量、图片上下文大小、知识库文本抽取长度、RAG 上下文预算比例均改为运行时读取配置，并继续受模型上下文窗口预算保护。
- **上传限制运行时生效**：聊天附件上传、图片上传和知识库文档上传的文件大小限制从启动期固定值改为按请求读取运行时配置，管理员保存后即可影响后续上传。
- **RAG 检索上限可治理**：RAG Top K、候选数量上限、分片大小上限和知识库引用预算改为可配置，同时保留最大边界，避免 128K 上下文下无约束放大检索成本。
- **配置输入更友好**：全局参数页支持 `128K`、`64M` 等短单位输入，保存时统一解析为规范数值；运行时设置状态在新页面和旧弹窗之间同步显示。
- **资源保护辅助层**：新增统一资源限制服务，集中计算上传、图片、附件、知识库和 RAG 的有效运行时限制，减少各业务模块硬编码。

#### 验证
- `npm run check`
- `node tests/security-chat.test.js`
- `node tests/security-rag.test.js`
- `git diff --check`

## [v0.0.125] - 2026-06-18
### 模型配置、并发治理与速度统计收口

本版把近期几处前端和后端联动优化统一收进更新日志，并启用新的版本号。模型配置弹窗恢复了上下文窗口 Token 及对应的提交、编辑回填和重置逻辑；并发治理弹窗样式对齐了模型配置弹窗；模型配置页顶部原有的上下文阈值快捷入口已移除，统一收口到并发治理页面的全局上下文设置。与此同时，聊天速度统计改为只按可见答案 token 与答案生成耗时计算，避免把模型思考时间计入 tps。

#### 变更内容
- **模型配置弹窗恢复上下文窗口**：恢复 `上下文窗口 (Token)` 配置项及相关提交、编辑回填和重置逻辑，保证模型配置与后端字段保持一致。
- **并发治理样式统一**：并发治理弹窗参照模型配置弹窗的结构和视觉样式重新整理，输入区和底部操作区保持一致的交互体验。
- **全局上下文阈值收口**：模型配置页面顶部的上下文阈值快捷编辑入口移除，统一由并发治理页面维护全局上下文设置，避免重复配置。
- **TPS 统计修正**：聊天响应速度不再按整段请求耗时除以总 token 数，改为仅统计可见答案片段的 token 和答案生成耗时，避免把思考阶段算进每秒 token。

#### 验证
- `npm run check`
- `npm run lint`
- `node tests/security-chat.test.js`

## [v0.0.124] - 2026-06-17
### 优化知识图谱卡片悬停信息

知识库页面的知识图谱关系明细卡片此前同时使用浏览器原生 `title` 和自定义浮层，鼠标悬停时会叠出两个信息弹窗；同时实体节点列表卡片没有完整信息浮层，关系浮层也没有把完整来源片段展示出来。
#### 修复内容
- **关系卡片只保留一个自定义浮层**：移除关系明细卡片、关系起点/终点和地图节点上的原生 `title`，避免浏览器默认 tooltip 与自定义 tooltip 同时出现。
- **实体节点卡片补齐完整信息**：左侧实体节点列表现在会生成 `data-graph-node-tooltip`，悬停或键盘聚焦时展示实体名称、类型、关系数、可信度、描述和 ID。
- **关系浮层展示完整来源内容**：关系 tooltip 新增完整 `chunk_text` 来源片段，不再只依赖卡片列表中的两行截断摘要。
- **长内容可查看**：知识图谱 tooltip 增加更宽的最大宽度、最大高度、自动换行和滚动能力，鼠标移入浮层时不会立即消失，便于查看长来源文本。
#### 验证
- 新增前端渲染回归测试，覆盖“关系卡片不再带 `title`”“实体卡片带完整 tooltip”“关系 tooltip 包含完整来源内容”。

## [v0.0.123] - 2026-06-17
### 关闭思考时恢复正文流式输出

上一版为避免思考脚手架外显，会在关闭聊天思考时等上游完成后一次性回放清理后的最终正文，虽然安全但体验不够流畅。本版本改为服务端增量过滤：标准 reasoning 字段继续丢弃，普通正文保持流式输出。

#### 修复内容
- **关闭思考仍可流式输出**：`/api/chat` 在 `chat_thinking_enabled=0` 时不再简单阻断所有可见内容增量，而是对普通 `content` 做增量过滤后继续 SSE 输出。
- **脚手架增量过滤**：新增 `createVisibleReasoningStreamFilter()`，识别 `Analyze User Input` / `Draft` / `Check Constraints` 等可见思考脚手架；检测到脚手架时先缓存并跳过思考段，从 `Draft:` 后开始流式输出正文，到检查段前停止。
- **保留最终兜底清理**：持久化助手消息前仍执行 `stripVisibleReasoningScaffold()`，防止非标准模型把思考内容混入最终文本。

#### 验证
- 新增聊天 no-think 单测，覆盖“脚手架不外显且 Draft 后正文可流式输出”“普通正文 chunk 可立即通过”两个场景。

## [v0.0.122] - 2026-06-17
### 修正关闭思考后的 Token 速度虚高

关闭聊天思考后，前端会在上游完成时一次性收到清理后的最终正文；原先仍按“首个可见 token 到当前时间”的流式公式计算速度，导致短时间回放正文时出现 `144000 t/s` 这类不可信数值。

#### 修复内容
- **服务端下发真实统计**：聊天流结束保存助手消息后，后端按本次请求总耗时计算 `costTime` 与 `tokens_per_sec`，写入消息记录，并随 `message_saved` SSE 事件返回前端。
- **前端最终统计不再估算覆盖**：收到服务端 `costTime/tps` 后，前端直接使用该值展示并跳过后续 `/chat/stats` 估算覆盖，避免把一次性回放当成超高速生成。
- **实时速度口径保守化**：流式生成过程中的临时 `t/s` 改为 `可见 token / 请求总耗时`，不再用最后一次可见内容的极短时间差计算。

#### 验证
- 更新聊天路由回归用例，覆盖 `message_saved` 携带 `costTime/tps` 且数据库 `tokens_per_sec` 同步写入。

## [v0.0.121] - 2026-06-17
### 聊天思考开关改为正向语义并真正隐藏思考过程

本版本修正上一版“关闭思考”开关的语义与实际行为不一致问题：模型配置弹窗改为“聊天中开启思考”，打开表示保留/显示思考过程，关闭表示普通聊天对思考模型追加 `/no_think` 并隐藏上游返回的思考内容。

#### 修复内容
- **字段名正向化**：模型字段统一改为 `chat_thinking_enabled INTEGER DEFAULT 0`，前端保存、编辑回填、表单重置、后台模型列表、可用模型接口和快捷查询均使用该字段；默认关闭，只有思考模型且 `chat_thinking_enabled=1` 时普通聊天才保留思考。
- **移除反向字段口径**：后端保存模型时不再读取旧的反向字段，避免“开关关闭但仍显示思考”的语义混淆。
- **流式思考内容不再外显**：`createStreamAccumulator` 新增 `includeThoughtContent` 控制；普通聊天关闭思考时会丢弃 `reasoning_content` 增量，不再把思考增量写入 SSE。
- **兜底清理可见思考脚手架**：对未按标准 reasoning 字段返回、而是把 `Analyze User Input` / `Draft` / `Check Constraints` 等思考脚手架混进正文的模型，关闭思考时会在最终回放前提取 `Draft` 正文并剥离检查段，避免用户侧继续看到推理过程。
- **UI 语义修正**：模型配置弹窗开关更名为“聊天中开启思考”，说明改为“关闭时普通聊天会隐藏思考过程并追加 `/no_think`”，实现打开=开启、关闭=关闭的直观行为。

#### 验证
- 新增聊天 no-think 单测，覆盖默认关闭会追加 `/no_think`、打开开关不会追加、流式 reasoning 增量被丢弃、可见思考脚手架被剥离等场景。

## [v0.0.120] - 2026-06-17
### 普通聊天思考模型可配置关闭思考

采纳“模型配置里标记思考模型后，再提供聊天中开启思考开关”的方案：普通聊天不做全局强制关闭，而是按模型配置选择是否在聊天请求中追加 `/no_think`。公文/工具型任务仍保持原有专用策略，普通聊天可按模型与场景单独控制。

#### 配置闭环
- **新增模型字段**：`models` 表新增 `chat_thinking_enabled INTEGER DEFAULT 0`，新库 schema 与旧库迁移均已补齐；模型列表快捷查询、后台模型列表、可用模型接口均返回该字段。
- **模型管理弹窗新增开关**：在“思考模型”配置旁新增“聊天中开启思考”开关，保存 payload、编辑回填、表单重置均已接入；该开关仅对已标记 `supports_reasoning` 的模型生效，关闭时普通聊天追加 `/no_think`。
- **后端统一判定**：新增 `shouldDisableChatThinking(model)`，只有 `supports_reasoning=1` 且模型配置关闭聊天思考时才启用聊天关闭思考，避免普通模型误触发。

#### 聊天链路
- **普通聊天按模型追加 `/no_think`**：`/api/chat` 在 RAG、视觉消息、MCP 上下文注入之后，并在 `fitMessagesToContextBudget` 预算裁剪之前，对最后一条用户消息追加 `/no_think`，让预算逻辑也统计这几个 token。
- **不污染历史消息**：追加逻辑只作用于本次发给上游模型的消息副本，不改已入库的用户原文；如果用户已经手动写了 `/no_think`，不会重复追加。
- **多模态兼容**：当最后一条用户消息是文本+图片数组时，仅在第一个文本 part 追加 `/no_think`，图片 part 保持不变。

#### 验证
- 新增 `tests/security-chat/chat-no-think.js`，覆盖“仅思考模型生效”“只追加最后一条 user”“不重复追加”“不修改原数组”“多模态消息保留图片 part”等场景。
- 已按要求未打开浏览器验证；命令行验证通过：`node tests/security-chat.test.js`（79/79）、`npm run lint`、`npm run check`。

#### 文档与版本
- **版本号启用**：应用版本升级至 `v0.0.120`，同步更新 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。

## [v0.0.119] - 2026-06-17
### 模型上下文设置全面修复与完善

依据上一轮对"模型上下文设置"的全项目检查，修复并完善了 9 处缺口与不一致。涉及 `server/db/schema.js`、`server/db/migrate.js`、`server/routes/models.js`、`server/services/models.js`、`server/services/context-budget.js`、`server/services/agent-model.js`、`server/services/agent-streaming-runtime.js`、`server/routes/chat.js`、`server/llm.js`、`client/chat/partials/pre-app-modals.html`、`client/chat/models-actions.js`、`client/chat/models.js`，新增 `tests/security-chat/context-settings.js`、`tests/security-chat/context-thought.js`。

#### 新增「上下文窗口」配置（修复死字段）
- **补齐 `context_window_tokens` 列**：`getModelContextBudget` 早已读取 `modelCfg.context_window_tokens`，但 `models` 表此前没有该列（永远 `undefined`，是死代码路径）。本版补齐 schema 列与迁移 `ensureColumn`，并在模型管理弹窗新增「上下文窗口 (Token)」字段（占位："模型实际总上下文，如 32K；留空按输入上限推导"），打通创建/编辑/回显。管理员现在可直接按离线模型实际 `n_ctx` 设定上下文窗口，无需再用「输入上限 + 预留」反推。
- **弹窗状态重置补齐**：`resetModelForm()` 现在会同步清空 `m-context-window-tokens`，避免编辑过带上下文窗口的模型后，新建模型时字段残留并被误保存。
- **无界预算告警**：当模型既未配 `max_input_tokens`/`context_window_tokens`、也无全局上下文环境变量时，请求预算为"无界"（不裁剪）。现对大输入（≥16K token）节流告警（5 分钟一次），提醒管理员补配上下文窗口，避免超长上下文无感知直发上游。

#### 输出上限口径统一
- **Agent 调用尊重模型配置**：`agent-model.js` 的 `callModelJson`/`callModelStreamingWithTools` 原先把 `max_tokens` 写死 1200、完全无视模型配置。改为 `显式值 → modelCfg.max_tokens → 1200`（新增 `resolveAgentMaxTokens`），与 chat/openai/apps 口径一致，避免推理型模型（Qwen3 等）被 1200 截断后正文为空。
- **流式 Agent 不再覆盖模型配置**：`agent-streaming-runtime.js` 移除显式传入的 `maxTokens: 1200`，让流式工具规划同样走 `resolveAgentMaxTokens`，避免开启 `AGENT_STREAMING_TOOLS=true` 时仍被固定输出上限截断。
- **网页聊天空 max_tokens 注释为刻意设计**：chat 接口在模型未配 `max_tokens` 时有意不下发输出上限（让上游用自身默认、不人为截断交互式回复），补注释说明与 openai/apps 的 2000 兜底差异，避免被误判为遗漏。

#### 压缩阈值随模型上下文收紧
- **模型感知的压缩触发**：`getContext` 的记忆压缩触发阈值改为 `min(全局阈值, 模型输入预算)`——仅在模型配置的输入预算小于全局阈值时**提前**压缩，避免请求时只能硬丢历史；未配置上下文的模型预算无界，行为不变（仅下调、绝不上调，零意外）。

#### 思考过程不回灌上下文
- **助手思考块剥离后再发送给模型**：聊天记录仍保存并展示 `<thought>`/`<thinking>`/`<think>` 思考块，但 `getContext` 在组装下一轮模型输入前会只对 assistant 历史消息剥离这些思考块，用户原文中的同名标签保持不变。
- **上下文用量不再统计思考块**：`buildContextMeta` 对 assistant 历史按剥离后的正文估算上下文 token，避免长思考内容把上下文用量条、压缩触发和后续预算判断虚高；消息自身 `token_count` 仍保留上游实际输出/用量语义。
- **记忆压缩不总结思考链**：后台记忆压缩摘要使用同一套上下文正文提取逻辑，避免早期 assistant 思考内容被摘要化后重新进入长期记忆。

#### 模型 token 设置范围/关系校验（防异常值入库与转发）
- **统一范围校验**：模型创建/更新接口此前对 `max_input_tokens`/`max_tokens`/`context_window_tokens` 只做裸 `parseInt`，负数/超大/非数字可直接入库，且负的 `max_tokens` 会被 chat、openai 接口原样转发给上游模型服务。新增 `validateModelTokenSettings`/`normalizeModelTokenLimit`（`server/services/models.js`），统一校验：留空/0 视为不限，负数、非数字、超过 2000 万上限一律拒绝（400）。
- **严格数字校验**：后端不再接受 `123abc` 这类会被 `parseInt` 部分解析的值；前端仍支持 `8K`、`32K` 等短单位输入，并在提交前转换为规范整数。
- **关系校验**：当设置了上下文窗口时，要求「输入 Token 上限」「输出 Token 上限」均小于窗口，避免相互矛盾的配置；前端提交前也做同样的轻量校验即时反馈，后端仍为权威。
- **`daily_token_limit` 维持原有 `Math.max(…,0)` 钳制**，本次将同等防护扩展到三项上下文 token 字段。

#### 验证
- 新增上下文相关单测 10 项（`context_window_tokens` 生效、与 `max_input_tokens` 取小、无界判定、Agent 输出上限解析、token 范围校验、关系校验、assistant 思考块剥离、上下文用量排除思考 token）。
- `npm run check`、`npm run lint`（0 错误）与 `node tests/security-chat.test.js`（**77/77**）通过；此前完整安全回归 `node tests/security.test.js`（**214/214**）保持记录。

#### 文档与版本
- **版本号启用**：应用版本升级至 `v0.0.119`，同步更新 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。

## [v0.0.118] - 2026-06-17
### 修复公文写作对推理型模型（Qwen3 等）返回空正文

修复生产环境用 Qwen3 等"思考(reasoning)型"模型时，公文写作全文润色/起草/审校等**非流式**操作弹出"AI 未返回有效内容"的问题（Qwen2.5 等非思考模型不受影响）。根因：思考型模型默认先输出大段思考，公文写作专用接口把输出上限**写死 2000**、且非流式解析**只读 `message.content`**，导致思考耗尽 token 后正文为空、却返回 200 无报错。涉及改动 `server/routes/apps.js`、`client/chat/apps-workbench-ai.js`，新增 `tests/security-chat/official-writing-ai.js`。

#### 修复内容
- **关闭工具任务的思考**：公文起草/润色/审校属工具型任务、无需思维链。对推理型模型（按管理员 `supports_reasoning` 标志，或 Qwen3 / QwQ / DeepSeek-R1 等模型名兜底识别）在最后一条 user 消息追加 `/no_think` 软开关（Qwen3 chat template 原生支持，llama.cpp 等通用）。
- **输出上限不再写死**：`max_tokens` 由固定 2000 改为 `max(任务基线, 模型配置 max_tokens)`，管理员在"模型管理"调高最大输出即可生效。
- **加厚非流式解析**：复用聊天侧 `extractModelText`（兼容字符串/数组 content、Responses 风格 `output_text`/`output[]`），并剥离内联 `<think>…</think>` 思考块；不把 `reasoning_content` 当成稿返回。
- **空正文诊断与明确提示**：后端在返回空正文时记录 `finish_reason`/`usage`/是否含 `reasoning_content` 等诊断日志，并把 `finish_reason` 透传前端；被截断（`length`）时前端提示"输出被截断，请调高最大输出 Token 或缩短正文"，不再是笼统的"未返回有效内容"。

#### 验证
- 新增 AI 路由纯函数单测 5 项（思考块剥离、多形态正文解析、仅 reasoning_content 返回空、推理模型识别、`/no_think` 软开关不改原数组）。
- `npm run check`、`npm run lint`（0 错误）与 `node tests/security.test.js`（**206/206**）通过。

#### 文档与版本
- **版本号启用**：应用版本升级至 `v0.0.118`，同步更新 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。

## [v0.0.117] - 2026-06-17
### 公文写作导出功能抽为独立 apps-workbench-export.js

在上一版按工作流拆分的基础上，把分散在 `apps-workbench-proofread.js` 与 `apps-workbench-rewrite.js` 的文件导出逻辑集中抽到新文件 `client/chat/apps-workbench-export.js`，使"导出"成为单一内聚关注点。与上一版同样是**纯结构搬移、函数实现零改动**，被搬移函数体经哈希比对前后完全一致。涉及新增 `apps-workbench-export.js`，改动 `apps-workbench-proofread.js`、`apps-workbench-rewrite.js`、`app-workspaces.js`、`stats-monitor-utils.js`、`scripts/check_chat_assets.js`。

#### 导出模块抽取
- **集中导出关注点**：DOCX/红头 DOCX（含 ZIP 底层 `buildZip`/`crc32String`/`encodeZipText` 与段落 XML、`buildOfficialWritingDocxBlob`/`buildOfficialWritingRedHeaderXml`）、Markdown（`buildOfficialWritingMarkdown`）、带版式文本（`buildOfficialWritingFormattedText`）、打印 PDF 用 HTML（`buildOfficialWritingExportHtml`）、纯文本（`exportOfficialWritingText`）、下载工具（`downloadOfficialWritingFile`/`downloadOfficialWritingBlob`）与统一入口 `exportOfficialWriting(type)` 共 20 个函数集中到 `apps-workbench-export.js`。
- **就近关注点保留**：剪贴板与"带入聊天"相关的 `copyAppsText`、`buildOfficialWritingPrompt`、`copyOfficialWritingPrompt`、`sendOfficialWritingToChat` 不属于文件导出，保留在 proofread 切片。
- **口径澄清**：导出入口的 `pdf` 类型并非生成 PDF，而是打开打印窗口由用户"另存为 PDF"；红头 DOCX 需先在发文要素填写印发机关。

#### 安全性与验证
- **零逻辑漂移**：被搬移的 20 个函数按整函数边界切割，前后函数体哈希一致；7 个切片顶层声明总数仍为 226，无重复声明、无遗漏，切缝处无半截函数。
- **加载顺序**：`app-workspaces.js` 在 rewrite 之后、rag 之前加载 export 切片；导出函数仅在用户点击时运行，无加载时依赖。
- **回归验证**：隔离上下文拼接执行 7 切片无加载时报错且入口正常注册；服务启动确认 7 切片均 200、旧路径 404；`npm run check`、`npm run lint`（0 错误）与 `node tests/security.test.js`（**201/201**）通过。

#### 文档与版本
- **版本号启用**：应用版本升级至 `v0.0.117`，同步更新 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。

## [v0.0.116] - 2026-06-17
### 公文写作工作台脚本按工作流安全拆分

本版本把单文件 `client/chat/apps-workbench.js`（3612 行 / ~166KB）按工作流拆分为 6 个全局脚本，沿用项目既有的 `dag-*`/`mcp-*` 多文件顺序加载范式，降低单文件维护成本。拆分为纯结构调整，**不改动任何函数实现**，逐切片与原文件按字节比对一致。涉及删除 `client/chat/apps-workbench.js`，新增 6 个切片文件，改动 `client/chat/app-workspaces.js`、`client/chat/stats-monitor-utils.js`、`scripts/check_chat_assets.js`、`eslint.config.js`。

#### 脚本拆分
- **按工作流切分 6 个文件**：`apps-workbench-core.js`（注册/共享状态/库读写/首页）、`-editor.js`（多文档管理/表单/版式渲染/编辑选区撤销）、`-proofread.js`（校对检查/抽屉视图/批注/版本/ZIP·DOCX·HTML 导出）、`-ai.js`（模型选择/`requestOfficialWritingAi`/全文与选区 AI）、`-rewrite.js`（流式改写+逐句对比/Markdown 与格式化导出/重置）、`-rag.js`（知识库检索/素材动作/事件绑定与 `window.*` 入口）。
- **顺序加载保证**：`app-workspaces.js` 的 `WORKSPACE_SCRIPT_GROUPS.apps` 按 core→editor→proofread→ai→rewrite→rag 顺序列出，`Pivot.loadScripts` 以 `await` 串行且 `script.async=false` 注入，保证唯一的加载时调用（`createOfficialWritingState()`）的依赖已就绪。
- **实现零改动**：6 个切片按原文件行边界切割，拼接后与原 `apps-workbench.js` 逐字节一致；跨文件共享的 226 个顶层声明仍在同一全局作用域，无重复声明、无函数重排。

#### 配套校验
- **ESLint 全局登记**：新增 `appsWorkbenchGlobals`，登记 129 个跨切片引用的标识符（7 个可变状态标 `writable`，122 个函数/常量标 `readonly`），与 `chatBundleGlobals` 同一机制；`npm run lint` 全量 0 错误。
- **资产守卫适配**：`scripts/check_chat_assets.js` 改为读取并合并 6 个切片后再校验关键守卫片段，并逐个确认切片存在；监控脚本标签表补齐 6 个新路径。
- **加载与服务验证**：在隔离全局上下文中拼接执行 6 个切片，确认无加载时报错且 `window.openAppsWorkbench`/`closeAppsWorkbench`/`PIVOT_APP_REGISTRY` 正常注册；启动服务确认 6 个切片均 200、旧路径 `apps-workbench.js` 返回 404。

#### 文档与版本
- **版本号启用**：应用版本升级至 `v0.0.116`，同步更新 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。
- **回归验证通过**：`npm run check`、`npm run lint`（0 错误）与 `node tests/security.test.js`（**201/201**）全部通过。

## [v0.0.115] - 2026-06-17
### 模型补全转发收口到统一 forwardChatCompletion 服务

本版本把分散在三处的“上游模型 axios 转发”收口到单一服务 `server/services/model-forwarder.js` 的 `forwardChatCompletion()`：聊天链路（`/api/chat`，含 Responses API 与降级到 Chat Completions 的两次调用）、OpenAI 兼容接口（`/v1/chat/completions`）与应用中心公文写作（`/api/apps/official-writing/ai`）共用同一套出站安全校验与请求装配。涉及新增 `server/services/model-forwarder.js`，改动 `server/routes/chat.js`、`server/routes/openai.js`、`server/routes/apps.js`，新增 `tests/security-chat/model-forwarder.js`。

#### 转发逻辑去重
- **单一转发入口**：原本在三个路由里重复出现的 `assertSafeModelRuntimeUrl` → `createSafeModelHttpAgents` → `axios({...proxy:false})` 五处样板（聊天链路占其中三处：Responses、降级、常规）统一为 `forwardChatCompletion({ modelCfg, user, url, data, headers, stream, timeout })` 一次调用，各路由不再各自 `require('axios')` 与安全适配器。
- **安全口径不可绕过**：SSRF/出网主机校验与受限 HTTP agent 现在内聚在服务内部，任何走该入口的转发都会先校验再发起，杜绝某处遗漏安全检查；禁用系统代理（`proxy:false`）与超时口径集中维护（默认 180s，聊天常规/降级调用按需传入 300s）。
- **行为等价**：仅抽取“发起请求并返回 axios 响应”这一层，URL 构建、payload 组装、流式/非流式响应消费仍由各路由自行处理，聊天的 Responses→Chat Completions 自动降级、图表流捕获与原始回放等专有逻辑保持不变。

#### 文档与版本
- **版本号启用**：应用版本升级至 `v0.0.115`，同步更新 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。
- **回归验证通过**：新增转发服务契约单测 3 项；`node tests/security.test.js` 全量 **201/201** 通过，ESLint **0 错误**，`npm run check` 通过。

## [v0.0.114] - 2026-06-16
### 公文写作 AI 调用收口到后端专用接口

本版本把“公文写作”的 AI 调用从前端直连 OpenAI 兼容补全接口，重构为后端专用接口 `POST /api/apps/official-writing/ai`：前端只传结构化参数（`mode`/`action`/`docType`/`standard`/`source`/`draft`/`requirements`/`comments`/`selection`），由后端统一组装提示词、做模型权限/配额/上下文预算检查、上游转发、用量统计与审计标签。涉及新增 `server/services/official-writing.js`、`server/routes/apps.js`，改动 `server/index.js`、`client/chat/apps-workbench.js`、`tests/security-chat.test.js`（新增 `tests/security-chat/official-writing.js`）、`README.md`。

#### AI 调用封装（后端专用接口）
- **提示词组装移到后端**：起草、润色、全文改写、审校、选区处理与逐句流式改写的系统/用户提示词统一在 `server/services/official-writing.js` 的 `buildOfficialWritingMessages()` 中构建，前端不再手写 prompt；选区动作（润色/压缩/扩写/公文语气）指令也由后端维护，消除前后端重复。
- **统一鉴权与策略口径**：新接口复用 `getAccessibleModel`、`fitMessagesToContextBudget`、并发信号量与端点槽位、`recordModelTokenUsage` 等既有能力，与聊天/`/v1` 接口同一套权限、配额、预算与并发保护；调用统一打 `公文写作 AI` 审计标签，记录操作模式、动作与模型。
- **流式与非流式归一**：逐句改写走 SSE 流式转发，其余模式走一次性补全；前端新增共享方法 `requestOfficialWritingAi()` 取代此前重复的 `callOfficialWritingModel()` 与 `streamOfficialWritingModel()`，错误处理、超时与流式解析只维护一份。

#### 模型选择兜底
- **默认模型回退**：公文写作不再强制要求先在聊天页选模型。后端按“显式选择 → 个人默认模型（`users.default_model_id`）→ 系统默认模型（`is_default`）”顺序解析，均无可用模型时返回清晰提示，体验不再被“请先在聊天页选择模型”打断。

#### 审校结果更稳健
- **JSON 解析收口到后端**：审校模式的 JSON 数组提取与字段校验改在后端完成（兼容代码块包裹、前后说明，过滤脏数据并规整字段），前端直接消费结构化条目；无有效条目时仍退回整体审校意见，避免结果丢失。

#### 文档口径
- **接口口径以最新版为准**：公文写作 AI 当前统一走后端专用接口 `/api/apps/official-writing/ai`，不再由前端直连 `/v1/chat/completions`；历史版本摘要中出现的 `/api/chat/completions`、`/v1/chat/completions` 为各自版本的当时实现记录，排障时以本版说明为准。

#### 文档与版本
- **版本号启用**：应用版本升级至 `v0.0.114`，同步更新 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。
- **回归验证通过**：`npm run check` 全量通过；新增公文写作服务单测 6 项；`node tests/security.test.js` 全量 **192/192** 通过，ESLint **0 错误**。

## [v0.0.113] - 2026-06-16
### 全栈深度体检：安全收口、性能优化、稳定性加固与数据库 MCP 重构

本版本对全项目（服务端 ~29k 行、客户端 ~29k 行）做了一次系统性深度检查，并对查出的问题进行全面修复与优化。改动覆盖安全、性能、服务端稳定性、客户端内存与错误处理四大方向，并完成数据库 MCP 多方言分发逻辑的去重重构。涉及 `server/services/database-mcp.js`、`server/routes/admin-users.js`、`server/index.js`、`server/db/migrate.js`、`server/db/schema.js`、`server/services/agent-runtime.js`、`server/routes/mcp.js`、`server/routes/admin-stats.js`、`server/metrics.js`、`server/services/rag-index.js`、`server/services/knowledge-graph.js`、`server/services/context-budget.js`、`client/chat/render-charts.js`、`client/chat/engine.js`、`client/chat/engine-sessions.js`、`client/chat/render-messages.js`、`client/chat/apps-workbench.js`、`client/chat/engine-mcp-tools.js`、`client/chat/mcp-workbench-main.js`、`client/chat/mcp-workbench-common.js` 等。

#### 安全收口
- **数据库 MCP 连接 SSRF 防护**：外部数据库（PostgreSQL/MySQL/SQL Server/MongoDB）在**连接时**统一经 `assertSafeDatabaseHost`/`databaseSafeLookup` 做 DNS 解析后 IP 校验，PostgreSQL 通过驱动 `lookup` 钩子在握手阶段复核解析 IP，其余方言在连接前重解析校验，拦截把内网/本机/云元数据地址藏在域名背后的 SSRF 与 DNS rebinding；配置态保留同步的字面量私网快速拦截。
- **用户 CSV 导入校验**：导入路径套用与 `register()` 一致的用户名正则（`^[a-zA-Z0-9_.-]{3,32}$`）与密码强度校验，非法行跳过并在响应中回报 `skippedRows`；无密码行改为每行随机强密码，取代固定默认口令。

#### 性能优化
- **统计查询谓词下推**：`tokenUsageSubquery` 把日期/用户过滤条件下推进 `messages`/`model_usage_events` 联合查询的各分支内部，避免外层过滤导致两表全量物化；`monitor-summary`、`trend`、`report`、`details`、`model-costs` 及 `metrics.js` 的「当日/近 N 天/近 15 分钟」谓词由 `date()/datetime()` 包裹列改写为裸列半开区间，使 `created_at`/`timestamp` 索引生效。
- **监控快照与主机名缓存**：`/monitor-summary` 整体响应加 5 秒 TTL 缓存，本地主机名 DNS 解析加 60 秒缓存，降低仪表盘轮询开销。
- **RAG 向量检索提速**：嵌入向量解析结果与 L2 模长加入带界缓存（重建索引时失效），余弦相似度改为复用预存模长、每次查询仅计算一次查询向量模长，消除热路径上的重复 `JSON.parse` 与重复归一化。
- **知识图谱检索与摄取优化**：`findQueryEntities` 增加候选实体 SQL 预过滤与 `LIMIT`，不再每条消息全表扫描；图谱摄取的 upsert 语句提升到模块级复用，并以 `RETURNING` 取代回查 SELECT，减少每实体一次往返。
- **上下文裁剪复杂度优化**：`context-budget` 消息裁剪由每次重算全量 token（O(n²)）改为维护运行总数递减（O(n)）。

#### 服务端稳定性
- **未捕获 Promise 不再杀进程**：`unhandledRejection` 由直接 `process.exit(1)` 改为记录日志后继续运行，`uncaughtException` 行为保持不变，避免单个游离 Promise 拖垮服务。
- **目录体积统计加固**：`getDirSizeAsync` 增加 32 层递归深度上限并跳过符号链接，防止深层嵌套或环路导致栈溢出/挂起。
- **迁移簿记修正**：移除 `runMigrations` 末尾会把命令式 schema 误标为「已应用迁移」的无条件 `recordSchemaMigration` 调用，使迁移状态如实反映。
- **智能体重试身份与查询收口**：失败重试合并 `retry_limit`/`retry_count` 为单次查询，并用 `getRunUser(runId)` 重取用户身份，避免以过期/已禁用身份重试。
- **新增索引与去重**：补充 `idx_rag_feedback_user_doc(user_id, doc_name)` 索引；收敛 `mcp.js` 中 `SYSTEM_MCP_SERVICES` 的重复声明。

#### 客户端内存与错误处理
- **ECharts 实例泄漏修复**：新增 `disposePivotEchart`/`teardownPivotCharts`，流式重渲染替换消息 DOM 前先 `dispose()` 旧图表实例并移除 `window` resize 监听，消除长会话下图表实例与监听的无界累积。
- **流读取器与流后错误隔离**：聊天流在 `finally` 中可靠 `cancel()`/`releaseLock()` 读取器；流式成功后的统计上报失败不再误报为对话错误。
- **会话切换渲染优化**：`createSession`/`selectSession` 增加 `res.ok` 前置检查与 try/catch，并以 `DocumentFragment` 一次性挂载历史消息、统一渲染图表与滚动，消除逐条 reflow。
- **公文写作工作台修复**：RAG 集合加载标志位仅在成功后置位（修复首次失败后永不重载）；按键管线对保存/分析/列表重建做 250ms 防抖（输入即时、blur/导出前 flush）；评论与建议数组限长，避免无界增长。
- **监听器与转义收口**：MCP 同意弹窗改用共享 settle 显式清理双监听，避免确认后残留累积；MCP 工作台 `data-*` 中的 `server.id` 统一转义。

#### 数据库 MCP 重构
- **多方言分发去重**：将 `executeSqlTool` 中 sqlite/postgres/mysql/sqlserver 四套近乎重复的五工具分发（约 218 行）重构为「方言适配器表 `RELATIONAL_DIALECTS` + 单一共享分发器 `runRelationalTool`」，把连接包装、取行调用、元数据 SQL 与方言/参数风格收敛到适配器，治理与脱敏逻辑只写一份；`withSqlite` 改为 `async` 以正确等待异步分发完成后再关闭句柄。行为与原实现保持等价。

#### 文档与版本
- **版本号启用**：应用版本升级至 `v0.0.113`，同步更新 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。
- **回归验证通过**：`npm test`（`check` + `lint` + `node tests/security.test.js`）全量 **192/192** 通过，ESLint **0 错误**。

## [v0.0.112] - 2026-06-16
### 智能体工作台布局优化、角色与规范库弹窗及卡片体验收口

本版本主要围绕智能体工作台排版、角色与规范库小屏幕布局兼容，以及套用弹窗交互细节进行多处细节体验收口。改动集中在 `client/chat/partials/workspaces/agent.html`、`client/chat/styles/workspaces/agent/agent-layout.css`、`client/chat/styles/sessions-prompts.css`、`client/chat/partials/pre-app-modals.html` 和 `client/chat/extra.js`。

#### 智能体工作台布局优化
- **快速任务头部减重**：将快速任务标题与右侧的操作按钮组置于同一行两端对齐，而将折行率较高的说明段落移至下方并占据完整宽度，从而消除此前因文字过窄折行导致的头部高度过度拉高问题。

#### 角色与规范库卡片体验
- **折行与防截断收口**：卡片底部操作按钮容器开启 `flex-wrap: wrap` 并微调按钮内边距和最小宽度，以防在窄屏和小显示器下按钮溢出截断；同时去掉卡片说明文案与提示词主体的 2 行截断显示限制，允许内容较长时弹性撑开高度完整显示。
- **全局悬停提示**：为卡片说明文字与提示词主体内容新增原生 `title` 属性，鼠标悬停时会浮出气泡框提示最完整的原始文本。

#### 套用规范弹窗优化
- **关闭按钮右上角化**：将“套用角色与规范”弹窗的关闭按钮定位从原先的标题下方移动到弹窗右上角，并改用全局统一的 `.btn-danger-outline` 红色线框字样样式。

#### 文档与版本
- **版本号启用**：应用版本升级至 `v0.0.112`，同步更新 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。
- **回归验证通过**：通过 `npm run check` 校验及 `node tests/security-agent.test.js` 全量 35 个安全回归测试。

## [v0.0.111] - 2026-06-16
### API 接入总开关与管理员控制收口

本版本为第三方 API 接入补上全局总开关，并把后台控制、API Key 鉴权和 OpenAI-compatible `/v1` 路由一起收口到管理员设置。改动覆盖 `server/services/api-access-settings.js`、`server/db/index.js`、`server/auth.js`、`server/routes/settings.js`、`server/routes/auth.js`、`server/routes/openai.js`、`client/chat/admin-settings.js`、`client/chat/auth.js`、`client/chat/app.js`、`client/chat/partials/settings/usage-api-account.html`、`client/chat/styles/admin/admin-account-api.css` 以及相关安全回归测试。
#### 全局 API 接入开关
- **系统级开关落库**：新增 `api_access_enabled` 系统设置，启动时写入默认值，管理员可在系统设置中统一启停。
- **前后端联动**：API 账号页新增总开关、状态提示和关闭文案，开关关闭后会同步禁用创建入口、模型列表和相关按钮。
- **鉴权与路由拦截**：API Key 登录、`/auth/keys` 创建和 OpenAI-compatible `/v1` 路由在开关关闭时直接拒绝访问，避免仅靠前端隐藏。
- **安全回归补齐**：补充 API Key 创建阻断、设置读写和路由级拦截测试，覆盖开关主链路。
#### 文档与版本
- **版本号启用**：应用版本升级至 `v0.0.111`，同步更新 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。
- **验证通过**：`node tests/security.test.js` 已通过，新增回归用例覆盖总开关场景。

## [v0.0.110] - 2026-06-16
### 聊天首屏资源瘦身、RAG 运行时兼容与启动调度优化

本版本围绕首屏性能、RAG 运行时兼容、未授权访问容错与服务启动稳定性收口。改动集中在 `client/chat/pivot-core.js`、`client/chat/app-workspaces.js`、`client/chat/partials/scripts.html`、`client/chat/render-charts.js`、`client/chat/admin.js`、`client/chat/rag-format.js`、`client/chat/rag-documents.js`、`server/config.js`、`server/index.js`、`scripts/check_chat_assets.js` 与 `tests/security-chat/rendering-streams.js`，并同步补齐版本与文档说明。

#### 首屏脚本与大 vendor 按需加载
- **首屏脚本清单精简**：将工作区、管理后台、知识库和图表相关脚本从首屏拆出，`partials/scripts.html` 只保留轻量核心脚本，首屏初始资源明显收缩。
- **通用脚本加载器**：在 `pivot-core.js` 中新增版本化资源地址与一次性脚本加载缓存，统一提供 `loadScriptOnce` / `loadScripts`。
- **工作区按需进入**：`app-workspaces.js` 只在实际打开 Apps、Agent、Knowledge、MCP 工作台时再加载对应脚本组，减少首页常驻负担。
- **大 vendor 懒加载**：`echarts.min.js` 改为图表渲染时按需拉取，首屏不再强制带上大体积 vendor。
- **管理后台收口**：管理员相关统计、公告、工具策略等脚本全部改为进入对应功能后再加载。

#### RAG 运行时修复
- **兼容导出补回**：`rag-format.js` 恢复 `escapeRagHtml`、`escapeRagAttr`、`formatRagDateToCN`、`formatRagSize` 等兼容导出，修复 `escapeRagHtml is not defined`。
- **未授权静默处理**：`rag-documents.js` 对知识库专题与标签接口的 401 做统一处理，未登录或无权限时直接回退缓存并静默退出，避免控制台刷错。
- **工作台依赖补齐**：进入知识工作台时显式等待知识库脚本组就绪，避免专题/标签控件在资源未加载完毕时失效。

#### 服务启动与配置治理
- **维护任务延迟启动**：后台维护任务不再在模块加载时抢跑，改为服务监听成功后再调度，默认延迟 10 秒启动。
- **环境变量对齐**：`.env.example` 补充维护任务启动延迟说明，`.env` 同步补齐缺失的运行参数。

#### 验证
- **回归验证通过**：`npm run check`、`npm run lint` 已通过；此前完整 `npm test` 189/189 通过。
- **首屏实测收敛**：首屏 HTML 实测不再加载 `apps-workbench`、`agent-workflow`、`tool-policy`、`rag-graph` 和 `echarts`。
- **RAG 报错消失**：专题与标签接口在无权限场景下不再抛出未捕获错误，复现日志已收敛。
- **版本号启用**：应用版本升级至 `v0.0.110`，同步更新 `package.json`、`package-lock.json`、`README.md` 和 `CHANGELOG.md`。

## [v0.0.109] - 2026-06-12
### 公文写作工作台版式、布局与 AI 操作边界深度优化

本版本继续围绕应用中心“公文写作”做体验收口，重点解决顶部工具栏信息密度、正文/对照模式排版不贴近正式公文、左右面板职责重复、AI 全文操作与选区操作边界不清，以及左侧大纲长文本展示等问题。改动集中在 `client/chat/apps-workbench.js`、`client/chat/partials/workspaces/apps.html` 与 `client/chat/styles/workspaces/apps.css`，不涉及后端数据结构迁移。

#### 顶部工具栏与面板布局
- **单行优先的顶部工作流**：顶部改为“文种 / 规范库 / 材料来源 / 正文或对照模式 / AI 指令 / 全文 AI / 编辑 / 面板 / 更多”的同一行优先布局，宽屏下减少上下两层割裂，窄屏下再自然换行。
- **AI 指令命名收口**：原“修改要求”改为“AI指令”，占位提示随起草、润色、全文改写、审校模式变化，表达它是发送给模型的任务约束，而不是页面状态。
- **面板开关去重**：顶部新增统一的“资料栏 / 审改栏”面板开关，用高亮态表示可见状态；移除“更多”里的“审改中心”、右侧审改栏标题中的重复“收起”和顶部重复“保存版本”，版本保存统一保留在右侧“版本”面板。
- **对照模式保留资料栏**：对照模式不再自动隐藏左侧资料面板，资料栏只由顶部“资料栏”开关控制，避免切换模式时工具区突然消失。
- **右侧审改栏 tab 收口**：审改栏 7 个 tab 改为单行横向滚动，避免“修改建议 / 发文要素 / 批注 / 版本 / 规范检查 / 校对 / 材料引用”在窄抽屉内挤成多行。

#### AI 操作边界与调用修复
- **全文 AI 与选区工具分离**：顶部“起草 / 润色 / 改写 / 审校”明确标记为“全文AI”，只处理当前正文或整篇任务；选中文字后的浮动工具条增加“选区”标识，只处理选中的原文或正文片段，避免同名按钮造成误解。
- **全文改写语义明确**：顶部原“局改”改为“改写”，模式名由“局部修改”调整为“全文改写”；真正的局部润色、压缩、扩写、公文语气和逐句改写继续由选区浮动工具承担。
- **AI 接口路径修复**：公文写作的起草、润色、改写、审校和流式选区改写统一调用 OpenAI 兼容 `/v1/chat/completions`，修复此前按钮点击后出现 `AI 请求失败（404）` 的问题。

#### 正文与对照模式版式
- **正文渲染改为版式化 surface**：正文与原文编辑由隐藏 textarea 存储数据、contenteditable surface 负责呈现，按标题、主送机关、一级标题、条目、正文段落、收尾句、落款和日期分类渲染，保留选区、批注、AI 替换和版本回填能力。
- **公文版式细化**：正文模式扩大纸张可用宽度，收窄灰底留白；标题居中、仿宋正文、一级标题首行缩进 2 字符并加粗，`（一）（二）` 条目按正文缩进且不误加粗，正文段落首行缩进 2 字符。
- **行距与落款优化**：收紧正文和对照模式行距与空行高度；`特此通知。` 等收尾句按正文段落缩进；识别 `【待补充】年【待补充】月【待补充】日` 等日期占位，落款单位与日期作为落款组右对齐。
- **对照模式比例调整**：对照模式中间区采用原文 + 正文稿双栏，正文稿略宽，适合原文核对和正文审改；左侧资料栏和右侧审改栏继续作为工具面板存在。

#### 大纲与交互细节
- **大纲悬停看全量**：左侧大纲条目保留完整文本到 `title` 和 `data-full-text`，默认仍单行截断；鼠标悬停或键盘聚焦时在条目内部多行展开显示完整内容。
- **去除大纲水平滚动条**：大纲列表收回横向 overflow，不再因为悬停提示产生左右水平滚动条。
- **左侧资料库宽度与右侧审改栏宽度微调**：整体三栏比例向中间编辑区倾斜，正文仍是主工作区，两侧面板保持辅助定位。

#### 文档、版本与验证
- **文档同步更新**：README、使用手册、项目汇报和更新日志同步记录本次公文写作页面布局、AI 作用域、正文渲染、版式和大纲交互优化。
- **版本号启用**：应用版本升级至 `v0.0.109`，同步更新 `package.json`、`package-lock.json`、README、使用手册、项目汇报和更新日志。
- **校验通过**：版本文档更新后已通过 `npm run check` 与 `git diff --check`，确认文本完整性、JS 语法、聊天资产一致性以及 diff 空白格式均正常。

## [v0.0.108] - 2026-06-12
### 公文写作工作台功能扩展：多文档、发文要素、流式逐句改写、知识库检索、多格式导出与校对

本版本在 `v0.0.107` 公文写作接入真实模型的基础上，进行六项功能扩展，把工作台从“单篇草稿纸”升级为“多文档公文工具”。仅改动 `client/chat/apps-workbench.js`、`apps.html` 与 `apps.css`，不涉及后端；知识库检索复用既有 `/api/rag/debug-query`，流式改写复用既有聊天 SSE 解析与 `/api/chat/completions`。

#### 多文档工作台
- **从单 key 升级为文档库**：本地存储由单文档 `pivot_official_writing_state_v1` 升级为多文档库 `pivot_official_writing_library_v2`，左侧资料库顶部新增“我的公文”列表，支持新建、打开、重命名、删除，每篇独立保存正文、原文、版本、批注、建议、自动草稿和发文要素。
- **旧数据平滑迁移**：首次加载时自动把旧版单文档迁移为库中第一篇，并在保存成功后清理旧 key；删空文档库会自动新建一篇空白公文，保证始终有活动文档。
- **切换隔离**：切换文档时先回写当前编辑内容，并清空撤销/重做栈与选区记忆，避免跨文档串改。

#### 发文要素结构化表单
- **版头 + 版记字段**：审改栏新增“发文要素”页，独立录入密级与保密期限、紧急程度、发文字号、签发人（版头）以及抄送、印发机关、印发日期（版记），随文档持久化。
- **导出自动排版**：导出 DOCX / 红头 / 带版式文本 / Markdown 时，按发文要素自动排版版头（右上密级与紧急程度、签发人，居中发文字号）与版记（分隔线下抄送、印发机关与日期）。

#### AI 流式改写 + 逐句对比接受
- **流式改写**：选区浮动工具条新增“逐句改写”，调用模型流式返回并实时预览生成过程。
- **逐句红绿对比**：改写完成后按句子做 LCS 对齐，以红（原文，删除线）/绿（改写）逐句呈现，支持单句“接受改写/保留原文”、一键全部接受或全部保留，按所选逐句拼装后再应用到选区，而非整段替换；支持 `Esc` 关闭。

#### 知识库 / 历史公文检索接入
- **左侧“历史/规范”接入知识库**：两个原为空壳的标签页接入既有知识库检索 `/api/rag/debug-query`，支持关键词检索、按专题库范围过滤，结果显示来源与相关度。
- **一键引用**：检索结果可“引用到正文”（带来源前缀插入）或“作为依据”（追加到修改要求），真正实现参考历史公文与规范条文起草。

#### 导出格式扩展
- **新增三种导出**：在原 DOCX / PDF / 文本基础上，新增导出 Markdown（标题与层级用 # 分级、发文要素引用块）、导出带版式纯文本（标题居中、落款右对齐、版记分区）、以及套用单位红头模板的 DOCX（红色机关名称 + 红色分隔线，机关名取自印发机关）。

#### 公文校对增强
- **校对检查页**：审改栏新增“校对”页，检查常见易错词与不规范表述（如截止/截至、进行+动词冗余、汇报/报告）、敏感与绝对化措辞、标点规范（连续标点、疑似英文标点、引号与括号成对），以及标题与所选文种的一致性；问题可点击定位到正文对应位置。

#### 文档、版本与验证
- **文档同步更新**：README、使用手册、项目汇报与更新日志同步记录本次六项扩展。
- **版本号启用**：应用版本升级至 `v0.0.108`，同步更新 `package.json`、`package-lock.json`、README、使用手册、项目汇报和更新日志。
- **校验通过**：`npm run check`（215 个 JS 文件语法、文本完整性、聊天资产校验与安全测试）与 `eslint`（0 错误 0 警告）均已通过；AI 流式改写与知识库检索建议在登录态与可用模型/知识库环境下做一次端到端确认。

## [v0.0.107] - 2026-06-12
### 公文写作工作台接入真实模型、规范库联动与编辑可靠性增强

本版本聚焦应用中心“公文写作”工作台的能力补全：将原先基于本地正则模拟的 AI 操作改为真正调用项目现有的模型补全接口；让所选规范库真正驱动差异化的检查规则；并在引用判定、本地存储、DOCX 导出、撤销/重做与无障碍五个方向做可靠性与体验收口。本次仅改动 `client/chat/apps-workbench.js`、`apps.html` 与 `apps.css`，不涉及后端。

#### AI 能力（核心）
- **AI 操作接入真实模型**：起草、润色、局部修改、审校以及选区工具（润色/压缩/扩写/公文语气）由本地正则替换改为调用 OpenAI 兼容补全接口 `/api/chat/completions`，复用全局 `apiFetch`（自动注入 CSRF 与同源凭证），模型取自聊天页的模型选择器。
- **审校结构化回填**：审校模式要求模型按 JSON 数组返回逐条问题（标题/原文片段/修改建议），并在正文中定位后回填为可接受/拒绝/替换的建议；模型未按 JSON 输出时退回为整体审校意见，避免结果丢失。
- **处理态与容错**：AI 处理期间显示加载指示器并禁用相关按钮，未选模型、请求失败或返回为空均有明确提示，避免静默失败。删除已废弃的本地 `transformOfficialWritingText` 正则逻辑。

#### 规范检查与规范库联动
- **规范库驱动差异化规则**：新增按规范库划分的附加检查集——党政机关公文格式校验发文字号、签发人、紧急程度；会议纪要规范校验会议要素与议定事项；单位内部规范校验落款单位。通用规则（标题、占位、日期、引用、语气）始终生效，所选规范库现在真正改变检查结果。

#### 引用判定
- **模糊匹配替代精确子串**：材料引用判定从 `includes` 精确子串改为 2-gram 字符重叠率叠加关键数字命中（精确子串仍作强匹配，改写后的引用按 ≥45% 重叠或命中关键数字且 ≥25% 识别），减少正文对材料稍作改写后被误判为“未引用”的误报；引用卡片标注“相近 N%”。

#### 本地存储可靠性
- **容量保护与备份**：`saveOfficialWritingState` 写入前预估体积（软上限约 3.5MB），超限或触发 `QuotaExceededError` 时按“自动草稿 → 历史版本”梯度自动精简并重试，仍失败则提示导出备份；新增“导出备份”入口，可导出完整工作区 JSON。

#### DOCX 导出
- **补元数据与版式**：补齐 `docProps/core.xml`、`docProps/app.xml` 及对应内容类型与关系声明，提升与 Word/WPS 的兼容性；按层级识别标题、主送机关、一/二/三级标题、落款单位与成文日期，分别套用居中加粗、黑体、首行缩进与右对齐样式。

#### 编辑可靠性与无障碍
- **撤销/重做**：新增基于正文与原文快照的撤销栈（上限 50 步），覆盖建议替换/插入、模板套用、载入版本、同步原文等破坏性操作；工具栏新增撤销/重做按钮，支持 `Ctrl+Z` / `Ctrl+Y`（及 `Ctrl+Shift+Z`）。
- **无障碍增强**：审改栏抽屉补齐 `role="region"`、tab 的 `role="tab"`/`aria-selected`/`aria-controls` 与 roving tabindex、面板 `role="tabpanel"`、选区工具条 `role="toolbar"`；支持方向键在分页间导航、`Esc` 收起抽屉并归还焦点，并为工作台内可聚焦控件提供 `:focus-visible` 焦点环。

#### 文档、版本与验证
- **文档同步更新**：README、使用手册、项目汇报与更新日志同步记录本次公文写作工作台的能力补全与体验收口。
- **版本号启用**：应用版本升级至 `v0.0.107`，同步更新 `package.json`、`package-lock.json`、README、使用手册、项目汇报和更新日志。
- **静态检查通过**：改动文件经 `eslint`（0 错误 0 警告）与 `node --check` 语法校验通过；AI 调用路径建议在登录态与可用模型环境下做一次端到端确认。

## [v0.0.106] - 2026-06-11
### 统计查询合并、错误响应收口、数据库标识符加固与工程化基线

本版本在 `v0.0.105` 后端性能与依赖优化的基础上，继续推进 P1/P2/P3 三档优化：合并运维统计端点的多次数据库往返、并行化模型主机解析；对前端拖拽与轮询做节流与生命周期收口；统一服务端错误响应避免泄漏内部细节；为数据库 MCP 的标识符拼接增加字符级防御；并补齐 ESLint 规则、`engines` 声明与持续集成基线。

#### 后端性能（P1）
- **运维统计查询合并**：`/api/admin/ops-summary` 将原先 8-12 个独立 `COUNT`/`SUM` 查询合并为单条标量子查询（超级管理员与个人维度各一条），减少 SQLite 往返与锁争用；目录大小等异步文件操作保持并行。
- **模型主机解析并行化**：模型端点概览原先在 `for` 循环中串行 `await` 主机本地性判定，改为 `Promise.all` 并行解析后按原顺序聚合，避免 DNS 解析延迟累加，同时保持本地/远程列表顺序与未知计数语义不变。

#### 前端体验（P1）
- **DAG 编辑器拖拽节流**：智能体 DAG 编辑器的 `pointermove` 接入 `Pivot.rafThrottle`，将每帧数十次的指针移动合并到每帧最多一次，降低拖拽时的重复计算与重排；销毁时正确解绑节流后的处理器。
- **公告轮询生命周期收口**：公告 60 秒轮询新增 `stopAnnouncements()` 清理钩子，并在登出流程中调用，避免轮询在无需时继续运行。

#### 安全加固（P2）
- **错误响应统一收口**：全局错误中间件对 5xx 服务端错误默认返回通用提示，不再依赖 `NODE_ENV` 判断（运维漏设环境变量也不会泄漏）；4xx 客户端提示与 503 可重试信息保持原样，个别需透出的 5xx 可显式标记 `err.expose`，避免 SQL 错误、内部路径与堆栈泄漏到响应。
- **数据库标识符字符校验**：数据库 MCP 的 `quoteIdentifier`/`quoteSqlIdentifierPart` 在引号转义之外新增 `assertSafeIdentifier`，拒绝包含控制字符（换行、回车、制表、null 字节等）与超长的表名/字段名，作为 `PRAGMA table_info` 等标识符拼接场景的防御纵深。

#### 工程化基线（P3）
- **ESLint 规则补齐**：在既有 `no-undef`/`no-unused-vars` 基础上，前后端配置统一新增 `no-cond-assign`、`no-dupe-keys`、`no-dupe-args`、`no-unreachable`、`no-constant-condition`、`no-func-assign`、`use-isnan`、`valid-typeof`、`no-self-assign`、`no-unsafe-negation` 等低误报、抓真实 bug 的规则，现有代码零新增告警。
- **运行时版本声明**：`package.json` 新增 `engines.node >= 20`，与 Dockerfile 使用的 Node 22 对齐。
- **持续集成基线**：新增 `.github/workflows/ci.yml`，在 push/PR 到 `main` 时于 Ubuntu + Node 22 上执行 `npm ci`、生产依赖审计、文本完整性、语法检查、聊天资产校验、Lint 与安全测试。

#### 验证
- **回归验证通过**：`npm run check`（215 个 JS 文件）、`npm run lint`（含新增规则）与安全测试 `189/189` 均已通过；`require('./server/db')` 加载成功并确认 6 个知识图谱索引在真实库中创建；标识符字符校验与合并查询逻辑均已实测确认。

## [v0.0.105] - 2026-06-11
### 知识图谱索引补齐、xlsx 依赖镜像化与 uuid 漏洞清零

本版本聚焦后端性能与依赖健康度的基础优化：为此前完全缺索引的知识图谱关系与提及表补齐索引，让按用户、状态和实体维度的高频查询从全表扫描转为索引查找；将 Excel 解析依赖从 `cdn.sheetjs.com` 直链切换为 npm 官方镜像 `@e965/xlsx`，消除 `npm ci`/Docker 构建对外部 CDN 的运行时依赖；并将 `uuid` 升级至 `^11`，在保持 CommonJS `require()` 兼容的前提下清除 `npm audit` 告警。

#### 后端性能
- **知识图谱索引补齐**：`knowledge_entity_mentions` 新增 `user_id`（按用户统计）和 `chunk_id`（按分块级联删除）索引；`knowledge_relations` 新增 `user_id+status`、`source_entity_id+status`、`target_entity_id+status` 和 `source_chunk_id` 索引，覆盖关系统计、图谱展开和按分块删除等高频查询。
- **避免重复索引**：`knowledge_entity_mentions.entity_id` 已由既有 `UNIQUE(entity_id, chunk_id)` 约束前缀覆盖，本次不重复创建，索引清单按实际查询模式精修。
- **新老库自动生效**：索引经 `initSchema()` 幂等执行，现有数据库下次启动会自动补齐，无需手动迁移；查询计划已确认 `WHERE user_id=? AND status=?` 由全表扫描转为覆盖索引查找。

#### 依赖治理
- **xlsx 依赖镜像化**：将 `xlsx` 从 `https://cdn.sheetjs.com/...tgz` 直链切换为 npm registry 官方镜像 `@e965/xlsx@^0.20.3`，恢复版本锁定与 integrity 校验；同步更新 `server/document-text.js`、`server/services/builtin-mcp-reports.js` 和相关测试的引用，API 调用方式保持不变。
- **uuid 漏洞清零**：`uuid` 由 `9.0.1` 升级至 `^11.1.1`，`npm audit` 由 1 条 moderate 告警降为 0 漏洞；选用 v11 而非 v12+ 以保留 CommonJS `require()` 兼容（v12+ 为 ESM-only，会导致现有 `require` 写法报错）。该漏洞仅影响 `v3/v5/v6` 且传入 `buf` 参数的场景，项目仅使用 `v4()`，本身不受影响，升级用于清除审计告警。

#### 文档、版本与验证
- **文档同步更新**：README、使用手册、项目汇报和更新日志同步记录知识图谱索引补齐、xlsx 依赖镜像化和 uuid 升级。
- **版本号启用**：应用版本升级至 `v0.0.105`，同步更新 `package.json`、`package-lock.json`、README、使用手册、项目汇报和更新日志。
- **回归验证通过**：`npm run check`（215 个 JS 文件语法、文本完整性与聊天资产校验）、`npm run lint` 与安全测试 `189/189` 均已通过；`@e965/xlsx` 与 `uuid` 的 `require` 与功能调用、知识图谱索引创建与查询计划均已实测确认。

## [v0.0.104] - 2026-06-11
### 应用中心与公文写作工作台体验升级

本版本在 `v0.0.103` 的角色与规范库基础上，新增面向具体业务场景的应用中心，并围绕“公文写作”做工作台级体验收口：顶部只负责参数和 AI 快捷动作，中间保留正文/对照编辑主空间，左侧固定资料库，右侧审改栏统一承载修改建议、批注、版本、规范检查和材料引用。整体目标是让公文起草、润色、局部修改、审校、版本对比和材料引用更接近真实办公写作流程。

#### 应用中心
- **业务应用入口沉淀**：新增应用中心工作区，用于承载公文写作等具体业务场景能力，避免把常用应用继续挤在左侧主导航中。
- **首页减重**：应用中心首页移除冗余说明区和筛选条，当前聚焦可用应用卡片，后续新增应用时继续沉淀到应用中心。
- **刷新状态保持**：用户停留在公文写作页面时刷新页面会回到公文写作工作台，不再自动退回应用中心首页。

#### 公文写作工作台
- **顶部工具栏重构**：顶部收敛为文种、规范库、材料来源、修改要求、正文/对照模式、AI 快捷动作和更多菜单；复制提示词、带入聊天、导出和送审/定稿版本入口统一归入“更多”。
- **布局职责清晰**：正文模式用于正式写作，对照模式用于原文和正文稿并排审改；移除独立“审校模式”，`审校`保留为动作按钮，点击后打开右侧规范检查。
- **左侧资料库固定**：左侧资料库保留大纲、材料、模板、历史和规范标签，去除不会影响正文区域的折叠开关，减少无效控件。
- **编辑区减负**：移除底部重复统计状态栏和正文区内重复的审校问题列表，让正文编辑区保留主要空间；对照模式下修复正文稿区域高度和滚动显示问题。
- **右侧审改栏收口**：右侧审改栏只保留结果类 Tab：修改建议、批注、版本、规范检查和材料引用；移除重复的 AI 助手 Tab，避免和顶部 AI 工具按钮混淆。
- **审改栏按钮适配**：右侧 Tab 按钮改为自适应换行，不再依赖横向滚动条显示。
- **版本对比优化**：版本面板增加历史版本模块，版本卡片展示名称、类型和时间；列表高度放宽，默认对比结果提示更明确，历史版本和差异结果区域分离。

#### 文档、版本与验证
- **文档同步更新**：README、使用手册、项目汇报和更新日志同步记录应用中心、公文写作工作台布局、审改栏、版本对比和刷新状态保持优化。
- **版本号启用**：应用版本升级至 `v0.0.104`，同步更新 `package.json`、`package-lock.json`、README、使用手册、项目汇报和更新日志。
- **回归验证通过**：`node --check client\chat\apps-workbench.js`、`npm run check` 和 `npm run lint` 已通过；本轮按要求未启动浏览器，由用户手动检查页面效果。

## [v0.0.103] - 2026-06-10
### 角色与规范库、工具归属可见与旧库迁移兼容

本版本将原“指令中心”正式改造为“角色与规范库”：规范资产不再只服务聊天里的 System Prompt 切换，而是可按角色、输出规范、任务方法和工作流节点分类管理，并按聊天、自由任务、工作流三个入口选择适用范围。同时补齐超级管理员查看其它用户工具时的所属用户信息，修复旧数据库升级时 `prompts` 新字段尚未补齐但 schema 阶段提前建索引导致服务启动失败的问题。

#### 角色与规范库
- **入口重命名与定位收口**：系统设置中的“指令中心”调整为“角色与规范库”，页面文案聚焦可复用角色、输出规范、任务方法和工作流节点提示。
- **规范资产结构化**：`prompts` 增加说明、类型、适用入口和更新时间字段，支持按类型、入口和关键词过滤；默认规范同步补齐角色、输出规范和任务方法分类。
- **三入口套用**：聊天输入区新增“套用角色与规范”入口，自由任务和工作流编排页新增“套用规范”按钮；聊天会写入当前会话系统提示词，自由任务会追加到补充说明，工作流会写入或创建大模型节点。
- **工作流节点兼容**：工作流套用规范时会优先复用已有 `agent.llm` 节点；角色类规范写入 `systemPrompt`，输出规范、任务方法和工作流节点类规范写入节点 `prompt`。

#### 工具归属与治理可见性
- **工具所属用户信息**：MCP 服务和工具缓存返回所属用户元数据，超级管理员查看其它用户服务或工具时可看到“所属：用户/全局”标识。
- **自由任务工具标签补齐**：自由任务工具目录保留 MCP 工具所属用户信息，多个数据库连接来源会汇总展示，便于区分同名或相似工具来源。

#### 旧库兼容与验证
- **旧数据库启动兼容**：初始化阶段会在 schema 建索引前补齐旧 `prompts` 表缺失的 `user_id`、`scope`、`created_at`、`updated_at`、`description`、`type` 和 `target_surfaces` 字段，避免 `no such column: type` 启动失败。
- **文档同步更新**：README、使用手册、项目汇报和更新日志同步记录角色与规范库、三入口套用、工具归属可见和旧库迁移兼容。
- **版本号启用**：应用版本升级至 `v0.0.103`，同步更新 `package.json`、`package-lock.json`、README、使用手册、项目汇报和更新日志。
- **回归验证通过**：`npm run check`、`npm run lint`、`node --test-name-pattern "role and policy library serves chat free task and workflow entry points" tests\security-agent.test.js`、`node --test-name-pattern "非 root 管理员不能创建全局提示词或共享 MCP 服务" tests\security-mcp\access-database.js`、`node -e "require('./server/db')"` 和本地 HTTP 静态资源检查均已通过；内置浏览器插件缺少 `scripts/browser-client.mjs`，未执行插件级点击验收。

## [v0.0.102] - 2026-06-10
### 第三方代码补全兼容、工具策略只读态与登录落点修复

本版本围绕生产部署后的接入兼容和权限体验做小版本修复：OpenAI 兼容网关补齐代码补全客户端常见的 `prompt`/`suffix` 风格请求兼容，避免第三方工具在 `/v1/chat/completions` 发送空 `messages` 时直接返回 400；工具策略页面继续允许普通管理员查看全局策略，但只读卡片不再展示“编辑”入口；用户从登录页完成登录后统一进入主页面，不再回到上次停留的工作区。

#### OpenAI 兼容接口
- **代码补全请求兼容**：`/v1/chat/completions` 在收到空 `messages` 且请求体包含 `prompt`、`input`、`prefix` 或 `suffix` 时，会自动构造用户消息交给后端模型调用，兼容 Continue 等代码补全客户端的请求形态。
- **补全参数透传补齐**：聊天补全转发继续保留 `stop`、`top_p`、`presence_penalty`、`frequency_penalty` 和 `max_completion_tokens` 等常见参数，减少第三方客户端因参数被丢弃产生的行为差异。
- **接口路径口径保持**：本轮不调整 Base URL 和 `/v1` 接入说明，现有 OpenAI-compatible 接入地址继续按当前配置使用。

#### 权限与页面体验
- **工具策略只读态收口**：非内置 `admin` 的管理员仍可查看工具策略页面，但卡片不再显示“编辑”按钮，避免给只读用户提供不可执行的操作入口。
- **登录落点修复**：用户通过登录表单成功登录后进入主页面；已有登录态刷新页面时，仍可保留原有工作区恢复能力。

#### 文档、版本与验证
- **文档同步更新**：README、使用手册、项目汇报和更新日志同步记录第三方代码补全兼容、工具策略只读态和登录落点修复。
- **版本号启用**：应用版本升级至 `v0.0.102`，同步更新 `package.json`、`package-lock.json`、README、使用手册、项目汇报和更新日志。
- **回归验证通过**：`node scripts\check_syntax.js`、`node tests\security-chat.test.js`、指定文件 ESLint 和 `git diff --check` 已通过。

## [v0.0.101] - 2026-06-10
### 知识库范围选择、专题库标签联动与整理闭环优化

本版本在 `v0.0.100` 的专题库/标签范围检索基础上继续收口知识库体验：聊天输入区只有启用知识库时才显示范围选择；选中某个专题库后，标签选择只展示该专题库内已经使用的标签，形成类似一级/二级菜单的联动关系；知识库列表、召回测试、聊天底部可用资料提示和回答顶部命中提示统一使用同一套范围条件，避免“显示选了某个范围、实际仍查全库”的不一致。

#### 范围选择与显示一致
- **知识库范围按需显示**：普通聊天输入区只有打开“知识库”后才展示专题库和标签选择，关闭知识库时不再占用输入区空间。
- **专题库到标签二级联动**：选择专题库后，后续标签下拉只显示该专题库内的标签；保持“全部专题库”时才展示全局标签范围，避免选择到不属于当前专题库的标签。
- **提示口径统一**：聊天输入区的可用资料提示、回答顶部的知识库命中提示、召回调试和知识库工作台列表统一展示当前专题库/标签范围，减少前后显示不一致。

#### 知识库整理与创建闭环
- **专题库和标签先创建后使用**：知识库工作台补齐“新建专题库”和“新建标签”入口，上传、筛选和整理文档时复用已经创建的名称。
- **表格只展示归属信息**：知识库文档表格中的专题库和标签改为归属展示，不再把每一行做成可直接编辑的下拉框，降低误操作。
- **整理弹窗统一样式**：整理文档弹窗使用全局统一的弹窗、输入框、下拉框和复选框样式；专题库通过下拉选择，标签通过复选框选择，不再让用户手工填写标签名称。

#### 检索边界与旧库兼容
- **选中专题库只查该专题库**：聊天、召回调试、关键词兜底、向量召回、最近分块兜底和 Graph-RAG 查询都带入当前专题库/标签范围，选择某个专题库时不会再引用其它知识库资料。
- **标签接口支持范围参数**：`/rag/tags` 支持按 `collectionId` 返回标签，前端按专题库缓存标签列表并在上传、整理、列表筛选、召回测试和聊天输入区复用。
- **旧数据库启动兼容**：继续补齐旧库中 `knowledge_docs.collection_id` 可能缺失时的初始化兼容，避免 schema 阶段提前创建集合索引导致 `no such column: collection_id` 启动失败。

#### 文档、版本与验证
- **文档同步更新**：README、使用手册、项目汇报和更新日志同步记录知识库分类管理、二级联动、整理弹窗和范围检索边界。
- **版本号启用**：应用版本升级至 `v0.0.101`，同步更新 `package.json`、`package-lock.json`、README、使用手册、项目汇报和更新日志。
- **回归验证通过**：`node tests\security-rag.test.js`（57/57）、`npm run check`、`npx eslint server\services\rag-documents.js server\rag.js client\chat\rag-documents.js client\chat\rag.js client\chat\app-workspaces.js tests\security-rag\documents.js`、`node -e "require('./server/db'); console.log('db init ok')"` 和相关 `node --check` 已通过；浏览器实测待本地服务启动审批恢复后补充。

## [v0.0.100] - 2026-06-09
### 知识库专题库与标签范围检索

本版本围绕“知识库会越来越多，默认把全部知识库都带入召回会浪费且容易串域”的问题做两阶段收口：第一阶段新增专题库/集合管理，让资料可以按业务域、项目或部门分组；第二阶段补齐文档标签，让同一专题库内还可以按场景、阶段、系统或关键词进一步过滤。聊天、召回调试、Graph-RAG 和后台缓存都会尊重同一套范围条件，默认仍可使用全部知识库，但用户可以按需缩小引用范围。

#### 专题库范围
- **集合表与文档归属**：新增 `knowledge_collections`，文档支持 `collection_id`，上传时可选择已有专题库或新建专题库，后续也可在知识库列表中调整归属。
- **列表与召回筛选**：知识库工作台新增专题库筛选，文档列表展示所属专题库；RAG 调试和普通聊天均可选择只引用指定专题库。
- **Graph-RAG 同步收口**：知识图谱实体、关系与来源文档查询会按专题库范围过滤，避免图谱关系跨专题库误召回。

#### 标签范围
- **文档多标签**：新增 `knowledge_doc_tags`，文档支持多个标签；上传弹窗、文档列表和标签接口可维护标签。
- **标签筛选与引用**：知识库列表、召回调试和普通聊天新增标签范围选择，可在专题库之外进一步缩小召回候选。
- **图谱标签过滤**：Graph-RAG 同步支持标签范围，只有命中标签文档的实体和关系会参与当前范围查询。

#### 检索、缓存与验证
- **统一作用域传递**：聊天接口、调试接口、关键词兜底检索、FTS/LIKE、向量候选合并、最近分块兜底和图谱查询统一接收 `ragScope`，避免前后端作用域不一致。
- **缓存键纳入范围**：RAG 召回缓存键新增专题库和标签范围，防止“全库结果”和“局部库结果”互相命中。
- **旧库迁移兼容**：修复旧数据库启动时 `knowledge_docs.collection_id` 尚未迁移、但 schema 初始化提前创建集合索引导致的 `no such column: collection_id` 错误；集合索引改由迁移阶段在补列后创建。
- **阶段验证通过**：第一阶段 `npm run check` 与 `node tests\security-rag.test.js`（54/54）通过；第二阶段 `node scripts\check_syntax.js`、`npm run check` 与 `node tests\security-rag.test.js`（55/55）通过；最终回归 `npm run check`、`npm run lint`、`node tests\security-rag.test.js`（56/56）和 `node tests\security-chat.test.js`（51/51）通过；本轮按要求未打开浏览器验证。
- **版本号启用**：应用版本升级至 `v0.0.100`，同步更新 `package.json`、`package-lock.json`、README、使用手册、项目汇报和更新日志。

## [v0.0.99] - 2026-06-09
### 聊天输入状态轻量化、知识库关键词兜底与工具箱限流提示修复

本版本围绕主页普通对话中的知识库和工具箱体验做可用性收口：输入区开启状态不再显示在输入框中部，而是跟随知识库/工具箱按钮以更小的提示标签展示；知识库在向量服务鉴权失败、限流或短时不可用时，会保留文本分片并回退到关键词检索；工具箱调用也区分工具规划模型限流和真实工具执行失败，避免把上游 `429` 原始错误直接暴露给用户。

#### 聊天输入区
- **状态提示贴近按钮**：知识库和工具箱开启后的提示移动到对应按钮右侧，不再占用输入框中部空间。
- **提示文案简化**：就绪提示收敛为“4 份资料可用”“27 个工具可用”等计数式短文案，减少重复说明。
- **紧凑标签样式**：状态标签缩小字号、间距和内边距，并增加收缩与省略规则，避免挤压发送按钮和右侧工具区。

#### 知识库降级可用
- **向量失败不阻断索引**：文档分片生成向量失败时仍会写入文本分片和全文检索内容，资料可继续作为关键词知识参与回答。
- **查询自动关键词兜底**：普通聊天和召回调试在查询向量生成失败时，会回退到 FTS/LIKE 候选和关键词评分，而不是直接返回空知识库。
- **语义召回边界更清晰**：知识库基础可用性不再完全依赖向量服务，但高质量语义召回仍建议配置可用的 Embedding 模型；对话模型不应直接作为向量模型使用，除非服务商明确提供 embeddings 接口并返回向量数组。

#### 工具箱错误提示
- **规划与执行分阶段提示**：普通聊天工具箱调用区分“工具规划模型请求失败”和“工具服务执行失败”，定位更准确。
- **429 限流提示中文化**：工具规划模型被上游限流时提示“工具规划模型暂时被限流，已跳过本轮工具调用，请稍后重试”，不再展示 `Request failed with status code 429`。
- **避免编造工具结果**：工具规划或执行失败时会向主回答注入失败说明，要求模型不要编造工具调用结果。

#### 文档、版本与验证
- **版本号启用**：应用版本升级至 `v0.0.99`，同步更新 `package.json`、`package-lock.json`、README、项目汇报和更新日志。
- **回归验证通过**：`npm run check`、`npm run lint`、`node tests\security-rag.test.js`（53/53）、`node tests\security-mcp.test.js`（23/23）和 `node tests\security-chat.test.js`（51/51）已通过。

## [v0.0.98] - 2026-06-09
### 工具策略卡片化治理与编辑弹窗体验收口

本版本继续围绕系统设置中的“工具策略”页面做界面和治理体验收口：去除左侧滚动工具包列表，把系统工具和全局工具服务直接按工具卡片平铺展示；卡片样式对齐工具箱页面，工具启停继续放在卡片开关上；编辑按钮打开居中的治理弹窗，只保留风险等级、审批要求和适用说明等治理字段。

#### 工具策略页面
- **标题收敛为工具策略**：系统设置页面标题从“全局工具策略”调整为“工具策略”，与左侧设置菜单和实际入口保持一致。
- **统计口径简化**：工具列表标题行只展示工具总数，去除容易误解的“工具包”数量，避免把 24 个工具和 15 个工具包并列显示造成歧义。
- **工具卡片直接平铺**：移除左侧滚动列表和右侧单包详情区域，所有系统工具和全局工具服务直接以卡片形式铺在页面上，卡片信息结构参考工具箱页面。
- **启停开关留在卡片**：每个工具卡片右上角保留轨道式启用开关，编辑弹窗不再重复展示工具启用区域，减少同一状态在两个位置维护。
- **说明文字左对齐**：卡片上的工具说明统一靠左显示，避免中英文混排时出现居中后难以扫读的问题。
- **每页 18 个工具**：工具策略页统一按 18 个工具分页，并使用全局统一的分页控件；翻页只切换前端缓存数据，刷新、保存和启停后再重新拉取后端状态。

#### 治理编辑弹窗
- **弹窗居中显示**：点击卡片“编辑”后，治理面板以页面居中的弹窗展示，不再挤在页面局部区域内。
- **标题左对齐**：弹窗顶部只展示工具名称和类型/包名信息，去除“治理面板”字样和右上角关闭按钮，面板标题统一靠左。
- **治理字段减重**：弹窗只保留风险等级、审批要求和适用说明；工具启用状态由卡片开关负责。
- **表单标签左对齐**：风险等级、审批要求和适用说明等标签统一靠左显示，减少视觉跳动。
- **风险下拉框高度优化**：风险等级下拉框高度收缩到常规表单控件节奏，避免弹窗内第一项显得过高。

#### 文档、版本与验证
- **版本号启用**：应用版本升级至 `v0.0.98`，同步更新 `package.json`、`package-lock.json`、README、使用手册、项目汇报和更新日志。
- **回归验证通过**：`npm run check`、`npx eslint client/chat/tool-policy.js` 和 `node tests\security-chat\rendering-streams.js` 已通过，确认卡片化策略页、治理弹窗和资源加载链路未破坏现有安全渲染。

## [v0.0.97] - 2026-06-09
### 工具箱命名收口、全局工具策略与模型管理权限修复

本版本围绕工具入口、工具治理和模型管理做产品边界收口：将用户侧“能力库”统一调整为更直观的“工具箱”，去掉工具箱页面中重复的引导、说明和带入聊天动作；把工具级治理从普通工具卡片中移到系统设置的全局工具策略页；同时修复模型测试、模型删除和工具策略入口中的权限与交互问题。

#### 工具箱体验收口
- **导航命名统一为工具箱**：主页导航继续保持“知识库、智能体、工具箱”等三字菜单节奏，用户侧文案从“能力库”收口为“工具箱”，更贴近日常理解。
- **移除重复说明文字**：工具箱页面不再展示“三步用好能力库”“可以回到聊天使用”“健康状态良好”等常驻提示，系统工具和个人连接卡片只保留真正有助于判断的名称、状态和操作。
- **工具卡片信息减重**：系统工具卡片去掉重复用途说明和“带入聊天”按钮，工具是否参与聊天改由聊天输入区的工具箱开关控制，减少同一动作在多个入口重复出现。
- **工具列表弹窗简化**：普通工具列表聚焦工具名称、用途和启用状态，不再把风险等级、审批和适用说明暴露给普通用户，避免工具弹窗变成复杂治理表单。

#### 全局工具策略
- **治理入口移到系统设置**：底层工具治理能力、后端接口和智能体审批逻辑继续保留，但管理入口放入系统设置中的“工具策略”，适合作为管理员/高级治理入口。
- **只管理全局工具**：工具策略页面只展示系统工具和全局工具服务，用户自己配置的数据库连接、报表目录、消息通知和个人外部服务不在此处治理，避免管理员误改个人连接。
- **权限边界收紧**：普通管理员可以查看全局工具策略；只有内置系统管理员可以修改全局工具启停、风险等级和审批要求。个人工具仍按所有者权限和运行时安全策略生效。
- **页面布局优化**：全局工具策略页改为紧凑的工具包列表和详情表格布局，左侧列表在合理高度内滚动，右侧表格按内容收缩，避免页面超长、右侧大面积空白和无意义滚动条。

#### 模型配置与删除
- **模型测试权限修复**：私人模型只允许所有者测试；全局模型允许管理员测试，普通用户不会看到全局模型测试按钮，系统管理员也不会看到他人私人模型测试按钮。
- **测试按钮按权限显示**：前端渲染模型列表时会隐藏当前用户无权测试的按钮，避免点击后出现 `403 Forbidden`。
- **模型删除改为软删除**：删除模型时不再直接物理删除记录，而是标记为 `deleted` 并清理默认模型引用，避免历史会话、任务或监控引用导致 `500 Internal Server Error`。

#### 工程与验证
- **工具策略入口修复**：系统设置页“工具策略”菜单按钮恢复可点击，相关脚本和设置页资源加载顺序补齐。
- **全局策略接口收口**：工具策略相关接口改为只返回和修改全局工具包，个人工具包不会通过全局策略接口暴露。
- **版本号启用**：应用版本升级至 `v0.0.97`，同步更新 `package.json`、`package-lock.json`、README、使用手册、项目汇报和更新日志。
- **回归验证通过**：`node tests\security-auth.test.js`、`node tests\security-mcp.test.js`、`node tests\security-chat.test.js` 和 `npm run check` 已通过；本轮未使用登录态浏览器截图做视觉验收。

## [v0.0.96] - 2026-06-09
### 知识库工作台体验减重与知识图谱顶部工具区合并

本版本围绕知识库页面顶部区域和知识图谱工作台做体验收口：去掉重复引导和常驻提示，把上传交互改为弹窗式拖拽/选择文件，把概览、质量诊断和知识图谱筛选区整理成更轻、更紧凑的工作台布局，减少用户在管理文档、测试召回和查看图谱时的视觉负担。

#### 知识库顶部区域
- **移除重复引导卡片**：知识库页面不再常驻展示“三步用好知识库”和“可以开始提问”快捷卡片，避免与顶部工具栏里的召回测试、知识图谱和聊天知识库开关形成重复。
- **上传入口改为弹窗**：点击“上传文档”会打开上传弹窗，弹窗内提供拖拽上传和点击选择文件两种方式；页面内不再嵌入拖拽条，也不再因为上传区域改变列表布局。
- **上传后自动收起**：文档加入后台索引队列后会关闭上传弹窗并刷新知识库列表，保持上传流程轻量闭环。

#### 概览与质量诊断
- **概览指标统一承载**：知识库概览条合并文档数、就绪数、处理中、失败、分块、源文件、队列、评分、反馈、实体和关系等关键指标，避免“实体/图谱实体”“就绪/可用”等同类信息左右重复。
- **质量诊断按需显示**：质量诊断区域不再常驻展示泛提示和 0 值指标；只有出现异常、停用、空分块或具体问题文档时才显示处理提示。
- **表格节奏统一**：知识库文档表格继续保持每页 15 条，表格底部到分页分割线的间距调整为与其它数据表一致的 10px 节奏。

#### 知识图谱工作台
- **顶部工具区合并一行**：知识图谱页面把统计摘要、实体搜索、类型筛选、质量筛选、搜索按钮、图谱查询输入框和“查询图谱”按钮合并到同一行显示。
- **查询结果独立展示**：Graph-RAG 查询结果继续保留在顶部工具区下方的独立结果区域，避免查询返回内容时撑乱筛选工具栏。
- **响应式兜底保留**：窄屏下知识图谱顶部工具区仍会自动改为单列布局，保证搜索、筛选和查询控件不重叠。

#### 文档、版本与验证
- **版本号启用**：应用版本升级至 `v0.0.96`，同步更新 `package.json`、`package-lock.json`、README、项目汇报和更新日志。
- **回归验证通过**：`node tests\security-chat.test.js`（51/51）、`npm run check` 和 `npm run lint` 已通过；本地浏览器仍停留在登录页，本轮未使用用户账号进行登录后截图验证。

## [v0.0.95] - 2026-06-09
### 智能体任务记录、DAG 详情与自由任务模板保存体验收口

本版本围绕智能体任务记录和自由任务模板保存路径做轻量体验收口：任务列表默认每页 15 条，工作流任务详情不再同时展示“工作流节点”和重复的“执行步骤”，并把“保存为模板”前置到快速任务主面板，减少用户在配置面板之间来回寻找入口。

#### 任务记录分页
- **每页 15 条记录**：智能体任务记录表格前端请求、后端路由默认 limit 和服务层兜底默认值统一改为 15，分页控件结构和统一样式保持不变。
- **翻页行为保持一致**：列表序号继续按当前页与服务端返回 limit 计算，筛选、搜索和空页回退逻辑不变。

#### DAG 任务详情
- **去除重复执行明细**：当任务存在 DAG 节点记录时，详情页只保留工作流节点作为执行明细，不再重复渲染通用执行步骤和工具统计区域。
- **节点结果可读化补齐**：DAG 节点卡片会直接展示结构化查询表格、图表摘要或可渲染输出，大模型节点继续优先展示 Markdown 正文，原始输入输出仍保留在折叠项中便于排查。

#### 自由任务模板
- **保存入口前置**：快速任务标题区新增“保存为模板”按钮，用户填写目标和参数后可直接保存，不必先打开“模板与计划”。
- **模板库入口保留**：模板与计划面板内保留“保存当前任务”入口，继续用于模板库管理场景；两个入口复用同一保存逻辑并避免重复绑定。
- **模板来源说明**：自由任务模板来自用户手动保存的当前目标、补充说明、上下文和模型参数；模板库继续按现有权限展示个人模板以及 admin 可见范围内的共享模板。

#### 文档、版本与验证
- **版本号启用**：应用版本升级至 `v0.0.95`，同步更新 `package.json`、`package-lock.json`、README、项目汇报和更新日志。
- **回归验证通过**：`node tests\security-agent.test.js`（34/34）和 `npm run check` 已通过；本地页面 DOM 验证确认快速任务头部已出现“保存为模板”入口。

## [v0.0.94] - 2026-06-09
### 能力库工具级治理、数据库安全与配置诊断升级

本轮围绕能力库页面现有工具与配置完整性做企业级补强：保留系统能力、数据库、报表文件、IM 通知和外部能力服务的原有入口，同时把能力库从“能连接、能调用”升级为“可治理、可诊断、可审计地调用”，并明确能力可服务于聊天、自由任务和工作流。

#### 工具级治理
- **工具级启停**：能力包下的单个工具可独立启用或停用，列表展示和真实调用路径都会尊重治理配置，避免只能整包停用。
- **风险与审批配置**：工具动作弹窗新增风险等级、是否需要审批和适用说明，便于把高风险动作与日常动作区分治理。
- **能力调用链路收口**：聊天、自由任务和工作流通过同一套 MCP 工具过滤逻辑获取可用工具，已停用工具不会继续暴露给模型或智能体。

#### 数据库能力治理
- **表/字段白名单**：数据库连接新增允许访问表/集合、字段白名单配置；启用字段白名单后，SQL 必须使用明确字段名，降低误查敏感数据风险。
- **敏感字段脱敏**：查询、分组统计和 MongoDB 采样/聚合结果会按敏感字段配置进行返回前脱敏。
- **查询安全提示**：数据库结果新增治理状态与轻量成本估计，包含查询超时、限制行数、涉及表和行级权限提示，帮助用户判断查询边界。

#### 配置诊断
- **报表目录诊断**：报表文件能力新增配置诊断，可检查目录可读性并预览可读文件。
- **IM 测试发送**：IM 通知能力新增测试发送、最近投递状态和失败重试提示，方便上线前确认目标白名单和 Webhook 配置。
- **外部能力健康检查**：外部能力服务新增健康检查 URL、调用超时、认证模式、工具 Schema 校验和示例提示词配置。

#### 文档、版本与验证
- **版本号启用**：应用版本升级至 `v0.0.94`，同步更新 `package.json`、`package-lock.json`、README 和更新日志。
- **回归验证通过**：`npm run check`、`npm run lint` 和 `node tests\security-mcp.test.js`（21/21）已通过；按用户要求，本轮未启动浏览器验证。

## [v0.0.93] - 2026-06-09
### 自由任务定位与工作流孵化闭环

本轮围绕“普通智能体是否还有存在必要”的合理性分析做产品收口：保留普通入口，但将其明确定位为自由任务和快速任务，用于临时分析、排查、探索和目标参数复用；工作流编排则作为企业生产任务入口，承载编排、发布、版本、计划任务和审计复用。两者通过“从自由任务生成工作流草稿”连接起来，让临时任务可以自然沉淀为标准流程。

#### 自由任务与工作流定位
- **普通入口改为自由任务**：前端工作台标题、说明、模板区、历史区和相关提示从“智能体”调整为“自由任务 / 快速任务”，避免与工作流编排抢定位。
- **工作流入口强化生产语义**：工作流编排页强调编排、发布、版本、计划运行和审计复用，适合企业稳定流程和生产任务。
- **企业部署默认建议**：运行前提示区区分自由任务与工作流任务；自由任务适合分析、排查和临时处理，稳定流程建议转成工作流草稿后发布运行。

#### 自由任务生成工作流草稿
- **新增后端草稿接口**：`POST /api/agents/runs/:id/workflow-draft` 会读取自由任务的工具执行步骤，生成顺序工具节点和最终大模型总结节点；工作流任务本身不再重复生成草稿。
- **新增前端详情动作**：自由任务详情新增“生成工作流草稿”按钮，成功后直接打开工作流编排页，并加载可检查、可保存、可发布的草稿画布。
- **审计与错误兜底**：生成草稿会写入审计日志；非自由任务、无效任务和异常节点会给出明确错误提示。

#### 模板、任务记录与验证
- **模板体系分层**：自由任务模板用于复用目标、参数、上下文和模型路由；工作流模板用于复用步骤、节点和依赖结构。
- **统一记录分层展示**：后台继续保留统一任务记录，前端新增“全部类型 / 自由任务 / 工作流任务”筛选，并在表格中展示任务类型。
- **回归验证通过**：`npm run check`、`npm run lint`、`node tests\security-agent.test.js`（32/32）、`node tests\security-rag.test.js`（51/51）、`node tests\security-mcp.test.js`（19/19）和 `node tests\security.test.js`（173/173）已通过；按用户要求，本轮未再启动浏览器验证。

## [v0.0.92] - 2026-06-09
### 知识图谱质量治理、待确认关系与 Graph-RAG 查询体验升级

本轮围绕知识图谱做完整升级：把低可信抽取关系从“直接进入可用图谱”改为“待确认后参与正式召回”，并补齐图谱质量评分、重复实体建议、关系状态筛选、来源片段查看、图谱查询调试和智能体工具调用，让知识库中的关系型知识更可审计、更可修正，也更容易被普通用户和智能体可靠使用。

#### 后端图谱治理
- **关系状态治理**：`knowledge_relations` 新增 `status` 字段，低于可信度阈值的共现关系自动进入 `pending`，确认后提升为 `active`，删除关系继续使用 `deleted` 软状态保留审计边界。
- **图谱质量评分**：图谱概览新增 `qualityScore`、等级、孤立实体、低可信关系、缺来源关系、待确认关系和重复实体组统计，并返回治理建议。
- **重复实体建议**：基于实体类型、规范化名称前缀和别名推断疑似重复实体组，为后续合并校准提供候选。
- **Graph-RAG 查询接口**：新增 `/rag/graph/query`，按问题返回相关实体、关系路径、来源文档、分块 ID 和回答提示；正式聊天上下文仍只使用 `active` 关系，避免未确认关系污染回答。

#### 前端图谱工作台
- **筛选与审核闭环**：知识图谱工作台新增实体质量筛选、关系状态筛选、关系类型筛选和最低可信度过滤；pending 关系可在关系卡片中直接确认。
- **来源证据可见**：关系卡片展示关系状态、可信度、来源文档和来源分块片段，方便用户判断关系是否应保留、编辑或删除。
- **图谱查询调试**：工作台新增图谱查询输入框，可直接查看命中的实体、关系路径、来源和回答提示，便于排查 Graph-RAG 召回。
- **移动端适配**：新增筛选栏、查询面板和来源片段的响应式样式，避免全屏图谱在窄屏下拥挤或重叠。

#### 智能体与测试
- **智能体图谱工具**：内置智能体工具新增 `knowledge.graph.query`，智能体可在当前用户权限内查询知识图谱关系路径，用于回答职责、依赖、归属和影响类问题。
- **回归测试补齐**：新增低可信关系 pending、确认转 active、质量摘要、关系筛选、图谱查询和重复实体建议的 RAG 回归用例；`node scripts\check_syntax.js` 与 `node tests\security-rag.test.js`（51/51）已通过。
- **版本号启用**：应用版本升级至 `v0.0.92`，同步更新 `package.json`、`package-lock.json`、README、使用手册和项目汇报中的当前版本说明。

## [v0.0.91] - 2026-06-09
### 知识库、能力库与智能体治理体验增强

本轮围绕知识库、能力库和智能体的实际使用闭环继续升级：在不额外探测外部服务、不放宽权限边界的前提下，为知识库增加更稳的召回缓存版本域和质量信号，为能力库增加健康评分与治理建议，为智能体运行前预检增加可读的就绪度、上下文预算和 MCP 风险提示，让用户在运行前就能看懂“哪里可用、哪里需要处理”。

#### 知识库优化

- **召回缓存防陈旧**：RAG 召回缓存键新增知识库版本域，纳入就绪启用文档数、分块数、文档更新时间、知识图谱更新时间和检索参数，文档、图谱或召回配置变化后不会继续命中过期结果。
- **质量报告信号扩展**：知识库质量报告新增质量分、就绪率、反馈有用率、陈旧就绪文档、知识图谱实体/关系覆盖和治理建议，便于区分“资料不足”“索引待刷新”“反馈效果偏低”等问题。
- **工作台体验增强**：知识库质量面板直接展示质量分、就绪率、反馈有用率、异常/停用/空分块文档和图谱实体数，减少用户在多个区域之间来回排查。

#### 能力库优化

- **能力健康评分**：能力库治理接口新增健康评分、健康等级、活跃率、调用失败率、异常服务和未检查服务统计，基于本地配置与调用日志计算，不主动扩大外部网络探测面。
- **治理建议可视化**：能力库工作台展示健康分、调用失败率和最多 3 条治理建议，帮助用户优先处理异常连接、未刷新工具缓存或失败率偏高的能力。
- **兼容既有治理数据**：新增健康对象同时保留原有 governance summary 字段，避免破坏已有前端和测试依赖。

#### 智能体优化

- **运行前就绪度**：智能体预检新增 readinessScore，综合模型、目标、工具、Token 预算、审批等待风险和 MCP 健康情况给出运行前可读评分。
- **上下文预算提醒**：预检会估算输入 Token，当任务目标、补充说明和上下文来源可能超过预算时提前告警，降低运行后才失败或回答被截断的概率。
- **MCP 风险提示**：预检摘要新增 MCP 服务总数、启用数、异常数、未检查数，并对异常或未检查能力给出警告，让智能体用户在点击运行前看到能力库风险。

#### 文档、版本与验证

- **版本号启用**：应用版本升级至 `v0.0.91`，同步更新 `package.json`、`package-lock.json`、README、使用手册和项目汇报中的当前版本说明。
- **验证覆盖**：新增知识库缓存版本域、知识库质量信号和智能体预检治理回归用例；`npm run check`、`npm run lint`、`node tests\security-rag.test.js`（50/50）、`node tests\security-mcp.test.js`（19/19）、`node tests\security-agent.test.js`（31/31）和 `node tests\security.test.js`（171/171）均已通过；本轮按用户要求未打开浏览器验证。

## [v0.0.90] - 2026-06-08
### 知识库命名回归、入口点击修复与 lint 清理补丁

本次补丁针对知识库前端术语、知识库工作台入口和工程 lint 噪音做集中收口：把近期过度口语化的按钮和面板名称恢复为原先更稳定的产品命名，修复“检索配置”和“召回测试”入口点击异常，并把历史遗留的 ESLint warning 清理到零，避免后续变更被旧提示淹没。

#### 知识库前端名称回归

- **恢复知识库核心入口命名**：将“试问一下”恢复为“召回测试”，“资料关系”恢复为“知识图谱”，“高级设置”恢复为“检索配置”，保持知识库页面、弹窗、按钮和测试断言里的名称一致。
- **恢复检索与索引相关术语**：将“内容段”恢复为“分块”，将匹配参数恢复为“阈值 / 命中 / 候选”，将“重新学习”等口语化表达收口为“重新索引”“批量重建”等原有知识库语义。
- **同步结果与空状态文案**：召回测试 loading、失败提示、文档列表空状态、知识图谱实体/关系统计和聊天流式状态文案同步回归，避免同一流程里新旧术语混用。

#### 知识库入口点击修复

- **修复“检索配置”入口时序问题**：知识库工作台打开时会先确保 `admin-settings.js` 已加载，再绑定检索配置弹窗事件，避免脚本尚未加载时按钮点击无响应。
- **修复“召回测试”相关绑定被替换的问题**：检索配置弹窗事件绑定不再使用 `cloneNode` 替换按钮，改为带标记的幂等 `addEventListener`，避免已有 DOM 状态或后续监听被意外丢失。
- **补齐入口回归断言**：安全聊天渲染测试新增对 `ensureAdminSettingsScript`、检索配置事件幂等绑定、召回测试/知识图谱/检索配置旧名称的断言，防止同类问题回退。

#### ESLint warning 治理

- **补齐前端拆分脚本全局声明**：在 `eslint.config.js` 中补齐 agent、DAG、MCP、sidebar、stats、engine、render 等前端拆分脚本共享的全局名，解决跨文件浏览器脚本被误判为 `no-undef` 的历史问题。
- **收窄 client unused 检查范围**：`client/**/*.js` 的 `no-unused-vars` 改为只检查局部变量，放过旧式浏览器脚本需要暴露给其他文件使用的顶层函数和常量，同时继续保留函数内部 unused 检查。
- **清理过期禁用注释和真实 unused**：移除不再需要的 `no-unused-vars` 禁用注释；仍依赖跨文件全局的文件只保留更窄的 `no-undef` 禁用；同时移除 `tests/security-auth.test.js` 中未使用的 `http` 解构。

#### 验证通过

- **lint 干净通过**：`npm run lint` 通过，且不再输出 warning。
- **项目检查通过**：`npm run check` 通过。
- **安全回归通过**：`node tests\security-chat.test.js` 通过 51/51，`node tests\security-auth.test.js` 通过 20/20。

### 知识库与能力库傻瓜式使用闭环升级

本版本只围绕知识库、能力库和普通聊天中的使用闭环继续做强：把“上传资料 / 启用能力 / 回到聊天 / 看见调用依据”这些原本需要用户自己理解的步骤，收口为更直观的中文引导、可点击动作和回答内状态追踪。普通用户不需要懂 RAG、MCP、索引、分块或工具调用，也能按页面提示完成“先准备、再试问、最后聊天使用”的完整流程。

#### 知识库体验升级

- **三步式知识库引导**：知识库工作台新增“上传文档 → 测试问题 → 聊天时打开知识库”的首屏引导，配合拖拽上传区域，普通用户进入页面后能直接知道下一步该做什么。
- **上传和学习语义中文化**：知识库按钮、空状态、进度、失败重试、批量重新学习、内容段、资料关系、删除审计等文案统一改为更接近用户语言的中文表达，减少“索引、分块、召回”等技术词暴露。
- **就绪后双动作闭环**：知识库有可用资料时，下一步卡片同时提供“试问一下”和“打开聊天知识库”；用户可以先验证资料命中效果，也可以一键回到聊天并自动点亮知识库开关。
- **试问结果更可操作**：试问弹窗新增示例问题、命中结果分组、可用性判断和“用这个问题去聊天”动作；命中足够时提示可直接带着问题回聊天，命中不足时提示换问法或补充资料。
- **质量诊断更像使用提醒**：知识库质量面板把异常、停用、空资料、重新学习和聊天开关提示放在同一个诊断区域，帮助用户发现“资料已上传但聊天没引用”的常见原因。
- **聊天回答显示引用来源**：普通聊天命中知识库后，会在回答上方保留“知识库已找到 N 条可引用资料”并展示最多 3 个来源文件名，用户能直接看到本轮回答是否真正依据资料。

#### 能力库体验升级

- **三步式能力库引导**：能力库工作台新增“启用能力 → 确认可用动作 → 聊天中授权使用”的首屏引导，并在治理面板提供“打开聊天能力库”入口。
- **系统能力与个人能力更清楚**：图表生成、报告编排、文档解析、数据处理、格式转换、报表文件、消息通知、数据库连接等能力卡片补齐中文用途、适用问题和动作数量说明。
- **配置表单更适合普通用户**：数据库、报表目录、IM 通知和外部能力服务的配置表单补齐中文占位、提示和推荐默认值；SQLite、普通数据库、报表目录等场景给出更明确的填写说明。
- **动作示例可直接带回聊天**：能力动作弹窗新增“可以这样问”的示例问题，点击后自动回到聊天、打开能力库并把示例填入输入框，减少用户不知道如何提问的问题。
- **能力调用状态更可读**：聊天 MCP 调用事件新增 `actionName`、`toolName`、`serverName` 等字段，前端优先展示“能力库：某服务 / 执行只读 SQL、生成图表、查询表格数据”等中文动作名，不再让普通用户只看到 `db.*` 这类技术名。

#### 普通聊天闭环升级

- **输入区工具状态条**：聊天输入区新增知识库/能力库状态条，开启后会自动检查当前是否有就绪资料或可用工具，并给出“打开知识库”“打开能力库”等可点击处理入口。
- **回答内保留依据和能力状态**：普通聊天流式回答会保留知识库命中、知识库未命中、能力库规划、能力库调用、调用完成、缺少工具和调用失败等状态提示；失败或缺少配置时可直接打开对应工作台处理。
- **知识库与能力库一键回聊天**：知识库和能力库工作台都支持从下一步卡片一键回到聊天并点亮对应开关，形成“准备 → 验证 → 使用”的闭环。

#### 工程拆分与中文编码治理

- **后端智能体运行时拆分**：`server/services/agent-runtime.js` 拆出 `agent-tool-runtime.js`、`agent-dag-runtime.js`、`agent-streaming-runtime.js`，降低单文件体积，并保持拆分文件中文注释和中文提示。
- **安全测试按领域拆分**：`security-rag.test.js`、`security-chat.test.js`、`security-mcp.test.js` 拆分为知识库、聊天渲染/会话/模型/缓存/工具、MCP 访问/服务/聊天上下文/配置等子文件，入口文件保持兼容。
- **中文编码逐文件检查**：按本轮新拆分和修改文件逐个检查中文注释、提示和测试文本，未使用脚本批量修复，确保新增拆分文件没有中文编码错误。

#### 测试与验证

- **回归测试补齐**：新增知识库引用来源提取、聊天回答状态追踪、能力库中文动作名、知识库/能力库下一步动作、拆分入口加载等断言。
- **浏览器实测通过**：本地页面实测知识库首屏双动作、能力库打开聊天闭环、聊天输入区状态条和控制台错误；知识库“打开聊天知识库”会回到聊天并显示可参与回答资料数。
- **验证通过**：`node tests\security-chat.test.js`、`node tests\security-rag.test.js`、`node tests\security-mcp.test.js` 和 `npm run check` 均已通过。
- **版本号启用**：应用版本升级至 `v0.0.90`，同步更新 `package.json`、`package-lock.json`、README 和项目汇报中的当前版本展示与说明。

## [v0.0.89] - 2026-06-06
### 工作流向导运行时兼容与大模型结果展示优化

本版本继续收口工作流 DAG 的实际使用体验：修复拆分后的统计图向导和数据库参数辅助在工具列表作用域上的运行时错误，并统一大模型节点在“节点卡片”和“执行步骤”中的结果展示方式，让用户默认看到可读的 Markdown 正文，而不是模型输出对象的 JSON 包装。

#### DAG 向导兼容修复

- **修复统计图向导工具列表作用域**：数据库向导辅助函数不再直接依赖外层 `currentTools()`，改为显式接收当前工具集合，修复打开统计图模板时 `currentTools is not defined` 的运行时错误。
- **参数弹窗与数据库辅助统一传参**：普通节点参数弹窗、数据库连接下拉、读取表/字段辅助面板和侧栏输入摘要统一使用同一份 `wizardTools`，避免不同入口读取到不一致的数据库连接或工具列表。
- **统计图模板入口更稳**：统计图向导复用当前工具集合查找数据库连接、图表工具和 `agent.llm` 工具，减少重复调用和拆分后作用域漂移带来的隐性问题。

#### 大模型结果展示优化

- **DAG 节点卡片 Markdown 化**：大模型节点的可读输出区域新增紧凑 Markdown 样式，标题、段落、列表、代码和表格统一左对齐，避免报告标题过大、列表标记偏移和内容居中导致的阅读割裂。
- **执行步骤不再默认展示 LLM JSON**：任务步骤里的“完成 DAG 节点：大模型处理”会优先从 `content`、`text`、`markdown`、`answer` 等字段提取模型正文并渲染为 Markdown；原始 JSON 仍保留在“查看原始数据”折叠项中，便于排查。
- **共享大模型正文提取逻辑**：节点卡片和步骤详情复用同一套 LLM 输出解析逻辑，避免同一个大模型节点在不同区域显示结果不一致。

#### 测试与版本同步

- **回归用例补齐**：新增统计图向导工具列表传递、大模型节点 Markdown 可读输出、步骤详情隐藏 LLM JSON 包装等断言，覆盖参数向导、运行节点卡片和执行步骤三条展示链路。
- **验证通过**：`npm run check` 通过；定向 `tests/security-agent.test.js` 中的向导、LLM 节点展示和工作流工作台相关用例通过。
- **版本号启用**：应用版本升级至 `v0.0.89`，同步更新 `package.json`、`package-lock.json`、README、使用手册和项目汇报中的当前版本展示与说明。

## [v0.0.88] - 2026-06-06
### 工作流 LLM 节点编排约束与统计图模板补齐

本版本围绕工作流里大模型节点的位置和模板数据流继续收口：参考成熟工作流产品的“开始输入 / 上游节点 / LLM 处理”编排思路，把 LLM 节点从“只要存在即可”升级为“必须有明确输入来源”，同时补齐统计图模板中的大模型节点配置，让图表类工作流默认具备数据解读步骤和稳定的结构化图表生成链路。

#### LLM 节点编排规则

- **起始 LLM 节点输入门禁**：大模型节点可以作为第一个节点，但提示词必须显式引用 `{{goal}}` 或 `{{inputs.*}}`；如果既没有上游依赖，也没有引用工作流输入，会提示“缺少上游输入”，避免首节点空跑。
- **上游结果依赖更明确**：当 LLM 节点需要处理数据库、检索、图表或其他节点输出时，必须连接对应上游节点；仅在提示词中写 `{{nodes.xxx.output}}` 不再被视为合法起始输入，防止运行时并行调度导致数据尚未产出。
- **前后端双重兜底**：前端画布校验、保存校验、运行预检和 DAG 运行时均接入同一规则；直接提交 JSON、恢复旧任务或绕过页面运行时也会被后端阻断。

#### 统计图模板补齐

- **模板默认加入 LLM 配置**：统计图向导生成的工作流从“分组统计 → 生成统计图”升级为“分组统计 → 大模型处理 → 生成统计图”，LLM 节点自动带入当前可用模型、系统提示词、用户提示词、输出格式和 Token 参数。
- **数据解读与图表生成分工清晰**：LLM 节点负责基于查询结果输出简洁数据解读，图表节点仍直接消费 `{{nodes.group_count.output.rows}}` 结构化结果，避免模型改写数据导致图表不稳定。
- **模板选中与编辑体验保持一致**：生成模板后默认选中“大模型处理”节点，方便用户先确认模型和提示词，再继续调整图表节点或数据库参数。

#### 测试与版本同步

- **回归用例补齐**：新增 LLM 起始节点输入校验测试，更新 DAG 数据库路由与 LLM 节点兜底测试；测试装配同步补齐 `dag-toolbar-tools.js`、`dag-toolbar-db.js` 和 `agent-workflow-runners.js`，与真实页面脚本加载顺序保持一致。
- **验证通过**：`npm run check`、`node tests\security-agent.test.js`、`node tests\security-mcp.test.js` 均已通过；`node --test tests\...` 在当前沙箱中因 `spawn EPERM` 无法拉起子进程，已改用单文件方式完成验证。
- **版本号启用**：应用版本升级至 `v0.0.88`，同步更新 `package.json`、`package-lock.json`、README、使用手册和项目汇报中的当前版本展示。

## [v0.0.87] - 2026-06-06
### 监控页资源说明细化

本版本继续收口监控页的资源展示：对路由/静态资源名称增加更细的自动识别规则，并补充新拆分脚本与样式入口的说明，让监控页里每个新文件都能更直观地识别出来。

#### 监控页细化

- **资源名称兜底增强**：系统监控页在路由说明表之外，新增对 `.js` / `.css` 静态资源的自动命名兜底，避免新拆分文件只显示原始路径。
- **新拆分文件补充说明**：为模型动作、DAG 工具、会话引擎、工作流核心/运行器、监控页自身脚本，以及 `chat.css` 下的入口样式补充更细的资源名称。
- **显示效果收敛**：监控页的 HTTP 资源列表现在能更明确地展示聊天脚本、聊天样式、公共样式与第三方资源，方便定位具体模块。

#### 版本与文档同步

- **版本号启用**：应用版本升级至 `v0.0.87`，同步更新 `package.json`、`package-lock.json`、README 与项目汇报中的当前版本展示。

## [v0.0.86] - 2026-06-06
### 前后端模块化拆分与运行时兼容修复

本版本围绕工程治理继续收口：前端聊天、管理端、DAG、工作流、RAG、统计与样式继续按领域拆分，后端内置 MCP 与安全测试也进一步模块化，同时修复了拆分后暴露出来的若干运行时兼容问题，并把当前版本同步到新的文档与包元数据。

#### 前端模块化拆分

- **聊天与管理端继续拆分**：`models.js`、`engine.js`、`dag-toolbar.js`、`agent-workflows.js` 等继续拆为更细的领域脚本，新增模型动作、会话引擎、默认模型、DAG 工具元数据、DAG 数据库辅助、工作流核心与运行器等文件，减少单文件体积。
- **工作台与渲染链路继续分层**：`agents-dag-editor.js`、`agents.js`、`render.js`、`sidebar.js`、`stats.js`、`rag.js` 等同步拆出独立模块，聊天页、工作流页、RAG 图谱和侧边栏加载顺序重新编排。
- **样式继续模块化**：`styles/admin.css`、`styles/stats-monitor.css`、`styles/workspaces/agent.css`、`styles/workspaces/mcp.css` 继续拆分为更细的子样式块，便于后续按页面和功能维护。

#### 运行时兼容修复

- **DAG 转义命名冲突修正**：`dag-core.js` 的通用转义函数改为独立命名，避免与拆分后的 DAG 工具链重复声明。
- **图谱平移事件修复**：`rag.js` 中的图谱拖拽/平移结束事件改为包装调用，避免 `stopGraphMapPan` 先于定义触发引用错误。
- **工具栏依赖注入修复**：`dag-toolbar.js` 的 `fitToContent` 改为通过上下文注入，`agents-dag-editor.js` 负责传入实际实现，避免工具栏在拆分后丢失依赖。
- **工作流与模型页回归修正**：工作流 JSON 读取、节点抽屉、模型管理动作与默认模型保存逻辑按模块重新暴露，保证原有入口继续可用。

#### 后端与测试治理

- **内置 MCP 外观聚合拆分**：`server/services/builtin-mcp.js` 继续保持聚合入口，但内部已按 common / reports / visualization / documents / data / format / im 拆分，后续新增能力更容易落位。
- **安全测试分域**：`tests/security.test.js` 仅保留聚合入口，认证、聊天、MCP、RAG、智能体等安全回归拆成独立测试文件，提升可维护性。
- **数据库连接模块收口**：`server/db/connection.js` 与相关服务继续整理，配合能力库拆分让连接与工具路由更清晰。

#### 版本与文档同步

- **版本号启用**：应用版本升级至 `v0.0.86`，同步更新 `package.json`、`package-lock.json`、README 与项目汇报中的当前版本展示。
- **验证通过**：关键拆分脚本 `node --check` 通过，`git diff --check` 已清理尾随空行与格式问题。

## [v0.0.85] - 2026-06-05
### 智能体任务详情与任务列表交互精简

本版本围绕智能体任务记录的查看体验做轻量收口：任务详情页移除信息密度偏低的时间轴模块，把注意力留给进度、结果、工作流节点、工具统计和执行步骤；任务列表则补齐任务列悬浮信息，同时取消问号鼠标样式。

#### 智能体任务详情

- **移除任务时间轴**：智能体任务详情弹窗不再渲染“任务时间轴”，同步清理 `.agent-timeline` 前端渲染函数与样式，减少重复信息和视觉干扰。
- **执行步骤标题左对齐**：任务具体执行步骤正文中的 Markdown 标题统一左对齐，和段落、列表、表格保持一致阅读方向。
- **保留关键执行信息**：详情页继续展示进度汇总、最终结果、错误信息、可视化结果、工作流节点、工具调用统计与逐步执行内容。

#### 智能体任务列表

- **任务列悬浮信息补全**：任务列悬浮时展示任务、状态、创建/开始/完成时间、模型、模式、步骤数、工具数、错误数和目标，便于不打开详情也能快速判断任务上下文。
- **移除问号鼠标样式**：任务列悬浮保留自定义信息提示，但鼠标样式改回普通箭头；同时移除原生 `title` 提示，避免浏览器默认提示与自定义悬浮层叠加。

#### 版本与验证

- **版本号启用**：应用版本升级至 `v0.0.85`，同步更新 `package.json`、`package-lock.json`、README 与项目汇报中的当前版本展示。
- **验证通过**：`npm run check` 通过；`eslint client/chat/agents.js` 通过；本地 `http://localhost:3000/` 可正常打开到登录页。

## [v0.0.84] - 2026-06-05
### 版本历史补齐、文档同步与回归验证对齐

本版本用于启用新的应用版本号，并修正发布文档的版本连续性：保留 `v0.0.83` 的完整功能更新记录，同时将当前运行版本、README、项目汇报和 package 元数据同步到 `v0.0.84`。

#### 文档与版本治理

- **补齐 v0.0.83 历史记录**：恢复 `v0.0.83` 条目，避免更新日志从 `v0.0.84` 直接跳到 `v0.0.82`。
- **当前版本同步**：`package.json`、`package-lock.json`、README 顶部版本徽章、最新版本说明、项目汇报和运行时版本读取均同步为 `v0.0.84`。
- **运行时版本确认**：服务端 `getAppVersion()` 继续从 `CHANGELOG.md` 顶部读取最新版本，当前返回 `v0.0.84`。

#### 回归验证对齐

- **工作流前端断言对齐**：补齐 DAG 参数弹窗大工作台尺寸、节点工具摘要卡紧凑布局，以及工具栏“发布 / 发布并运行 / 运行发布版”入口，确保实现与文档及回归测试一致。
- **验证通过**：`npm run check` 通过；`npm run lint` 通过；`node --test tests/security.test.js` 在 Windows 非沙箱环境完整通过 **160/160**。

## [v0.0.83] - 2026-06-04
### 工作流编排重构、权限层级统一与能力库数据查询增强

本版本围绕“工作流作为独立流程资产”继续收口体验：把运行、发布、版本管理和 LLM 执行配置前移到工作流画布自身，同时补齐权限层级语义、能力库数据库清单/计数能力，以及聊天 MCP 工具调用的确定性回退。

#### 工作流编排体验：编辑、发布、运行一体化

- **工作流顶部区域重排**：工作流页顶部改为“当前画布 + 已保存工作流”双入口，移除常驻工作流名称输入框，保存新草稿时再通过自定义输入弹窗命名；“已保存工作流”右侧四个操作按钮取消外层容器框，保持无边框透明按钮组。
- **生命周期状态更清晰**：原版本文字改为生命周期 chip，分别展示当前版本、发布版本和运行状态；新建、加载、版本管理和删除与已保存工作流选择器保持同一行紧凑排列。
- **工具栏分组收口**：DAG 工具栏按“节点 / 模板 / 操作 / 发布 / 运行”分组，发布和运行入口同处一行；下拉触发器视觉上保持普通按钮样式，不再显示下拉箭头，并支持点击外部自动收起。
- **工作流运行入口独立化**：移除智能体任务面板里的工作流运行模式、工作流选择器和底部运行调试区域；工作流预览运行、发布版运行、发布并运行统一从工作流画布发起。
- **运行输入面板归位**：`{{inputs.*}}` 运行变量只在工作流画布内扫描与填写，避免智能体任务面板和工作流画布各维护一套输入状态。

#### LLM 节点成为工作流核心执行节点

- **新增 `agent.llm` 大模型节点**：工具目录新增“大模型”分组，LLM 节点可配置节点模型、系统提示词、用户提示词、输出格式、温度、最大输出长度和工作流最大步骤数。
- **空画布自动创建 LLM 节点**：新建或加载空工作流时自动补齐默认“大模型处理”节点，减少空画布运行失败。
- **运行配置迁入节点**：工作流运行 payload 不再依赖工作流级“运行模型 / 最大步骤”字段，而是从第一个 `agent.llm` 节点读取模型与步骤上限。
- **强制保留至少一个 LLM 节点**：前端保存、校验、运行时要求至少存在一个已配置模型的 LLM 节点；删除或把最后一个 LLM 节点改成其他工具会被阻止。后端保存与运行时也增加兜底校验和默认配置补齐。
- **模型可见性收紧**：工作流 LLM 节点模型选择器只展示全局模型和当前用户自己的私有模型；不再因为当前用户是管理员就暴露其他用户的私有模型，和系统模型选择器保持一致。

#### DAG 参数编辑与节点配置优化

- **参数编辑弹窗化**：DAG 节点参数编辑迁入更大的固定高度工作台弹窗，避免复杂 JSON、SQL 或提示词在窄侧栏里难以编辑。
- **常用参数中文化**：数据库、报表、图表、格式化、IM、知识库和 LLM 节点的常用参数补齐中文标签、说明和占位提示，减少直接暴露英文字段名造成的理解成本。
- **工具摘要卡紧凑化**：节点工具元信息卡改为更紧凑的全宽摘要样式，展示工具名称、来源 badge、描述和工具 ID，移动端自动换行。
- **任务补充说明改为多行文本**：智能体任务的“补充说明”从单行输入改为全宽 textarea，更适合填写部门范围、资料来源或执行限制。
- **统计图向导补充说明**：统计图工作流向导为可选数据库 schema 字段增加说明，降低多 schema 数据库下的填写歧义。

#### 权限层级语义统一

- **新增权限工具模块**：新增 `server/permissions.js`，统一提供 `admin / manager / user` 三层权限语义、角色归一化、内置 `admin` 识别和前后端权限标识。
- **内置 `admin` 与普通管理员区分**：系统管理员显示为“系统管理员”，普通管理员显示为“管理员”；用户列表、登录标识、设置页、模型页、能力库和智能体审计文案统一改用权限层级表达。
- **超级管理员门禁集中化**：开放注册、全局模型、全局能力服务、用户详细记录、管理员账号管理和删除审计等操作统一使用 `isSuperAdmin()` 判断，不再分散手写 `username === 'admin'`。
- **安全出口判断对齐权限工具**：模型、MCP 和数据库私网连接校验改用 `isAdmin()` / `isSuperAdmin()`，避免角色判断在不同模块里出现语义漂移。

#### 能力库与数据库 MCP 增强

- **数据库 MCP 工具抽象为通用动作**：DAG 工具目录不再把每个数据库连接的 `mcp.<id>.db.list_tables`、`mcp.<id>.db.describe_table`、`mcp.<id>.db.run_readonly_query` 等底层工具逐个展开给用户，而是优先收敛为“列出数据表 / 查看表结构 / 只读 SQL 查询 / 分组统计 / 统计数据表数量”等通用数据库动作，避免数据库连接一多就让工具下拉膨胀。
- **数据库连接改为资源参数**：数据库连接从“工具身份”迁移为节点参数面板里的“数据库连接”选择器；节点输入继续承载 `connectionId`、`schema`、`table`、`column`、`sql` 等业务参数，并保留“读取表 / 读取字段”等辅助能力，且辅助读取会跟随当前选择的数据源。
- **运行时按 `connectionId` 路由真实 MCP**：工作流保存时节点可保持通用形态，例如 `tool: "db.run_readonly_query"` + `input.connectionId`；运行时再根据 `connectionId` 转发到真实工具 `mcp.<connectionId>.db.run_readonly_query`，审计和调用日志仍记录具体连接与工具。
- **渐进收敛边界清晰**：本轮仅对 schema 基本统一的数据库 MCP 做抽象；普通第三方 MCP 工具仍按原始工具暴露，避免把语义和参数差异较大的外部能力过早合并。
- **数据库表数量工具**：SQL 数据库 MCP 新增 `db.count_tables`，支持 SQLite、PostgreSQL、MySQL 和 SQL Server，按表/视图或 schema 统计数量并返回结构化 `total` 与 `rows`。
- **MongoDB 集合数量工具**：MongoDB MCP 新增 `db.count_collections`，用于回答“有多少个集合”等集合盘点问题。
- **聊天 MCP 回退更可靠**：普通对话中当用户明确要求查询数据表清单、表数量或集合数量时，即使规划器返回 `none`，也会确定性选择 `list_tables` / `count_tables` / `list_collections` / `count_collections` 等工具执行。
- **工具匹配支持短名与来源提示**：规划器返回短工具名时可解析到完整 MCP 工具名；多个同名工具可根据用户提到的服务名优先匹配，调用日志区分 `chat` 与 `chat_fallback` 来源。
- **缺工具时明确告知**：用户明确要求能力库或数据库工具调用但当前无匹配工具时，会向主模型注入“缺少匹配能力库工具”的上下文，避免误答成本轮不需要工具或编造结果。

#### 回归测试与验证

- **新增回归用例**：补充权限层级、DAG 参数弹窗、任务补充说明 textarea、统计图 schema 说明、工具摘要卡、工作流预览/发布版运行入口、LLM 工作流节点、数据库 MCP 通用工具折叠、连接参数路由、聊天 MCP 缺工具提示和数据库表/集合计数调用测试。
- **验证通过**：`npm run check` 通过；`npm run lint` 通过；`node --test tests/security.test.js` 在 Windows 非沙箱环境完整通过 **160/160**。浏览器可视化验证按本轮要求未执行。

## [v0.0.82] - 2026-06-03
### 普通对话图表时序修正、持久化修复与规划器回退

本版本补齐普通对话图表的”实时渲染 → 消息落库 → 刷新恢复”闭环，修复图表在模型思考期间过早出现的问题，并新增规划器确定性回退——当 LLM 规划器无法可靠决策时，正则提取表名/字段构造 SQL 直接执行。

#### 图表时序修正：图表随文字同步出现

- **延迟注入**：SSE `chart` 事件到达时不再立即调用 `injectChartBlock` 注入 DOM，只存入 `pendingCharts`。待第一个文字 `content` 事件到达（`firstTokenTime` 首次赋值时）才批量注入图表块，确保图表与模型回复文字同步出现，不再出现”还在思考图表就先出来了”的体验。
- **错误/中断路径兜底**：在 `AbortError` 和通用错误路径中，调用 `pendingCharts.forEach(spec => injectChartBlock(...))` 确保图表块在异常退出时也被注入 DOM 并渲染。
- **修复渲染参数错误**：`injectChartBlock` 中将 `renderPivotCharts(chartBlock)` 修正为 `renderPivotCharts(msgEl)`——`renderPivotCharts` 使用 `querySelectorAll` 只查后代元素，不能将目标元素自身作为 root。

#### 图表持久化与刷新恢复

- **SSE 图表写入持久化正文**：`server/routes/chat.js` 新增 `rememberStreamedChart()` 拦截 `writeSse`——捕获本轮流式响应中发给前端的 `type: “chart”` 图表 spec，去重后存入 `streamedChartSpecs[]`。保存 assistant 消息前通过 `appendStreamedChartsToAssistantContent()` 将图表序列化为 `pivot-echart` fenced code block 追加到消息正文末尾。刷新页面后，历史消息通过现有 Markdown 渲染链路重新生成 `.pivot-echart-block`，不再依赖首次流式 DOM 注入。
- **同轮图表事件去重**：后端 `rememberStreamedChart` 对相同 chart spec 建立去重集合，重复图表事件不再写入 SSE，也不会重复合并进消息正文。
- **前端重复事件兜底**：`client/chat/engine.js` 新增 `pendingChartKeys`，即使后端或网络重放重复 chart 事件，当前助手消息也只注入一次图表 DOM。
- **兼容已有可渲染图表块**：`contentIncludesRenderableChart()` 检测模型正文是否已包含 `pivot-echart`/`pivot-chart` 代码块，若已存在则不再追加本轮 SSE 图表，避免刷新后正文出现重复图表。

#### 规划器确定性回退：规划器失败时直接构造 SQL

- **`detectStrongDataQueryIntent()`**：不依赖 LLM 判断，通过正则检测用户是否明确要求查询数据库（匹配表名/数据库+表名/SQL 关键词/分组统计关键词）。
- **`extractTableName()` / `extractGroupByField()`**：从用户自然语言中正则提取表名和分组字段（支持”查询 table_account 表”、”按 group_id 分组”等多种中文表达）。
- **`buildFallbackDataQueryInput()`**：用提取到的表名+分组字段构造 `SELECT ... GROUP BY ... LIMIT 50` 或 `db.group_count` 参数，兼容 `run_readonly_query` 和 `group_count` 两种工具。
- **规划器失败时自动回退**：在 `maybeBuildMcpChatContext` 中，当 LLM 规划器返回 `action: none` 或 JSON 解析失败后，先调用 `detectStrongDataQueryIntent` 判断——若用户明确要查数据，直接用 `buildFallbackDataQueryInput` 构造 SQL 执行，绕过不可靠的规划器。回退失败则降级到 `buildMcpToolsHint` 提示。

#### 图表 SSE 去重：消除自动图表链路的重复发射

- **`server/services/chat-mcp-context.js`**：去掉 `maybeBuildChartAfterDataTool()` 中的额外 `emitChartSse()` 调用，统一由 `extractMcpResultText()` 负责发出图表事件，防止自动图表链路天然发送两次（一次在直接执行 viz 时，一次在 auto-chart 后）。

#### 回归测试与验证

- **新增回归用例**：`tests/security.test.js` 增加”chat route embeds streamed chart specs into persisted assistant content”、”chat renderer accepts loose ECharts-style chart specs”和”chat MCP fallback builds group_count input”测试。
- **验证通过**：`npm run check` 通过（129 个 JS 文件）；`node tests/security.test.js` 安全回归 **144/144** 全部通过。

## [v0.0.81] - 2026-06-03
### 普通对话图表链路确定性修复与中文思考强化

本轮针对普通对话中"模型返回代码而非真正图表"的核心问题，重构图表结果从工具到前端的传递链路，同时强化所有模型交互路径的中文思考约束。

#### 图表传送链路：SSE 双通道架构

**问题分析**：能力工具 `viz.build_chart` 和自动图表生成都能正常产出结构化 `pivot_chart`，但结果会先转成 `pivot-echart` 代码块文本嵌入 system context，再交给主模型"原样保留"。该步骤不可靠——模型经常将其改写为 Python matplotlib / JavaScript ECharts 示例代码，导致前端无法渲染真实图表。MCP 调用日志确认 `viz.build_chart` 成功返回图表数据，但后续助手消息中 `pivot-echart` 块为 0，反而出现 Python 代码块 12 条、ECharts 3 条。

**核心修复——SSE 直通前端**：
- **`server/services/chat-mcp-context.js`**：新增 `emitChartSse()` 函数，图表结构化数据通过 SSE `{"type":"chart","data":...}` 事件直接发送给前端，**完全绕过模型文本生成**。`extractMcpResultText()` 现返回简短文字引用（"已生成图表：xxx，前端已直接渲染"）替代原来的 `pivot-echart` 代码块；`maybeBuildChartAfterDataTool()` 自动图表同步走 SSE 直达通道。
- **`client/chat/engine.js`**：新增 `chart` SSE 事件处理分支 + `injectChartBlock()` 辅助函数——收到图表数据后立即创建 `.pivot-echart-block` DOM 元素并注入当前助手消息（位于 `.text-body` 之外，不会被流式文本渲染覆盖），随后调用 `renderPivotCharts` 初始化 ECharts。成功/错误/中断三条路径均确保图表块被渲染。
- **修复渲染参数错误**：`renderPivotCharts(root)` 使用 `root.querySelectorAll` 查找后代元素，不可将图表块自身作为 root 传入，修正为传入父容器。

#### 图表意图检测与工具调度优化

- **`server/services/chat-mcp-context.js` — 意图检测扩展**：`getMcpToolIntent()` 正则新增 `统计分析|数据对比|数据汇总|数据概览|分布情况|数据分布|占比|排名|排行|展示.*数据|数据.*展示|呈现.*数据|查询.*统计|洞察`，覆盖"帮我分析销售数据"等隐式图表请求。
- **规划器提示词强化**：新增关键指令——"如果用户询问数据、统计、分析相关的问题，且存在可用的数据库或数据查询工具，请优先选择合适的数据查询工具，不要返回 action=none。主模型无法自行查询数据库，会回退到编造数据或生成无效代码。"
- **规划器跳过时注入工具提示**：新增 `buildMcpToolsHint()` 函数，当规划器返回 `action=none` 或工具过滤后为空时，不再返回空字符串，而是注入可用工具列表和禁止自编代码的提示给主模型，防止其回退到代码生成。

#### 中文思考约束全覆盖

- **`server/routes/chat.js`**：`CHAT_LANGUAGE_SYSTEM_PROMPT` 重构为结构化规则——`【重要语言规则】`（4 条中文约束）+ `【重要工具规则】`（4 条工具行为约束），明确禁止生成 Python/JS 图表代码，要求保留 `pivot-echart`/`pivot-table` 块。
- **`server/services/agent-runtime.js` — 流式 Agent**：`tryRunAgentStreaming()` 系统提示词补上 `【重要语言规则】你的思考、推理和所有输出必须使用中文。禁止使用英文提纲或英文推理过程。`——此前流式路径完全没有中文思考约束。
- **`server/services/agent-runtime.js` — 规划器**：`buildPlannerMessages()` 新增显式 `【重要语言规则】` 声明，Schema 标签从 `"最终答案"` 升级为 `"最终答案（中文）"`。

#### 测试同步

- **`tests/security.test.js`**：`chat language instruction` 测试断言更新，匹配新的 `【重要语言规则】` 和 `禁止使用英文提纲或英文推理` 措辞。

### 回归验证
- **验证通过**：`npm run check` 通过（129 个 JS 文件）；`node tests/security.test.js` 安全回归 **141/141** 全部通过。

## [v0.0.80] - 2026-06-03
### 智能体工作流与数据库统计闭环

本版本汇总本轮智能体工作流、DAG 运行、数据库 MCP 统计工具、Agent 中文提示词和文档版本同步相关修复优化。

#### 工作流运行与编排体验
- **工作流运行入口独立化**：DAG 模式从“工作流编排”统一收口为“工作流”，运行面板新增工作流选择器和“打开工作流”入口，运行时必须选择已保存工作流，避免直接提交空画布或临时 JSON。
- **新建工作流弹窗**：新增工作流名称、说明和默认执行目标表单，创建后自动进入 DAG 模式并初始化空白画布；保存时同步 `description`，工作流库和运行选择保持同一个活动工作流。
- **运行目标自动推断**：DAG 运行未填写目标时，优先使用工作流名称生成“执行工作流：xxx”，再回退到当前画布节点数，减少因目标为空导致的任务创建失败。
- **已保存工作流校验修复**：运行面板现在同时兼容字符串 JSON 和已解析的 `dag_spec` 对象，不再把已保存工作流误判为“工作流 JSON 需要修正”。
- **运行输入可视化**：运行面板和工作流画布都会扫描 `{{inputs.*}}` 引用并生成输入表单，提交时自动合并为 `dagInputs`，替代手写 JSON textarea。
- **画布编辑增强**：工具栏改为“节点 / 模板 / 操作”分组，新增统计图模板向导；节点坐标持久化、智能默认工具、字段 tooltip、Delete/Backspace/Escape 快捷键和保存前校验门禁保持可用。
- **DAG 结果可视化**：运行详情新增迷你拓扑状态图，按完成、运行中、错误、跳过、待执行区分节点状态，并展示依赖边和图例。
- **软删除恢复**：工作流删除后展示“撤销删除”，后端新增 `PATCH /api/agents/workflows/:id/restore`，30 天内可按用户权限恢复。

#### 数据库 MCP 与模板数据流
- **新增 `db.group_count` 工具**：数据库 MCP 增加安全只读分组统计能力，支持 SQLite、PostgreSQL、MySQL 和 SQL Server，按表名、分组字段、别名、排序和 limit 自动生成统计 SQL，适合普通用户快速生成分布图。
- **统计图模板向导**：DAG 编辑器可读取数据库表和字段，自动生成“分组统计 -> 生成统计图”两节点工作流；优先使用 `db.group_count`，缺失时回退到只读 SQL 查询。
- **结构化结果引用修复**：DAG 模板引用 `{{nodes.xxx.output.rows}}` 时会自动回退读取 `structuredContent.rows`，图表节点可以直接消费 MCP 结构化数据行。

#### 智能体与普通对话图表链路优化
- **图表结果保真**：智能体工作流图表节点可直接消费 `structuredContent.rows`，普通对话在查询数据后会优先补调用 `viz.build_chart`，并要求最终回答原样保留 `pivot-echart` 结构化图表块，减少图表结果退化为 Python/ECharts 代码或纯文本的情况。

#### Agent 运行时与文案
- **Agent 提示词中文化**：核心规划提示、上下文指导、流式工具调用提示和最终答案合成提示全部汉化，要求模型以中文进行简短推理和最终输出。
- **普通聊天思考过程中文约束**：普通聊天请求会注入中文语言约束，要求最终答案以及可见的 `reasoning_content`、`<think>`、`<thought>` 思考内容默认使用中文，减少推理面板出现英文提纲。
- **运行日志中文修复**：修复任务通知、重跑节点、断点续跑、审批、工具执行、DAG 节点状态和模型路由等步骤标题中的乱码文案。
- **DAG 节点并发可配**：互不依赖节点并行数改为 `AGENT_DAG_NODE_CONCURRENCY`（默认 4），运行时状态 API 暴露该值，README 与 `.env.example` 同步补充配置说明。

#### 测试与文档
- **安全回归补充**：`tests/security.test.js` 新增 `db.group_count` 工具暴露和执行验证，覆盖 `output.rows` 到 `structuredContent.rows` 的模板引用兼容，并补充普通聊天可见思考中文约束测试。
- **版本同步修正**：应用版本升级至 `v0.0.80`，同步更新 `package.json`、`package-lock.json`、README 顶部版本、近期升级摘要、当前版本说明和配置文档。

### 回归验证
- **验证通过**：`npm run check` 通过；`npm run lint` 通过；`node --test-name-pattern "chat language instruction asks visible reasoning" tests/security.test.js` 定向安全回归通过；`node --test-name-pattern "DAG templates read MCP structured rows|database MCP preset exposes SQLite readonly tools" tests/security.test.js` 定向安全回归通过。
- **完整安全回归备注**：已执行 `node tests/security.test.js`，新增的 `output.rows` 兼容与 `db.group_count` 用例通过；完整套件在当前 Windows 环境的上传/RAG 文件清理阶段因 `EPERM: unlink` 文件锁触发 5 个既有用例失败（135/140），需释放文件句柄或清理测试残留后复跑完整套件。

## [v0.0.79] - 2026-06-03
### Agent 提示词中文化

将 Agent 运行时核心系统提示词从英文迁移为中文，确保大模型思考过程与最终输出一致使用中文。

#### 修改范围（agent-runtime.js）

- **核心 Agent 系统提示词（`buildAgentMessages`）**：身份声明、行为规则、JSON Schema、运行模式说明、上下文指导、工具列表标签全部汉化；Schema 的 `thought` 字段标注为 `简短推理（中文）`，强制模型用中文思考。
- **运行模式标签**：`standard`/`deep`/`audit`/`dag` 四种模式改为中文说明（标准模式—稳扎稳打 / 深度模式—允许额外检索 / 审计模式—必须强调证据、限制和风险 / DAG 模式—按工作流图执行），帮助模型理解当前模式的行为预期。
- **上下文模式**：`recent`/`knowledge`/`none` 对应的英文提示改为"使用最近的对话上下文 / 使用知识库上下文 / 不包含额外的会话上下文"。
- **流式 Agent 系统提示词（`tryRunAgentStreaming`）**：身份声明、目标标签、工具调用指导全部汉化。
- **最终答案合成提示词（`synthesizeFinalAnswer`）**：系统提示汉化并追加 `输出请使用中文。`；兜底文案 `No final answer was generated.` 改为 `未能生成最终答案。`。

### 回归验证
- **验证通过**：`npm run check` 通过；`npm run lint` 通过；`node tests/security.test.js` 安全回归通过。

## [v0.0.78] - 2026-06-03
### 智能体工作流编排 UX 优化

经过用户体验评审后，针对工作流编排（DAG）功能的 10 项重点优化：

#### 画布编辑器（agents-dag-editor.js）
- **智能工具选择**：新增节点时统计画布上已使用工具的频次，同工具使用 ≥2 次时自动设为默认值，否则留空让用户主动选择，避免盲目预填列表第一项。
- **节点坐标持久化**：序列化时保留用户手动调整的 `_x`/`_y` 坐标，重新加载时已有坐标的节点不再被自动布局覆盖，仅对新节点触发拓扑排列。
- **节点属性提示**：重试次数和超时（ms）字段增加 title tooltip，说明"0 表示不重试 / 使用全局默认超时"。
- **键盘快捷键**：画布聚焦时 `Delete`/`Backspace` 删除选中节点，`Escape` 取消选中并关闭节点抽屉；仅在非文本编辑态响应，不与表单输入冲突。
- **校验方法暴露**：`mount()` 返回新增 `validate()` 方法，供外部在保存/发布前调用。

#### 工作流库（agents.js）
- **保存前校验门禁**：`saveAgentWorkflowToLibrary` 保存前自动调用画布校验，如存在循环依赖、未选工具、依赖缺失等问题，弹出确认弹窗，用户可强制保存或取消修正。
- **删除软撤销**：工作流删除后版本标签位置显示"↩ 撤销删除"按钮，调用新增的 `PATCH /api/agents/workflows/:id/restore` 接口，30 天内可一键恢复。
- **模板替换提醒**：统计图向导在替换当前画布前，检测画布与已保存内容的差异，如有未保存更改则在确认弹窗中明确提示"当前画布有未保存的更改"。

#### 运行时输入面板（agent-dag.html + agents.js + agent.css）
- **可视化变量编辑**：新增运行时输入面板，自动扫描 DAG JSON 中所有 `{{inputs.*}}` 引用，动态生成对应表单字段；用户填写后值优先于 JSON textarea 合并到 `dagInputs` 提交。
- 面板随画布变更自动刷新，带"重新扫描变量"按钮，`runMode` 切换为标准模式时自动隐藏。

#### DAG 运行结果可视化（agents.js + agent.css）
- **迷你状态图**：运行详情中节点列表上方渲染 SVG 拓扑图，节点按状态着色（绿色完成、蓝色运行中、红色错误、黄色跳过、灰色待执行），边上绘制依赖箭头，底部附图例。
- 拓扑分层基于节点 `depends_on` 的 Kahn 风格布局，与编辑器画布视觉一致。

#### 运行时（agent-runtime.js + agent-workflows.js + routes/agents.js）
- **DAG 节点并发可配**：硬编码的并发数 4 改为环境变量 `AGENT_DAG_NODE_CONCURRENCY`（默认 4），运行时状态 API 暴露该值；编辑器工具栏 tooltip 补充提示。
- **软删除恢复 API**：新增 `restoreAgentWorkflow` 服务函数，按 workflow_id + 用户校验，仅恢复 30 天内删除的工作流；新增 `PATCH /api/agents/workflows/:id/restore` 路由。

### 回归验证
- **验证通过**：`npm run check` 通过；`npm run lint` 通过；`node tests/security.test.js` 安全回归通过。

## [v0.0.77] - 2026-06-03
### 缺陷修复
- **工作流图表工具重复显示去重**：智能体工作流编排的「选择工具」下拉中，「图表与展示」分类下 `生成图表 · viz.build_chart`、`生成表格 · viz.build_table` 出现重复项。根因是可视化工具同时注册为**内置智能体工具**（`agent-tools.js`，对所有用户始终可用）与「图表生成」**内置 MCP 能力**（`builtin-mcp.js`，用户添加后才出现），而工具目录在 `agent-tool-catalog.js` 的 `formatToolList` 合并内置工具与缓存 MCP 工具时未去重。现按裸工具名去重——缓存 MCP 工具与内置工具同名时只保留内置工具；外部第三方 MCP 工具因带 `mcp.<id>.` 服务前缀不受影响，工具执行链路保持不变。

### 回归验证
- **验证通过**：`npm run check`（129 个 JS 文件语法 + 前端资源校验）通过；`npm run lint` 通过；完整 `node tests/security.test.js` 安全回归 `139/139` 通过。

## [v0.0.76] - 2026-06-03
### 安全加固
- **普通用户私网出口默认收紧**：个人 MCP 外部端点、内置 IM Webhook 与数据库 MCP 连接默认禁止普通用户配置 loopback、link-local、局域网或内部主机地址；管理员仍可在显式允许私网出口的部署策略下接入内网资源。`MCP_RESTRICT_PRIVATE_DATABASE_HOSTS_TO_ADMIN` 改为默认开启，仅当明确设置为 `false` 时才允许普通用户配置私网数据库。
- **可信反向代理识别**：新增 `TRUSTED_PROXY_IPS` 配置，只有直连来源命中受信任 IP/CIDR 时才采信 `X-Forwarded-For`；访问日志同步使用同一套客户端 IP 解析逻辑，避免伪造请求头污染审计来源。
- **出站 DNS rebinding 防护闭环**：模型调用、MCP JSON-RPC、RAG Embedding、模型/向量模型探测、模型健康检查、标题生成、记忆压缩和告警 Webhook 等 HTTP 出口统一使用安全 lookup Agent，请求时复用受保护的 DNS 解析，阻断校验后重解析到敏感地址的 TOCTOU 风险。
- **HTML 清洗兜底加固**：前端 `PivotSafeHtml.sanitizeHtml` 在 DOMPurify 未加载时不再返回原始 HTML，而是退回到文本转义，避免依赖加载异常时产生 XSS 兜底缺口。

### 配置与文档
- **新增代理配置说明**：`.env` 与 `.env.example` 新增中文注释的 `TRUSTED_PROXY_IPS`，说明仅受信反向代理可传递真实客户端 IP。
- **版本升级**：应用版本升级至 `v0.0.76`，同步更新 `package.json`、`package-lock.json` 与 README 版本展示。

### 回归验证
- **安全测试补齐**：新增/调整 MCP loopback 阻断、数据库私网默认限制、可信代理 IP、出站安全 lookup 和 DOMPurify 缺失兜底测试。
- **验证通过**：`npm run check` 通过；完整 `node tests/security.test.js` 安全回归在 Windows 非沙箱环境 `139/139` 通过。

## [v0.0.75] - 2026-06-03
### 安全复查修复
- **记忆压缩出站补齐 SSRF 守卫**：会话记忆压缩（`llm.js` 的 `compressMemory`）此前是唯一未经运行时校验的模型出站点，现已补齐调用时 DNS 解析守卫，与聊天补全、标题生成、智能体等其它出站点一致，闭合 DNS rebinding 残留缺口。
- **MongoDB / MySQL 连接 TLS 修复**：此前 MongoDB 的 SSL 开关被静默忽略（勾选 SSL 仍始终明文连接，凭据可被中间人窃取）、MySQL 的「信任自签名证书」开关被丢弃，本轮一并修复——MongoDB 改用 `tls`/`tlsAllowInvalidCertificates`，MySQL 对齐 `rejectUnauthorized`，默认校验证书且仅在显式信任自签名时放行，与 PostgreSQL/SQL Server 行为一致。
- **工作簿解析行数上限**：用户上传的 Excel/CSV 经 `document-text.js` 解析时新增单表 5 万行上限（`sheetRows`），作为纵深防御缓解超大或恶意表格触发的 CPU 耗尽与 ReDoS。

### 依赖安全
- **xlsx 升级至修复版**：`xlsx` 升级至 SheetJS 官方修复版 `0.20.3`（修复原型污染 GHSA-4r6h-8v6p-xvw6 与 ReDoS GHSA-5pgg-2g8v-p4x9）。npm 源已停更在有漏洞的 `0.18.5`，改用官方 CDN 分发；用户上传文件直接进入 `XLSX.read` 的高危攻击面已消除。`npm audit` 仅剩 `uuid` 一项 moderate（项目仅用 `v4()`、不传 buffer 参数，不受该 CVE 影响）。

### 前端体验
- **智能体任务过滤防抖**：智能体任务列表的过滤文本框（`agents.js`）改为防抖触发刷新，避免逐键发起请求风暴与响应乱序覆盖列表，下拉筛选仍即时生效。

### 复查方法与验证
- **多维度深入审查**：以并行子代理对安全、服务端、前端、依赖/构建四个维度做全面复查，对每个发现独立验证后修复，确认上一轮重构无回归。
- **验证通过**：改动文件语法检查与模块加载通过，`node tests/security.test.js` 安全回归 `135/135` 通过。

### 版本同步
- **版本升级**：应用版本升级至 `v0.0.75`，同步更新 `package.json`、`package-lock.json` 与 README 版本展示。

## [v0.0.74] - 2026-06-03
### 安全加固
- **MCP 出站 SSRF 防护**：外部 MCP JSON-RPC 调用（`mcp-client.js`）和 IM Webhook 推送（`builtin-mcp.js`）在请求发起前新增出站地址校验，拦截 loopback、link-local 和云元数据（如 `169.254.169.254`）等敏感目标；校验在调用时执行并包含 DNS 解析，可阻断 DNS rebinding 绕过，普通局域网地址仍放行以兼容内网模型/服务接入。
- **数据库连接默认校验 TLS 证书**：PostgreSQL（`rejectUnauthorized`）与 SQL Server（`trustServerCertificate`）连接默认改为校验服务端证书，防止开启 SSL 后仍被中间人攻击窃取凭据与数据；新增显式「信任自签名证书」开关（`allowSelfSigned`），仅在用户主动开启时才放行自签名证书。
- **访问日志敏感参数脱敏**：HTTP 访问日志新增 `scrubUrl`，对 URL 查询串中的 `token`、`access_token`、`api_key`、`signature`、`secret` 等敏感参数做 `[REDACTED]` 脱敏，避免附件访问令牌等凭据随 URL 落入日志被重放。

### 性能优化
- **聊天热路径消除同步文件读**：报表 MCP 工具（`builtin-mcp.js`）的文件解析、目录遍历与预览（`resolveReportFile`/`listReportFiles`/`readWorkbookRows`/`readTextPreview`/`queryReportTable`）以及 PDF/图片读取（`document-text.js`/`image-safety.js`）全部从 `fs.readFileSync` 改为异步 `fs.promises`，XLSX 改为异步读 buffer 后再解析，避免大文件同步读阻塞事件循环影响并发请求。
- **监控指标内存增长收敛**：HTTP 指标路由统计在路由未匹配（404、扫描器随机 URL）时归入常量 `unmatched` 桶，不再按原始 URL 无限创建键，防止 `routeStats` Map 无界增长。
- **GPU 监控定时器 unref**：GPU 动态负载监控的 `setInterval` 补齐 `.unref()`，与维护/模型运行时监控一致，不再阻止进程干净退出。

### 工程重构（行为不变）
- **巨型文件按领域拆分**：`agent-runtime.js` 从约 2797 行拆分为 runs / dag-utils / monitoring / preflight / schedules / templates / tool-catalog / workflows / artifacts 等模块（主文件降至约 1571 行）；`routes/chat.js` 的 `/chat` 处理器拆出 errors / mcp-context / rag-context / route-helpers / vision 等模块（路由文件降至约 711 行），按现有导出边界拆分，对外行为保持不变。
- **前端 RAG 模块拆分**：`rag.js` 拆出 `rag-format.js`、`rag-graph-layout.js`、`rag-graph-render.js`、`rag-graph-ui.js`，通过 `window.Pivot.*` 命名空间挂载并带默认值兜底消费。
- **前端 HTML 转义统一**：各处 `escapeHtml`/`escapeRagHtml`/`escapeChatStatusHtml` 统一委托 `PivotSafeHtml.escapeHtml`，`safe-html.js` 提供完整 5 字符转义（含 `"` 与 `'`），消除模块间转义语义分叉。
- **前端工具命名空间合并挂载**：`pivot-core.js` 改为合并挂载到既有 `window.Pivot` 而非首次定义即早退，并桥接 `Pivot.html`，避免工具函数被覆盖丢失。
- **裸 fetch 统一收敛**：`rag.js`、`users.js`、`models.js`、`stats.js` 的裸 `fetch` 全部改用带 CSRF 头与 401 自动刷新的 `apiFetch` 封装。

### 依赖安全
- **修复 6 项依赖漏洞**：`npm audit fix` 非破坏性升级 `axios`（1.15.2 → 1.16.1，含原型污染 MITM 与代理绕过修复）、`qs`、`brace-expansion`、`ip-address`、`express-rate-limit`；保留 `uuid`（仅使用 `v4()`，不受 buffer 越界 CVE 影响）。

### 回归验证
- **错误文案回归修复**：拆分 `agent-runtime` 时误将 rerun / rerun-node / resume 三处面向用户的中文错误提示改成英文，已恢复为「当前任务仍在执行中…」等原中文文案。
- **验证通过**：全部 57 个改动/新增 JS 文件语法检查通过，完整服务端模块成功加载，`node tests/security.test.js` 安全回归 `135/135` 通过。

### 版本同步
- **版本升级**：应用版本升级至 `v0.0.74`，同步更新 `package.json`、`package-lock.json` 与 README 版本展示。

## [v0.0.73] - 2026-06-02
### 知识图谱体验优化
- **关系地图图谱化展示**：知识图谱页面的局部关系视图升级为中文关系地图，节点、关系标签、可信度、统计摘要和操作入口统一中文化，关系类型以“相关、负责、归属、依赖、包含、影响”等标签展示。
- **关系地图交互增强**：关系地图支持空白区域拖拽平移、滚轮缩放、放大/缩小/1:1 复位控件；节点点击继续用于切换中心实体，拖拽逻辑不会再误吞节点点击。
- **节点完整信息提示**：关系地图节点在名称被截断时会保留完整 `title`，并提供悬浮提示数据，包含实体名称、类型、中心/关系数、实体 ID、可信度和描述等可用信息。
- **实体列表可信度完整显示**：左侧实体节点卡片为可信度徽标预留固定宽度，完整显示“可信度 0.xx”，避免被长实体名称挤压成省略号。
- **实体与关系校准弹窗化**：实体校准从页面底部嵌入区域改为独立弹窗；关系编辑也统一进入校准弹窗，浏览关系地图时不再被表单区域挤占。
- **关系明细卡片压缩**：右侧关系明细卡片合并来源、描述和操作按钮为紧凑底部行，减小内边距、行高、按钮高度和卡片间距，提高同屏关系数量；卡片悬浮可查看完整起点、关系、终点、来源、可信度和描述。
- **页面顶部与工具栏对齐修正**：知识图谱全屏页清除外层弹层默认 padding，标题行、关闭按钮和内容起点对齐知识库页面；统计标签与搜索筛选合并到同一行，减少顶部占用空间。

### 系统监控展示修复
- **慢查询与异常告警滚动修复**：系统监控页慢查询和异常告警区域补齐内部滚动约束，长列表可在卡片内部滚动查看，不再被外层布局截断。

### 文档与版本同步
- **文档同步**：README、使用手册和 CHANGELOG 已记录本轮知识图谱交互、校准弹窗、关系明细密度、顶部对齐和系统监控滚动修复。
- **版本升级**：应用版本升级至 `v0.0.73`，同步更新 `package.json`、`package-lock.json` 与 README 版本展示。
- **验证通过**：已运行 `npm run check` 与 `npm run lint`。

## [v0.0.72] - 2026-06-01
### 公告中心体验优化
- **登录页公告公开展示**：公告新增“登录页展示”开关和公开只读接口，未登录用户可在登录页查看管理员发布的全员公开公告；仅内置超级管理员 `admin` 可设置该开关，避免普通定向公告被公开泄露。
- **登录页公告布局优化**：登录页公告从登录框内移至左侧独立面板，桌面端与登录卡片分离展示，窄屏下自动收敛到登录卡片下方，避免遮挡登录表单。
- **主页公告入口迁移**：聊天主页公告入口从右下角悬浮按钮移动到左侧系统名称旁，公告中心弹层同步从品牌区附近打开，减少与输入框发送按钮和页面浮动操作的视觉冲突。
- **不再提示语义收紧**：站内公告“隐藏/不再显示”统一改为“ 不再提示 ”；普通公告点击后会记录当前用户已读并从站内公告入口、横幅和公告中心隐藏，强制确认公告在确认前不提供“不再提示”入口。

### 公告管理
- **登录页展示独立列**：公告管理表格新增“登录页”列，以“是/否”徽标展示登录页公开状态，标题列不再混排“登录页展示”标记，表格扫描更清晰。
- **权限与迁移强化**：补齐 `show_on_login` 字段迁移和索引顺序，老库升级会先补列再建索引；登录页公告仍要求全员投放且仅超级管理员可配置。
- **详情与操作增强**：公告管理表新增序号列和详情按钮，详情弹窗展示投放范围、登录页展示、确认/阅读情况、创建与更新时间；编辑、撤下、删除按钮继续按 `canEdit/canDelete` 权限控制。

### 回归验证
- **公告状态测试扩展**：公告路由测试覆盖登录页公开公告、普通管理员禁止发布登录页公告、普通公告“不再提示”后同时置为已读并从 active 列表消失、强制确认公告确认状态持久化。
- **验证通过**：已运行 `npm run check`、`npm run lint`、`node tests/security.test.js`，安全回归 `135/135` 通过。

### 版本同步
- **版本升级**：应用版本升级至 `v0.0.72`，同步记录公告中心、登录页公告、公告管理表格和“不再提示”语义优化。

## [v0.0.71] - 2026-06-01
### 公告中心
- **新增系统公告能力**：新增公告数据表、用户阅读确认状态表和完整 API，支持公告类型、优先级、发布窗口、草稿/发布/归档状态、软删除和审计记录。
- **支持定向投放**：管理员可按全员、单位、角色或指定用户发布公告；普通用户只会看到命中自己范围且处于有效时间窗口内的公告。
- **用户端通知体验**：聊天页新增顶部公告横幅、右下角公告中心和强制确认弹窗；公告支持标为已读、关闭和确认，强制确认公告会阻止用户忽略关键通知。
### 后台管理
- **新增公告管理页**：系统设置新增“公告管理”页，提供公告搜索、状态筛选、新建/编辑/删除、确认要求和投放范围配置。
- **回归测试补齐**：新增公告路由测试，覆盖管理员创建、单位定向可见性、非目标用户隔离、确认/关闭状态落库和软删除审计链路。
### 版本同步
- **版本升级**：应用版本升级至 `v0.0.71`，同步记录公告中心、公告管理页和公告测试覆盖。

## [v0.0.70] - 2026-06-01
### 部署与镜像构建
- **Compose 构建入口补齐**：`docker-compose.yml` 移除新版 Docker Compose 已废弃的 `version` 字段，并为 `pivot` 服务补充 `build.context` 与 `build.dockerfile`，确保 `docker compose build --no-cache` 能直接使用当前目录的 `Dockerfile` 构建 `pivot` 镜像。

### 模型端点本地识别
- **Docker 容器名不再误标远端**：系统监控的模型端点状态会在 Pivot 运行于 Docker/容器环境时，把 `llama-server`、`ollama` 等单段 Docker 服务名识别为本地端点，不再显示为“远端”。
- **本地别名识别增强**：保留 `PIVOT_TRUST_DOCKER_INTERNAL_HOSTS=true/false` 显式开关，新增 `PIVOT_RUNNING_IN_CONTAINER=true/false` 显式容器运行声明；同时补充本机域名解析到本机 IP 时的本地识别能力，提升公网域名、内网别名和容器网络混合部署下的监控准确性。

### RAG 上下文预算裁剪
- **用户态 RAG 注入可被预算器识别**：`fitMessagesToContextBudget` 现在会优先识别 `PIVOT_RAG_CONTEXT_BEGIN/END` 稳定边界标记，即使知识库上下文以 `role: "user"` 插入，也会按 RAG 生成型上下文优先裁剪或丢弃，而不是被误当成普通历史消息。
- **旧版 RAG system 消息继续兼容**：预算器仍保留对历史 system 形式知识库提示的识别，兼容旧会话、旧测试和可能的第三方调用链路。

### 回归验证
- **专项用例补齐**：新增 user-role RAG 上下文预算裁剪测试，以及 Docker 容器服务名本地识别测试，覆盖显式开启、显式关闭和容器运行声明场景。
- **验证通过**：已运行 `node tests/security.test.js`，`134/134` 通过。

### 版本同步
- **版本升级**：应用版本升级至 `v0.0.70`，同步记录部署构建、模型端点本地识别和 RAG 上下文预算裁剪修复。

## [v0.0.69] - 2026-06-01
### 在线更新程序移除
- **功能入口彻底清除**：移除系统设置中的“在线更新”导航、页面片段、按钮绑定、前端脚本和专用样式，避免页面继续暴露已废弃的更新入口。
- **后端链路下线**：删除 updater 相关 API 路由、客户端封装、自动轮询监控服务和启动挂载逻辑，主应用不再读取或调用 updater sidecar。
- **部署面收口**：移除 `updater/` sidecar 程序、Dockerfile、独立 package、Compose `pivot-updater` 服务、`online-update` profile、Docker socket 挂载和 updater 环境变量示例。
- **文档与检查同步**：README、CHANGELOG、`.env.example`、聊天资源检查和语法检查目录均同步移除在线更新相关说明与强制校验项，并完成全局残留扫描。

### 设置页与代码质量
- **设置页滚动条修复**：系统设置缩放画布不再把旧页面高度带到新 tab；重新测量前清理舞台高度，并对 2px 内测量误差做容差处理，减少短页面误出现垂直滚动条。
- **浏览器全局调用规范化**：知识图谱关系编辑中的 `prompt` 改为 `window.prompt`，与项目其它前端调用保持一致并修复 ESLint `no-undef`。
- **浏览器全局变量显式化**：设置页缩放脚本中的 `requestAnimationFrame`、`cancelAnimationFrame` 和 `ResizeObserver` 调用统一显式挂到 `window`，减少 lint 噪音。

### 版本与验证
- **版本升级**：应用版本升级至 `v0.0.69`，同步记录在线更新程序移除、设置页滚动修复和 lint 清理。
- **验证通过**：`npm run check`、`npm run lint` 已通过；`updater`、`在线更新`、`PIVOT_UPDATE`、`PIVOT_ONLINE`、`pivot-updater`、`online-update` 残留扫描为零。

## [v0.0.68] - 2026-06-01
### 知识图谱与 Graph-RAG
- **知识图谱三阶段落地**：新增实体、实体提及、关系三类图谱数据表与索引服务，支持从知识库分块中抽取系统、部门、流程、文档等实体和关系，并在文档重建时自动清理旧图谱数据后重新构建。
- **Graph-RAG 增强检索**：知识库召回链路会把图谱邻居关系纳入候选召回与上下文增强，命中实体时追加“参考知识图谱信息”，调试检索结果同步返回图谱上下文，便于排查召回依据。
- **企业级图谱治理接口**：新增图谱摘要、实体查询、关系查询、实体详情、实体编辑、实体合并、关系编辑、关系删除和单文档图谱重建接口，支撑后续人工校正和实体合并流程。
- **知识图谱工作台**：知识库页面新增知识图谱入口，支持实体搜索、类型筛选、实体列表、局部关系图、关系列表、实体编辑、实体合并、关系编辑删除和文档图谱重建。
- **全屏图谱体验优化**：知识图谱页面改为真正全屏承载，样式、字体、字号、搜索框、下拉框、按钮、统计标签和面板密度对齐知识库页面，适配未来大量节点的企业级查看场景。
- **刷新状态保持**：在知识图谱页面刷新浏览器后会继续停留在知识图谱页面，并尽量恢复当前文档上下文；关闭知识图谱或知识库时会清理恢复状态，避免误回到旧页面。

### 版本治理
- **单一版本锚点**：继续以 `CHANGELOG.md` 顶部最新版本标题作为运行时版本来源，并新增版本同步脚本，可自动把最新版本同步到 `package.json`、`package-lock.json` 和 README 展示版本。
- **版本升级**：应用版本升级至 `v0.0.68`，同步记录本轮 Graph-RAG、知识图谱工作台、全屏样式、刷新保持、设置页适配和版本锚点自动同步能力。

### 管理界面与弹窗适配
- **设置页比例缩放**：系统设置工作区新增固定宽度画布与视口比例缩放，宽屏保留完整信息密度，窄屏自动缩放并保持内部滚动，避免监控、审计、用量和 API 账号页在主工作区内被挤乱。
- **模型配置弹窗收口**：模型配置表单改为“内容区滚动 + 底部操作栏固定”的布局，模型能力开关、费用配置、并发与健康检查字段在不同高度窗口下都能稳定访问。
- **全局弹窗边界兜底**：统一限制弹窗和覆盖层的最大宽高与内部滚动，附件预览、监控热点接口、输入提示、重命名和标签弹窗不再在小窗口或缩放环境中溢出视口。
- **管理表格滚动修复**：审计日志、系统监控、会话搜索结果等表格区域补齐内部滚动与最小高度约束，减少底部内容不可见或横向布局被强行折叠的问题。

### 验证
- **静态检查**：`npm run check` 已通过。
- **图谱回归测试**：新增知识图谱抽取、索引、检索增强和人工治理动作测试；图谱相关用例已通过。完整 `tests/security.test.js` 在当前 Windows 沙箱中仍受既有上传目录清理 `EPERM` 影响，非本轮图谱功能回归。

## [v0.0.67] - 2026-05-31
### 聊天元数据实时补齐
- **失败消息元数据即时显示**：模型端点熔断、上游异常等 `assistant_error` 事件会立即刷新消息底部的耗时、Token 和速率，不再需要刷新页面后才显示完整元数据。
- **错误事件 token 回传**：后端保存失败消息时同步返回 `tokenCount`，前端使用该值更新当前气泡 footer，并异步写回 `/api/chat/stats` 保持刷新前后一致。
- **版本同步**：应用版本升级至 `v0.0.67`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG。

## [v0.0.64] - 2026-05-31
### Markdown 消息渲染与横向滚动
- **整段 Markdown 围栏自动去壳**：聊天渲染在遇到整条回复被外层 `markdown` / `text/plain` / `plain` 等代码围栏包住时，会先去掉外层壳再按 Markdown 渲染；流式输出过程中即使外层围栏尚未闭合，也会避免把整段正文临时显示成巨大代码块。
- **局部滚动与换行兜底**：代码块、表格和 prose 类代码块统一限制在消息气泡内部，真实代码继续保留局部横向滚动，Markdown/text 误包裹内容会自动换行，避免必须滚到块底部才能拖动横向滚动条；普通无序列表恢复项目符号，只对 checkbox 任务列表隐藏 bullet。
- **资源检查回归覆盖**：`scripts/check_chat_assets.js` 新增渲染沙盒校验，覆盖外层 Markdown 去壳、未闭合 prose 围栏、真实代码块保留、嵌入式 Markdown 示例和表格滚动容器。
### 流式输出体验优化
- **非标准上游响应回放**：当上游模型虽然请求了 `stream: true`，但实际返回完整 Chat JSON 或单个完整 message payload 时，聊天接口会提取完整回答并按 SSE 内容事件回放，避免保存空回复或只在结束时一次性展示。
- **前端大块增量平滑拆分**：聊天前端对单次返回的大段 `content` 增量做本地分块回放，首段立即显示，后续按短间隔渐进渲染；真实流式小增量仍保持原有即时刷新，长回答不会因为上游攒包而突然整段跳出。
### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.64`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG，以刷新聊天页脚本版本参数。

## [v0.0.63] - 2026-05-31
### 聊天错误持久化
- **模型失败可回看**：聊天接口在模型响应异常、流传输中断、队列/端点繁忙、已保存用户消息后的上下文超限等失败分支，会同步写入一条 `assistant` 错误消息，内容包含错误摘要、状态码/错误代码和截断后的详情，刷新会话后仍能看到失败原因。
- **前端临时气泡绑定落库消息**：聊天流收到 `assistant_error` 事件时会把当前临时助手气泡绑定到后端返回的 `messageId`，并渲染已保存的错误内容，避免刷新后只剩用户消息。
- **错误正文去状态化**：持久化错误消息只保留真实失败原因，不再追加“错误已保存、刷新可查看”等系统状态说明，避免污染会话内容。
- **回归测试**：新增聊天路由上游模型 500 失败用例，验证 SSE 返回错误消息 ID 且数据库中保留对应 assistant 错误记录。

### 长上下文模型预算修复
- **输入上限不再被默认 16K 暗中截断**：当模型配置了 `max_input_tokens`（例如 `256K`）但没有单独配置总上下文窗口时，上下文预算会以该输入上限推导有效窗口，不再被 `DEFAULT_CONTEXT_WINDOW_TOKENS=16384` 限制，避免 20K 文档附件在 256K 模型上仍提示输入过长。
- **移除固定默认上下文窗口**：未配置模型输入上限或显式总窗口时，不再套用固定 `16K` 预拦截；系统会把请求交给上游模型，由模型服务返回真实窗口限制，避免不同模型被同一个本地默认值误伤。
- **回归测试**：新增 256K 输入窗口用例，验证超过旧 16K 默认窗口的附件型长输入可通过预算检查。

### 助手回复模型标识
- **消息底部显示模型名**：助手消息的底部统计栏新增模型标识，历史消息通过 `messages.model_id` 联表展示模型名称，新生成消息直接显示当前选中的模型，便于回看每条回复由哪个模型生成。

### 上下文压缩阈值后台可配置
- **阈值从硬编码改为运行时设置**：会话上下文压缩阈值不再固定为 `MEMORY_THRESHOLD` 启动常量，而是写入 `app_settings.memory_threshold` 并由 `server/llm.js` 在上下文元信息、自动压缩判断和手动压缩判断中动态读取；`.env` / `.env.example` 中的 `MEMORY_THRESHOLD` 仅作为首次初始化默认值，后续管理员保存后即时生效。
- **管理员设置入口补齐**：新增 `server/services/memory-config.js` 统一解析 `12K`、`1M`、纯数字等 Token 表达，并限制有效范围为 `256` 到 `10M`；新增 `PUT /api/admin/settings/memory`，允许管理员按模型上下文窗口灵活调整压缩阈值，同时保持普通管理员无法修改其它全局系统设置的权限边界。
- **前端入口收紧**：模型配置页新增紧凑的“上下文阈值”输入与保存按钮，直接显示在“添加模型”按钮旁边，不再占用单独的大块横幅区域；保存成功后会刷新当前会话上下文用量展示。
- **保存按钮事件修复**：上下文阈值保存按钮移除内联 `onclick`，改为脚本事件监听，兼容当前 `script-src-attr 'none'` 的 CSP 策略，避免点击保存没有反应。
- **文档与回归测试同步**：`.env`、`.env.example` 与 README 补充中文说明，标明环境变量仅为初始化默认值；安全测试新增阈值解析与管理员保存权限用例，覆盖 `1M` 等长上下文模型场景。
### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.63`，同步更新 `package.json`、`package-lock.json`、README 与 CHANGELOG。
- **回归验证**：已运行 `npm run check`、`npm run lint`；完整 `node tests/security.test.js` 中本次新增用例已通过，现有上传/清理相关用例在 Windows 环境中仍可能因临时文件删除 `EPERM` 失败。

## [v0.0.62] - 2026-05-31
### 管理员账号自愈与安全保障
- **内置管理员账号自愈**：重构并新增了内置管理员 `admin` 账号的自愈与修复机制。在系统每次启动或运行数据预置时，会自动触发对管理员账号的检测与检查。如果发现该账号被误降级为普通用户、被禁用或被意外软删除，系统会自动触发自愈操作，重新将其恢复为正常的 `admin` 角色与 `active` 启用状态。

### Node.js v24.x 运行兼容性调优
- **better-sqlite3 升级**：将原生 SQLite 依赖包 `better-sqlite3` 由 `^9.4.3` 升级至 **`^11.3.0`**。完美适配 Node.js v24 在 Windows/Linux 平台下的运行，直接下载官方预编译二进制包，避免了全新拉取仓库时由于缺少本地 node-gyp 编译环境所导致的 `npm install` 失败以及文件锁定错误。

### 自动化安全回归测试
- **安全回归测试加固**：在 `tests/security.test.js` 中新增了 `'seed repair restores built-in admin role and login status'` 测试用例，对受损管理员账户的自愈修补、状态激活、被修改属性的恢复进行了全路径校验。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.62`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG。

## [v0.0.61] - 2026-05-29
### 智能体工作流编排升级
- **工作流库与版本治理**：工作流编排支持保存到工作流库、加载已有工作流、发布稳定版本、查看版本列表、版本差异对比、回滚历史版本和删除工作流；计划任务可优先引用已发布版本，否则使用当前画布快照，降低周期任务因草稿变动产生的不确定性。
- **画布编辑体验重构**：节点编辑从画布下方迁移为右侧抽屉，JSON 高级视图迁移为弹窗；画布页面顶部的工作流名称、工作流库、加载、版本、发布、删除等控件重新对齐，输入框尺寸和字号收紧，去除多余文本残留。
- **节点配置能力补齐**：节点支持失败策略、重试次数、超时、工具参数模板、输入 schema 提示和变量插入；任务详情中失败的 DAG 节点可单独重跑，节点输入/输出、耗时、尝试次数和条件更清晰。
- **工具选择器易用性优化**：节点工具下拉改为“中文名称 + 分类分组 + 描述提示”，按知识与会话、数据库、报表与文件、图表与展示、数据处理、文档处理、格式转换、消息通知、系统诊断和外部能力分组展示；选中后显示用途、来源、审批标签和内部工具 ID。
- **运行前状态与校验**：智能体任务面板在工作流编排模式下显示当前画布状态，非 DAG 模式隐藏工作流运行输入；运行前会阻断空工作流、未选择工具节点和非法 JSON，避免用户提交不可执行任务。
- **模板与工作流闭环**：DAG 模板会保存工作流快照、运行输入、工作流引用和版本信息，应用模板时同步恢复画布、输入和运行模式，不再只恢复目标与策略。

### 智能体功能完整性与后端契约
- **模型路由策略动态加载**：智能体模型路由下拉改为从 `/api/agents/model-routers` 动态读取，前端不再硬编码策略枚举。
- **工作流后端接口完善**：新增并完善工作流 CRUD、发布、版本列表、版本 diff、版本回滚、DAG 节点重跑、结果版本保存/对比/回滚等接口与审计动作，数据库迁移同步补齐 `agent_workflows`、`agent_workflow_versions`、模板 DAG 字段和 DAG 节点治理字段。
- **工具加载稳定性修复**：即使“可用工具”列表面板尚未渲染，工作流节点抽屉也会先加载工具缓存，修复节点工具下拉为空的问题。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.61`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、README、使用手册与 CHANGELOG。
- **回归验证**：已运行 `npm run check` 与 `git diff --check`；本地服务 `http://localhost:3000` 可达。Codex 内置浏览器因当前会话安全策略拒绝访问 localhost:3000，未进行浏览器点击级截图验证。

## [v0.0.60] - 2026-05-28
### 长会话上下文压缩链路修复
- **自动压缩同步生效**：达到上下文阈值时，模型请求不再后台 fire-and-forget 压缩后仍使用旧上下文，而是先完成压缩、重新读取消息，再组装当前这次请求的模型上下文，确保本轮调用立即使用压缩后的摘要与近期消息。
- **摘要顺序修正**：长期摘要会排在近期消息之前，避免模型先看到后续对话再看到历史摘要造成时间线混乱。
- **手动压缩状态提示优化**：手动压缩遇到已有压缩任务时，接口返回更明确的进行中/重复任务提示，前端显示 warning toast，不再误导为“没有可压缩内容”。
- **回归测试补齐**：新增自动压缩落库、旧消息软归档、当前上下文包含摘要且摘要顺序正确的回归测试，覆盖长会话首次触发压缩的关键路径。

### 刷新后停留当前页面
- **主工作区刷新恢复**：前端使用当前标签页级别的 `sessionStorage` 记录活动工作区，刷新后会恢复聊天、知识库、能力库、智能体、工作流编排、手册、设置和打印/导出工作区，而不是固定回到聊天首页。
- **聊天与打印位置恢复**：聊天页会记住当前会话，打印/导出页会记住对应会话 ID，刷新后尽量回到原会话或原打印预览。
- **设置页子 tab 恢复**：设置页会记录当前子 tab，刷新后恢复到原 tab；普通用户如果恢复到管理员专属 tab，会自动回退到可访问的模型配置页，避免权限越界。

### 会话消息操作稳定性
- **消息复制按钮兜底修复**：会话消息复制改为异步捕获剪贴板写入异常，并在剪贴板 API 不可用、非安全上下文或权限失败时回退到隐藏 textarea 复制，避免点击复制按钮时抛出未处理异常。
- **重新生成知识库命中修复**：点击助手消息的重新生成时，如果请求体没有新的用户输入，后端会从当前会话历史中取最近一条真实用户消息作为 RAG 检索词，避免原本已命中知识库的回答重新生成后显示“未命中知识库”。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.60`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。
- **回归验证**：已运行 `npm run check`、`npm run lint`、专项 ESLint 与 `node tests/security.test.js`，覆盖前端资源、语法、刷新状态恢复、消息复制、重新生成 RAG 查询和完整安全回归测试。

## [v0.0.59] - 2026-05-28
### 知识库会话回答链路优化
- **RAG 上下文注入策略修复**：普通对话启用知识库且召回命中后，不再把知识库片段追加为末尾 `system` 消息，改为包装成带强约束的知识库上下文并插入到当前用户问题之前，提升 OpenAI-compatible 网关和本地模型对知识库依据的遵循稳定性。
- **知识库回答约束增强**：知识库上下文新增明确规则，要求本轮优先依据检索结果回答，知识库与历史会话或模型记忆冲突时以知识库为准；资料不足时明确说明“知识库中未找到足够依据”，减少模型自行编造。
- **会话侧命中状态可见**：聊天流新增 `rag` 状态事件，前端会展示“知识库已命中”或“知识库未命中”，便于用户确认本轮是否真正使用了知识库，而不是只看到图标点亮。
- **监控命中率口径修正**：系统监控中的“命中率”改为展示 RAG 召回到可用知识片段的比例，不再误用缓存命中率；同时新增“缓存命中率”单独展示缓存复用情况。
- **监控统计巡检修正**：模型端点状态保留在数据概览页面；系统监控“索引分片”改为统计当前 ready、未删除且启用的可召回分片数；Prometheus HTTP 直方图 bucket 输出恢复为正确累计计数。
- **审计动作口径修正**：发送按钮点击不再把浏览器事件误传成 `regenerate=true`，后端也只接受显式 true，避免首次发送被记为“重新生成回答”。
- **回归测试补齐**：新增 RAG 会话注入顺序与强约束提示测试，覆盖知识库上下文必须紧贴当前问题、并保留引用来源提示的关键行为。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.59`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。
- **回归验证**：已运行 `npm run check`、`npm run lint` 与 `node tests/security.test.js`，覆盖前端资源、语法、Lint、知识库会话注入和完整安全回归测试。

## [v0.0.58] - 2026-05-27
### 附件上传与会话消息同步
- **图片消息即时显示修复**：发送带图片或附件的消息时，前端会在清空待发送附件前固化附件快照，并用独立的用户消息附件渲染链路展示图片和文件卡片，避免上传图片分析后会话框不显示用户消息、必须刷新才出现。
- **附件预览与消息渲染优化**：统一按 MIME 与扩展名识别图片，补齐图片 Markdown 渲染器、消息图片尺寸与加载后滚动贴底，文件附件继续以预览/下载卡片显示。
- **上传会话归属加固**：附件上传必须携带当前会话 ID，后端校验会话属于当前用户；前端切换会话时清空未发送附件，上传批次锁定初始会话，避免上传中的附件落到错误会话或污染当前输入框。
- **附件 URL 编码统一**：新增安全的附件 URL 编码方法，上传响应、附件库、管理员附件列表、会话历史补 token 统一处理空格、括号等文件名字符，并兼容旧的未编码历史 URL。
- **模型视觉解析兼容性增强**：历史上下文、视觉消息构建和模型能力判断均支持编码/旧式附件图片 Markdown，避免刷新后能显示但模型侧无法解析图片。
- **附件安全与清理**：缺失会话、无权会话或不存在会话的上传会被拒绝并清理临时文件；图片检测在 MIME 不可靠时回退到扩展名，减少浏览器或平台 MIME 差异导致的误判。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.58`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。
- **回归验证**：已运行专项 ESLint、`node tests/security.test.js`、完整 `npm test` 与本地 HTTP 首页响应检查；覆盖附件 URL 编码、会话归属、历史 token 补齐、视觉输入识别和前端资源检查。

## [v0.0.57] - 2026-05-27
### 用户注册与账号治理
- **开放注册后台开关**：新增运行时开放注册设置，内置 `admin` 可在“系统设置 > 用户管理”右上角直接开启或关闭；设置写入 `app_settings.allow_public_registration`，`.env` 中的 `ALLOW_PUBLIC_REGISTRATION` 仅作为首次初始化默认值，后续切换无需重启应用。
- **注册入口联动**：`/api/auth/config` 与登录页注册入口实时读取开放注册状态，关闭时隐藏注册入口并拦截 `/auth/register`，开启后自注册账号默认创建为普通用户。
- **注册错误提示优化**：注册、管理员添加用户、重置密码和个人改密统一使用 `UserInputError` 返回 400；密码、用户名格式、用户名重复等可预期输入错误会直接展示给用户，不再落入生产环境“系统内部错误”。
- **密码规则体验优化**：密码规则统一为“至少 8 位，并同时包含字母和数字”；注册页密码框内直接显示规则提示，前端会在提交前提示缺少长度、字母或数字的具体原因。
- **用户管理交互优化**：用户列表加载支持页码回退，开放注册开关以紧凑行内控件显示在用户操作区；导入、保存、重置密码等失败时优先展示后端返回的明确原因。

### 上下文压缩与聊天状态
- **手动压缩入口补齐**：上下文用量圆环可点击触发当前会话压缩，压缩中显示忙碌态，完成后刷新当前会话和上下文用量。
- **上下文接口增强**：会话上下文接口补齐实时 token 用量查询与手动压缩返回信息，前端可在消息保存、统计写入和压缩完成后主动刷新显示。
- **模型调用超时治理**：记忆压缩与模型调用链路加入更明确的超时、取消和错误返回，减少上游模型慢响应导致的悬挂或延迟写入。

### OpenAI 兼容接口
- **模型发现列表修复**：`/v1/models` 不再混入内置工具伪模型 `pivot-tools`，避免第三方客户端自动获取模型列表时误识别为可选大模型；内置工具能力继续通过 `/v1/tools` 与显式工具调用路径提供。

### 运营与审计体验
- **审计日志显示名补齐**：审计列表和导出内容新增显示名字段，管理员排查用户操作时不再只依赖用户名。
- **用量与 API 账户展示优化**：用量明细、API Key 与第三方调用记录继续收紧列宽、显示名、Token 统计和空态提示，提升管理端表格可读性。
- **前端文案与布局收口**：注册、账号安全、用户管理和上下文用量相关文案统一为更明确的用户侧表达，减少需要管理员猜测原因的错误提示。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.57`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。
- **回归验证**：已运行聊天资源检查、语法检查、定向 ESLint、密码校验、开放注册设置读写验证与 OpenAI 模型发现回归测试，覆盖本轮注册、用户管理、兼容接口和前端资源组装路径。

## [v0.0.56] - 2026-05-26
### 聊天流式体验与消息同步
- **用户消息显示修复**：流式回复保存完成后会同步回填用户消息与助手消息的持久化 ID，并刷新当前会话上下文用量；如果当前会话 DOM 中缺少刚发送或刚生成的消息，会自动重新加载当前会话，避免用户消息需要刷新页面才显示。
- **代码块流式滚动修复**：生成过程中会同时保持聊天列表和最新代码块贴近底部；当用户主动向上滚动查看历史时不会强制抢回视口，代码块内容会跟随最新增量显示在可见位置。
- **上下文用量实时刷新**：新增前端 `refreshCurrentContextUsage` 刷新入口，消息保存和统计写入后会重新拉取当前会话上下文元信息，避免输入框圆环长期停留在旧百分比。

### 上下文压缩交互与策略
- **手动压缩入口补齐**：输入框上下文圆环改为可点击按钮，压缩中显示忙碌态，完成后提示结果、刷新用量，并在当前会话重新加载压缩后的消息视图。
- **上下文接口补齐**：新增 `GET /api/sessions/:id/context` 查询实时上下文用量，新增 `POST /api/sessions/:id/compact` 手动压缩当前会话；手动压缩会复用当前选择模型或默认模型，并记录“手动压缩上下文”审计动作。
- **压缩超时治理**：记忆压缩默认超时从 60 秒放宽到 180 秒，超时后会取消上游模型请求并返回明确的 504 提示，避免接口已超时但模型稍后返回又继续写入摘要。
- **自动压缩触发调整**：后台压缩不再等待“活跃消息超过 10 条”，默认在活跃上下文 token 超过 `MEMORY_THRESHOLD`（当前 12K）且存在可归档早期消息时触发；`MEMORY_MIN_MESSAGES_TO_COMPRESS` 默认降为 `1`，`MEMORY_SUMMARY_KEEP_COUNT` 可配置保留最近消息数量，更适合单条超长输入的场景。
- **压缩链路兼容增强**：记忆压缩调用统一复用模型适配器生成 OpenAI-compatible URL 与鉴权头，压缩成功会返回摘要条数和摘要 token，失败会记录并向手动接口返回明确错误。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.56`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。
- **回归验证**：`npm run check` 已覆盖本轮聊天资源、语法和文本文档完整性检查。

## [v0.0.55] - 2026-05-26
### 能力库与工具链稳定性
- **能力库全链路复核**：补齐模型到能力工具调用的筛选与规划步骤，用户提出“查数据并生成图表”等复合任务时，会先暴露查询/取数能力，在查询成功后继续衔接图表生成能力，减少模型直接跳到图表或遗漏取数工具的情况。
- **图表生成链路加固**：确认 `pivot-echart` 渲染路径与聊天回答中的图表块识别逻辑可稳定工作，查询结果可以继续转为可视化图表展示。
- **用户侧术语统一**：继续清理普通对话、智能体审批、能力工作台和常见状态中的旧实现名，用户界面统一使用“能力库 / 能力服务 / 检索配置”等表达，内部兼容命名保留但不再暴露给日常使用路径。

### 知识库检索链路优化
- **召回候选合并优化**：全文检索命中不再被候选兜底逻辑覆盖，改为在已有命中基础上补充候选，提升关键词命中、向量候选和兜底候选共同参与排序的稳定性。
- **空库体验修正**：当知识库为空或没有候选分块时，不再先调用向量服务，避免把“没有可检索内容”误报成向量配置失败。
- **空文档状态修正**：未解析出可索引文本的文件会明确进入失败状态并提示“未解析出可索引文本”，不再显示为“就绪 0 分块”。
- **个人检索配置摘要修正**：知识库召回摘要会反映当前用户自己的检索配置，而不是只展示全局默认配置，便于排查个人配置下的召回表现。
- **索引超时治理**：文档入库索引使用独立的向量请求超时，默认从 30 秒放宽到 120 秒，并将底层 timeout 转换为可读的检索配置提示，减少大 PDF 或本地向量服务冷启动时的误失败。
- **检索配置文案统一**：知识库页面与相关管理入口将技术化表述收口为“检索配置 / 召回测试”，降低普通用户理解成本。

### 知识库文件格式扩展
- **新增文档类型**：知识库上传与索引支持从 `doc`、`docx`、`xls`、`xlsx`、`csv`、`json`、`html`、`htm` 中提取文本并建立分块索引，保留原有 `txt`、`md`、`pdf` 支持。
- **解析链路复用**：格式扩展接入既有文档抽取流程，而不是只放宽后缀校验，确保文件内容会先解析成可索引文本，再进入分块、索引和召回链路。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.55`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。
- **工程质量收口**：补齐前端 lint 环境中的浏览器全局声明，清理能力库与智能体工作台新增代码里的未使用变量，并将 `npm test` 收紧为 `check + lint + security.test.js`，避免发版前漏跑 lint。
- **列表查询减压**：智能体任务列表改为先分页再聚合步骤/工具/错误统计，减少每行相关子查询；会话侧栏加载默认跳过总数统计，使用 `limit + 1` 与游标判断是否还有更多，保留后端 `includeTotal` 默认兼容。
- **回归验证**：`npm run check`、`npm run lint` 与 `node tests/security.test.js` 已用于覆盖本轮能力库、知识库和文件格式扩展相关回归路径。

## [v0.0.54] - 2026-05-25
### 能力库工作台升级
- **MCP 页面升级为能力库**：导航、标题、分区与治理信息统一改为“能力库 / 系统能力 / 个人能力 / 能力治理”，保留底层 MCP 兼容能力，但面向用户的表达更贴近“可调用能力”。
- **系统能力卡片化**：图表生成、报告编排、文档解析、数据处理、格式转换改为系统能力卡片，支持右上角椭圆开关、卡片内接入工具数量、工具弹窗查看名称与说明，刷新工具入口收口到工具弹窗内。
- **个人能力卡片化**：数据库连接、报表文件、消息通知与已配置的个人连接服务统一显示在个人能力区域；数据库连接卡片去掉连接地址和刷新状态噪声，已配置连接以独立卡片展示编辑、工具、删除和启停能力。
- **工具文案清理**：工具弹窗与智能体能力清单去除“内置”和多余“MCP”前缀，长工具名改为在卡片下方完整显示，悬停可查看完整信息，避免名称被徽标或卡片边界截断。
- **图表渲染兜底**：聊天回答中的 `chart` / `CHART` / `pivot-chart` 图表代码块会自动识别并渲染为图表，兼容 `data.x + data.y + config` 简化格式，避免图表生成结果退化成 JSON 代码块。
- **图表样式优化**：图表标题、图例、坐标轴名称和绘图区间距重新调整，Y 轴名称改为居中纵向显示，避免与标题或刻度文字重叠。
- **回复状态文案统一**：普通对话和智能体审批状态中的“调用 MCP 工具”改为“调用能力库工具”，避免用户在新版界面里看到旧名称。
- **能力治理合并**：去除单独“能力源”说明区，将刷新入口与活跃、异常、未检查、数据库等统计合并到治理栏，状态按钮统一白色样式并去除彩色圆点。

### 智能体工作台与工作流编排
- **工作流编排独立承载**：DAG 改名为“工作流编排”，从高级设置中移出，作为智能体任务面板上的功能按钮打开完整页面；画布去除垂直滚动条，支持鼠标平移、缩放、适配画布、小地图、节点预设、校验、草稿保存和正式保存。
- **高级设置前置**：原高级设置弹窗的运行模式、工具审批、模型路由、Token 预算、失败重试、上下文来源、补充说明等参数直接显示在任务面板中，运行参数对齐为紧凑表单，“允许能力库工具”改为椭圆轨道开关。
- **模板与计划弹窗重排**：模板库、计划执行、计划队列、通知中心重构为大尺寸多列布局，减少大面积空白，提升模板保存、定时计划和通知查看的可读性。
- **能力与结果弹窗重排**：能力清单、可用工具、结果沉淀改为更紧凑的三列视图；可用工具去重并缩小卡片，一行可显示更多工具，超出高度后独立滚动，悬停只展示完整工具信息。
- **工作流入口整理**：工作流编排、模板与计划、能力与结果三个按钮同排展示，移除高级设置里重复的工作流说明卡，降低新任务区域的操作层级。

### 任务记录表格化
- **任务列表改为数据表格**：智能体任务记录从左侧卡片列表 + 右侧详情改为统一表格，新增序号列、固定任务列宽、状态列和操作列；详情按钮打开弹窗展示原执行详情内容，删除按钮直接位于操作列。
- **字段拆分与 Token 统计**：模型、模式、步骤、工具、错误、输入 Token、输出 Token、总 Token、创建时间、状态分列展示；Token 数值统一按 K / M / B 压缩显示，顶部统计也同步使用同一格式。
- **任务检索优化**：任务状态筛选、统计胶囊和搜索框合并为一行，搜索框缩短；任务列悬停只显示被截断的任务内容，不再重复显示表格中已有字段。
- **后端分页支持**：`/api/agents/runs` 新增 `page`、`limit`、`total` 返回与状态/关键字过滤，前端按页加载任务记录，序号按页码连续计算。

### 全局表格与分页统一
- **分页组件抽取**：新增 `renderWorkspacePagination(...)` 轻量渲染函数，智能体任务记录和知识库页面复用统一分页控件。
- **统一分页样式**：全局 `.pagination`、工作区分页与系统设置相关表格分页统一按钮尺寸、间距、字体和与表格之间的距离，避免各页面分页控件大小不一。
- **知识库分页同步**：知识库列表分页改为复用全局分页渲染与样式，移除局部 `rag-page-btn` 绑定逻辑。

### 工程拆分与内置能力
- **智能体脚本拆分**：将 MCP/能力库工作台相关逻辑拆出到 `client/chat/mcp-workbench.js`，缓解 `agents.js` 体积膨胀，脚本资源检查同步增加新文件校验。
- **内置能力扩展**：补充报表文件、文档解析、数据处理、格式转换等能力服务定义和工具说明，报告编排、图表、表格等工具继续可被智能体按授权调用。
- **安全与回归覆盖**：补充能力工具、能力市场、任务记录分页和相关服务边界测试，保持 CSP 与资源引用检查覆盖新增工作区和脚本。
- **文档与版本同步**：版本升级至 `v0.0.54`，`package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG 已同步。

## [v0.0.53] - 2026-05-24
### 智能体任务历史可视化
- **任务时间轴**：任务详情面板新增水平时间轴 `.agent-timeline`，每一步显示为色块，宽度按 `duration_ms` 比例分配，最小宽度 1.5% 保证窄段也可见；色块按 `step.type` 着色 —— 规划（蓝紫）、工具（绿）、控制（橙）、审批（紫），失败步骤一律红色覆盖；悬停展示 `step_index + 工具名 + 毫秒 + 失败标记`。
- **图例**：时间轴上方展示 4 种 step 类型与失败状态的图例胶囊；色块全部使用同一套主题色，与暗色模式兼容。
- **工具调用统计**：`buildAgentToolStatsMarkup` 按 `tool_name` 聚合 `type='tool'` 步骤，统计调用次数、总耗时、平均耗时、失败次数；按调用次数和总耗时排名，渲染为带"调用次数占比"进度条的列表；存在失败时该行标红 `has-error` 并强调"N 失败"。
- **空数据兜底**：当任务还没有任何步骤，或所有 `duration_ms = 0`（异步刚启动）时仍能按等宽切分展示，不报错也不显示空白。
- **样式与主题**：在 `styles/workspaces/agent.css` 末尾新增约 150 行样式，包含 `agent-timeline` / `agent-tool-stats` 两个模块及 4 套类型色板，兼容 light / dark 主题。
- **不改动后端**：可视化全部基于已有 `getRunDetailForUser` 返回的 `steps` 数据，零新增 API、零迁移。
- **验证**：`npm run check` 全部 103 文件通过，`node tests/security.test.js` 维持 `100/100`。

### 会话打印 / PDF 导出工作区收口
- **主工作区展示**：会话“打印 / 导出 PDF”从新标签页改为主工作区 `print` 视图，通过同源 iframe 加载 `/api/sessions/:id/print?embed=1`，关闭行为与设置、MCP、智能体等工作区保持一致。
- **预览区域修复**：新增 `client/chat/partials/workspaces/print.html`，`print-frame` 复用手册工作区的全宽全高布局，避免浏览器默认 iframe 尺寸导致内容区域被压缩。
- **工具栏布局优化**：打印页嵌入模式隐藏内部标题和关闭按钮，导出时间与“打印 / 导出为 PDF”按钮同一行展示，按钮固定在右侧。
- **资源守卫**：`scripts/check_chat_assets.js` 新增打印工作区结构和样式检查，防止后续缺少 `print-frame`、主工作区注册或撑满样式。

### 工程收尾与贡献者清理
- **长期 warning 清理**：修复 DAG 编辑器浏览器全局引用、侧栏打印入口全局引用，以及无用变量、无用导入和无用参数，`npm run lint` 已零警告通过。
- **文档同步**：`使用手册.md` 将打印 / 导出 PDF 描述更新为主工作区预览，不再描述为打开新标签页。
- **Docker 时区校准**：`Dockerfile` 新增安装 `tzdata` 并设定默认时区为 `Asia/Shanghai`，同时在 `.env` 与 `.env.example` 中补充 `TZ=Asia/Shanghai` 配置，解决容器日志与东八区相差 8 小时的问题。
- **智能体步骤限制提升**：将智能体的最大步骤限制从固定的 `8` 步放宽至 `50` 步，同时将新建任务的默认步骤上限从 `5` 步调优至 `10` 步，解决了部分复杂任务执行步骤受限被截断的问题。
- **验证**：`npm run verify` 通过，包含资源检查、lint 和 `tests/security.test.js` `100/100`。

## [v0.0.52] - 2026-05-24
### 流式 function calling 实时演示面板
- **后端事件推送**：`tryRunAgentStreaming` 在每次累加器更新时通过 `callModelStreamingWithTools` 的 `onDelta` 回调以 `agent.streaming` SSE 事件推送累加快照，含 `runId / step / content / partialToolCalls / finishReason`；步骤结束追加一次 `completed: true` 的最终快照。
- **节流策略**：100ms 时间窗口或累计 ≥120 字符增量才推送一次，避免 SSE 风暴；高频 token 推送下 CPU 与网络消耗可控。
- **前端订阅**：`client/chat/agents.js` 在已有 `EventSource` 通道上新增 `agent.streaming` 事件监听，只为当前打开的任务渲染实时面板，避免后台任务覆盖前台 UI。
- **流式面板渲染**：任务详情顶部插入 `.agent-streaming-panel`，含蓝色脉冲指示器、当前 step 与 finish_reason、累加文本预览、每个 partial tool call 的名称与 arguments 字符数及前 240 字预览；任务终态 `stop` 等场景 5 秒后淡出，避免长期占据视野。
- **暗色模式适配**：所有面板背景、边框和工具卡片均使用 `var(--text-primary)`、`var(--text-muted)` 等主题变量，并在 `.dark-mode` 下切换暗色透明背景。
- **回归测试加固**：新增 1 项单测验证 `agent.streaming` 事件按用户隔离投递并携带 `runId / partialToolCalls / finishReason` 等字段；`npm test` 通过 `100/100`。
- **验证**：`npm run check` 全部 103 文件通过；旧路径（默认 `AGENT_STREAMING_TOOLS=false`）保持零行为变更，新流式分支仅在显式启用后才会推送 `agent.streaming` 事件。

## [v0.0.51] - 2026-05-24
### DAG 编辑器：缩放 / 平移 / 小地图
- **滚轮缩放**：以光标位置为锚点，缩放范围 0.3x–2.5x；缩放前后光标对应同一内容点，不会"跳动"。
- **空白处拖动平移**：在画布空白区按下后变为 `grabbing` 光标，拖动整张图；拖动节点和拖动空白互不干扰。
- **工具栏新增"适配画布"**：一键重置缩放 = 1、平移 = (0, 0)，看到完整内容。
- **右下角小地图**：固定 140x90 浮窗，缩略显示所有节点和当前视口框；点击小地图任意位置把视口中心跳到该处，便于大型 DAG 快速定位。
- **暗色模式适配**：小地图背景在 `dark-mode` 下使用半透明深色底。
- **资源回收**：destroy 时一并移除 wheel 事件、小地图 click 事件，避免泄漏。
- **验证**：`npm run check` 全部 103 文件通过，`node tests/security.test.js` 维持 `99/99`；编辑器与现有 textarea 双向同步保持原行为。

## [v0.0.50] - 2026-05-24
### 模型路由 `auto-escalate` 成本升级策略
- **新增第 6 个路由策略**：`auto-escalate` —— 第一轮按 `auto-cost` 选最便宜的可用模型尝试；任务合成最终答案后，根据置信度判断是否升级到更强模型再合成一次。
- **`server/services/model-router.js`** 新增两个工具函数：
  - `assessConfidence({ output, finishReason, minOutputChars })`：启发式判定输出是否低置信，覆盖空输出、过短输出、异常 finishReason、中英文低置信短语（"不确定"、"无法判断"、"i don't know"、"not sure" 等）。
  - `pickEscalationModel({ user, currentModel, messages })`：从用户可见模型池中挑一个严格更强的候选（价格更高或上下文窗口更大），同时保证能容纳当前输入，找不到时返回 null。
- **`agent-runtime` 接入**：`runAgent` 在最终 `synthesizeFinalAnswer` 完成后，若策略为 `auto-escalate` 且置信度判定不通过，自动调用 `pickEscalationModel` 选升级目标、写入 `chosen_model_id`、在 `agent_steps` 留下 `control` 类型步骤记录升级原因，然后用更强模型重新合成一次最终答案；升级失败保留首轮答案。
- **前端入口**：`partials/workspaces/agent.html` 的"模型路由"下拉新增"成本升级（置信不足时升级）"选项；模板加载/保存与运行时透传与其他策略一致。
- **回归测试加固**：新增 3 项单测覆盖 `auto-escalate` 在 `STRATEGIES`/`listStrategies` 中的注册、`assessConfidence` 多种低置信场景与正常输出场景；`npm test` 通过 `99/99`。
- **验证**：`npm run check` 全部 103 文件通过。

## [v0.0.49] - 2026-05-24
### agent-runtime 接入流式 function calling
- **新增 `tryRunAgentStreaming`**：在 `runAgent` 主循环里增加一条可选的流式分支，按 OpenAI tools 协议把工具调用从"回合制 JSON"升级为流式 `tool_calls` 协议。
  - 系统 prompt 引导模型通过 `tool_calls` 给出结构化参数；累加器解析后逐个执行工具，把结果作为 `role=tool` 消息喂回模型，循环直到模型直接输出最终答复。
  - 每轮规划与每次工具调用都会写入 `agent_steps`，含 `plan` 类型的累加器快照与 `tool` 类型的实际执行记录，审计与回看完全保留。
  - 工具未授权 / 未在白名单时写入 `error` 步骤但继续把错误返回给模型，让模型自行决定下一步，不直接挂任务。
  - 触发 MCP 审批暂停时，与旧分支行为一致：直接返回，等用户审批后会重新入队。
- **环境变量开关**：默认 `AGENT_STREAMING_TOOLS=false`，所有现有任务行为零改动；置为 `true` 才启用流式分支。任何**流式失败（网络、累加器异常、超时）会自动回退**到旧回合制 JSON 协议，并在 `agent_steps` 留下 `control` 步骤记录，确保任务最终能完成。
- **`run_mode = 'dag'` 永远走旧分支**：DAG 已经是图调度，不需要也不应该被流式工具替换。
- **工具列表协议化**：新增 `buildAgentToolSchemas`，把内部 `toolList` 转成 OpenAI tools schema 格式（带 `description` 与 `input_schema`），复用 v0.0.48 的 `streaming-tools.js` 工具函数。
- **.env.example 同步**：新增 `AGENT_STREAMING_TOOLS=false` 与说明注释。
- **验证**：`npm run check` 全部 103 文件通过，`node tests/security.test.js` 维持 `96/96`；旧分支单测与 v0.0.48 新增的 streaming-tools 单测全部回归。

## [v0.0.48] - 2026-05-24
### 流式 function calling 基础设施
- **新增 `server/services/streaming-tools.js`**：纯函数实现的 OpenAI streaming tool_calls 协议累加器。
  - 维护多个 `tool_calls`（按 index 索引）的 name、id、arguments 字符串增量累加；同步累加 assistant `content` 文本与 `usage` 字段。
  - `finalize()` 返回 `{ content, toolCalls, finishReason, usage, hasToolCalls, errors }`，每个 `toolCall` 同时包含 `argumentsRaw` 与已 `JSON.parse` 的 `arguments`；解析失败保留原始字符串和 `parseError`，调用方可降级处理。
  - 提供安全上限 `TOOL_CALL_LIMIT = 16`，超出后丢弃并记录错误，防止异常增量耗尽内存。
  - 兼容 legacy `function_call` delta 协议（旧版 OpenAI 接口仍可工作）。
  - 辅助函数：`buildOpenAiToolsPayload`（工具列表 → OpenAI tools 数组）、`buildAssistantToolMessage`（累加结果 → assistant message + tool_calls）、`buildToolResultMessage`（工具输出 → role=tool 消息）。
- **agent-model 新增 `callModelStreamingWithTools(modelCfg, messages, tools, options)`**：
  - 复用 `withAgentModelConcurrency` 接入全局/端点并发保护和成功失败统计。
  - 通过 `axios responseType: 'stream'` 拉取 SSE，由 `server/streaming.js` 的 `createSseEventParser` 解析逻辑帧后投喂累加器。
  - `options.onDelta(snapshot)` 回调暴露增量状态，便于前端实时渲染（待 agent-runtime 接入流程后启用）。
  - **不替换 `callModelText` / `callModelJson`**：作为可选 API 并存，agent-runtime 当前默认仍走回合制 JSON 协议，避免回归。后续可通过环境变量 `AGENT_STREAMING_TOOLS=true` 渐进切换。
- **回归测试加固**：新增 7 项单测覆盖单工具增量累加、多工具与文本混合、`arguments` JSON 解析失败降级、`TOOL_CALL_LIMIT` 边界、`function_call` 旧协议兼容、`buildOpenAiToolsPayload` 过滤无名工具、`buildAssistantToolMessage` / `buildToolResultMessage` 输出结构。
- **验证**：`npm run check` 全部 103 文件通过，`node tests/security.test.js` 通过 `96/96`（新增 7 项）。

## [v0.0.47] - 2026-05-24
### 智能体 DAG 可视化编辑器
- **新增 `client/chat/agents-dag-editor.js`**：纯原生 SVG + 拖拽实现的 DAG 可视化编辑器（约 400 行，零依赖）。
  - 节点：可拖动、双击聚焦、`is-selected` 高亮；显示标题与所选工具。
  - 边：贝塞尔曲线 + 箭头标记，依赖关系一目了然。
  - 出端口（节点右侧小圆点）拖到目标节点的入端口即可创建依赖；ghost 路径实时跟随光标。
  - 工具栏："+ 添加节点"、"自动布局"（按拓扑层次重排）、"从 JSON 同步"。
  - 详情面板：选中节点后可编辑 ID（自动迁移依赖引用）、标题、工具下拉（从 `agentToolsCache` 填充）、条件、输入 JSON、依赖勾选；输入 JSON 无效时高亮但不破坏现有数据。
- **与 `#agent-dag-spec` textarea 双向同步**：编辑器变化 → textarea 自动 stringify；用户手动改 textarea → 编辑器解析并重绘；JSON 视图改为 `<details>` 折叠形态，普通用户不会被原始 JSON 吓到，专家用户依然可以打开手改。
- **agent.html 集成**：在原 textarea 位置外包一层 `pivot-dag-editor-wrap`，注入画布、工具栏、详情面板三块容器；`agents.js` 的 `loadAgentTools()` 完成后自动 `mountAgentDagEditor()` 将工具列表喂给编辑器。
- **样式**：在 `styles/workspaces/agent.css` 末尾新增约 180 行编辑器样式，包含网格画布背景、节点/边/端口/选中态、详情面板栅格表单、依赖标签胶囊；保持原生主题变量，兼容暗色模式。
- **CSP 兼容**：所有事件均通过 `addEventListener` 绑定，无内联 onclick；幂等挂载，`mountAgentDagEditor` 重复调用会先 destroy 旧实例。
- **验证**：`npm run check` 全部 102 文件通过，`node tests/security.test.js` 维持 `89/89`。

## [v0.0.46] - 2026-05-24
### 智能体模型路由策略
- **新增 `server/services/model-router.js`**：提供 5 种模型选择策略，让智能体任务根据输入特征自动从用户可见的模型池中选模。
  - `fixed`：保持旧行为，使用任务配置的固定模型。
  - `auto-vision`：输入含图片时优先选 `supports_vision = 1` 的模型，否则按上下文匹配兜底。
  - `auto-context`：选能容纳输入但 `max_input_tokens` 最小的模型，避免大窗口模型浪费配额。
  - `auto-cost`：选 `input_price_per_million + output_price_per_million` 最低的可用模型。
  - `auto-load`：按"端点活跃数 / 端点并发上限"比值最小的候选，缓解长任务排队。
- **数据库扩展**：`agent_templates.model_router`、`agent_runs.model_router`、`agent_runs.chosen_model_id` 字段已加入 `schema.js` 和 `migrate.js`，旧库自动升级，默认值 `fixed` 保持兼容。
- **运行时接入**：`server/services/agent-runtime.js` 的 `runAgent` 在启动时按 `run.model_router` 调用 `chooseModel`，命中替换时写入 `chosen_model_id` 并插入一条 `control` 类型步骤记录路由理由；路由失败或返回原模型时回退到原配置，不影响现有任务。
- **接口与前端**：`/api/agents/model-routers` 公开 5 项策略元数据；智能体工作台"MCP 审批"旁新增"模型路由"下拉，模板加载/保存自动透传 `model_router`；`createAgentRun` 与 `agent_templates` CRUD 全链路兼容。
- **回归测试**：新增 5 项单测覆盖 `normalizeStrategy` 合法值/兜底、`listStrategies` 完整性、`estimateMessageTokens` 中英文混合估算、`hasUsableInputWindow` 边界、`modelTotalPrice` 缺字段安全；`npm test` 通过 `89/89`。
- **验证**：`npm run check` 全部 101 文件通过。

## [v0.0.45] - 2026-05-22
### 功能扩展：RAG 调试可视化与会话 PDF 导出
- **RAG 召回测试可视化增强**：`/api/rag/debug-query` 返回结果新增 `elapsedMs` 检索耗时；前端调试弹窗在原有命中列表基础上新增：
  - 关键词高亮（自动转义后包裹 `<mark>`），便于快速识别召回原因；
  - 每个候选的得分进度条，按相对最高分占比展示，方便对比相似度梯度；
  - 按源文件聚合的命中汇总条（命中数 / 候选总数 / 峰值得分 / 平均得分），帮助快速判断哪个文件主导了召回。
- **会话打印 / PDF 导出**：新增 `/api/sessions/:id/print` 打印友好 HTML 视图，使用 CSP nonce 注入脚本，遵循全局安全策略；浏览器内按 Ctrl/Cmd+P 即可"导出为 PDF"，无需服务端引入 puppeteer 等重型依赖。会话右键菜单的"导出为 Markdown"旁新增"打印 / 导出 PDF"入口。
- **文档与版本同步**：版本升级至 `v0.0.45`，`package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG 已同步。
- **验证状态**：`npm run check` 全部 100 文件通过，`node tests/security.test.js` 通过 `84/84`。

## [v0.0.44] - 2026-05-22
### 前端体验优化
- **`window.Pivot` 全局工具命名空间**：新增 `client/chat/pivot-core.js`，集中提供 `throttle` / `debounce` / `rafThrottle` / `LruCache` / `formatBytes` / `formatNumber` / `clearChildren` / `chooseStreamInterval` 等工具，向前兼容现有 `window.*` 全局函数，新代码可统一从 `window.Pivot` 调用，逐步减少分散在各模块的重复实现。
- **流式 Markdown 自适应节流**：聊天流式渲染从固定 80ms 间隔改为按累计内容长度阶梯式自适应（80 / 140 / 220 / 320ms），长回答中 `marked.parse` 的重排开销显著下降；短消息保持 80ms 视觉响应，长消息自动放缓刷新频率，避免高密度 token 推送时的卡顿。
- **加载顺序调整**：`partials/scripts.html` 在 `safe-html.js` 后新增加载 `pivot-core.js`，确保后续业务脚本可直接读取 `window.Pivot`。
- **验证状态**：`npm run check` 全部通过，`node tests/security.test.js` 维持 `84/84`。

## [v0.0.43] - 2026-05-22
### 基础设施与安全收口
- **LRU / TtlCache 工具**：新增 `server/cache.js`，提供带容量上限和 TTL 的 `LruCache` 与懒清理的 `TtlCache`；服务端 `dirSizeCache` 改用 LRU，避免长时间运行的目录尺寸缓存无限增长，符合内网长期常驻部署的内存控制要求。
- **后台启动错误日志**：GPU 监控、模型端点监控、知识库索引恢复、智能体任务恢复、智能体计划调度等启动失败的 `.catch(() => {})` 改为带 `logger.warn` 的告警，方便内网部署排查初始化失败原因，不再静默吞错。
- **MCP 调用日志脱敏**：`server/security.js` 新增 `redactSecrets` / `maskSecretString`，自动脱敏 `api_key`、`authorization`、`Bearer`、`sk-*`、JWT 等敏感字段；`server/services/mcp-client.js` 的 `previewValue` 在写入审计表前先过脱敏，避免敏感参数被普通管理员或合规导出包看到。
- **异步任务超时与并发治理**：新增 `server/services/concurrency.js` 的 `withTimeout`、`KeyedConcurrencyGuard`、`TimeoutError`；`compressMemory` 后台压缩接入超时（可通过 `MEMORY_COMPRESSION_TIMEOUT_MS` 调整）和会话级去重（同会话不重复触发），全局并发上限可通过 `MEMORY_COMPRESSION_MAX_CONCURRENT` 控制，避免长会话同时压垮上游模型。
- **MCP 审批服务端二次校验**：移除 `agent-runtime` 中 `metadata.approval === 'all_mcp_approved'` 的快捷放行死分支，只承认通过 `approveAgentTool` 写入的 `approvedTools` 白名单，杜绝后续路由或迁移意外绕过审批策略。
- **智能体运行时拆分**：从 1978 行的 `server/services/agent-runtime.js` 拆出独立模块 `server/services/agent-validators.js`，包含所有常量、`parseJsonObject`、`normalizeMaxSteps` / `normalizeApprovalPolicy` / `normalizeDagSpec` / `normalizeToolAllowlist` 等纯函数；`agent-runtime.js` 行数从 1978 降至 1834，对外导出表完全保持，路由层引用无需修改，便于后续继续拆 DAG / 调度器 / 产物子模块。
- **回归测试加固**：`tests/security.test.js` 新增 9 项单测覆盖 `LruCache` 容量与 TTL 行为、`TtlCache` 懒清理、`withTimeout` 超时与正常返回、`KeyedConcurrencyGuard` 重复键跳过与并发上限、`redactSecrets` 嵌套对象与原对象不变性、`maskSecretString` 行内脱敏；当前 `npm test` 已通过 `84/84`。

## [v0.0.42] - 2026-05-22
### 上下文预算与知识库问答稳定性
- **模型上下文预算保护**：新增统一的上下文预算服务，在请求自部署模型前按模型输入上限、输出预留和安全余量估算总上下文，避免系统提示词、历史会话、知识库 RAG 片段、MCP 结果和当前输入叠加超过 16K 窗口后才由上游返回 400。
- **知识库 RAG 自动裁剪**：普通对话启用知识库时，会优先保留当前问题和必要系统提示，并自动减少较早历史、过长知识库片段或 MCP 工具结果；仍然超限时返回明确的 `CONTEXT_LENGTH_EXCEEDED`，提示用户缩短输入、新建会话或拆分问题。
- **单条超长输入前置拦截**：当前用户输入本身超过模型可处理范围时，服务端会在写入会话历史前直接拒绝请求，避免失败内容残留到历史上下文并影响后续重试。
- **OpenAI 兼容接口同步保护**：`/v1/chat/completions` 转发前接入同一套上下文预算检查，超限时返回 `context_length_exceeded`，第三方调用方可据此区分“内容过长”和“API 配置/余额/服务异常”。
- **前端错误提示明确化**：聊天流式事件新增上下文预算状态，自动裁剪时提示“已自动减少较早历史或知识库片段后继续生成”，超限失败时展示内容过长原因，不再泛化为上游流式错误或余额配置问题。
### 验证
- **回归测试补齐**：新增上下文预算裁剪和单条超长输入拦截测试，覆盖历史、知识库片段和当前输入的关键边界；当前 `npm test` 已通过，安全测试为 `75/75`。

## [v0.0.41] - 2026-05-21
### 普通用户使用手册
- **独立用户手册**：新增 `使用手册.md`，按登录、界面、对话、附件、知识库、MCP、智能体、模型配置、第三方 API Key、状态排查、常见问题、安全建议和首次使用清单组织，重点指导普通用户完成日常操作。
- **管理员内容收口**：手册定位调整为普通用户指南，移除管理员上线清单和管理员常用操作章节，仅保留必要的“联系管理员”提示。

### 前端入口与部署可用性
- **手册入口**：左下角用户区新增“手册”按钮，用户可在应用内工作区直接查看使用说明，不再跳出到新标签页。
- **服务端手册页面**：新增 `/manual` 独立页面和 `/manual?embed=1` 嵌入页面，由服务端读取根目录 `使用手册.md` 并渲染为 HTML，保证本地运行和 Docker 部署后的访问路径一致。
- **Docker 打包保护**：`Dockerfile` 通过 `COPY . .` 带入根目录手册；`scripts/check_chat_assets.js` 新增检查，确认手册文件存在、前端入口存在、`.dockerignore` 未排除手册，避免镜像部署后页面打不开。

### 侧栏用户区体验
- **长名称显示优化**：左下角用户名称保留单行省略与完整名称悬浮提示，减少长显示名被按钮挤压后的误读。
- **管理员标识优化**：内置 `admin` 系统管理员显示绿色小盾牌图标，普通管理员显示灰色用户标识图标，二者均为无边框紧凑样式。
- **底部布局紧凑化**：用户名、手册、设置和退出保持单行布局，收紧按钮高度与版权间距，避免占用过多纵向空间。

### 文档与验证
- **版本升级**：应用版本升级至 `v0.0.41`，`package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG 已同步。
- **验证状态**：`npm run check` 已通过；临时启动当前代码验证 `/manual` 返回 200，主页面模板包含手册入口。

## [v0.0.40] - 2026-05-21
### 监控与模型状态
- **GPU 利用率显示修正**：GPU 与并发保护区域将 `nvidia-smi` 返回的核心利用率按比例正确换算为百分比，显存占用明确标记为“显存”，避免出现显存很高但利用率固定显示 0% 的误读。
- **模型端点状态收口**：系统监控页的模型端点状态只同步并展示当前仍启用的模型配置；已删除、停用或历史残留的端点运行态会被清理，不再继续出现在监控区域。

### MCP 工作台体验
- **工具缓存紧凑化**：已缓存可调用工具改为自适应卡片网格，减少单个工具占用整行造成的空白，工具来源、原始工具名和测试标识保持可见。
- **调用记录归属优化**：MCP 治理区保留全局摘要，具体调用记录移动到各自 MCP 服务卡片内展示；无调用记录时使用轻量空态提示，避免长条占位浪费空间。
- **类型下拉排序优化**：新增与编辑 MCP Server 的类型下拉统一排序为外部服务、数据库服务和内置业务模板，常用配置入口更靠前。

### 智能体工作台与运行策略
- **默认模型一致性**：智能体工作台复用主页面右上角模型选择器的可见模型、排序、当前选择和默认模型逻辑，并修复个人/全局模型文案乱码。
- **审批策略修正**：`safe_mcp_auto` 会对高风险 MCP 在执行前进行人工审批，`approve_all_mcp` 会对全部 MCP 进行审批；待审批任务会按活跃状态参与刷新和操作限制。
- **重跑与续跑约束**：运行中的任务不能直接重新运行；断点续跑会继承上一轮 DAG 配置与失败上下文，避免丢失编排信息。
- **模板与计划管理补齐**：个人模板和计划增加删除入口，减少配置残留和无效堆积。
- **导出内容补齐**：智能体任务导出补充 DAG 节点信息，便于还原 DAG 执行轨迹。
- **工具白名单边界修复**：用户取消勾选全部工具时会按“无可用工具”处理，不再误回退为全部工具可用。

### 模型配置弹窗
- **新增/编辑模型弹窗紧凑化**：模型配置弹窗压缩标题字号、说明字号、输入框高度、行距和按钮高度，更适合密集配置场景。
- **字段布局收敛**：保留两列布局，同时收小字段间距和能力开关卡片内边距，提升同屏可见字段数量。

### 文档与验证
- **版本升级**：应用版本升级至 `v0.0.40`，`package.json`、`package-lock.json`、README 与 CHANGELOG 已同步。
- **历史记录恢复**：CHANGELOG 保留并恢复 `v0.0.38` 版本记录，避免发布历史断档。
- **验证状态**：`npm run check` 与 `node tests/security.test.js` 已通过；`npm run lint` 仅保留既有的 `server/services/mcp-client.js` 未使用参数警告。

## [v0.0.39] - 2026-05-19
### 监控、MCP 工作台与智能体模型选择补丁
- **GPU 利用率显示修正**：GPU 与并发保护区域将 `nvidia-smi` 返回的核心利用率按比例正确换算为百分比，显存占用明确标记为“显存”，避免出现显存很高但利用率固定显示 0% 的误读。
- **模型端点状态收口**：系统监控页的模型端点状态只同步并展示当前仍启用的模型配置；已删除、停用或历史残留的端点运行态会被清理，不再继续出现在监控区域。
- **MCP 工具缓存布局优化**：已缓存可调用工具改为紧凑自适应卡片网格，减少单个工具占用整行造成的空白；工具来源、原始工具名和测试标识保持可见。
- **MCP 调用记录归属优化**：MCP 治理区保留全局摘要，具体调用记录移动到各自 MCP 服务卡片内展示；无调用记录时使用轻量空态提示，避免长条占位浪费空间。
- **MCP 类型下拉排序优化**：新增与编辑 MCP Server 的类型下拉统一排序为外部服务、数据库服务和内置业务模板，常用配置入口更靠前。
- **智能体默认模型一致性**：智能体工作台复用主页面右上角模型选择器的可见模型、排序、当前选择和默认模型逻辑，避免两个入口默认显示不一致。

### 权限、输入工具与用户可用性
- **三类权限边界收口**：重新梳理超级管理员、管理员、普通用户的模型、MCP、知识库、智能体、系统设置与审计能力边界；内置用户名 `admin` 作为超级管理员保留全局/共享/审计能力，管理员与普通用户默认维护个人资源。
- **聊天输入框工具开关修复**：知识库与 MCP 按钮改为点击变色的图标按钮，不再使用复选框视觉；修复管理员和普通用户无法选中知识库/MCP 的事件绑定问题，并保持本地开关状态同步。
- **MCP 使用确认优化**：普通对话启用 MCP 时保留一次性风险确认，确认结果在当前浏览器会话内复用，降低重复弹窗干扰，同时避免用户无感触发外部工具。
- **全局模型测试放开**：管理员和普通用户可对自己可见、可使用的超级管理员全局模型执行连接测试并查看状态；编辑、删除和密钥查看仍只允许模型所有者或超级管理员。
- **模型配置状态检测补齐**：模型配置页面中，非内置 `admin` 的管理员可对列表中可见的全局模型执行运维测试；普通用户按部门可见范围测试全局模型，自动状态点和手动测试按钮均复用服务端存储密钥但不暴露密钥明文。

### MCP 服务拆分与局域网工作流
- **内置 MCP 服务扩展**：新增报表与数据文件、可视化图表、报告编排、局域网消息通知四类内置 MCP 服务；服务可进入工具缓存，并被普通对话与智能体按能力组合调用。
- **独立 MCP Server 组合式流程**：报表/数据文件只负责读取和查询文件，可视化只负责生成图表或展示表格，报告编排只负责按固定格式组装内容，消息通知只负责推送到内网聊天工具，避免把完整业务流程塞进单个 MCP server。
- **图表与报告渲染链路**：普通对话可接收 `pivot-chart`、`pivot-table`、`pivot-report` 结构化结果，前端可将 MCP 生成的图表配置渲染为可视化展示，并把表格、报告 Markdown 注入最终回答。
- **数据库 MCP 生产诊断增强**：局域网数据库连接默认允许个人配置，连接拒绝、认证失败、网络不可达等错误返回更明确的状态码、主机端口与排查建议；敏感信息继续脱敏。
- **数据库连接测试提速**：数据库 MCP 点击“测试连接”时使用真实数据库驱动执行快速验证，并由 `MCP_DATABASE_TEST_TIMEOUT_MS` 控制超时，默认 10 秒返回可读诊断；正式工具查询仍使用 `MCP_DATABASE_CONNECT_TIMEOUT_MS`，避免生产环境防火墙丢包导致页面长时间等待，同时避免 TCP 预探测在容器网络中误判。
- **数据库测试链路一致性修复**：修复测试连接未把 `options.ssl`、`schema` 展开到驱动配置的问题，避免用户勾选 SSL/TLS 时测试仍按非 SSL 连接；外层测试超时增加缓冲时间，减少局域网 DNS 反向解析、握手慢或防火墙探测导致的假超时，并在错误提示中补充 MySQL `skip-name-resolve` 排查建议。
- **环境变量配置对齐**：同步 `.env` 与 `.env.example` 的配置项，补齐 `PIVOT_ADVERTISE_HOSTS`、`MCP_DATABASE_CONNECT_TIMEOUT_MS` 和 `MCP_DATABASE_TEST_TIMEOUT_MS`，保留现有生产值和密钥不被覆盖。
- **MCP 治理布局优化**：MCP 工作台治理区域改为紧凑横向状态条，展示 7 日调用、错误、平均耗时、服务状态和最近调用，避免少量内容撑高页面。

### 知识库、智能体与报告分析
- **知识库完整性复核**：确认知识库上传、索引、启停、删除、批量重建、失败重试、质量报告、调试检索、反馈与 RAG 配置链路完整可用。
- **智能体完整性复核**：确认智能体模型选择、工具清单、预检、运行、取消、重跑、断点续跑、模板、计划、通知、产物沉淀、导出与知识库工具调用链路完整可用。
- **智能体任务标题修复**：修复任务名称在异常编码或历史坏数据下显示为连续问号的问题；新建任务会自动使用目标摘要兜底，历史任务列表、悬停提示、删除审计、实时运行事件、通知、导出与产物标题也会回退到可读目标文本，并恢复任务创建、计划与运行提示的中文文案。
- **智能体运行提示链路修复**：进一步排查任务从排队进入运行中的实时事件与通知链路，`agent.run` SSE 事件不再发送原始坏标题，运行开始、状态刷新、通知 Toast 和通知列表都会过滤连续问号并回退到任务目标摘要，避免右侧顶部提示出现不可读的问号标题。
- **智能体模型排队治理**：智能体规划、总结等模型调用接入与普通聊天一致的全局 AI 并发队列和模型端点队列；当其它用户正在使用同一模型时，智能体任务会等待可用执行槽，而不是绕过队列直接请求模型并显示 503。
- **固定格式报告工作流**：支持“数据库/文件取数 → 可视化图表 → 报告编排 → 内网通知”的拆分式组合，为数据表分析、固定报告、图表和表格展示提供更清晰的扩展路径。

### 界面与监控体验
- **流式回答会话隔离修复**：修复模型正在回答时切换左侧会话，可能导致原会话前端内容被新会话渲染覆盖或统计上报到错误会话的问题；发送请求时会锁定本次生成所属会话 ID，切走后回答仍保存到原会话，完成后提示用户可切回查看。
- **侧栏宽度稳定**：修复左侧会话列表因标题字数或时间标签变化导致宽度跳动的问题，列表项和时间列保持固定收缩边界。
- **系统监控卡片完整显示**：优化 GPU 与并发保护、今日模型 Token、慢查询与异常告警等区域布局，修复显卡信息、慢查询信息、输入框和按钮对齐不完整的问题。
- **本机 GPU 语义修正**：GPU 与并发保护区域只显示 Pivot 部署服务器本机 GPU 与全局并发保护，不再混入远端模型提示；同服务器模型可通过请求主机、`PUBLIC_URL`、`CORS_ORIGIN`、`PIVOT_ADVERTISE_HOSTS` 或 `PIVOT_LOCAL_MODEL_HOSTS` 识别为本地端点。
- **慢查询告警展示优化**：慢查询与异常告警列表改为卡片式记录，左侧展示告警标题、来源、类型和级别标签，右侧独立展示耗时徽标，并将长毫秒值自动格式化为秒或分钟，避免单条告警被挤压成多行。
- **MCP 表单体验补齐**：报表、可视化、报告、消息通知和数据库 MCP 模板在工作台中可直接选择；数据库测试连接、错误提示和工具缓存刷新更贴近生产排查流程。

### 文档与验证
- **文档同步**：README 与 CHANGELOG 已同步记录本轮权限、MCP、知识库、智能体、模型配置状态检测、监控和界面优化。
- **版本升级**：应用版本升级至 `v0.0.39`，`package.json`、`package-lock.json`、README 与 CHANGELOG 已同步。
- **验证状态**：`npm test` 已通过，包含文本完整性、语法检查、聊天资源检查和安全测试 `73/73`。

## [v0.0.38] - 2026-05-19
### 权限、输入工具与用户可用性
- **三类权限边界收口**：重新梳理超级管理员、管理员、普通用户的模型、MCP、知识库、智能体、系统设置与审计能力边界；内置用户名 `admin` 作为超级管理员保留全局/共享/审计能力，管理员与普通用户默认维护个人资源。
- **聊天输入框工具开关修复**：知识库与 MCP 按钮改为点击变色的图标按钮，不再使用复选框视觉；修复管理员和普通用户无法选中知识库/MCP 的事件绑定问题，并保持本地开关状态同步。
- **MCP 使用确认优化**：普通对话启用 MCP 时保留一次性风险确认，确认结果在当前浏览器会话内复用，降低重复弹窗干扰，同时避免用户无感触发外部工具。

### MCP 服务拆分与局域网工作流
- **内置 MCP 服务扩展**：新增报表与数据文件、可视化图表、报告编排、局域网消息通知四类内置 MCP 服务；服务可进入工具缓存，并被普通对话与智能体按能力组合调用。
- **独立 MCP Server 组合式流程**：报表/数据文件只负责读取和查询文件，可视化只负责生成图表或展示表格，报告编排只负责按固定格式组装内容，消息通知只负责推送到内网聊天工具，避免把完整业务流程塞进单个 MCP server。
- **图表与报告渲染链路**：普通对话可接收 `pivot-chart`、`pivot-table`、`pivot-report` 结构化结果，前端可将 MCP 生成的图表配置渲染为可视化展示，并把表格、报告 Markdown 注入最终回答。
- **数据库 MCP 生产诊断增强**：局域网数据库连接默认允许个人配置，连接拒绝、认证失败、网络不可达等错误返回更明确的状态码、主机端口与排查建议；敏感信息继续脱敏。
- **MCP 治理布局优化**：MCP 工作台治理区域改为紧凑横向状态条，展示 7 日调用、错误、平均耗时、服务状态和最近调用，避免少量内容撑高页面。

### 知识库、智能体与报告分析
- **知识库完整性复核**：确认知识库上传、索引、启停、删除、批量重建、失败重试、质量报告、调试检索、反馈与 RAG 配置链路完整可用。
- **智能体完整性复核**：确认智能体模型选择、工具清单、预检、运行、取消、重跑、断点续跑、模板、计划、通知、产物沉淀、导出与知识库工具调用链路完整可用。
- **固定格式报告工作流**：支持“数据库/文件取数 → 可视化图表 → 报告编排 → 内网通知”的拆分式组合，为数据表分析、固定报告、图表和表格展示提供更清晰的扩展路径。

### 界面与监控体验
- **侧栏宽度稳定**：修复左侧会话列表因标题字数或时间标签变化导致宽度跳动的问题，列表项和时间列保持固定收缩边界。
- **系统监控卡片完整显示**：优化 GPU 与并发保护、今日模型 Token、慢查询与异常告警等区域布局，修复显卡信息、慢查询信息、输入框和按钮对齐不完整的问题。
- **MCP 表单体验补齐**：报表、可视化、报告、消息通知和数据库 MCP 模板在工作台中可直接选择；数据库测试连接、错误提示和工具缓存刷新更贴近生产排查流程。

### 文档与验证
- **文档同步**：README 与 CHANGELOG 已同步记录本轮权限、MCP、知识库、智能体、监控和界面优化。
- **版本升级**：应用版本升级至 `v0.0.38`，`package.json`、`package-lock.json`、README 与 CHANGELOG 已同步。
- **验证状态**：`npm test` 已通过，包含文本完整性、语法检查、聊天资源检查和安全测试 `70/70`。

## [v0.0.37] - 2026-05-17
### 工作区、缓存与部署稳定性
- **主工作区全屏稳定**：搜索、系统设置、MCP、知识库和智能体统一在右侧主工作区展示，不再覆盖左侧面板；设置页、MCP、知识库和智能体关闭后可稳定回到对话区。
- **PWA 更新边界说明**：继续保留 vendor-only 缓存策略，业务页面、`/chat/*` 脚本样式、API、版本清单和 Service Worker 交回服务端与浏览器刷新；生产部署升级后建议重建容器、刷新页面或清理站点缓存。
- **Docker 构建提示补充**：明确 `debconf: delaying package configuration, since apt-utils is not installed` 属于精简镜像中的非阻断提示；`prebuild-install` deprecated 属于上游原生依赖传递警告，不影响当前构建。

### MCP、知识库与智能体治理升级
- **MCP 治理面板**：新增 MCP 调用日志表与调用记录，工作台展示 7 日调用量、错误数、平均耗时、活跃/异常/未检查服务和近期调用明细。
- **MCP 调用来源标记**：普通对话、智能体和手动工具调用会写入来源，便于后续排查数据库 MCP、外部 MCP 和智能体任务的链路。
- **知识库质量报告**：知识库工作台新增质量概览，展示文档就绪、索引失败、反馈、召回命中和存储等指标，辅助判断 RAG 配置是否生效。
- **智能体任务预检**：创建智能体任务前会检查目标长度、模型可用性、工具范围、MCP 风险、知识库状态、DAG 节点和 Token 预算，阻断明显不可运行的任务。

### 局域网数据库 MCP 连接
- **个人局域网数据库连接**：数据库 MCP 默认允许普通用户和非内置 `admin` 管理员连接自己的局域网数据库，适配生产环境中数据库不在 Pivot 本机的部署方式。
- **可恢复旧限制**：如需恢复旧策略，可在服务端 `.env`、Docker Compose `environment` 或 `docker run -e` 中设置 `MCP_RESTRICT_PRIVATE_DATABASE_HOSTS_TO_ADMIN=true`，限制私有网段数据库主机仅管理员可配置。
- **连接测试错误可读化**：数据库连接测试失败时按验证错误、权限错误和连接失败返回 400/403/502 JSON，不再统一表现为 500；日志会避免输出密码等敏感字段。
- **连接超时治理**：PostgreSQL、MySQL、SQL Server 和 MongoDB 连接测试统一使用 `MCP_DATABASE_CONNECT_TIMEOUT_MS`，默认 10 秒，避免生产环境长时间卡住。

### 权限模型收口
- **超级管理员边界统一**：内置用户名 `admin` 作为超级管理员；非内置 `admin` 管理员默认使用自己的模型、Embedding/RAG、MCP、知识库和智能体配置。
- **MCP 与知识库个人化**：普通用户和非内置 `admin` 管理员可打开并维护自己的 MCP 与知识库，只有内置 `admin` 可管理全局/共享能力。
- **系统设置个人化**：普通用户进入设置页显示“个人设置”，默认打开个人模型配置；数据概览、系统监控、用户、审计和报表等管理员页继续受权限保护。
- **搜索个人范围**：系统内搜索继续限定在当前用户自己的会话标题、消息内容、标签和归档记录中，不暴露其他用户数据。
- **智能体共享与工具隔离**：共享智能体模板、管理员专用内置工具和全局智能体审计能力收紧到内置 `admin`；普通用户和非内置 `admin` 管理员使用个人模板、个人任务和个人指标。

### 系统监控布局
- **监控页无纵向滚动优化**：系统监控页改为明确的两行三列区域布局，压缩卡片行高和间距，避免主工作区出现不必要的垂直滚动条。
- **慢查询与异常告警完整展示**：为 GPU、系统资源、健康维护、今日模型 Token、知识库与存储、慢查询与异常告警卡片增加语义布局区域，修复“慢查询与异常告警”被挤到左下角且显示不完整的问题。

### 文档与验证
- **版本升级**：应用版本升级至 `v0.0.37`，README、CHANGELOG、`package.json` 和 `package-lock.json` 已同步。
- **验证状态**：`npm test` 已通过，包含文本完整性、语法检查、聊天资源检查和安全测试 `64/64`。

## [v0.0.36] - 2026-05-16
### 视觉与工作台细节优化
- **智能体模板库展示优化**：优化了智能体模板库中时间相关数据的可读性，解决了长日期在特定布局下被省略号截断的问题，确保时间戳和元信息完整展示。
- **系统设置界面深度精简**：
    - 移除了设置面板顶部的 Pivot Logo 和文字标题，释放了更多垂直空间给配置内容。
    - 取消了侧边栏顶部的分类标签，使导航结构更加扁平化，提升了视觉清爽度。
    - 进一步优化了各设置项的布局间距，确保在不同分辨率下的审美一致性。

### 文档与版本同步
- **版本升级**：版本升级至 `v0.0.36`，README 与 package 元数据已同步。
- **验证状态**：`npm run verify` 已通过，确认 UI 调整未影响现有功能。

## [v0.0.35] - 2026-05-16
### 智能体平台能力扩展
- **对话分支 / Fork**：消息操作栏新增“从这里分叉”入口，后端新增 `POST /api/sessions/:id/fork`，会复制源会话从开头到指定消息的上下文，写入父会话、根会话、分叉消息与备注字段，便于保留上文后探索不同方案。
- **智能体结果版本管理**：结果沉淀新增版本表与当前版本指针，支持备注、创建新版本、对比相邻版本和回滚到历史版本；智能体工作台的沉淀结果可直接打开版本弹窗进行维护。
- **慢查询与异常告警**：新增 `observability_events` 记录慢 SQL、模型端点慢响应与 RAG 慢检索，并提供管理员监控面板、事件状态接口和 webhook 告警配置。
- **智能体 DAG 编排**：智能体运行模式新增 `dag`，支持以 JSON 节点图声明依赖、条件跳过和并行工具调用，执行过程写入 `agent_dag_nodes` 并在任务详情中展示节点状态与输出。
- **插件化能力市场**：新增 `capability_packages`，把内置工具、MCP 服务和数据库连接抽象为可启停能力包；智能体工具列表、普通对话 MCP 工具和 MCP 调用入口会按能力包状态过滤。
### 文档与验证
- **更新日志同步**：本次功能开发已写入 CHANGELOG，覆盖分叉会话、结果版本、观测告警、DAG 编排和能力市场。
- **验证说明**：按用户要求不进行浏览器测试，仅执行命令行检查与自动化测试。

## [v0.0.34] - 2026-05-16
### 智能体任务记录体验修复
- **任务记录悬停信息补全**：智能体任务记录列表的悬停提示从仅显示标题扩展为标题、状态、创建/开始/完成时间、模型、模式、工具数、错误数、步骤数和目标等完整信息；同时任务卡片在鼠标悬停或键盘聚焦时展开标题与元信息，避免模型、模式、工具调用和步骤统计被单行省略。
### 侧栏与会话入口轻量化
- **侧栏工具入口重排**：将“搜索”从常驻输入框改为与“知识库 / MCP / 智能体”一致的侧栏按钮，并放入同一工具组，四个入口间距保持一致。
- **会话列表空间释放**：移除会话列表上方常驻标签汇总、搜索框、当前/归档/批量工具条，避免标签过多或工具条过宽压缩会话列表。
- **工具区视觉收口**：移除智能体入口下方多余分割线，保留轻量留白，让侧栏工具区和会话列表衔接更自然。
- **管理员身份区恢复同排布局**：底部用户名称、管理员标识、设置和退出按钮恢复同一行显示；收紧“系统管理员”和“管理员”胶囊间距，同时保留更清晰的管理员徽标样式。

### 会话搜索弹窗升级
- **独立搜索弹窗**：点击侧栏“搜索”后打开独立会话搜索弹窗，支持按标题、消息内容和 `#标签` 定位历史会话，侧栏不再承载搜索输入框。
- **当前/归档视图等高**：搜索弹窗固定整体高度，当前会话和归档会话结果区域保持一致尺寸，不再因归档数据少而缩短或跳动。
- **批量操作迁移到弹窗**：批量按钮和批量标签操作栏移动到搜索弹窗内；进入批量模式后仅弹窗结果列表显示复选框，底层侧栏会话列表不再同步进入批量状态。
- **搜索结果行与主列表一致**：搜索结果中的会话标签显示在标题前方，复用主会话列表标签样式；标题和消息条数保持同一行显示，并加大标题与消息数间距。
- **三点菜单补齐**：搜索结果行悬停时显示三点操作按钮，交互与主会话列表一致，支持重命名、编辑标签、归档/恢复、导出和删除等原有会话操作。
- **交互细节收敛**：搜索结果行悬停时隐藏时间并显示操作按钮；批量模式下隐藏三点菜单，避免选择和单条操作相互干扰。

### 工作区与设置页面体验延续
- **全屏工作区一致性**：知识库、MCP、智能体和系统设置均以右侧主区域工作区方式展示，关闭按钮尺寸和红色描边样式保持一致。
- **系统设置视觉统一**：持续优化系统设置各页面标题说明、数据表格列宽、字体、分割线间距、筛选区布局、账户安全面板和 API 接入管理信息对齐。
- **附件预览体验优化**：附件库预览窗口按钮样式统一，新标签打开入口保留在预览窗口中，文件名点击不再重复触发打开行为。

### 文档与版本同步
- **文档更新**：README 和 CHANGELOG 已记录本轮侧栏、会话搜索弹窗、批量标签、管理员标识和工作区入口优化。
- **版本同步**：应用版本升级至 `0.0.34`，同步更新 `package.json`、`package-lock.json` 与 README 当前版本说明。
- **验证状态**：`npm run verify` 已通过，包含文本完整性检查、前端资源检查、ESLint 和安全测试 `64/64`。

## [v0.0.33] - 2026-05-16
### 局域网部署与稳定性优化
- **局域网多实例可靠性**：根路径页面改为复用启动时预加载模板，减少高频访问时的同步磁盘读取；智能体队列改为数据库锁抢占，支持多实例协同、锁超时恢复和运行状态统计。
- **运行指标修正**：Prometheus 直方图 bucket 输出恢复为累计计数，便于内网监控平台正确计算分位值和告警。
- **代码职责收拢**：统一 logger 导出形态，抽出 `agent-model.js` 承载智能体模型调用与 Token 记账，新增 `number.js` 统一正整数环境变量解析。
- **数据访问去重**：复用会话消息查询语句，减少 `getMessages` 与 `getMessagesForContext` 的重复 SQL 维护成本。
- **迁移边界说明**：`migrate.js` 明确历史兼容迁移与新版 `schema_migrations` 的职责边界，后续结构升级优先使用稳定迁移 ID。
- **文本与前端安全检查**：新增 `npm run check:text` 并纳入 `npm run check`；抽出 `safe-html.js` 统一处理 HTML 转义与净化，并收紧前端 CSP 脚本策略。

### 管理后台 UI 细节优化
- **数据概览数值居中**：优化了运营概览页面顶部统计卡片的布局，将数值设置为水平居中显示，同时保持统计项名称在左上角的原始位置，提升了数据的可读性与美观度。
- **模型端点监控排列优化**：
  - 重构了模型端点状态列表的对齐逻辑，将名称与主机地址改为两端分布，解决了长 URL 导致的布局松散问题。
  - 引入了等宽字体 (Monospace) 展示主机地址，并优化了字号与颜色，使技术信息展示更加专业且层次分明。
  - 为监控项增加了平滑的悬浮过渡效果（边框色、阴影）及更大的圆角（12px），提升了管理后台的现代感。
  - 修正了状态详情（如并发、排队等）的间距与垂直对齐，确保多端点场景下的视觉一致性。

### 实时体验与附件预览
- **智能体实时事件推送**：新增 `/api/events` SSE 实时通道，推送智能体任务入队、运行、步骤变化、完成、失败、审批与通知事件；前端工作台优先使用实时事件刷新。
- **内网低依赖兜底**：实时推送基于浏览器原生 EventSource 和服务端内存订阅表实现，不新增外部运行依赖；断线或浏览器不支持时自动回退到原有轮询。
- **用户级隔离**：实时事件按登录用户订阅和投递，任务与通知只会发送给所属用户，并新增用户级 SSE 事件隔离测试。
- **附件在线预览**：新增聊天附件与附件库统一预览窗，支持图片、PDF、文本、Markdown、CSV、JSON 等常见内网文档直接查看；不支持的格式保留新标签打开入口。

### 会话标签治理
- **标签汇总与筛选**：新增标签汇总接口和多标签筛选能力，便于按项目、部门、风险等级或审计事项整理历史会话。
- **批量标签操作**：新增会话批量加标、移除标签、替换标签能力，并支持标签重命名和全局移除。
- **列表体验修正**：会话列表默认保持原有显示样式，不再常驻复选框；点击“批量”后才进入选择模式，批量操作、弹窗和提示全部中文化。
- **会话菜单交互优化**：修复会话“更多”菜单（三个点）定位偏差问题，确保菜单始终跟随按钮弹出；优化点击逻辑，点击菜单按钮时不再触发会话切换，提升操作连贯性。

### 设置页工作区化
- **右侧主区域展示**：系统设置从遮罩弹窗升级为与知识库、MCP、智能体一致的右侧工作区视图，保留左侧设置导航和内容区独立滚动。
- **返回体验统一**：设置页顶部新增工作区标题栏和“返回对话”按钮，左下角设置入口会随当前工作区高亮，整体交互更接近系统级模块。

### 导出审计与费用统计
- **合规审计包**：新增管理员 `pivot_compliance_audit.zip` 导出能力，集中打包会话清单、审计日志、模型用量明细和模型费用统计，适合内网离线留存、抽查和交付审计。
- **模型费用配置**：模型配置支持输入/输出百万 Token 单价与币种，并在数据库迁移中自动补齐 `input_price_per_million`、`output_price_per_million` 和 `price_currency` 字段。
- **费用报表与导出**：用量统计、费用明细和 CSV 导出按统一口径估算成本，便于多模型、多部门场景下核算内网资源开销。

### 文档与验证
- **文档更新**：README 和 CHANGELOG 已同步记录本轮优化、扩展、局域网部署建议和审计导出说明。
- **验证状态**：当前 `npm run verify` 已通过；文本完整性、语法检查、ESLint 和安全测试均完成，安全测试 `64/64` 通过。

## [v0.0.32] - 2026-05-16
### 输入界面与上下文 UI 深度优化
- **输入框布局重构**：将工具图标分离为左右两组，左侧为输入源配置（附件、知识库 RAG、MCP），右侧为状态与发送（上下文用量、清空输入、发送/停止），提升操作逻辑清晰度。
- **上下文用量圆环化**：移除顶栏文本显示，改为输入框内嵌入式的圆环进度条按钮。支持动态颜色反馈（正常、预警、临界）及悬浮展示详细 Token 用量信息。
- **图标标识统一**：将输入框内的 RAG 和 MCP 图标更新为与左侧导航面板一致的 SVG 路径，增强全局功能辨识度。
- **新增一键清空**：输入框右侧新增“小刷子”图标按钮，支持一键清除输入内容并重置高度；图标采用更专业的线条路径优化。
- **输入体验优化**：
  - 将输入框默认高度从 `62px` 增加到 `96px`，提供更宽敞的初始输入空间。
  - 修复了输入首个字符时因盒模型计算和过渡动画导致的输入框高度跳变问题。
  - 调整内边距，确保文字从最左侧开始输入，且多行输入时不会被底部工具图标遮挡。
- **交互细节**：将上下文按钮的鼠标指针从“问号”改为“手型”，符合通用交互直觉。

## [v0.0.31] - 2026-05-16
### 智能体工作台企业级升级
- 智能体从任务弹窗进一步升级为独立工作台，右侧主工作区集中承载任务执行、模板库、计划队列、通知中心、结果沉淀和能力清单。
- 新增 `agent_templates`、`agent_schedules`、`agent_artifacts`、`agent_notifications` 四类企业级对象，任务记录补齐模板、计划、上下文来源和断点续跑字段。
- 新增模板库 API 与前端模板条，可保存并复用目标、运行模式、MCP 策略、工具白名单、审批策略、Token 预算、失败重试和上下文来源。
- 新增计划任务 API 与服务端定时调度器，支持每日、每周和手动运行计划，到期计划自动入队并写入计划通知。
- 新增上下文来源管理，任务可选择自动、最近会话、知识库优先、自定义说明或不扩展上下文，运行时注入规划提示。
- 新增通知中心，任务完成、失败、停止、等待 MCP 审批、计划入队和结果沉淀都会生成用户级通知。
- 新增结果沉淀能力，任务最终答案或错误摘要可保存为结果资产，并在工作台中展示最近沉淀。
- 新增断点续跑入口，会基于上一轮任务状态、错误和最近失败步骤创建继续任务，避免只支持从头重跑。
- 计划任务与智能体运行继续使用当前用户可运行模型规则，用户私有模型只对所有者可见和可执行。
- 安全测试新增企业智能体闭环用例，覆盖模板、计划、上下文、结果沉淀、通知、断点续跑和用户隔离；当前 `npm run check`、`npm run lint`、`node tests/security.test.js` 均通过，安全测试 `61/61`。

### 产品体验与文档补充
- **智能体命名统一**：
  - 将早期任务类产品文案统一替换为“智能体 / 智能体任务”，覆盖左侧入口、页面标题、空态、Toast、确认弹窗、删除审计、后端错误提示、审计日志动作名和测试用例标题。
  - 保留内部 `agents` API、数据库表和运行时命名不变，避免因纯文案升级影响接口兼容性。
  - 全仓扫描确认未再命中旧产品名称，`npm run check` 与 `node tests/security.test.js` 均通过。
- **智能体工作台信息层级优化**：
  - 新任务区从“所有控制项平铺”调整为“目标、快捷任务、模型、步骤、运行”的轻量默认视图。
  - MCP、运行模式、审批策略、Token 预算、失败重试和上下文来源收纳到“高级设置”折叠区。
  - 模板库、计划执行、计划队列和通知中心收纳到“模板与计划”折叠区。
  - 能力清单、运行状态和结果沉淀收纳到“能力与结果”折叠区，保留内部滚动并移除外层任务面板的重复滚动条。
  - 修复展开“能力与结果”时挤压上方折叠标题的问题，折叠标题保持固定高度。
- **工作台返回入口增强**：
  - 智能体、MCP、知识库三个主工作台右上角“返回对话”按钮统一增强为更醒目的返回按钮。
  - 按钮增加左箭头、浅绿色强调底色、更大的尺寸和悬停态，提升从工作台回到对话窗口的可发现性。
- **知识库召回测试体验优化**：
  - 召回测试弹窗标题和说明移动到左上角，关闭按钮移动到右上角，移除底部重复关闭按钮。
  - “开始测试”点击后进入明确的运行状态：按钮变为“测试中...”、禁用重复点击，并在结果区显示正在测试召回效果的加载提示。
  - 请求完成或失败后自动恢复按钮，失败时在结果区展示可理解的失败提示。
- **README 文档恢复与合并**：
  - 参照 `0.0.27` 详细版 README 重新合并当前文档，将 README 从简化版扩展为完整说明。
  - 新版 README 补齐最新 `0.0.31` 智能体工作台、MCP、数据库 MCP、知识库 RAG、普通对话工具开关、模型能力、安全审计、运维监控、部署、环境变量、迁移和目录结构说明。
  - 修复 README / CHANGELOG 中文乱码与 BOM 问题，确认 UTF-8 读取正常且无旧产品名称残留。

## [v0.0.30] - 2026-05-16
### 企业级智能体产品化升级
- **可靠性闭环**：
  - 服务启动时自动恢复排队中的智能体任务，并将长时间无心跳的运行中任务标记为异常，避免重启后任务悬挂。
  - 新增任务级运行超时、单次工具调用超时、失败自动重试和重试次数记录。
  - 新增 `AGENT_RUN_TIMEOUT_MS`、`AGENT_TOOL_TIMEOUT_MS`、`AGENT_STALE_RUNNING_MINUTES` 等运行治理配置。
- **权限、审批与预算**：
  - 智能体任务新增 MCP 审批策略，高风险 MCP 工具可挂起等待用户批准或拒绝；数据库只读 MCP 继续支持自动执行。
  - 能力清单支持勾选工具白名单，创建任务时仅允许被选中的工具进入规划和执行范围。
  - 新增任务 Token 预算，超过预算后任务自动停止并记录原因。
- **任务模板、筛选与运营指标**：
  - 快捷任务模板可联动运行模式和 MCP 开关，支持风险总结、待办整理、资料检查等参数化入口。
  - 任务记录新增状态筛选和关键词搜索。
  - 工作台新增 7 日任务数、成功率、失败数和 Token 消耗指标。
- **审计报告与导出**：
  - 任务详情新增审批操作和导出入口，可导出 Markdown 或 JSON，包含目标、模型、步骤、工具输入输出、Token 和最终结果。
  - 导出次数写入任务记录，便于后续审计追踪。
- **验证状态**：
  - 已通过 `npm run check`。
  - 已通过 `npm run lint`。
  - 已通过 `node tests/security.test.js`，当前安全测试 `59/59` 通过。

## [v0.0.29] - 2026-05-15
### 企业级智能体运行时
- **运行队列与并发治理**：
  - 智能体任务从直接后台启动升级为受控队列，默认最多并发运行 2 个任务，可通过 `AGENT_MAX_CONCURRENT_RUNS` 调整。
  - 新增运行时状态接口 `/api/agents/runtime`，前端工作台展示当前并发、总排队数和我的排队数。
  - 任务记录补齐 `priority`、`started_at`、`last_heartbeat_at`、`completed_at` 等字段，并新增状态/优先级索引，便于后续运维排查和队列扩展。
- **工具权限与可审计边界**：
  - 智能体任务新增 `tool_policy` 和 `tool_allowlist`，后端会在规划前过滤可用工具，并在执行前二次校验工具名。
  - 工作台新增“允许 MCP 工具”开关；关闭后智能体只能使用内置只读能力，避免任务默认访问外部 MCP 或数据库。
  - 重新运行任务会继承原任务的运行模式、工具策略、工具白名单和优先级。
- **步骤审计与用量统计**：
  - 智能体步骤新增 `error_message`、`started_at`、`completed_at` 字段，计划、工具调用、控制动作和错误都保留更完整执行轨迹。
  - 智能体模型规划和总结的输入/输出/总 Token 会累计到任务记录，并继续写入统一模型用量事件。
  - 任务详情展示运行模式、工具范围、Token 用量、步骤数、工具数、错误数和耗时。
- **工作台交互增强**：
  - 新任务区新增运行模式选择：标准、深度、审计；规划提示会根据模式要求调整检索深度和证据约束。
  - 能力清单区标记工具来源为“内置”或“MCP”，帮助用户理解智能体可调用范围。
  - 并发和队列状态随自动刷新一起更新，长任务运行时状态更透明。
- **验证状态**：
  - 已通过 `npm run check`。
  - 已通过 `npm run lint`。
  - 已通过 `node tests/security.test.js`，当前安全测试 `59/59` 通过。

## [v0.0.28] - 2026-05-15
### 数据库 MCP 与普通对话工具调用
- **普通对话窗口接入 MCP**：
  - `/api/chat` 新增 MCP 工具规划流程，启用后会先判断本轮是否需要工具，再最多调用 1 个已缓存 MCP 工具，并把工具结果作为上下文注入正常流式回复。
  - 前端输入框新增轻量胶囊式 `MCP` 开关，状态持久化到本地；启用后发送前会弹出确认，避免模型在用户无感情况下访问外部 MCP 或数据库。
  - MCP 调用过程通过 SSE 返回“正在判断”“正在调用”“调用完成”“调用失败”等状态，前端会在当前助手消息中展示进度。
  - MCP 规划步骤兼容 Responses API，并在兼容服务不支持时回退到 Chat Completions。
- **统一数据库 MCP 预设**：
  - MCP 工作台新增统一的“数据库 MCP Server”预设，不再按 PostgreSQL、MySQL、SQL Server、SQLite、MongoDB 拆分多个入口；数据库类型作为连接表单字段选择。
  - 支持 PostgreSQL、MySQL/MariaDB、SQL Server、SQLite、MongoDB 五类数据库连接，新增 `pg`、`mysql2`、`mssql`、`mongodb` 依赖。
  - 数据库 MCP 服务使用 `pivot-db://` 内置协议接入现有 MCP 工具缓存和调用链路，外部 MCP Server 与内置数据库 MCP 可共用服务列表、刷新缓存和工具调用接口。
- **数据库 MCP 只读工具与安全边界**：
  - SQL 数据库提供 `db.list_tables`、`db.describe_table`、`db.run_readonly_query`；MongoDB 提供 `db.list_collections`、`db.sample_collection`、`db.aggregate`。
  - 只读 SQL 执行限制为单语句，并仅允许 `SELECT`、`WITH`、`SHOW`、`DESCRIBE`、`DESC`、`EXPLAIN` 等读取类语句；写入、DDL、管理类关键字会被拦截。
  - SQLite 文件路径限制在 `MCP_SQLITE_ROOTS` 或应用数据目录内，并以只读方式打开。
  - 新增数据库 MCP 连接测试接口，保存前和编辑时均可测试连接。
- **MCP 工作台体验优化**：
  - 已保存 MCP 服务点击“编辑”后改为弹窗编辑，不再回填到新增区域，避免用户无法取消编辑状态。
  - 编辑弹窗移除重复的顶部关闭按钮，仅保留底部取消/保存操作。
  - MCP 服务空态、缓存工具标题、工具说明和按钮文案进一步中文化；已缓存工具区域显示更贴近“可调用工具”的语义。
  - MCP 输入区按钮改为参考主流聊天工具的紧凑胶囊样式，与上传和发送按钮形成统一操作组。
- **知识库正式化与普通对话接入说明**：
  - 知识库已作为正式功能接入普通对话检索链路，不再在实验室功能中提供全局启停开关。
  - 普通对话输入区新增“知识库”胶囊开关，用户可按本轮问题决定是否检索私有文档；开关状态会在本地记忆。
  - `.env` 与 `.env.example` 移除知识库启停配置，避免用户误以为知识库仍是实验功能。
  - README 更新知识库说明，管理员仍可在知识库的 RAG 配置中调整召回阈值、命中数和候选数。
- **会话与知识库界面细节修复**：
  - 打开历史会话后自动滚动到底部，避免默认停在第一条消息。
  - 大模型回复完成、非流式返回、异常或中断后均保持底部可见，不再跳回顶部。
  - 点击左侧会话列表切换会话时不再强制刷新列表到首页，仅在新建会话等必要场景刷新侧边栏。
  - 知识库数据表底部距离、分页横线间距和操作列“详情/删除”按钮尺寸对齐系统设置页面密度。
  - 输入框工具区统一为“知识库 / MCP / 附件 / 发送”一组操作，附件按钮增加明确的悬停提示。
- **数据库迁移与启动稳定性**：
  - 修复旧数据库启动时 `agent_runs.deleted_at` 索引创建早于字段补齐导致的 `SqliteError: no such column: deleted_at`。
  - MCP 数据库连接表、服务索引和工具缓存索引迁移顺序调整为字段补齐后再创建。
- **验证状态**：
  - 已通过 `npm run check`。
  - 已通过 `npm run lint`。
  - 已通过 `node tests/security.test.js`，当前安全测试 `59/59` 通过。

## [v0.0.27] - 2026-05-15
### 智能体任务、MCP 与知识库工作台
- **左侧工具入口重构**：将“智能体”“知识库”“MCP”从传统设置面板中剥离，作为主页左侧面板中的常用工具入口；其中 MCP 入口统一命名为“MCP”，减少“设置项”和“工作工具”的语义混淆。
- **智能体任务工作台上线**：
  - 新增智能体任务弹窗，可选择模型、输入目标、设置最大步骤并后台执行任务。
  - 智能体任务支持任务列表、任务详情、执行进度、工具调用步骤、最终结果、停止任务和重新运行。
  - 任务执行结果排版改为左对齐段落化展示，优化步骤卡片、最终总结、原始数据折叠和滚动区域布局。
  - 任务记录列表与右侧详情区去重展示，减少重复标题、重复时间和重复摘要。
  - 模型选择器接入与主界面一致的模型能力图标体系，并补齐文本模型“文”能力标识。
- **智能体任务记录删除治理**：
  - 智能体任务记录新增“移除记录”操作，仅对已结束任务显示；运行中任务需先停止再移除。
  - 底层使用软删除，新增 `deleted_at`、`deleted_by_user`、`delete_reason` 字段，普通用户列表和详情会自动过滤已移除记录。
  - 内置 `admin` 超级管理员新增“删除审计”入口，可查看被移除的智能体任务记录、所属用户、模型、状态、步骤统计和删除时间。
  - 智能体记录移除会写入审计日志，后台运行器遇到已移除记录会安静退出，避免误报错误日志。
- **智能体内置工具扩展**：
  - 智能体能力清单接入知识库检索、会话检索、最近会话、知识库文档列表、可用模型列表等用户级工具。
  - 系统健康、模型运行状态等敏感工具限定为内置 `admin` 超级管理员可用，普通用户和普通管理员不可调用。
  - 智能体工具说明、任务标题、按钮和空态文案统一中文化。
- **MCP 工作台接入**：
  - 新增 MCP 服务管理弹窗，支持配置 MCP JSON-RPC 地址、API Key、说明、个人/全局共享范围，并刷新工具缓存。
  - 外部 MCP 工具进入智能体可用工具池，智能体任务可按目标选择调用。
  - 普通管理员创建 MCP 服务时默认保存为个人服务，只有内置 `admin` 超级管理员可创建全局共享服务。
- **知识库工作台优化**：
  - 知识库从系统设置页迁移为左侧独立工作台，承载文档列表、RAG 配置、召回测试、删除审计、批量重建、批量删除、失败重试和上传文档。
  - 知识库、MCP、智能体三个工具弹窗统一加宽、统一标题与说明对齐、统一关闭按钮为红色描边和红色字体。
  - 知识库列表默认每页 15 条，增加分页控件；移除冗余“文档列表/每页显示...”说明。
  - 知识库数据表继续压缩单元格行高，操作列按钮改为更紧凑样式，整体对齐系统设置页的数据表密度。

### 权限、模型配置与审计
- **超级管理员与普通管理员边界收紧**：
  - 内置用户名 `admin` 明确作为超级管理员，普通管理员不能创建全局模型、全局 Prompt、全局 MCP 服务，也不能管理其他管理员账号。
  - 普通管理员新增模型时默认保存为个人模型，可配置自己的模型接口和向量模型，不再误写成全局配置。
  - 普通管理员可以看到有权限使用的全局资源，但不能删除或覆盖超级管理员维护的全局模型和全局配置。
- **模型配置布局优化**：
  - “保存范围”和“可见部门”调整到模型名称、模型标识下方同一行，个人模型场景下可见部门提示更清晰。
  - 对话模型和向量模型均区分个人配置与系统默认配置，用户侧优先使用个人配置，未配置时回退系统默认。
  - 主界面、智能体弹窗和模型列表统一模型能力 SVG 图标与颜色样式，减少不同位置图标含义不一致的问题。
- **RAG 向量模型与 Token 统计修正**：
  - 向量模型请求纳入统一 Token 统计口径，补齐 embedding 输入/总量估算，避免总 Token 显著高于输入/输出显示值。
  - API Key 用量、模型用量和用户用量统计统一进行 token usage 归一化，确保 `total_tokens` 不小于输入与输出合计。
  - 向量服务自动登记为 usage-only 模型，便于在报表中追踪 RAG embedding 消耗。
- **审计日志中文化**：
  - 兼容并本地化历史英文审计动作，例如 `RAG_DOCUMENT_UPLOAD`、RAG 启停、删除、重建、召回反馈、向量模型连接测试等。
  - 智能体、MCP、知识库新增功能的审计动作统一使用中文，审计详情输出为中文字段说明。
  - 审计日志筛选同时兼容旧英文动作值与新中文动作名，避免历史日志无法搜索。

### 主页侧边栏与会话体验
- **侧边栏工具区重排**：品牌区下方新增智能体、知识库、MCP 工具入口，搜索行移动到工具入口下方，更符合“工具优先、搜索其次”的信息层级。
- **会话时间显示优化**：会话列表时间改为更自然的相对时间表达，避免从“1 天”一路显示到“99 天”的生硬递增。
- **整体中文化与弹窗一致性**：智能体、MCP、知识库弹窗内的按钮、说明、空态、工具名称、任务状态和错误提示统一中文显示；关闭按钮样式与系统设置弹窗保持一致。

### 测试与验证
- **安全测试覆盖扩展**：
  - 新增智能体任务 JSON 解析、工具权限、停止/重跑、软删除、超级管理员删除审计测试。
  - 新增普通管理员无法创建全局资源、无法管理管理员账号、无法保存全局向量配置等权限边界测试。
  - 新增 RAG 向量 token accounting、审计动作中文化和 embedding usage-only 模型统计测试。
- **验证状态**：当前 `npm run verify` 已通过语法检查、ESLint 和安全测试，测试通过 `58/58`。

## [v0.0.26] - 2026-05-14
### 知识库 (RAG) 交互体验与管理优化
- **乱码修复**：彻底解决了知识库上传文档时中文文件名显示为乱码的问题（已适配 `multer` 的 `latin1` 到 `UTF-8` 转码逻辑）。
- **指标卡片化**：重新设计了知识库文档详情的统计区域，采用精致的卡片化布局，并为“状态”、“进度”、“分块数”等关键指标增加了专属矢量图标，支持状态颜色感知（就绪为绿、错误为红）。
- **空间利用率提升**：
    - 优化了统计栏的栅格系统，确保 6 个关键指标在单行内整齐排列，避免跨行显示。
    - 解决了创建与更新时间在窄屏下显示不全（被省略号截断）的问题。
    - 优化了文档分块列表的布局，采用了更紧凑的字号和行高，显著提升了单屏信息密度。
- **界面文案简化**：
    - 将“知识库文档管理”标题简化为“知识库”，使界面更清爽。
    - 去除了召回测试弹窗标题中多余的 "(RAG Debug)" 标识。

## [v0.0.25] - 2026-05-14
### 界面美化与视觉体验优化
- **AI 头像升级**：将原有的六边形助理头像替换为极简、充满科技感的“星芒（Sparkle）”图标，更符合 AI 助手的现代认知风格。
- **登录页无缝融合**：通过引入 `mix-blend-mode: multiply;` 并移除强阴影轮廓，完美解决了登录页面 Logo 存在明显“白底色块”的问题，实现了 Logo 与背景卡片的纯净无缝贴合。
- **气泡消息边距修复**：优化了消息体内的 Markdown 段落排版（应用 `.message-content p:last-child { margin-bottom: 0; }`），去除了用户发送气泡和 AI 回复卡片底部多余的留白间距，使整体聊天界面更加紧凑匀称。
- **知识库中文文件名修复**：新增 `normalizeUploadedOriginalName()` 上传文件名规范化逻辑，保留正常中文文件名，并自动修复 multer/代理链路可能产生的 latin1 mojibake，避免 `knowledge_docs.name` 入库后在知识库列表显示乱码。
- **上传入口编码一致性**：RAG 知识库上传与普通附件上传统一使用同一套文件名规范化逻辑，避免一个入口修复、另一个入口二次转码的行为分叉。
- **回归测试补齐**：新增中文文件名保留/修复测试，以及知识库上传入库测试，确认 `测试文档.txt` 会以正确中文名称写入 `knowledge_docs`；当前 `npm run verify` 通过 `49/49`。

## [v0.0.24] - 2026-05-14
### 安全、维护与长期运行治理
- **后台维护闭环增强**：
  - 新增软删除存储垃圾回收任务，自动清理超过保留期的附件物理文件、知识库源文件、知识库分块、`knowledge_chunks_fts` 与历史消息全文索引，避免 `uploads/` 和 SQLite FTS 长期膨胀。
  - 统一将数据库优化职责收拢到 `server/services/maintenance.js`，移除连接层重复定时器，并在维护周期内执行 `PRAGMA optimize` 与 `PRAGMA incremental_vacuum(...)`。
  - 维护状态新增存储 GC、数据库优化、备份等任务结果，便于系统监控页和 Prometheus 持续观测。
  - 存储 GC 会记录物理文件删除结果，只有删除成功或文件已不存在时才清空数据库引用；遇到文件锁、权限异常等失败场景会保留记录，等待下一轮维护任务重试。
- **内置数据库热备份与滚动清理**：
  - 维护服务内置 SQLite `db.backup()` 热备份，默认每 24 小时生成时间戳备份到 `data/backups/`。
  - 新增 `DB_BACKUP_DIR`、`DB_BACKUP_RETENTION_DAYS`、`DB_BACKUP_MAX_VERSIONS` 配置，默认保留 7 天且最多保留最近 7 个备份版本，防止备份目录撑爆磁盘。
  - 备份文件名统一使用东八区时间并追加毫秒，同一秒内连续触发备份时会自动生成不冲突的文件名，避免热备份被覆盖。
  - Prometheus 指标新增数据库备份成功时间、备份文件大小和滚动清理文件数；监控页“健康与维护”同步显示数据库备份状态。
  - 修复手动 `scripts/backup_db.js` 的数据库入口兼容问题，手动备份和自动备份复用同一数据库连接。
- **账号与探测安全加固**：
  - 管理员强制重置用户密码时同步吊销目标用户全部 refresh token，避免被盗账号在密码重置后继续保持登录。
  - 模型连通性探测接口新增专项限流，并保证鉴权先于限流执行，降低被泄露 API Key 滥用为外部 DDoS 跳板的风险。
  - OpenAI-compatible 网关新增 `/v1/embeddings`，第三方客户端可使用 Pivot API Key 通过当前用户 RAG embedding 配置转发调用向量模型；`/v1/models` 同步暴露已配置的 embedding 模型。
  - API 接入管理页的“可用模型”同步显示向量模型，并以“聊天/向量”能力标签和目标接口区分 `/v1/chat/completions` 与 `/v1/embeddings`。
  - 向量转发接口新增独立限流，支持字符串、字符串数组和 token 数组输入，并返回标准 OpenAI embeddings 响应结构与 API Key 用量统计。
  - 出站请求 SSRF 防护覆盖本机、内网、链路本地地址、云元数据地址和受限主机别名，RAG HTTP 向量服务与外部模型探测统一走安全校验。
  - 上传接口新增专项限流，配合文档解压上限和 zip 炸弹防护，降低高频上传导致磁盘或 CPU 被耗尽的风险。

### 前端稳定性、CSP 与交互优化
- **前端事件绑定去内联化**：
  - 移除 `client/chat` 下全部 `onclick=` 内联点击事件，静态按钮改为 `data-static-action` / `data-auth-password-toggle`，由 `app.js` 统一事件委托。
  - 模型列表、提示词列表、附件列表、API Key 列表和侧边栏会话菜单全部改为 `data-*` 声明式动作绑定，避免把可执行 JavaScript 拼接进 HTML。
  - 会话更多菜单改为 DOM API 构建菜单项并绑定事件，降低标题、标签等用户数据进入事件字符串的风险，为后续收紧 CSP 打基础。
- **表格渲染安全组件化起步**：
  - 新增 `renderTableMessage()`，表格空态、加载态和错误态使用 DOM API 与 `textContent` 渲染，减少裸 `innerHTML` 字符串提示行。
  - API Key、第三方 API 调用记录、用户输入输出记录等高频表格已切换到安全提示行工具。
- **原生输入弹窗去浏览器化**：
  - 新增应用内 `showInputPrompt()` 安全输入弹窗，支持自定义标题、说明、占位符、密码类型、必填校验以及 Enter/Escape 键盘操作。
  - 管理员重置用户密码、查看模型明文密钥前的当前密码校验、加密文档上传密码输入已全部从原生 `prompt()` 迁移到统一弹窗。
  - 复扫 `client/chat` 已无原生 `prompt()` 与 `onclick/onchange/oninput` 内联事件残留，进一步降低 CSP 收紧阻力。
- **流式输出与大文本体验优化**：
  - 前端 SSE 解析升级为按标准事件边界处理，避免网络分片导致 JSON 半包被静默丢弃。
  - 长思考模型输出采用节流渲染与缓冲刷新，降低 DeepSeek-R1 等长 reasoning 场景下的主线程卡顿概率。
  - 多模态上传增加进度反馈，用户上传大 PDF 或扫描件时可感知上传过程。
  - 自定义模型选择器补齐键盘可访问性，支持方向键切换、Enter 选择和 Escape 关闭。

### RAG 与时间一致性
- **RAG 配置可调优**：
  - 实验室 RAG 配置新增 Chunk Size 和 Overlap，可按文档类型微调分块最大长度与重叠字符数。
  - RAG 参数保存后自动清空相关缓存，确保新的召回参数立即生效。
- **东八区时间一致性**：
  - 保留并继续使用 `getBeijingTimestamp()`，所有新增维护状态、审计时间、清理状态和数据库写入时间继续统一使用 Asia/Shanghai 语义。
  - 修正历史 UTC 时间补正、refresh token 过期判断和维护任务时间展示的时区一致性。

### 代码质量与验证
- **Zero Warnings 代码基线**：
  - 清理遗留 ESLint 警告，保持 `npm run lint` 零警告。
  - 新增 `npm run verify`，统一执行语法检查、ESLint 和安全测试。
- **测试覆盖扩展**：
  - 安全测试覆盖 SSRF 拦截、SSE 分片解析、存储 GC、增量 vacuum 状态、管理员重置密码令牌吊销、模型探测限流顺序、数据库热备份与滚动清理。
  - 当前验证命令：`npm run verify`，测试通过 `47/47`。

## [v0.0.23] - 2026-05-14
### 模型选择器视觉增强与能力约束
- **模型选择器视觉重构**：
  - 将原生 HTML `select` 升级为高性能自定义下拉组件，支持在触发器和列表中显示模型能力标识。
  - 模型名称旁实时展示视觉 (👁️) 与推理/思考 (🧠) 图标，图标状态随能力配置动态切换（仅在具备能力时显示）。
  - 下拉列表采用双行卡片布局，整合展示模型显示名称、私有标识、物理标识、日额度及核心能力，信息密度与视觉美感显著提升。
- **模型能力硬性约束**：
  - 建立“能力-功能”联动机制：若当前选中模型未开启视觉能力，系统将自动拦截并禁用附件上传功能，按钮切换为禁用状态并配合 `not-allowed` 指针。
  - 强化发送前校验：在消息发送环节增加二次检查逻辑，对于不支持附件处理的模型提供明确的弹窗告知，防止用户发送包含图片或文档的无效请求。
- **交互体验细节打磨**：
  - 为模型下拉列表增加平滑的淡入与位移动画效果，支持点击页面空白区域自动收起。
  - 增加模型切换时的附件兼容性预警，防止在已有附件的情况下切换到不支持附件的模型导致发送受限。
  - 优化了下拉触发器的布局，采用 `margin-left: auto` 确保切换箭头在各种名称长度下始终靠右对齐。

## [v0.0.22] - 2026-05-13
### 本轮完整优化汇总
- **RAG 文档治理升级**：知识库文档补充 `status`、`is_enabled`、分块数量、已索引分块、处理进度、错误信息、源文件路径/大小和处理时间等元数据，支持启用/停用、批量删除、批量重建、失败重试、详情查看和分块预览。
- **RAG 删除审计增强**：知识库文档删除改为软删除，保留文档元数据、分片、源文件路径和删除时间；普通用户列表与召回链路会自动排除已删除文档，内置 `admin` 超级管理员可在知识管理页查看“删除审计”记录。
- **RAG 索引与检索增强**：抽离 RAG 配置、文档管理、索引服务和缓存能力；支持并发索引、写入事务、服务重启后恢复处理中任务，并在调试查询中临时调整相似度阈值、topK 与候选数量。
- **RAG 向量配置轻量化**：将历史“云端 API / 本地 HTTP 服务”统一为 `HTTP 服务`模式，兼容云端、本机与局域网 Embedding 接口；彻底移除进程内模型解析配置，项目本身不再承担向量模型加载运行时，历史 `cloud/local` 配置值统一兼容映射为 `http`。
- **RAG 向量模型自动发现**：知识库页的向量模型配置弹窗对齐大语言模型配置体验，将 API Key 放置在 Base URL 下方，并新增“从 API 获取模型列表”能力；后端同时兼容 OpenAI-compatible `/models` 与 Ollama `/api/tags` 模型发现路径，索引请求可从 Base URL 自动补全到 `/v1/embeddings`。
- **RAG 个人向量配置**：普通用户现在可以在知识库页保存自己的 Embedding HTTP 服务配置，索引、调试召回和聊天召回优先使用个人配置，未配置时自动回退管理员系统默认；普通用户配置地址仍沿用安全校验，默认不允许内网/本机地址，避免越权探测内网服务。
- **RAG 质量闭环**：新增 `rag_feedback` 表、反馈接口与审计记录，调试结果可提交“有用/无用”反馈；RAG 上传、删除、启停、重建、批量操作和反馈均写入审计日志。
- **RAG 管理体验优化**：知识库管理页增加汇总指标、批量操作状态、调试结果明细、命中文档/分块分数展示；将 RAG 中原本依赖浏览器 `alert/confirm` 的详情与确认交互替换为系统内自定义弹窗，保持视觉风格、遮罩行为和操作按钮一致。
- **RAG 安全与限流**：调试查询接口加入速率限制，避免 embedding 服务被管理端高频压测；RAG 设置保存后自动清空缓存，确保新阈值和候选参数立即生效。
- **系统健康检查**：新增系统健康服务，`/api/health` 返回数据库、数据目录、上传目录、内存和磁盘检查结果。
- **自动维护扩展**：维护任务覆盖审计日志清理、第三方 API 调用日志清理、过期刷新令牌清理和 `PRAGMA optimize`，系统会自动记录任务结果和错误信息。
- **账号安全加固**：用户修改密码后会清理该用户全部刷新令牌，降低旧设备或旧刷新令牌继续续期的风险。
- **测试覆盖补充**：新增系统健康快照、维护任务、过期刷新令牌清理、RAG 中文检索、个人向量配置、向量模型发现和安全边界相关测试；当前 `npm test` 覆盖 70 个 JS 文件语法检查和 35 项测试。

### 系统监控面板深度优化 (Monitoring & Observability)
- **卡片式指标升级**：
  - 新增“维护任务”监控卡片，实时展示后台维护服务运行状态及审计日志保留天数。
  - 为所有核心指标（并发、Token、消息、延迟等）补齐了 SVG 矢量图标，并优化了内边距与字体比例。
  - 将消息统计细化为“今日消息数”与“15分钟活跃用户”，提升即时观测粒度。
- **资源信息透明化**：
  - 恢复并扩展了系统资源详细列表（9行），涵盖运行主机、操作系统、Node 版本、CPU 型号、系统/进程时长及负载分布。
  - 内存与磁盘空间引入动态彩色进度条（绿/琥珀/红），支持根据负载自动切换颜色，并同步显示物理路径与剩余容量。
- **运行健康与维护审计**：
  - 管理端监控面板同步展示健康与维护状态，覆盖数据库、数据目录、附件目录等核心组件的实时健康矩阵。
  - 新增后台维护任务执行记录，展示审计日志清理、API 日志清理、令牌清理和数据库优化的最近成功时间及处理条数。
- **GPU 监控专业化**：
  - 为多显卡服务器引入 GPU 序号识别（#0, #1...），并优化了显存占用与利用率的并排展示格式。
  - 监控面板顶层集成“保护状态”与“拒绝阈值”显示，并根据端点部署形态自动切换“本地模式”或“集群模式”提示语。
- **模型端点实时链路监控**：
  - 新增“模型端点运行状态”区域，展示 Host 地址、挂载模型名、并发占用/上限、排队深度、连续失败次数及熔断倒计时。
  - 引入“本地/远端”身份标识勋章，通过集群模式感知逻辑，明确告知用户当前 GPU 指标的归属范围。
- **RAG 与存储深度观测**：
  - 新增独立的“知识库与存储”监控板块，聚合展示 RAG 检索总数、缓存命中率、平均检索延迟、索引分块数、数据库大小及附件库总容量。
- **全站接口语义化映射**：
  - 极大扩充了路由名称映射表（ROUTE_NAME_MAP），为全站 API 及静态资源路径提供了精准的中文描述，彻底消除“热点接口”列表中的未知路径。
- **Prometheus 指标补齐**：指标接口增加系统健康、维护状态、RAG 检索次数、缓存命中、候选数量、命中数量、Top Score、检索耗时和入库耗时，方便生产环境持续观测。

### 性能优化 (Deep Performance Optimization)
- **数据库加速**：为 `messages`、`audit_logs`、`model_usage_events` 等核心表增加了时间戳索引，消除全表扫描隐患。
- **并发索引**：RAG 文档索引流程升级为并发处理模式（Batch Size: 5），大幅提升大型文档上传处理速度。
- **自动维护增强**：新增 `api_call_logs` 定期清理任务，默认保留 30 天，防止日志无限增长影响性能。
- **数据库事务**：在 RAG 写入过程中引入批量事务处理，降低磁盘 IO 损耗。

### UI/UX 改进
- **极简监控面板**：重新设计了系统资源展示逻辑，将信息密度提升了 250%，核心指标由 10 行精简至 4 行。
- **警示视觉重构**：采用琥珀色/蓝色标签体系替换传统红色告警，提示语精简 60% 以上，减少视觉压力。
- **响应式修复**：解决了内存进度条在小屏幕下的布局溢出问题。
- **热点接口弹窗优化**：统一了接口详情弹窗的关闭按钮样式，采用红色描边与标准字重，使其在视觉上与用户记录弹窗保持高度一致。
- **监控布局重构**：新增了独立的“知识库与存储”监控卡片，将 RAG 检索统计（次数、命中率、延迟）与磁盘存储（数据库、附件）从核心硬件区域剥离，信息分类更专业。
- **界面一致性增强**：统筹了管理后台所有弹窗操作按钮的高度（34px）与对齐规范，消除了细微的视觉跳跃感。
- **硬件监控修复**：确保并恢复了监控面板中本机 GPU 具体核心状态（显存占用、利用率）的正确显示。
- **RAG 召回测试体验重构**：将知识库页面内联的“召回测试”面板剥离为独立的专业化模态弹窗；采用彻底的行列分离 Flex 布局重构了参数面板，实现了各项参数与按钮的像素级对齐，并强化了按钮点击的缩放动画反馈。
- **向量配置安全交互**：在知识库 RAG 向量配置弹窗中引入了 API Key “明/密文切换（眼睛图标）”机制，并智能回填占位符，既保证了本地展示安全，又缓解了用户“找不到密码”的焦虑。

### 问题修复 (Bug Fixes)
- **数据概览零值修复**：修复了 `escapeHtml` 函数在处理数值 `0` 时的逻辑缺陷，确保附件数量等指标在为零时能正常显示数字 “0” 而非空白。
- **用户管理重复按钮修复**：彻底解决了因 DOM 观察器重复触发导致用户列表中“记录”按钮堆叠的问题，优化了渲染流程。
- **隐藏配置鉴权修复**：修复了向量模型配置或列表拉取时，如果前端使用星号隐藏了 API Key，点击操作会导致后端收到空值从而引发 401 的错误。现在后端会自动智能回退并继承已保存的有效密钥。
- **API 代理容灾修复**：为后端所有的 `/api` 和 `/v1` 路由增加了专属的 404 JSON 拦截器，防止 Nginx/代理层抛出 HTML 错误页面导致前端出现 `Unexpected token '<'` 致命解析异常。

## [0.0.21] - 2026-05-13

### PWA 更新机制、监管弹窗分页与系统设置交互优化
- **PWA 可控更新策略继续收紧**：
  - Service Worker 不再拦截 `navigate` 页面导航请求，避免覆盖服务端版本注入、浏览器默认离线行为和服务端缓存头策略。
  - Service Worker 保持 `vendor-only` 策略，仅缓存稳定 vendor 资源；业务页面、`/chat/*` 脚本样式、`/api/*`、`/version.json` 与 `/manifest.json` 均交回浏览器和服务端处理。
  - 保留 `/version.json`、`pwa-manager.js`、旧缓存清理与 `/pwa-reset` 兜底能力，形成可检测、可提示、可清理的更新体系。
- **系统设置关闭按钮统一优化**：
  - 系统设置主弹窗移除右上角浮动 `X`，改为侧边栏底部常驻“关闭设置”按钮，避免与数据概览、系统监控、审计报表等页面右上角操作按钮堆叠。
  - API Key 创建/显示子弹窗统一使用文字“关闭”按钮，不再使用 `X` 图标按钮。
  - 第三方 API 调用记录弹窗移除右上角浮动关闭按钮，改为弹窗头部内置“关闭”按钮，并禁止点击遮罩误关闭。
- **超级管理员监管记录分页**：
  - 超级管理员用户输入输出记录弹窗新增分页，每页默认 15 条，支持切换会话、包含已删除记录、刷新时正确重置或保持分页状态。
  - `/admin/users/:id/messages` 支持 `page`、`limit` 参数并返回 `total/page/limit`，避免一次性加载大量用户历史输入输出记录。
  - 第三方 API 调用记录弹窗明确登记分页状态，继续按每页 15 条展示，并区分输入 Token 与输出 Token。
- **监管弹窗与记录查看体验**：
  - 用户输入输出记录弹窗和第三方 API 调用记录弹窗继续保持加宽布局、内容截断、悬停查看完整内容和弹窗内关闭按钮。
  - 同步更新内联兜底脚本，降低旧缓存或懒加载脚本未及时刷新时导致记录按钮、记录弹窗或分页缺失的概率。
- **侧边栏会话列表分页优化**：
  - 侧边栏会话列表无限滚动从 `page + OFFSET` 分页升级为游标分页，后续加载通过 `nextCursor` 获取更早会话，降低新会话创建、旧会话更新置顶等场景导致的重复项或漏项风险。
  - `/sessions` 接口新增游标分页返回值 `nextCursor`，并保留旧 `page` 参数兼容历史调用。
  - 会话列表排序游标使用日期、置顶状态、更新时间和会话 ID 作为稳定依据，提升大列表滚动加载的一致性。
  - 侧边栏底部新增“加载中”“已加载全部”“暂无会话”“加载失败，请重试”等状态提示，增强无限滚动的视觉反馈。
- **版本同步**：
  - 应用版本升级至 `0.0.21`，同步更新 `package.json`、`package-lock.json`、README 当前版本说明和前端版本兜底值。

#### 验证
- 已通过 `npm test`

## [0.0.20] - 2026-05-13

### 超级管理员监管入口与记录弹窗完善
- **超级管理员用户输入输出记录**：
  - 用户管理列表为内置 `admin` 超级管理员新增“记录”按钮，用于按用户查看聊天输入、模型输出和软删除后的历史消息。
  - 用户记录弹窗支持按会话筛选、包含已删除记录，并展示时间、会话、输入/输出类型、模型、Token 和消息内容。
  - 长消息内容默认单行截断，鼠标悬停可查看完整内容，避免监管表格被长文本撑开。
- **第三方 API 调用记录可视化**：
  - API 接入管理页将超级管理员入口明确为“API 调用记录”，用于查看用户通过第三方 API Key 调用时的输入、输出与 Token 明细。
  - 第三方 API 调用记录弹窗拆分展示输入 Token 与输出 Token，保留请求内容、响应内容、状态、用户、API Key、模型和调用时间。
  - 优化调用记录弹窗宽度与内部表格布局，去除右侧大块空白，避免出现不必要的水平滚动条。
- **会话历史列表交互优化**：
  - 会话历史列表改为标题右侧直接显示相对时间，鼠标悬停时隐藏时间并显示三点操作按钮。
  - 移除列表标题前方按日期分组的重复日期展示，使会话列表更紧凑。
- **文档与版本同步**：
  - 应用版本升级至 `0.0.20`，README、package 元数据和锁文件同步更新。
  - 将本轮未完整沉淀到文档的超级管理员记录查看、第三方 API 调用记录入口和弹窗布局优化补充到更新日志。
- **PWA 可控更新体系**：
  - Service Worker 改为 `vendor-only` 策略，仅缓存稳定第三方库和图标，业务页面、`/chat/*` 脚本样式和 API 请求始终走网络。
  - 新增 `/version.json` 版本清单，前端通过 `pwa-manager.js` 检测版本变化并提示用户刷新。
  - `pwa-manager.js` 负责注册 Service Worker、检测更新、清理旧 `pivot-*` 缓存，并提供 `window.PivotPwa.reset()` 兜底清理能力。
  - 新版 Service Worker 激活时会删除历史旧 `pivot-*` 缓存，兼容旧缓存退场。
  - 新增 `/pwa-reset` 急救页面，可在浏览器仍被旧 PWA 控制时手动注销 Service Worker 并删除旧缓存。
  - 服务端对 `/chat`、`/common/styles`、`/manifest.json`、`/version.json`、`/pwa-manager.js` 和 `/sw.js` 统一发送 `no-store` 响应头，减少浏览器 HTTP 缓存与 PWA 缓存叠加导致的旧版残留。

#### 验证
- 已通过 `npm test`

## [0.0.19] - 2026-05-12

### 数据保留、超级管理员边界与 PWA 缓存修复
- **删除行为改为可监管保留**：
  - 用户删除聊天消息、聊天会话或附件时，数据库不再物理删除对应记录，而是写入软删除标记，保留原始会话、消息、附件元数据、审计日志、用量统计和用量明细。
  - 删除用户时改为禁用并标记删除状态，不再清空其历史会话、消息、附件、审计和用量数据，便于后续合规追溯。
- **`admin` 超级管理员与普通管理员分层**：
  - 仅内置用户名为 `admin` 的超级管理员可查看所有用户元数据、附件文件、已删除用户及其已删除数据。
  - 普通 `role=admin` 管理员保留用户管理、审计日志、系统监控和审计报表权限，但不开放用户具体会话内容、附件文件和已删除用户历史数据。
  - 非 `admin` 用户不能重置内置 `admin` 账户密码；前端隐藏按钮，后端同步拒绝越权请求。
- **附件库归属展示**：
  - 超级管理员进入附件库时，表格新增“用户”列，显示附件所属用户，便于跨用户审计和归属核对。
  - 附件库接口返回 `isSuperAdmin` 标识和附件所属用户名/昵称，普通用户仍只看到自己的附件。
- **PWA 缓存刷新修复**：
  - 应用版本升级至 `0.0.19`，聊天页主脚本、样式和管理端懒加载脚本追加构建修订参数，避免浏览器继续加载旧版 `extra.js` 导致附件库“用户”列不生效。
  - Service Worker 注册版本加入构建修订号，并将 `/chat/*` 与公共样式资源改为优先网络、失败回退缓存，降低 PWA 缓存卡旧界面的概率。
- **第三方 API 调用内容留痕**：
  - 新增 `api_call_logs` 表，第三方 API Key 调用 `/v1/chat/completions` 时会记录请求 `messages`、响应正文、错误信息、Token、模型、API Key、IP 和时间。
  - API 接入管理页新增“第三方 API 调用记录”，仅内置 `admin` 超级管理员可见和检索，用于特殊情况下追溯第三方客户端输入与输出。
- **超级管理员用户记录查看**：
  - 用户管理列表为内置 `admin` 超级管理员新增“记录”入口，可按用户打开输入输出记录弹窗。
  - 记录弹窗支持按会话筛选、包含已删除记录，并展示时间、会话、输入/输出类型、模型、Token 和消息内容，内容过长时截断并支持悬停查看完整值。
- **Token 与后台界面优化汇总**：
  - 数据概览、系统监控、审计报表、API 接入管理、用量统计和用量明细中的 Token 数量统一使用 `K`、`M`、`B` 等短单位展示，小于 `1000` 直接显示数字，`0` 不再显示为 `0 Tokens`。
  - 模型配置补充输入 Token 上限、输出 Token 上限、思考模型、视觉能力等参数展示，API Key 位于 Base URL 后，模型名称与模型标识同一行，可见部门单独成行。
  - 审计日志、附件库、用户管理等后台表格统一为更紧凑的数据表样式，每页默认 `15` 条，长内容截断并通过悬停查看完整值。
  - 审计报表时间筛选补充更多预设范围，并支持自定义开始日期和结束日期。

#### 验证
- 已通过 `npm test`

## [0.0.18] - 2026-05-12

### 管理后台表格、模型配置与 Token 统计统一优化
- **全站数据表样式统一**：
  - 用户管理、模型设置、审计日志、附件库、用量统计、用量明细等后台数据表统一采用更紧凑的字体、行高、单元格截断和悬停完整提示样式。
  - 用户管理、审计日志、附件库等列表默认每页显示 `15` 条数据，降低分页频率并保持表格高度一致。
  - 审计日志页面优化了标题、筛选区、表格和分页控件之间的间距，避免页面出现不必要的垂直滚动条和表格底部大块空白。
- **审计日志体验优化**：
  - 审计日志“操作”和“详情”列改为单行截断展示，鼠标悬停时显示完整内容。
  - 审计日志筛选栏和分页区域对齐附件库页面的布局节奏，提升密集日志场景下的可读性。
- **模型配置能力增强**：
  - 模型配置新增“输入 Token 上限”和“思考模型”能力开关，配合原有输出 Token 上限、视觉输入、并发上限、健康检查等参数形成完整配置。
  - 模型列表和模型选择器使用图标展示视觉、思考、文本等能力标识，不再使用“视觉”等文字标签占用空间。
  - 模型配置弹窗布局调整为：API Key 放在 Base URL 后，模型展示名称与模型标识同一行，可见部门独立整行展示，其余模型参数按两列排列。
- **Token 统计口径完善**：
  - 模型调用、用户用量统计、用量明细和第三方 API Key 调用新增输入 Token、输出 Token、总 Token 的拆分统计。
  - OpenAI 兼容接口会记录 API Key 调用的输入 Token、输出 Token 和总 Token，用于配额、报表和明细统一展示。
  - 用户删除会话时，不再丢失对应用量统计；系统会在删除前保留消息 Token 用量事件，确保历史统计和明细可追溯。
- **Token 数量显示统一**：
  - 数据概览、系统监控、审计报表、API 接入管理、用量统计和用量明细中涉及 Token 数量的展示统一使用 `K`、`M`、`B` 等短单位。
  - 移除了容易产生歧义的 `T` 后缀；小于 `1000` 的 Token 数直接显示数字，例如 `0`、`97`，不再显示 `0 Tokens`。
  - Token 趋势图和审计报表柱状图增加具体数值标注，便于直接读取每个点或每个条目的消耗量。
- **审计报表时间筛选增强**：
  - 审计报表预设范围补充为今天、近 3 天、近 7 天、近 14 天、近 30 天、近 90 天、近半年、近 1 年和近 2 年。
  - 新增自定义开始日期和结束日期筛选，支持按精确日期区间生成报表。
- **API 接入管理优化**：
  - API Key 管理表新增输入 Token 统计，并保持输入、输出、总 Token 口径一致。
  - 可用模型名称居中显示，API Key 页面说明文案精简为“你可以生成 API Key 用于在第三方应用中调用本系统的模型能力。”
- **数据保留与安全边界修复**：
  - 用户删除消息、会话或附件时改为软删除：普通用户界面隐藏，数据库保留原始会话、消息、附件元数据、文件、审计日志和用量统计。
  - 删除用户时改为禁用并标记删除，不再物理清空其历史会话、消息、附件、审计和用量数据。
  - 普通 `role=admin` 管理员保留用户管理、审计日志、系统监控和审计报表能力，但不开放用户具体会话内容、附件文件和已删除用户历史数据。
  - 只有内置用户名为 `admin` 的超级管理员可以查看所有用户元数据、附件文件、已删除用户及其已删除数据，用作低频应急监管入口。
  - 非 `admin` 用户不能对内置 `admin` 账号执行重置密码操作；前端隐藏按钮，后端同步拒绝该请求。
- **系统设置与账户安全界面优化**：
  - 系统设置弹窗中涉及模型 Token 用量的输入和展示改为带单位的格式，避免大数字撑开布局。
  - 账户安全页面的修改密码卡片居中显示，表单宽度和视觉重心更符合弹窗式后台页面布局。
- **版本与缓存同步**：
  - 应用版本号同步升级至 `0.0.18`，静态资源版本随发布刷新，降低浏览器加载旧脚本导致界面未更新的概率。

#### 验证
- 已通过 `npm run check`

## [0.0.17] - 2026-05-09

### 并发等待、视觉能力配置与模型配置弹窗优化
- **并发排队文案澄清**：
  - 聊天页排队提示从“前方还有 N 个请求，当前正在处理 X/Y 个”调整为区分“前面等待请求数”和“当前生成占用数”，避免 `前面 0 个请求` 与 `正在生成 1/1` 同时出现时产生歧义。
  - 排队事件会展示最长等待时间，便于用户判断是否继续等待。
- **排队等待时间延长**：
  - 全局 AI 队列默认超时从 `60000ms` 调整为 `300000ms`。
  - 模型端点队列新增并同步 `MODEL_ENDPOINT_QUEUE_TIMEOUT_MS=300000`，避免局域网模型长推理场景过早排队超时。
  - `.env`、`.env.example` 与 README 配置示例已同步更新。
- **模型视觉能力显式配置**：
  - `models` 表新增 `supports_vision INTEGER DEFAULT 0` 字段，启动迁移会自动为历史数据库补齐该字段。
  - 模型配置弹窗新增“视觉输入（图片/扫描件）”开关；模型列表与模型下拉会显示“视觉/文本”能力标识。
  - 聊天接口与 OpenAI 兼容 `/v1/chat/completions` 在收到图片或扫描件视觉输入时，会先检查模型是否启用视觉能力；未启用时直接返回清晰提示，不再把视觉请求盲目转发给上游。
  - 明确区分普通文档与视觉输入：Word、Excel、文本型 PDF 会先抽取文本，仍可交给文本模型处理；图片与扫描 PDF 兜底图片才依赖视觉能力开关。
- **模型配置弹窗布局优化**：
  - “添加/编辑模型配置”弹窗宽度扩展至 `820px` 自适应布局，并改为两列网格，减少纵向高度。
  - 保留“模型标识”等长输入整行展示，避免按钮和输入框被挤压。
- **部署升级文档补充**：
  - README 增加容器部署后确认新镜像、新代码和数据库字段迁移的检查命令。
  - 补充 `docker compose down && docker build -t pivot . && docker compose up -d` 的重建流程，以及 `PRAGMA table_info(models)` 字段检查命令。

#### 验证
- 已通过 `npm run check`
- 已通过 `npm test`

## [0.0.16] - 2026-05-08

### 认证时效、检查链路与部署文档修复
- **Refresh Token 过期时间统一**：刷新令牌的 `expires_at` 改为使用 `getBeijingTimestamp(expiresAt)` 写入，和过期校验使用同一时区格式，避免实际有效期比配置提前约 8 小时结束。
- **Lint 配置补齐**：在 `eslint.config.js` 的 Node 全局变量声明中加入 `setImmediate`，修复测试文件通过但 `npm run lint` 被 `no-undef` 阻断的问题。
- **OpenAI 兼容接口用量统计补齐**：`/v1/chat/completions` 命中“不支持能力”兜底响应时，现在同样会记录 `model_usage_events`，并在 API Key 调用场景累加 `usage_tokens`，保证配额和统计口径一致。
- **容器部署说明补充**：README 离线部署步骤加入 `docker network create ai-bridge`，避免外部网络未预创建时 `docker-compose up -d` 启动失败。

#### 验证
- 已通过 `npm run lint`
- 已通过 `npm test`
- 已通过 `npm audit --audit-level=moderate`

## [0.0.15] - 2026-05-08

### API 接入优化与 UI 细节打磨
- **模型标识标准化**：
  - 优化了 `openai.js` 兼容接口，将外部调用标识 (`id`) 统一为语义化的模型名称 (`model_name`)，使第三方客户端接入更直观。
  - 在“API 接入”面板新增“可用模型”列表展示，方便用户快速获取外部调用所需的模型 ID。
- **并发排队体验增强**：
  - 聊天接口现在会在进入并发控制前先建立 SSE 连接，用户请求进入队列时可立即收到排队状态，而不是长时间停留在等待响应。
  - 前端聊天气泡新增“正在排队，前方还有 N 个请求”提示，并在首次排队时弹出提示；全局 AI 队列与模型端点队列都会通过统一的 `queue` 事件反馈等待与就绪状态。
  - 队列满、排队超时、模型端点繁忙等 503 场景会保留并展示后端可读错误文案，避免生产环境只显示泛化的“服务器内部错误”或“服务器拒绝 (503)”。
  - `ConcurrencySemaphore` 新增排队回调元数据（队列位置、前方请求数、队列上限、超时时间等），并补充单测覆盖队列位置回传。
- **默认并发保护收紧**：
  - 全局 AI 并发默认值从 `5` 调整为 `1`，`.env` / `.env.example` / README 示例统一为 `MAX_CONCURRENT_AI_REQUESTS=1`。
  - 模型端点默认并发从 `2` 调整为 `1`，新增 `MODEL_ENDPOINT_DEFAULT_CONCURRENCY=1` 配置说明；模型配置中 `max_concurrent=0` 继续表示“跟随系统默认”。
- **登录与安全增强**：
  - 登录及注册页面新增“眼睛”图标，支持密码明文切换显示。
  - 优化了 `handleUnauthorized` 逻辑，仅清除认证 token 而不再清空全部 `localStorage`，保留用户主题等偏好设置。
  - 登录态切换时会同步刷新用户信息、管理员入口、上下文用量和模型选择器，减少登录后模型列表未初始化或管理员控件未显现的问题。
- **日期时间显示增强**：
  - **消息时间戳**：聊天对话中的每条消息现在都会显示发送/接收时间。AI 回复的时间戳与性能统计（Token/TPS）优雅整合在页脚显示。
  - **智能格式化**：引入了符合中文习惯的智能日期格式化（如“今天 10:45”、“昨天”、“5月8日”等），并统一使用 `Asia/Shanghai` 时区。
- **会话列表分组**：
  - 侧边栏会话列表新增按日期分组显示（如“今天”、“昨天”、“本周”等），提升了长列表场景下的会话检索效率。
  - 会话项悬浮提示（Tooltip）现在会显示详细的更新时间及消息条数，信息展示更丰富。
- **管理面板交互升级**：
  - **API Key 弹窗优化**：扩展了密钥生成弹窗宽度至 `550px`，并为长密钥增加了水平滚动支持和更明显的复制按钮。
  - **视觉细节修复**：将实验室功能的 `Experimental` 标签汉化为“实验性”，并优化了监控页部分样式的对齐。
  - **模型选择器稳定性**：统一模型下拉刷新逻辑，选项说明改为中文，并在无可用模型、默认模型恢复、管理员过滤等场景下提供更稳定的状态展示。
- **后端健壮性提升**：
  - 改进了转发 OpenAI 请求时的 URL 拼接逻辑，自动处理路径末尾斜杠及重复的 `/v1` 片段。
  - 为转发请求添加了标准的 `User-Agent` 及更完善的 Header 校验。

## [0.0.14] - 2026-05-07

### 版本与缓存机制优化
- 将应用版本号来源统一为 `CHANGELOG.md` 顶部最新版本标题，后端启动时自动读取并注入前端页面，后续发版优先只维护更新日志版本号。
- 聊天页静态资源版本参数改为 `__APP_VERSION__` 模板，由服务端按当前版本替换，避免 `chat.html`、`config.js`、`sw.js` 多处手工同步。
- Service Worker 注册地址追加当前版本参数，并让 `/`、`/chat/chat.html`、`/sw.js` 返回禁用缓存响应头，降低局域网生产环境被 PWA 或浏览器旧缓存卡住的概率。
- 同步记录本轮审计报表、系统监控页长名称展示与标签靠左对齐等界面调整，确保 0.0.14 打包后可见变更完整落档。
- **环境依赖升级**：将 Docker 基础镜像升级至 Node.js 22 LTS，并明确 `package.json` 中的 `engines` 要求，以解决 ESLint 10 依赖包 Espree 的引擎版本警告。

## [0.0.13] - 2026-05-07

### 优化与增强
- **审计报表可视化深度优化**：
  - **动态标签宽度计算**：引入 `ctx.measureText` 动态计算最长部门/用户名，并自动调整图表左侧边距（140px - 180px 动态适配），彻底解决长名称遮挡或空间浪费问题。
  - **对齐与布局调整**：标签改为右对齐并紧贴进度条，配合 `fillText` 的最大宽度限制，确保在极端长名称下依然保持布局整齐。
  - **渲染性能提升**：移除了硬编码的字符串截断逻辑，改为更优雅的 Canvas 渲染约束。
- **系统监控面板改进**：
  - **模型名称全显**：移除了“今日模型 Token”区域的固定宽度限制，支持长名称自动适配，并增加了 Tooltip 悬浮提示。
  - **GPU 列表序号显示**：为显卡列表添加了 `#0`, `#1`... 序号，极大方便了多显卡服务器的监控识别。
  - **布局灵活性增强**：放开了 GPU 名称的宽度束缚，适配复杂的显卡型号与序号并排显示的场景。
- **版本维护**：
  - 全站静态资源引用版本号统一升级至 `0.0.13`，强制浏览器刷新缓存。
  - 同步更新 `package.json`、`README` 及 `sw.js` 缓存版本。

## [0.0.12] - 2026-05-07
### 系统监控深度优化、审计报表增强与稳定性维护

本次更新对系统监控面板和审计报表进行了深度抛光，显著提升了数据的可读性与界面的美观度，并针对日志噪音和稳定性进行了多项细节修复。

#### 1. 系统监控页面深度优化 (Ops & Monitoring)
- **视觉对齐与布局重构**：
  - **标题统一样式**：修复了“系统监控”标题与其他页面不一致的问题，统一采用 1.5rem 标准字号与边距。
  - **头部紧凑化**：将“最近刷新时间”移至右侧操作栏，与“自动刷新”复选框并排显示，释放纵向空间。
  - **保护状态整合**：将 GPU 与并发保护中的“保护状态”和“拒绝阈值”合并至同一行展示。
- **监控卡片视觉增强**：
  - 为所有监控指标卡片添加了 SVG 矢量图标，并引入平滑的 hover 动效与阴影过渡。
  - 系统内存使用率引入动态进度条，支持根据负载自动切换颜色，视觉反馈更直观。
- **全站接口中文化 (Route Mapping)**：
  - 极大扩充了 `ROUTE_NAME_MAP`，覆盖了全站 API 及静态脚本路径。
  - 增强了路径匹配逻辑，支持大小写不敏感匹配、自动过滤查询参数并剥离路径多余空格，彻底解决了 `/chat/config.js` 等接口名称映射不准的问题。
- **容器体验优化**：
  - 调高了“热点接口”容器的最大高度，确保在 10 条数据模式下不再出现垂直滚动条。

#### 2. 审计报表页面增强 (Analytics & Reports)
- **标题精简**：原“可视化审计报表”正式更名为“审计报表”，界面更干练。
- **图表展示优化**：
  - **排行榜左对齐**：将“用户消耗排行”与“部门消耗对比”柱状图的标签对齐方式由“右对齐”改为“左对齐”，并收窄左侧边距，解决了名称长度不一时导致的左侧巨大留白问题。
  - **响应式宽度修正**：优化了图表在容器内的宽度自适应计算逻辑，确保在不同分辨率下均能完美对齐。

#### 3. 稳定性与系统维护 (Stability & Maintenance)
- **日志降噪治理**：在 HTTP 日志中屏蔽了 Chrome DevTools 产生的 `.well-known/appspecific/com.chrome.devtools.json` 404 噪音日志，保持审计日志的纯净。
- **修复弃用警告**：移除了 `server/routes/admin-stats.js` 中已弃用的 `req.host` 调用，统一更新为 `req.hostname`。
- **管理面板调优**：将系统设置/管理面板的整体高度从 `90vh` 提升至 `94vh`，提供更广阔的运维视野。
- **版本强制刷新**：同步更新了 `sw.js` 缓存版本，确保客户端能及时感知版本更新并加载最新的 UI 逻辑。

#### 验证
- 已通过 `npm run check` 语法校验。
- 手动验证全站接口中文映射均已生效。



## [0.0.11] - 2026-05-07
### RAG 检索、安全加固、额度统计与可观测性升级

本轮更新围绕 RAG 存储检索、安全边界、OpenAI 兼容接口额度统计和运维可观测性做了一次集中优化，保持 SQLite 部署形态不变，同时为后续向量库或更成熟 embedding 索引层预留了更清晰的演进路径。

#### 1. RAG 存储、检索与中文召回增强
- **RAG 模块拆分**：将原本较大的 RAG 逻辑拆为 `server/rag.js` 路由门面、`server/services/rag-index.js` 检索/索引服务和 `server/services/rag-cache.js` 缓存服务，降低后续维护成本。
- **SQLite FTS5 候选召回**：为知识库分片增加 FTS5 索引、触发器和历史数据回填逻辑，RAG 检索先走 FTS 候选预过滤，再进行 embedding 相似度排序。
- **中文 ngram 检索预处理**：新增 `server/services/rag-tokenizer.js`，在应用层生成中文 1-3 gram 检索 token；知识库分片新增 `search_content` 字段，FTS 索引使用“原文 + 中文 ngram token”，改善中文短词、单字和词组召回。
- **旧数据平滑迁移**：启动迁移会自动为历史 `knowledge_chunks` 回填 `search_content`，并重建 RAG FTS 索引，无需手动重新上传知识库文档。
- **RAG 缓存与阈值配置**：补充 RAG 检索 TTL 缓存、候选数量和相似度阈值配置；实际运行以 `.env` 为准，`.env.example` 仅作为模板。

#### 2. OpenAI 兼容接口额度与统计修正
- **修复 `/v1` 绕过每日模型额度问题**：新增 `model_usage_events` 表记录 API Key / Cookie 方式调用 OpenAI 兼容接口产生的 token 用量。
- **统一额度口径**：`getModelDailyUsage()` 现在合并统计网页聊天消息和 `/v1/chat/completions` 调用，模型 `daily_token_limit` 对第三方接入同样生效。
- **统一管理统计口径**：后台 usage、trend、report、details/export、ops-summary 与 Prometheus token 指标均合并 `messages` 和 `model_usage_events`，避免“限额已计入但看板漏算”的割裂。

#### 3. 安全边界与输入防护
- **附件 token 绑定完整资源**：附件访问 token 校验改为同时绑定 `user_id`、`session_id`、`file_path` 和过期时间，防止同一用户任意有效附件 token 被用于访问其他可猜路径附件。
- **文档 ZIP 解压上限**：DOCX/XLSX 内部 ZIP 解析增加 entry 数量、单 entry 解压大小、总解压大小和 `maxOutputLength` 限制，降低压缩炸弹导致的 CPU / 内存风险。
- **前端动态 HTML 转义补强**：管理端模型选项、附件预览、附件链接和统计卡片等动态内容增加 HTML / 属性转义，减少异常数据进入 UI 后的注入风险。

#### 4. 可观测性与测试
- **RAG 指标补齐**：新增 RAG 检索次数、缓存命中、候选数量、命中数量、Top Score、检索耗时和入库耗时等 Prometheus 指标。
- **测试覆盖扩展**：新增模型用量事件计入每日额度、ZIP 解压上限、RAG 中文 ngram FTS 命中等安全/检索测试。

#### 验证
- 已通过 `npm test`
- 已通过 `npm run lint`

## [0.0.10] - 2026-05-07
### 认证安全、流式解析与检索性能补齐

本次更新集中整理了最近一轮功能修复与工程化优化，主要覆盖认证链路、SSE 解析、RAG 检索和基础测试体系。

#### 1. 认证与会话安全
- 新增 CSRF token 机制，登录、刷新、`/auth/me` 与登出流程均完成同步处理。
- 前端鉴权请求统一携带 `X-CSRF-Token`，不再仅依赖本地 token 状态。
- 登录页改为标准表单提交，修复密码输入框不在 form 中的浏览器告警。

#### 2. 流式输出与检索性能
- 将聊天与 OpenAI 兼容接口的 SSE 解析统一到公共模块，减少重复逻辑与断流风险。
- RAG 检索加入候选预过滤与缓存控制，降低高频查询的重复计算开销。
- 补充安全的 FTS 查询构造与附件路径解析，减少异常输入带来的边界问题。

#### 3. 配置、脚本与测试
- 新增 ESLint flat config，并将 lint/test/check 纳入日常校验流程。
- 补充安全相关单测，覆盖 CSRF、SSE、FTS 与附件路径校验等核心逻辑。
- 增加审计日志保留周期、RAG 缓存参数等环境变量说明，并同步示例配置。

#### 4. 其他修正
- PWA 缓存版本更新。
- 模型地址与附件处理继续加强校验，降低非法 URL 与越权路径风险。

#### 验证
- 已通过 `npm run lint`
- 已通过 `npm test`
- 已通过 `npm run check`

#### 5. 上下文软归档、前端轻量化与交互修正
- 会话压缩改为“软归档 + 摘要”模式，旧消息不再物理删除，而是标记为 `context_archived` 并保留压缩时间。
- 聊天页右上角新增上下文用量胶囊，按活跃 token / 阈值显示百分比，并用颜色提示当前压力。
- `GET /sessions/:id` 现在会返回 `contextMeta`，前端切换会话时同步刷新上下文状态。
- 管理员面板相关脚本改为按需加载，降低 `chat.html` 初始加载体积。
- 认证页事件绑定增加空节点保护，避免旧缓存或局部加载导致 `addEventListener` 空指针。
- 聊天页资源版本和 Service Worker 缓存名统一提升到 `0.0.10`，并让导航页优先走网络获取最新内容。
- 顶栏模型选择器做了垂直对齐修正，避免下拉框显得偏上。

## [0.0.9] - 2026-05-07
### 架构性能、全文搜索、PWA 离线化与全栈安全性深度优化

本次更新是对系统的一次全面升级，不仅增强了管理员的监控能力，还通过底层架构重构显著提升了大规模数据场景下的响应速度与安全性。

#### 1. 运维与监控增强 (Ops & Monitoring)
- **多维度审计日志筛选**：新增用户名、操作类型、关键字及日期范围的组合过滤，支持条件同步导出 CSV。
- **监控精准化识别**：重构主机识别算法，支持 FQDN、Docker 内部主机及 IPv6 的自动探测，确保本地/远端负载判断准确。
- **热点接口治理**：全量映射系统 API 名称，消除监控面板中的“未知接口”提示。

#### 2. 核心架构与性能调优 (Architecture & Performance)
- **异步记忆压缩**：将长会话摘要处理改为异步非阻塞模式，彻底解决响应卡顿问题。
- **数据库自动维护**：开启每日 `PRAGMA optimize` 任务及增量空间回收（Incremental Vacuum）。
- **前端渲染效能**：引入 `content-visibility: auto` 与 GPU 加速，大幅优化超长消息列表的滚动性能。

#### 3. 全文搜索系统集成 (Full-Text Search)
- **FTS5 引擎**：基于 SQLite FTS5 实现消息内容秒级全文检索.
- **实时索引同步**：配置数据库触发器，确保消息变更自动实时同步至搜索表。
- **搜索 API**：新增 `/api/sessions/search/content` 接口，支持上下文片段提取。

#### 4. 离线化与 PWA 增强 (Offline & PWA)
- **桌面应用模式**：新增 `manifest.json` 与 `sw.js`，支持将 Pivot 作为独立应用安装至桌面或移动端。
- **智能资源预缓存**：核心静态资源与第三方 vendor 库自动离线缓存，实现秒开体验。

#### 5. 全栈安全性加固 (Security & Compliance)
- **精细化 CSP 策略**：强化 Helmet 配置，严防 XSS 攻击的同时兼容 PWA 与内联事件。
- **附件令牌过期机制**：为所有附件引入 7 天自动过期机制，防止链接滥用。
- **生产日志脱敏**：生产环境下自动切换为高性能 JSON 模式，并对 API Key、密码等敏感信息进行脱敏处理。

#### 6. 深度系统优化与代码审计 (System Deep Optimization)
- **核心架构重构**：统一抽取 HTTP 常用工具函数（分页、IP 获取等）至 `server/http.js`，提升全站代码复用性与一致性。
- **RAG 性能飞跃**：引入 RAG 检索结果缓存机制（TTL 缓存），大幅减少高频重复提问时的向量计算与匹配损耗。
- **稳定性加固**：增加 RAG 向量解析的安全容错处理，防止数据库异常数据或损坏的分片导致对话服务崩溃。
- **更精细的限流策略**：对话接口限流由 IP 维度升级为用户维度，更公平地分配系统算力资源并防止恶意刷取。
- **后台维护服务**：新增系统后台维护任务，自动定期（每 24h）清理陈旧审计日志，保持数据库轻量化运行。

#### 验证
- 已执行 `npm run check`，全站 JavaScript 语法与逻辑链路验证通过。

## [0.0.8] - 2026-05-05
### 多模态附件读取、图片安全保护、远端模型控制与登录体验优化

本次更新围绕聊天会话中的文档/图片上传、多模态模型调用、远端模型部署保护以及登录态体验进行了系统性优化。重点解决了 PDF 与 Office 文档可上传但大模型无法可靠读取、图片过大可能造成内存风险、局域网多模态模型仅支持单图解析、远端模型无法用本机 GPU 指标判断负载，以及 30 分钟登录态过短的问题。

#### 附件上传与文档解析 (Attachment & Document Parsing)
- **扩展文档解析能力**：
  - 新增统一文档抽取模块，支持 `pdf`、`docx`、`doc`、`xlsx`、`xls`、`txt`、`md`、`csv` 等常见格式。
  - 接入 `word-extractor` 与 `xlsx`，补齐旧二进制 `.doc/.xls` 的读取能力，改善“上传成功但模型读不到内容”的问题。
  - 对加密 PDF/Office 文件增加密码处理入口；无法解密或需要密码时给出明确错误提示。
- **扫描 PDF 多模态兜底**：
  - 当 PDF 无法抽取文本时，自动尝试将页面渲染为图片交给视觉模型解析。
  - 针对当前局域网多模态模型一次只支持一张图片的限制，扫描 PDF 默认只取 1 页进入视觉解析，避免一次请求携带多图失败。
- **上传体验增强**：
  - 聊天上传入口支持多选文件。
  - 上传图片和文件预览区限制最多保留 5 个附件，超出时阻止继续追加并提示用户。

#### 图片安全与多模态限制 (Image Safety & Vision Limits)
- **图片 OOM 防护**：
  - 新增图片安全处理模块，限制上传体积、输入像素、上下文图片大小和模型请求中的图片数量。
  - 上传图片会统一经过 `sharp` 解码、缩放、压缩和格式归一化，防止超大图、异常动图或高像素图片导致内存压力。
  - 禁止前端和后端宽泛接受任意 `image/*`，收紧为常见安全图片格式并移除 SVG 上传入口。
- **单图解析约束**：
  - 后端在聊天历史和当前请求层面强制限制每次模型请求最多携带 1 张图片。
  - 多余图片会被替换为文本说明，避免远端模型因多图输入而报错。
  - 前端上传、预览和发送区域增加“当前模型每次仅解析 1 张图片”的提示。

#### 远端模型监控与并发控制 (Remote Model Runtime Protection)
- **区分本机与远端模型负载**：
  - 系统监控页现在会识别模型 Endpoint 是本机还是远端，并明确提示本机 GPU 指标只代表 Pivot 所在服务器。
  - 保留本机 `nvidia-smi` GPU 动态保护作为本机总闸，同时不再把它误认为远端模型服务器负载。
- **新增端点级运行时保护**：
  - 新增模型端点运行时服务，按 Endpoint 聚合请求，支持端点级并发限制、排队、连续失败统计和熔断保护。
  - `/api/chat` 与 OpenAI 兼容的 `/v1/chat/completions` 均接入端点级保护。
  - 连续失败达到阈值后自动短暂熔断，并清理排队请求，避免远端模型服务被持续压垮。
- **模型配置扩展**：
  - 模型配置新增“端点并发上限”和“远端健康检查 URL”。
  - 支持为局域网模型设置并发为 `1`，适配一次只能可靠处理一张图片或显存较紧张的远端服务。
- **监控页增强**：
  - 系统监控新增“模型端点状态”，展示端点 host、健康探针状态、并发、排队、失败次数与熔断倒计时。
  - 修复 `describeEndpointMonitor is not defined` 前端运行时错误。

#### 登录态与配置同步 (Auth & Configuration)
- **登录有效期优化**：
  - Access Token 默认有效期由 30 分钟调整为 480 分钟。
  - Refresh Token 默认有效期由 7 天调整为 30 天。
  - 新增 `.env` 与 `.env.example` 配置项：`ACCESS_TOKEN_EXPIRES_MINUTES`、`REFRESH_TOKEN_EXPIRES_DAYS`，并确保实际运行配置与示例文件一致。
- **前端续期逻辑整理**：
  - 抽取公共 `refreshAccessToken` 逻辑，减少重复实现，为后续统一所有鉴权请求的自动续期打基础。

#### 验证
- 已执行 `npm run check`，项目 JavaScript 语法检查通过。

## [0.0.7] - 2026-05-05
### 实验室卡片化、管理权限加固与 API 用量统计

本次更新重点引入了实验室功能的全新视觉呈现，对系统核心管理员权限进行了全方位的硬性加固，并上线了第三方接入的 API Key 消费统计系统，使资源消耗透明化。

#### UI 深度优化与布局重构 (UI/UX Optimization)
- **实验室功能视觉重构**：
  - 将实验室功能由单调的列表升级为现代化的**卡片式栅格布局**，引入了玻璃拟态背景与平滑 hover 动效。
  - 替换了 RAG 知识库功能的图标，由 Emoji 升级为更具科技感的矢量 SVG 图标，并增加了 `Experimental` 状态标签。
- **附件库与模型配置布局抛光**：
  - 深度调优了附件库表格列宽，收紧文件名并放宽上传时间，解决了日期截断问题。
  - 优化了模型管理界面的操作列宽度，使四个功能按钮排版舒展，不再拥挤。
- **监控页面细节对齐**：
  - 修复了 GPU 与并发保护区域文字换行时的对齐问题，现在所有警告与提示信息均保持一致的左对齐。

#### 安全加固与权限保护 (Security & Protection)
- **内置管理员硬性保护**：
  - **后端硬拦截**：禁止删除内置 `admin` 账号，禁止降低其权限或禁用其状态。
  - **前端交互约束**：自动隐藏 `admin` 的删除按钮，并在编辑时锁定角色与状态字段，防止误操作。
  - **系统可用性兜底**：强制要求系统必须保留至少一个活跃管理员账号。

#### API 用量统计与透明化 (New Feature)
- **API Key 消费统计**：
  - 在数据库底层实现了 Token 消耗的实时累加，支持 OpenAI 协议下的流式与非流式调用计数。
- **额度透明化展示**：
  - 在「API 接入」管理界面新增「已用额度」列，支持千分位格式化显示各密钥的 Token 消费量。

## [0.0.6] - 2026-05-04
### 架构解耦、渲染升级与高频操作增强

本次更新对前端架构进行了大规模的模块化重构，大幅增强了 Markdown 渲染引擎的容错性与数学公式支持，并听取用户反馈引入了单点会话导出、消息重新生成等高频实用功能。

本版本引入了强大的第三方 API 接入网关，支持 OpenAI 标准协议，并实现了 API Key 的自主管理。同时深度优化了模型配置流程，支持一键探测远程模型列表，并对系统监控面板和配置体系进行了全方位的布局优化与标准化，显著提升了管理效率与交互体验。

#### 系统监控与配置标准化 (Optimization)
- **监控布局深度优化**：
  - **紧凑型卡片设计**：调小了监控指标卡片的内边距与间距，并优化了最小宽度，确保 8 大核心指标在主流分辨率下均可单行对齐。
  - **标题视觉校准**：将“系统监控”标题明确设置为左对齐，修复了之前布局中可能存在的视觉重心偏移。
  - **纵向空间释放**：大幅压缩了 GPU 保护、资源列表及模型统计行的上下间距，并为监控面板增加了独立的滚动条支持，确保底部的「热点接口」表格始终可查。
- **配置体系规范化**：
  - **全量同步**：深度对齐了 `.env` 与 `.env.example` 文件，补全了 `LOG_LEVEL`、`PUBLIC_URL` 等关键隐藏配置。
  - **全方位文档化**：为配置文件中所有变量添加了详尽的中文注释，涵盖参数作用、单位及推荐值，降低了运维门槛。
- **GPU 监控中文化**：
  - 针对 Windows 环境或容器透传异常导致的 `nvidia-smi` 报错进行了拦截与翻译，现在会提供清晰的中文排查建议。

#### 第三方 API 接入与智能管理 (New)
- **OpenAI 标准协议支持**：
  - 全面支持 `/v1/chat/completions` 和 `/v1/models` 接口。
  - 允许第三方应用（如沉浸式翻译、NextChat、ChatBox 等）无缝接入 Pivot 系统作为模型网关。
- **API Key 自主管理系统**：
  - 新增独立的「API 接入」管理频道，支持用户自主生成、管理 `sk-` 格式密钥。
  - **安全弹窗交互**：重构了密钥创建流程，采用高层级（z-index: 5000）自定义弹窗，支持一键复制并强制安全提醒。
  - **智能地址指南**：接入指南支持根据当前访问环境（内网/公网）智能动态生成 Base URL。
- **智能模型管理升级**：
  - **自动模型探测**：在添加/编辑模型时增加「一键抓取」按钮，可自动探测并获取上游服务支持的所有模型标识。
  - **掩码状态兼容**：修复了在 API Key 为掩码状态下无法进行连接测试或模型探测的问题。
- **UI/UX 深度抛光**：
  - **紧凑型侧边栏**：调小了管理面板菜单项的上下间距，视觉更干练。
  - **宽屏管理容器**：将 API Key 管理容器宽度扩展至 1350px。
  - **SQL 语法健壮性**：彻底修复了因 SQLite 字符串引用语法导致的 `500` 认证错误。

- **前端模块化解耦**：将原本庞大的 `app.js` 彻底拆分为 10 多个专注单一领域的子模块（涵盖 UI渲染、对话引擎、会话侧边栏、模型管理、用户管理、统计图表等），极大提升了代码的可读性与长期可维护性。
- **渲染器防御性降级**：彻底摒弃了易受底层库升级破坏的 AST Token 自定义渲染方案，全面转为“原生渲染 + 正则后处理包裹”的防御性渲染策略。

#### Markdown 与显示增强
- **原生 KaTeX 数学公式支持**：实现了完全脱机的本地化数学公式渲染，通过与代码块语法的严密隔离，解决了公式错乱与解析失效的痛点。
- **复杂表格护城河**：完美修复了 Markdown 表格渲染出现 `[object Object]` 错误和右侧多出空白列的问题；现已支持超长表格的横向滚动交互。
- **视觉排版抛光**：
  - 增强了侧边栏顶部品牌 Logo 与系统名称的尺寸与间距，提升视觉冲击力。
  - 增加 H1-H6 标题的个性化排版样式，修复了用户气泡框内代码和链接的对比度问题。
  - 调整了【审计日志】等后台管理界面的表格列宽，防止大数字换行错乱。

#### 新增高阶功能
- **单会话全量导出**：在侧边栏会话菜单中新增「导出为 Markdown」，支持将长篇对话一键打包至本地 `.md` 文件，完美保留格式与时间戳。
- **精准重新回答**：为 AI 回复卡片新增了「重新回答」操作，一键回退当前错误输出并基于最新的上文精准重建流式响应。
- **可视化审计报表**：在系统设置中新增「审计报表」模块，支持按部门、用户及时间跨度（7天/30天/90天/1年）动态生成 Token 消耗趋势、Top 10 用户排行及部门用量分布图。
- **OpenAI 标准接口兼容**：新增 API Key 管理系统，支持通过标准的 `/v1/chat/completions` 接口供第三方客户端（如沉浸式翻译、NextChat）接入使用。
- **高级模型参数开放**：
  - 在底层数据库平滑新增了 `temperature` 和 `max_tokens` 字段。
  - 允许管理员在【模型配置】界面为每个大模型接口绑定专属的温度（发散度）与输出长度限制，实现更专业的大模型驾驭能力。

#### 后端健壮性提升
- **日志降噪治理**：精简了终端日志输出，屏蔽了前端高频轮询与静态资源加载带来的冗余请求日志，仅保留核心中文业务日志。
- **总结接口超时免疫**：将会话总结（Title Generation）的网络超时阈值由 10 秒延长至 30 秒，彻底解决了并发状态下偶发的“会话标题生成失败”报错。

## [0.0.5] - 2026-05-04
### 思考过程 UI 深度优化与流式渲染交互增强

本次更新极大提升了模型思考过程的视觉美感与交互流畅度，并解决了流式输出时的交互闪烁问题。

#### 思考过程 (Thinking Process) UI 优化
- **CSS Grid 平滑动画**：重构了展开/收起逻辑，采用 CSS Grid 替代传统的 `max-height` 方案，彻底解决了动画截断与速度不一致的问题，过渡极其流畅。
- **视觉风格升级**：采用紫色调渐变背景（Purple/Violet），增加了“灯泡”发光图标与圆形的“›”旋转指示器，视觉层次更显专业。
- **呼吸脉冲特效**：为“正在思考”状态增加了边框呼吸灯与阴影脉冲效果，使界面更具动感。
- **闪跳行为修复**：修复了在收起窗口时内容先瞬间展开再收起的逻辑 Bug。

#### 流式渲染与交互增强
- **增量 DOM 更新策略**：优化了流式渲染机制。在模型思考阶段，系统现在仅对思考区域进行增量文本追加，不再进行全量 `innerHTML` 重绘。
- **无缝交互体验**：得益于增量更新，用户现在可以在模型持续输出思考内容的同时，自由地点击展开或折叠窗口，动画不会被打断。
- **智能自动滚动**：当思考块处于展开状态时，新产生的内容会自动驱动内部容器滚动到底部。

#### UI 细节抛光
- **停止按钮改进**：调大了“停止生成”按钮中间的红色方框，使其比例更协调，视觉识别度更高。
- **布局居中增强**：通过 Flexbox 确保了停止按钮图标在各种缩放下的完美居中。

## [0.0.4] - 2026-05-04
### 交互体验与模型管理功能增强

本次更新专注于提升模型管理的安全性、前端渲染的美观度以及 Token 统计的精确性。

#### 模型管理
- **明文密钥查看**：在模型配置弹窗中增加了“眼睛”图标，支持管理员在必要时安全地查看已配置的 API Key 明文。
- **配置测试修复**：修复了模型编辑弹窗中“测试连接”失效的问题。现在系统能够智能处理掩码状态下的 Key，并支持在保存前即时验证修改后的接口地址。
- **管理辅助**：提供了一键重置管理员密码的辅助脚本，解决了因 `.env` 配置变更导致的初始化同步问题。

#### 渲染与交互
- **代码高亮集成**：引入了 `highlight.js` 库，为对话中的代码块增加了多语言语法高亮（GitHub Dark 主题），显著提升了技术内容的阅读体验。
- **流式稳定性优化**：
  - 优化了 Gzip 压缩过滤器，解决了部分环境下因缓冲导致的非流式回复问题。
  - 增加了 `X-Content-Type-Options: nosniff` 响应头，确保消息能够实时、逐字呈现。

#### 性能与统计
- **Token 精确计算**：
  - **官方数据优先**：系统现在会自动捕获并记录大模型厂商 API 返回的官方 `usage` 统计，确保计费与统计的绝对一致。
  - **改进版估算算法**：对于不返回使用量的模型，升级为区分中英文权重的本地估算算法（中文 2.0/字，英文 0.5/字符），计算精度大幅提升。
- **实时指标优化**：前端 TPS（每秒 Token 数）显示现在同步采用加权算法，能够更专业地反映模型的真实推理速率。

## [0.0.3] - 2026-05-04
### 系统稳定性与品牌一致性优化

本次更新重点聚焦于系统的生产级稳定性增强、文件处理性能优化以及全站品牌视觉的标准化。

#### 性能与稳定性优化
- **非阻塞 I/O 重构**：将附件目录体积统计由同步改为异步执行，防止大规模附件库导致的事件轮询阻塞。
- **全局异常守护**：在后端引入 `uncaughtException` 和 `unhandledRejection` 进程监听，彻底解决非预期错误导致的服务僵死问题。
- **稳健错误处理**：增加全局 Express 错误拦截中间件，所有系统 500 异常现在均以标准的 JSON 格式返回，提升前端容错性。
- **数据库优化**：配置 SQLite 增量真空（Incremental Vacuum）和 5000ms 忙等待超时，提升高并发下的写入稳定性。

#### 品牌与视觉提升
- **Favicon 修复**：更新浏览器标签图标为白色背景的六边形 Logo，确保在不同浏览器主题（深色/浅色）下的视觉一致性。
- **代码资产审计**：
  - 修复了核心代码头部的中文字符编码乱码问题。
  - 标准化了所有后端与前端文件的中英文双语表头。
- **自动灾备方案**: 新增全量快照备份脚本，支持带时间戳的数据库永久留存，增强离线抗风险能力。

## [0.0.2] - 2026-05-04
### 管理增强与效率套件升级

本次更新将 Pivot AI 正式升级为全栈管理平台，引入了深度运营监控、指令中心以及多项 UI/UX 优化。

#### 新增功能
- **效率工具集 (Efficiency Suite)**：
  - **指令中心 (Prompt Center)**：内置常用指令模板，支持 AI 角色（System Prompt）一键切换。
  - **全局会话搜索**：集成带防抖逻辑的实时搜索，支持按标题、标签快速过滤。
  - **会话管理进阶**：支持对话置顶（Pin）、归档（Archive）及多标签管理。
- **运营概览面板**：
  - 单行展示 8 大核心指标（用户、消息、Token 消耗等），数据一目了然。
  - 集成近 30 天 Token 使用趋势图。
- **管理与审计**：
  - **用量统计**：支持查看各用户的最后活动时间、总 Token 消耗及消息量。
  - **审计日志**：完整记录系统内的敏感操作（登录、删除、配置变更），支持 IP 地址追踪。
- **模型监控与权限**：
  - **连通性测试**：支持实时探测模型 API 状态，显示延迟（ms），并具备并发保护。
  - **额度限制**：支持设置模型每日 Token 消耗上限，并可按部门分配使用权限。

#### 优化与修复
- **UI/UX 体验**：
  - 系统设置弹窗宽度增加至 `1450px`，优化了所有表格的列宽分。
  - 移除了所有浏览器原生 `confirm`，统一采用系统自定义确认弹窗。
  - 修复了弹窗层级冲突（Z-Index），确保二级确认弹窗始终在最上层。
  - 全面汉化：版权信息、侧边栏标题及操作提示等均已本地化。
- **安全性与健壮性**：
  - 修复了模型测试接口在异常时返回 500 的问题，增强了错误日志的诊断信息。
  - 实现了数据库字段自动迁移逻辑，支持平滑升级。
  - 侧边栏支持折叠，优化了系统标题的视觉对比度。


## [0.0.1] - 2026-04-29
### 初始版本发布 (Initial Release)

本项目第一个正式稳定版本，核心聚焦于**私有化内网环境下的 Pivot AI 全栈安全性、品牌形象统一以及极致的交互体验**。

### 新增功能
- **品牌视觉升级**: 部署了全新的翡翠绿风格 Logo（白色背景）；优化了浏览器 Favicon 在标签页的显示尺寸；移除了登录页冗余文案，保持界面干练。
- **性能监测系统**: 对话过程中实时显示推理耗时、生成 Token 长度以及每秒字数 (TPS)，便于性能对标。
- **资产确权体系**: 重构上传逻辑，实现附件按“用户/会话”路径物理隔离，并在数据库 `attachments` 表中建立强关联。
- **生成中断机制**: 支持一键停止模型回复，通过前端 `AbortController` 与后端连接销毁联动，实现秒级算力回收。
- **自动灾备方案**: 新增全量快照备份脚本，支持带时间戳的数据库永久留存，增强离线抗风险能力。

### 安全与稳定性加固
- **SQL 注入防御**: 全面重构 SQL 语句，采用参数化查询机制，消除注入风险。
- **XSS 攻击防护**: 引入 `DOMPurify` 库对所有消息和会话标题进行严格过滤。
- **资源泄露修复**: 针对 SSE 流式连接增加销毁监听，防止用户异常断开导致的内存溢出。
- **数据库引擎优化**: 开启 SQLite 的 WAL (预写日志) 模式及增量 Vacuum，提升高并发下的写入稳定性。
- **鉴权加固**: 强制使用 256 位随机环境变量密钥驱动 JWT 体系。

### 部署与运维
- **容器化适配**: 优化 `Dockerfile`，预装 `libc6-compat` 库以解决 Alpine 环境下的图片处理引擎兼容性。
- **持久化配置**: 完善 `docker-compose.yml`，对数据库、附件目录及备份路径进行全路径挂载。
- **国内源加速**: `Dockerfile` 基础镜像切换至国内阿里云镜像仓库，解决拉取缓慢问题。
