# 更新日志 (CHANGELOG)

## [v0.0.95] - 2026-06-09
### 智能体任务记录、DAG 详情与自由任务模板保存体验收口

本版本围绕智能体任务记录和自由任务模板保存路径做轻量体验收口：任务列表默认每页 15 条，工作流任务详情不再同时展示“工作流节点”和重复的“执行步骤”，并把“保存为模板”前置到快速任务主面板，减少用户在配置面板之间来回寻找入口。

#### 任务记录分页
- **每页 15 条记录**：智能体任务记录表格前端请求、后端路由默认 limit 和服务层兜底默认值统一改为 15，分页控件结构和统一样式保持不变。
- **翻页行为保持一致**：列表序号继续按当前页与服务端返回 limit 计算，筛选、搜索和空页回退逻辑不变。

#### DAG 任务详情
- **去除重复执行明细**：当任务存在 DAG 节点记录时，详情页只保留工作流节点作为执行明细，不再重复渲染通用执行步骤和工具统计区域。
- **节点结果可读化补齐**：DAG 节点卡片会直接展示结构化查询表格、图表摘要或可渲染输出，大模型节点继续优先展示 Markdown 正文，原始输入输出仍保留在折叠项中便于排查。

#### 自由任务模板
- **保存入口前置**：快速任务标题区新增“保存为模板”按钮，用户填写目标和参数后可直接保存，不必先打开“模板与计划”。
- **模板库入口保留**：模板与计划面板内保留“保存当前任务”入口，继续用于模板库管理场景；两个入口复用同一保存逻辑并避免重复绑定。
- **模板来源说明**：自由任务模板来自用户手动保存的当前目标、补充说明、上下文和模型参数；模板库继续按现有权限展示个人模板以及 admin 可见范围内的共享模板。

#### 文档、版本与验证
- **版本号启用**：应用版本升级至 `v0.0.95`，同步更新 `package.json`、`package-lock.json`、README、项目汇报和更新日志。
- **回归验证通过**：`node tests\security-agent.test.js`（34/34）和 `npm run check` 已通过；本地页面 DOM 验证确认快速任务头部已出现“保存为模板”入口。

## [v0.0.94] - 2026-06-09
### 能力库工具级治理、数据库安全与配置诊断升级

本轮围绕能力库页面现有工具与配置完整性做企业级补强：保留系统能力、数据库、报表文件、IM 通知和外部能力服务的原有入口，同时把能力库从“能连接、能调用”升级为“可治理、可诊断、可审计地调用”，并明确能力可服务于聊天、自由任务和工作流。

#### 工具级治理
- **工具级启停**：能力包下的单个工具可独立启用或停用，列表展示和真实调用路径都会尊重治理配置，避免只能整包停用。
- **风险与审批配置**：工具动作弹窗新增风险等级、是否需要审批和适用说明，便于把高风险动作与日常动作区分治理。
- **能力调用链路收口**：聊天、自由任务和工作流通过同一套 MCP 工具过滤逻辑获取可用工具，已停用工具不会继续暴露给模型或智能体。

#### 数据库能力治理
- **表/字段白名单**：数据库连接新增允许访问表/集合、字段白名单配置；启用字段白名单后，SQL 必须使用明确字段名，降低误查敏感数据风险。
- **敏感字段脱敏**：查询、分组统计和 MongoDB 采样/聚合结果会按敏感字段配置进行返回前脱敏。
- **查询安全提示**：数据库结果新增治理状态与轻量成本估计，包含查询超时、限制行数、涉及表和行级权限提示，帮助用户判断查询边界。

#### 配置诊断
- **报表目录诊断**：报表文件能力新增配置诊断，可检查目录可读性并预览可读文件。
- **IM 测试发送**：IM 通知能力新增测试发送、最近投递状态和失败重试提示，方便上线前确认目标白名单和 Webhook 配置。
- **外部能力健康检查**：外部能力服务新增健康检查 URL、调用超时、认证模式、工具 Schema 校验和示例提示词配置。

#### 文档、版本与验证
- **版本号启用**：应用版本升级至 `v0.0.94`，同步更新 `package.json`、`package-lock.json`、README 和更新日志。
- **回归验证通过**：`npm run check`、`npm run lint` 和 `node tests\security-mcp.test.js`（21/21）已通过；按用户要求，本轮未启动浏览器验证。

## [v0.0.93] - 2026-06-09
### 自由任务定位与工作流孵化闭环

本轮围绕“普通智能体是否还有存在必要”的合理性分析做产品收口：保留普通入口，但将其明确定位为自由任务和快速任务，用于临时分析、排查、探索和目标参数复用；工作流编排则作为企业生产任务入口，承载编排、发布、版本、计划任务和审计复用。两者通过“从自由任务生成工作流草稿”连接起来，让临时任务可以自然沉淀为标准流程。

#### 自由任务与工作流定位
- **普通入口改为自由任务**：前端工作台标题、说明、模板区、历史区和相关提示从“智能体”调整为“自由任务 / 快速任务”，避免与工作流编排抢定位。
- **工作流入口强化生产语义**：工作流编排页强调编排、发布、版本、计划运行和审计复用，适合企业稳定流程和生产任务。
- **企业部署默认建议**：运行前提示区区分自由任务与工作流任务；自由任务适合分析、排查和临时处理，稳定流程建议转成工作流草稿后发布运行。

#### 自由任务生成工作流草稿
- **新增后端草稿接口**：`POST /api/agents/runs/:id/workflow-draft` 会读取自由任务的工具执行步骤，生成顺序工具节点和最终大模型总结节点；工作流任务本身不再重复生成草稿。
- **新增前端详情动作**：自由任务详情新增“生成工作流草稿”按钮，成功后直接打开工作流编排页，并加载可检查、可保存、可发布的草稿画布。
- **审计与错误兜底**：生成草稿会写入审计日志；非自由任务、无效任务和异常节点会给出明确错误提示。

#### 模板、任务记录与验证
- **模板体系分层**：自由任务模板用于复用目标、参数、上下文和模型路由；工作流模板用于复用步骤、节点和依赖结构。
- **统一记录分层展示**：后台继续保留统一任务记录，前端新增“全部类型 / 自由任务 / 工作流任务”筛选，并在表格中展示任务类型。
- **回归验证通过**：`npm run check`、`npm run lint`、`node tests\security-agent.test.js`（32/32）、`node tests\security-rag.test.js`（51/51）、`node tests\security-mcp.test.js`（19/19）和 `node tests\security.test.js`（173/173）已通过；按用户要求，本轮未再启动浏览器验证。

## [v0.0.92] - 2026-06-09
### 知识图谱质量治理、待确认关系与 Graph-RAG 查询体验升级

本轮围绕知识图谱做完整升级：把低可信抽取关系从“直接进入可用图谱”改为“待确认后参与正式召回”，并补齐图谱质量评分、重复实体建议、关系状态筛选、来源片段查看、图谱查询调试和智能体工具调用，让知识库中的关系型知识更可审计、更可修正，也更容易被普通用户和智能体可靠使用。

#### 后端图谱治理
- **关系状态治理**：`knowledge_relations` 新增 `status` 字段，低于可信度阈值的共现关系自动进入 `pending`，确认后提升为 `active`，删除关系继续使用 `deleted` 软状态保留审计边界。
- **图谱质量评分**：图谱概览新增 `qualityScore`、等级、孤立实体、低可信关系、缺来源关系、待确认关系和重复实体组统计，并返回治理建议。
- **重复实体建议**：基于实体类型、规范化名称前缀和别名推断疑似重复实体组，为后续合并校准提供候选。
- **Graph-RAG 查询接口**：新增 `/rag/graph/query`，按问题返回相关实体、关系路径、来源文档、分块 ID 和回答提示；正式聊天上下文仍只使用 `active` 关系，避免未确认关系污染回答。

#### 前端图谱工作台
- **筛选与审核闭环**：知识图谱工作台新增实体质量筛选、关系状态筛选、关系类型筛选和最低可信度过滤；pending 关系可在关系卡片中直接确认。
- **来源证据可见**：关系卡片展示关系状态、可信度、来源文档和来源分块片段，方便用户判断关系是否应保留、编辑或删除。
- **图谱查询调试**：工作台新增图谱查询输入框，可直接查看命中的实体、关系路径、来源和回答提示，便于排查 Graph-RAG 召回。
- **移动端适配**：新增筛选栏、查询面板和来源片段的响应式样式，避免全屏图谱在窄屏下拥挤或重叠。

#### 智能体与测试
- **智能体图谱工具**：内置智能体工具新增 `knowledge.graph.query`，智能体可在当前用户权限内查询知识图谱关系路径，用于回答职责、依赖、归属和影响类问题。
- **回归测试补齐**：新增低可信关系 pending、确认转 active、质量摘要、关系筛选、图谱查询和重复实体建议的 RAG 回归用例；`node scripts\check_syntax.js` 与 `node tests\security-rag.test.js`（51/51）已通过。
- **版本号启用**：应用版本升级至 `v0.0.92`，同步更新 `package.json`、`package-lock.json`、README、使用手册和项目汇报中的当前版本说明。

## [v0.0.91] - 2026-06-09
### 知识库、能力库与智能体治理体验增强

本轮围绕知识库、能力库和智能体的实际使用闭环继续升级：在不额外探测外部服务、不放宽权限边界的前提下，为知识库增加更稳的召回缓存版本域和质量信号，为能力库增加健康评分与治理建议，为智能体运行前预检增加可读的就绪度、上下文预算和 MCP 风险提示，让用户在运行前就能看懂“哪里可用、哪里需要处理”。

#### 知识库优化

- **召回缓存防陈旧**：RAG 召回缓存键新增知识库版本域，纳入就绪启用文档数、分块数、文档更新时间、知识图谱更新时间和检索参数，文档、图谱或召回配置变化后不会继续命中过期结果。
- **质量报告信号扩展**：知识库质量报告新增质量分、就绪率、反馈有用率、陈旧就绪文档、知识图谱实体/关系覆盖和治理建议，便于区分“资料不足”“索引待刷新”“反馈效果偏低”等问题。
- **工作台体验增强**：知识库质量面板直接展示质量分、就绪率、反馈有用率、异常/停用/空分块文档和图谱实体数，减少用户在多个区域之间来回排查。

#### 能力库优化

- **能力健康评分**：能力库治理接口新增健康评分、健康等级、活跃率、调用失败率、异常服务和未检查服务统计，基于本地配置与调用日志计算，不主动扩大外部网络探测面。
- **治理建议可视化**：能力库工作台展示健康分、调用失败率和最多 3 条治理建议，帮助用户优先处理异常连接、未刷新工具缓存或失败率偏高的能力。
- **兼容既有治理数据**：新增健康对象同时保留原有 governance summary 字段，避免破坏已有前端和测试依赖。

#### 智能体优化

- **运行前就绪度**：智能体预检新增 readinessScore，综合模型、目标、工具、Token 预算、审批等待风险和 MCP 健康情况给出运行前可读评分。
- **上下文预算提醒**：预检会估算输入 Token，当任务目标、补充说明和上下文来源可能超过预算时提前告警，降低运行后才失败或回答被截断的概率。
- **MCP 风险提示**：预检摘要新增 MCP 服务总数、启用数、异常数、未检查数，并对异常或未检查能力给出警告，让智能体用户在点击运行前看到能力库风险。

#### 文档、版本与验证

- **版本号启用**：应用版本升级至 `v0.0.91`，同步更新 `package.json`、`package-lock.json`、README、使用手册和项目汇报中的当前版本说明。
- **验证覆盖**：新增知识库缓存版本域、知识库质量信号和智能体预检治理回归用例；`npm run check`、`npm run lint`、`node tests\security-rag.test.js`（50/50）、`node tests\security-mcp.test.js`（19/19）、`node tests\security-agent.test.js`（31/31）和 `node tests\security.test.js`（171/171）均已通过；本轮按用户要求未打开浏览器验证。

## [v0.0.90] - 2026-06-08
### 知识库命名回归、入口点击修复与 lint 清理补丁

本次补丁针对知识库前端术语、知识库工作台入口和工程 lint 噪音做集中收口：把近期过度口语化的按钮和面板名称恢复为原先更稳定的产品命名，修复“检索配置”和“召回测试”入口点击异常，并把历史遗留的 ESLint warning 清理到零，避免后续变更被旧提示淹没。

#### 知识库前端名称回归

- **恢复知识库核心入口命名**：将“试问一下”恢复为“召回测试”，“资料关系”恢复为“知识图谱”，“高级设置”恢复为“检索配置”，保持知识库页面、弹窗、按钮和测试断言里的名称一致。
- **恢复检索与索引相关术语**：将“内容段”恢复为“分块”，将匹配参数恢复为“阈值 / 命中 / 候选”，将“重新学习”等口语化表达收口为“重新索引”“批量重建”等原有知识库语义。
- **同步结果与空状态文案**：召回测试 loading、失败提示、文档列表空状态、知识图谱实体/关系统计和聊天流式状态文案同步回归，避免同一流程里新旧术语混用。

#### 知识库入口点击修复

- **修复“检索配置”入口时序问题**：知识库工作台打开时会先确保 `admin-settings.js` 已加载，再绑定检索配置弹窗事件，避免脚本尚未加载时按钮点击无响应。
- **修复“召回测试”相关绑定被替换的问题**：检索配置弹窗事件绑定不再使用 `cloneNode` 替换按钮，改为带标记的幂等 `addEventListener`，避免已有 DOM 状态或后续监听被意外丢失。
- **补齐入口回归断言**：安全聊天渲染测试新增对 `ensureAdminSettingsScript`、检索配置事件幂等绑定、召回测试/知识图谱/检索配置旧名称的断言，防止同类问题回退。

#### ESLint warning 治理

- **补齐前端拆分脚本全局声明**：在 `eslint.config.js` 中补齐 agent、DAG、MCP、sidebar、stats、engine、render 等前端拆分脚本共享的全局名，解决跨文件浏览器脚本被误判为 `no-undef` 的历史问题。
- **收窄 client unused 检查范围**：`client/**/*.js` 的 `no-unused-vars` 改为只检查局部变量，放过旧式浏览器脚本需要暴露给其他文件使用的顶层函数和常量，同时继续保留函数内部 unused 检查。
- **清理过期禁用注释和真实 unused**：移除不再需要的 `no-unused-vars` 禁用注释；仍依赖跨文件全局的文件只保留更窄的 `no-undef` 禁用；同时移除 `tests/security-auth.test.js` 中未使用的 `http` 解构。

#### 验证通过

- **lint 干净通过**：`npm run lint` 通过，且不再输出 warning。
- **项目检查通过**：`npm run check` 通过。
- **安全回归通过**：`node tests\security-chat.test.js` 通过 51/51，`node tests\security-auth.test.js` 通过 20/20。

### 知识库与能力库傻瓜式使用闭环升级

本版本只围绕知识库、能力库和普通聊天中的使用闭环继续做强：把“上传资料 / 启用能力 / 回到聊天 / 看见调用依据”这些原本需要用户自己理解的步骤，收口为更直观的中文引导、可点击动作和回答内状态追踪。普通用户不需要懂 RAG、MCP、索引、分块或工具调用，也能按页面提示完成“先准备、再试问、最后聊天使用”的完整流程。

#### 知识库体验升级

- **三步式知识库引导**：知识库工作台新增“上传文档 → 测试问题 → 聊天时打开知识库”的首屏引导，配合拖拽上传区域，普通用户进入页面后能直接知道下一步该做什么。
- **上传和学习语义中文化**：知识库按钮、空状态、进度、失败重试、批量重新学习、内容段、资料关系、删除审计等文案统一改为更接近用户语言的中文表达，减少“索引、分块、召回”等技术词暴露。
- **就绪后双动作闭环**：知识库有可用资料时，下一步卡片同时提供“试问一下”和“打开聊天知识库”；用户可以先验证资料命中效果，也可以一键回到聊天并自动点亮知识库开关。
- **试问结果更可操作**：试问弹窗新增示例问题、命中结果分组、可用性判断和“用这个问题去聊天”动作；命中足够时提示可直接带着问题回聊天，命中不足时提示换问法或补充资料。
- **质量诊断更像使用提醒**：知识库质量面板把异常、停用、空资料、重新学习和聊天开关提示放在同一个诊断区域，帮助用户发现“资料已上传但聊天没引用”的常见原因。
- **聊天回答显示引用来源**：普通聊天命中知识库后，会在回答上方保留“知识库已找到 N 条可引用资料”并展示最多 3 个来源文件名，用户能直接看到本轮回答是否真正依据资料。

#### 能力库体验升级

- **三步式能力库引导**：能力库工作台新增“启用能力 → 确认可用动作 → 聊天中授权使用”的首屏引导，并在治理面板提供“打开聊天能力库”入口。
- **系统能力与个人能力更清楚**：图表生成、报告编排、文档解析、数据处理、格式转换、报表文件、消息通知、数据库连接等能力卡片补齐中文用途、适用问题和动作数量说明。
- **配置表单更适合普通用户**：数据库、报表目录、IM 通知和外部能力服务的配置表单补齐中文占位、提示和推荐默认值；SQLite、普通数据库、报表目录等场景给出更明确的填写说明。
- **动作示例可直接带回聊天**：能力动作弹窗新增“可以这样问”的示例问题，点击后自动回到聊天、打开能力库并把示例填入输入框，减少用户不知道如何提问的问题。
- **能力调用状态更可读**：聊天 MCP 调用事件新增 `actionName`、`toolName`、`serverName` 等字段，前端优先展示“能力库：某服务 / 执行只读 SQL、生成图表、查询表格数据”等中文动作名，不再让普通用户只看到 `db.*` 这类技术名。

#### 普通聊天闭环升级

- **输入区工具状态条**：聊天输入区新增知识库/能力库状态条，开启后会自动检查当前是否有就绪资料或可用工具，并给出“打开知识库”“打开能力库”等可点击处理入口。
- **回答内保留依据和能力状态**：普通聊天流式回答会保留知识库命中、知识库未命中、能力库规划、能力库调用、调用完成、缺少工具和调用失败等状态提示；失败或缺少配置时可直接打开对应工作台处理。
- **知识库与能力库一键回聊天**：知识库和能力库工作台都支持从下一步卡片一键回到聊天并点亮对应开关，形成“准备 → 验证 → 使用”的闭环。

#### 工程拆分与中文编码治理

- **后端智能体运行时拆分**：`server/services/agent-runtime.js` 拆出 `agent-tool-runtime.js`、`agent-dag-runtime.js`、`agent-streaming-runtime.js`，降低单文件体积，并保持拆分文件中文注释和中文提示。
- **安全测试按领域拆分**：`security-rag.test.js`、`security-chat.test.js`、`security-mcp.test.js` 拆分为知识库、聊天渲染/会话/模型/缓存/工具、MCP 访问/服务/聊天上下文/配置等子文件，入口文件保持兼容。
- **中文编码逐文件检查**：按本轮新拆分和修改文件逐个检查中文注释、提示和测试文本，未使用脚本批量修复，确保新增拆分文件没有中文编码错误。

#### 测试与验证

- **回归测试补齐**：新增知识库引用来源提取、聊天回答状态追踪、能力库中文动作名、知识库/能力库下一步动作、拆分入口加载等断言。
- **浏览器实测通过**：本地页面实测知识库首屏双动作、能力库打开聊天闭环、聊天输入区状态条和控制台错误；知识库“打开聊天知识库”会回到聊天并显示可参与回答资料数。
- **验证通过**：`node tests\security-chat.test.js`、`node tests\security-rag.test.js`、`node tests\security-mcp.test.js` 和 `npm run check` 均已通过。
- **版本号启用**：应用版本升级至 `v0.0.90`，同步更新 `package.json`、`package-lock.json`、README 和项目汇报中的当前版本展示与说明。

## [v0.0.89] - 2026-06-06
### 工作流向导运行时兼容与大模型结果展示优化

本版本继续收口工作流 DAG 的实际使用体验：修复拆分后的统计图向导和数据库参数辅助在工具列表作用域上的运行时错误，并统一大模型节点在“节点卡片”和“执行步骤”中的结果展示方式，让用户默认看到可读的 Markdown 正文，而不是模型输出对象的 JSON 包装。

#### DAG 向导兼容修复

- **修复统计图向导工具列表作用域**：数据库向导辅助函数不再直接依赖外层 `currentTools()`，改为显式接收当前工具集合，修复打开统计图模板时 `currentTools is not defined` 的运行时错误。
- **参数弹窗与数据库辅助统一传参**：普通节点参数弹窗、数据库连接下拉、读取表/字段辅助面板和侧栏输入摘要统一使用同一份 `wizardTools`，避免不同入口读取到不一致的数据库连接或工具列表。
- **统计图模板入口更稳**：统计图向导复用当前工具集合查找数据库连接、图表工具和 `agent.llm` 工具，减少重复调用和拆分后作用域漂移带来的隐性问题。

#### 大模型结果展示优化

- **DAG 节点卡片 Markdown 化**：大模型节点的可读输出区域新增紧凑 Markdown 样式，标题、段落、列表、代码和表格统一左对齐，避免报告标题过大、列表标记偏移和内容居中导致的阅读割裂。
- **执行步骤不再默认展示 LLM JSON**：任务步骤里的“完成 DAG 节点：大模型处理”会优先从 `content`、`text`、`markdown`、`answer` 等字段提取模型正文并渲染为 Markdown；原始 JSON 仍保留在“查看原始数据”折叠项中，便于排查。
- **共享大模型正文提取逻辑**：节点卡片和步骤详情复用同一套 LLM 输出解析逻辑，避免同一个大模型节点在不同区域显示结果不一致。

#### 测试与版本同步

- **回归用例补齐**：新增统计图向导工具列表传递、大模型节点 Markdown 可读输出、步骤详情隐藏 LLM JSON 包装等断言，覆盖参数向导、运行节点卡片和执行步骤三条展示链路。
- **验证通过**：`npm run check` 通过；定向 `tests/security-agent.test.js` 中的向导、LLM 节点展示和工作流工作台相关用例通过。
- **版本号启用**：应用版本升级至 `v0.0.89`，同步更新 `package.json`、`package-lock.json`、README、使用手册和项目汇报中的当前版本展示与说明。

## [v0.0.88] - 2026-06-06
### 工作流 LLM 节点编排约束与统计图模板补齐

本版本围绕工作流里大模型节点的位置和模板数据流继续收口：参考成熟工作流产品的“开始输入 / 上游节点 / LLM 处理”编排思路，把 LLM 节点从“只要存在即可”升级为“必须有明确输入来源”，同时补齐统计图模板中的大模型节点配置，让图表类工作流默认具备数据解读步骤和稳定的结构化图表生成链路。

#### LLM 节点编排规则

- **起始 LLM 节点输入门禁**：大模型节点可以作为第一个节点，但提示词必须显式引用 `{{goal}}` 或 `{{inputs.*}}`；如果既没有上游依赖，也没有引用工作流输入，会提示“缺少上游输入”，避免首节点空跑。
- **上游结果依赖更明确**：当 LLM 节点需要处理数据库、检索、图表或其他节点输出时，必须连接对应上游节点；仅在提示词中写 `{{nodes.xxx.output}}` 不再被视为合法起始输入，防止运行时并行调度导致数据尚未产出。
- **前后端双重兜底**：前端画布校验、保存校验、运行预检和 DAG 运行时均接入同一规则；直接提交 JSON、恢复旧任务或绕过页面运行时也会被后端阻断。

#### 统计图模板补齐

- **模板默认加入 LLM 配置**：统计图向导生成的工作流从“分组统计 → 生成统计图”升级为“分组统计 → 大模型处理 → 生成统计图”，LLM 节点自动带入当前可用模型、系统提示词、用户提示词、输出格式和 Token 参数。
- **数据解读与图表生成分工清晰**：LLM 节点负责基于查询结果输出简洁数据解读，图表节点仍直接消费 `{{nodes.group_count.output.rows}}` 结构化结果，避免模型改写数据导致图表不稳定。
- **模板选中与编辑体验保持一致**：生成模板后默认选中“大模型处理”节点，方便用户先确认模型和提示词，再继续调整图表节点或数据库参数。

#### 测试与版本同步

- **回归用例补齐**：新增 LLM 起始节点输入校验测试，更新 DAG 数据库路由与 LLM 节点兜底测试；测试装配同步补齐 `dag-toolbar-tools.js`、`dag-toolbar-db.js` 和 `agent-workflow-runners.js`，与真实页面脚本加载顺序保持一致。
- **验证通过**：`npm run check`、`node tests\security-agent.test.js`、`node tests\security-mcp.test.js` 均已通过；`node --test tests\...` 在当前沙箱中因 `spawn EPERM` 无法拉起子进程，已改用单文件方式完成验证。
- **版本号启用**：应用版本升级至 `v0.0.88`，同步更新 `package.json`、`package-lock.json`、README、使用手册和项目汇报中的当前版本展示。

## [v0.0.87] - 2026-06-06
### 监控页资源说明细化

本版本继续收口监控页的资源展示：对路由/静态资源名称增加更细的自动识别规则，并补充新拆分脚本与样式入口的说明，让监控页里每个新文件都能更直观地识别出来。

#### 监控页细化

- **资源名称兜底增强**：系统监控页在路由说明表之外，新增对 `.js` / `.css` 静态资源的自动命名兜底，避免新拆分文件只显示原始路径。
- **新拆分文件补充说明**：为模型动作、DAG 工具、会话引擎、工作流核心/运行器、监控页自身脚本，以及 `chat.css` 下的入口样式补充更细的资源名称。
- **显示效果收敛**：监控页的 HTTP 资源列表现在能更明确地展示聊天脚本、聊天样式、公共样式与第三方资源，方便定位具体模块。

#### 版本与文档同步

- **版本号启用**：应用版本升级至 `v0.0.87`，同步更新 `package.json`、`package-lock.json`、README 与项目汇报中的当前版本展示。

## [v0.0.86] - 2026-06-06
### 前后端模块化拆分与运行时兼容修复

本版本围绕工程治理继续收口：前端聊天、管理端、DAG、工作流、RAG、统计与样式继续按领域拆分，后端内置 MCP 与安全测试也进一步模块化，同时修复了拆分后暴露出来的若干运行时兼容问题，并把当前版本同步到新的文档与包元数据。

#### 前端模块化拆分

- **聊天与管理端继续拆分**：`models.js`、`engine.js`、`dag-toolbar.js`、`agent-workflows.js` 等继续拆为更细的领域脚本，新增模型动作、会话引擎、默认模型、DAG 工具元数据、DAG 数据库辅助、工作流核心与运行器等文件，减少单文件体积。
- **工作台与渲染链路继续分层**：`agents-dag-editor.js`、`agents.js`、`render.js`、`sidebar.js`、`stats.js`、`rag.js` 等同步拆出独立模块，聊天页、工作流页、RAG 图谱和侧边栏加载顺序重新编排。
- **样式继续模块化**：`styles/admin.css`、`styles/stats-monitor.css`、`styles/workspaces/agent.css`、`styles/workspaces/mcp.css` 继续拆分为更细的子样式块，便于后续按页面和功能维护。

#### 运行时兼容修复

- **DAG 转义命名冲突修正**：`dag-core.js` 的通用转义函数改为独立命名，避免与拆分后的 DAG 工具链重复声明。
- **图谱平移事件修复**：`rag.js` 中的图谱拖拽/平移结束事件改为包装调用，避免 `stopGraphMapPan` 先于定义触发引用错误。
- **工具栏依赖注入修复**：`dag-toolbar.js` 的 `fitToContent` 改为通过上下文注入，`agents-dag-editor.js` 负责传入实际实现，避免工具栏在拆分后丢失依赖。
- **工作流与模型页回归修正**：工作流 JSON 读取、节点抽屉、模型管理动作与默认模型保存逻辑按模块重新暴露，保证原有入口继续可用。

#### 后端与测试治理

- **内置 MCP 外观聚合拆分**：`server/services/builtin-mcp.js` 继续保持聚合入口，但内部已按 common / reports / visualization / documents / data / format / im 拆分，后续新增能力更容易落位。
- **安全测试分域**：`tests/security.test.js` 仅保留聚合入口，认证、聊天、MCP、RAG、智能体等安全回归拆成独立测试文件，提升可维护性。
- **数据库连接模块收口**：`server/db/connection.js` 与相关服务继续整理，配合能力库拆分让连接与工具路由更清晰。

#### 版本与文档同步

- **版本号启用**：应用版本升级至 `v0.0.86`，同步更新 `package.json`、`package-lock.json`、README 与项目汇报中的当前版本展示。
- **验证通过**：关键拆分脚本 `node --check` 通过，`git diff --check` 已清理尾随空行与格式问题。

## [v0.0.85] - 2026-06-05
### 智能体任务详情与任务列表交互精简

本版本围绕智能体任务记录的查看体验做轻量收口：任务详情页移除信息密度偏低的时间轴模块，把注意力留给进度、结果、工作流节点、工具统计和执行步骤；任务列表则补齐任务列悬浮信息，同时取消问号鼠标样式。

#### 智能体任务详情

- **移除任务时间轴**：智能体任务详情弹窗不再渲染“任务时间轴”，同步清理 `.agent-timeline` 前端渲染函数与样式，减少重复信息和视觉干扰。
- **执行步骤标题左对齐**：任务具体执行步骤正文中的 Markdown 标题统一左对齐，和段落、列表、表格保持一致阅读方向。
- **保留关键执行信息**：详情页继续展示进度汇总、最终结果、错误信息、可视化结果、工作流节点、工具调用统计与逐步执行内容。

#### 智能体任务列表

- **任务列悬浮信息补全**：任务列悬浮时展示任务、状态、创建/开始/完成时间、模型、模式、步骤数、工具数、错误数和目标，便于不打开详情也能快速判断任务上下文。
- **移除问号鼠标样式**：任务列悬浮保留自定义信息提示，但鼠标样式改回普通箭头；同时移除原生 `title` 提示，避免浏览器默认提示与自定义悬浮层叠加。

#### 版本与验证

- **版本号启用**：应用版本升级至 `v0.0.85`，同步更新 `package.json`、`package-lock.json`、README 与项目汇报中的当前版本展示。
- **验证通过**：`npm run check` 通过；`eslint client/chat/agents.js` 通过；本地 `http://localhost:3000/` 可正常打开到登录页。

## [v0.0.84] - 2026-06-05
### 版本历史补齐、文档同步与回归验证对齐

本版本用于启用新的应用版本号，并修正发布文档的版本连续性：保留 `v0.0.83` 的完整功能更新记录，同时将当前运行版本、README、项目汇报和 package 元数据同步到 `v0.0.84`。

#### 文档与版本治理

- **补齐 v0.0.83 历史记录**：恢复 `v0.0.83` 条目，避免更新日志从 `v0.0.84` 直接跳到 `v0.0.82`。
- **当前版本同步**：`package.json`、`package-lock.json`、README 顶部版本徽章、最新版本说明、项目汇报和运行时版本读取均同步为 `v0.0.84`。
- **运行时版本确认**：服务端 `getAppVersion()` 继续从 `CHANGELOG.md` 顶部读取最新版本，当前返回 `v0.0.84`。

#### 回归验证对齐

- **工作流前端断言对齐**：补齐 DAG 参数弹窗大工作台尺寸、节点工具摘要卡紧凑布局，以及工具栏“发布 / 发布并运行 / 运行发布版”入口，确保实现与文档及回归测试一致。
- **验证通过**：`npm run check` 通过；`npm run lint` 通过；`node --test tests/security.test.js` 在 Windows 非沙箱环境完整通过 **160/160**。

## [v0.0.83] - 2026-06-04
### 工作流编排重构、权限层级统一与能力库数据查询增强

本版本围绕“工作流作为独立流程资产”继续收口体验：把运行、发布、版本管理和 LLM 执行配置前移到工作流画布自身，同时补齐权限层级语义、能力库数据库清单/计数能力，以及聊天 MCP 工具调用的确定性回退。

#### 工作流编排体验：编辑、发布、运行一体化

- **工作流顶部区域重排**：工作流页顶部改为“当前画布 + 已保存工作流”双入口，移除常驻工作流名称输入框，保存新草稿时再通过自定义输入弹窗命名；“已保存工作流”右侧四个操作按钮取消外层容器框，保持无边框透明按钮组。
- **生命周期状态更清晰**：原版本文字改为生命周期 chip，分别展示当前版本、发布版本和运行状态；新建、加载、版本管理和删除与已保存工作流选择器保持同一行紧凑排列。
- **工具栏分组收口**：DAG 工具栏按“节点 / 模板 / 操作 / 发布 / 运行”分组，发布和运行入口同处一行；下拉触发器视觉上保持普通按钮样式，不再显示下拉箭头，并支持点击外部自动收起。
- **工作流运行入口独立化**：移除智能体任务面板里的工作流运行模式、工作流选择器和底部运行调试区域；工作流预览运行、发布版运行、发布并运行统一从工作流画布发起。
- **运行输入面板归位**：`{{inputs.*}}` 运行变量只在工作流画布内扫描与填写，避免智能体任务面板和工作流画布各维护一套输入状态。

#### LLM 节点成为工作流核心执行节点

- **新增 `agent.llm` 大模型节点**：工具目录新增“大模型”分组，LLM 节点可配置节点模型、系统提示词、用户提示词、输出格式、温度、最大输出长度和工作流最大步骤数。
- **空画布自动创建 LLM 节点**：新建或加载空工作流时自动补齐默认“大模型处理”节点，减少空画布运行失败。
- **运行配置迁入节点**：工作流运行 payload 不再依赖工作流级“运行模型 / 最大步骤”字段，而是从第一个 `agent.llm` 节点读取模型与步骤上限。
- **强制保留至少一个 LLM 节点**：前端保存、校验、运行时要求至少存在一个已配置模型的 LLM 节点；删除或把最后一个 LLM 节点改成其他工具会被阻止。后端保存与运行时也增加兜底校验和默认配置补齐。
- **模型可见性收紧**：工作流 LLM 节点模型选择器只展示全局模型和当前用户自己的私有模型；不再因为当前用户是管理员就暴露其他用户的私有模型，和系统模型选择器保持一致。

#### DAG 参数编辑与节点配置优化

- **参数编辑弹窗化**：DAG 节点参数编辑迁入更大的固定高度工作台弹窗，避免复杂 JSON、SQL 或提示词在窄侧栏里难以编辑。
- **常用参数中文化**：数据库、报表、图表、格式化、IM、知识库和 LLM 节点的常用参数补齐中文标签、说明和占位提示，减少直接暴露英文字段名造成的理解成本。
- **工具摘要卡紧凑化**：节点工具元信息卡改为更紧凑的全宽摘要样式，展示工具名称、来源 badge、描述和工具 ID，移动端自动换行。
- **任务补充说明改为多行文本**：智能体任务的“补充说明”从单行输入改为全宽 textarea，更适合填写部门范围、资料来源或执行限制。
- **统计图向导补充说明**：统计图工作流向导为可选数据库 schema 字段增加说明，降低多 schema 数据库下的填写歧义。

#### 权限层级语义统一

- **新增权限工具模块**：新增 `server/permissions.js`，统一提供 `admin / manager / user` 三层权限语义、角色归一化、内置 `admin` 识别和前后端权限标识。
- **内置 `admin` 与普通管理员区分**：系统管理员显示为“系统管理员”，普通管理员显示为“管理员”；用户列表、登录标识、设置页、模型页、能力库和智能体审计文案统一改用权限层级表达。
- **超级管理员门禁集中化**：开放注册、全局模型、全局能力服务、用户详细记录、管理员账号管理和删除审计等操作统一使用 `isSuperAdmin()` 判断，不再分散手写 `username === 'admin'`。
- **安全出口判断对齐权限工具**：模型、MCP 和数据库私网连接校验改用 `isAdmin()` / `isSuperAdmin()`，避免角色判断在不同模块里出现语义漂移。

#### 能力库与数据库 MCP 增强

- **数据库 MCP 工具抽象为通用动作**：DAG 工具目录不再把每个数据库连接的 `mcp.<id>.db.list_tables`、`mcp.<id>.db.describe_table`、`mcp.<id>.db.run_readonly_query` 等底层工具逐个展开给用户，而是优先收敛为“列出数据表 / 查看表结构 / 只读 SQL 查询 / 分组统计 / 统计数据表数量”等通用数据库动作，避免数据库连接一多就让工具下拉膨胀。
- **数据库连接改为资源参数**：数据库连接从“工具身份”迁移为节点参数面板里的“数据库连接”选择器；节点输入继续承载 `connectionId`、`schema`、`table`、`column`、`sql` 等业务参数，并保留“读取表 / 读取字段”等辅助能力，且辅助读取会跟随当前选择的数据源。
- **运行时按 `connectionId` 路由真实 MCP**：工作流保存时节点可保持通用形态，例如 `tool: "db.run_readonly_query"` + `input.connectionId`；运行时再根据 `connectionId` 转发到真实工具 `mcp.<connectionId>.db.run_readonly_query`，审计和调用日志仍记录具体连接与工具。
- **渐进收敛边界清晰**：本轮仅对 schema 基本统一的数据库 MCP 做抽象；普通第三方 MCP 工具仍按原始工具暴露，避免把语义和参数差异较大的外部能力过早合并。
- **数据库表数量工具**：SQL 数据库 MCP 新增 `db.count_tables`，支持 SQLite、PostgreSQL、MySQL 和 SQL Server，按表/视图或 schema 统计数量并返回结构化 `total` 与 `rows`。
- **MongoDB 集合数量工具**：MongoDB MCP 新增 `db.count_collections`，用于回答“有多少个集合”等集合盘点问题。
- **聊天 MCP 回退更可靠**：普通对话中当用户明确要求查询数据表清单、表数量或集合数量时，即使规划器返回 `none`，也会确定性选择 `list_tables` / `count_tables` / `list_collections` / `count_collections` 等工具执行。
- **工具匹配支持短名与来源提示**：规划器返回短工具名时可解析到完整 MCP 工具名；多个同名工具可根据用户提到的服务名优先匹配，调用日志区分 `chat` 与 `chat_fallback` 来源。
- **缺工具时明确告知**：用户明确要求能力库或数据库工具调用但当前无匹配工具时，会向主模型注入“缺少匹配能力库工具”的上下文，避免误答成本轮不需要工具或编造结果。

#### 回归测试与验证

- **新增回归用例**：补充权限层级、DAG 参数弹窗、任务补充说明 textarea、统计图 schema 说明、工具摘要卡、工作流预览/发布版运行入口、LLM 工作流节点、数据库 MCP 通用工具折叠、连接参数路由、聊天 MCP 缺工具提示和数据库表/集合计数调用测试。
- **验证通过**：`npm run check` 通过；`npm run lint` 通过；`node --test tests/security.test.js` 在 Windows 非沙箱环境完整通过 **160/160**。浏览器可视化验证按本轮要求未执行。

## [v0.0.82] - 2026-06-03
### 普通对话图表时序修正、持久化修复与规划器回退

本版本补齐普通对话图表的”实时渲染 → 消息落库 → 刷新恢复”闭环，修复图表在模型思考期间过早出现的问题，并新增规划器确定性回退——当 LLM 规划器无法可靠决策时，正则提取表名/字段构造 SQL 直接执行。

#### 图表时序修正：图表随文字同步出现

- **延迟注入**：SSE `chart` 事件到达时不再立即调用 `injectChartBlock` 注入 DOM，只存入 `pendingCharts`。待第一个文字 `content` 事件到达（`firstTokenTime` 首次赋值时）才批量注入图表块，确保图表与模型回复文字同步出现，不再出现”还在思考图表就先出来了”的体验。
- **错误/中断路径兜底**：在 `AbortError` 和通用错误路径中，调用 `pendingCharts.forEach(spec => injectChartBlock(...))` 确保图表块在异常退出时也被注入 DOM 并渲染。
- **修复渲染参数错误**：`injectChartBlock` 中将 `renderPivotCharts(chartBlock)` 修正为 `renderPivotCharts(msgEl)`——`renderPivotCharts` 使用 `querySelectorAll` 只查后代元素，不能将目标元素自身作为 root。

#### 图表持久化与刷新恢复

- **SSE 图表写入持久化正文**：`server/routes/chat.js` 新增 `rememberStreamedChart()` 拦截 `writeSse`——捕获本轮流式响应中发给前端的 `type: “chart”` 图表 spec，去重后存入 `streamedChartSpecs[]`。保存 assistant 消息前通过 `appendStreamedChartsToAssistantContent()` 将图表序列化为 `pivot-echart` fenced code block 追加到消息正文末尾。刷新页面后，历史消息通过现有 Markdown 渲染链路重新生成 `.pivot-echart-block`，不再依赖首次流式 DOM 注入。
- **同轮图表事件去重**：后端 `rememberStreamedChart` 对相同 chart spec 建立去重集合，重复图表事件不再写入 SSE，也不会重复合并进消息正文。
- **前端重复事件兜底**：`client/chat/engine.js` 新增 `pendingChartKeys`，即使后端或网络重放重复 chart 事件，当前助手消息也只注入一次图表 DOM。
- **兼容已有可渲染图表块**：`contentIncludesRenderableChart()` 检测模型正文是否已包含 `pivot-echart`/`pivot-chart` 代码块，若已存在则不再追加本轮 SSE 图表，避免刷新后正文出现重复图表。

#### 规划器确定性回退：规划器失败时直接构造 SQL

- **`detectStrongDataQueryIntent()`**：不依赖 LLM 判断，通过正则检测用户是否明确要求查询数据库（匹配表名/数据库+表名/SQL 关键词/分组统计关键词）。
- **`extractTableName()` / `extractGroupByField()`**：从用户自然语言中正则提取表名和分组字段（支持”查询 table_account 表”、”按 group_id 分组”等多种中文表达）。
- **`buildFallbackDataQueryInput()`**：用提取到的表名+分组字段构造 `SELECT ... GROUP BY ... LIMIT 50` 或 `db.group_count` 参数，兼容 `run_readonly_query` 和 `group_count` 两种工具。
- **规划器失败时自动回退**：在 `maybeBuildMcpChatContext` 中，当 LLM 规划器返回 `action: none` 或 JSON 解析失败后，先调用 `detectStrongDataQueryIntent` 判断——若用户明确要查数据，直接用 `buildFallbackDataQueryInput` 构造 SQL 执行，绕过不可靠的规划器。回退失败则降级到 `buildMcpToolsHint` 提示。

#### 图表 SSE 去重：消除自动图表链路的重复发射

- **`server/services/chat-mcp-context.js`**：去掉 `maybeBuildChartAfterDataTool()` 中的额外 `emitChartSse()` 调用，统一由 `extractMcpResultText()` 负责发出图表事件，防止自动图表链路天然发送两次（一次在直接执行 viz 时，一次在 auto-chart 后）。

#### 回归测试与验证

- **新增回归用例**：`tests/security.test.js` 增加”chat route embeds streamed chart specs into persisted assistant content”、”chat renderer accepts loose ECharts-style chart specs”和”chat MCP fallback builds group_count input”测试。
- **验证通过**：`npm run check` 通过（129 个 JS 文件）；`node tests/security.test.js` 安全回归 **144/144** 全部通过。

## [v0.0.81] - 2026-06-03
### 普通对话图表链路确定性修复与中文思考强化

本轮针对普通对话中"模型返回代码而非真正图表"的核心问题，重构图表结果从工具到前端的传递链路，同时强化所有模型交互路径的中文思考约束。

#### 图表传送链路：SSE 双通道架构

**问题分析**：能力工具 `viz.build_chart` 和自动图表生成都能正常产出结构化 `pivot_chart`，但结果会先转成 `pivot-echart` 代码块文本嵌入 system context，再交给主模型"原样保留"。该步骤不可靠——模型经常将其改写为 Python matplotlib / JavaScript ECharts 示例代码，导致前端无法渲染真实图表。MCP 调用日志确认 `viz.build_chart` 成功返回图表数据，但后续助手消息中 `pivot-echart` 块为 0，反而出现 Python 代码块 12 条、ECharts 3 条。

**核心修复——SSE 直通前端**：
- **`server/services/chat-mcp-context.js`**：新增 `emitChartSse()` 函数，图表结构化数据通过 SSE `{"type":"chart","data":...}` 事件直接发送给前端，**完全绕过模型文本生成**。`extractMcpResultText()` 现返回简短文字引用（"已生成图表：xxx，前端已直接渲染"）替代原来的 `pivot-echart` 代码块；`maybeBuildChartAfterDataTool()` 自动图表同步走 SSE 直达通道。
- **`client/chat/engine.js`**：新增 `chart` SSE 事件处理分支 + `injectChartBlock()` 辅助函数——收到图表数据后立即创建 `.pivot-echart-block` DOM 元素并注入当前助手消息（位于 `.text-body` 之外，不会被流式文本渲染覆盖），随后调用 `renderPivotCharts` 初始化 ECharts。成功/错误/中断三条路径均确保图表块被渲染。
- **修复渲染参数错误**：`renderPivotCharts(root)` 使用 `root.querySelectorAll` 查找后代元素，不可将图表块自身作为 root 传入，修正为传入父容器。

#### 图表意图检测与工具调度优化

- **`server/services/chat-mcp-context.js` — 意图检测扩展**：`getMcpToolIntent()` 正则新增 `统计分析|数据对比|数据汇总|数据概览|分布情况|数据分布|占比|排名|排行|展示.*数据|数据.*展示|呈现.*数据|查询.*统计|洞察`，覆盖"帮我分析销售数据"等隐式图表请求。
- **规划器提示词强化**：新增关键指令——"如果用户询问数据、统计、分析相关的问题，且存在可用的数据库或数据查询工具，请优先选择合适的数据查询工具，不要返回 action=none。主模型无法自行查询数据库，会回退到编造数据或生成无效代码。"
- **规划器跳过时注入工具提示**：新增 `buildMcpToolsHint()` 函数，当规划器返回 `action=none` 或工具过滤后为空时，不再返回空字符串，而是注入可用工具列表和禁止自编代码的提示给主模型，防止其回退到代码生成。

#### 中文思考约束全覆盖

- **`server/routes/chat.js`**：`CHAT_LANGUAGE_SYSTEM_PROMPT` 重构为结构化规则——`【重要语言规则】`（4 条中文约束）+ `【重要工具规则】`（4 条工具行为约束），明确禁止生成 Python/JS 图表代码，要求保留 `pivot-echart`/`pivot-table` 块。
- **`server/services/agent-runtime.js` — 流式 Agent**：`tryRunAgentStreaming()` 系统提示词补上 `【重要语言规则】你的思考、推理和所有输出必须使用中文。禁止使用英文提纲或英文推理过程。`——此前流式路径完全没有中文思考约束。
- **`server/services/agent-runtime.js` — 规划器**：`buildPlannerMessages()` 新增显式 `【重要语言规则】` 声明，Schema 标签从 `"最终答案"` 升级为 `"最终答案（中文）"`。

#### 测试同步

- **`tests/security.test.js`**：`chat language instruction` 测试断言更新，匹配新的 `【重要语言规则】` 和 `禁止使用英文提纲或英文推理` 措辞。

### 回归验证
- **验证通过**：`npm run check` 通过（129 个 JS 文件）；`node tests/security.test.js` 安全回归 **141/141** 全部通过。

## [v0.0.80] - 2026-06-03
### 智能体工作流与数据库统计闭环

本版本汇总本轮智能体工作流、DAG 运行、数据库 MCP 统计工具、Agent 中文提示词和文档版本同步相关修复优化。

#### 工作流运行与编排体验
- **工作流运行入口独立化**：DAG 模式从“工作流编排”统一收口为“工作流”，运行面板新增工作流选择器和“打开工作流”入口，运行时必须选择已保存工作流，避免直接提交空画布或临时 JSON。
- **新建工作流弹窗**：新增工作流名称、说明和默认执行目标表单，创建后自动进入 DAG 模式并初始化空白画布；保存时同步 `description`，工作流库和运行选择保持同一个活动工作流。
- **运行目标自动推断**：DAG 运行未填写目标时，优先使用工作流名称生成“执行工作流：xxx”，再回退到当前画布节点数，减少因目标为空导致的任务创建失败。
- **已保存工作流校验修复**：运行面板现在同时兼容字符串 JSON 和已解析的 `dag_spec` 对象，不再把已保存工作流误判为“工作流 JSON 需要修正”。
- **运行输入可视化**：运行面板和工作流画布都会扫描 `{{inputs.*}}` 引用并生成输入表单，提交时自动合并为 `dagInputs`，替代手写 JSON textarea。
- **画布编辑增强**：工具栏改为“节点 / 模板 / 操作”分组，新增统计图模板向导；节点坐标持久化、智能默认工具、字段 tooltip、Delete/Backspace/Escape 快捷键和保存前校验门禁保持可用。
- **DAG 结果可视化**：运行详情新增迷你拓扑状态图，按完成、运行中、错误、跳过、待执行区分节点状态，并展示依赖边和图例。
- **软删除恢复**：工作流删除后展示“撤销删除”，后端新增 `PATCH /api/agents/workflows/:id/restore`，30 天内可按用户权限恢复。

#### 数据库 MCP 与模板数据流
- **新增 `db.group_count` 工具**：数据库 MCP 增加安全只读分组统计能力，支持 SQLite、PostgreSQL、MySQL 和 SQL Server，按表名、分组字段、别名、排序和 limit 自动生成统计 SQL，适合普通用户快速生成分布图。
- **统计图模板向导**：DAG 编辑器可读取数据库表和字段，自动生成“分组统计 -> 生成统计图”两节点工作流；优先使用 `db.group_count`，缺失时回退到只读 SQL 查询。
- **结构化结果引用修复**：DAG 模板引用 `{{nodes.xxx.output.rows}}` 时会自动回退读取 `structuredContent.rows`，图表节点可以直接消费 MCP 结构化数据行。

#### 智能体与普通对话图表链路优化
- **图表结果保真**：智能体工作流图表节点可直接消费 `structuredContent.rows`，普通对话在查询数据后会优先补调用 `viz.build_chart`，并要求最终回答原样保留 `pivot-echart` 结构化图表块，减少图表结果退化为 Python/ECharts 代码或纯文本的情况。

#### Agent 运行时与文案
- **Agent 提示词中文化**：核心规划提示、上下文指导、流式工具调用提示和最终答案合成提示全部汉化，要求模型以中文进行简短推理和最终输出。
- **普通聊天思考过程中文约束**：普通聊天请求会注入中文语言约束，要求最终答案以及可见的 `reasoning_content`、`<think>`、`<thought>` 思考内容默认使用中文，减少推理面板出现英文提纲。
- **运行日志中文修复**：修复任务通知、重跑节点、断点续跑、审批、工具执行、DAG 节点状态和模型路由等步骤标题中的乱码文案。
- **DAG 节点并发可配**：互不依赖节点并行数改为 `AGENT_DAG_NODE_CONCURRENCY`（默认 4），运行时状态 API 暴露该值，README 与 `.env.example` 同步补充配置说明。

#### 测试与文档
- **安全回归补充**：`tests/security.test.js` 新增 `db.group_count` 工具暴露和执行验证，覆盖 `output.rows` 到 `structuredContent.rows` 的模板引用兼容，并补充普通聊天可见思考中文约束测试。
- **版本同步修正**：应用版本升级至 `v0.0.80`，同步更新 `package.json`、`package-lock.json`、README 顶部版本、近期升级摘要、当前版本说明和配置文档。

### 回归验证
- **验证通过**：`npm run check` 通过；`npm run lint` 通过；`node --test-name-pattern "chat language instruction asks visible reasoning" tests/security.test.js` 定向安全回归通过；`node --test-name-pattern "DAG templates read MCP structured rows|database MCP preset exposes SQLite readonly tools" tests/security.test.js` 定向安全回归通过。
- **完整安全回归备注**：已执行 `node tests/security.test.js`，新增的 `output.rows` 兼容与 `db.group_count` 用例通过；完整套件在当前 Windows 环境的上传/RAG 文件清理阶段因 `EPERM: unlink` 文件锁触发 5 个既有用例失败（135/140），需释放文件句柄或清理测试残留后复跑完整套件。

## [v0.0.79] - 2026-06-03
### Agent 提示词中文化

将 Agent 运行时核心系统提示词从英文迁移为中文，确保大模型思考过程与最终输出一致使用中文。

#### 修改范围（agent-runtime.js）

- **核心 Agent 系统提示词（`buildAgentMessages`）**：身份声明、行为规则、JSON Schema、运行模式说明、上下文指导、工具列表标签全部汉化；Schema 的 `thought` 字段标注为 `简短推理（中文）`，强制模型用中文思考。
- **运行模式标签**：`standard`/`deep`/`audit`/`dag` 四种模式改为中文说明（标准模式—稳扎稳打 / 深度模式—允许额外检索 / 审计模式—必须强调证据、限制和风险 / DAG 模式—按工作流图执行），帮助模型理解当前模式的行为预期。
- **上下文模式**：`recent`/`knowledge`/`none` 对应的英文提示改为"使用最近的对话上下文 / 使用知识库上下文 / 不包含额外的会话上下文"。
- **流式 Agent 系统提示词（`tryRunAgentStreaming`）**：身份声明、目标标签、工具调用指导全部汉化。
- **最终答案合成提示词（`synthesizeFinalAnswer`）**：系统提示汉化并追加 `输出请使用中文。`；兜底文案 `No final answer was generated.` 改为 `未能生成最终答案。`。

### 回归验证
- **验证通过**：`npm run check` 通过；`npm run lint` 通过；`node tests/security.test.js` 安全回归通过。

## [v0.0.78] - 2026-06-03
### 智能体工作流编排 UX 优化

经过用户体验评审后，针对工作流编排（DAG）功能的 10 项重点优化：

#### 画布编辑器（agents-dag-editor.js）
- **智能工具选择**：新增节点时统计画布上已使用工具的频次，同工具使用 ≥2 次时自动设为默认值，否则留空让用户主动选择，避免盲目预填列表第一项。
- **节点坐标持久化**：序列化时保留用户手动调整的 `_x`/`_y` 坐标，重新加载时已有坐标的节点不再被自动布局覆盖，仅对新节点触发拓扑排列。
- **节点属性提示**：重试次数和超时（ms）字段增加 title tooltip，说明"0 表示不重试 / 使用全局默认超时"。
- **键盘快捷键**：画布聚焦时 `Delete`/`Backspace` 删除选中节点，`Escape` 取消选中并关闭节点抽屉；仅在非文本编辑态响应，不与表单输入冲突。
- **校验方法暴露**：`mount()` 返回新增 `validate()` 方法，供外部在保存/发布前调用。

#### 工作流库（agents.js）
- **保存前校验门禁**：`saveAgentWorkflowToLibrary` 保存前自动调用画布校验，如存在循环依赖、未选工具、依赖缺失等问题，弹出确认弹窗，用户可强制保存或取消修正。
- **删除软撤销**：工作流删除后版本标签位置显示"↩ 撤销删除"按钮，调用新增的 `PATCH /api/agents/workflows/:id/restore` 接口，30 天内可一键恢复。
- **模板替换提醒**：统计图向导在替换当前画布前，检测画布与已保存内容的差异，如有未保存更改则在确认弹窗中明确提示"当前画布有未保存的更改"。

#### 运行时输入面板（agent-dag.html + agents.js + agent.css）
- **可视化变量编辑**：新增运行时输入面板，自动扫描 DAG JSON 中所有 `{{inputs.*}}` 引用，动态生成对应表单字段；用户填写后值优先于 JSON textarea 合并到 `dagInputs` 提交。
- 面板随画布变更自动刷新，带"重新扫描变量"按钮，`runMode` 切换为标准模式时自动隐藏。

#### DAG 运行结果可视化（agents.js + agent.css）
- **迷你状态图**：运行详情中节点列表上方渲染 SVG 拓扑图，节点按状态着色（绿色完成、蓝色运行中、红色错误、黄色跳过、灰色待执行），边上绘制依赖箭头，底部附图例。
- 拓扑分层基于节点 `depends_on` 的 Kahn 风格布局，与编辑器画布视觉一致。

#### 运行时（agent-runtime.js + agent-workflows.js + routes/agents.js）
- **DAG 节点并发可配**：硬编码的并发数 4 改为环境变量 `AGENT_DAG_NODE_CONCURRENCY`（默认 4），运行时状态 API 暴露该值；编辑器工具栏 tooltip 补充提示。
- **软删除恢复 API**：新增 `restoreAgentWorkflow` 服务函数，按 workflow_id + 用户校验，仅恢复 30 天内删除的工作流；新增 `PATCH /api/agents/workflows/:id/restore` 路由。

### 回归验证
- **验证通过**：`npm run check` 通过；`npm run lint` 通过；`node tests/security.test.js` 安全回归通过。

## [v0.0.77] - 2026-06-03
### 缺陷修复
- **工作流图表工具重复显示去重**：智能体工作流编排的「选择工具」下拉中，「图表与展示」分类下 `生成图表 · viz.build_chart`、`生成表格 · viz.build_table` 出现重复项。根因是可视化工具同时注册为**内置智能体工具**（`agent-tools.js`，对所有用户始终可用）与「图表生成」**内置 MCP 能力**（`builtin-mcp.js`，用户添加后才出现），而工具目录在 `agent-tool-catalog.js` 的 `formatToolList` 合并内置工具与缓存 MCP 工具时未去重。现按裸工具名去重——缓存 MCP 工具与内置工具同名时只保留内置工具；外部第三方 MCP 工具因带 `mcp.<id>.` 服务前缀不受影响，工具执行链路保持不变。

### 回归验证
- **验证通过**：`npm run check`（129 个 JS 文件语法 + 前端资源校验）通过；`npm run lint` 通过；完整 `node tests/security.test.js` 安全回归 `139/139` 通过。

## [v0.0.76] - 2026-06-03
### 安全加固
- **普通用户私网出口默认收紧**：个人 MCP 外部端点、内置 IM Webhook 与数据库 MCP 连接默认禁止普通用户配置 loopback、link-local、局域网或内部主机地址；管理员仍可在显式允许私网出口的部署策略下接入内网资源。`MCP_RESTRICT_PRIVATE_DATABASE_HOSTS_TO_ADMIN` 改为默认开启，仅当明确设置为 `false` 时才允许普通用户配置私网数据库。
- **可信反向代理识别**：新增 `TRUSTED_PROXY_IPS` 配置，只有直连来源命中受信任 IP/CIDR 时才采信 `X-Forwarded-For`；访问日志同步使用同一套客户端 IP 解析逻辑，避免伪造请求头污染审计来源。
- **出站 DNS rebinding 防护闭环**：模型调用、MCP JSON-RPC、RAG Embedding、模型/向量模型探测、模型健康检查、标题生成、记忆压缩和告警 Webhook 等 HTTP 出口统一使用安全 lookup Agent，请求时复用受保护的 DNS 解析，阻断校验后重解析到敏感地址的 TOCTOU 风险。
- **HTML 清洗兜底加固**：前端 `PivotSafeHtml.sanitizeHtml` 在 DOMPurify 未加载时不再返回原始 HTML，而是退回到文本转义，避免依赖加载异常时产生 XSS 兜底缺口。

### 配置与文档
- **新增代理配置说明**：`.env` 与 `.env.example` 新增中文注释的 `TRUSTED_PROXY_IPS`，说明仅受信反向代理可传递真实客户端 IP。
- **版本升级**：应用版本升级至 `v0.0.76`，同步更新 `package.json`、`package-lock.json` 与 README 版本展示。

### 回归验证
- **安全测试补齐**：新增/调整 MCP loopback 阻断、数据库私网默认限制、可信代理 IP、出站安全 lookup 和 DOMPurify 缺失兜底测试。
- **验证通过**：`npm run check` 通过；完整 `node tests/security.test.js` 安全回归在 Windows 非沙箱环境 `139/139` 通过。

## [v0.0.75] - 2026-06-03
### 安全复查修复
- **记忆压缩出站补齐 SSRF 守卫**：会话记忆压缩（`llm.js` 的 `compressMemory`）此前是唯一未经运行时校验的模型出站点，现已补齐调用时 DNS 解析守卫，与聊天补全、标题生成、智能体等其它出站点一致，闭合 DNS rebinding 残留缺口。
- **MongoDB / MySQL 连接 TLS 修复**：此前 MongoDB 的 SSL 开关被静默忽略（勾选 SSL 仍始终明文连接，凭据可被中间人窃取）、MySQL 的「信任自签名证书」开关被丢弃，本轮一并修复——MongoDB 改用 `tls`/`tlsAllowInvalidCertificates`，MySQL 对齐 `rejectUnauthorized`，默认校验证书且仅在显式信任自签名时放行，与 PostgreSQL/SQL Server 行为一致。
- **工作簿解析行数上限**：用户上传的 Excel/CSV 经 `document-text.js` 解析时新增单表 5 万行上限（`sheetRows`），作为纵深防御缓解超大或恶意表格触发的 CPU 耗尽与 ReDoS。

### 依赖安全
- **xlsx 升级至修复版**：`xlsx` 升级至 SheetJS 官方修复版 `0.20.3`（修复原型污染 GHSA-4r6h-8v6p-xvw6 与 ReDoS GHSA-5pgg-2g8v-p4x9）。npm 源已停更在有漏洞的 `0.18.5`，改用官方 CDN 分发；用户上传文件直接进入 `XLSX.read` 的高危攻击面已消除。`npm audit` 仅剩 `uuid` 一项 moderate（项目仅用 `v4()`、不传 buffer 参数，不受该 CVE 影响）。

### 前端体验
- **智能体任务过滤防抖**：智能体任务列表的过滤文本框（`agents.js`）改为防抖触发刷新，避免逐键发起请求风暴与响应乱序覆盖列表，下拉筛选仍即时生效。

### 复查方法与验证
- **多维度深入审查**：以并行子代理对安全、服务端、前端、依赖/构建四个维度做全面复查，对每个发现独立验证后修复，确认上一轮重构无回归。
- **验证通过**：改动文件语法检查与模块加载通过，`node tests/security.test.js` 安全回归 `135/135` 通过。

### 版本同步
- **版本升级**：应用版本升级至 `v0.0.75`，同步更新 `package.json`、`package-lock.json` 与 README 版本展示。

## [v0.0.74] - 2026-06-03
### 安全加固
- **MCP 出站 SSRF 防护**：外部 MCP JSON-RPC 调用（`mcp-client.js`）和 IM Webhook 推送（`builtin-mcp.js`）在请求发起前新增出站地址校验，拦截 loopback、link-local 和云元数据（如 `169.254.169.254`）等敏感目标；校验在调用时执行并包含 DNS 解析，可阻断 DNS rebinding 绕过，普通局域网地址仍放行以兼容内网模型/服务接入。
- **数据库连接默认校验 TLS 证书**：PostgreSQL（`rejectUnauthorized`）与 SQL Server（`trustServerCertificate`）连接默认改为校验服务端证书，防止开启 SSL 后仍被中间人攻击窃取凭据与数据；新增显式「信任自签名证书」开关（`allowSelfSigned`），仅在用户主动开启时才放行自签名证书。
- **访问日志敏感参数脱敏**：HTTP 访问日志新增 `scrubUrl`，对 URL 查询串中的 `token`、`access_token`、`api_key`、`signature`、`secret` 等敏感参数做 `[REDACTED]` 脱敏，避免附件访问令牌等凭据随 URL 落入日志被重放。

### 性能优化
- **聊天热路径消除同步文件读**：报表 MCP 工具（`builtin-mcp.js`）的文件解析、目录遍历与预览（`resolveReportFile`/`listReportFiles`/`readWorkbookRows`/`readTextPreview`/`queryReportTable`）以及 PDF/图片读取（`document-text.js`/`image-safety.js`）全部从 `fs.readFileSync` 改为异步 `fs.promises`，XLSX 改为异步读 buffer 后再解析，避免大文件同步读阻塞事件循环影响并发请求。
- **监控指标内存增长收敛**：HTTP 指标路由统计在路由未匹配（404、扫描器随机 URL）时归入常量 `unmatched` 桶，不再按原始 URL 无限创建键，防止 `routeStats` Map 无界增长。
- **GPU 监控定时器 unref**：GPU 动态负载监控的 `setInterval` 补齐 `.unref()`，与维护/模型运行时监控一致，不再阻止进程干净退出。

### 工程重构（行为不变）
- **巨型文件按领域拆分**：`agent-runtime.js` 从约 2797 行拆分为 runs / dag-utils / monitoring / preflight / schedules / templates / tool-catalog / workflows / artifacts 等模块（主文件降至约 1571 行）；`routes/chat.js` 的 `/chat` 处理器拆出 errors / mcp-context / rag-context / route-helpers / vision 等模块（路由文件降至约 711 行），按现有导出边界拆分，对外行为保持不变。
- **前端 RAG 模块拆分**：`rag.js` 拆出 `rag-format.js`、`rag-graph-layout.js`、`rag-graph-render.js`、`rag-graph-ui.js`，通过 `window.Pivot.*` 命名空间挂载并带默认值兜底消费。
- **前端 HTML 转义统一**：各处 `escapeHtml`/`escapeRagHtml`/`escapeChatStatusHtml` 统一委托 `PivotSafeHtml.escapeHtml`，`safe-html.js` 提供完整 5 字符转义（含 `"` 与 `'`），消除模块间转义语义分叉。
- **前端工具命名空间合并挂载**：`pivot-core.js` 改为合并挂载到既有 `window.Pivot` 而非首次定义即早退，并桥接 `Pivot.html`，避免工具函数被覆盖丢失。
- **裸 fetch 统一收敛**：`rag.js`、`users.js`、`models.js`、`stats.js` 的裸 `fetch` 全部改用带 CSRF 头与 401 自动刷新的 `apiFetch` 封装。

### 依赖安全
- **修复 6 项依赖漏洞**：`npm audit fix` 非破坏性升级 `axios`（1.15.2 → 1.16.1，含原型污染 MITM 与代理绕过修复）、`qs`、`brace-expansion`、`ip-address`、`express-rate-limit`；保留 `uuid`（仅使用 `v4()`，不受 buffer 越界 CVE 影响）。

### 回归验证
- **错误文案回归修复**：拆分 `agent-runtime` 时误将 rerun / rerun-node / resume 三处面向用户的中文错误提示改成英文，已恢复为「当前任务仍在执行中…」等原中文文案。
- **验证通过**：全部 57 个改动/新增 JS 文件语法检查通过，完整服务端模块成功加载，`node tests/security.test.js` 安全回归 `135/135` 通过。

### 版本同步
- **版本升级**：应用版本升级至 `v0.0.74`，同步更新 `package.json`、`package-lock.json` 与 README 版本展示。

## [v0.0.73] - 2026-06-02
### 知识图谱体验优化
- **关系地图图谱化展示**：知识图谱页面的局部关系视图升级为中文关系地图，节点、关系标签、可信度、统计摘要和操作入口统一中文化，关系类型以“相关、负责、归属、依赖、包含、影响”等标签展示。
- **关系地图交互增强**：关系地图支持空白区域拖拽平移、滚轮缩放、放大/缩小/1:1 复位控件；节点点击继续用于切换中心实体，拖拽逻辑不会再误吞节点点击。
- **节点完整信息提示**：关系地图节点在名称被截断时会保留完整 `title`，并提供悬浮提示数据，包含实体名称、类型、中心/关系数、实体 ID、可信度和描述等可用信息。
- **实体列表可信度完整显示**：左侧实体节点卡片为可信度徽标预留固定宽度，完整显示“可信度 0.xx”，避免被长实体名称挤压成省略号。
- **实体与关系校准弹窗化**：实体校准从页面底部嵌入区域改为独立弹窗；关系编辑也统一进入校准弹窗，浏览关系地图时不再被表单区域挤占。
- **关系明细卡片压缩**：右侧关系明细卡片合并来源、描述和操作按钮为紧凑底部行，减小内边距、行高、按钮高度和卡片间距，提高同屏关系数量；卡片悬浮可查看完整起点、关系、终点、来源、可信度和描述。
- **页面顶部与工具栏对齐修正**：知识图谱全屏页清除外层弹层默认 padding，标题行、关闭按钮和内容起点对齐知识库页面；统计标签与搜索筛选合并到同一行，减少顶部占用空间。

### 系统监控展示修复
- **慢查询与异常告警滚动修复**：系统监控页慢查询和异常告警区域补齐内部滚动约束，长列表可在卡片内部滚动查看，不再被外层布局截断。

### 文档与版本同步
- **文档同步**：README、使用手册和 CHANGELOG 已记录本轮知识图谱交互、校准弹窗、关系明细密度、顶部对齐和系统监控滚动修复。
- **版本升级**：应用版本升级至 `v0.0.73`，同步更新 `package.json`、`package-lock.json` 与 README 版本展示。
- **验证通过**：已运行 `npm run check` 与 `npm run lint`。

## [v0.0.72] - 2026-06-01
### 公告中心体验优化
- **登录页公告公开展示**：公告新增“登录页展示”开关和公开只读接口，未登录用户可在登录页查看管理员发布的全员公开公告；仅内置超级管理员 `admin` 可设置该开关，避免普通定向公告被公开泄露。
- **登录页公告布局优化**：登录页公告从登录框内移至左侧独立面板，桌面端与登录卡片分离展示，窄屏下自动收敛到登录卡片下方，避免遮挡登录表单。
- **主页公告入口迁移**：聊天主页公告入口从右下角悬浮按钮移动到左侧系统名称旁，公告中心弹层同步从品牌区附近打开，减少与输入框发送按钮和页面浮动操作的视觉冲突。
- **不再提示语义收紧**：站内公告“隐藏/不再显示”统一改为“ 不再提示 ”；普通公告点击后会记录当前用户已读并从站内公告入口、横幅和公告中心隐藏，强制确认公告在确认前不提供“不再提示”入口。

### 公告管理
- **登录页展示独立列**：公告管理表格新增“登录页”列，以“是/否”徽标展示登录页公开状态，标题列不再混排“登录页展示”标记，表格扫描更清晰。
- **权限与迁移强化**：补齐 `show_on_login` 字段迁移和索引顺序，老库升级会先补列再建索引；登录页公告仍要求全员投放且仅超级管理员可配置。
- **详情与操作增强**：公告管理表新增序号列和详情按钮，详情弹窗展示投放范围、登录页展示、确认/阅读情况、创建与更新时间；编辑、撤下、删除按钮继续按 `canEdit/canDelete` 权限控制。

### 回归验证
- **公告状态测试扩展**：公告路由测试覆盖登录页公开公告、普通管理员禁止发布登录页公告、普通公告“不再提示”后同时置为已读并从 active 列表消失、强制确认公告确认状态持久化。
- **验证通过**：已运行 `npm run check`、`npm run lint`、`node tests/security.test.js`，安全回归 `135/135` 通过。

### 版本同步
- **版本升级**：应用版本升级至 `v0.0.72`，同步记录公告中心、登录页公告、公告管理表格和“不再提示”语义优化。

## [v0.0.71] - 2026-06-01
### 公告中心
- **新增系统公告能力**：新增公告数据表、用户阅读确认状态表和完整 API，支持公告类型、优先级、发布窗口、草稿/发布/归档状态、软删除和审计记录。
- **支持定向投放**：管理员可按全员、单位、角色或指定用户发布公告；普通用户只会看到命中自己范围且处于有效时间窗口内的公告。
- **用户端通知体验**：聊天页新增顶部公告横幅、右下角公告中心和强制确认弹窗；公告支持标为已读、关闭和确认，强制确认公告会阻止用户忽略关键通知。
### 后台管理
- **新增公告管理页**：系统设置新增“公告管理”页，提供公告搜索、状态筛选、新建/编辑/删除、确认要求和投放范围配置。
- **回归测试补齐**：新增公告路由测试，覆盖管理员创建、单位定向可见性、非目标用户隔离、确认/关闭状态落库和软删除审计链路。
### 版本同步
- **版本升级**：应用版本升级至 `v0.0.71`，同步记录公告中心、公告管理页和公告测试覆盖。

## [v0.0.70] - 2026-06-01
### 部署与镜像构建
- **Compose 构建入口补齐**：`docker-compose.yml` 移除新版 Docker Compose 已废弃的 `version` 字段，并为 `pivot` 服务补充 `build.context` 与 `build.dockerfile`，确保 `docker compose build --no-cache` 能直接使用当前目录的 `Dockerfile` 构建 `pivot` 镜像。

### 模型端点本地识别
- **Docker 容器名不再误标远端**：系统监控的模型端点状态会在 Pivot 运行于 Docker/容器环境时，把 `llama-server`、`ollama` 等单段 Docker 服务名识别为本地端点，不再显示为“远端”。
- **本地别名识别增强**：保留 `PIVOT_TRUST_DOCKER_INTERNAL_HOSTS=true/false` 显式开关，新增 `PIVOT_RUNNING_IN_CONTAINER=true/false` 显式容器运行声明；同时补充本机域名解析到本机 IP 时的本地识别能力，提升公网域名、内网别名和容器网络混合部署下的监控准确性。

### RAG 上下文预算裁剪
- **用户态 RAG 注入可被预算器识别**：`fitMessagesToContextBudget` 现在会优先识别 `PIVOT_RAG_CONTEXT_BEGIN/END` 稳定边界标记，即使知识库上下文以 `role: "user"` 插入，也会按 RAG 生成型上下文优先裁剪或丢弃，而不是被误当成普通历史消息。
- **旧版 RAG system 消息继续兼容**：预算器仍保留对历史 system 形式知识库提示的识别，兼容旧会话、旧测试和可能的第三方调用链路。

### 回归验证
- **专项用例补齐**：新增 user-role RAG 上下文预算裁剪测试，以及 Docker 容器服务名本地识别测试，覆盖显式开启、显式关闭和容器运行声明场景。
- **验证通过**：已运行 `node tests/security.test.js`，`134/134` 通过。

### 版本同步
- **版本升级**：应用版本升级至 `v0.0.70`，同步记录部署构建、模型端点本地识别和 RAG 上下文预算裁剪修复。

## [v0.0.69] - 2026-06-01
### 在线更新程序移除
- **功能入口彻底清除**：移除系统设置中的“在线更新”导航、页面片段、按钮绑定、前端脚本和专用样式，避免页面继续暴露已废弃的更新入口。
- **后端链路下线**：删除 updater 相关 API 路由、客户端封装、自动轮询监控服务和启动挂载逻辑，主应用不再读取或调用 updater sidecar。
- **部署面收口**：移除 `updater/` sidecar 程序、Dockerfile、独立 package、Compose `pivot-updater` 服务、`online-update` profile、Docker socket 挂载和 updater 环境变量示例。
- **文档与检查同步**：README、CHANGELOG、`.env.example`、聊天资源检查和语法检查目录均同步移除在线更新相关说明与强制校验项，并完成全局残留扫描。

### 设置页与代码质量
- **设置页滚动条修复**：系统设置缩放画布不再把旧页面高度带到新 tab；重新测量前清理舞台高度，并对 2px 内测量误差做容差处理，减少短页面误出现垂直滚动条。
- **浏览器全局调用规范化**：知识图谱关系编辑中的 `prompt` 改为 `window.prompt`，与项目其它前端调用保持一致并修复 ESLint `no-undef`。
- **浏览器全局变量显式化**：设置页缩放脚本中的 `requestAnimationFrame`、`cancelAnimationFrame` 和 `ResizeObserver` 调用统一显式挂到 `window`，减少 lint 噪音。

### 版本与验证
- **版本升级**：应用版本升级至 `v0.0.69`，同步记录在线更新程序移除、设置页滚动修复和 lint 清理。
- **验证通过**：`npm run check`、`npm run lint` 已通过；`updater`、`在线更新`、`PIVOT_UPDATE`、`PIVOT_ONLINE`、`pivot-updater`、`online-update` 残留扫描为零。

## [v0.0.68] - 2026-06-01
### 知识图谱与 Graph-RAG
- **知识图谱三阶段落地**：新增实体、实体提及、关系三类图谱数据表与索引服务，支持从知识库分块中抽取系统、部门、流程、文档等实体和关系，并在文档重建时自动清理旧图谱数据后重新构建。
- **Graph-RAG 增强检索**：知识库召回链路会把图谱邻居关系纳入候选召回与上下文增强，命中实体时追加“参考知识图谱信息”，调试检索结果同步返回图谱上下文，便于排查召回依据。
- **企业级图谱治理接口**：新增图谱摘要、实体查询、关系查询、实体详情、实体编辑、实体合并、关系编辑、关系删除和单文档图谱重建接口，支撑后续人工校正和实体合并流程。
- **知识图谱工作台**：知识库页面新增知识图谱入口，支持实体搜索、类型筛选、实体列表、局部关系图、关系列表、实体编辑、实体合并、关系编辑删除和文档图谱重建。
- **全屏图谱体验优化**：知识图谱页面改为真正全屏承载，样式、字体、字号、搜索框、下拉框、按钮、统计标签和面板密度对齐知识库页面，适配未来大量节点的企业级查看场景。
- **刷新状态保持**：在知识图谱页面刷新浏览器后会继续停留在知识图谱页面，并尽量恢复当前文档上下文；关闭知识图谱或知识库时会清理恢复状态，避免误回到旧页面。

### 版本治理
- **单一版本锚点**：继续以 `CHANGELOG.md` 顶部最新版本标题作为运行时版本来源，并新增版本同步脚本，可自动把最新版本同步到 `package.json`、`package-lock.json` 和 README 展示版本。
- **版本升级**：应用版本升级至 `v0.0.68`，同步记录本轮 Graph-RAG、知识图谱工作台、全屏样式、刷新保持、设置页适配和版本锚点自动同步能力。

### 管理界面与弹窗适配
- **设置页比例缩放**：系统设置工作区新增固定宽度画布与视口比例缩放，宽屏保留完整信息密度，窄屏自动缩放并保持内部滚动，避免监控、审计、用量和 API 账号页在主工作区内被挤乱。
- **模型配置弹窗收口**：模型配置表单改为“内容区滚动 + 底部操作栏固定”的布局，模型能力开关、费用配置、并发与健康检查字段在不同高度窗口下都能稳定访问。
- **全局弹窗边界兜底**：统一限制弹窗和覆盖层的最大宽高与内部滚动，附件预览、监控热点接口、输入提示、重命名和标签弹窗不再在小窗口或缩放环境中溢出视口。
- **管理表格滚动修复**：审计日志、系统监控、会话搜索结果等表格区域补齐内部滚动与最小高度约束，减少底部内容不可见或横向布局被强行折叠的问题。

### 验证
- **静态检查**：`npm run check` 已通过。
- **图谱回归测试**：新增知识图谱抽取、索引、检索增强和人工治理动作测试；图谱相关用例已通过。完整 `tests/security.test.js` 在当前 Windows 沙箱中仍受既有上传目录清理 `EPERM` 影响，非本轮图谱功能回归。

## [v0.0.67] - 2026-05-31
### 聊天元数据实时补齐
- **失败消息元数据即时显示**：模型端点熔断、上游异常等 `assistant_error` 事件会立即刷新消息底部的耗时、Token 和速率，不再需要刷新页面后才显示完整元数据。
- **错误事件 token 回传**：后端保存失败消息时同步返回 `tokenCount`，前端使用该值更新当前气泡 footer，并异步写回 `/api/chat/stats` 保持刷新前后一致。
- **版本同步**：应用版本升级至 `v0.0.67`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG。

## [v0.0.64] - 2026-05-31
### Markdown 消息渲染与横向滚动
- **整段 Markdown 围栏自动去壳**：聊天渲染在遇到整条回复被外层 `markdown` / `text/plain` / `plain` 等代码围栏包住时，会先去掉外层壳再按 Markdown 渲染；流式输出过程中即使外层围栏尚未闭合，也会避免把整段正文临时显示成巨大代码块。
- **局部滚动与换行兜底**：代码块、表格和 prose 类代码块统一限制在消息气泡内部，真实代码继续保留局部横向滚动，Markdown/text 误包裹内容会自动换行，避免必须滚到块底部才能拖动横向滚动条；普通无序列表恢复项目符号，只对 checkbox 任务列表隐藏 bullet。
- **资源检查回归覆盖**：`scripts/check_chat_assets.js` 新增渲染沙盒校验，覆盖外层 Markdown 去壳、未闭合 prose 围栏、真实代码块保留、嵌入式 Markdown 示例和表格滚动容器。
### 流式输出体验优化
- **非标准上游响应回放**：当上游模型虽然请求了 `stream: true`，但实际返回完整 Chat JSON 或单个完整 message payload 时，聊天接口会提取完整回答并按 SSE 内容事件回放，避免保存空回复或只在结束时一次性展示。
- **前端大块增量平滑拆分**：聊天前端对单次返回的大段 `content` 增量做本地分块回放，首段立即显示，后续按短间隔渐进渲染；真实流式小增量仍保持原有即时刷新，长回答不会因为上游攒包而突然整段跳出。
### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.64`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG，以刷新聊天页脚本版本参数。

## [v0.0.63] - 2026-05-31
### 聊天错误持久化
- **模型失败可回看**：聊天接口在模型响应异常、流传输中断、队列/端点繁忙、已保存用户消息后的上下文超限等失败分支，会同步写入一条 `assistant` 错误消息，内容包含错误摘要、状态码/错误代码和截断后的详情，刷新会话后仍能看到失败原因。
- **前端临时气泡绑定落库消息**：聊天流收到 `assistant_error` 事件时会把当前临时助手气泡绑定到后端返回的 `messageId`，并渲染已保存的错误内容，避免刷新后只剩用户消息。
- **错误正文去状态化**：持久化错误消息只保留真实失败原因，不再追加“错误已保存、刷新可查看”等系统状态说明，避免污染会话内容。
- **回归测试**：新增聊天路由上游模型 500 失败用例，验证 SSE 返回错误消息 ID 且数据库中保留对应 assistant 错误记录。

### 长上下文模型预算修复
- **输入上限不再被默认 16K 暗中截断**：当模型配置了 `max_input_tokens`（例如 `256K`）但没有单独配置总上下文窗口时，上下文预算会以该输入上限推导有效窗口，不再被 `DEFAULT_CONTEXT_WINDOW_TOKENS=16384` 限制，避免 20K 文档附件在 256K 模型上仍提示输入过长。
- **移除固定默认上下文窗口**：未配置模型输入上限或显式总窗口时，不再套用固定 `16K` 预拦截；系统会把请求交给上游模型，由模型服务返回真实窗口限制，避免不同模型被同一个本地默认值误伤。
- **回归测试**：新增 256K 输入窗口用例，验证超过旧 16K 默认窗口的附件型长输入可通过预算检查。

### 助手回复模型标识
- **消息底部显示模型名**：助手消息的底部统计栏新增模型标识，历史消息通过 `messages.model_id` 联表展示模型名称，新生成消息直接显示当前选中的模型，便于回看每条回复由哪个模型生成。

### 上下文压缩阈值后台可配置
- **阈值从硬编码改为运行时设置**：会话上下文压缩阈值不再固定为 `MEMORY_THRESHOLD` 启动常量，而是写入 `app_settings.memory_threshold` 并由 `server/llm.js` 在上下文元信息、自动压缩判断和手动压缩判断中动态读取；`.env` / `.env.example` 中的 `MEMORY_THRESHOLD` 仅作为首次初始化默认值，后续管理员保存后即时生效。
- **管理员设置入口补齐**：新增 `server/services/memory-config.js` 统一解析 `12K`、`1M`、纯数字等 Token 表达，并限制有效范围为 `256` 到 `10M`；新增 `PUT /api/admin/settings/memory`，允许管理员按模型上下文窗口灵活调整压缩阈值，同时保持普通管理员无法修改其它全局系统设置的权限边界。
- **前端入口收紧**：模型配置页新增紧凑的“上下文阈值”输入与保存按钮，直接显示在“添加模型”按钮旁边，不再占用单独的大块横幅区域；保存成功后会刷新当前会话上下文用量展示。
- **保存按钮事件修复**：上下文阈值保存按钮移除内联 `onclick`，改为脚本事件监听，兼容当前 `script-src-attr 'none'` 的 CSP 策略，避免点击保存没有反应。
- **文档与回归测试同步**：`.env`、`.env.example` 与 README 补充中文说明，标明环境变量仅为初始化默认值；安全测试新增阈值解析与管理员保存权限用例，覆盖 `1M` 等长上下文模型场景。
### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.63`，同步更新 `package.json`、`package-lock.json`、README 与 CHANGELOG。
- **回归验证**：已运行 `npm run check`、`npm run lint`；完整 `node tests/security.test.js` 中本次新增用例已通过，现有上传/清理相关用例在 Windows 环境中仍可能因临时文件删除 `EPERM` 失败。

## [v0.0.62] - 2026-05-31
### 管理员账号自愈与安全保障
- **内置管理员账号自愈**：重构并新增了内置管理员 `admin` 账号的自愈与修复机制。在系统每次启动或运行数据预置时，会自动触发对管理员账号的检测与检查。如果发现该账号被误降级为普通用户、被禁用或被意外软删除，系统会自动触发自愈操作，重新将其恢复为正常的 `admin` 角色与 `active` 启用状态。

### Node.js v24.x 运行兼容性调优
- **better-sqlite3 升级**：将原生 SQLite 依赖包 `better-sqlite3` 由 `^9.4.3` 升级至 **`^11.3.0`**。完美适配 Node.js v24 在 Windows/Linux 平台下的运行，直接下载官方预编译二进制包，避免了全新拉取仓库时由于缺少本地 node-gyp 编译环境所导致的 `npm install` 失败以及文件锁定错误。

### 自动化安全回归测试
- **安全回归测试加固**：在 `tests/security.test.js` 中新增了 `'seed repair restores built-in admin role and login status'` 测试用例，对受损管理员账户的自愈修补、状态激活、被修改属性的恢复进行了全路径校验。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.62`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG。

## [v0.0.61] - 2026-05-29
### 智能体工作流编排升级
- **工作流库与版本治理**：工作流编排支持保存到工作流库、加载已有工作流、发布稳定版本、查看版本列表、版本差异对比、回滚历史版本和删除工作流；计划任务可优先引用已发布版本，否则使用当前画布快照，降低周期任务因草稿变动产生的不确定性。
- **画布编辑体验重构**：节点编辑从画布下方迁移为右侧抽屉，JSON 高级视图迁移为弹窗；画布页面顶部的工作流名称、工作流库、加载、版本、发布、删除等控件重新对齐，输入框尺寸和字号收紧，去除多余文本残留。
- **节点配置能力补齐**：节点支持失败策略、重试次数、超时、工具参数模板、输入 schema 提示和变量插入；任务详情中失败的 DAG 节点可单独重跑，节点输入/输出、耗时、尝试次数和条件更清晰。
- **工具选择器易用性优化**：节点工具下拉改为“中文名称 + 分类分组 + 描述提示”，按知识与会话、数据库、报表与文件、图表与展示、数据处理、文档处理、格式转换、消息通知、系统诊断和外部能力分组展示；选中后显示用途、来源、审批标签和内部工具 ID。
- **运行前状态与校验**：智能体任务面板在工作流编排模式下显示当前画布状态，非 DAG 模式隐藏工作流运行输入；运行前会阻断空工作流、未选择工具节点和非法 JSON，避免用户提交不可执行任务。
- **模板与工作流闭环**：DAG 模板会保存工作流快照、运行输入、工作流引用和版本信息，应用模板时同步恢复画布、输入和运行模式，不再只恢复目标与策略。

### 智能体功能完整性与后端契约
- **模型路由策略动态加载**：智能体模型路由下拉改为从 `/api/agents/model-routers` 动态读取，前端不再硬编码策略枚举。
- **工作流后端接口完善**：新增并完善工作流 CRUD、发布、版本列表、版本 diff、版本回滚、DAG 节点重跑、结果版本保存/对比/回滚等接口与审计动作，数据库迁移同步补齐 `agent_workflows`、`agent_workflow_versions`、模板 DAG 字段和 DAG 节点治理字段。
- **工具加载稳定性修复**：即使“可用工具”列表面板尚未渲染，工作流节点抽屉也会先加载工具缓存，修复节点工具下拉为空的问题。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.61`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、README、使用手册与 CHANGELOG。
- **回归验证**：已运行 `npm run check` 与 `git diff --check`；本地服务 `http://localhost:3000` 可达。Codex 内置浏览器因当前会话安全策略拒绝访问 localhost:3000，未进行浏览器点击级截图验证。

## [v0.0.60] - 2026-05-28
### 长会话上下文压缩链路修复
- **自动压缩同步生效**：达到上下文阈值时，模型请求不再后台 fire-and-forget 压缩后仍使用旧上下文，而是先完成压缩、重新读取消息，再组装当前这次请求的模型上下文，确保本轮调用立即使用压缩后的摘要与近期消息。
- **摘要顺序修正**：长期摘要会排在近期消息之前，避免模型先看到后续对话再看到历史摘要造成时间线混乱。
- **手动压缩状态提示优化**：手动压缩遇到已有压缩任务时，接口返回更明确的进行中/重复任务提示，前端显示 warning toast，不再误导为“没有可压缩内容”。
- **回归测试补齐**：新增自动压缩落库、旧消息软归档、当前上下文包含摘要且摘要顺序正确的回归测试，覆盖长会话首次触发压缩的关键路径。

### 刷新后停留当前页面
- **主工作区刷新恢复**：前端使用当前标签页级别的 `sessionStorage` 记录活动工作区，刷新后会恢复聊天、知识库、能力库、智能体、工作流编排、手册、设置和打印/导出工作区，而不是固定回到聊天首页。
- **聊天与打印位置恢复**：聊天页会记住当前会话，打印/导出页会记住对应会话 ID，刷新后尽量回到原会话或原打印预览。
- **设置页子 tab 恢复**：设置页会记录当前子 tab，刷新后恢复到原 tab；普通用户如果恢复到管理员专属 tab，会自动回退到可访问的模型配置页，避免权限越界。

### 会话消息操作稳定性
- **消息复制按钮兜底修复**：会话消息复制改为异步捕获剪贴板写入异常，并在剪贴板 API 不可用、非安全上下文或权限失败时回退到隐藏 textarea 复制，避免点击复制按钮时抛出未处理异常。
- **重新生成知识库命中修复**：点击助手消息的重新生成时，如果请求体没有新的用户输入，后端会从当前会话历史中取最近一条真实用户消息作为 RAG 检索词，避免原本已命中知识库的回答重新生成后显示“未命中知识库”。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.60`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。
- **回归验证**：已运行 `npm run check`、`npm run lint`、专项 ESLint 与 `node tests/security.test.js`，覆盖前端资源、语法、刷新状态恢复、消息复制、重新生成 RAG 查询和完整安全回归测试。

## [v0.0.59] - 2026-05-28
### 知识库会话回答链路优化
- **RAG 上下文注入策略修复**：普通对话启用知识库且召回命中后，不再把知识库片段追加为末尾 `system` 消息，改为包装成带强约束的知识库上下文并插入到当前用户问题之前，提升 OpenAI-compatible 网关和本地模型对知识库依据的遵循稳定性。
- **知识库回答约束增强**：知识库上下文新增明确规则，要求本轮优先依据检索结果回答，知识库与历史会话或模型记忆冲突时以知识库为准；资料不足时明确说明“知识库中未找到足够依据”，减少模型自行编造。
- **会话侧命中状态可见**：聊天流新增 `rag` 状态事件，前端会展示“知识库已命中”或“知识库未命中”，便于用户确认本轮是否真正使用了知识库，而不是只看到图标点亮。
- **监控命中率口径修正**：系统监控中的“命中率”改为展示 RAG 召回到可用知识片段的比例，不再误用缓存命中率；同时新增“缓存命中率”单独展示缓存复用情况。
- **监控统计巡检修正**：模型端点状态保留在数据概览页面；系统监控“索引分片”改为统计当前 ready、未删除且启用的可召回分片数；Prometheus HTTP 直方图 bucket 输出恢复为正确累计计数。
- **审计动作口径修正**：发送按钮点击不再把浏览器事件误传成 `regenerate=true`，后端也只接受显式 true，避免首次发送被记为“重新生成回答”。
- **回归测试补齐**：新增 RAG 会话注入顺序与强约束提示测试，覆盖知识库上下文必须紧贴当前问题、并保留引用来源提示的关键行为。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.59`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。
- **回归验证**：已运行 `npm run check`、`npm run lint` 与 `node tests/security.test.js`，覆盖前端资源、语法、Lint、知识库会话注入和完整安全回归测试。

## [v0.0.58] - 2026-05-27
### 附件上传与会话消息同步
- **图片消息即时显示修复**：发送带图片或附件的消息时，前端会在清空待发送附件前固化附件快照，并用独立的用户消息附件渲染链路展示图片和文件卡片，避免上传图片分析后会话框不显示用户消息、必须刷新才出现。
- **附件预览与消息渲染优化**：统一按 MIME 与扩展名识别图片，补齐图片 Markdown 渲染器、消息图片尺寸与加载后滚动贴底，文件附件继续以预览/下载卡片显示。
- **上传会话归属加固**：附件上传必须携带当前会话 ID，后端校验会话属于当前用户；前端切换会话时清空未发送附件，上传批次锁定初始会话，避免上传中的附件落到错误会话或污染当前输入框。
- **附件 URL 编码统一**：新增安全的附件 URL 编码方法，上传响应、附件库、管理员附件列表、会话历史补 token 统一处理空格、括号等文件名字符，并兼容旧的未编码历史 URL。
- **模型视觉解析兼容性增强**：历史上下文、视觉消息构建和模型能力判断均支持编码/旧式附件图片 Markdown，避免刷新后能显示但模型侧无法解析图片。
- **附件安全与清理**：缺失会话、无权会话或不存在会话的上传会被拒绝并清理临时文件；图片检测在 MIME 不可靠时回退到扩展名，减少浏览器或平台 MIME 差异导致的误判。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.58`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。
- **回归验证**：已运行专项 ESLint、`node tests/security.test.js`、完整 `npm test` 与本地 HTTP 首页响应检查；覆盖附件 URL 编码、会话归属、历史 token 补齐、视觉输入识别和前端资源检查。

## [v0.0.57] - 2026-05-27
### 用户注册与账号治理
- **开放注册后台开关**：新增运行时开放注册设置，内置 `admin` 可在“系统设置 > 用户管理”右上角直接开启或关闭；设置写入 `app_settings.allow_public_registration`，`.env` 中的 `ALLOW_PUBLIC_REGISTRATION` 仅作为首次初始化默认值，后续切换无需重启应用。
- **注册入口联动**：`/api/auth/config` 与登录页注册入口实时读取开放注册状态，关闭时隐藏注册入口并拦截 `/auth/register`，开启后自注册账号默认创建为普通用户。
- **注册错误提示优化**：注册、管理员添加用户、重置密码和个人改密统一使用 `UserInputError` 返回 400；密码、用户名格式、用户名重复等可预期输入错误会直接展示给用户，不再落入生产环境“系统内部错误”。
- **密码规则体验优化**：密码规则统一为“至少 8 位，并同时包含字母和数字”；注册页密码框内直接显示规则提示，前端会在提交前提示缺少长度、字母或数字的具体原因。
- **用户管理交互优化**：用户列表加载支持页码回退，开放注册开关以紧凑行内控件显示在用户操作区；导入、保存、重置密码等失败时优先展示后端返回的明确原因。

### 上下文压缩与聊天状态
- **手动压缩入口补齐**：上下文用量圆环可点击触发当前会话压缩，压缩中显示忙碌态，完成后刷新当前会话和上下文用量。
- **上下文接口增强**：会话上下文接口补齐实时 token 用量查询与手动压缩返回信息，前端可在消息保存、统计写入和压缩完成后主动刷新显示。
- **模型调用超时治理**：记忆压缩与模型调用链路加入更明确的超时、取消和错误返回，减少上游模型慢响应导致的悬挂或延迟写入。

### OpenAI 兼容接口
- **模型发现列表修复**：`/v1/models` 不再混入内置工具伪模型 `pivot-tools`，避免第三方客户端自动获取模型列表时误识别为可选大模型；内置工具能力继续通过 `/v1/tools` 与显式工具调用路径提供。

### 运营与审计体验
- **审计日志显示名补齐**：审计列表和导出内容新增显示名字段，管理员排查用户操作时不再只依赖用户名。
- **用量与 API 账户展示优化**：用量明细、API Key 与第三方调用记录继续收紧列宽、显示名、Token 统计和空态提示，提升管理端表格可读性。
- **前端文案与布局收口**：注册、账号安全、用户管理和上下文用量相关文案统一为更明确的用户侧表达，减少需要管理员猜测原因的错误提示。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.57`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。
- **回归验证**：已运行聊天资源检查、语法检查、定向 ESLint、密码校验、开放注册设置读写验证与 OpenAI 模型发现回归测试，覆盖本轮注册、用户管理、兼容接口和前端资源组装路径。

## [v0.0.56] - 2026-05-26
### 聊天流式体验与消息同步
- **用户消息显示修复**：流式回复保存完成后会同步回填用户消息与助手消息的持久化 ID，并刷新当前会话上下文用量；如果当前会话 DOM 中缺少刚发送或刚生成的消息，会自动重新加载当前会话，避免用户消息需要刷新页面才显示。
- **代码块流式滚动修复**：生成过程中会同时保持聊天列表和最新代码块贴近底部；当用户主动向上滚动查看历史时不会强制抢回视口，代码块内容会跟随最新增量显示在可见位置。
- **上下文用量实时刷新**：新增前端 `refreshCurrentContextUsage` 刷新入口，消息保存和统计写入后会重新拉取当前会话上下文元信息，避免输入框圆环长期停留在旧百分比。

### 上下文压缩交互与策略
- **手动压缩入口补齐**：输入框上下文圆环改为可点击按钮，压缩中显示忙碌态，完成后提示结果、刷新用量，并在当前会话重新加载压缩后的消息视图。
- **上下文接口补齐**：新增 `GET /api/sessions/:id/context` 查询实时上下文用量，新增 `POST /api/sessions/:id/compact` 手动压缩当前会话；手动压缩会复用当前选择模型或默认模型，并记录“手动压缩上下文”审计动作。
- **压缩超时治理**：记忆压缩默认超时从 60 秒放宽到 180 秒，超时后会取消上游模型请求并返回明确的 504 提示，避免接口已超时但模型稍后返回又继续写入摘要。
- **自动压缩触发调整**：后台压缩不再等待“活跃消息超过 10 条”，默认在活跃上下文 token 超过 `MEMORY_THRESHOLD`（当前 12K）且存在可归档早期消息时触发；`MEMORY_MIN_MESSAGES_TO_COMPRESS` 默认降为 `1`，`MEMORY_SUMMARY_KEEP_COUNT` 可配置保留最近消息数量，更适合单条超长输入的场景。
- **压缩链路兼容增强**：记忆压缩调用统一复用模型适配器生成 OpenAI-compatible URL 与鉴权头，压缩成功会返回摘要条数和摘要 token，失败会记录并向手动接口返回明确错误。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.56`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。
- **回归验证**：`npm run check` 已覆盖本轮聊天资源、语法和文本文档完整性检查。

## [v0.0.55] - 2026-05-26
### 能力库与工具链稳定性
- **能力库全链路复核**：补齐模型到能力工具调用的筛选与规划步骤，用户提出“查数据并生成图表”等复合任务时，会先暴露查询/取数能力，在查询成功后继续衔接图表生成能力，减少模型直接跳到图表或遗漏取数工具的情况。
- **图表生成链路加固**：确认 `pivot-echart` 渲染路径与聊天回答中的图表块识别逻辑可稳定工作，查询结果可以继续转为可视化图表展示。
- **用户侧术语统一**：继续清理普通对话、智能体审批、能力工作台和常见状态中的旧实现名，用户界面统一使用“能力库 / 能力服务 / 检索配置”等表达，内部兼容命名保留但不再暴露给日常使用路径。

### 知识库检索链路优化
- **召回候选合并优化**：全文检索命中不再被候选兜底逻辑覆盖，改为在已有命中基础上补充候选，提升关键词命中、向量候选和兜底候选共同参与排序的稳定性。
- **空库体验修正**：当知识库为空或没有候选分块时，不再先调用向量服务，避免把“没有可检索内容”误报成向量配置失败。
- **空文档状态修正**：未解析出可索引文本的文件会明确进入失败状态并提示“未解析出可索引文本”，不再显示为“就绪 0 分块”。
- **个人检索配置摘要修正**：知识库召回摘要会反映当前用户自己的检索配置，而不是只展示全局默认配置，便于排查个人配置下的召回表现。
- **索引超时治理**：文档入库索引使用独立的向量请求超时，默认从 30 秒放宽到 120 秒，并将底层 timeout 转换为可读的检索配置提示，减少大 PDF 或本地向量服务冷启动时的误失败。
- **检索配置文案统一**：知识库页面与相关管理入口将技术化表述收口为“检索配置 / 召回测试”，降低普通用户理解成本。

### 知识库文件格式扩展
- **新增文档类型**：知识库上传与索引支持从 `doc`、`docx`、`xls`、`xlsx`、`csv`、`json`、`html`、`htm` 中提取文本并建立分块索引，保留原有 `txt`、`md`、`pdf` 支持。
- **解析链路复用**：格式扩展接入既有文档抽取流程，而不是只放宽后缀校验，确保文件内容会先解析成可索引文本，再进入分块、索引和召回链路。

### 文档与版本同步
- **版本升级**：应用版本升级至 `v0.0.55`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。
- **工程质量收口**：补齐前端 lint 环境中的浏览器全局声明，清理能力库与智能体工作台新增代码里的未使用变量，并将 `npm test` 收紧为 `check + lint + security.test.js`，避免发版前漏跑 lint。
- **列表查询减压**：智能体任务列表改为先分页再聚合步骤/工具/错误统计，减少每行相关子查询；会话侧栏加载默认跳过总数统计，使用 `limit + 1` 与游标判断是否还有更多，保留后端 `includeTotal` 默认兼容。
- **回归验证**：`npm run check`、`npm run lint` 与 `node tests/security.test.js` 已用于覆盖本轮能力库、知识库和文件格式扩展相关回归路径。

## [v0.0.54] - 2026-05-25
### 能力库工作台升级
- **MCP 页面升级为能力库**：导航、标题、分区与治理信息统一改为“能力库 / 系统能力 / 个人能力 / 能力治理”，保留底层 MCP 兼容能力，但面向用户的表达更贴近“可调用能力”。
- **系统能力卡片化**：图表生成、报告编排、文档解析、数据处理、格式转换改为系统能力卡片，支持右上角椭圆开关、卡片内接入工具数量、工具弹窗查看名称与说明，刷新工具入口收口到工具弹窗内。
- **个人能力卡片化**：数据库连接、报表文件、消息通知与已配置的个人连接服务统一显示在个人能力区域；数据库连接卡片去掉连接地址和刷新状态噪声，已配置连接以独立卡片展示编辑、工具、删除和启停能力。
- **工具文案清理**：工具弹窗与智能体能力清单去除“内置”和多余“MCP”前缀，长工具名改为在卡片下方完整显示，悬停可查看完整信息，避免名称被徽标或卡片边界截断。
- **图表渲染兜底**：聊天回答中的 `chart` / `CHART` / `pivot-chart` 图表代码块会自动识别并渲染为图表，兼容 `data.x + data.y + config` 简化格式，避免图表生成结果退化成 JSON 代码块。
- **图表样式优化**：图表标题、图例、坐标轴名称和绘图区间距重新调整，Y 轴名称改为居中纵向显示，避免与标题或刻度文字重叠。
- **回复状态文案统一**：普通对话和智能体审批状态中的“调用 MCP 工具”改为“调用能力库工具”，避免用户在新版界面里看到旧名称。
- **能力治理合并**：去除单独“能力源”说明区，将刷新入口与活跃、异常、未检查、数据库等统计合并到治理栏，状态按钮统一白色样式并去除彩色圆点。

### 智能体工作台与工作流编排
- **工作流编排独立承载**：DAG 改名为“工作流编排”，从高级设置中移出，作为智能体任务面板上的功能按钮打开完整页面；画布去除垂直滚动条，支持鼠标平移、缩放、适配画布、小地图、节点预设、校验、草稿保存和正式保存。
- **高级设置前置**：原高级设置弹窗的运行模式、工具审批、模型路由、Token 预算、失败重试、上下文来源、补充说明等参数直接显示在任务面板中，运行参数对齐为紧凑表单，“允许能力库工具”改为椭圆轨道开关。
- **模板与计划弹窗重排**：模板库、计划执行、计划队列、通知中心重构为大尺寸多列布局，减少大面积空白，提升模板保存、定时计划和通知查看的可读性。
- **能力与结果弹窗重排**：能力清单、可用工具、结果沉淀改为更紧凑的三列视图；可用工具去重并缩小卡片，一行可显示更多工具，超出高度后独立滚动，悬停只展示完整工具信息。
- **工作流入口整理**：工作流编排、模板与计划、能力与结果三个按钮同排展示，移除高级设置里重复的工作流说明卡，降低新任务区域的操作层级。

### 任务记录表格化
- **任务列表改为数据表格**：智能体任务记录从左侧卡片列表 + 右侧详情改为统一表格，新增序号列、固定任务列宽、状态列和操作列；详情按钮打开弹窗展示原执行详情内容，删除按钮直接位于操作列。
- **字段拆分与 Token 统计**：模型、模式、步骤、工具、错误、输入 Token、输出 Token、总 Token、创建时间、状态分列展示；Token 数值统一按 K / M / B 压缩显示，顶部统计也同步使用同一格式。
- **任务检索优化**：任务状态筛选、统计胶囊和搜索框合并为一行，搜索框缩短；任务列悬停只显示被截断的任务内容，不再重复显示表格中已有字段。
- **后端分页支持**：`/api/agents/runs` 新增 `page`、`limit`、`total` 返回与状态/关键字过滤，前端按页加载任务记录，序号按页码连续计算。

### 全局表格与分页统一
- **分页组件抽取**：新增 `renderWorkspacePagination(...)` 轻量渲染函数，智能体任务记录和知识库页面复用统一分页控件。
- **统一分页样式**：全局 `.pagination`、工作区分页与系统设置相关表格分页统一按钮尺寸、间距、字体和与表格之间的距离，避免各页面分页控件大小不一。
- **知识库分页同步**：知识库列表分页改为复用全局分页渲染与样式，移除局部 `rag-page-btn` 绑定逻辑。

### 工程拆分与内置能力
- **智能体脚本拆分**：将 MCP/能力库工作台相关逻辑拆出到 `client/chat/mcp-workbench.js`，缓解 `agents.js` 体积膨胀，脚本资源检查同步增加新文件校验。
- **内置能力扩展**：补充报表文件、文档解析、数据处理、格式转换等能力服务定义和工具说明，报告编排、图表、表格等工具继续可被智能体按授权调用。
- **安全与回归覆盖**：补充能力工具、能力市场、任务记录分页和相关服务边界测试，保持 CSP 与资源引用检查覆盖新增工作区和脚本。
- **文档与版本同步**：版本升级至 `v0.0.54`，`package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG 已同步。

## [v0.0.53] - 2026-05-24
### 智能体任务历史可视化
- **任务时间轴**：任务详情面板新增水平时间轴 `.agent-timeline`，每一步显示为色块，宽度按 `duration_ms` 比例分配，最小宽度 1.5% 保证窄段也可见；色块按 `step.type` 着色 —— 规划（蓝紫）、工具（绿）、控制（橙）、审批（紫），失败步骤一律红色覆盖；悬停展示 `step_index + 工具名 + 毫秒 + 失败标记`。
- **图例**：时间轴上方展示 4 种 step 类型与失败状态的图例胶囊；色块全部使用同一套主题色，与暗色模式兼容。
- **工具调用统计**：`buildAgentToolStatsMarkup` 按 `tool_name` 聚合 `type='tool'` 步骤，统计调用次数、总耗时、平均耗时、失败次数；按调用次数和总耗时排名，渲染为带"调用次数占比"进度条的列表；存在失败时该行标红 `has-error` 并强调"N 失败"。
- **空数据兜底**：当任务还没有任何步骤，或所有 `duration_ms = 0`（异步刚启动）时仍能按等宽切分展示，不报错也不显示空白。
- **样式与主题**：在 `styles/workspaces/agent.css` 末尾新增约 150 行样式，包含 `agent-timeline` / `agent-tool-stats` 两个模块及 4 套类型色板，兼容 light / dark 主题。
- **不改动后端**：可视化全部基于已有 `getRunDetailForUser` 返回的 `steps` 数据，零新增 API、零迁移。
- **验证**：`npm run check` 全部 103 文件通过，`node tests/security.test.js` 维持 `100/100`。

### 会话打印 / PDF 导出工作区收口
- **主工作区展示**：会话“打印 / 导出 PDF”从新标签页改为主工作区 `print` 视图，通过同源 iframe 加载 `/api/sessions/:id/print?embed=1`，关闭行为与设置、MCP、智能体等工作区保持一致。
- **预览区域修复**：新增 `client/chat/partials/workspaces/print.html`，`print-frame` 复用手册工作区的全宽全高布局，避免浏览器默认 iframe 尺寸导致内容区域被压缩。
- **工具栏布局优化**：打印页嵌入模式隐藏内部标题和关闭按钮，导出时间与“打印 / 导出为 PDF”按钮同一行展示，按钮固定在右侧。
- **资源守卫**：`scripts/check_chat_assets.js` 新增打印工作区结构和样式检查，防止后续缺少 `print-frame`、主工作区注册或撑满样式。

### 工程收尾与贡献者清理
- **长期 warning 清理**：修复 DAG 编辑器浏览器全局引用、侧栏打印入口全局引用，以及无用变量、无用导入和无用参数，`npm run lint` 已零警告通过。
- **文档同步**：`使用手册.md` 将打印 / 导出 PDF 描述更新为主工作区预览，不再描述为打开新标签页。
- **Docker 时区校准**：`Dockerfile` 新增安装 `tzdata` 并设定默认时区为 `Asia/Shanghai`，同时在 `.env` 与 `.env.example` 中补充 `TZ=Asia/Shanghai` 配置，解决容器日志与东八区相差 8 小时的问题。
- **智能体步骤限制提升**：将智能体的最大步骤限制从固定的 `8` 步放宽至 `50` 步，同时将新建任务的默认步骤上限从 `5` 步调优至 `10` 步，解决了部分复杂任务执行步骤受限被截断的问题。
- **验证**：`npm run verify` 通过，包含资源检查、lint 和 `tests/security.test.js` `100/100`。

## [v0.0.52] - 2026-05-24
### 流式 function calling 实时演示面板
- **后端事件推送**：`tryRunAgentStreaming` 在每次累加器更新时通过 `callModelStreamingWithTools` 的 `onDelta` 回调以 `agent.streaming` SSE 事件推送累加快照，含 `runId / step / content / partialToolCalls / finishReason`；步骤结束追加一次 `completed: true` 的最终快照。
- **节流策略**：100ms 时间窗口或累计 ≥120 字符增量才推送一次，避免 SSE 风暴；高频 token 推送下 CPU 与网络消耗可控。
- **前端订阅**：`client/chat/agents.js` 在已有 `EventSource` 通道上新增 `agent.streaming` 事件监听，只为当前打开的任务渲染实时面板，避免后台任务覆盖前台 UI。
- **流式面板渲染**：任务详情顶部插入 `.agent-streaming-panel`，含蓝色脉冲指示器、当前 step 与 finish_reason、累加文本预览、每个 partial tool call 的名称与 arguments 字符数及前 240 字预览；任务终态 `stop` 等场景 5 秒后淡出，避免长期占据视野。
- **暗色模式适配**：所有面板背景、边框和工具卡片均使用 `var(--text-primary)`、`var(--text-muted)` 等主题变量，并在 `.dark-mode` 下切换暗色透明背景。
- **回归测试加固**：新增 1 项单测验证 `agent.streaming` 事件按用户隔离投递并携带 `runId / partialToolCalls / finishReason` 等字段；`npm test` 通过 `100/100`。
- **验证**：`npm run check` 全部 103 文件通过；旧路径（默认 `AGENT_STREAMING_TOOLS=false`）保持零行为变更，新流式分支仅在显式启用后才会推送 `agent.streaming` 事件。

## [v0.0.51] - 2026-05-24
### DAG 编辑器：缩放 / 平移 / 小地图
- **滚轮缩放**：以光标位置为锚点，缩放范围 0.3x–2.5x；缩放前后光标对应同一内容点，不会"跳动"。
- **空白处拖动平移**：在画布空白区按下后变为 `grabbing` 光标，拖动整张图；拖动节点和拖动空白互不干扰。
- **工具栏新增"适配画布"**：一键重置缩放 = 1、平移 = (0, 0)，看到完整内容。
- **右下角小地图**：固定 140x90 浮窗，缩略显示所有节点和当前视口框；点击小地图任意位置把视口中心跳到该处，便于大型 DAG 快速定位。
- **暗色模式适配**：小地图背景在 `dark-mode` 下使用半透明深色底。
- **资源回收**：destroy 时一并移除 wheel 事件、小地图 click 事件，避免泄漏。
- **验证**：`npm run check` 全部 103 文件通过，`node tests/security.test.js` 维持 `99/99`；编辑器与现有 textarea 双向同步保持原行为。

## [v0.0.50] - 2026-05-24
### 模型路由 `auto-escalate` 成本升级策略
- **新增第 6 个路由策略**：`auto-escalate` —— 第一轮按 `auto-cost` 选最便宜的可用模型尝试；任务合成最终答案后，根据置信度判断是否升级到更强模型再合成一次。
- **`server/services/model-router.js`** 新增两个工具函数：
  - `assessConfidence({ output, finishReason, minOutputChars })`：启发式判定输出是否低置信，覆盖空输出、过短输出、异常 finishReason、中英文低置信短语（"不确定"、"无法判断"、"i don't know"、"not sure" 等）。
  - `pickEscalationModel({ user, currentModel, messages })`：从用户可见模型池中挑一个严格更强的候选（价格更高或上下文窗口更大），同时保证能容纳当前输入，找不到时返回 null。
- **`agent-runtime` 接入**：`runAgent` 在最终 `synthesizeFinalAnswer` 完成后，若策略为 `auto-escalate` 且置信度判定不通过，自动调用 `pickEscalationModel` 选升级目标、写入 `chosen_model_id`、在 `agent_steps` 留下 `control` 类型步骤记录升级原因，然后用更强模型重新合成一次最终答案；升级失败保留首轮答案。
- **前端入口**：`partials/workspaces/agent.html` 的"模型路由"下拉新增"成本升级（置信不足时升级）"选项；模板加载/保存与运行时透传与其他策略一致。
- **回归测试加固**：新增 3 项单测覆盖 `auto-escalate` 在 `STRATEGIES`/`listStrategies` 中的注册、`assessConfidence` 多种低置信场景与正常输出场景；`npm test` 通过 `99/99`。
- **验证**：`npm run check` 全部 103 文件通过。

## [v0.0.49] - 2026-05-24
### agent-runtime 接入流式 function calling
- **新增 `tryRunAgentStreaming`**：在 `runAgent` 主循环里增加一条可选的流式分支，按 OpenAI tools 协议把工具调用从"回合制 JSON"升级为流式 `tool_calls` 协议。
  - 系统 prompt 引导模型通过 `tool_calls` 给出结构化参数；累加器解析后逐个执行工具，把结果作为 `role=tool` 消息喂回模型，循环直到模型直接输出最终答复。
  - 每轮规划与每次工具调用都会写入 `agent_steps`，含 `plan` 类型的累加器快照与 `tool` 类型的实际执行记录，审计与回看完全保留。
  - 工具未授权 / 未在白名单时写入 `error` 步骤但继续把错误返回给模型，让模型自行决定下一步，不直接挂任务。
  - 触发 MCP 审批暂停时，与旧分支行为一致：直接返回，等用户审批后会重新入队。
- **环境变量开关**：默认 `AGENT_STREAMING_TOOLS=false`，所有现有任务行为零改动；置为 `true` 才启用流式分支。任何**流式失败（网络、累加器异常、超时）会自动回退**到旧回合制 JSON 协议，并在 `agent_steps` 留下 `control` 步骤记录，确保任务最终能完成。
- **`run_mode = 'dag'` 永远走旧分支**：DAG 已经是图调度，不需要也不应该被流式工具替换。
- **工具列表协议化**：新增 `buildAgentToolSchemas`，把内部 `toolList` 转成 OpenAI tools schema 格式（带 `description` 与 `input_schema`），复用 v0.0.48 的 `streaming-tools.js` 工具函数。
- **.env.example 同步**：新增 `AGENT_STREAMING_TOOLS=false` 与说明注释。
- **验证**：`npm run check` 全部 103 文件通过，`node tests/security.test.js` 维持 `96/96`；旧分支单测与 v0.0.48 新增的 streaming-tools 单测全部回归。

## [v0.0.48] - 2026-05-24
### 流式 function calling 基础设施
- **新增 `server/services/streaming-tools.js`**：纯函数实现的 OpenAI streaming tool_calls 协议累加器。
  - 维护多个 `tool_calls`（按 index 索引）的 name、id、arguments 字符串增量累加；同步累加 assistant `content` 文本与 `usage` 字段。
  - `finalize()` 返回 `{ content, toolCalls, finishReason, usage, hasToolCalls, errors }`，每个 `toolCall` 同时包含 `argumentsRaw` 与已 `JSON.parse` 的 `arguments`；解析失败保留原始字符串和 `parseError`，调用方可降级处理。
  - 提供安全上限 `TOOL_CALL_LIMIT = 16`，超出后丢弃并记录错误，防止异常增量耗尽内存。
  - 兼容 legacy `function_call` delta 协议（旧版 OpenAI 接口仍可工作）。
  - 辅助函数：`buildOpenAiToolsPayload`（工具列表 → OpenAI tools 数组）、`buildAssistantToolMessage`（累加结果 → assistant message + tool_calls）、`buildToolResultMessage`（工具输出 → role=tool 消息）。
- **agent-model 新增 `callModelStreamingWithTools(modelCfg, messages, tools, options)`**：
  - 复用 `withAgentModelConcurrency` 接入全局/端点并发保护和成功失败统计。
  - 通过 `axios responseType: 'stream'` 拉取 SSE，由 `server/streaming.js` 的 `createSseEventParser` 解析逻辑帧后投喂累加器。
  - `options.onDelta(snapshot)` 回调暴露增量状态，便于前端实时渲染（待 agent-runtime 接入流程后启用）。
  - **不替换 `callModelText` / `callModelJson`**：作为可选 API 并存，agent-runtime 当前默认仍走回合制 JSON 协议，避免回归。后续可通过环境变量 `AGENT_STREAMING_TOOLS=true` 渐进切换。
- **回归测试加固**：新增 7 项单测覆盖单工具增量累加、多工具与文本混合、`arguments` JSON 解析失败降级、`TOOL_CALL_LIMIT` 边界、`function_call` 旧协议兼容、`buildOpenAiToolsPayload` 过滤无名工具、`buildAssistantToolMessage` / `buildToolResultMessage` 输出结构。
- **验证**：`npm run check` 全部 103 文件通过，`node tests/security.test.js` 通过 `96/96`（新增 7 项）。

## [v0.0.47] - 2026-05-24
### 智能体 DAG 可视化编辑器
- **新增 `client/chat/agents-dag-editor.js`**：纯原生 SVG + 拖拽实现的 DAG 可视化编辑器（约 400 行，零依赖）。
  - 节点：可拖动、双击聚焦、`is-selected` 高亮；显示标题与所选工具。
  - 边：贝塞尔曲线 + 箭头标记，依赖关系一目了然。
  - 出端口（节点右侧小圆点）拖到目标节点的入端口即可创建依赖；ghost 路径实时跟随光标。
  - 工具栏："+ 添加节点"、"自动布局"（按拓扑层次重排）、"从 JSON 同步"。
  - 详情面板：选中节点后可编辑 ID（自动迁移依赖引用）、标题、工具下拉（从 `agentToolsCache` 填充）、条件、输入 JSON、依赖勾选；输入 JSON 无效时高亮但不破坏现有数据。
- **与 `#agent-dag-spec` textarea 双向同步**：编辑器变化 → textarea 自动 stringify；用户手动改 textarea → 编辑器解析并重绘；JSON 视图改为 `<details>` 折叠形态，普通用户不会被原始 JSON 吓到，专家用户依然可以打开手改。
- **agent.html 集成**：在原 textarea 位置外包一层 `pivot-dag-editor-wrap`，注入画布、工具栏、详情面板三块容器；`agents.js` 的 `loadAgentTools()` 完成后自动 `mountAgentDagEditor()` 将工具列表喂给编辑器。
- **样式**：在 `styles/workspaces/agent.css` 末尾新增约 180 行编辑器样式，包含网格画布背景、节点/边/端口/选中态、详情面板栅格表单、依赖标签胶囊；保持原生主题变量，兼容暗色模式。
- **CSP 兼容**：所有事件均通过 `addEventListener` 绑定，无内联 onclick；幂等挂载，`mountAgentDagEditor` 重复调用会先 destroy 旧实例。
- **验证**：`npm run check` 全部 102 文件通过，`node tests/security.test.js` 维持 `89/89`。

## [v0.0.46] - 2026-05-24
### 智能体模型路由策略
- **新增 `server/services/model-router.js`**：提供 5 种模型选择策略，让智能体任务根据输入特征自动从用户可见的模型池中选模。
  - `fixed`：保持旧行为，使用任务配置的固定模型。
  - `auto-vision`：输入含图片时优先选 `supports_vision = 1` 的模型，否则按上下文匹配兜底。
  - `auto-context`：选能容纳输入但 `max_input_tokens` 最小的模型，避免大窗口模型浪费配额。
  - `auto-cost`：选 `input_price_per_million + output_price_per_million` 最低的可用模型。
  - `auto-load`：按"端点活跃数 / 端点并发上限"比值最小的候选，缓解长任务排队。
- **数据库扩展**：`agent_templates.model_router`、`agent_runs.model_router`、`agent_runs.chosen_model_id` 字段已加入 `schema.js` 和 `migrate.js`，旧库自动升级，默认值 `fixed` 保持兼容。
- **运行时接入**：`server/services/agent-runtime.js` 的 `runAgent` 在启动时按 `run.model_router` 调用 `chooseModel`，命中替换时写入 `chosen_model_id` 并插入一条 `control` 类型步骤记录路由理由；路由失败或返回原模型时回退到原配置，不影响现有任务。
- **接口与前端**：`/api/agents/model-routers` 公开 5 项策略元数据；智能体工作台"MCP 审批"旁新增"模型路由"下拉，模板加载/保存自动透传 `model_router`；`createAgentRun` 与 `agent_templates` CRUD 全链路兼容。
- **回归测试**：新增 5 项单测覆盖 `normalizeStrategy` 合法值/兜底、`listStrategies` 完整性、`estimateMessageTokens` 中英文混合估算、`hasUsableInputWindow` 边界、`modelTotalPrice` 缺字段安全；`npm test` 通过 `89/89`。
- **验证**：`npm run check` 全部 101 文件通过。

## [v0.0.45] - 2026-05-22
### 功能扩展：RAG 调试可视化与会话 PDF 导出
- **RAG 召回测试可视化增强**：`/api/rag/debug-query` 返回结果新增 `elapsedMs` 检索耗时；前端调试弹窗在原有命中列表基础上新增：
  - 关键词高亮（自动转义后包裹 `<mark>`），便于快速识别召回原因；
  - 每个候选的得分进度条，按相对最高分占比展示，方便对比相似度梯度；
  - 按源文件聚合的命中汇总条（命中数 / 候选总数 / 峰值得分 / 平均得分），帮助快速判断哪个文件主导了召回。
- **会话打印 / PDF 导出**：新增 `/api/sessions/:id/print` 打印友好 HTML 视图，使用 CSP nonce 注入脚本，遵循全局安全策略；浏览器内按 Ctrl/Cmd+P 即可"导出为 PDF"，无需服务端引入 puppeteer 等重型依赖。会话右键菜单的"导出为 Markdown"旁新增"打印 / 导出 PDF"入口。
- **文档与版本同步**：版本升级至 `v0.0.45`，`package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG 已同步。
- **验证状态**：`npm run check` 全部 100 文件通过，`node tests/security.test.js` 通过 `84/84`。

## [v0.0.44] - 2026-05-22
### 前端体验优化
- **`window.Pivot` 全局工具命名空间**：新增 `client/chat/pivot-core.js`，集中提供 `throttle` / `debounce` / `rafThrottle` / `LruCache` / `formatBytes` / `formatNumber` / `clearChildren` / `chooseStreamInterval` 等工具，向前兼容现有 `window.*` 全局函数，新代码可统一从 `window.Pivot` 调用，逐步减少分散在各模块的重复实现。
- **流式 Markdown 自适应节流**：聊天流式渲染从固定 80ms 间隔改为按累计内容长度阶梯式自适应（80 / 140 / 220 / 320ms），长回答中 `marked.parse` 的重排开销显著下降；短消息保持 80ms 视觉响应，长消息自动放缓刷新频率，避免高密度 token 推送时的卡顿。
- **加载顺序调整**：`partials/scripts.html` 在 `safe-html.js` 后新增加载 `pivot-core.js`，确保后续业务脚本可直接读取 `window.Pivot`。
- **验证状态**：`npm run check` 全部通过，`node tests/security.test.js` 维持 `84/84`。

## [v0.0.43] - 2026-05-22
### 基础设施与安全收口
- **LRU / TtlCache 工具**：新增 `server/cache.js`，提供带容量上限和 TTL 的 `LruCache` 与懒清理的 `TtlCache`；服务端 `dirSizeCache` 改用 LRU，避免长时间运行的目录尺寸缓存无限增长，符合内网长期常驻部署的内存控制要求。
- **后台启动错误日志**：GPU 监控、模型端点监控、知识库索引恢复、智能体任务恢复、智能体计划调度等启动失败的 `.catch(() => {})` 改为带 `logger.warn` 的告警，方便内网部署排查初始化失败原因，不再静默吞错。
- **MCP 调用日志脱敏**：`server/security.js` 新增 `redactSecrets` / `maskSecretString`，自动脱敏 `api_key`、`authorization`、`Bearer`、`sk-*`、JWT 等敏感字段；`server/services/mcp-client.js` 的 `previewValue` 在写入审计表前先过脱敏，避免敏感参数被普通管理员或合规导出包看到。
- **异步任务超时与并发治理**：新增 `server/services/concurrency.js` 的 `withTimeout`、`KeyedConcurrencyGuard`、`TimeoutError`；`compressMemory` 后台压缩接入超时（可通过 `MEMORY_COMPRESSION_TIMEOUT_MS` 调整）和会话级去重（同会话不重复触发），全局并发上限可通过 `MEMORY_COMPRESSION_MAX_CONCURRENT` 控制，避免长会话同时压垮上游模型。
- **MCP 审批服务端二次校验**：移除 `agent-runtime` 中 `metadata.approval === 'all_mcp_approved'` 的快捷放行死分支，只承认通过 `approveAgentTool` 写入的 `approvedTools` 白名单，杜绝后续路由或迁移意外绕过审批策略。
- **智能体运行时拆分**：从 1978 行的 `server/services/agent-runtime.js` 拆出独立模块 `server/services/agent-validators.js`，包含所有常量、`parseJsonObject`、`normalizeMaxSteps` / `normalizeApprovalPolicy` / `normalizeDagSpec` / `normalizeToolAllowlist` 等纯函数；`agent-runtime.js` 行数从 1978 降至 1834，对外导出表完全保持，路由层引用无需修改，便于后续继续拆 DAG / 调度器 / 产物子模块。
- **回归测试加固**：`tests/security.test.js` 新增 9 项单测覆盖 `LruCache` 容量与 TTL 行为、`TtlCache` 懒清理、`withTimeout` 超时与正常返回、`KeyedConcurrencyGuard` 重复键跳过与并发上限、`redactSecrets` 嵌套对象与原对象不变性、`maskSecretString` 行内脱敏；当前 `npm test` 已通过 `84/84`。

## [v0.0.42] - 2026-05-22
### 上下文预算与知识库问答稳定性
- **模型上下文预算保护**：新增统一的上下文预算服务，在请求自部署模型前按模型输入上限、输出预留和安全余量估算总上下文，避免系统提示词、历史会话、知识库 RAG 片段、MCP 结果和当前输入叠加超过 16K 窗口后才由上游返回 400。
- **知识库 RAG 自动裁剪**：普通对话启用知识库时，会优先保留当前问题和必要系统提示，并自动减少较早历史、过长知识库片段或 MCP 工具结果；仍然超限时返回明确的 `CONTEXT_LENGTH_EXCEEDED`，提示用户缩短输入、新建会话或拆分问题。
- **单条超长输入前置拦截**：当前用户输入本身超过模型可处理范围时，服务端会在写入会话历史前直接拒绝请求，避免失败内容残留到历史上下文并影响后续重试。
- **OpenAI 兼容接口同步保护**：`/v1/chat/completions` 转发前接入同一套上下文预算检查，超限时返回 `context_length_exceeded`，第三方调用方可据此区分“内容过长”和“API 配置/余额/服务异常”。
- **前端错误提示明确化**：聊天流式事件新增上下文预算状态，自动裁剪时提示“已自动减少较早历史或知识库片段后继续生成”，超限失败时展示内容过长原因，不再泛化为上游流式错误或余额配置问题。
### 验证
- **回归测试补齐**：新增上下文预算裁剪和单条超长输入拦截测试，覆盖历史、知识库片段和当前输入的关键边界；当前 `npm test` 已通过，安全测试为 `75/75`。

## [v0.0.41] - 2026-05-21
### 普通用户使用手册
- **独立用户手册**：新增 `使用手册.md`，按登录、界面、对话、附件、知识库、MCP、智能体、模型配置、第三方 API Key、状态排查、常见问题、安全建议和首次使用清单组织，重点指导普通用户完成日常操作。
- **管理员内容收口**：手册定位调整为普通用户指南，移除管理员上线清单和管理员常用操作章节，仅保留必要的“联系管理员”提示。

### 前端入口与部署可用性
- **手册入口**：左下角用户区新增“手册”按钮，用户可在应用内工作区直接查看使用说明，不再跳出到新标签页。
- **服务端手册页面**：新增 `/manual` 独立页面和 `/manual?embed=1` 嵌入页面，由服务端读取根目录 `使用手册.md` 并渲染为 HTML，保证本地运行和 Docker 部署后的访问路径一致。
- **Docker 打包保护**：`Dockerfile` 通过 `COPY . .` 带入根目录手册；`scripts/check_chat_assets.js` 新增检查，确认手册文件存在、前端入口存在、`.dockerignore` 未排除手册，避免镜像部署后页面打不开。

### 侧栏用户区体验
- **长名称显示优化**：左下角用户名称保留单行省略与完整名称悬浮提示，减少长显示名被按钮挤压后的误读。
- **管理员标识优化**：内置 `admin` 系统管理员显示绿色小盾牌图标，普通管理员显示灰色用户标识图标，二者均为无边框紧凑样式。
- **底部布局紧凑化**：用户名、手册、设置和退出保持单行布局，收紧按钮高度与版权间距，避免占用过多纵向空间。

### 文档与验证
- **版本升级**：应用版本升级至 `v0.0.41`，`package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG 已同步。
- **验证状态**：`npm run check` 已通过；临时启动当前代码验证 `/manual` 返回 200，主页面模板包含手册入口。

## [v0.0.40] - 2026-05-21
### 监控与模型状态
- **GPU 利用率显示修正**：GPU 与并发保护区域将 `nvidia-smi` 返回的核心利用率按比例正确换算为百分比，显存占用明确标记为“显存”，避免出现显存很高但利用率固定显示 0% 的误读。
- **模型端点状态收口**：系统监控页的模型端点状态只同步并展示当前仍启用的模型配置；已删除、停用或历史残留的端点运行态会被清理，不再继续出现在监控区域。

### MCP 工作台体验
- **工具缓存紧凑化**：已缓存可调用工具改为自适应卡片网格，减少单个工具占用整行造成的空白，工具来源、原始工具名和测试标识保持可见。
- **调用记录归属优化**：MCP 治理区保留全局摘要，具体调用记录移动到各自 MCP 服务卡片内展示；无调用记录时使用轻量空态提示，避免长条占位浪费空间。
- **类型下拉排序优化**：新增与编辑 MCP Server 的类型下拉统一排序为外部服务、数据库服务和内置业务模板，常用配置入口更靠前。

### 智能体工作台与运行策略
- **默认模型一致性**：智能体工作台复用主页面右上角模型选择器的可见模型、排序、当前选择和默认模型逻辑，并修复个人/全局模型文案乱码。
- **审批策略修正**：`safe_mcp_auto` 会对高风险 MCP 在执行前进行人工审批，`approve_all_mcp` 会对全部 MCP 进行审批；待审批任务会按活跃状态参与刷新和操作限制。
- **重跑与续跑约束**：运行中的任务不能直接重新运行；断点续跑会继承上一轮 DAG 配置与失败上下文，避免丢失编排信息。
- **模板与计划管理补齐**：个人模板和计划增加删除入口，减少配置残留和无效堆积。
- **导出内容补齐**：智能体任务导出补充 DAG 节点信息，便于还原 DAG 执行轨迹。
- **工具白名单边界修复**：用户取消勾选全部工具时会按“无可用工具”处理，不再误回退为全部工具可用。

### 模型配置弹窗
- **新增/编辑模型弹窗紧凑化**：模型配置弹窗压缩标题字号、说明字号、输入框高度、行距和按钮高度，更适合密集配置场景。
- **字段布局收敛**：保留两列布局，同时收小字段间距和能力开关卡片内边距，提升同屏可见字段数量。

### 文档与验证
- **版本升级**：应用版本升级至 `v0.0.40`，`package.json`、`package-lock.json`、README 与 CHANGELOG 已同步。
- **历史记录恢复**：CHANGELOG 保留并恢复 `v0.0.38` 版本记录，避免发布历史断档。
- **验证状态**：`npm run check` 与 `node tests/security.test.js` 已通过；`npm run lint` 仅保留既有的 `server/services/mcp-client.js` 未使用参数警告。

## [v0.0.39] - 2026-05-19
### 监控、MCP 工作台与智能体模型选择补丁
- **GPU 利用率显示修正**：GPU 与并发保护区域将 `nvidia-smi` 返回的核心利用率按比例正确换算为百分比，显存占用明确标记为“显存”，避免出现显存很高但利用率固定显示 0% 的误读。
- **模型端点状态收口**：系统监控页的模型端点状态只同步并展示当前仍启用的模型配置；已删除、停用或历史残留的端点运行态会被清理，不再继续出现在监控区域。
- **MCP 工具缓存布局优化**：已缓存可调用工具改为紧凑自适应卡片网格，减少单个工具占用整行造成的空白；工具来源、原始工具名和测试标识保持可见。
- **MCP 调用记录归属优化**：MCP 治理区保留全局摘要，具体调用记录移动到各自 MCP 服务卡片内展示；无调用记录时使用轻量空态提示，避免长条占位浪费空间。
- **MCP 类型下拉排序优化**：新增与编辑 MCP Server 的类型下拉统一排序为外部服务、数据库服务和内置业务模板，常用配置入口更靠前。
- **智能体默认模型一致性**：智能体工作台复用主页面右上角模型选择器的可见模型、排序、当前选择和默认模型逻辑，避免两个入口默认显示不一致。

### 权限、输入工具与用户可用性
- **三类权限边界收口**：重新梳理超级管理员、管理员、普通用户的模型、MCP、知识库、智能体、系统设置与审计能力边界；内置用户名 `admin` 作为超级管理员保留全局/共享/审计能力，管理员与普通用户默认维护个人资源。
- **聊天输入框工具开关修复**：知识库与 MCP 按钮改为点击变色的图标按钮，不再使用复选框视觉；修复管理员和普通用户无法选中知识库/MCP 的事件绑定问题，并保持本地开关状态同步。
- **MCP 使用确认优化**：普通对话启用 MCP 时保留一次性风险确认，确认结果在当前浏览器会话内复用，降低重复弹窗干扰，同时避免用户无感触发外部工具。
- **全局模型测试放开**：管理员和普通用户可对自己可见、可使用的超级管理员全局模型执行连接测试并查看状态；编辑、删除和密钥查看仍只允许模型所有者或超级管理员。
- **模型配置状态检测补齐**：模型配置页面中，非内置 `admin` 的管理员可对列表中可见的全局模型执行运维测试；普通用户按部门可见范围测试全局模型，自动状态点和手动测试按钮均复用服务端存储密钥但不暴露密钥明文。

### MCP 服务拆分与局域网工作流
- **内置 MCP 服务扩展**：新增报表与数据文件、可视化图表、报告编排、局域网消息通知四类内置 MCP 服务；服务可进入工具缓存，并被普通对话与智能体按能力组合调用。
- **独立 MCP Server 组合式流程**：报表/数据文件只负责读取和查询文件，可视化只负责生成图表或展示表格，报告编排只负责按固定格式组装内容，消息通知只负责推送到内网聊天工具，避免把完整业务流程塞进单个 MCP server。
- **图表与报告渲染链路**：普通对话可接收 `pivot-chart`、`pivot-table`、`pivot-report` 结构化结果，前端可将 MCP 生成的图表配置渲染为可视化展示，并把表格、报告 Markdown 注入最终回答。
- **数据库 MCP 生产诊断增强**：局域网数据库连接默认允许个人配置，连接拒绝、认证失败、网络不可达等错误返回更明确的状态码、主机端口与排查建议；敏感信息继续脱敏。
- **数据库连接测试提速**：数据库 MCP 点击“测试连接”时使用真实数据库驱动执行快速验证，并由 `MCP_DATABASE_TEST_TIMEOUT_MS` 控制超时，默认 10 秒返回可读诊断；正式工具查询仍使用 `MCP_DATABASE_CONNECT_TIMEOUT_MS`，避免生产环境防火墙丢包导致页面长时间等待，同时避免 TCP 预探测在容器网络中误判。
- **数据库测试链路一致性修复**：修复测试连接未把 `options.ssl`、`schema` 展开到驱动配置的问题，避免用户勾选 SSL/TLS 时测试仍按非 SSL 连接；外层测试超时增加缓冲时间，减少局域网 DNS 反向解析、握手慢或防火墙探测导致的假超时，并在错误提示中补充 MySQL `skip-name-resolve` 排查建议。
- **环境变量配置对齐**：同步 `.env` 与 `.env.example` 的配置项，补齐 `PIVOT_ADVERTISE_HOSTS`、`MCP_DATABASE_CONNECT_TIMEOUT_MS` 和 `MCP_DATABASE_TEST_TIMEOUT_MS`，保留现有生产值和密钥不被覆盖。
- **MCP 治理布局优化**：MCP 工作台治理区域改为紧凑横向状态条，展示 7 日调用、错误、平均耗时、服务状态和最近调用，避免少量内容撑高页面。

### 知识库、智能体与报告分析
- **知识库完整性复核**：确认知识库上传、索引、启停、删除、批量重建、失败重试、质量报告、调试检索、反馈与 RAG 配置链路完整可用。
- **智能体完整性复核**：确认智能体模型选择、工具清单、预检、运行、取消、重跑、断点续跑、模板、计划、通知、产物沉淀、导出与知识库工具调用链路完整可用。
- **智能体任务标题修复**：修复任务名称在异常编码或历史坏数据下显示为连续问号的问题；新建任务会自动使用目标摘要兜底，历史任务列表、悬停提示、删除审计、实时运行事件、通知、导出与产物标题也会回退到可读目标文本，并恢复任务创建、计划与运行提示的中文文案。
- **智能体运行提示链路修复**：进一步排查任务从排队进入运行中的实时事件与通知链路，`agent.run` SSE 事件不再发送原始坏标题，运行开始、状态刷新、通知 Toast 和通知列表都会过滤连续问号并回退到任务目标摘要，避免右侧顶部提示出现不可读的问号标题。
- **智能体模型排队治理**：智能体规划、总结等模型调用接入与普通聊天一致的全局 AI 并发队列和模型端点队列；当其它用户正在使用同一模型时，智能体任务会等待可用执行槽，而不是绕过队列直接请求模型并显示 503。
- **固定格式报告工作流**：支持“数据库/文件取数 → 可视化图表 → 报告编排 → 内网通知”的拆分式组合，为数据表分析、固定报告、图表和表格展示提供更清晰的扩展路径。

### 界面与监控体验
- **流式回答会话隔离修复**：修复模型正在回答时切换左侧会话，可能导致原会话前端内容被新会话渲染覆盖或统计上报到错误会话的问题；发送请求时会锁定本次生成所属会话 ID，切走后回答仍保存到原会话，完成后提示用户可切回查看。
- **侧栏宽度稳定**：修复左侧会话列表因标题字数或时间标签变化导致宽度跳动的问题，列表项和时间列保持固定收缩边界。
- **系统监控卡片完整显示**：优化 GPU 与并发保护、今日模型 Token、慢查询与异常告警等区域布局，修复显卡信息、慢查询信息、输入框和按钮对齐不完整的问题。
- **本机 GPU 语义修正**：GPU 与并发保护区域只显示 Pivot 部署服务器本机 GPU 与全局并发保护，不再混入远端模型提示；同服务器模型可通过请求主机、`PUBLIC_URL`、`CORS_ORIGIN`、`PIVOT_ADVERTISE_HOSTS` 或 `PIVOT_LOCAL_MODEL_HOSTS` 识别为本地端点。
- **慢查询告警展示优化**：慢查询与异常告警列表改为卡片式记录，左侧展示告警标题、来源、类型和级别标签，右侧独立展示耗时徽标，并将长毫秒值自动格式化为秒或分钟，避免单条告警被挤压成多行。
- **MCP 表单体验补齐**：报表、可视化、报告、消息通知和数据库 MCP 模板在工作台中可直接选择；数据库测试连接、错误提示和工具缓存刷新更贴近生产排查流程。

### 文档与验证
- **文档同步**：README 与 CHANGELOG 已同步记录本轮权限、MCP、知识库、智能体、模型配置状态检测、监控和界面优化。
- **版本升级**：应用版本升级至 `v0.0.39`，`package.json`、`package-lock.json`、README 与 CHANGELOG 已同步。
- **验证状态**：`npm test` 已通过，包含文本完整性、语法检查、聊天资源检查和安全测试 `73/73`。

## [v0.0.38] - 2026-05-19
### 权限、输入工具与用户可用性
- **三类权限边界收口**：重新梳理超级管理员、管理员、普通用户的模型、MCP、知识库、智能体、系统设置与审计能力边界；内置用户名 `admin` 作为超级管理员保留全局/共享/审计能力，管理员与普通用户默认维护个人资源。
- **聊天输入框工具开关修复**：知识库与 MCP 按钮改为点击变色的图标按钮，不再使用复选框视觉；修复管理员和普通用户无法选中知识库/MCP 的事件绑定问题，并保持本地开关状态同步。
- **MCP 使用确认优化**：普通对话启用 MCP 时保留一次性风险确认，确认结果在当前浏览器会话内复用，降低重复弹窗干扰，同时避免用户无感触发外部工具。

### MCP 服务拆分与局域网工作流
- **内置 MCP 服务扩展**：新增报表与数据文件、可视化图表、报告编排、局域网消息通知四类内置 MCP 服务；服务可进入工具缓存，并被普通对话与智能体按能力组合调用。
- **独立 MCP Server 组合式流程**：报表/数据文件只负责读取和查询文件，可视化只负责生成图表或展示表格，报告编排只负责按固定格式组装内容，消息通知只负责推送到内网聊天工具，避免把完整业务流程塞进单个 MCP server。
- **图表与报告渲染链路**：普通对话可接收 `pivot-chart`、`pivot-table`、`pivot-report` 结构化结果，前端可将 MCP 生成的图表配置渲染为可视化展示，并把表格、报告 Markdown 注入最终回答。
- **数据库 MCP 生产诊断增强**：局域网数据库连接默认允许个人配置，连接拒绝、认证失败、网络不可达等错误返回更明确的状态码、主机端口与排查建议；敏感信息继续脱敏。
- **MCP 治理布局优化**：MCP 工作台治理区域改为紧凑横向状态条，展示 7 日调用、错误、平均耗时、服务状态和最近调用，避免少量内容撑高页面。

### 知识库、智能体与报告分析
- **知识库完整性复核**：确认知识库上传、索引、启停、删除、批量重建、失败重试、质量报告、调试检索、反馈与 RAG 配置链路完整可用。
- **智能体完整性复核**：确认智能体模型选择、工具清单、预检、运行、取消、重跑、断点续跑、模板、计划、通知、产物沉淀、导出与知识库工具调用链路完整可用。
- **固定格式报告工作流**：支持“数据库/文件取数 → 可视化图表 → 报告编排 → 内网通知”的拆分式组合，为数据表分析、固定报告、图表和表格展示提供更清晰的扩展路径。

### 界面与监控体验
- **侧栏宽度稳定**：修复左侧会话列表因标题字数或时间标签变化导致宽度跳动的问题，列表项和时间列保持固定收缩边界。
- **系统监控卡片完整显示**：优化 GPU 与并发保护、今日模型 Token、慢查询与异常告警等区域布局，修复显卡信息、慢查询信息、输入框和按钮对齐不完整的问题。
- **MCP 表单体验补齐**：报表、可视化、报告、消息通知和数据库 MCP 模板在工作台中可直接选择；数据库测试连接、错误提示和工具缓存刷新更贴近生产排查流程。

### 文档与验证
- **文档同步**：README 与 CHANGELOG 已同步记录本轮权限、MCP、知识库、智能体、监控和界面优化。
- **版本升级**：应用版本升级至 `v0.0.38`，`package.json`、`package-lock.json`、README 与 CHANGELOG 已同步。
- **验证状态**：`npm test` 已通过，包含文本完整性、语法检查、聊天资源检查和安全测试 `70/70`。

## [v0.0.37] - 2026-05-17
### 工作区、缓存与部署稳定性
- **主工作区全屏稳定**：搜索、系统设置、MCP、知识库和智能体统一在右侧主工作区展示，不再覆盖左侧面板；设置页、MCP、知识库和智能体关闭后可稳定回到对话区。
- **PWA 更新边界说明**：继续保留 vendor-only 缓存策略，业务页面、`/chat/*` 脚本样式、API、版本清单和 Service Worker 交回服务端与浏览器刷新；生产部署升级后建议重建容器、刷新页面或清理站点缓存。
- **Docker 构建提示补充**：明确 `debconf: delaying package configuration, since apt-utils is not installed` 属于精简镜像中的非阻断提示；`prebuild-install` deprecated 属于上游原生依赖传递警告，不影响当前构建。

### MCP、知识库与智能体治理升级
- **MCP 治理面板**：新增 MCP 调用日志表与调用记录，工作台展示 7 日调用量、错误数、平均耗时、活跃/异常/未检查服务和近期调用明细。
- **MCP 调用来源标记**：普通对话、智能体和手动工具调用会写入来源，便于后续排查数据库 MCP、外部 MCP 和智能体任务的链路。
- **知识库质量报告**：知识库工作台新增质量概览，展示文档就绪、索引失败、反馈、召回命中和存储等指标，辅助判断 RAG 配置是否生效。
- **智能体任务预检**：创建智能体任务前会检查目标长度、模型可用性、工具范围、MCP 风险、知识库状态、DAG 节点和 Token 预算，阻断明显不可运行的任务。

### 局域网数据库 MCP 连接
- **个人局域网数据库连接**：数据库 MCP 默认允许普通用户和非内置 `admin` 管理员连接自己的局域网数据库，适配生产环境中数据库不在 Pivot 本机的部署方式。
- **可恢复旧限制**：如需恢复旧策略，可在服务端 `.env`、Docker Compose `environment` 或 `docker run -e` 中设置 `MCP_RESTRICT_PRIVATE_DATABASE_HOSTS_TO_ADMIN=true`，限制私有网段数据库主机仅管理员可配置。
- **连接测试错误可读化**：数据库连接测试失败时按验证错误、权限错误和连接失败返回 400/403/502 JSON，不再统一表现为 500；日志会避免输出密码等敏感字段。
- **连接超时治理**：PostgreSQL、MySQL、SQL Server 和 MongoDB 连接测试统一使用 `MCP_DATABASE_CONNECT_TIMEOUT_MS`，默认 10 秒，避免生产环境长时间卡住。

### 权限模型收口
- **超级管理员边界统一**：内置用户名 `admin` 作为超级管理员；非内置 `admin` 管理员默认使用自己的模型、Embedding/RAG、MCP、知识库和智能体配置。
- **MCP 与知识库个人化**：普通用户和非内置 `admin` 管理员可打开并维护自己的 MCP 与知识库，只有内置 `admin` 可管理全局/共享能力。
- **系统设置个人化**：普通用户进入设置页显示“个人设置”，默认打开个人模型配置；数据概览、系统监控、用户、审计和报表等管理员页继续受权限保护。
- **搜索个人范围**：系统内搜索继续限定在当前用户自己的会话标题、消息内容、标签和归档记录中，不暴露其他用户数据。
- **智能体共享与工具隔离**：共享智能体模板、管理员专用内置工具和全局智能体审计能力收紧到内置 `admin`；普通用户和非内置 `admin` 管理员使用个人模板、个人任务和个人指标。

### 系统监控布局
- **监控页无纵向滚动优化**：系统监控页改为明确的两行三列区域布局，压缩卡片行高和间距，避免主工作区出现不必要的垂直滚动条。
- **慢查询与异常告警完整展示**：为 GPU、系统资源、健康维护、今日模型 Token、知识库与存储、慢查询与异常告警卡片增加语义布局区域，修复“慢查询与异常告警”被挤到左下角且显示不完整的问题。

### 文档与验证
- **版本升级**：应用版本升级至 `v0.0.37`，README、CHANGELOG、`package.json` 和 `package-lock.json` 已同步。
- **验证状态**：`npm test` 已通过，包含文本完整性、语法检查、聊天资源检查和安全测试 `64/64`。

## [v0.0.36] - 2026-05-16
### 视觉与工作台细节优化
- **智能体模板库展示优化**：优化了智能体模板库中时间相关数据的可读性，解决了长日期在特定布局下被省略号截断的问题，确保时间戳和元信息完整展示。
- **系统设置界面深度精简**：
    - 移除了设置面板顶部的 Pivot Logo 和文字标题，释放了更多垂直空间给配置内容。
    - 取消了侧边栏顶部的分类标签，使导航结构更加扁平化，提升了视觉清爽度。
    - 进一步优化了各设置项的布局间距，确保在不同分辨率下的审美一致性。

### 文档与版本同步
- **版本升级**：版本升级至 `v0.0.36`，README 与 package 元数据已同步。
- **验证状态**：`npm run verify` 已通过，确认 UI 调整未影响现有功能。

## [v0.0.35] - 2026-05-16
### 智能体平台能力扩展
- **对话分支 / Fork**：消息操作栏新增“从这里分叉”入口，后端新增 `POST /api/sessions/:id/fork`，会复制源会话从开头到指定消息的上下文，写入父会话、根会话、分叉消息与备注字段，便于保留上文后探索不同方案。
- **智能体结果版本管理**：结果沉淀新增版本表与当前版本指针，支持备注、创建新版本、对比相邻版本和回滚到历史版本；智能体工作台的沉淀结果可直接打开版本弹窗进行维护。
- **慢查询与异常告警**：新增 `observability_events` 记录慢 SQL、模型端点慢响应与 RAG 慢检索，并提供管理员监控面板、事件状态接口和 webhook 告警配置。
- **智能体 DAG 编排**：智能体运行模式新增 `dag`，支持以 JSON 节点图声明依赖、条件跳过和并行工具调用，执行过程写入 `agent_dag_nodes` 并在任务详情中展示节点状态与输出。
- **插件化能力市场**：新增 `capability_packages`，把内置工具、MCP 服务和数据库连接抽象为可启停能力包；智能体工具列表、普通对话 MCP 工具和 MCP 调用入口会按能力包状态过滤。
### 文档与验证
- **更新日志同步**：本次功能开发已写入 CHANGELOG，覆盖分叉会话、结果版本、观测告警、DAG 编排和能力市场。
- **验证说明**：按用户要求不进行浏览器测试，仅执行命令行检查与自动化测试。

## [v0.0.34] - 2026-05-16
### 智能体任务记录体验修复
- **任务记录悬停信息补全**：智能体任务记录列表的悬停提示从仅显示标题扩展为标题、状态、创建/开始/完成时间、模型、模式、工具数、错误数、步骤数和目标等完整信息；同时任务卡片在鼠标悬停或键盘聚焦时展开标题与元信息，避免模型、模式、工具调用和步骤统计被单行省略。
### 侧栏与会话入口轻量化
- **侧栏工具入口重排**：将“搜索”从常驻输入框改为与“知识库 / MCP / 智能体”一致的侧栏按钮，并放入同一工具组，四个入口间距保持一致。
- **会话列表空间释放**：移除会话列表上方常驻标签汇总、搜索框、当前/归档/批量工具条，避免标签过多或工具条过宽压缩会话列表。
- **工具区视觉收口**：移除智能体入口下方多余分割线，保留轻量留白，让侧栏工具区和会话列表衔接更自然。
- **管理员身份区恢复同排布局**：底部用户名称、管理员标识、设置和退出按钮恢复同一行显示；收紧“系统管理员”和“管理员”胶囊间距，同时保留更清晰的管理员徽标样式。

### 会话搜索弹窗升级
- **独立搜索弹窗**：点击侧栏“搜索”后打开独立会话搜索弹窗，支持按标题、消息内容和 `#标签` 定位历史会话，侧栏不再承载搜索输入框。
- **当前/归档视图等高**：搜索弹窗固定整体高度，当前会话和归档会话结果区域保持一致尺寸，不再因归档数据少而缩短或跳动。
- **批量操作迁移到弹窗**：批量按钮和批量标签操作栏移动到搜索弹窗内；进入批量模式后仅弹窗结果列表显示复选框，底层侧栏会话列表不再同步进入批量状态。
- **搜索结果行与主列表一致**：搜索结果中的会话标签显示在标题前方，复用主会话列表标签样式；标题和消息条数保持同一行显示，并加大标题与消息数间距。
- **三点菜单补齐**：搜索结果行悬停时显示三点操作按钮，交互与主会话列表一致，支持重命名、编辑标签、归档/恢复、导出和删除等原有会话操作。
- **交互细节收敛**：搜索结果行悬停时隐藏时间并显示操作按钮；批量模式下隐藏三点菜单，避免选择和单条操作相互干扰。

### 工作区与设置页面体验延续
- **全屏工作区一致性**：知识库、MCP、智能体和系统设置均以右侧主区域工作区方式展示，关闭按钮尺寸和红色描边样式保持一致。
- **系统设置视觉统一**：持续优化系统设置各页面标题说明、数据表格列宽、字体、分割线间距、筛选区布局、账户安全面板和 API 接入管理信息对齐。
- **附件预览体验优化**：附件库预览窗口按钮样式统一，新标签打开入口保留在预览窗口中，文件名点击不再重复触发打开行为。

### 文档与版本同步
- **文档更新**：README 和 CHANGELOG 已记录本轮侧栏、会话搜索弹窗、批量标签、管理员标识和工作区入口优化。
- **版本同步**：应用版本升级至 `0.0.34`，同步更新 `package.json`、`package-lock.json` 与 README 当前版本说明。
- **验证状态**：`npm run verify` 已通过，包含文本完整性检查、前端资源检查、ESLint 和安全测试 `64/64`。

## [v0.0.33] - 2026-05-16
### 局域网部署与稳定性优化
- **局域网多实例可靠性**：根路径页面改为复用启动时预加载模板，减少高频访问时的同步磁盘读取；智能体队列改为数据库锁抢占，支持多实例协同、锁超时恢复和运行状态统计。
- **运行指标修正**：Prometheus 直方图 bucket 输出恢复为累计计数，便于内网监控平台正确计算分位值和告警。
- **代码职责收拢**：统一 logger 导出形态，抽出 `agent-model.js` 承载智能体模型调用与 Token 记账，新增 `number.js` 统一正整数环境变量解析。
- **数据访问去重**：复用会话消息查询语句，减少 `getMessages` 与 `getMessagesForContext` 的重复 SQL 维护成本。
- **迁移边界说明**：`migrate.js` 明确历史兼容迁移与新版 `schema_migrations` 的职责边界，后续结构升级优先使用稳定迁移 ID。
- **文本与前端安全检查**：新增 `npm run check:text` 并纳入 `npm run check`；抽出 `safe-html.js` 统一处理 HTML 转义与净化，并收紧前端 CSP 脚本策略。

### 管理后台 UI 细节优化
- **数据概览数值居中**：优化了运营概览页面顶部统计卡片的布局，将数值设置为水平居中显示，同时保持统计项名称在左上角的原始位置，提升了数据的可读性与美观度。
- **模型端点监控排列优化**：
  - 重构了模型端点状态列表的对齐逻辑，将名称与主机地址改为两端分布，解决了长 URL 导致的布局松散问题。
  - 引入了等宽字体 (Monospace) 展示主机地址，并优化了字号与颜色，使技术信息展示更加专业且层次分明。
  - 为监控项增加了平滑的悬浮过渡效果（边框色、阴影）及更大的圆角（12px），提升了管理后台的现代感。
  - 修正了状态详情（如并发、排队等）的间距与垂直对齐，确保多端点场景下的视觉一致性。

### 实时体验与附件预览
- **智能体实时事件推送**：新增 `/api/events` SSE 实时通道，推送智能体任务入队、运行、步骤变化、完成、失败、审批与通知事件；前端工作台优先使用实时事件刷新。
- **内网低依赖兜底**：实时推送基于浏览器原生 EventSource 和服务端内存订阅表实现，不新增外部运行依赖；断线或浏览器不支持时自动回退到原有轮询。
- **用户级隔离**：实时事件按登录用户订阅和投递，任务与通知只会发送给所属用户，并新增用户级 SSE 事件隔离测试。
- **附件在线预览**：新增聊天附件与附件库统一预览窗，支持图片、PDF、文本、Markdown、CSV、JSON 等常见内网文档直接查看；不支持的格式保留新标签打开入口。

### 会话标签治理
- **标签汇总与筛选**：新增标签汇总接口和多标签筛选能力，便于按项目、部门、风险等级或审计事项整理历史会话。
- **批量标签操作**：新增会话批量加标、移除标签、替换标签能力，并支持标签重命名和全局移除。
- **列表体验修正**：会话列表默认保持原有显示样式，不再常驻复选框；点击“批量”后才进入选择模式，批量操作、弹窗和提示全部中文化。
- **会话菜单交互优化**：修复会话“更多”菜单（三个点）定位偏差问题，确保菜单始终跟随按钮弹出；优化点击逻辑，点击菜单按钮时不再触发会话切换，提升操作连贯性。

### 设置页工作区化
- **右侧主区域展示**：系统设置从遮罩弹窗升级为与知识库、MCP、智能体一致的右侧工作区视图，保留左侧设置导航和内容区独立滚动。
- **返回体验统一**：设置页顶部新增工作区标题栏和“返回对话”按钮，左下角设置入口会随当前工作区高亮，整体交互更接近系统级模块。

### 导出审计与费用统计
- **合规审计包**：新增管理员 `pivot_compliance_audit.zip` 导出能力，集中打包会话清单、审计日志、模型用量明细和模型费用统计，适合内网离线留存、抽查和交付审计。
- **模型费用配置**：模型配置支持输入/输出百万 Token 单价与币种，并在数据库迁移中自动补齐 `input_price_per_million`、`output_price_per_million` 和 `price_currency` 字段。
- **费用报表与导出**：用量统计、费用明细和 CSV 导出按统一口径估算成本，便于多模型、多部门场景下核算内网资源开销。

### 文档与验证
- **文档更新**：README 和 CHANGELOG 已同步记录本轮优化、扩展、局域网部署建议和审计导出说明。
- **验证状态**：当前 `npm run verify` 已通过；文本完整性、语法检查、ESLint 和安全测试均完成，安全测试 `64/64` 通过。

## [v0.0.32] - 2026-05-16
### 输入界面与上下文 UI 深度优化
- **输入框布局重构**：将工具图标分离为左右两组，左侧为输入源配置（附件、知识库 RAG、MCP），右侧为状态与发送（上下文用量、清空输入、发送/停止），提升操作逻辑清晰度。
- **上下文用量圆环化**：移除顶栏文本显示，改为输入框内嵌入式的圆环进度条按钮。支持动态颜色反馈（正常、预警、临界）及悬浮展示详细 Token 用量信息。
- **图标标识统一**：将输入框内的 RAG 和 MCP 图标更新为与左侧导航面板一致的 SVG 路径，增强全局功能辨识度。
- **新增一键清空**：输入框右侧新增“小刷子”图标按钮，支持一键清除输入内容并重置高度；图标采用更专业的线条路径优化。
- **输入体验优化**：
  - 将输入框默认高度从 `62px` 增加到 `96px`，提供更宽敞的初始输入空间。
  - 修复了输入首个字符时因盒模型计算和过渡动画导致的输入框高度跳变问题。
  - 调整内边距，确保文字从最左侧开始输入，且多行输入时不会被底部工具图标遮挡。
- **交互细节**：将上下文按钮的鼠标指针从“问号”改为“手型”，符合通用交互直觉。

## [v0.0.31] - 2026-05-16
### 智能体工作台企业级升级
- 智能体从任务弹窗进一步升级为独立工作台，右侧主工作区集中承载任务执行、模板库、计划队列、通知中心、结果沉淀和能力清单。
- 新增 `agent_templates`、`agent_schedules`、`agent_artifacts`、`agent_notifications` 四类企业级对象，任务记录补齐模板、计划、上下文来源和断点续跑字段。
- 新增模板库 API 与前端模板条，可保存并复用目标、运行模式、MCP 策略、工具白名单、审批策略、Token 预算、失败重试和上下文来源。
- 新增计划任务 API 与服务端定时调度器，支持每日、每周和手动运行计划，到期计划自动入队并写入计划通知。
- 新增上下文来源管理，任务可选择自动、最近会话、知识库优先、自定义说明或不扩展上下文，运行时注入规划提示。
- 新增通知中心，任务完成、失败、停止、等待 MCP 审批、计划入队和结果沉淀都会生成用户级通知。
- 新增结果沉淀能力，任务最终答案或错误摘要可保存为结果资产，并在工作台中展示最近沉淀。
- 新增断点续跑入口，会基于上一轮任务状态、错误和最近失败步骤创建继续任务，避免只支持从头重跑。
- 计划任务与智能体运行继续使用当前用户可运行模型规则，用户私有模型只对所有者可见和可执行。
- 安全测试新增企业智能体闭环用例，覆盖模板、计划、上下文、结果沉淀、通知、断点续跑和用户隔离；当前 `npm run check`、`npm run lint`、`node tests/security.test.js` 均通过，安全测试 `61/61`。

### 产品体验与文档补充
- **智能体命名统一**：
  - 将早期任务类产品文案统一替换为“智能体 / 智能体任务”，覆盖左侧入口、页面标题、空态、Toast、确认弹窗、删除审计、后端错误提示、审计日志动作名和测试用例标题。
  - 保留内部 `agents` API、数据库表和运行时命名不变，避免因纯文案升级影响接口兼容性。
  - 全仓扫描确认未再命中旧产品名称，`npm run check` 与 `node tests/security.test.js` 均通过。
- **智能体工作台信息层级优化**：
  - 新任务区从“所有控制项平铺”调整为“目标、快捷任务、模型、步骤、运行”的轻量默认视图。
  - MCP、运行模式、审批策略、Token 预算、失败重试和上下文来源收纳到“高级设置”折叠区。
  - 模板库、计划执行、计划队列和通知中心收纳到“模板与计划”折叠区。
  - 能力清单、运行状态和结果沉淀收纳到“能力与结果”折叠区，保留内部滚动并移除外层任务面板的重复滚动条。
  - 修复展开“能力与结果”时挤压上方折叠标题的问题，折叠标题保持固定高度。
- **工作台返回入口增强**：
  - 智能体、MCP、知识库三个主工作台右上角“返回对话”按钮统一增强为更醒目的返回按钮。
  - 按钮增加左箭头、浅绿色强调底色、更大的尺寸和悬停态，提升从工作台回到对话窗口的可发现性。
- **知识库召回测试体验优化**：
  - 召回测试弹窗标题和说明移动到左上角，关闭按钮移动到右上角，移除底部重复关闭按钮。
  - “开始测试”点击后进入明确的运行状态：按钮变为“测试中...”、禁用重复点击，并在结果区显示正在测试召回效果的加载提示。
  - 请求完成或失败后自动恢复按钮，失败时在结果区展示可理解的失败提示。
- **README 文档恢复与合并**：
  - 参照 `0.0.27` 详细版 README 重新合并当前文档，将 README 从简化版扩展为完整说明。
  - 新版 README 补齐最新 `0.0.31` 智能体工作台、MCP、数据库 MCP、知识库 RAG、普通对话工具开关、模型能力、安全审计、运维监控、部署、环境变量、迁移和目录结构说明。
  - 修复 README / CHANGELOG 中文乱码与 BOM 问题，确认 UTF-8 读取正常且无旧产品名称残留。

## [v0.0.30] - 2026-05-16
### 企业级智能体产品化升级
- **可靠性闭环**：
  - 服务启动时自动恢复排队中的智能体任务，并将长时间无心跳的运行中任务标记为异常，避免重启后任务悬挂。
  - 新增任务级运行超时、单次工具调用超时、失败自动重试和重试次数记录。
  - 新增 `AGENT_RUN_TIMEOUT_MS`、`AGENT_TOOL_TIMEOUT_MS`、`AGENT_STALE_RUNNING_MINUTES` 等运行治理配置。
- **权限、审批与预算**：
  - 智能体任务新增 MCP 审批策略，高风险 MCP 工具可挂起等待用户批准或拒绝；数据库只读 MCP 继续支持自动执行。
  - 能力清单支持勾选工具白名单，创建任务时仅允许被选中的工具进入规划和执行范围。
  - 新增任务 Token 预算，超过预算后任务自动停止并记录原因。
- **任务模板、筛选与运营指标**：
  - 快捷任务模板可联动运行模式和 MCP 开关，支持风险总结、待办整理、资料检查等参数化入口。
  - 任务记录新增状态筛选和关键词搜索。
  - 工作台新增 7 日任务数、成功率、失败数和 Token 消耗指标。
- **审计报告与导出**：
  - 任务详情新增审批操作和导出入口，可导出 Markdown 或 JSON，包含目标、模型、步骤、工具输入输出、Token 和最终结果。
  - 导出次数写入任务记录，便于后续审计追踪。
- **验证状态**：
  - 已通过 `npm run check`。
  - 已通过 `npm run lint`。
  - 已通过 `node tests/security.test.js`，当前安全测试 `59/59` 通过。

## [v0.0.29] - 2026-05-15
### 企业级智能体运行时
- **运行队列与并发治理**：
  - 智能体任务从直接后台启动升级为受控队列，默认最多并发运行 2 个任务，可通过 `AGENT_MAX_CONCURRENT_RUNS` 调整。
  - 新增运行时状态接口 `/api/agents/runtime`，前端工作台展示当前并发、总排队数和我的排队数。
  - 任务记录补齐 `priority`、`started_at`、`last_heartbeat_at`、`completed_at` 等字段，并新增状态/优先级索引，便于后续运维排查和队列扩展。
- **工具权限与可审计边界**：
  - 智能体任务新增 `tool_policy` 和 `tool_allowlist`，后端会在规划前过滤可用工具，并在执行前二次校验工具名。
  - 工作台新增“允许 MCP 工具”开关；关闭后智能体只能使用内置只读能力，避免任务默认访问外部 MCP 或数据库。
  - 重新运行任务会继承原任务的运行模式、工具策略、工具白名单和优先级。
- **步骤审计与用量统计**：
  - 智能体步骤新增 `error_message`、`started_at`、`completed_at` 字段，计划、工具调用、控制动作和错误都保留更完整执行轨迹。
  - 智能体模型规划和总结的输入/输出/总 Token 会累计到任务记录，并继续写入统一模型用量事件。
  - 任务详情展示运行模式、工具范围、Token 用量、步骤数、工具数、错误数和耗时。
- **工作台交互增强**：
  - 新任务区新增运行模式选择：标准、深度、审计；规划提示会根据模式要求调整检索深度和证据约束。
  - 能力清单区标记工具来源为“内置”或“MCP”，帮助用户理解智能体可调用范围。
  - 并发和队列状态随自动刷新一起更新，长任务运行时状态更透明。
- **验证状态**：
  - 已通过 `npm run check`。
  - 已通过 `npm run lint`。
  - 已通过 `node tests/security.test.js`，当前安全测试 `59/59` 通过。

## [v0.0.28] - 2026-05-15
### 数据库 MCP 与普通对话工具调用
- **普通对话窗口接入 MCP**：
  - `/api/chat` 新增 MCP 工具规划流程，启用后会先判断本轮是否需要工具，再最多调用 1 个已缓存 MCP 工具，并把工具结果作为上下文注入正常流式回复。
  - 前端输入框新增轻量胶囊式 `MCP` 开关，状态持久化到本地；启用后发送前会弹出确认，避免模型在用户无感情况下访问外部 MCP 或数据库。
  - MCP 调用过程通过 SSE 返回“正在判断”“正在调用”“调用完成”“调用失败”等状态，前端会在当前助手消息中展示进度。
  - MCP 规划步骤兼容 Responses API，并在兼容服务不支持时回退到 Chat Completions。
- **统一数据库 MCP 预设**：
  - MCP 工作台新增统一的“数据库 MCP Server”预设，不再按 PostgreSQL、MySQL、SQL Server、SQLite、MongoDB 拆分多个入口；数据库类型作为连接表单字段选择。
  - 支持 PostgreSQL、MySQL/MariaDB、SQL Server、SQLite、MongoDB 五类数据库连接，新增 `pg`、`mysql2`、`mssql`、`mongodb` 依赖。
  - 数据库 MCP 服务使用 `pivot-db://` 内置协议接入现有 MCP 工具缓存和调用链路，外部 MCP Server 与内置数据库 MCP 可共用服务列表、刷新缓存和工具调用接口。
- **数据库 MCP 只读工具与安全边界**：
  - SQL 数据库提供 `db.list_tables`、`db.describe_table`、`db.run_readonly_query`；MongoDB 提供 `db.list_collections`、`db.sample_collection`、`db.aggregate`。
  - 只读 SQL 执行限制为单语句，并仅允许 `SELECT`、`WITH`、`SHOW`、`DESCRIBE`、`DESC`、`EXPLAIN` 等读取类语句；写入、DDL、管理类关键字会被拦截。
  - SQLite 文件路径限制在 `MCP_SQLITE_ROOTS` 或应用数据目录内，并以只读方式打开。
  - 新增数据库 MCP 连接测试接口，保存前和编辑时均可测试连接。
- **MCP 工作台体验优化**：
  - 已保存 MCP 服务点击“编辑”后改为弹窗编辑，不再回填到新增区域，避免用户无法取消编辑状态。
  - 编辑弹窗移除重复的顶部关闭按钮，仅保留底部取消/保存操作。
  - MCP 服务空态、缓存工具标题、工具说明和按钮文案进一步中文化；已缓存工具区域显示更贴近“可调用工具”的语义。
  - MCP 输入区按钮改为参考主流聊天工具的紧凑胶囊样式，与上传和发送按钮形成统一操作组。
- **知识库正式化与普通对话接入说明**：
  - 知识库已作为正式功能接入普通对话检索链路，不再在实验室功能中提供全局启停开关。
  - 普通对话输入区新增“知识库”胶囊开关，用户可按本轮问题决定是否检索私有文档；开关状态会在本地记忆。
  - `.env` 与 `.env.example` 移除知识库启停配置，避免用户误以为知识库仍是实验功能。
  - README 更新知识库说明，管理员仍可在知识库的 RAG 配置中调整召回阈值、命中数和候选数。
- **会话与知识库界面细节修复**：
  - 打开历史会话后自动滚动到底部，避免默认停在第一条消息。
  - 大模型回复完成、非流式返回、异常或中断后均保持底部可见，不再跳回顶部。
  - 点击左侧会话列表切换会话时不再强制刷新列表到首页，仅在新建会话等必要场景刷新侧边栏。
  - 知识库数据表底部距离、分页横线间距和操作列“详情/删除”按钮尺寸对齐系统设置页面密度。
  - 输入框工具区统一为“知识库 / MCP / 附件 / 发送”一组操作，附件按钮增加明确的悬停提示。
- **数据库迁移与启动稳定性**：
  - 修复旧数据库启动时 `agent_runs.deleted_at` 索引创建早于字段补齐导致的 `SqliteError: no such column: deleted_at`。
  - MCP 数据库连接表、服务索引和工具缓存索引迁移顺序调整为字段补齐后再创建。
- **验证状态**：
  - 已通过 `npm run check`。
  - 已通过 `npm run lint`。
  - 已通过 `node tests/security.test.js`，当前安全测试 `59/59` 通过。

## [v0.0.27] - 2026-05-15
### 智能体任务、MCP 与知识库工作台
- **左侧工具入口重构**：将“智能体”“知识库”“MCP”从传统设置面板中剥离，作为主页左侧面板中的常用工具入口；其中 MCP 入口统一命名为“MCP”，减少“设置项”和“工作工具”的语义混淆。
- **智能体任务工作台上线**：
  - 新增智能体任务弹窗，可选择模型、输入目标、设置最大步骤并后台执行任务。
  - 智能体任务支持任务列表、任务详情、执行进度、工具调用步骤、最终结果、停止任务和重新运行。
  - 任务执行结果排版改为左对齐段落化展示，优化步骤卡片、最终总结、原始数据折叠和滚动区域布局。
  - 任务记录列表与右侧详情区去重展示，减少重复标题、重复时间和重复摘要。
  - 模型选择器接入与主界面一致的模型能力图标体系，并补齐文本模型“文”能力标识。
- **智能体任务记录删除治理**：
  - 智能体任务记录新增“移除记录”操作，仅对已结束任务显示；运行中任务需先停止再移除。
  - 底层使用软删除，新增 `deleted_at`、`deleted_by_user`、`delete_reason` 字段，普通用户列表和详情会自动过滤已移除记录。
  - 内置 `admin` 超级管理员新增“删除审计”入口，可查看被移除的智能体任务记录、所属用户、模型、状态、步骤统计和删除时间。
  - 智能体记录移除会写入审计日志，后台运行器遇到已移除记录会安静退出，避免误报错误日志。
- **智能体内置工具扩展**：
  - 智能体能力清单接入知识库检索、会话检索、最近会话、知识库文档列表、可用模型列表等用户级工具。
  - 系统健康、模型运行状态等敏感工具限定为内置 `admin` 超级管理员可用，普通用户和普通管理员不可调用。
  - 智能体工具说明、任务标题、按钮和空态文案统一中文化。
- **MCP 工作台接入**：
  - 新增 MCP 服务管理弹窗，支持配置 MCP JSON-RPC 地址、API Key、说明、个人/全局共享范围，并刷新工具缓存。
  - 外部 MCP 工具进入智能体可用工具池，智能体任务可按目标选择调用。
  - 普通管理员创建 MCP 服务时默认保存为个人服务，只有内置 `admin` 超级管理员可创建全局共享服务。
- **知识库工作台优化**：
  - 知识库从系统设置页迁移为左侧独立工作台，承载文档列表、RAG 配置、召回测试、删除审计、批量重建、批量删除、失败重试和上传文档。
  - 知识库、MCP、智能体三个工具弹窗统一加宽、统一标题与说明对齐、统一关闭按钮为红色描边和红色字体。
  - 知识库列表默认每页 15 条，增加分页控件；移除冗余“文档列表/每页显示...”说明。
  - 知识库数据表继续压缩单元格行高，操作列按钮改为更紧凑样式，整体对齐系统设置页的数据表密度。

### 权限、模型配置与审计
- **超级管理员与普通管理员边界收紧**：
  - 内置用户名 `admin` 明确作为超级管理员，普通管理员不能创建全局模型、全局 Prompt、全局 MCP 服务，也不能管理其他管理员账号。
  - 普通管理员新增模型时默认保存为个人模型，可配置自己的模型接口和向量模型，不再误写成全局配置。
  - 普通管理员可以看到有权限使用的全局资源，但不能删除或覆盖超级管理员维护的全局模型和全局配置。
- **模型配置布局优化**：
  - “保存范围”和“可见部门”调整到模型名称、模型标识下方同一行，个人模型场景下可见部门提示更清晰。
  - 对话模型和向量模型均区分个人配置与系统默认配置，用户侧优先使用个人配置，未配置时回退系统默认。
  - 主界面、智能体弹窗和模型列表统一模型能力 SVG 图标与颜色样式，减少不同位置图标含义不一致的问题。
- **RAG 向量模型与 Token 统计修正**：
  - 向量模型请求纳入统一 Token 统计口径，补齐 embedding 输入/总量估算，避免总 Token 显著高于输入/输出显示值。
  - API Key 用量、模型用量和用户用量统计统一进行 token usage 归一化，确保 `total_tokens` 不小于输入与输出合计。
  - 向量服务自动登记为 usage-only 模型，便于在报表中追踪 RAG embedding 消耗。
- **审计日志中文化**：
  - 兼容并本地化历史英文审计动作，例如 `RAG_DOCUMENT_UPLOAD`、RAG 启停、删除、重建、召回反馈、向量模型连接测试等。
  - 智能体、MCP、知识库新增功能的审计动作统一使用中文，审计详情输出为中文字段说明。
  - 审计日志筛选同时兼容旧英文动作值与新中文动作名，避免历史日志无法搜索。

### 主页侧边栏与会话体验
- **侧边栏工具区重排**：品牌区下方新增智能体、知识库、MCP 工具入口，搜索行移动到工具入口下方，更符合“工具优先、搜索其次”的信息层级。
- **会话时间显示优化**：会话列表时间改为更自然的相对时间表达，避免从“1 天”一路显示到“99 天”的生硬递增。
- **整体中文化与弹窗一致性**：智能体、MCP、知识库弹窗内的按钮、说明、空态、工具名称、任务状态和错误提示统一中文显示；关闭按钮样式与系统设置弹窗保持一致。

### 测试与验证
- **安全测试覆盖扩展**：
  - 新增智能体任务 JSON 解析、工具权限、停止/重跑、软删除、超级管理员删除审计测试。
  - 新增普通管理员无法创建全局资源、无法管理管理员账号、无法保存全局向量配置等权限边界测试。
  - 新增 RAG 向量 token accounting、审计动作中文化和 embedding usage-only 模型统计测试。
- **验证状态**：当前 `npm run verify` 已通过语法检查、ESLint 和安全测试，测试通过 `58/58`。

## [v0.0.26] - 2026-05-14
### 知识库 (RAG) 交互体验与管理优化
- **乱码修复**：彻底解决了知识库上传文档时中文文件名显示为乱码的问题（已适配 `multer` 的 `latin1` 到 `UTF-8` 转码逻辑）。
- **指标卡片化**：重新设计了知识库文档详情的统计区域，采用精致的卡片化布局，并为“状态”、“进度”、“分块数”等关键指标增加了专属矢量图标，支持状态颜色感知（就绪为绿、错误为红）。
- **空间利用率提升**：
    - 优化了统计栏的栅格系统，确保 6 个关键指标在单行内整齐排列，避免跨行显示。
    - 解决了创建与更新时间在窄屏下显示不全（被省略号截断）的问题。
    - 优化了文档分块列表的布局，采用了更紧凑的字号和行高，显著提升了单屏信息密度。
- **界面文案简化**：
    - 将“知识库文档管理”标题简化为“知识库”，使界面更清爽。
    - 去除了召回测试弹窗标题中多余的 "(RAG Debug)" 标识。

## [v0.0.25] - 2026-05-14
### 界面美化与视觉体验优化
- **AI 头像升级**：将原有的六边形助理头像替换为极简、充满科技感的“星芒（Sparkle）”图标，更符合 AI 助手的现代认知风格。
- **登录页无缝融合**：通过引入 `mix-blend-mode: multiply;` 并移除强阴影轮廓，完美解决了登录页面 Logo 存在明显“白底色块”的问题，实现了 Logo 与背景卡片的纯净无缝贴合。
- **气泡消息边距修复**：优化了消息体内的 Markdown 段落排版（应用 `.message-content p:last-child { margin-bottom: 0; }`），去除了用户发送气泡和 AI 回复卡片底部多余的留白间距，使整体聊天界面更加紧凑匀称。
- **知识库中文文件名修复**：新增 `normalizeUploadedOriginalName()` 上传文件名规范化逻辑，保留正常中文文件名，并自动修复 multer/代理链路可能产生的 latin1 mojibake，避免 `knowledge_docs.name` 入库后在知识库列表显示乱码。
- **上传入口编码一致性**：RAG 知识库上传与普通附件上传统一使用同一套文件名规范化逻辑，避免一个入口修复、另一个入口二次转码的行为分叉。
- **回归测试补齐**：新增中文文件名保留/修复测试，以及知识库上传入库测试，确认 `测试文档.txt` 会以正确中文名称写入 `knowledge_docs`；当前 `npm run verify` 通过 `49/49`。

## [v0.0.24] - 2026-05-14
### 安全、维护与长期运行治理
- **后台维护闭环增强**：
  - 新增软删除存储垃圾回收任务，自动清理超过保留期的附件物理文件、知识库源文件、知识库分块、`knowledge_chunks_fts` 与历史消息全文索引，避免 `uploads/` 和 SQLite FTS 长期膨胀。
  - 统一将数据库优化职责收拢到 `server/services/maintenance.js`，移除连接层重复定时器，并在维护周期内执行 `PRAGMA optimize` 与 `PRAGMA incremental_vacuum(...)`。
  - 维护状态新增存储 GC、数据库优化、备份等任务结果，便于系统监控页和 Prometheus 持续观测。
  - 存储 GC 会记录物理文件删除结果，只有删除成功或文件已不存在时才清空数据库引用；遇到文件锁、权限异常等失败场景会保留记录，等待下一轮维护任务重试。
- **内置数据库热备份与滚动清理**：
  - 维护服务内置 SQLite `db.backup()` 热备份，默认每 24 小时生成时间戳备份到 `data/backups/`。
  - 新增 `DB_BACKUP_DIR`、`DB_BACKUP_RETENTION_DAYS`、`DB_BACKUP_MAX_VERSIONS` 配置，默认保留 7 天且最多保留最近 7 个备份版本，防止备份目录撑爆磁盘。
  - 备份文件名统一使用东八区时间并追加毫秒，同一秒内连续触发备份时会自动生成不冲突的文件名，避免热备份被覆盖。
  - Prometheus 指标新增数据库备份成功时间、备份文件大小和滚动清理文件数；监控页“健康与维护”同步显示数据库备份状态。
  - 修复手动 `scripts/backup_db.js` 的数据库入口兼容问题，手动备份和自动备份复用同一数据库连接。
- **账号与探测安全加固**：
  - 管理员强制重置用户密码时同步吊销目标用户全部 refresh token，避免被盗账号在密码重置后继续保持登录。
  - 模型连通性探测接口新增专项限流，并保证鉴权先于限流执行，降低被泄露 API Key 滥用为外部 DDoS 跳板的风险。
  - OpenAI-compatible 网关新增 `/v1/embeddings`，第三方客户端可使用 Pivot API Key 通过当前用户 RAG embedding 配置转发调用向量模型；`/v1/models` 同步暴露已配置的 embedding 模型。
  - API 接入管理页的“可用模型”同步显示向量模型，并以“聊天/向量”能力标签和目标接口区分 `/v1/chat/completions` 与 `/v1/embeddings`。
  - 向量转发接口新增独立限流，支持字符串、字符串数组和 token 数组输入，并返回标准 OpenAI embeddings 响应结构与 API Key 用量统计。
  - 出站请求 SSRF 防护覆盖本机、内网、链路本地地址、云元数据地址和受限主机别名，RAG HTTP 向量服务与外部模型探测统一走安全校验。
  - 上传接口新增专项限流，配合文档解压上限和 zip 炸弹防护，降低高频上传导致磁盘或 CPU 被耗尽的风险。

### 前端稳定性、CSP 与交互优化
- **前端事件绑定去内联化**：
  - 移除 `client/chat` 下全部 `onclick=` 内联点击事件，静态按钮改为 `data-static-action` / `data-auth-password-toggle`，由 `app.js` 统一事件委托。
  - 模型列表、提示词列表、附件列表、API Key 列表和侧边栏会话菜单全部改为 `data-*` 声明式动作绑定，避免把可执行 JavaScript 拼接进 HTML。
  - 会话更多菜单改为 DOM API 构建菜单项并绑定事件，降低标题、标签等用户数据进入事件字符串的风险，为后续收紧 CSP 打基础。
- **表格渲染安全组件化起步**：
  - 新增 `renderTableMessage()`，表格空态、加载态和错误态使用 DOM API 与 `textContent` 渲染，减少裸 `innerHTML` 字符串提示行。
  - API Key、第三方 API 调用记录、用户输入输出记录等高频表格已切换到安全提示行工具。
- **原生输入弹窗去浏览器化**：
  - 新增应用内 `showInputPrompt()` 安全输入弹窗，支持自定义标题、说明、占位符、密码类型、必填校验以及 Enter/Escape 键盘操作。
  - 管理员重置用户密码、查看模型明文密钥前的当前密码校验、加密文档上传密码输入已全部从原生 `prompt()` 迁移到统一弹窗。
  - 复扫 `client/chat` 已无原生 `prompt()` 与 `onclick/onchange/oninput` 内联事件残留，进一步降低 CSP 收紧阻力。
- **流式输出与大文本体验优化**：
  - 前端 SSE 解析升级为按标准事件边界处理，避免网络分片导致 JSON 半包被静默丢弃。
  - 长思考模型输出采用节流渲染与缓冲刷新，降低 DeepSeek-R1 等长 reasoning 场景下的主线程卡顿概率。
  - 多模态上传增加进度反馈，用户上传大 PDF 或扫描件时可感知上传过程。
  - 自定义模型选择器补齐键盘可访问性，支持方向键切换、Enter 选择和 Escape 关闭。

### RAG 与时间一致性
- **RAG 配置可调优**：
  - 实验室 RAG 配置新增 Chunk Size 和 Overlap，可按文档类型微调分块最大长度与重叠字符数。
  - RAG 参数保存后自动清空相关缓存，确保新的召回参数立即生效。
- **东八区时间一致性**：
  - 保留并继续使用 `getBeijingTimestamp()`，所有新增维护状态、审计时间、清理状态和数据库写入时间继续统一使用 Asia/Shanghai 语义。
  - 修正历史 UTC 时间补正、refresh token 过期判断和维护任务时间展示的时区一致性。

### 代码质量与验证
- **Zero Warnings 代码基线**：
  - 清理遗留 ESLint 警告，保持 `npm run lint` 零警告。
  - 新增 `npm run verify`，统一执行语法检查、ESLint 和安全测试。
- **测试覆盖扩展**：
  - 安全测试覆盖 SSRF 拦截、SSE 分片解析、存储 GC、增量 vacuum 状态、管理员重置密码令牌吊销、模型探测限流顺序、数据库热备份与滚动清理。
  - 当前验证命令：`npm run verify`，测试通过 `47/47`。

## [v0.0.23] - 2026-05-14
### 模型选择器视觉增强与能力约束
- **模型选择器视觉重构**：
  - 将原生 HTML `select` 升级为高性能自定义下拉组件，支持在触发器和列表中显示模型能力标识。
  - 模型名称旁实时展示视觉 (👁️) 与推理/思考 (🧠) 图标，图标状态随能力配置动态切换（仅在具备能力时显示）。
  - 下拉列表采用双行卡片布局，整合展示模型显示名称、私有标识、物理标识、日额度及核心能力，信息密度与视觉美感显著提升。
- **模型能力硬性约束**：
  - 建立“能力-功能”联动机制：若当前选中模型未开启视觉能力，系统将自动拦截并禁用附件上传功能，按钮切换为禁用状态并配合 `not-allowed` 指针。
  - 强化发送前校验：在消息发送环节增加二次检查逻辑，对于不支持附件处理的模型提供明确的弹窗告知，防止用户发送包含图片或文档的无效请求。
- **交互体验细节打磨**：
  - 为模型下拉列表增加平滑的淡入与位移动画效果，支持点击页面空白区域自动收起。
  - 增加模型切换时的附件兼容性预警，防止在已有附件的情况下切换到不支持附件的模型导致发送受限。
  - 优化了下拉触发器的布局，采用 `margin-left: auto` 确保切换箭头在各种名称长度下始终靠右对齐。

## [v0.0.22] - 2026-05-13
### 本轮完整优化汇总
- **RAG 文档治理升级**：知识库文档补充 `status`、`is_enabled`、分块数量、已索引分块、处理进度、错误信息、源文件路径/大小和处理时间等元数据，支持启用/停用、批量删除、批量重建、失败重试、详情查看和分块预览。
- **RAG 删除审计增强**：知识库文档删除改为软删除，保留文档元数据、分片、源文件路径和删除时间；普通用户列表与召回链路会自动排除已删除文档，内置 `admin` 超级管理员可在知识管理页查看“删除审计”记录。
- **RAG 索引与检索增强**：抽离 RAG 配置、文档管理、索引服务和缓存能力；支持并发索引、写入事务、服务重启后恢复处理中任务，并在调试查询中临时调整相似度阈值、topK 与候选数量。
- **RAG 向量配置轻量化**：将历史“云端 API / 本地 HTTP 服务”统一为 `HTTP 服务`模式，兼容云端、本机与局域网 Embedding 接口；彻底移除进程内模型解析配置，项目本身不再承担向量模型加载运行时，历史 `cloud/local` 配置值统一兼容映射为 `http`。
- **RAG 向量模型自动发现**：知识库页的向量模型配置弹窗对齐大语言模型配置体验，将 API Key 放置在 Base URL 下方，并新增“从 API 获取模型列表”能力；后端同时兼容 OpenAI-compatible `/models` 与 Ollama `/api/tags` 模型发现路径，索引请求可从 Base URL 自动补全到 `/v1/embeddings`。
- **RAG 个人向量配置**：普通用户现在可以在知识库页保存自己的 Embedding HTTP 服务配置，索引、调试召回和聊天召回优先使用个人配置，未配置时自动回退管理员系统默认；普通用户配置地址仍沿用安全校验，默认不允许内网/本机地址，避免越权探测内网服务。
- **RAG 质量闭环**：新增 `rag_feedback` 表、反馈接口与审计记录，调试结果可提交“有用/无用”反馈；RAG 上传、删除、启停、重建、批量操作和反馈均写入审计日志。
- **RAG 管理体验优化**：知识库管理页增加汇总指标、批量操作状态、调试结果明细、命中文档/分块分数展示；将 RAG 中原本依赖浏览器 `alert/confirm` 的详情与确认交互替换为系统内自定义弹窗，保持视觉风格、遮罩行为和操作按钮一致。
- **RAG 安全与限流**：调试查询接口加入速率限制，避免 embedding 服务被管理端高频压测；RAG 设置保存后自动清空缓存，确保新阈值和候选参数立即生效。
- **系统健康检查**：新增系统健康服务，`/api/health` 返回数据库、数据目录、上传目录、内存和磁盘检查结果。
- **自动维护扩展**：维护任务覆盖审计日志清理、第三方 API 调用日志清理、过期刷新令牌清理和 `PRAGMA optimize`，系统会自动记录任务结果和错误信息。
- **账号安全加固**：用户修改密码后会清理该用户全部刷新令牌，降低旧设备或旧刷新令牌继续续期的风险。
- **测试覆盖补充**：新增系统健康快照、维护任务、过期刷新令牌清理、RAG 中文检索、个人向量配置、向量模型发现和安全边界相关测试；当前 `npm test` 覆盖 70 个 JS 文件语法检查和 35 项测试。

### 系统监控面板深度优化 (Monitoring & Observability)
- **卡片式指标升级**：
  - 新增“维护任务”监控卡片，实时展示后台维护服务运行状态及审计日志保留天数。
  - 为所有核心指标（并发、Token、消息、延迟等）补齐了 SVG 矢量图标，并优化了内边距与字体比例。
  - 将消息统计细化为“今日消息数”与“15分钟活跃用户”，提升即时观测粒度。
- **资源信息透明化**：
  - 恢复并扩展了系统资源详细列表（9行），涵盖运行主机、操作系统、Node 版本、CPU 型号、系统/进程时长及负载分布。
  - 内存与磁盘空间引入动态彩色进度条（绿/琥珀/红），支持根据负载自动切换颜色，并同步显示物理路径与剩余容量。
- **运行健康与维护审计**：
  - 管理端监控面板同步展示健康与维护状态，覆盖数据库、数据目录、附件目录等核心组件的实时健康矩阵。
  - 新增后台维护任务执行记录，展示审计日志清理、API 日志清理、令牌清理和数据库优化的最近成功时间及处理条数。
- **GPU 监控专业化**：
  - 为多显卡服务器引入 GPU 序号识别（#0, #1...），并优化了显存占用与利用率的并排展示格式。
  - 监控面板顶层集成“保护状态”与“拒绝阈值”显示，并根据端点部署形态自动切换“本地模式”或“集群模式”提示语。
- **模型端点实时链路监控**：
  - 新增“模型端点运行状态”区域，展示 Host 地址、挂载模型名、并发占用/上限、排队深度、连续失败次数及熔断倒计时。
  - 引入“本地/远端”身份标识勋章，通过集群模式感知逻辑，明确告知用户当前 GPU 指标的归属范围。
- **RAG 与存储深度观测**：
  - 新增独立的“知识库与存储”监控板块，聚合展示 RAG 检索总数、缓存命中率、平均检索延迟、索引分块数、数据库大小及附件库总容量。
- **全站接口语义化映射**：
  - 极大扩充了路由名称映射表（ROUTE_NAME_MAP），为全站 API 及静态资源路径提供了精准的中文描述，彻底消除“热点接口”列表中的未知路径。
- **Prometheus 指标补齐**：指标接口增加系统健康、维护状态、RAG 检索次数、缓存命中、候选数量、命中数量、Top Score、检索耗时和入库耗时，方便生产环境持续观测。

### 性能优化 (Deep Performance Optimization)
- **数据库加速**：为 `messages`、`audit_logs`、`model_usage_events` 等核心表增加了时间戳索引，消除全表扫描隐患。
- **并发索引**：RAG 文档索引流程升级为并发处理模式（Batch Size: 5），大幅提升大型文档上传处理速度。
- **自动维护增强**：新增 `api_call_logs` 定期清理任务，默认保留 30 天，防止日志无限增长影响性能。
- **数据库事务**：在 RAG 写入过程中引入批量事务处理，降低磁盘 IO 损耗。

### UI/UX 改进
- **极简监控面板**：重新设计了系统资源展示逻辑，将信息密度提升了 250%，核心指标由 10 行精简至 4 行。
- **警示视觉重构**：采用琥珀色/蓝色标签体系替换传统红色告警，提示语精简 60% 以上，减少视觉压力。
- **响应式修复**：解决了内存进度条在小屏幕下的布局溢出问题。
- **热点接口弹窗优化**：统一了接口详情弹窗的关闭按钮样式，采用红色描边与标准字重，使其在视觉上与用户记录弹窗保持高度一致。
- **监控布局重构**：新增了独立的“知识库与存储”监控卡片，将 RAG 检索统计（次数、命中率、延迟）与磁盘存储（数据库、附件）从核心硬件区域剥离，信息分类更专业。
- **界面一致性增强**：统筹了管理后台所有弹窗操作按钮的高度（34px）与对齐规范，消除了细微的视觉跳跃感。
- **硬件监控修复**：确保并恢复了监控面板中本机 GPU 具体核心状态（显存占用、利用率）的正确显示。
- **RAG 召回测试体验重构**：将知识库页面内联的“召回测试”面板剥离为独立的专业化模态弹窗；采用彻底的行列分离 Flex 布局重构了参数面板，实现了各项参数与按钮的像素级对齐，并强化了按钮点击的缩放动画反馈。
- **向量配置安全交互**：在知识库 RAG 向量配置弹窗中引入了 API Key “明/密文切换（眼睛图标）”机制，并智能回填占位符，既保证了本地展示安全，又缓解了用户“找不到密码”的焦虑。

### 问题修复 (Bug Fixes)
- **数据概览零值修复**：修复了 `escapeHtml` 函数在处理数值 `0` 时的逻辑缺陷，确保附件数量等指标在为零时能正常显示数字 “0” 而非空白。
- **用户管理重复按钮修复**：彻底解决了因 DOM 观察器重复触发导致用户列表中“记录”按钮堆叠的问题，优化了渲染流程。
- **隐藏配置鉴权修复**：修复了向量模型配置或列表拉取时，如果前端使用星号隐藏了 API Key，点击操作会导致后端收到空值从而引发 401 的错误。现在后端会自动智能回退并继承已保存的有效密钥。
- **API 代理容灾修复**：为后端所有的 `/api` 和 `/v1` 路由增加了专属的 404 JSON 拦截器，防止 Nginx/代理层抛出 HTML 错误页面导致前端出现 `Unexpected token '<'` 致命解析异常。

## [0.0.21] - 2026-05-13

### PWA 更新机制、监管弹窗分页与系统设置交互优化
- **PWA 可控更新策略继续收紧**：
  - Service Worker 不再拦截 `navigate` 页面导航请求，避免覆盖服务端版本注入、浏览器默认离线行为和服务端缓存头策略。
  - Service Worker 保持 `vendor-only` 策略，仅缓存稳定 vendor 资源；业务页面、`/chat/*` 脚本样式、`/api/*`、`/version.json` 与 `/manifest.json` 均交回浏览器和服务端处理。
  - 保留 `/version.json`、`pwa-manager.js`、旧缓存清理与 `/pwa-reset` 兜底能力，形成可检测、可提示、可清理的更新体系。
- **系统设置关闭按钮统一优化**：
  - 系统设置主弹窗移除右上角浮动 `X`，改为侧边栏底部常驻“关闭设置”按钮，避免与数据概览、系统监控、审计报表等页面右上角操作按钮堆叠。
  - API Key 创建/显示子弹窗统一使用文字“关闭”按钮，不再使用 `X` 图标按钮。
  - 第三方 API 调用记录弹窗移除右上角浮动关闭按钮，改为弹窗头部内置“关闭”按钮，并禁止点击遮罩误关闭。
- **超级管理员监管记录分页**：
  - 超级管理员用户输入输出记录弹窗新增分页，每页默认 15 条，支持切换会话、包含已删除记录、刷新时正确重置或保持分页状态。
  - `/admin/users/:id/messages` 支持 `page`、`limit` 参数并返回 `total/page/limit`，避免一次性加载大量用户历史输入输出记录。
  - 第三方 API 调用记录弹窗明确登记分页状态，继续按每页 15 条展示，并区分输入 Token 与输出 Token。
- **监管弹窗与记录查看体验**：
  - 用户输入输出记录弹窗和第三方 API 调用记录弹窗继续保持加宽布局、内容截断、悬停查看完整内容和弹窗内关闭按钮。
  - 同步更新内联兜底脚本，降低旧缓存或懒加载脚本未及时刷新时导致记录按钮、记录弹窗或分页缺失的概率。
- **侧边栏会话列表分页优化**：
  - 侧边栏会话列表无限滚动从 `page + OFFSET` 分页升级为游标分页，后续加载通过 `nextCursor` 获取更早会话，降低新会话创建、旧会话更新置顶等场景导致的重复项或漏项风险。
  - `/sessions` 接口新增游标分页返回值 `nextCursor`，并保留旧 `page` 参数兼容历史调用。
  - 会话列表排序游标使用日期、置顶状态、更新时间和会话 ID 作为稳定依据，提升大列表滚动加载的一致性。
  - 侧边栏底部新增“加载中”“已加载全部”“暂无会话”“加载失败，请重试”等状态提示，增强无限滚动的视觉反馈。
- **版本同步**：
  - 应用版本升级至 `0.0.21`，同步更新 `package.json`、`package-lock.json`、README 当前版本说明和前端版本兜底值。

#### 验证
- 已通过 `npm test`

## [0.0.20] - 2026-05-13

### 超级管理员监管入口与记录弹窗完善
- **超级管理员用户输入输出记录**：
  - 用户管理列表为内置 `admin` 超级管理员新增“记录”按钮，用于按用户查看聊天输入、模型输出和软删除后的历史消息。
  - 用户记录弹窗支持按会话筛选、包含已删除记录，并展示时间、会话、输入/输出类型、模型、Token 和消息内容。
  - 长消息内容默认单行截断，鼠标悬停可查看完整内容，避免监管表格被长文本撑开。
- **第三方 API 调用记录可视化**：
  - API 接入管理页将超级管理员入口明确为“API 调用记录”，用于查看用户通过第三方 API Key 调用时的输入、输出与 Token 明细。
  - 第三方 API 调用记录弹窗拆分展示输入 Token 与输出 Token，保留请求内容、响应内容、状态、用户、API Key、模型和调用时间。
  - 优化调用记录弹窗宽度与内部表格布局，去除右侧大块空白，避免出现不必要的水平滚动条。
- **会话历史列表交互优化**：
  - 会话历史列表改为标题右侧直接显示相对时间，鼠标悬停时隐藏时间并显示三点操作按钮。
  - 移除列表标题前方按日期分组的重复日期展示，使会话列表更紧凑。
- **文档与版本同步**：
  - 应用版本升级至 `0.0.20`，README、package 元数据和锁文件同步更新。
  - 将本轮未完整沉淀到文档的超级管理员记录查看、第三方 API 调用记录入口和弹窗布局优化补充到更新日志。
- **PWA 可控更新体系**：
  - Service Worker 改为 `vendor-only` 策略，仅缓存稳定第三方库和图标，业务页面、`/chat/*` 脚本样式和 API 请求始终走网络。
  - 新增 `/version.json` 版本清单，前端通过 `pwa-manager.js` 检测版本变化并提示用户刷新。
  - `pwa-manager.js` 负责注册 Service Worker、检测更新、清理旧 `pivot-*` 缓存，并提供 `window.PivotPwa.reset()` 兜底清理能力。
  - 新版 Service Worker 激活时会删除历史旧 `pivot-*` 缓存，兼容旧缓存退场。
  - 新增 `/pwa-reset` 急救页面，可在浏览器仍被旧 PWA 控制时手动注销 Service Worker 并删除旧缓存。
  - 服务端对 `/chat`、`/common/styles`、`/manifest.json`、`/version.json`、`/pwa-manager.js` 和 `/sw.js` 统一发送 `no-store` 响应头，减少浏览器 HTTP 缓存与 PWA 缓存叠加导致的旧版残留。

#### 验证
- 已通过 `npm test`

## [0.0.19] - 2026-05-12

### 数据保留、超级管理员边界与 PWA 缓存修复
- **删除行为改为可监管保留**：
  - 用户删除聊天消息、聊天会话或附件时，数据库不再物理删除对应记录，而是写入软删除标记，保留原始会话、消息、附件元数据、审计日志、用量统计和用量明细。
  - 删除用户时改为禁用并标记删除状态，不再清空其历史会话、消息、附件、审计和用量数据，便于后续合规追溯。
- **`admin` 超级管理员与普通管理员分层**：
  - 仅内置用户名为 `admin` 的超级管理员可查看所有用户元数据、附件文件、已删除用户及其已删除数据。
  - 普通 `role=admin` 管理员保留用户管理、审计日志、系统监控和审计报表权限，但不开放用户具体会话内容、附件文件和已删除用户历史数据。
  - 非 `admin` 用户不能重置内置 `admin` 账户密码；前端隐藏按钮，后端同步拒绝越权请求。
- **附件库归属展示**：
  - 超级管理员进入附件库时，表格新增“用户”列，显示附件所属用户，便于跨用户审计和归属核对。
  - 附件库接口返回 `isSuperAdmin` 标识和附件所属用户名/昵称，普通用户仍只看到自己的附件。
- **PWA 缓存刷新修复**：
  - 应用版本升级至 `0.0.19`，聊天页主脚本、样式和管理端懒加载脚本追加构建修订参数，避免浏览器继续加载旧版 `extra.js` 导致附件库“用户”列不生效。
  - Service Worker 注册版本加入构建修订号，并将 `/chat/*` 与公共样式资源改为优先网络、失败回退缓存，降低 PWA 缓存卡旧界面的概率。
- **第三方 API 调用内容留痕**：
  - 新增 `api_call_logs` 表，第三方 API Key 调用 `/v1/chat/completions` 时会记录请求 `messages`、响应正文、错误信息、Token、模型、API Key、IP 和时间。
  - API 接入管理页新增“第三方 API 调用记录”，仅内置 `admin` 超级管理员可见和检索，用于特殊情况下追溯第三方客户端输入与输出。
- **超级管理员用户记录查看**：
  - 用户管理列表为内置 `admin` 超级管理员新增“记录”入口，可按用户打开输入输出记录弹窗。
  - 记录弹窗支持按会话筛选、包含已删除记录，并展示时间、会话、输入/输出类型、模型、Token 和消息内容，内容过长时截断并支持悬停查看完整值。
- **Token 与后台界面优化汇总**：
  - 数据概览、系统监控、审计报表、API 接入管理、用量统计和用量明细中的 Token 数量统一使用 `K`、`M`、`B` 等短单位展示，小于 `1000` 直接显示数字，`0` 不再显示为 `0 Tokens`。
  - 模型配置补充输入 Token 上限、输出 Token 上限、思考模型、视觉能力等参数展示，API Key 位于 Base URL 后，模型名称与模型标识同一行，可见部门单独成行。
  - 审计日志、附件库、用户管理等后台表格统一为更紧凑的数据表样式，每页默认 `15` 条，长内容截断并通过悬停查看完整值。
  - 审计报表时间筛选补充更多预设范围，并支持自定义开始日期和结束日期。

#### 验证
- 已通过 `npm test`

## [0.0.18] - 2026-05-12

### 管理后台表格、模型配置与 Token 统计统一优化
- **全站数据表样式统一**：
  - 用户管理、模型设置、审计日志、附件库、用量统计、用量明细等后台数据表统一采用更紧凑的字体、行高、单元格截断和悬停完整提示样式。
  - 用户管理、审计日志、附件库等列表默认每页显示 `15` 条数据，降低分页频率并保持表格高度一致。
  - 审计日志页面优化了标题、筛选区、表格和分页控件之间的间距，避免页面出现不必要的垂直滚动条和表格底部大块空白。
- **审计日志体验优化**：
  - 审计日志“操作”和“详情”列改为单行截断展示，鼠标悬停时显示完整内容。
  - 审计日志筛选栏和分页区域对齐附件库页面的布局节奏，提升密集日志场景下的可读性。
- **模型配置能力增强**：
  - 模型配置新增“输入 Token 上限”和“思考模型”能力开关，配合原有输出 Token 上限、视觉输入、并发上限、健康检查等参数形成完整配置。
  - 模型列表和模型选择器使用图标展示视觉、思考、文本等能力标识，不再使用“视觉”等文字标签占用空间。
  - 模型配置弹窗布局调整为：API Key 放在 Base URL 后，模型展示名称与模型标识同一行，可见部门独立整行展示，其余模型参数按两列排列。
- **Token 统计口径完善**：
  - 模型调用、用户用量统计、用量明细和第三方 API Key 调用新增输入 Token、输出 Token、总 Token 的拆分统计。
  - OpenAI 兼容接口会记录 API Key 调用的输入 Token、输出 Token 和总 Token，用于配额、报表和明细统一展示。
  - 用户删除会话时，不再丢失对应用量统计；系统会在删除前保留消息 Token 用量事件，确保历史统计和明细可追溯。
- **Token 数量显示统一**：
  - 数据概览、系统监控、审计报表、API 接入管理、用量统计和用量明细中涉及 Token 数量的展示统一使用 `K`、`M`、`B` 等短单位。
  - 移除了容易产生歧义的 `T` 后缀；小于 `1000` 的 Token 数直接显示数字，例如 `0`、`97`，不再显示 `0 Tokens`。
  - Token 趋势图和审计报表柱状图增加具体数值标注，便于直接读取每个点或每个条目的消耗量。
- **审计报表时间筛选增强**：
  - 审计报表预设范围补充为今天、近 3 天、近 7 天、近 14 天、近 30 天、近 90 天、近半年、近 1 年和近 2 年。
  - 新增自定义开始日期和结束日期筛选，支持按精确日期区间生成报表。
- **API 接入管理优化**：
  - API Key 管理表新增输入 Token 统计，并保持输入、输出、总 Token 口径一致。
  - 可用模型名称居中显示，API Key 页面说明文案精简为“你可以生成 API Key 用于在第三方应用中调用本系统的模型能力。”
- **数据保留与安全边界修复**：
  - 用户删除消息、会话或附件时改为软删除：普通用户界面隐藏，数据库保留原始会话、消息、附件元数据、文件、审计日志和用量统计。
  - 删除用户时改为禁用并标记删除，不再物理清空其历史会话、消息、附件、审计和用量数据。
  - 普通 `role=admin` 管理员保留用户管理、审计日志、系统监控和审计报表能力，但不开放用户具体会话内容、附件文件和已删除用户历史数据。
  - 只有内置用户名为 `admin` 的超级管理员可以查看所有用户元数据、附件文件、已删除用户及其已删除数据，用作低频应急监管入口。
  - 非 `admin` 用户不能对内置 `admin` 账号执行重置密码操作；前端隐藏按钮，后端同步拒绝该请求。
- **系统设置与账户安全界面优化**：
  - 系统设置弹窗中涉及模型 Token 用量的输入和展示改为带单位的格式，避免大数字撑开布局。
  - 账户安全页面的修改密码卡片居中显示，表单宽度和视觉重心更符合弹窗式后台页面布局。
- **版本与缓存同步**：
  - 应用版本号同步升级至 `0.0.18`，静态资源版本随发布刷新，降低浏览器加载旧脚本导致界面未更新的概率。

#### 验证
- 已通过 `npm run check`

## [0.0.17] - 2026-05-09

### 并发等待、视觉能力配置与模型配置弹窗优化
- **并发排队文案澄清**：
  - 聊天页排队提示从“前方还有 N 个请求，当前正在处理 X/Y 个”调整为区分“前面等待请求数”和“当前生成占用数”，避免 `前面 0 个请求` 与 `正在生成 1/1` 同时出现时产生歧义。
  - 排队事件会展示最长等待时间，便于用户判断是否继续等待。
- **排队等待时间延长**：
  - 全局 AI 队列默认超时从 `60000ms` 调整为 `300000ms`。
  - 模型端点队列新增并同步 `MODEL_ENDPOINT_QUEUE_TIMEOUT_MS=300000`，避免局域网模型长推理场景过早排队超时。
  - `.env`、`.env.example` 与 README 配置示例已同步更新。
- **模型视觉能力显式配置**：
  - `models` 表新增 `supports_vision INTEGER DEFAULT 0` 字段，启动迁移会自动为历史数据库补齐该字段。
  - 模型配置弹窗新增“视觉输入（图片/扫描件）”开关；模型列表与模型下拉会显示“视觉/文本”能力标识。
  - 聊天接口与 OpenAI 兼容 `/v1/chat/completions` 在收到图片或扫描件视觉输入时，会先检查模型是否启用视觉能力；未启用时直接返回清晰提示，不再把视觉请求盲目转发给上游。
  - 明确区分普通文档与视觉输入：Word、Excel、文本型 PDF 会先抽取文本，仍可交给文本模型处理；图片与扫描 PDF 兜底图片才依赖视觉能力开关。
- **模型配置弹窗布局优化**：
  - “添加/编辑模型配置”弹窗宽度扩展至 `820px` 自适应布局，并改为两列网格，减少纵向高度。
  - 保留“模型标识”等长输入整行展示，避免按钮和输入框被挤压。
- **部署升级文档补充**：
  - README 增加容器部署后确认新镜像、新代码和数据库字段迁移的检查命令。
  - 补充 `docker compose down && docker build -t pivot . && docker compose up -d` 的重建流程，以及 `PRAGMA table_info(models)` 字段检查命令。

#### 验证
- 已通过 `npm run check`
- 已通过 `npm test`

## [0.0.16] - 2026-05-08

### 认证时效、检查链路与部署文档修复
- **Refresh Token 过期时间统一**：刷新令牌的 `expires_at` 改为使用 `getBeijingTimestamp(expiresAt)` 写入，和过期校验使用同一时区格式，避免实际有效期比配置提前约 8 小时结束。
- **Lint 配置补齐**：在 `eslint.config.js` 的 Node 全局变量声明中加入 `setImmediate`，修复测试文件通过但 `npm run lint` 被 `no-undef` 阻断的问题。
- **OpenAI 兼容接口用量统计补齐**：`/v1/chat/completions` 命中“不支持能力”兜底响应时，现在同样会记录 `model_usage_events`，并在 API Key 调用场景累加 `usage_tokens`，保证配额和统计口径一致。
- **容器部署说明补充**：README 离线部署步骤加入 `docker network create ai-bridge`，避免外部网络未预创建时 `docker-compose up -d` 启动失败。

#### 验证
- 已通过 `npm run lint`
- 已通过 `npm test`
- 已通过 `npm audit --audit-level=moderate`

## [0.0.15] - 2026-05-08

### API 接入优化与 UI 细节打磨
- **模型标识标准化**：
  - 优化了 `openai.js` 兼容接口，将外部调用标识 (`id`) 统一为语义化的模型名称 (`model_name`)，使第三方客户端接入更直观。
  - 在“API 接入”面板新增“可用模型”列表展示，方便用户快速获取外部调用所需的模型 ID。
- **并发排队体验增强**：
  - 聊天接口现在会在进入并发控制前先建立 SSE 连接，用户请求进入队列时可立即收到排队状态，而不是长时间停留在等待响应。
  - 前端聊天气泡新增“正在排队，前方还有 N 个请求”提示，并在首次排队时弹出提示；全局 AI 队列与模型端点队列都会通过统一的 `queue` 事件反馈等待与就绪状态。
  - 队列满、排队超时、模型端点繁忙等 503 场景会保留并展示后端可读错误文案，避免生产环境只显示泛化的“服务器内部错误”或“服务器拒绝 (503)”。
  - `ConcurrencySemaphore` 新增排队回调元数据（队列位置、前方请求数、队列上限、超时时间等），并补充单测覆盖队列位置回传。
- **默认并发保护收紧**：
  - 全局 AI 并发默认值从 `5` 调整为 `1`，`.env` / `.env.example` / README 示例统一为 `MAX_CONCURRENT_AI_REQUESTS=1`。
  - 模型端点默认并发从 `2` 调整为 `1`，新增 `MODEL_ENDPOINT_DEFAULT_CONCURRENCY=1` 配置说明；模型配置中 `max_concurrent=0` 继续表示“跟随系统默认”。
- **登录与安全增强**：
  - 登录及注册页面新增“眼睛”图标，支持密码明文切换显示。
  - 优化了 `handleUnauthorized` 逻辑，仅清除认证 token 而不再清空全部 `localStorage`，保留用户主题等偏好设置。
  - 登录态切换时会同步刷新用户信息、管理员入口、上下文用量和模型选择器，减少登录后模型列表未初始化或管理员控件未显现的问题。
- **日期时间显示增强**：
  - **消息时间戳**：聊天对话中的每条消息现在都会显示发送/接收时间。AI 回复的时间戳与性能统计（Token/TPS）优雅整合在页脚显示。
  - **智能格式化**：引入了符合中文习惯的智能日期格式化（如“今天 10:45”、“昨天”、“5月8日”等），并统一使用 `Asia/Shanghai` 时区。
- **会话列表分组**：
  - 侧边栏会话列表新增按日期分组显示（如“今天”、“昨天”、“本周”等），提升了长列表场景下的会话检索效率。
  - 会话项悬浮提示（Tooltip）现在会显示详细的更新时间及消息条数，信息展示更丰富。
- **管理面板交互升级**：
  - **API Key 弹窗优化**：扩展了密钥生成弹窗宽度至 `550px`，并为长密钥增加了水平滚动支持和更明显的复制按钮。
  - **视觉细节修复**：将实验室功能的 `Experimental` 标签汉化为“实验性”，并优化了监控页部分样式的对齐。
  - **模型选择器稳定性**：统一模型下拉刷新逻辑，选项说明改为中文，并在无可用模型、默认模型恢复、管理员过滤等场景下提供更稳定的状态展示。
- **后端健壮性提升**：
  - 改进了转发 OpenAI 请求时的 URL 拼接逻辑，自动处理路径末尾斜杠及重复的 `/v1` 片段。
  - 为转发请求添加了标准的 `User-Agent` 及更完善的 Header 校验。

## [0.0.14] - 2026-05-07

### 版本与缓存机制优化
- 将应用版本号来源统一为 `CHANGELOG.md` 顶部最新版本标题，后端启动时自动读取并注入前端页面，后续发版优先只维护更新日志版本号。
- 聊天页静态资源版本参数改为 `__APP_VERSION__` 模板，由服务端按当前版本替换，避免 `chat.html`、`config.js`、`sw.js` 多处手工同步。
- Service Worker 注册地址追加当前版本参数，并让 `/`、`/chat/chat.html`、`/sw.js` 返回禁用缓存响应头，降低局域网生产环境被 PWA 或浏览器旧缓存卡住的概率。
- 同步记录本轮审计报表、系统监控页长名称展示与标签靠左对齐等界面调整，确保 0.0.14 打包后可见变更完整落档。
- **环境依赖升级**：将 Docker 基础镜像升级至 Node.js 22 LTS，并明确 `package.json` 中的 `engines` 要求，以解决 ESLint 10 依赖包 Espree 的引擎版本警告。

## [0.0.13] - 2026-05-07

### 优化与增强
- **审计报表可视化深度优化**：
  - **动态标签宽度计算**：引入 `ctx.measureText` 动态计算最长部门/用户名，并自动调整图表左侧边距（140px - 180px 动态适配），彻底解决长名称遮挡或空间浪费问题。
  - **对齐与布局调整**：标签改为右对齐并紧贴进度条，配合 `fillText` 的最大宽度限制，确保在极端长名称下依然保持布局整齐。
  - **渲染性能提升**：移除了硬编码的字符串截断逻辑，改为更优雅的 Canvas 渲染约束。
- **系统监控面板改进**：
  - **模型名称全显**：移除了“今日模型 Token”区域的固定宽度限制，支持长名称自动适配，并增加了 Tooltip 悬浮提示。
  - **GPU 列表序号显示**：为显卡列表添加了 `#0`, `#1`... 序号，极大方便了多显卡服务器的监控识别。
  - **布局灵活性增强**：放开了 GPU 名称的宽度束缚，适配复杂的显卡型号与序号并排显示的场景。
- **版本维护**：
  - 全站静态资源引用版本号统一升级至 `0.0.13`，强制浏览器刷新缓存。
  - 同步更新 `package.json`、`README` 及 `sw.js` 缓存版本。

## [0.0.12] - 2026-05-07
### 系统监控深度优化、审计报表增强与稳定性维护

本次更新对系统监控面板和审计报表进行了深度抛光，显著提升了数据的可读性与界面的美观度，并针对日志噪音和稳定性进行了多项细节修复。

#### 1. 系统监控页面深度优化 (Ops & Monitoring)
- **视觉对齐与布局重构**：
  - **标题统一样式**：修复了“系统监控”标题与其他页面不一致的问题，统一采用 1.5rem 标准字号与边距。
  - **头部紧凑化**：将“最近刷新时间”移至右侧操作栏，与“自动刷新”复选框并排显示，释放纵向空间。
  - **保护状态整合**：将 GPU 与并发保护中的“保护状态”和“拒绝阈值”合并至同一行展示。
- **监控卡片视觉增强**：
  - 为所有监控指标卡片添加了 SVG 矢量图标，并引入平滑的 hover 动效与阴影过渡。
  - 系统内存使用率引入动态进度条，支持根据负载自动切换颜色，视觉反馈更直观。
- **全站接口中文化 (Route Mapping)**：
  - 极大扩充了 `ROUTE_NAME_MAP`，覆盖了全站 API 及静态脚本路径。
  - 增强了路径匹配逻辑，支持大小写不敏感匹配、自动过滤查询参数并剥离路径多余空格，彻底解决了 `/chat/config.js` 等接口名称映射不准的问题。
- **容器体验优化**：
  - 调高了“热点接口”容器的最大高度，确保在 10 条数据模式下不再出现垂直滚动条。

#### 2. 审计报表页面增强 (Analytics & Reports)
- **标题精简**：原“可视化审计报表”正式更名为“审计报表”，界面更干练。
- **图表展示优化**：
  - **排行榜左对齐**：将“用户消耗排行”与“部门消耗对比”柱状图的标签对齐方式由“右对齐”改为“左对齐”，并收窄左侧边距，解决了名称长度不一时导致的左侧巨大留白问题。
  - **响应式宽度修正**：优化了图表在容器内的宽度自适应计算逻辑，确保在不同分辨率下均能完美对齐。

#### 3. 稳定性与系统维护 (Stability & Maintenance)
- **日志降噪治理**：在 HTTP 日志中屏蔽了 Chrome DevTools 产生的 `.well-known/appspecific/com.chrome.devtools.json` 404 噪音日志，保持审计日志的纯净。
- **修复弃用警告**：移除了 `server/routes/admin-stats.js` 中已弃用的 `req.host` 调用，统一更新为 `req.hostname`。
- **管理面板调优**：将系统设置/管理面板的整体高度从 `90vh` 提升至 `94vh`，提供更广阔的运维视野。
- **版本强制刷新**：同步更新了 `sw.js` 缓存版本，确保客户端能及时感知版本更新并加载最新的 UI 逻辑。

#### 验证
- 已通过 `npm run check` 语法校验。
- 手动验证全站接口中文映射均已生效。



## [0.0.11] - 2026-05-07
### RAG 检索、安全加固、额度统计与可观测性升级

本轮更新围绕 RAG 存储检索、安全边界、OpenAI 兼容接口额度统计和运维可观测性做了一次集中优化，保持 SQLite 部署形态不变，同时为后续向量库或更成熟 embedding 索引层预留了更清晰的演进路径。

#### 1. RAG 存储、检索与中文召回增强
- **RAG 模块拆分**：将原本较大的 RAG 逻辑拆为 `server/rag.js` 路由门面、`server/services/rag-index.js` 检索/索引服务和 `server/services/rag-cache.js` 缓存服务，降低后续维护成本。
- **SQLite FTS5 候选召回**：为知识库分片增加 FTS5 索引、触发器和历史数据回填逻辑，RAG 检索先走 FTS 候选预过滤，再进行 embedding 相似度排序。
- **中文 ngram 检索预处理**：新增 `server/services/rag-tokenizer.js`，在应用层生成中文 1-3 gram 检索 token；知识库分片新增 `search_content` 字段，FTS 索引使用“原文 + 中文 ngram token”，改善中文短词、单字和词组召回。
- **旧数据平滑迁移**：启动迁移会自动为历史 `knowledge_chunks` 回填 `search_content`，并重建 RAG FTS 索引，无需手动重新上传知识库文档。
- **RAG 缓存与阈值配置**：补充 RAG 检索 TTL 缓存、候选数量和相似度阈值配置；实际运行以 `.env` 为准，`.env.example` 仅作为模板。

#### 2. OpenAI 兼容接口额度与统计修正
- **修复 `/v1` 绕过每日模型额度问题**：新增 `model_usage_events` 表记录 API Key / Cookie 方式调用 OpenAI 兼容接口产生的 token 用量。
- **统一额度口径**：`getModelDailyUsage()` 现在合并统计网页聊天消息和 `/v1/chat/completions` 调用，模型 `daily_token_limit` 对第三方接入同样生效。
- **统一管理统计口径**：后台 usage、trend、report、details/export、ops-summary 与 Prometheus token 指标均合并 `messages` 和 `model_usage_events`，避免“限额已计入但看板漏算”的割裂。

#### 3. 安全边界与输入防护
- **附件 token 绑定完整资源**：附件访问 token 校验改为同时绑定 `user_id`、`session_id`、`file_path` 和过期时间，防止同一用户任意有效附件 token 被用于访问其他可猜路径附件。
- **文档 ZIP 解压上限**：DOCX/XLSX 内部 ZIP 解析增加 entry 数量、单 entry 解压大小、总解压大小和 `maxOutputLength` 限制，降低压缩炸弹导致的 CPU / 内存风险。
- **前端动态 HTML 转义补强**：管理端模型选项、附件预览、附件链接和统计卡片等动态内容增加 HTML / 属性转义，减少异常数据进入 UI 后的注入风险。

#### 4. 可观测性与测试
- **RAG 指标补齐**：新增 RAG 检索次数、缓存命中、候选数量、命中数量、Top Score、检索耗时和入库耗时等 Prometheus 指标。
- **测试覆盖扩展**：新增模型用量事件计入每日额度、ZIP 解压上限、RAG 中文 ngram FTS 命中等安全/检索测试。

#### 验证
- 已通过 `npm test`
- 已通过 `npm run lint`

## [0.0.10] - 2026-05-07
### 认证安全、流式解析与检索性能补齐

本次更新集中整理了最近一轮功能修复与工程化优化，主要覆盖认证链路、SSE 解析、RAG 检索和基础测试体系。

#### 1. 认证与会话安全
- 新增 CSRF token 机制，登录、刷新、`/auth/me` 与登出流程均完成同步处理。
- 前端鉴权请求统一携带 `X-CSRF-Token`，不再仅依赖本地 token 状态。
- 登录页改为标准表单提交，修复密码输入框不在 form 中的浏览器告警。

#### 2. 流式输出与检索性能
- 将聊天与 OpenAI 兼容接口的 SSE 解析统一到公共模块，减少重复逻辑与断流风险。
- RAG 检索加入候选预过滤与缓存控制，降低高频查询的重复计算开销。
- 补充安全的 FTS 查询构造与附件路径解析，减少异常输入带来的边界问题。

#### 3. 配置、脚本与测试
- 新增 ESLint flat config，并将 lint/test/check 纳入日常校验流程。
- 补充安全相关单测，覆盖 CSRF、SSE、FTS 与附件路径校验等核心逻辑。
- 增加审计日志保留周期、RAG 缓存参数等环境变量说明，并同步示例配置。

#### 4. 其他修正
- PWA 缓存版本更新。
- 模型地址与附件处理继续加强校验，降低非法 URL 与越权路径风险。

#### 验证
- 已通过 `npm run lint`
- 已通过 `npm test`
- 已通过 `npm run check`

#### 5. 上下文软归档、前端轻量化与交互修正
- 会话压缩改为“软归档 + 摘要”模式，旧消息不再物理删除，而是标记为 `context_archived` 并保留压缩时间。
- 聊天页右上角新增上下文用量胶囊，按活跃 token / 阈值显示百分比，并用颜色提示当前压力。
- `GET /sessions/:id` 现在会返回 `contextMeta`，前端切换会话时同步刷新上下文状态。
- 管理员面板相关脚本改为按需加载，降低 `chat.html` 初始加载体积。
- 认证页事件绑定增加空节点保护，避免旧缓存或局部加载导致 `addEventListener` 空指针。
- 聊天页资源版本和 Service Worker 缓存名统一提升到 `0.0.10`，并让导航页优先走网络获取最新内容。
- 顶栏模型选择器做了垂直对齐修正，避免下拉框显得偏上。

## [0.0.9] - 2026-05-07
### 架构性能、全文搜索、PWA 离线化与全栈安全性深度优化

本次更新是对系统的一次全面升级，不仅增强了管理员的监控能力，还通过底层架构重构显著提升了大规模数据场景下的响应速度与安全性。

#### 1. 运维与监控增强 (Ops & Monitoring)
- **多维度审计日志筛选**：新增用户名、操作类型、关键字及日期范围的组合过滤，支持条件同步导出 CSV。
- **监控精准化识别**：重构主机识别算法，支持 FQDN、Docker 内部主机及 IPv6 的自动探测，确保本地/远端负载判断准确。
- **热点接口治理**：全量映射系统 API 名称，消除监控面板中的“未知接口”提示。

#### 2. 核心架构与性能调优 (Architecture & Performance)
- **异步记忆压缩**：将长会话摘要处理改为异步非阻塞模式，彻底解决响应卡顿问题。
- **数据库自动维护**：开启每日 `PRAGMA optimize` 任务及增量空间回收（Incremental Vacuum）。
- **前端渲染效能**：引入 `content-visibility: auto` 与 GPU 加速，大幅优化超长消息列表的滚动性能。

#### 3. 全文搜索系统集成 (Full-Text Search)
- **FTS5 引擎**：基于 SQLite FTS5 实现消息内容秒级全文检索.
- **实时索引同步**：配置数据库触发器，确保消息变更自动实时同步至搜索表。
- **搜索 API**：新增 `/api/sessions/search/content` 接口，支持上下文片段提取。

#### 4. 离线化与 PWA 增强 (Offline & PWA)
- **桌面应用模式**：新增 `manifest.json` 与 `sw.js`，支持将 Pivot 作为独立应用安装至桌面或移动端。
- **智能资源预缓存**：核心静态资源与第三方 vendor 库自动离线缓存，实现秒开体验。

#### 5. 全栈安全性加固 (Security & Compliance)
- **精细化 CSP 策略**：强化 Helmet 配置，严防 XSS 攻击的同时兼容 PWA 与内联事件。
- **附件令牌过期机制**：为所有附件引入 7 天自动过期机制，防止链接滥用。
- **生产日志脱敏**：生产环境下自动切换为高性能 JSON 模式，并对 API Key、密码等敏感信息进行脱敏处理。

#### 6. 深度系统优化与代码审计 (System Deep Optimization)
- **核心架构重构**：统一抽取 HTTP 常用工具函数（分页、IP 获取等）至 `server/http.js`，提升全站代码复用性与一致性。
- **RAG 性能飞跃**：引入 RAG 检索结果缓存机制（TTL 缓存），大幅减少高频重复提问时的向量计算与匹配损耗。
- **稳定性加固**：增加 RAG 向量解析的安全容错处理，防止数据库异常数据或损坏的分片导致对话服务崩溃。
- **更精细的限流策略**：对话接口限流由 IP 维度升级为用户维度，更公平地分配系统算力资源并防止恶意刷取。
- **后台维护服务**：新增系统后台维护任务，自动定期（每 24h）清理陈旧审计日志，保持数据库轻量化运行。

#### 验证
- 已执行 `npm run check`，全站 JavaScript 语法与逻辑链路验证通过。

## [0.0.8] - 2026-05-05
### 多模态附件读取、图片安全保护、远端模型控制与登录体验优化

本次更新围绕聊天会话中的文档/图片上传、多模态模型调用、远端模型部署保护以及登录态体验进行了系统性优化。重点解决了 PDF 与 Office 文档可上传但大模型无法可靠读取、图片过大可能造成内存风险、局域网多模态模型仅支持单图解析、远端模型无法用本机 GPU 指标判断负载，以及 30 分钟登录态过短的问题。

#### 附件上传与文档解析 (Attachment & Document Parsing)
- **扩展文档解析能力**：
  - 新增统一文档抽取模块，支持 `pdf`、`docx`、`doc`、`xlsx`、`xls`、`txt`、`md`、`csv` 等常见格式。
  - 接入 `word-extractor` 与 `xlsx`，补齐旧二进制 `.doc/.xls` 的读取能力，改善“上传成功但模型读不到内容”的问题。
  - 对加密 PDF/Office 文件增加密码处理入口；无法解密或需要密码时给出明确错误提示。
- **扫描 PDF 多模态兜底**：
  - 当 PDF 无法抽取文本时，自动尝试将页面渲染为图片交给视觉模型解析。
  - 针对当前局域网多模态模型一次只支持一张图片的限制，扫描 PDF 默认只取 1 页进入视觉解析，避免一次请求携带多图失败。
- **上传体验增强**：
  - 聊天上传入口支持多选文件。
  - 上传图片和文件预览区限制最多保留 5 个附件，超出时阻止继续追加并提示用户。

#### 图片安全与多模态限制 (Image Safety & Vision Limits)
- **图片 OOM 防护**：
  - 新增图片安全处理模块，限制上传体积、输入像素、上下文图片大小和模型请求中的图片数量。
  - 上传图片会统一经过 `sharp` 解码、缩放、压缩和格式归一化，防止超大图、异常动图或高像素图片导致内存压力。
  - 禁止前端和后端宽泛接受任意 `image/*`，收紧为常见安全图片格式并移除 SVG 上传入口。
- **单图解析约束**：
  - 后端在聊天历史和当前请求层面强制限制每次模型请求最多携带 1 张图片。
  - 多余图片会被替换为文本说明，避免远端模型因多图输入而报错。
  - 前端上传、预览和发送区域增加“当前模型每次仅解析 1 张图片”的提示。

#### 远端模型监控与并发控制 (Remote Model Runtime Protection)
- **区分本机与远端模型负载**：
  - 系统监控页现在会识别模型 Endpoint 是本机还是远端，并明确提示本机 GPU 指标只代表 Pivot 所在服务器。
  - 保留本机 `nvidia-smi` GPU 动态保护作为本机总闸，同时不再把它误认为远端模型服务器负载。
- **新增端点级运行时保护**：
  - 新增模型端点运行时服务，按 Endpoint 聚合请求，支持端点级并发限制、排队、连续失败统计和熔断保护。
  - `/api/chat` 与 OpenAI 兼容的 `/v1/chat/completions` 均接入端点级保护。
  - 连续失败达到阈值后自动短暂熔断，并清理排队请求，避免远端模型服务被持续压垮。
- **模型配置扩展**：
  - 模型配置新增“端点并发上限”和“远端健康检查 URL”。
  - 支持为局域网模型设置并发为 `1`，适配一次只能可靠处理一张图片或显存较紧张的远端服务。
- **监控页增强**：
  - 系统监控新增“模型端点状态”，展示端点 host、健康探针状态、并发、排队、失败次数与熔断倒计时。
  - 修复 `describeEndpointMonitor is not defined` 前端运行时错误。

#### 登录态与配置同步 (Auth & Configuration)
- **登录有效期优化**：
  - Access Token 默认有效期由 30 分钟调整为 480 分钟。
  - Refresh Token 默认有效期由 7 天调整为 30 天。
  - 新增 `.env` 与 `.env.example` 配置项：`ACCESS_TOKEN_EXPIRES_MINUTES`、`REFRESH_TOKEN_EXPIRES_DAYS`，并确保实际运行配置与示例文件一致。
- **前端续期逻辑整理**：
  - 抽取公共 `refreshAccessToken` 逻辑，减少重复实现，为后续统一所有鉴权请求的自动续期打基础。

#### 验证
- 已执行 `npm run check`，项目 JavaScript 语法检查通过。

## [0.0.7] - 2026-05-05
### 实验室卡片化、管理权限加固与 API 用量统计

本次更新重点引入了实验室功能的全新视觉呈现，对系统核心管理员权限进行了全方位的硬性加固，并上线了第三方接入的 API Key 消费统计系统，使资源消耗透明化。

#### UI 深度优化与布局重构 (UI/UX Optimization)
- **实验室功能视觉重构**：
  - 将实验室功能由单调的列表升级为现代化的**卡片式栅格布局**，引入了玻璃拟态背景与平滑 hover 动效。
  - 替换了 RAG 知识库功能的图标，由 Emoji 升级为更具科技感的矢量 SVG 图标，并增加了 `Experimental` 状态标签。
- **附件库与模型配置布局抛光**：
  - 深度调优了附件库表格列宽，收紧文件名并放宽上传时间，解决了日期截断问题。
  - 优化了模型管理界面的操作列宽度，使四个功能按钮排版舒展，不再拥挤。
- **监控页面细节对齐**：
  - 修复了 GPU 与并发保护区域文字换行时的对齐问题，现在所有警告与提示信息均保持一致的左对齐。

#### 安全加固与权限保护 (Security & Protection)
- **内置管理员硬性保护**：
  - **后端硬拦截**：禁止删除内置 `admin` 账号，禁止降低其权限或禁用其状态。
  - **前端交互约束**：自动隐藏 `admin` 的删除按钮，并在编辑时锁定角色与状态字段，防止误操作。
  - **系统可用性兜底**：强制要求系统必须保留至少一个活跃管理员账号。

#### API 用量统计与透明化 (New Feature)
- **API Key 消费统计**：
  - 在数据库底层实现了 Token 消耗的实时累加，支持 OpenAI 协议下的流式与非流式调用计数。
- **额度透明化展示**：
  - 在「API 接入」管理界面新增「已用额度」列，支持千分位格式化显示各密钥的 Token 消费量。

## [0.0.6] - 2026-05-04
### 架构解耦、渲染升级与高频操作增强

本次更新对前端架构进行了大规模的模块化重构，大幅增强了 Markdown 渲染引擎的容错性与数学公式支持，并听取用户反馈引入了单点会话导出、消息重新生成等高频实用功能。

本版本引入了强大的第三方 API 接入网关，支持 OpenAI 标准协议，并实现了 API Key 的自主管理。同时深度优化了模型配置流程，支持一键探测远程模型列表，并对系统监控面板和配置体系进行了全方位的布局优化与标准化，显著提升了管理效率与交互体验。

#### 系统监控与配置标准化 (Optimization)
- **监控布局深度优化**：
  - **紧凑型卡片设计**：调小了监控指标卡片的内边距与间距，并优化了最小宽度，确保 8 大核心指标在主流分辨率下均可单行对齐。
  - **标题视觉校准**：将“系统监控”标题明确设置为左对齐，修复了之前布局中可能存在的视觉重心偏移。
  - **纵向空间释放**：大幅压缩了 GPU 保护、资源列表及模型统计行的上下间距，并为监控面板增加了独立的滚动条支持，确保底部的「热点接口」表格始终可查。
- **配置体系规范化**：
  - **全量同步**：深度对齐了 `.env` 与 `.env.example` 文件，补全了 `LOG_LEVEL`、`PUBLIC_URL` 等关键隐藏配置。
  - **全方位文档化**：为配置文件中所有变量添加了详尽的中文注释，涵盖参数作用、单位及推荐值，降低了运维门槛。
- **GPU 监控中文化**：
  - 针对 Windows 环境或容器透传异常导致的 `nvidia-smi` 报错进行了拦截与翻译，现在会提供清晰的中文排查建议。

#### 第三方 API 接入与智能管理 (New)
- **OpenAI 标准协议支持**：
  - 全面支持 `/v1/chat/completions` 和 `/v1/models` 接口。
  - 允许第三方应用（如沉浸式翻译、NextChat、ChatBox 等）无缝接入 Pivot 系统作为模型网关。
- **API Key 自主管理系统**：
  - 新增独立的「API 接入」管理频道，支持用户自主生成、管理 `sk-` 格式密钥。
  - **安全弹窗交互**：重构了密钥创建流程，采用高层级（z-index: 5000）自定义弹窗，支持一键复制并强制安全提醒。
  - **智能地址指南**：接入指南支持根据当前访问环境（内网/公网）智能动态生成 Base URL。
- **智能模型管理升级**：
  - **自动模型探测**：在添加/编辑模型时增加「一键抓取」按钮，可自动探测并获取上游服务支持的所有模型标识。
  - **掩码状态兼容**：修复了在 API Key 为掩码状态下无法进行连接测试或模型探测的问题。
- **UI/UX 深度抛光**：
  - **紧凑型侧边栏**：调小了管理面板菜单项的上下间距，视觉更干练。
  - **宽屏管理容器**：将 API Key 管理容器宽度扩展至 1350px。
  - **SQL 语法健壮性**：彻底修复了因 SQLite 字符串引用语法导致的 `500` 认证错误。

- **前端模块化解耦**：将原本庞大的 `app.js` 彻底拆分为 10 多个专注单一领域的子模块（涵盖 UI渲染、对话引擎、会话侧边栏、模型管理、用户管理、统计图表等），极大提升了代码的可读性与长期可维护性。
- **渲染器防御性降级**：彻底摒弃了易受底层库升级破坏的 AST Token 自定义渲染方案，全面转为“原生渲染 + 正则后处理包裹”的防御性渲染策略。

#### Markdown 与显示增强
- **原生 KaTeX 数学公式支持**：实现了完全脱机的本地化数学公式渲染，通过与代码块语法的严密隔离，解决了公式错乱与解析失效的痛点。
- **复杂表格护城河**：完美修复了 Markdown 表格渲染出现 `[object Object]` 错误和右侧多出空白列的问题；现已支持超长表格的横向滚动交互。
- **视觉排版抛光**：
  - 增强了侧边栏顶部品牌 Logo 与系统名称的尺寸与间距，提升视觉冲击力。
  - 增加 H1-H6 标题的个性化排版样式，修复了用户气泡框内代码和链接的对比度问题。
  - 调整了【审计日志】等后台管理界面的表格列宽，防止大数字换行错乱。

#### 新增高阶功能
- **单会话全量导出**：在侧边栏会话菜单中新增「导出为 Markdown」，支持将长篇对话一键打包至本地 `.md` 文件，完美保留格式与时间戳。
- **精准重新回答**：为 AI 回复卡片新增了「重新回答」操作，一键回退当前错误输出并基于最新的上文精准重建流式响应。
- **可视化审计报表**：在系统设置中新增「审计报表」模块，支持按部门、用户及时间跨度（7天/30天/90天/1年）动态生成 Token 消耗趋势、Top 10 用户排行及部门用量分布图。
- **OpenAI 标准接口兼容**：新增 API Key 管理系统，支持通过标准的 `/v1/chat/completions` 接口供第三方客户端（如沉浸式翻译、NextChat）接入使用。
- **高级模型参数开放**：
  - 在底层数据库平滑新增了 `temperature` 和 `max_tokens` 字段。
  - 允许管理员在【模型配置】界面为每个大模型接口绑定专属的温度（发散度）与输出长度限制，实现更专业的大模型驾驭能力。

#### 后端健壮性提升
- **日志降噪治理**：精简了终端日志输出，屏蔽了前端高频轮询与静态资源加载带来的冗余请求日志，仅保留核心中文业务日志。
- **总结接口超时免疫**：将会话总结（Title Generation）的网络超时阈值由 10 秒延长至 30 秒，彻底解决了并发状态下偶发的“会话标题生成失败”报错。

## [0.0.5] - 2026-05-04
### 思考过程 UI 深度优化与流式渲染交互增强

本次更新极大提升了模型思考过程的视觉美感与交互流畅度，并解决了流式输出时的交互闪烁问题。

#### 思考过程 (Thinking Process) UI 优化
- **CSS Grid 平滑动画**：重构了展开/收起逻辑，采用 CSS Grid 替代传统的 `max-height` 方案，彻底解决了动画截断与速度不一致的问题，过渡极其流畅。
- **视觉风格升级**：采用紫色调渐变背景（Purple/Violet），增加了“灯泡”发光图标与圆形的“›”旋转指示器，视觉层次更显专业。
- **呼吸脉冲特效**：为“正在思考”状态增加了边框呼吸灯与阴影脉冲效果，使界面更具动感。
- **闪跳行为修复**：修复了在收起窗口时内容先瞬间展开再收起的逻辑 Bug。

#### 流式渲染与交互增强
- **增量 DOM 更新策略**：优化了流式渲染机制。在模型思考阶段，系统现在仅对思考区域进行增量文本追加，不再进行全量 `innerHTML` 重绘。
- **无缝交互体验**：得益于增量更新，用户现在可以在模型持续输出思考内容的同时，自由地点击展开或折叠窗口，动画不会被打断。
- **智能自动滚动**：当思考块处于展开状态时，新产生的内容会自动驱动内部容器滚动到底部。

#### UI 细节抛光
- **停止按钮改进**：调大了“停止生成”按钮中间的红色方框，使其比例更协调，视觉识别度更高。
- **布局居中增强**：通过 Flexbox 确保了停止按钮图标在各种缩放下的完美居中。

## [0.0.4] - 2026-05-04
### 交互体验与模型管理功能增强

本次更新专注于提升模型管理的安全性、前端渲染的美观度以及 Token 统计的精确性。

#### 模型管理
- **明文密钥查看**：在模型配置弹窗中增加了“眼睛”图标，支持管理员在必要时安全地查看已配置的 API Key 明文。
- **配置测试修复**：修复了模型编辑弹窗中“测试连接”失效的问题。现在系统能够智能处理掩码状态下的 Key，并支持在保存前即时验证修改后的接口地址。
- **管理辅助**：提供了一键重置管理员密码的辅助脚本，解决了因 `.env` 配置变更导致的初始化同步问题。

#### 渲染与交互
- **代码高亮集成**：引入了 `highlight.js` 库，为对话中的代码块增加了多语言语法高亮（GitHub Dark 主题），显著提升了技术内容的阅读体验。
- **流式稳定性优化**：
  - 优化了 Gzip 压缩过滤器，解决了部分环境下因缓冲导致的非流式回复问题。
  - 增加了 `X-Content-Type-Options: nosniff` 响应头，确保消息能够实时、逐字呈现。

#### 性能与统计
- **Token 精确计算**：
  - **官方数据优先**：系统现在会自动捕获并记录大模型厂商 API 返回的官方 `usage` 统计，确保计费与统计的绝对一致。
  - **改进版估算算法**：对于不返回使用量的模型，升级为区分中英文权重的本地估算算法（中文 2.0/字，英文 0.5/字符），计算精度大幅提升。
- **实时指标优化**：前端 TPS（每秒 Token 数）显示现在同步采用加权算法，能够更专业地反映模型的真实推理速率。

## [0.0.3] - 2026-05-04
### 系统稳定性与品牌一致性优化

本次更新重点聚焦于系统的生产级稳定性增强、文件处理性能优化以及全站品牌视觉的标准化。

#### 性能与稳定性优化
- **非阻塞 I/O 重构**：将附件目录体积统计由同步改为异步执行，防止大规模附件库导致的事件轮询阻塞。
- **全局异常守护**：在后端引入 `uncaughtException` 和 `unhandledRejection` 进程监听，彻底解决非预期错误导致的服务僵死问题。
- **稳健错误处理**：增加全局 Express 错误拦截中间件，所有系统 500 异常现在均以标准的 JSON 格式返回，提升前端容错性。
- **数据库优化**：配置 SQLite 增量真空（Incremental Vacuum）和 5000ms 忙等待超时，提升高并发下的写入稳定性。

#### 品牌与视觉提升
- **Favicon 修复**：更新浏览器标签图标为白色背景的六边形 Logo，确保在不同浏览器主题（深色/浅色）下的视觉一致性。
- **代码资产审计**：
  - 修复了核心代码头部的中文字符编码乱码问题。
  - 标准化了所有后端与前端文件的中英文双语表头。
- **自动灾备方案**: 新增全量快照备份脚本，支持带时间戳的数据库永久留存，增强离线抗风险能力。

## [0.0.2] - 2026-05-04
### 管理增强与效率套件升级

本次更新将 Pivot AI 正式升级为全栈管理平台，引入了深度运营监控、指令中心以及多项 UI/UX 优化。

#### 新增功能
- **效率工具集 (Efficiency Suite)**：
  - **指令中心 (Prompt Center)**：内置常用指令模板，支持 AI 角色（System Prompt）一键切换。
  - **全局会话搜索**：集成带防抖逻辑的实时搜索，支持按标题、标签快速过滤。
  - **会话管理进阶**：支持对话置顶（Pin）、归档（Archive）及多标签管理。
- **运营概览面板**：
  - 单行展示 8 大核心指标（用户、消息、Token 消耗等），数据一目了然。
  - 集成近 30 天 Token 使用趋势图。
- **管理与审计**：
  - **用量统计**：支持查看各用户的最后活动时间、总 Token 消耗及消息量。
  - **审计日志**：完整记录系统内的敏感操作（登录、删除、配置变更），支持 IP 地址追踪。
- **模型监控与权限**：
  - **连通性测试**：支持实时探测模型 API 状态，显示延迟（ms），并具备并发保护。
  - **额度限制**：支持设置模型每日 Token 消耗上限，并可按部门分配使用权限。

#### 优化与修复
- **UI/UX 体验**：
  - 系统设置弹窗宽度增加至 `1450px`，优化了所有表格的列宽分。
  - 移除了所有浏览器原生 `confirm`，统一采用系统自定义确认弹窗。
  - 修复了弹窗层级冲突（Z-Index），确保二级确认弹窗始终在最上层。
  - 全面汉化：版权信息、侧边栏标题及操作提示等均已本地化。
- **安全性与健壮性**：
  - 修复了模型测试接口在异常时返回 500 的问题，增强了错误日志的诊断信息。
  - 实现了数据库字段自动迁移逻辑，支持平滑升级。
  - 侧边栏支持折叠，优化了系统标题的视觉对比度。


## [0.0.1] - 2026-04-29
### 初始版本发布 (Initial Release)

本项目第一个正式稳定版本，核心聚焦于**私有化内网环境下的 Pivot AI 全栈安全性、品牌形象统一以及极致的交互体验**。

### 新增功能
- **品牌视觉升级**: 部署了全新的翡翠绿风格 Logo（白色背景）；优化了浏览器 Favicon 在标签页的显示尺寸；移除了登录页冗余文案，保持界面干练。
- **性能监测系统**: 对话过程中实时显示推理耗时、生成 Token 长度以及每秒字数 (TPS)，便于性能对标。
- **资产确权体系**: 重构上传逻辑，实现附件按“用户/会话”路径物理隔离，并在数据库 `attachments` 表中建立强关联。
- **生成中断机制**: 支持一键停止模型回复，通过前端 `AbortController` 与后端连接销毁联动，实现秒级算力回收。
- **自动灾备方案**: 新增全量快照备份脚本，支持带时间戳的数据库永久留存，增强离线抗风险能力。

### 安全与稳定性加固
- **SQL 注入防御**: 全面重构 SQL 语句，采用参数化查询机制，消除注入风险。
- **XSS 攻击防护**: 引入 `DOMPurify` 库对所有消息和会话标题进行严格过滤。
- **资源泄露修复**: 针对 SSE 流式连接增加销毁监听，防止用户异常断开导致的内存溢出。
- **数据库引擎优化**: 开启 SQLite 的 WAL (预写日志) 模式及增量 Vacuum，提升高并发下的写入稳定性。
- **鉴权加固**: 强制使用 256 位随机环境变量密钥驱动 JWT 体系。

### 部署与运维
- **容器化适配**: 优化 `Dockerfile`，预装 `libc6-compat` 库以解决 Alpine 环境下的图片处理引擎兼容性。
- **持久化配置**: 完善 `docker-compose.yml`，对数据库、附件目录及备份路径进行全路径挂载。
- **国内源加速**: `Dockerfile` 基础镜像切换至国内阿里云镜像仓库，解决拉取缓慢问题。

---
