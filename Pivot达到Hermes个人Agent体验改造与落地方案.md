# Pivot 达到 Hermes 个人 Agent 体验的改造与落地方案

> 文档状态：可执行方案（评审后可直接拆解为迭代任务）  
> 基线版本：Pivot v0.1.152，代码基线 `be0cc70`  
> 编制日期：2026-09-19  
> 目标：在不削弱 Pivot 企业安全、内网部署和审计能力的前提下，让个人用户获得接近 Hermes 的持续、主动、会学习、随时可用的 Agent 体验。

## 1. 结论与实施策略

Pivot 不需要重写 Agent Runtime，也不需要复制 Hermes 的 CLI、Python 实现或默认权限模型。Pivot 已经具备个人档案、长期记忆、历史会话检索、后台学习队列、个人 Skill 发布、持续目标、统一待办、通知渠道、浏览器、MCP、子任务和工作流等多数基础构件。主要工作是把这些分散能力接成自然的个人助手旅程，并补齐“渠道内双向会话”和面向普通用户的连续体验。

建议把“达到 Hermes 个人 Agent 体验”分为两个验收层级，避免把“功能清单相似”误当成“用户体验相似”：

1. **个人 Agent Beta（本方案 P0）**：用户能快速开始对话；Agent 能跨会话记住并解释偏好、回忆历史、从重复任务中形成可复用经验、按计划主动完成工作、在一个日常消息渠道双向收发消息，并在高风险动作前继续执行 Pivot 的审批与审计规则。
2. **个人 Agent 完整体验（P1/P2）**：扩展第二个消息渠道、完善后台多 Agent、技能发现与安装、项目上下文、移动端审批和更丰富的语音/多模态入口。

按现有底座估算，P0 约需 **23～39 人周**，由 1 名后端、1 名前端和兼职 QA 并行，约 **10～14 周日历时间**；P1/P2 再需约 **12～20 人周**，取决于渠道平台和技能目录范围。估算不含外部平台资质审批、客户环境联调等待和组织级 Skill 市场。

## 2. Hermes 体验基准与 Pivot 当前基线

Hermes 当前公开文档描述的个人 Agent 体验重点包括：跨会话持久记忆和历史会话搜索、技能的创建与复用、自然语言管理定时任务、事件触发、后台结果回传、多平台消息 Gateway、工具集发现和子 Agent 委派。参考：[Memory](https://hermes-agent.nousresearch.com/docs/user-guide/features/memory)、[Skills](https://hermes-agent.nousresearch.com/docs/user-guide/features/skills)、[Cron](https://hermes-agent.nousresearch.com/docs/user-guide/features/cron)、[Messaging Gateway](https://hermes-agent.nousresearch.com/docs/user-guide/messaging)、[Delegation](https://hermes-agent.nousresearch.com/docs/user-guide/features/delegation)。本方案按 2026-09-19 可访问的官方文档确定基准，不把其不断变化的社区 Skill 数量作为 Pivot 的验收目标。

| 能力 | Pivot 当前已具备 | 当前判断 | 本方案处理方式 |
|---|---|---|---|
| 个人身份、称呼、语气、详略与快速设置 | 档案服务与工作台已有“3 分钟快速设置” | 有能力，入口仍偏控制台 | 将快速设置前移到首次使用和聊天入口，保留高级选项 |
| 长期记忆 | 记忆分类、敏感拦截、检索、禁用、编辑、删除和使用记录 | 治理强，解释/纠正链路可更顺 | 把“记住/忘记/为什么使用”做成对话中的直接操作 |
| 历史会话回忆 | 消息全文搜索；Agent 工具 `sessions.search`、`sessions.recent` | 基础已有 | 强化自然语言可发现性、引用回链和检索权限提示 |
| 自动学习与个人 Skill | 可恢复学习队列、经验提议、真实 Skill 版本/验证/发布/匹配；有 PostgreSQL 集成测试 | 核心后端闭环已存在，用户感知和线上效果需验证 | 优先改善自动反馈、命中说明、暂停/撤销体验及效果评估，不另造学习引擎 |
| 持续目标与主动触发 | Timer/Cron、Webhook、文件、只读数据库；去重、冷却、租约、失败熔断 | 后端覆盖较全；创建方式偏表单 | 增加自然语言目标草案和运行前确认，复用 `agent_goals` |
| 收件箱 | 汇总任务、审批、通知、进化事件；支持已读、稍后处理、静音 | 基础已存在 | 统一排序和状态解释，补齐渠道交付状态、行动卡片和故障恢复 |
| 外部通知渠道 | Web、Webhook、Email、IM；企业微信、飞书、钉钉受控适配，带持久投递队列 | 以出站通知为主 | 建设统一双向 Channel Gateway；先接一个完整平台，不把通知 Webhook 算成聊天渠道 |
| 多 Agent | `agent.delegate`、父子 Run、资源预算、控制邮箱、子工作流/逐项迭代 | 有协作构件；自由并行、后台续投体验有限 | 在 P1 做显式任务组、进度和完成回传，不重写 DAG/Runtime |
| 工具和 Skill 发现 | 内置工具、MCP、个人导入、版本/签名/权限治理 | 安全与治理强，通用工具生态较窄 | 做受控的个人能力目录和可信来源，不直接开放任意包自动执行 |
| 语音、TTS、图像生成及大量原生渠道 | 有图片输入及部分媒体呈现；尚无 Hermes 式完整个人消息 Gateway/语音体验 | 有明确差距 | 不阻塞 P0；以渠道与用户数据为前提纳入 P2 |

### 2.1 基线代码入口

- 档案与记忆：`server/services/agent-profile.js`、`server/services/long-term-memory/`、`server/services/memory-governance.js`、`client/chat/partials/workspaces/agent.html`
- 学习与经验：`server/services/agent-learning.js`、`server/services/agent-evolution.js`、`client/chat/agent-learning.js`、`tests/agent-postgres-integration.test.js`
- 持续目标：`server/services/agent-goals.js`、`server/services/agent-triggers.js`、`server/services/agent-schedules.js`
- 收件箱与渠道：`server/services/agent-inbox.js`、`server/services/agent-channels.js`、`server/services/agent-channel-adapters.js`、`server/services/agent-channel-interactions.js`
- 会话回忆和委派：`server/services/agent-tools.js` 中的 `sessions.search`、`sessions.recent`、`agent.delegate`；`server/services/agent-collaboration.js`、`server/services/agent-run-resources.js`
- Skill 安全与发布：`server/services/agent-skill-*.js`、`server/services/agent-releases.js`
- 统一工作台：`client/chat/agent-harness.js`、`client/chat/partials/workspaces/agent.html`

**状态口径**：以上“已具备”表示代码与既有测试证明相应构件存在，不等同于“线上体验达标”。每个 P0 项仍需完成用户旅程、真实渠道/模型集成、失败路径和目标指标验收。

## 3. 目标体验与验收标准

用户不应该先理解 Runtime、DAG、Manifest、租约或发布状态，才能使用个人 Agent。普通用户主要感知四件事：**我是谁、它记住了什么、它能替我持续做什么、我如何控制它**。复杂治理继续存在，但应在操作需要时解释，不作为日常使用的前置负担。

### 3.1 P0 用户旅程

1. **第一次使用**：从聊天页进入向导，设置称呼/角色、表达偏好、工作范围和允许的资料/工具；可跳过高级项。3 分钟内完成并运行首个任务。
2. **日常对话**：普通对话保持低摩擦；需要长任务时可转为后台 Agent 执行。Agent 按权限检索记忆和历史会话，回答中能看到引用来源或“使用了哪条记忆/经验”。
3. **纠正与学习**：用户说“以后按这个格式”或点击“记住这点”，系统形成可查看的记忆/经验；对可安全自动启用的个人只读 Skill 自动验证，其余进入待确认。
4. **安排持续工作**：用户用自然语言说“每个工作日九点检查……并把摘要发给我”，Agent 生成目标草案，说明触发时间、数据范围、工具和输出渠道；用户确认后才保存启用。
5. **跨渠道接续**：用户从一个已绑定平台发消息，可以继续自己的 Pivot 会话；审批身份、附件和回复都映射到当前用户/租户，不把频道 ID 当作授权。
6. **控制与撤回**：用户能暂停目标、撤销经验、删除记忆、断开渠道、查看失败原因；高风险操作仍走审批，Agent 不得扩大自己的权限。

### 3.2 产品指标（首轮 Beta）

所有指标必须有时间窗、样本量、用户/租户范围和失败分类；小样本只报告“样本不足”，不得宣称达到目标。

| 指标 | Beta 目标 | 口径/备注 |
|---|---:|---|
| 快速设置完成时间 | 中位数 ≤ 3 分钟 | 首次使用可跳过渠道和高级安全选项 |
| 首个任务成功率 | ≥ 80% | 至少 30 个真实任务；按任务类别分层 |
| 跨会话回忆有用率 | ≥ 80% | 用户评价/抽样双重校验；显示引用可追溯 |
| 个人 Skill 下次命中率 | ≥ 70% | 仅统计验证并启用的 Skill，按任务类型分层 |
| 自动学习误启用率 | 0 个高风险/越权候选 | 低风险误启用需可在单次操作中暂停并撤销 |
| 主动任务按时启动率 | ≥ 95% | 宽限窗口按触发类型配置；独立报告运行和投递 |
| 通知投递成功率 | ≥ 98% | 不把 Agent 运行成功误报为渠道已送达 |
| 目标重复触发/重复副作用 | 0 个已确认事故 | 去重键、租约、外部副作用审批及故障演练 |
| 未授权渠道身份/跨用户访问 | 0 | 配对、租户隔离、回放攻击测试 |
| 删除/暂停后的误用 | 0 | 记忆、Skill、目标分别测试缓存失效与下一次运行 |

## 4. 产品和架构原则

1. **一个 Runtime，多种入口**：Web/Electron/消息平台都进入现有 Agent Run、会话、策略和审计链路；不建第二套个人 Agent 引擎。
2. **学习与执行分离**：学习任务只产生受约束候选；Skill/工作流运行必须解析到有效个人发布版本；每次工具调用仍重新经过能力策略、预算和审批。
3. **“记忆”不等于“无限保存”**：明确用户/任务作用域、来源、保留期、置信度、敏感等级和删除语义；默认不把任意消息原文转为长期记忆。
4. **控制面安全不可自我修改**：不允许 Agent 修改 PEP/PDP、租户权限、网络基线、凭据、审计策略、管理员提示模板或自身生产代码。
5. **运行与送达是两个状态机**：Run `completed` 不代表消息已送达。使用持久 Outbox、幂等键、重试、死信和可见的 `delivery_failed`。
6. **可信来源优先**：技能目录首先展示内置/已验证/管理员批准来源；第三方 Skill 需版本固定、内容摘要、权限预览和人工启用。
7. **隐私默认本地/组织内**：消息渠道可选且显式绑定；所有外部服务流量必须经现有网络策略，不因个人体验目标绕过内网控制。

## 5. 改造工作流与可执行任务

### P0-0：基线、迁移门禁与体验范围冻结（第 1 周）

**现状问题**：当前 Hermes 对标文档和 P0/P2 backlog 记录的完成状态与现有代码/测试有时间差；近期全量测试中，SQLite 迁移测试有两个用例失败，均指向缺少 `agent_dag_nodes` 表。

**任务**

- [ ] 新增个人 Agent Beta 旅程测试脚本：首次设置、记忆/回忆、学习后复用、自然语言创建目标、渠道双向消息和审批。
- [ ] 对上述每个旅程建立“代码已存在/需打磨/需新增”证据矩阵；将 `docs/agent-experience/P0-P2-backlog.md` 中陈旧状态改为指向本方案的状态源（可在本方案获批后单独更新）。
- [ ] 修复 `server/db/migrations/agent-dag-reuse-provenance.js`：SQLite 中 `agent_dag_nodes` 不存在时不得创建引用不存在表的索引；保证真实旧快照和空库升级均幂等。
- [ ] 固定本地/CI 验证门禁：`npm run check`、`npm run lint`、`npm run test:all`；部署候选另跑 `npm run test:e2e`、PostgreSQL 迁移和目标渠道真实连通性验收。
- [ ] 建立不少于 30 个代表性任务的人工评测集：跨会话回忆、重复任务、文件/报表、信息检索、主动目标和需要审批的副作用。

**验收**：两个旧 SQLite 快照测试修复且 `npm run test:all` 全通过；验收集每条样例有预期行为、权限范围和评分说明；P0/P1/P2 有明确冻结记录。

### P0-1：个人 Agent 首页与低门槛快速设置（第 1～3 周）

**已有基础**：个人档案、角色/语气/详略字段和“三分钟快速设置”已在 Agent 工作台实现。

**改造重点**：把它从“控制台中的一个页签”变为 Agent 的首次体验和持续入口，不重做底层档案表。

**任务**

- [ ] 首次登录/首次打开 Agent 时提供轻量引导：称呼与表达偏好 → 常见任务 → 数据/工具范围 → 可选通知渠道。每步可跳过，支持以后续配。
- [ ] 首屏提供“直接开始”输入框和可执行示例；普通回答与后台 Agent 执行的区别用用户语言说明。
- [ ] 为个人 Agent 增加一页概览：档案、记忆、已启用经验、运行中的目标、最近任务和待处理审批；普通用户默认不出现 Residency、运行包、供应链等运维细节。
- [ ] 加入显式偏好编辑与回滚：字段级版本/并发冲突处理复用 `agent-profile`，保存成功后说明何时生效。
- [ ] 新增关键产品事件：向导开始/完成/跳过、首个任务创建/成功/失败、入口转化；不记录提示词正文和附件内容。

**验收**

- 新账号从进入到发送首个有效任务中位数 ≤ 3 分钟；无须先配置 Skill 或工作流。
- 向导中断后可继续，不覆盖已有字段；用户可随时修改/清除个人档案。
- Web 与 Electron 使用同一服务契约，桌面端无网或服务器不可用时清楚显示状态，不静默丢输入。

### P0-2：记忆与历史会话回忆变成“可解释的个人连续性”（第 2～4 周）

**已有基础**：长期记忆抽取/检索和治理、消息全文搜索、`sessions.search`/`sessions.recent` 均已存在。

**任务**

- [ ] 统一“记忆”和“历史记录”的用户心智：记忆是精选、可编辑的稳定信息；历史会话是按需查找的原始工作记录，不把全文自动注入每次上下文。
- [ ] 在聊天中支持自然表达“记住/不要记住/忘记这条/更正我的偏好”，映射到现有 memory APIs；涉及歧义时先给出候选内容和确认卡片。
- [ ] 记忆命中时在运行详情/答案来源区展示记忆 ID、来源、更新时间和简短命中原因；提供“这条不相关/已过期/不要再用”动作。
- [ ] 提升 `sessions.search` 的可发现性：按当前用户、会话、时间范围检索；返回消息时间、会话标题、命中片段和可打开的来源链接；搜索结果不跨用户/租户。
- [ ] 记忆更新后让下一次运行立即生效，并清理进程缓存；禁用/删除必须同时影响检索、Prompt 注入和学习候选。
- [ ] 给敏感信息拦截、错误记忆纠正、过期记忆和来源删除建立回归测试。

**验收**

- “记住/忘记/更正”主要路径不用打开管理页面；每次记忆写入都能追溯来源与策略。
- 删除或暂停的记忆在下一次运行绝不命中；来源会话删除后不再产生新的学习/共享候选。
- `sessions.search` 的结果可直接打开原会话，且工具调用遵守当前用户 ACL。

### P0-3：自动学习从后台实现升级为用户能感知的成长闭环（第 3～6 周）

**已有基础**：`agent_learning_jobs` 队列、重复任务去重、恢复/限流、自动生成提议、Skill 验证与发布/匹配、经验列表和状态操作已有实现；`agent-postgres-integration.test.js` 覆盖真实个人 Skill 创建并可被匹配。

**改造重点**：不重写学习服务；补齐何时学习、学到什么、何时复用、效果如何以及如何撤回的连续感。

**任务**

- [ ] 统一学习触发策略：多步骤成功、失败后恢复成功、用户纠正、显式“记住这个做法”；避免单轮闲聊批量生成 Skill。
- [ ] 学习完成后给出低打扰结果卡片：“记住了什么/为什么/来源任务/是否自动启用”；验证失败说明失败类别，不展示未脱敏原始工具输出。
- [ ] 每个 Run 记录 `skillId/version/releaseId/matchReason`、注入摘要、是否执行了相关工具；任务完成后将成功/失败/用户有用度归因到版本。
- [ ] 在聊天答案和运行详情提供“已使用个人经验”可展开来源，不把完整 Skill 指令或内部策略泄漏给无权限用户。
- [ ] 低风险自动启用继续限定为已验证、权限差异为空、来源非敏感、工具在既有授权集合内；工作流只生成草稿；副作用永不因为学习而自动授权。
- [ ] 对个人经验提供统一暂停/恢复/撤销；撤销需马上停止新 Run 匹配，并保留可审计版本历史。
- [ ] 为 Skill 匹配加入负反馈/误匹配信号及置信度门槛；样本不足保持确定性静态匹配，不做“自我变聪明”的无证据宣称。

**验收**

- 3 次同类任务后能形成 0～1 个高质量候选，不因重复事件产生重复资产；下一次相似任务能解释是否匹配及理由。
- 验证失败、用户拒绝、撤销、来源删除、权限差异新增这六类路径均有集成测试。
- 低风险已验证经验可选择自动启用；无权限差异外的任何候选均需用户确认。

### P0-4：自然语言创建持续目标与可靠主动交付（第 4～7 周）

**已有基础**：`agent_goals` 支持 timer/Cron、Webhook、文件和数据库触发；已有授权、预算、冷却、去重、调度租约和失败熔断。

**任务**

- [ ] 给 Agent 增加“目标草案”工具/API：只解析成结构化草案，不直接启用。草案至少包含频率/时区、数据源、范围、工具策略、运行预算、结果去向、到期时间和失败熔断策略。
- [ ] 草案界面用自然语言复述并显示权限差异；用户确认后才调用现有 `agent_goals` 创建/启用接口。目标修改、暂停、恢复、终止和删除都可从对话执行，但删除/改变授权需确认。
- [ ] 首批只支持每日/工作日/每周/固定间隔、现有 Webhook 和已授权文件/数据库源；不接受任意模型生成 SQL/路径直接运行。
- [ ] 为每个目标绑定模型策略快照、允许能力集、结果保留期、渠道目标和时区；全局模型切换不能悄悄改变已授权目标的成本/权限。
- [ ] 将“Run 成功”和“输出投递成功”分别记录、展示和告警；使用现有 delivery outbox，补齐过期、死信、人工重投和重复发送演练。
- [ ] 增加运行预览/测试运行：用户能先看一次性结果，不会顺手创建生产调度。

**验收**

- 通过自然语言提出目标后，先展示草案，未经用户确认目标不进入 active。
- 同一触发重复投递只创建一个 Run；服务重启和租约过期可恢复；副作用重试必须再次审批或由幂等契约证明安全。
- 每次运行状态、下一次运行时间、最近错误、投递状态和暂停入口都可见。

### P0-5：双向消息 Channel Gateway（第 5～9 周，最大新工作包）

**差距说明**：当前 Pivot 有 Web/Webhook/Email/IM 出站投递及审批交互回调，企业微信/飞书/钉钉适配已有；尚不能因此认定已经具备 Hermes 式“在消息平台直接与个人 Agent 连续对话”的完整 Gateway。Hermes 官方 Gateway 把消息会话、运行、Cron 结果和语音消息放在同一后台进程/会话模型中。[Messaging Gateway](https://hermes-agent.nousresearch.com/docs/user-guide/messaging)

**决策**

- 先实现**一个完整、可运营的双向渠道**，不并行铺开多个平台。
- 通用个人 Beta 默认建议 Telegram Bot（事件回调、消息线程、测试成本相对可控）；若部署目标不允许 Telegram，则以飞书或企业微信作为首发，并把 Telegram 推到 P1。决策在 Phase 0 根据目标客户和网络约束确认。
- 共用 `Channel Adapter` 契约。已有 `agent_channel_bindings` 和 `agent_channel_deliveries` 继续承担绑定/出站；新增最少的 inbound event 去重、外部会话映射和投递回执持久化，不新建 Agent Run 表。

**任务**

- [ ] 定义 Adapter 契约：验证签名/平台身份、配对、解密密钥引用、解析事件、附件规范化、发送消息、编辑/分片、回执、限流、重试、健康状态和断开。
- [ ] 新增持久化 inbound idempotency/event log 与外部对话 → `user_id + tenant_id + session_id` 映射；唯一约束按平台、账号、对话和事件 ID 设计。
- [ ] 未配对用户采用 pairing code/管理员批准；禁止仅因知道用户名、手机号或 channel ID 建立 Pivot 身份。
- [ ] 入站消息转成现有会话消息/Agent Run；普通回答、后台 Agent、待审批通知分别使用已有执行策略和 UI 状态机。
- [ ] 回答送达必须走持久 outbox 并记录平台回执；Run 完成但平台投递失败时显示 `delivery_failed`，可查看/重投，不重复执行任务。
- [ ] 第一期支持文字、图片和常见文档附件（受现有附件类型/大小/病毒或内容检查约束）；按平台长度分片；音频/TTS 暂列 P2。
- [ ] 待审批消息用平台原生按钮/签名回调（平台支持时）或 Web/Electron 确认链接；决策仍回到 Pivot 服务端 approval API，渠道回调不拥有执行授权。
- [ ] 增加通道连接状态、最近收发、授权主体、撤销绑定和安全告警；密钥始终经 credential reference，不写入消息体、日志或客户端存储。

**验收**

- 未配对、跨用户、跨租户、伪造签名、重放事件、重复 webhook、过期审批均不进入执行。
- 文字消息可连续接续原会话；图片/附件以安全引用接入，不把平台外部 URL 直接交给工具执行。
- 失败注入覆盖平台 429/5xx、超时、回调乱序、进程重启、重复投递、死信/手动重投和密钥轮换。
- 至少 50 条测试消息中无未确认丢失/重复回复；正式 Beta 投递成功率 ≥ 98%。

### P1-1：个人能力目录与可信 Skill 发现（第 8～11 周）

Hermes Skills 的体验优势是按需加载、渐进披露、可创建/安装和易发现；其目录生态不等于 Pivot 应直接执行任意社区包。[Hermes Skills](https://hermes-agent.nousresearch.com/docs/user-guide/features/skills)

**任务**

- [ ] 把已安装的内置工具、MCP、个人 Skill、组织发布 Skill 汇总为“我的能力”目录；支持搜索、分类、适用任务、示例、权限范围、来源、版本和启用状态。
- [ ] 用户通过自然语言提到某种新能力时，Agent 能先建议已安装能力或受信任目录项，不得默认安装。
- [ ] 支持 SKILL.md/兼容包导入预览：显示权限、网络、文件范围、依赖、签名和摘要；不执行 Skill 包脚本；个人/组织域清晰区分。
- [ ] 新增“创建个人 Skill”自然语言流程：从明确选中的 Run/Trace 提炼草稿 → 用户看差异 → 声明式验证/无副作用试跑 → 个人发布；复用现有版本、签名和发布服务。
- [ ] 目录连接器采用管理员批准来源、固定版本与缓存；支持离线导入/导出。社区来源纳入前先有内容扫描、撤回和安全响应机制。

**验收**：用户不用手写 YAML 即可从一次明确选中的任务生成可读 Skill 草稿；未经验证/发布的版本在运行时不可见；工具权限从已授权集合新增时必须重新确认。

### P1-2：后台协作、任务完成回传与会话上下文（第 9～12 周）

**已有基础**：`agent.delegate`、`/agents/runs/:id/delegate`、父子 Run 资源预算/控制邮箱和工作流委派已经存在；目前普通个人聊天的后台并行工作仍不等于 Hermes 式一键委派任务组。

**任务**

- [ ] 在长任务中提供“拆分并行”受控能力：最多 N 个子任务、预算共享、工具范围继承且不能升权；每个子任务有目标、状态、取消和错误。
- [ ] 为委派定义结构化结果契约；格式不符时只允许一次有界修正，仍保留原始结果并标记不合格，不无限重试。
- [ ] 结果以新消息/收件箱事件返回父会话；使用 outbox 和 claim 防止重启后丢失或重复合成。
- [ ] 区分“等待中”“正在运行”“部分完成”“完成但未送达”；父 Run 取消时按依赖关系取消子 Run。
- [ ] 子 Agent 只拿到明确传入的上下文、授权和附件引用，不自动复制完整历史、长期记忆或密钥。

**验收**：完成一次 3 个独立子任务的并行研究/分析演示；父任务取消、单个子任务失败、服务重启和重复完成事件都有确定状态与测试。

### P2：扩展到更完整的 Hermes 式多模态个人 Agent

以下能力不是 P0 Beta 的阻塞项，应在核心旅程稳定后选择实现：

- 第二/第三个消息平台、平台间会话接续和跨端未读同步。
- 语音输入、转写、TTS 回复及语音渠道；优先复用可私有化或可配置 Provider，不绑定单一外部服务。
- 通用网页搜索/网页提取供应商接入，使用安全 URL/SSRF 策略并展示来源；当前 MCP/HTTP 不等同于搜索体验。
- 按项目授权上下文包（文件夹、知识库集合、仓库说明）及其版本/来源，不做未授权的全盘扫描。
- 更丰富的图片生成/多模态创作工具、移动端原生审批和个人工作台离线/本机 Agent 能力。
- 组织级 Skill 市场与跨租户分享；必须复用现有脱敏、签名、评测、审批、灰度、回滚和 ACL。

## 6. 数据/API 改造建议

### 6.1 复用现有数据模型

继续复用 `agent_profiles`、`memories`、`agent_learning_jobs`、`agent_evolution_proposals`、Skill versions/releases、`agent_goals`、`agent_runs`、`agent_inbox_events`、`agent_channel_bindings`、`agent_channel_deliveries`、Event Outbox 和消息/会话表。不为“个人版”再造平行记忆、目标、发布或 Run 系统。

### 6.2 Gateway 最小增量模型（需评审后定稿）

建议新增两类持久化记录：

1. **`agent_channel_sessions`**：`binding_id`、`platform_account_id`、`external_conversation_id`、`user_id`、`tenant_id`、`session_id`、身份配对来源、状态、最后活动时间、创建/撤销时间。唯一键由渠道和外部会话身份组成，读取时再校验绑定属主/租户。
2. **`agent_channel_inbound_events`**：渠道事件 ID、binding/session/user 引用、事件类型、脱敏载荷摘要、payload hash、处理状态、claim/attempt、错误码、入站附件 CAS 引用、时间戳。按平台事件幂等；不长期保存无必要的完整原始消息副本。

出站继续使用 delivery/outbox，但要保证 Run 状态和 delivery 状态独立。数据保留遵从消息/附件保留策略及用户删除生命周期；迁移须同时覆盖 PostgreSQL 生产 schema 与 SQLite 测试/兼容 schema。

### 6.3 API 契约增量

- `POST /api/agents/goals/parse`：输入自然语言，返回未启用的结构化目标草案、风险/权限/费用说明和缺失信息问题。
- `POST /api/agents/goals`：仍作为唯一创建入口；要求客户端提交用户确认的草案版本/幂等键。
- `GET/POST /api/agents/channels/:id/pairing`、渠道 provider webhook 路由：只做身份配对和事件受理，正常消息最终进入现有 Session/Run 服务。
- `GET /api/agents/channels/:id/status`：返回脱敏后的连接状态、最后收发时间、失败计数和重连/撤销提示。
- `GET /api/agents/learning/overview`：补充最近学习作业、已启用经验使用情况、效果样本量和来源 Run 深链；不返回越权的 Trace 原文。
- `POST /api/agents/runs/:id/remember`：显式保存稳定偏好/经验的用户入口；服务端验证 Run 属主、敏感规则和 memory policy。

以上 API 是目标契约，不要求一次性全部实现；优先使用既有路径和 POST 操作语义，避免因前端绕过服务端授权而产生第二套策略。

## 7. 迭代排期与工作量

### 7.1 10～14 周 P0 计划

| 迭代 | 周期 | 核心交付 | 依赖/退出条件 |
|---|---:|---|---|
| Sprint 0：证据和基线 | 第 1 周 | 旅程基线、旧 SQLite 迁移修复、Beta 评测集、渠道选择 | 全量测试通过；首发渠道和部署边界冻结 |
| Sprint 1：个人首页 | 第 2～3 周 | 首次设置入口、个人首页、统一“开始任务” | 首个任务中位耗时 ≤ 3 分钟；Web/Electron 契约一致 |
| Sprint 2：记忆连续性 | 第 3～4 周 | 对话“记住/忘记”、记忆引用、会话回忆深链 | 删除/禁用即时生效；来源 ACL 测试通过 |
| Sprint 3：学习闭环 | 第 4～6 周 | Skill 命中说明、经验效果、暂停/撤销即时生效、用户纠正触发 | PostgreSQL 集成测试覆盖自动启用/待确认/拒绝/回滚 |
| Sprint 4：主动目标 | 第 5～7 周 | 自然语言目标草案、确认/预览、目标结果去向和 delivery 状态 | 目标重复触发与重启演练通过 |
| Sprint 5：首个双向渠道 | 第 6～9 周 | Adapter、配对、入站会话映射、出站回执、审批回调 | 安全/可靠性矩阵通过；至少 50 条端到端消息 |
| Sprint 6：Beta 质量门 | 第 9～10 周 | 产品评测、运营面板、试点反馈和缺陷关闭 | P0 指标有样本；无 P0 安全缺陷；手动回滚演练通过 |

允许 Sprint 1～4 并行开发；Sprint 5 渠道契约和身份模型需在 Sprint 0 评审后启动，避免各平台实现不兼容。

### 7.2 估算和角色

| 工作包 | 后端 | 前端/桌面 | QA/安全 | 合计估算 |
|---|---:|---:|---:|---:|
| 基线/迁移/评测集 | 1～2 | 0 | 1～2 | 2～4 人周 |
| 首页与首次体验 | 0.5～1 | 2～3 | 0.5～1 | 3～5 人周 |
| 记忆与学习感知 | 1～2 | 1～2 | 1～2 | 3～6 人周 |
| 目标草案与主动交付 | 1～2 | 1～2 | 1～2 | 3～6 人周 |
| 双向消息 Gateway 首发渠道 | 4～6 | 2～3 | 2～3 | 8～12 人周 |
| Beta 质量/发布/故障演练 | 1～2 | 0.5～1 | 2～3 | 4～6 人周 |
| **P0 合计** |  |  |  | **23～39 人周** |

若要求 P0 同时支持 Telegram + 飞书/企业微信、完整语音和大规模外部 Skill 市场，应单独立项，粗略再加 12～20 人周；先验证一个渠道和核心旅程再扩展。

## 8. 测试与发布门禁

### 8.1 自动化测试

- **单元**：个人档案字段冲突、记忆分类/脱敏、目标语言解析/时区、Skill 命中解释、Adapter 负载归一化、幂等键和投递状态转换。
- **PostgreSQL 集成**：渠道绑定与租户 ACL、inbound event 原子认领、目标调度租约、learning job 恢复、Skill release 真实匹配、用户导出/删除。
- **SQLite 兼容**：空库、旧版本快照、重复执行迁移、缺少可选表时安全跳过；不允许新迁移因表缺失而崩溃。
- **端到端**：Web 快速设置 → 对话/记忆 → 学习 Skill → 后续复用；自然语言目标草案 → 人工确认 → 运行 → 渠道投递；消息平台配对 → 继续会话 → 审批 → 结果返回。
- **故障矩阵**：渠道 429/5xx、超时/断网、Webhook 重放、乱序事件、数据库短故障、服务重启、审批过期、Outbox 多实例竞争、目标租约过期、学习模型失败、死信重投。
- **安全测试**：伪造 channel user ID、跨租户 external conversation、凭据日志泄漏、恶意附件、Prompt 注入 Skill、目标解析器的路径/SQL 注入、SSRF/重定向、低样本可靠性误判。

### 8.2 发布门禁

每次个人 Agent Beta 发布至少满足：

- `npm run check`、`npm run lint`、`npm run test:all` 全通过；不得保留未经批准的 allowlist 失败。
- PostgreSQL migration 在空库和当前生产影子快照验证；SQLite 旧快照兼容测试通过。
- 一次目标环境真实渠道验收：配对、正常消息、附件、审批、断网恢复、撤销和重复事件。
- 固定评测集回归通过；关键回答比较给出来源和授权范围。
- 运行结果与外部投递分开观测；回滚和人工接管流程有人负责。
- 试点用户明确知道哪些数据会被记忆、如何查看/删除、渠道消息如何保留。

## 9. 安全、隐私与运行治理

1. **消息身份**：平台用户标识只能用于找到已配对身份；敏感操作继续要求 Pivot 登录/二次确认或现有签名审批。绑定被撤销后，后续消息立即拒绝。
2. **租户隔离**：所有 Channel Session、Run、记忆、Skill 和 delivery 查询都以服务端解析的 user/tenant scope 为准；不得信任 payload 中的 userId/tenantId。
3. **记忆治理**：对敏感、一次性、凭据和客户机密默认不记忆；保存前分类，展示来源，提供导出/删除和“停止记住此类信息”。
4. **主动执行**：目标创建时冻结授权集合、预算和期限；非幂等副作用无论从 Web、IM、定时器还是子 Agent 触发，仍走同一 PEP/PDP/审批。
5. **工具/Skill 供应链**：声明能力不等于获得能力；新权限需要明确 diff、用户确认、验证和不可变版本。第三方代码/包不在 P0 自动执行。
6. **通道故障隔离**：消息发送失败不重做已成功的业务 Run；Run 失败不伪装为平台故障；失败通知使用限频和死信，避免告警风暴。
7. **保留策略**：入站消息正文沿用会话保留策略；Webhook 原始载荷只保留安全排障必要字段和有限时间，不记录 token/signature；附件引用继承现有访问和 GC 规则。
8. **可观测性**：指标只存低基数事件类型、平台、状态、延迟、错误码和样本量；消息正文、个人记忆和凭据不写 Prometheus 标签或常规日志。

## 10. 风险及应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 把“支持出站通知”误判成“消息 Gateway 已完成” | 排期错误、用户预期落差 | P0 明确验收双向连续对话、身份绑定、回执和失败恢复 |
| 自动学习产生噪声或错误经验 | 个性化反而降低质量 | 仅从多步/纠正/明确请求学习；每项可解释、可撤销；固定评测和低置信度待确认 |
| 自然语言计划误解频率/范围 | 后台错误运行或成本增加 | 先返回目标草案，不直接启用；明确时区、数据源、上限、通知方式和到期时间 |
| 多渠道平台政策/凭证审批延误 | Gateway 成为关键路径 | Sprint 0 先定首发平台；Adapter 通用；必要时以飞书/企业微信替代 Telegram |
| 企业治理 UI 继续堆复杂度 | 个人体验仍不自然 | 普通用户四个心智对象：档案、记忆、经验、持续目标；运维和发布高级页按角色隐藏 |
| 继续叠加工作流特性，推迟个人体验 | 产品方向被局部工程任务挤占 | P0 仅允许支持个人 Agent 核心旅程的 Runtime 改造；其余放 P1/P2 |
| 全量测试被旧迁移缺陷阻塞 | 无法判断本轮真实质量 | 先修复 `agent_dag_nodes` SQLite 条件建索引，再将 `test:all` 设为发布门禁 |

## 11. Definition of Done：个人 Agent Beta

以下全部成立，才可对外称为“达到 Hermes 式个人 Agent 核心体验（Pivot 安全治理版）”：

- 用户首次设置 ≤ 3 分钟并能完成首个任务；不要求用户先学习 Skill/工作流概念。
- 至少一种外部消息渠道支持已配对用户双向会话、任务持续/结果回传、审批交互和断线恢复。
- Agent 能在跨会话场景检索历史并使用受治理的长期记忆；能展示来源，且可记住、更正、暂停和删除。
- 个人重复任务能形成真实、版本化、已验证 Skill；下次能解释复用；误用可撤销；新权限绝不默默增加。
- 用户能通过自然语言预览并确认计划目标；目标在后台可恢复，结果送达状态可信且可操作。
- 待办/审批/失败/主动提醒/经验反馈能够在个人工作台或渠道中形成连续的行动闭环。
- 关键旅程有 Postgres 集成、端到端、权限隔离、重放、故障恢复测试；Beta 指标有足量真实样本。

## 12. 项目启动清单

### 第一周必须完成

- [ ] 任命产品/技术/渠道负责人；明确试点人群、首发渠道和允许的消息数据边界。
- [ ] 复现并修复 SQLite `agent_dag_nodes` 迁移测试失败。
- [ ] 从当前版本产物演示 6 条 P0 旅程，记录真实阻塞和缺陷，不以代码存在替代可用性。
- [ ] 冻结 P0 范围和指标；形成 30 条评测任务、10 条安全/故障用例。
- [ ] 画出消息身份映射、会话续接、Run、审批、outbox、delivery 的状态时序图并评审。

### 第二周进入开发前必须完成

- [ ] 确认首发平台开发者账号、Webhook 可达性、签名/密钥和测试群/测试用户。
- [ ] API、migration、前端空/加载/失败/撤销状态全部评审。
- [ ] 信任边界、用户删除、消息保留、附件存储和人工接管方案书面确认。
- [ ] P0 Sprint 中的每项任务都有责任人、测试文件和 Demo 验收步骤。

---

## 附录 A：官方基准文档

- [Hermes Persistent Memory and Session Search](https://hermes-agent.nousresearch.com/docs/user-guide/features/memory)
- [Hermes Skills System](https://hermes-agent.nousresearch.com/docs/user-guide/features/skills)
- [Hermes Scheduled Tasks / Cron](https://hermes-agent.nousresearch.com/docs/user-guide/features/cron)
- [Hermes Messaging Gateway](https://hermes-agent.nousresearch.com/docs/user-guide/messaging)
- [Hermes Subagent Delegation](https://hermes-agent.nousresearch.com/docs/user-guide/features/delegation)
- [Hermes Security](https://hermes-agent.nousresearch.com/docs/user-guide/security)

## 附录 B：本方案刻意不做的事

- 不复制 Hermes 的 CLI/TUI、Python Runtime、单用户文件存储或某家模型 Provider。
- 不为了追赶“工具数”把无审查的第三方工具/Skill 接入生产运行时。
- 不把组织级工作流市场、完整语音平台和所有 IM 渠道塞入 P0。
- 不把模型自由生成的 cron、SQL、目录路径、渠道身份或工具权限直接写入并启用。
- 不以任意成功率数字宣称“比 Hermes 更聪明”；能力增长必须有样本、对照评测和回滚证据。
