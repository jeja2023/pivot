# Pivot (智枢) —— AI 智能中枢管理系统

![版本](https://img.shields.io/badge/%E7%89%88%E6%9C%AC-0.0.242-%2310b981)
![授权](https://img.shields.io/badge/%E6%8E%88%E6%9D%83-%E5%85%A8%E6%A0%88%E7%89%88-blue)

**Pivot (智枢)** 是一款面向私有化、离线化和企业内网场景的全栈 AI 工作平台。系统以会话为高频入口，以应用、自动化工作区、知识库和工具库承载业务执行，并提供多模型接入、第三方 OpenAI-compatible API、审计日志、系统监控和企业级权限治理能力，目标是在可控环境中提供稳定、安全、可审计的 AI 工作入口。

> 普通用户使用说明请阅读 [Pivot 使用帮助](使用帮助.md)。部署后也可在前端左下角点击”帮助”，在应用内打开同一份帮助内容；直接访问 `/manual` 仍可独立查看。

### 当前界面结构

左侧导航保持单层结构：`搜索`打开会话/任务/工作流全局搜索，`应用`进入应用中心，`自动化`进入统一工作区，并通过顶部的`任务运行`、`工作流`和`计划任务`标签切换对应功能，`知识库`管理资料，`工具库`管理数据源、工具与连接；下方展示最近会话，底部`设置`会按账号权限打开系统设置或个人设置。

## 最新版本：0.0.242

### v0.0.242 更新摘要

- **旧数据库可正常升级**：修复自动化新增字段尚未迁移时提前创建索引导致的 `no such column: dedupe_key` 启动崩溃，历史数据库会先补列、再创建依赖索引。
- **迁移过程保持幂等**：全新安装与历史升级共用迁移阶段创建自动化幂等和调度索引，重复启动不会重复修改或破坏现有数据。
- **新建任务弹窗更舒展**：提高桌面端弹窗可用高度，在常见桌面视口中完整展示更多任务配置，减少不必要的垂直滚动，同时保留移动端视口边界。
- **使用帮助定位完善**：移除已经不存在的左侧“任务”入口说明，统一引导用户从“自动化”进入“任务运行”、工作流和计划任务，并补充产品定位、核心能力与工作方式选择说明。
- **升级验证完整**：新增历史自动化表快照回归，并完成真实数据库备份、完整性检查、两次连续启动和全量 `384/384` 回归验证。

### v0.0.241 更新摘要

- **自动化计划可靠性**：调度器采用租约、计划时段幂等键和崩溃恢复机制，失败自动退避重试并记录错误，连续失败后才暂停。
- **自动化安全边界**：账号撤权会暂停计划并取消未完成任务；模板、工作流、计划和运行记录的关联全部由服务端校验，队列增加账号级并发和写操作限流。
- **自动化操作体验**：手动运行按钮具备幂等保护，任务中心可以按计划 ID 精确查看历史运行；新增周日、非法输入和撤权隔离回归测试。

### v0.0.240 更新摘要

- **图片附件预览更清晰**：聊天输入框中的图片以缩略图卡片展示，明确区分读取中、待上传和预览不可用状态，避免浏览器破损图标造成误解。
- **附件上传状态更可靠**：图片预览地址在 DOM 插入后绑定，加载失败时保留附件并提示发送时上传，不会误判为上传失败。
- **任务模板通知中文化**：通知中心将工作流完成、失败、审批等历史英文状态统一显示为中文。

- **输入区操作精简**：移除清空输入框按钮及对应处理逻辑，保留用户熟悉的编辑、撤销和发送操作。
- **模型选择器就近移动**：当前模型从页面右上角移动到输入框右侧、发送箭头左侧，切换模型和发送消息形成连续操作路径。
- **模型触发控件纯文本化**：输入区只显示当前模型名称，不再显示能力图标和下拉箭头；能力说明仍保留在模型列表中。
- **响应式布局稳定**：长模型名称使用省略显示，桌面与移动端均保持发送按钮尺寸且无横向溢出；模型列表继续向上展开。
- **普通用户帮助重构**：《使用帮助》移除版本提示、更新记录、管理员配置、部署和实现说明，按实际导航重组为纯用户操作手册。
- **文档职责固化**：版本同步不再改写用户帮助，资源检查阻止发布记录和底层实现名词重新进入普通用户手册。
- **验证结果**：`npm run check`、`npm run lint` 和 `git diff --check` 通过；输入区完成桌面与 `390×844` 移动端实际页面检查。

## 历史版本：0.0.238

### v0.0.238 更新摘要

- **单层主导航启用**：左侧栏固定为搜索、应用、任务、自动化、知识库、工具库、最近会话和设置，不采用二级菜单；设置按角色进入系统设置或个人设置。
- **全局搜索打通**：搜索弹窗默认查找会话，可切换任务和工作流，并直接打开目标会话、任务详情或工作流编辑器。
- **统一任务中心上线**：自主任务、工作流任务和计划运行记录在同一页面查看和筛选；新建自主任务按需展开，任务记录成为默认首屏。
- **自动化资产中心上线**：默认管理工作流、发布状态和计划任务，编辑时进入可视化画布，保留版本、发布、运行、计划和追踪能力。
- **产品边界收口**：会话处理即时交互，自主任务处理需要规划的多步目标，自动化沉淀可复用流程；原“智能体”主入口不再单独存在。
- **工具库命名统一**：用户侧统一使用“工具库”管理数据源、工具和连接，普通会话可直接调用其中能力；不再兼容识别“工具箱”输入。
- **响应式体验优化**：移动端侧栏默认收起并可明确关闭，任务指标、筛选区和全局搜索在窄屏下重新排布，避免溢出和遮挡。
- **验证结果**：`npm run check`、`npm run lint`、Node 全量回归 `376/376` 和智能体安全回归 `40/40` 通过；桌面与移动端关键页面完成视觉检查。

## 历史版本：0.0.237

### v0.0.237 更新摘要

- **纯内网 HTTP 正式收口**：支持无公网 IPv4/IPv6、无域名、无 HTTPS 证书的隔离局域网部署；桌面端信任边界直接从实际 `remoteUrl` 派生，可使用私有 IP、IPv6 字面量或局域网主机名。
- **服务端与桌面端边界加固**：SSRF 策略覆盖 IPv4-mapped IPv6；Electron 增加同源导航/重定向守卫，并为本机授权、工具、窗口和更新 IPC 增加发送来源校验。
- **登录安全升级**：refresh token 改为数据库摘要存储和事务式一次性轮换，迁移兼容现有会话；修复退出登录 Cookie 路径，并为开放注册增加独立限流。
- **监控信息最小化**：公开 `/api/health` 不再暴露目录路径，详细诊断改为登录后访问并缓存；Prometheus 指标默认关闭，使用 Bearer token 鉴权，匿名局域网访问必须显式启用。
- **主动内容治理**：HTML 附件强制下载并启用 `nosniff`；动态 HTML 统一通过安全清洗助手；打印导出移除 `document.write`，安全扫描覆盖更多危险 DOM API。
- **依赖与打包优化**：Electron 升级到 `39.8.10`、electron-builder 升级到 `26.15.3`；生产依赖审计为 0 项，打包排除历史安装器，ASAR 约从 296 MB 降至 153 MB。
- **测试隔离提速**：Node/Playwright 使用独立临时数据目录，删除重复测试聚合并消除外部 DNS 等待；Node 回归 `376/376`、E2E `2/2`、Windows unpacked 打包全部通过。
- **上线注意事项**：服务启动时自动迁移历史 refresh token；管理员需配置 `METRICS_TOKEN`、移除初始化后的 `DEFAULT_ADMIN_PASSWORD` 并轮换管理员密码；无证书环境保持桌面自动更新关闭，使用受控分发与 SHA-256 校验。

## 历史版本：0.0.236

### v0.0.236 更新摘要

- **智能体工作台收口**：去除重复的工作流入口，统一顶部命令区、工作流状态和已保存工作流选择器；节点库、工具栏和画布操作职责更清晰。
- **多智能体语义明确**：`agent.delegate` 负责隔离上下文中的角色化子任务，`agent.handoff` 负责整理已有结论和证据；常规委派不再需要重复连接交接节点。
- **提示词库退场**：移除用户侧提示词库和套用提示词入口，保留服务端历史数据兼容；角色、输出格式和任务方法直接配置在当前任务或工作流节点中。
- **质量评测弹窗统一**：新建评测集、用例编辑和质量总览使用全局表单网格、统一按钮与固定操作区，长内容滚动和窄屏布局更稳定。
- **只读 SQL 可视化编写**：数据库查询节点支持读取表/字段、选择列、筛选、汇总、分组、排序和行数限制，实时生成 SQL；复杂 JOIN/CTE 可切换高级 SQL。
- **只读 SQL 弹窗样式收口**：可视化查询构建器样式纳入主工作区入口，节点配置弹窗改为专用单列布局，字体、控件高度和 SQL 预览区更贴近全局表单规范。
- **高级 SQL 切换修复**：修复“高级 SQL”按钮点击后被旧 active 状态覆盖的问题，并在从可视化模式切换时自动带入已生成 SQL，方便继续手工编辑。
- **查询安全治理保持有效**：标识符和文本值安全处理，默认最多返回 1000 行，继续执行后端只读校验、白名单和敏感字段脱敏，并修复 SQL Server `TOP` 重复追加。
- **SVG 图标统一**：工作流编排中原有 emoji 图标替换为统一 SVG；无图标的文字操作保持原样，不新增装饰元素。
- **验证结果**：`npm run check`、`npm run lint` 通过，DAG/查询回归 `7/7`、智能体安全回归 `40/40` 通过。

## 历史版本：0.0.235

### v0.0.235 更新摘要

- **新消息自动打断上一次生成**：此前大模型回复期间再次发送消息会被静默丢弃并提示"请等待完成或先停止生成"，用户必须手动点停止才能追问；现改为先中断当前生成再串行接管，符合主流 AI 对话产品的交互预期。
- **串行接管无并发窗口**：新消息发出时先 `abort()` 当前请求，等上一次发送的 `catch/finally` 清理完毕后才发起新请求，不存在两次请求同时在途的情况。
- **快速连发防竞争**：引入 `latestSendEpoch` 单调计数器，等待期间若有更新的发送进来则当前这次自动放弃，由最新那次接管，避免重复或乱序请求。
- **打断文案区分**：被新消息打断时历史消息干净截断，不再追加"已由用户中断生成"；用户手动点"停止"仍保留原提示文字。
- **空输入保护**：空输入不触发打断逻辑，不会意外中止正在进行的生成。

## 历史版本：0.0.234

### v0.0.234 更新摘要

- **条件分支真正可用**：`when` 条件规则此前已持久化但运行时从未求值，本版本补齐 16 种操作符的完整求值逻辑；`condition` 新增"上游失败后执行"，可编排失败补偿与降级路径，跳过原因会记录判断变量、期望值和实际值。
- **三种通用节点**：新增代码执行（`agent.code`，`vm` 沙箱内联 JS）、HTTP 请求（`agent.http`，复用 SSRF 防护，支持 GET/POST/PUT/DELETE/PATCH）和变量聚合（`agent.merge`，多上游输出合并为单一对象）。
- **画布运行状态可视化**：11 类节点按图标与主题色区分职责；运行中显示指示器与边框脉冲，完成显示耗时，失败显示错误摘要，跳过降低不透明度，执行进度直接反映在拓扑图上。
- **左侧节点库面板**：Dify 风格常驻面板按四组展示 10 种节点类型，支持按名称、用途和工具名搜索，点击即插入画布；窄屏自动隐藏并保留原工具栏入口。
- **节点配置结构化**：条件分支改为"变量 + 判断 + 值"三段式表单，变量输入支持上游输出自动补全，一元操作符自动隐藏值输入框；属性抽屉改为位移与透明度过渡。
- **界面细节修复**：原始输出折叠区高度上限 180px → 380px；补齐运行步骤列表缺失的深色模式覆写；修正工具栏状态元素类名拼写错误导致的样式失效。
- **完整质量验证**：全仓测试 `692/692` 通过，ESLint 零错误零警告，语法、资源清单、安全 HTML、全局变量基线和 E2E 冒烟检查全部通过。

## 历史版本：0.0.233

### v0.0.233 更新摘要

- **结果体验产品化**：普通任务、工具步骤、工作流节点和最终答案不再以原始 JSON 作为主视图，统一展示为摘要、列表、键值信息、表格或 Markdown，原始结构保留在折叠区。
- **工作流编排稳定化**：节点坐标使用根级 `layout` 持久化，兼容旧坐标迁移且不覆盖人工布局；通过 `primaryLlmNodeId` 统一主大模型节点在编辑、预检、发布、运行和恢复阶段的语义。
- **契约与发布治理**：节点支持输入/输出 JSON Schema、工具契约同步和高级编辑；预检、发布门禁和运行时校验贯通，并提供字段级可读错误。
- **可追踪与可恢复**：Agent Trace 时间瀑布覆盖模型、工具、DAG、审批、委派和交接，敏感字段持久化前脱敏；持久化检查点支持自由任务恢复观察和工作流从失败节点续跑。
- **质量评测闭环**：支持评测集、真实任务批量回归、确定性规则评分、历史基线对比和任务追踪回链，评测数据及子任务按用户隔离。
- **多智能体协作**：新增 `agent.delegate`、`agent.handoff` 和 Supervisor 审阅模板，支持隔离上下文中的角色化委派、并行研究、独立审阅和结构化交接。
- **响应式界面升级**：评测中心、运行详情和编排器完成桌面与移动端适配，配置入口采用稳定 2×2 布局，移动端提示避开顶部命令区。
- **完整质量验证**：智能体核心回归 `51/51`、全仓测试 `691/691` 通过，ESLint 零错误零警告，并完成 1440px 桌面和 390px 窄屏实际浏览器验收。

## 历史版本：0.0.232

### v0.0.232 更新摘要

- **质量评测中心**：支持自由任务和已发布工作流的评测集、用例管理、批量真实运行、归档和用户隔离，工作台集中展示均分、通过率、运行进度、耗时与 Token。
- **确定性回归评分**：支持关键短语、禁用短语、期望文本、长度、耗时、Token、有效 JSON 和输出 JSON Schema 规则，并在结果中展示逐条判定、实际输出和任务追踪入口。
- **质量基线对比**：自动对比上一完成批次的平均分与通过率变化；普通任务可一键加入评测集，形成从真实问题到固定回归用例的闭环。
- **多智能体协作**：新增 `agent.delegate` 和 `agent.handoff`，支持角色化子任务委派、隔离上下文执行和包含证据、风险、未决问题与置信度的结构化交接。
- **Supervisor 编排模板**：工作流模板新增并行研究员、审阅员与主 Supervisor 裁决结构，自动设置主大模型节点、依赖和画布位置，并在 Trace 与 DAG 结果中使用独立可读类型展示。
- **运行治理**：评测子任务默认从普通任务列表隐藏但保留完整追踪；限制重复与并发批次，软删除历史用例，保证资源可控和结果可审计。
- **界面收口**：质量中心、用例详情和编辑器采用响应式布局与固定操作区，智能体配置入口调整为稳定的 2×2 操作矩阵，移动端操作提示不再遮挡顶部命令。

## 历史版本：0.0.231

### v0.0.231 更新摘要

- **运行链路可追踪**：新增 Agent Trace 和 Span 存储，覆盖模型路由、规划、流式工具、DAG 节点、总结与审批等待；运行详情以时间瀑布展示状态、耗时和可读上下文。
- **节点数据契约**：DAG 节点支持输入/输出 JSON Schema，运行前检查定义和静态输入，运行时校验解析后的真实输入与业务输出，并在节点结果中显示契约状态和字段级错误。
- **编排器体验升级**：节点侧栏展示契约覆盖状态与字段摘要，提供工具契约同步、输出类型快捷预设和高级契约编辑器，桌面与 390px 窄屏均完成布局验收。
- **发布治理增强**：预检展示输入/输出契约覆盖率，发布前阻止不可用工具、无效契约和输入不匹配；未定义输出契约以风险提示兼容旧工作流。
- **隐私与隔离**：追踪数据按任务所有者隔离，密码、令牌、密钥、授权头和 Cookie 等字段写入前递归脱敏，采集异常不会阻断任务执行。
- **检查点续跑**：规划、工具和 DAG 步骤会生成持久化检查点；运行详情显示可用数量，自由任务恢复成功观察，DAG 从失败节点继续并复用已完成结果。
- **回归验证**：智能体核心回归 `48/48`，392 个 JavaScript 文件语法、聊天资源、开发规范、文本完整性与安全 HTML 检查全部通过。

## 历史版本：0.0.230

### v0.0.230 更新摘要

- **工作流 DAG 布局持久化**：节点坐标改为根级 `layout` 保存，兼容旧 `_x/_y` 迁移，新增/删除/拖拽不再整图重排。
- **主大模型节点显式化**：编辑器、运行预检、恢复/续跑、自由任务草稿统一使用 `primaryLlmNodeId`，并在画布、侧栏和工具栏中提供明确标记。
- **结果展示可读化**：普通任务与工作流执行页的 JSON 输出改为结构化摘要、列表和表格展示，原始内容收进折叠区域。
- **模板与样式同步**：统计图模板改为查询 + 大模型 + 图表并行布局，补齐主节点视觉、抽屉状态和移动端样式。
- **回归测试补齐**：新增 DAG 布局测试，覆盖坐标迁移、主节点识别、依赖规则和结果展示回归。

### v0.0.229 更新摘要

- **生产依赖高危清零**：axios 升级到 1.18.1（含削弱 SSRF 防护的 NO_PROXY 绕过与原型污染）、sharp 升级到 0.35.3 修复 libvips 四个 CVE、js-yaml 升级到 4.3.0 修复二次方 CPU 消耗；`npm audit` 生产依赖为 0 项漏洞。
- **审计门禁可追溯**：豁免必须登记理由与复查日期，到期自动失效并让门禁转红，上游已有修复版本时不接受豁免。
- **CI 校验范围补齐**：此前 CI 只跑单个测试套件和三项检查，现改为 `npm run check` + ESLint + `npm run test:all` 全量 673 项。
- **内置报表 CSV 中文乱码修复**：读取 CSV 改为先按 UTF-8 解码并剥离 BOM，解决不带 BOM 的 UTF-8 报表中文表头和内容乱码后被带入模型上下文的问题。
- **运行环境要求**：`engines.node` 收紧为 `>=20.9.0`，与 sharp 0.35 一致。

### v0.0.228 更新摘要

- **Qwen3.6 thinking 硬关闭**：代码补全除 `/no_think` 外，会向 llama.cpp 发送 `chat_template_kwargs.enable_thinking=false`，解决 `peg-native` chat template 仍生成大量 thinking token、正文为空的问题。
- **普通模型隔离**：模板硬开关只应用于 Qwen3 系列，Qwen2.5 等普通模型请求体和补全行为保持不变。
- **诊断与验证**：空正文日志增加 `hardThinkingDisabled`，生产等价的非流式、流式和批量回归均要求硬开关生效；完整安全测试 `316/316` 通过。

### v0.0.227 更新摘要

- **Qwen3 代码补全修复**：代码补全会自动为 Qwen3.6 等推理模型关闭 thinking，避免输出预算耗在思考阶段后返回空补全。
- **双入口兼容**：`/v1/completions` 和使用 `prompt` / `input` / `prefix` / `suffix` 的 `/v1/chat/completions` 均应用同一处理，Qwen2.5 等普通模型行为不变。
- **Completions 兼容补齐**：文本 prompt 数组、`n`、`best_of`、`echo`、`logprobs`、`seed`、`stream_options`、`logit_bias` 和 `user` 均已支持，非流式与流式都能处理多个 choices 并正确汇总 usage。
- **错误与空正文可诊断**：上游流中断返回 SSE error 且不再伪造 `[DONE]`；两个补全入口都会记录 reasoning 状态、结束原因、token 用量和输出上限，不向客户端泄露思考内容。
- **验证与版本启用**：完整安全测试 `315/315` 已通过，应用版本升级至 `0.0.227`。

### v0.0.226 更新摘要

- **删除后可同名注册**：已删除账号不再继续占用用户名，管理员创建用户和开放注册均可重新使用该名称。
- **新旧账号数据隔离**：同名重新注册会创建全新用户 ID，不会继承旧账号设置、会话或其他关联数据。
- **历史数据自动兼容**：服务启动时自动迁移已有软删除用户，并在审计、统计和业务记录中继续显示删除前用户名。
- **验证与版本启用**：完整安全测试 `309/309`、项目检查和 ESLint 已通过，应用版本升级至 `0.0.226`。

### v0.0.225 更新摘要

- **更新日志补齐**：补齐 `CHANGELOG.md` 中缺失的 `v0.0.224` 与 `v0.0.225` 发布记录，保持历史版本连续。
- **版本号确认**：当前最新版本统一为 `0.0.225`，并同步 `package.json`、`package-lock.json`、README、使用帮助和项目汇报展示。

### v0.0.224 更新摘要

- **任务栏名称修复**：修复 Windows 客户端运行阶段任务栏只显示 Logo 图标、缺少系统名称的问题，支持窗口标题动态同步。
- **关于弹窗优化**：移除“关于”弹窗中的环境信息显示，并为无边框窗口增加 1px 细边框。
- **代码规范性修复**：修复 `mcp-workbench-form.js` 和 `mcp-workbench-main.js` 的 ESLint 未定义全局变量错误，并清理测试用例警告。
- **版本号启用**：应用版本升级至 `0.0.224`，用于桌面客户端重新打包和自动更新检测。

### v0.0.223 更新摘要

- **关于页面中文字体还原**：将 `about.html` 中的 HTML 字符实体还原为直观的 UTF-8 中文字符，提升源码可读性与维护效率。
- **侧边栏版权信息移除**：移除主页左侧导航栏面板中，用户名称及操作按钮下方的 copyright 声明，保持界面轻量与简洁。
- **登录页安全提示移除**：移除登录面板底部的“安全可信的内网 AI 平台”提示区，使登录界面更清爽。
- **版本号启用**：应用版本升级至 `0.0.223`，用于桌面客户端重新打包和自动更新检测。

### v0.0.222 更新摘要

- **关于弹窗再优化**：移除底部“确定”按钮，关闭操作收口到右上角和 `Esc`。
- **底部裁切修复**：收紧弹窗间距并清空底部区域，不再显示确认按钮和版权信息，避免操作区显示不完整。
- **版本号启用**：应用版本升级至 `0.0.222`，用于桌面客户端重新打包和自动更新检测。

### v0.0.221 更新摘要

- **关于弹窗优化**：“关于 Pivot”窗口放大至 `500x400`，品牌区、信息卡片和底部操作重新梳理。
- **运行模式不再截断**：运行模式独占一行显示，远程模式、本地模式等文案可完整阅读。
- **版本号启用**：应用版本升级至 `0.0.221`，用于桌面客户端重新打包和自动更新检测。

### v0.0.220 更新摘要

- **客户端菜单重组**：桌面客户端顶部菜单调整为“页面 / 显示 / 客户端”，移除点击后容易丢失输入焦点的“编辑”菜单。
- **页面刷新入口前置**：“页面”承载刷新页面、清理缓存并刷新，方便客户端更新后主动清理页面、PWA 和 Service Worker 缓存。
- **显示与客户端分工**：“显示”仅保留实际大小、放大、缩小和切换全屏；“客户端”保留检查更新、关于 Pivot 和退出客户端，帮助继续由左下角“帮助”入口承载。
- **文本编辑回归原生快捷键**：撤销、重做、剪切、复制、粘贴和全选继续使用输入框及系统原生快捷键，避免菜单点击改变焦点后造成无效操作。
- **回归验证**：`node scripts/check_chat_assets.js`、`node --check client/chat/desktop-menu.js`、`node --check desktop/main.js`、`node --check desktop/preload.js` 和 `npm run check` 已通过。
- **版本号启用**：应用版本升级至 `0.0.220`，用于桌面客户端重新打包和自动更新检测。

### v0.0.219 更新摘要

- **客户端顶部菜单收口**：桌面客户端顶部仅保留“编辑 / 视图 / 客户端”，完整移除“会话”菜单及其子菜单，避免和左侧页面导航重复。
- **视图菜单降噪**：“视图”仅保留刷新、实际大小、放大、缩小和切换全屏，移除“开发者工具”入口。
- **客户端维护入口保留**：“客户端”继续承载清理缓存并刷新、检查更新、关于 Pivot 和退出客户端；帮助继续由左下角“帮助”入口承载。
- **关于窗口样式优化**：“关于 Pivot”从 Windows 默认消息框改为自定义桌面模态窗口，统一展示品牌、版本、运行模式和环境信息。
- **回归验证**：`node scripts/check_chat_assets.js`、`node --check client/chat/desktop-menu.js`、`node --check desktop/main.js`、`node --check desktop/about-preload.js` 和 `npm run check` 已通过。
- **版本号启用**：应用版本保持 `0.0.219`，用于桌面客户端重新打包和自动更新检测。

### v0.0.218 更新摘要

- **客户端顶部菜单归位**：桌面客户端的“文件 / 编辑 / 视图 / 帮助”菜单移动到窗口最上方标题栏区域，与右侧窗口控制按钮保持同一行。
- **侧栏品牌恢复**：左侧栏顶部恢复 Pivot 智枢品牌标识，新建对话按钮继续保留在品牌区右侧。
- **顶部横线补齐**：标题栏底部分隔线贯通整个窗口宽度，右侧关闭按钮下方不再出现断线。
- **回归验证**：`node scripts/check_chat_assets.js`、`node --check client/chat/desktop-menu.js` 和 `npm run check` 已通过。
- **版本号启用**：应用版本升级至 `0.0.218`，用于桌面客户端重新打包和自动更新检测。
### v0.0.217 更新摘要

- **登录态过期自动回登录页**：cookie、access token、CSRF token 或 refresh token 失效后，不再需要用户手动退出；无法续期时会自动清理前端登录态并显示登录页。
- **自动刷新与原请求重试**：access/CSRF 失效但 refresh 仍有效时，前端会调用 `/api/auth/refresh` 获取新的 CSRF token，并自动重试原请求。
- **认证错误码补齐**：后端为缺失认证 cookie 和 CSRF 校验失败补充 `AUTH_MISSING`、`CSRF_INVALID`，前端统一请求层可稳定识别登录态失效。
- **默认失效时间说明**：当前默认 access/CSRF cookie 为 480 分钟（8 小时），refresh cookie 为 30 天，可通过环境变量调整。
- **桌面客户端刷新菜单**：客户端恢复中文“文件 / 编辑 / 视图 / 帮助”菜单，支持刷新页面、清理缓存并刷新、打开帮助和检查客户端更新；客户端原生标题栏不再显示重复的 Pivot 图标和标题文字。
- **回归验证补齐**：新增认证失效刷新/回登录测试，`node tests\security-auth.test.js` 和 `npm run check` 已通过。
- **版本号启用**：应用版本升级至 `0.0.217`，同步更新包版本、README、使用帮助、项目汇报和更新日志。

### v0.0.216 更新摘要

- **思考模型工具结果采信修复**：工具箱已完成本机报表目录查询后，思考模型会优先依据本轮 `mcp.0.*` 工具结果回答，不再被长期记忆带偏为“无法访问本机文件系统”。
- **MCP 结果上下文增强**：成功工具结果新增 `PIVOT_MCP_TOOL_RESULT_BEGIN/END` 标记，并以“assistant 工具结果 + user 基于结果回答”的末尾轮次进入最终模型请求。
- **本机目录回答约束**：当结果来自本机报表目录或本机虚拟工具时，最终回答明确禁止要求用户自行查看目录；若工具结果不足，只说明缺少的信息。
- **上下文预算与回归覆盖**：上下文裁剪会保护新的 MCP 工具结果标记，新增思考模型上下文形态和本机目录工具结果标记测试。
- **版本号启用**：应用版本升级至 `0.0.216`，同步更新包版本、README、使用帮助、项目汇报、工具箱设计方案和更新日志。

### v0.0.215 更新摘要

- **API 接入指南补齐**：API 接入管理页明确展示聊天对话、代码补全和向量生成三类外部接口，分别对应 `/v1/chat/completions`、`/v1/completions` 和 `/v1/embeddings`。
- **代码补全提示显性化**：接入指南和可用模型说明同步标注 `/v1/completions` 支持 `prompt`、`input`、`prefix`、`suffix` 风格请求，方便 Continue 等代码补全客户端接入。
- **聊天命名修正**：用户侧文案从“聊天补全”调整为“聊天对话”，保留标准兼容路径 `/v1/chat/completions`，避免与代码补全概念混淆。
- **版本号启用**：应用版本升级至 `0.0.215`，同步更新包版本、README、项目汇报和更新日志。

### v0.0.214 更新摘要

- **remote 本机 MCP 执行闭环**：桌面端授权后的 SQLite 与报表目录继续以 `mcp.0.*` 形式提供只读工具，补齐设备心跳、任务领取、结果回传和本机执行错误归一化。
- **普通对话本机目录查询增强**：查询本机目录文件清单时会确定性调用 `mcp.0.reports.list_files`，并识别授权根目录名称，避免误用数据库工具或误报规划失败。
- **工具箱体验优化**：本机授权入口、工具数量、工具弹窗、治理指标卡、数据来源标签和授权按钮进一步收口，减少拥挤、截断和状态不一致。
- **统计文案与诊断统一**：聊天消息统计统一显示 `Tokens` 与 `t/s`；本机桥接缺失、授权缺失、旧客户端、治理过滤和路径错误会给出更清晰的中文原因。
- **文档与测试同步**：CHANGELOG、README、使用帮助、项目汇报、工具箱设计方案和包版本同步启用 `0.0.214`，本机桥接链路补充安全回归覆盖。

### v0.0.213 更新摘要

- **remote 本机执行环境初始化修复**：桌面端执行 `mcp.0.*` 本机工具前会把数据、上传、分析和日志目录初始化到用户数据目录，避免 asar 内写入 `logs` 触发 `ENOTDIR`。
- **授权路径一致性修复**：本机执行器固定读取桌面端用户数据目录中的 `local-authorizations.json`，保证工具箱显示的授权与实际执行一致。
- **错误细节透传**：本机执行失败时保留受控底层细节，便于从服务端日志继续定位打包态和权限问题。
- **验证结果**：当前授权的 `D:\法律` 目录可列出 3 个 Markdown 文件，`node tests\security-mcp.test.js` 通过 31/31。

### v0.0.212 更新摘要

- **打包桌面端本机目录修复**：列本机报表目录时不再提前加载 Excel 解析库，避免 asar 打包后普通目录清单触发无关路径错误。
- **本机桥接错误状态修复**：服务端会把本机路径错误归一化为 400/404/403，不再把 `ENOTDIR` 等错误显示为 HTTP 500。
- **回归测试补齐**：新增桥接任务错误归一化测试，`security-mcp` 覆盖提升至 31 项。

### v0.0.211 更新摘要

- **本机目录名识别修复**：授权目录名为“法律”等场景下，普通对话会列出授权根目录文件，不再把目录名当作文件名过滤条件。
- **桌面端错误回传收口**：本机只读执行器会把路径不存在、路径类型不匹配和权限不足等错误结构化回传，聊天页提示更接近真实原因。
- **虚拟 MCP 日志修复**：`mcp.0.*` 调用日志改为本机虚拟工具记录口径，避免写入不存在的 `server_id=0` 触发外键错误。
- **前端可见文案中文化**：聊天消息统计与本机执行器诊断改为中文展示，减少普通用户看到内部接口名。
- **文档与版本同步**：更新 CHANGELOG、README、项目汇报、工具箱设计方案、package.json 和 package-lock.json，项目版本启用 0.0.211。

### v0.0.210 更新摘要

- **本机执行器轮询更稳**：聊天页不再因为 `currentUser` 初始化时序导致桌面端本机任务无人领取。
- **本机目录查询错误更准**：确定性使用 `mcp.0.reports.list_files` 后，失败会提示工具执行链路问题，不再误报工具规划失败。
- **回归测试补齐**：覆盖本机目录工具失败不再请求规划模型的场景。

### v0.0.209 更新摘要

- **聊天页本机执行器主动心跳**：普通对话页加载后会主动尝试同步桌面端本机执行器。
- **桥接诊断随请求上报**：发送启用工具箱的消息时，会同步桌面端桥、执行接口和授权摘要诊断。
- **缺失原因更精确**：区分普通浏览器、旧桌面客户端、未授权报表目录和心跳注册失败。

### v0.0.208 更新摘要

- **聊天页本机执行器心跳修复**：remote 桌面端打开聊天页后会默认加载本机执行器桥接脚本，不再依赖先进入工具箱工作区才同步心跳。
- **发送前同步授权快照**：普通对话启用工具箱后，发送消息前会主动同步桌面端本机授权摘要，确保 `mcp.0.*` 工具进入本轮可用工具上下文。
- **本机目录请求确定性调用**：查询本机目录文件清单时优先使用 `mcp.0.reports.list_files`，避免被误判为普通问答或误用数据库工具。
- **诊断提示更明确**：缺少心跳、缺少报表目录授权和工具治理过滤会分别提示，方便按客户端、授权和工具配置逐层排查。
- **文档与版本同步**：更新 CHANGELOG、README、项目汇报、工具箱设计方案、package.json 和 package-lock.json，项目版本启用 0.0.208。

### v0.0.207 更新摘要

- **remote 模式本机反向执行启用**：生产客户端继续使用 `mode: "remote"` 连接统一服务器，服务器侧大模型、OCR/PDF、法规查询、文档处理、工具策略和审计能力不拆散。
- **本机授权映射为 `mcp.0.*`**：桌面端授权本机 SQLite 或本机报表目录后，工具箱会动态暴露 `mcp.0.db.*` 和 `mcp.0.reports.*`，用于只读 SQL、目录扫描、文件摘要和 CSV/Excel 查询。
- **桌面端领取任务并本机执行**：服务器只创建待执行任务，桌面端长轮询领取后在用户电脑本机执行，再把结构化结果回传；服务器不主动访问用户电脑的 `localhost` 或本机绝对路径。
- **授权状态即时同步**：授权中心刷新、授权成功和撤销授权后会立即同步本机执行器状态，减少工具列表出现或消失的延迟。
- **安全回归覆盖**：新增 remote 桥接安全测试，已通过 `node tests\security-mcp.test.js`（26/26）和 `npm run check`。
- **文档与版本同步**：更新 CHANGELOG、README、使用帮助、项目汇报、工具箱设计方案、package.json 和 package-lock.json，项目版本启用 0.0.207。

### v0.0.206 更新摘要

- **我的电脑合并单入口**：将原先并排展示且指向同一个本机授权中心弹窗的“连接本机数据库”和“授权指定目录”合并为“管理本机资源授权”单一入口，并在 `mcp-system-cards.css` 中将顶层栅格平分为完美对称的 `1fr:1fr` 栅格，消除了原先左侧极长、右侧极挤的突兀感。
- **支持长设备名悬浮展示**：将“数据管理”与“我的电脑”左边头部信息区宽度由 `84px` / `112px` 增加至 `120px`，并在说明节点添加 `title` 属性，当设备名称由于超长被截断时，鼠标悬停即可查看完整的本地设备名称与授权描述。
- **指标卡片视觉降噪**：重构了治理工具指标卡片（可用服务、需要处理、待刷新、数据连接、调用错误率），移除刺眼的彩色高亮底色和脉冲红点，统一为低调大方的莫兰迪灰白背景（`#f8fafc`）与中灰框线（`rgba(148, 163, 184, 0.18)`），突出指标数据可读性。
- **刷新按钮位置优化**：刷新按钮从顶栏右侧移至 governance-metrics 卡片的最左侧，并将其定位外边距从 margin-left 调整为 margin-right，使布局在从左往右阅读时衔接更自然、间距更均匀。
- **工具卡片高度压缩**：压缩系统和实例卡片的高度（由 `100px` 压缩为 `70px`） and 内边距，减少标签（margin 压缩至 `2px 0`，padding 压缩至 `1px 5px`）、标题和段落文本的行间距，确保在屏幕分辨率受限或浏览器开启书签栏时能多留出超过 100px 的垂直空间，防止底部的“其它用户个人工具”入口被截断。
- **治理建议长文本修复**：为建议日志列表覆盖 `white-space: normal` 样式，保证长句子建议在小窗口下能够自动换行显示，不再因越界而被截断。
- **授权按钮配色优化**：重新设计了“我的电脑”右侧的授权/管理授权按钮样式，使用 `em` 承载更精细的双态视觉设计。激活态使用墨绿渐变底色，已授权态则为中性浅灰底，点击与悬浮过渡层次分明。

### v0.0.205 更新摘要

- **本轮变更统一归档**：汇总 `v0.0.197` 至 `v0.0.204` 的工具箱、数据分析、OCR/PDF、本机授权、桌面更新和部署优化，作为当前发布口径。
- **工具箱与本机能力闭环**：记录数据管理单入口、已导入数据集摘要、SQLite 导入、admin 多用户工具收口、桌面端本机授权中心和本机只读执行器等修改。
- **OCR/PDF 边界明确**：记录 OCR 服务彻底外置、HTTP 对接协议、独立 OCR 项目指南、文字识别/PDF 任务删除和多输出下载能力。
- **桌面更新与部署文档同步**：记录 `/downloads/` 自动更新、打包产物归档、离线局域网 HTTP 白名单更新策略和生产部署文档调整。
- **版本号启用**：当前版本启用 0.0.205，`CHANGELOG.md`、README、使用帮助、项目汇报、`package.json` 和 `package-lock.json` 保持一致。

### v0.0.204 更新摘要

- **离线局域网 HTTP 更新**：无互联网、无 HTTPS 的内网生产环境可以通过显式配置启用客户端自动更新。
- **HTTP 风险显式确认**：新增 `autoUpdate.allowInsecureHttp=true`，默认仍不允许普通 HTTP 更新源。
- **更新源白名单**：非本机 HTTP feed 必须配置 `autoUpdate.allowedOrigins`，只允许固定内网 IP/域名作为更新来源。
- **同站 downloads 继续复用**：`remoteUrl=http://.../`、`path=/downloads/`、`url=""` 会自动推导到内网 `/downloads/` 更新目录。
- **文档与版本同步**：更新配置模板、远程部署说明、downloads 说明、README、使用帮助和项目汇报，项目版本启用 0.0.204。

### v0.0.203 更新摘要

- **自动更新接入 downloads**：桌面客户端启用自动更新后，可默认从生产站点 `/downloads/` 读取 `latest.yml` 并下载安装新版本。
- **打包产物自动归档**：`npm run dist:win` 会把安装器、`.blockmap` 和 `latest.yml` 复制到 `downloads/`，并生成 `Pivot-Setup.exe` 供网页登录页下载。
- **安全策略保持收口**：生产更新源仍要求 HTTPS；本机 HTTP 仅用于显式放开的 loopback 开发测试。
- **配置更轻**：生产环境可只配置 `remoteUrl=https://.../`、`autoUpdate.enabled=true` 和默认 `autoUpdate.path=/downloads/`，无需另写独立更新 URL。
- **中文更新体验**：桌面端更新检查、下载完成和重启安装提示统一为中文。
- **文档与版本同步**：更新 CHANGELOG、README、使用帮助、项目汇报、远程部署说明和 downloads 目录说明，项目版本启用 0.0.203。

### v0.0.202 更新摘要

- **本机授权中心启用**：工具箱“我的电脑”中的连接本机数据库、授权本机报表目录现在可以打开本机授权中心。
- **桌面端授权桥**：桌面客户端新增受控 IPC/preload 方法，可选择本机 SQLite 文件或本机报表目录，并支持撤销授权。
- **本机路径安全收口**：完整本机路径只保存在桌面客户端本机配置中，页面只展示资源名称、设备名、授权时间和路径摘要，不上传服务器。
- **授权状态联动首页**：授权或撤销后工具箱首页会刷新本机入口状态，显示待授权、已授权或需客户端。
- **本机只读执行器补齐**：授权后的本机 SQLite 和本机报表目录会作为“我的电脑”虚拟 MCP 工具暴露，只允许只读查询、目录扫描、文件摘要和结构化结果回传；未检测到桌面端/本地助手或未授权时不会启用。
- **文档与版本同步**：更新 CHANGELOG、README、使用帮助、项目汇报、设计方案、package.json 和 package-lock.json，项目版本启用 0.0.202。

### v0.0.201 更新摘要

- **工具箱数据管理单入口**：导入数据文件、导入报表文件和已导入数据集摘要合并为一个“打开数据总览”主入口，避免多个按钮跳转同一页面。
- **本机授权入口独立呈现**：连接本机数据库和授权本机报表目录改为“我的电脑”动作区，继续强调需要桌面端或本地助手授权。
- **连接器卡片层级收口**：数据来源卡片网格只保留服务器可访问数据库、服务器可访问报表目录和已配置连接等长期入口，数据集管理不再单独占卡片。
- **宽扁卡片与可读性修复**：工具箱卡片、治理条和动作区保持宽扁规格，数据管理说明完整换行，标签和状态文字减少截断。
- **admin 多用户工具收口**：admin 首页只展开全局和本人连接，其它用户个人工具折叠成治理摘要，避免大量个人工具铺满工具箱。
- **文档与版本同步**：更新 CHANGELOG、README、使用帮助、项目汇报、package.json 和 package-lock.json，项目版本启用 0.0.201。

### v0.0.200 更新摘要

- **文字识别任务可删除**：文字识别数据列表和识别结果详情新增删除按钮，可直接移除不再需要的任务记录。
- **PDF 工具任务可删除**：PDF 工具数据列表和处理结果详情新增删除按钮，任务清理入口与文字识别保持一致。
- **PDF 多输出一键下载**：拆分 PDF、PDF 转图片等多输出任务点击“下载”会自动打包为 ZIP，详情中也提供“全部下载”。
- **受控产物清理**：后端新增统一删除服务，删除任务时同步清理输出文件、页面预览、识别块、复核记录和对应上传源文件。
- **文档与版本同步**：更新 CHANGELOG、README、使用帮助、项目汇报、package.json 和 package-lock.json，项目版本启用 0.0.200。

### v0.0.199 更新摘要

- **工具箱数据集摘要入口**：工具箱数据来源区不再逐个展示用户已导入文件，改为固定的“已导入数据集”摘要卡。
- **大数据量页面收口**：数据集列表、搜索、删除、导出和定位仍放在数据分析工作台，工具箱只承担入口和边界说明。
- **轻量统计接口**：新增数据集摘要接口，首页只读取数量和总行数，避免为了展示入口拉取字段画像、预览行和文件清单。
- **文档与版本同步**：更新 CHANGELOG、README、使用帮助、项目汇报、package.json 和 package-lock.json，项目版本启用 0.0.199。

### v0.0.198 更新摘要

- **OCR 项目彻底外置**：Pivot 主项目删除仓库内 OCR 服务实现、本地命令适配器和 OCR compose，只保留 HTTP OCR 客户端。
- **外部 OCR 协议固化**：新增 OCR 服务对接协议，明确 `GET /health`、`POST /ocr`、请求体、响应体和错误格式。
- **独立 OCR 项目文档**：新增独立 OCR 项目搭建指南，说明目录结构、镜像构建、Docker 网络、验收清单和 PaddleOCR-VL 选型建议。
- **生产部署边界清晰**：根 compose 只编排 Pivot，OCR 镜像、模型、字体、缓存和 GPU/CPU 依赖由独立 `ocr-service` 项目维护；OCR 服务地址可由管理员在文字识别页面保存更新。
- **旧配置兼容收口**：旧的 `paddle` / `tesseract` 引擎值会自动归一到 `http`，避免升级后继续寻找本地 OCR 命令。
- **文档与版本同步**：更新 CHANGELOG、README、使用帮助、项目汇报、package.json 和 package-lock.json，项目版本启用 0.0.198。

### v0.0.197 更新摘要

- **工具箱数据来源重组**：工具箱首页按数据来源、处理与分析、消息通知、扩展治理组织入口，卡片展示执行位置、归属范围、风险等级和自动化状态。
- **服务器可访问资源命名落地**：数据库入口统一为“服务器可访问数据库”，报表目录统一为“服务器可访问报表目录”，并明确 `localhost` 指 Pivot 服务器。
- **SQLite 数据集导入**：数据分析上传支持 CSV、Excel 和 SQLite（.sqlite/.sqlite3/.db），SQLite 会以只读方式导入可用表或视图并生成统一数据集。
- **已导入数据集摘要入口**：工具箱数据来源区只展示已导入数据集摘要入口，不逐个铺开用户文件；打开数据分析后继续查询、透视、图表或智能分析。
- **诊断提示增强**：数据库和报表目录诊断失败时返回服务器网络、服务器路径、读取权限和脱敏诊断信息，减少本机路径与服务器路径混淆。
- **文档与版本同步**：更新 CHANGELOG、README、使用帮助、项目汇报、package.json 和 package-lock.json，项目版本启用 0.0.197。

### v0.0.196 更新摘要

- **OCR 外部服务对接**：文字识别默认通过 HTTP 调用独立 OCR 项目，Pivot 不内置 OCR 引擎运行时。
- **OCR 协议归一**：Pivot 接收外部 OCR 服务返回的文本块、置信度和坐标信息，并允许已完成 OCR 任务重试。
- **OCR/PDF 表格统一**：文字识别和 PDF 工具任务列表对齐法规查询的数据表格布局，统一边框、行高、状态和操作列。
- **结果查看与下载收口**：OCR 识别结果和 PDF 输出不再直接铺在页面中，改为通过查看弹窗打开，并在任务表格提供下载入口。
- **OCR 彻底解耦**：生产 Docker 主镜像和本仓库不包含 OCR 服务实现；OCR 镜像、模型和运行时由独立项目交付，Pivot 只配置 `OCR_SERVICE_URL`。
- **文档与版本同步**：补齐 OCR 服务对接协议、生产离线部署、环境变量对齐和版本更新说明，项目版本启用 0.0.196。

### v0.0.195 更新摘要

- **文档处理底座落地**：共享底座已覆盖文件登记、任务队列、OCR 页面/文本块、PDF 操作、受控输出、清理策略和治理统计。
- **文字识别应用完善**：应用中心新增文字识别入口，支持图片/PDF 上传、坐标框叠加、低置信度高亮、人工复核和 TXT/Markdown/JSON/HTML/DOCX/可搜索 PDF 导出。
- **PDF 工具应用完善**：应用中心新增 PDF 工具入口，支持合并、拆分、旋转、删除页面、重排、提取文本、PDF 转图片、图片转 PDF 和可搜索 PDF。
- **结果分发与治理**：OCR 结果可进入知识库、法规库、公文材料和聊天输入框；入库文本保留来源页码与置信度，管理员可配置处理限制、任务统计和输出保留策略。
- **全局样式统一**：优化文字识别和 PDF 工具工作台布局，左侧配置栏、文件选择框、任务表格、详情区、空状态和输出列表对齐全局工作区样式。
- **文档与版本同步**：更新 CHANGELOG.md、README、使用帮助、项目汇报和包版本，项目版本启用 0.0.195。

### v0.0.194 更新摘要

- **设计规范项目化**：`DESIGN.md` 已重构为全项目设计规范，统一记录产品目标、信息架构、设计原则、视觉语言、组件语义、可访问性和模块设计索引。
- **公文写作方案拆分**：原公文写作专项设计迁移到 `docs/公文写作设计方案.md`，避免项目级设计规范继续被单一业务模块占用。
- **工具库方案根目录化**：`工具库数据接入与本机能力设计方案.md` 作为工具库数据源、报表文件、本机能力和执行位置的完整设计入口。
- **服务器可访问数据库命名启用**：数据库入口统一命名为“服务器可访问数据库”，并区分服务器报表目录、导入数据文件、本机数据库/本机报表目录和外部工具服务。
- **文档与版本同步**：更新 `CHANGELOG.md`、README、使用帮助、项目汇报和包版本，项目版本启用 `0.0.194`。

### v0.0.193 更新摘要

- **开发规范硬化**：完善 `开发规范.md`，明确开发前流程、文件拆分、大文件治理、前端全局 UI 样式、中文注释/提示/日志和测试验收要求。
- **自动化门禁落地**：新增 `check:standards`、`scripts/check_development_standards.js`、`.githooks/pre-commit` 和 `hooks:install`，把规范检查接入 `npm run check` 与 Git 提交前检查。
- **文档处理方案固化**：新增文档处理底座与 OCR/PDF 工具应用分阶段开发方案，明确底座优先、OCR 引擎策略、版式还原边界、知识库/法规查询集成和验收标准。
- **中文提示日志治理**：提交门禁会增量拦截英文-only 注释、英文提示、英文日志和英文错误文案，技术名词需放在中文语境中使用。
- **文档与版本同步**：更新 `CHANGELOG.md`、README、使用帮助、项目汇报和包版本，项目版本启用 `0.0.193`。

### v0.0.192 更新摘要

- **公文写作分步化**：首页改为已创建公文列表和创建入口，单篇公文进入后再进行正文编辑、AI 起草、审校和导出。
- **写作资源依据链路清晰**：本篇材料只随当前公文保存；知识库结果可点击“加入AI依据”写入本篇材料，AI 起草、审校、润色和全文改写会自动参考。
- **审校建议合并优化**：文字校对并入审校建议，弹窗拆成“基础校对”和“AI审校建议”两栏，AI 状态显示在弹窗内。
- **编辑页降噪**：AI 全文起草、审校建议和写作资源统一弹窗化，移除撤销/重做和复杂版本流，正文区域保持优先显示。
- **文档与版本同步**：更新 `CHANGELOG.md`、README、使用帮助、`DESIGN.md` 和包版本，项目版本启用 `0.0.192`。

### v0.0.191 更新摘要

- **生产依赖审计修复**：锁文件中的 `form-data` 与 `multer` 升级到已修复安全版本，解决 GitHub Actions 中 `npm run audit:policy` 的高危/严重阻断项。
- **Electron 主版本暂缓**：不执行 `npm audit fix --force` 强制升级到 `electron@43.0.0`，避免未经桌面端启动、打包和原生模块验证的破坏性升级混入生产依赖修复。
- **审计与测试通过**：`npm run audit:policy` 与 `npm test` 已通过，生产依赖阻断项清零。

### v0.0.190 更新摘要

- **AI 并发显示口径收口**：系统监控区分保存配置上限与当前有效上限，GPU 显存保护临时降档时不再被误解为全局参数回退。
- **GPU 临时保护可见**：AI 并发卡片提示“配置 2 · GPU 临时保护”，GPU 面板新增“AI上限（当前 / 配置）”与“降档中”状态。
- **保存后监控快照即时刷新**：全局参数保存成功后清空监控摘要缓存，避免刷新时命中旧的 `0/1` 或 `0/2` 快照。
- **回归验证补齐**：新增并发信号量、GPU 降档恢复和运行时设置保存路径断言；通过 `node tests\security-gpu-monitor.test.js`、`node tests\security-chat.test.js` 和 `npm run check`。

### v0.0.189 更新摘要

- **智能体能力与结果弹窗可滚动**：能力与结果页的“可用工具”和“结果沉淀”拆成独立面板，工具较多时列表在弹窗内部垂直滚动，不再撑出视口。
- **高度链路与样式冲突收敛**：补齐弹窗 body、details、结果 grid 和列表的高度约束，并将旧的 `overflow: visible` 兼容规则限定到配置收纳区，避免覆盖弹窗滚动。
- **验证闭环**：已用 Playwright 布局指标确认 `#agent-tool-list` 可真实滚动，并通过 `git diff --check` 与 `npm run check`。
- **文档与版本同步**：更新 `CHANGELOG.md`、README、优化路线图和使用帮助，项目版本启用 `0.0.189`。

### v0.0.188 更新摘要

- **前端模块迁移继续推进**：`chat.ui` 与 `chat.attachments` 已注册到 `Pivot.modules`，旧 `window.*` 入口继续兼容但不再新增直接赋值。
- **RAG 调试可复盘**：召回测试会记录调试历史、队列状态、命中数、分数摘要和耗时，弹窗内可查看最近问题并一键回填。
- **E2E runner 落地**：新增 `@playwright/test`、`npm run test:e2e`、Playwright smoke 规格和 `check:e2e-smoke` 静态门禁，可做资产完整性检查，也可执行真实浏览器 smoke。
- **桌面更新源校验**：自动更新 feed 强制 HTTPS 和允许来源校验，仅显式开发配置允许 loopback HTTP。
- **企业化/多节点骨架**：新增组织、团队、资源权限、策略对象、部署 provider 配置表，以及 provider registry、企业访问上下文和部署画像 provider 字段。
- **文档与版本同步**：更新日志、README、使用帮助、优化路线图和包版本已同步升级到 `0.0.188`。


### v0.0.187 更新摘要

- **聊天链路拆分**：预检、上下文组装、模型流打开和助手消息持久化已拆成独立服务，聊天路由保留为轻量 SSE 传输层。
- **Agent 状态机**：新增 run 状态迁移校验和失败重试原因记录，降低重复执行、非法回退和排障不可见风险。
- **RAG 调试增强**：调试接口和面板展示 dense/fused/FTS/MMR 分数、Hybrid 权重、选中 chunk 和索引队列状态。
- **前端全局门禁**：新增 `check:window-globals`，冻结现有 `client/chat` 历史 `window.*` 暴露基线，新增 API 需迁到 `Pivot.modules.*` 路径。
- **权限与部署扩展契约**：新增权限能力矩阵、数据分级/策略对象占位和部署画像，为组织/团队权限、多节点、对象存储、分布式队列与锁继续扩展预留稳定接口。
- **文档与版本同步**：更新日志、README、使用帮助、优化路线图和包版本已同步升级到 `0.0.187`。


### v0.0.186 更新摘要

- **后端安全边界收口**：上传、RAG/知识库导入、模型转发和外部 JSON 出网统一走受控 helper，增加 multipart 限制、魔数校验、SSRF 防护、超时和 payload 类型约束。
- **运行时与队列治理**：运行时设置缓存支持保存后失效，Agent 队列补充锁续期，聊天链路补充观测 trace，Electron 外链和来源边界加固。
- **前端状态复杂度降低**：新增 `Pivot.modules`、`Pivot.exposeModule()` 等模块入口，首批 `chat.sessions` / `chat.sidebar` / `chat.streaming` 已迁入命名空间并保留兼容别名。
- **HTML 渲染安全治理**：`PivotSafeHtml.setHtml()` 统一使用 DOMPurify 净化和 `replaceChildren()` 渲染，`client/chat` 直接 `innerHTML` 写入已迁走。
- **静态检查与 CI 防线**：`npm run check:safe-html` 默认阻断未经过 `PivotSafeHtml` 或 DOMPurify 的 `innerHTML` 写入，安全审计策略检查已接入。
- **文档与版本同步**：更新日志、README、使用帮助、优化路线图和包版本已同步升级到 `0.0.186`。


### v0.0.185 更新摘要

- **生产旧库读写统一**：新增 `app_settings` 统一读写入口，读取按 `updated_at` / `rowid` 取最新，避免重复 key 读回旧值。
- **无唯一约束兼容**：启动默认值初始化不再依赖 `ON CONFLICT(key)`，旧生产表写入时会清理同 key 旧行并插入最新配置。
- **全局设置路径收敛**：全局设置、RAG、API 接入、开放注册、记忆阈值和观测告警配置都统一走最新值读取。
- **生产重启更稳**：服务重启时保留数据库已有配置优先，避免 `.env` 默认值或旧表结构干扰已保存参数。
- **回归测试补齐**：新增旧生产 `app_settings` 无唯一约束模拟测试，覆盖默认值初始化、重复 key 取最新和保存后去重。
- **文档与版本同步**：更新日志、README、使用帮助和包版本已同步升级到 `0.0.185`。

### v0.0.184 更新摘要

- **全局参数持久化加固**：运行时参数读取最新保存值，并在保存后同步运行时快照，避免保存成功后刷新回退或消失。
- **共享端点并发稳定**：同一模型服务地址下多模型按全局默认与模型覆盖值聚合并发，请求路径也不会被低并发模型拉回。
- **保存后监控即时刷新**：全局参数保存后同步各运行时队列，并强制刷新监控摘要，避免缓存窗口显示旧并发。
- **用量统计分页与全量导出**：用量统计页补齐分页控件，后端返回 `total` / `page` / `limit`，并新增全量 CSV 导出接口。
- **长会话切换稳定到底部**：增强会话切换后的贴底滚动，覆盖大量消息、图表、图片或代码块延迟渲染造成的高度变化。
- **文档与版本同步**：更新日志、README、使用帮助和包版本已同步升级到 `0.0.184`。

### v0.0.183 更新摘要

- **长期记忆样式统一**：长期记忆页面改用全局统一的数据表格、分页控件和弹窗样式，行高、按钮尺寸、编辑弹窗与来源弹窗保持管理端一致，来源关闭按钮固定在弹窗右上角。
- **来源/编辑弹窗容错**：修复来源和编辑按钮点击后空白的问题，来源加载失败会提示，编辑时会校验缓存有效性。
- **会话记录默认到底部**：查看普通会话或管理员用户会话记录时，打开后默认贴到底部显示最新位置。
- **GPU 降档自动恢复**：GPU 显存保护把全局 AI 并发临时降到 1 后，显存恢复安全会自动回到设置页保存的并发值，例如 2。
- **配置来源说明**：`.env` 中的并发值仅作为数据库没有保存值时的默认值；设置页保存到 `app_settings`，生产更新部署需保持 `DATA_DIR` / Docker 数据卷不变。
- **文档与版本同步**：更新日志、README、使用帮助和包版本已同步升级到 `0.0.183`。

### v0.0.182 更新摘要

- **工具箱不再自动启用**：普通聊天发送消息或文档时，如果用户没有手动开启工具箱，前端不会再根据内容自动打开工具箱或弹出工具箱授权确认。
- **文档发送体验修复**：附件抽取文本继续用于模型理解文档，但不再参与工具箱启用判断，避免制度、表格、报告类文档误触发工具箱确认。
- **显式工具箱授权保留**：用户主动开启工具箱后，发送消息仍会执行会话授权确认，并携带 `mcpEnabled` / `mcpConfirmed` 状态。
- **Token 速度统计修正**：实时与最终 `t/s` 改为按完整输出 token / 本轮请求总耗时计算，避免思考过程的 token 或耗时被排除后导致速度虚高。
- **文档与版本同步**：更新日志、README、使用帮助和包版本已同步升级到 `0.0.182`。

### v0.0.181 更新摘要

- **法规查询安全拆分**：前端、后端路由和后端服务已拆分为多个职责清晰的小模块，并保留旧入口兼容。
- **加载失败修复**：补齐拆分后的事件绑定导出和模块就绪等待，修复打开应用时提示“法规查询加载失败”。
- **引用网络优化**：大关系集优先显示核心图，右侧保留全量关系明细，弹窗、滚动和窄屏布局同步优化。
- **节点文字溢出修复**：节点标题改为两行截断和悬浮提示，长条文标题不再超出节点边框。
- **文档与版本同步**：更新日志、README、使用帮助和包版本已同步升级到 `0.0.181`。

### v0.0.180 更新摘要

- **文档名/版本号识别修复**：上传优先从文件名识别标题并剥离尾部版本日期；从文件名日期自动识别版本号，兼容 `20240101`、`2024-01-01`、`2024年01月01日` 等写法（如 `中华人民共和国公司法_20240101.doc` → 版本“2024年01月01日”）。
- **条文条数按实际“第X条”计算**：长条被切分或含前言/章节标题时不再虚高；法律首部（标题/颁布信息/目录/序言）保留为“前言”条目不再丢失。
- **AI 问答“未选择模型”修复**：问答请求携带聊天页所选模型，无默认模型时不再报错；编辑弹窗可显示并修改版本号。
- **向量生成 400 修复**：复用知识库同一向量模型配置，改为分批（每批 5 条）+ 单条截断，修复整部法律一次性提交导致的 400，并修复向量与条文索引错位；失败仍降级为 BM25。
- **移除生效/失效日期**：删除 `effective_date`、`effective_date_normalized`、`expire_date` 三列（新增迁移，不可逆）；状态仅保留“有效 / 已归档”。
- **删除确认弹窗统一**：法规文档、保存检索、条文批注删除改用项目内自定义弹窗，不再使用浏览器默认 `confirm`。

### v0.0.179 更新摘要

- **法规弹窗样式全局统一**：法规查询相关的检索、详情、问答等所有弹窗重构适配系统全局的 `workspace-modal` 样式标准，实现头部固定和内容独立滚动。
- **检索表单 Grid 网格化重构**：将检索法规表单升级为 CSS Grid 双列网格布局，“检索内容”横跨双列，排版更合理。
- **修复高度拉伸 Bug**：重置输入控件的 `flex: none !important`，解决在垂直布局下原本的水平 Flex-basis 错误在高度上生效造成高度异常拉伸的 Bug，高度均完美复原为正常的 `36px`。
- **主工具栏按钮重排与主题色对调**：移除了 `.regulations-ai-entry-btn` 的 `margin-right: auto;` 从而让“检索”和“AI问答”按钮与其他按钮一并紧凑靠右排列，并对调了这两个按钮的 Primary/Secondary 样式，使检索按钮呈现高亮绿色，AI问答按钮呈灰白色。

### v0.0.178 更新摘要

- **品牌图标体系统一**：基于新 Logo 生成并替换 favicon、PWA、移动端和桌面端标准图标，去除白色背景，修复标签页图标过小和变形问题。
- **登录页视觉重设计**：左侧升级为白绿主题的“内网 AI 控制平面”展示区，加入中心 Logo、六个功能节点、科技网格背景和分层底座。
- **主视觉底座与节点图标**：为中心 Logo 增加白绿分层底座和柔光投影，重新设计“知识库检索”“数据分析”图标语义。
- **登录体验收口**：保留现有登录/注册方式，优化登录页无滚动适配、底部 copyright 居中和登录/注册切换按钮细节。

### v0.0.173 更新摘要

- **引用图谱关联法条**：导入时解析条文交叉引用（第X条、《书名号》依据/参照/废止），建立 `regulation_article_links` 关系表。
- **检索沿引用扩展**：AI 问答在 BM25 召回基础上沿引用边双向扩展，补充关联条文进上下文。
- **结构化 AI 回答**：提示升级为「核心法条 → 关联法条（含引用关系）→ 适用建议」结构。
- **前端分组展示**：依据条文按「直接命中 / 关联条文」分组，关联项标注引用关系（如「本条引用」「被引用（依据）」）。
- **复用 RAG 切片**：「第X条」结构用 chunkLegal 按章/节/条切分，生成 `heading_path`（《XX法》› 第三章 › 第十条）定位更精准。

### v0.0.172 更新摘要

- **法规化快速预设**：导入弹窗新增「快速预设」，一键填充法律、行政法规、司法解释、部门规章等常用公共字段，批量导入同类法规更高效。
- **版本标识按施行日期**：版本标识留空时按法规惯例用施行日期生成「YYYY年MM月DD日」标签；追加版本表单新增施行日期输入。
- **编辑/追加版本回归**：将弹窗化改造后失效的「编辑信息」「追加版本」重新接入详情弹窗，并清理孤儿代码。
- **界面打磨**：AI 问答弹窗放大并更名为「AI 问答」，修复法规表格被状态/操作列撑高的行高问题。

### v0.0.171 更新摘要

- **版本对比 diff**：详情支持选择两版本生成逐条对比，展示新增/删除/变更条文及行级差异高亮。
- **多轮条文问答**：AI 问答携带最近多轮历史，支持追问，每轮独立标注依据条文。
- **分页与健壮性**：文档列表分页加载与计数对齐，批量导入失败清单、重复检测、归档徽章。
- **状态治理**：文档支持启用/归档两种状态；条文解析支持多种结构。

### v0.0.170 更新摘要

- **更新提示源头加固**：运行时版本读取只接受规范三段式版本号，前端提示会从 `version/build` 中提取可展示版本，异常内容只显示友好刷新提示。
- **法规查询文档补齐**：继续保留 `v0.0.168` 法规查询批量导入与全局样式统一日志，并在帮助中补充批量导入支持格式。
- **版本同步启用**：当前版本升级至 `0.0.170`，README、使用帮助、包版本和更新日志已同步记录。

### v0.0.169 更新摘要

- **更新提示修复**：右下角 PWA 更新提示会自动提纯版本号，异常版本信息不再直接展示为 `v##`。
- **法规导入细节统一**：导入弹窗补齐顶部关闭入口，文件选择区改为统一控件，批量导入时显示已选数量和总大小。
- **历史日志补齐**：`v0.0.168` 更新日志已补回并保留，记录法规查询批量导入、全局样式统一和后端导入链路。
- **文档同步**：README、使用帮助、包版本和更新日志已同步升级到 `0.0.169`。

### v0.0.168 更新摘要

- **法规查询升级**：应用中心新增“法规查询”，支持法规制度文档批量导入、版本管理、条文级检索、详情查看、源文件下载和归档治理，并统一使用全局工作区布局、按钮、输入框、搜索框和维护弹窗样式；导入文档弹窗可一次选择多个文件，系统会优先从文件名识别标题，并从文件名中的日期识别版本号。
- **法规库后端与迁移**：新增法规服务、API 路由、版本化 migration、基础 schema 表结构和 FTS5 条文全文索引，法规源文件统一存放在 `uploads/regulations/`。
- **条文依据 AI 问答**：法规查询可基于命中条文构建 AI 上下文，围绕法规来源回答问题；管理员可维护文档元数据并追加新版本。
- **版本号同步启用**：当前版本升级至 `0.0.168`，更新日志、包元数据、README 和使用帮助已同步记录。

### v0.0.161 更新摘要

- **Docker 生产环境 Canvas 依赖修复**：Dockerfile 新增 libcairo2-dev、libpango1.0-dev 等系统库，解决 @napi-rs/canvas 加载失败导致的 PDF 解析警告。
- **客户端下载灵活化方案**：新增 downloads/ 目录与外部挂载支持，推荐通过 docker-compose 挂载实现热更新客户端，保持镜像精简（~500MB）。提供三种方案：外部挂载（推荐）、打包进镜像、外部托管。
- **完善部署文档**：新增 Docker 客户端下载配置、Docker 部署快速指南、验证清单等文档，涵盖配置示例、故障排查与最佳实践。

### v0.0.160 更新摘要

- **智能分析回复排版优化**：深度分析结果区取消聊天气泡最大宽度限制，Markdown 回复会铺满可用区域，避免文字集中在左侧、右侧大面积空白。
- **执行过程错误更可读**：SQL 查询等工具步骤补充成功/失败状态，失败时只展示可理解的短错误摘要，不再把 DuckDB 候选函数签名整段暴露在页面上。
- **SQL 数值字段处理增强**：根据字段画像将成绩、金额、数量等指标按 DOUBLE 物化，减少文本字段相加或平均导致的查询失败；学生编号、ID、单号等编号类字段按维度处理，避免分析口径跑偏。

### v0.0.139 更新摘要

- **数据分析工作台与并发链路**：应用中心新增数据分析应用，支持 CSV/XLS/XLSX 上传、数据集列表与详情、字段画像、统计摘要、图表生成、双数据集对比和 CSV 导出，并同步接入后端路由、分析服务与维护任务。
- **思考输出收口**：普通聊天与兼容接口中的 `reasoning_content`、`<think>` 等思考片段统一过滤，避免把思考过程暴露给前端或补全链路。
- **全局参数页排版修复**：长英文 runtime key 支持完整换行，右上角最近保存状态不再截断，输入框保持同一水平线。

### v0.0.136 更新摘要

- **关闭思考时速率修正**：修复思考模型关闭思考过程时 token 速率不准确的问题，使其流式和最终速率计算均基于总耗时进行对齐。
- **前后端计算口径同步**：后端 `buildAssistantSpeedStats` 与前端 `sendMessage` 在 `disableChatThinking` 模式下均使用请求起跑时间，保持体感精准度。
- **版本号同步启用**：当前版本已升级至 `0.0.136`，更新日志、包元数据和 README 已保持一致。

### v0.0.135 更新摘要

- **PWA 更新提示规范化**：更新提示改为 class 驱动，样式统一走全局主题变量并保留 fallback，避免不同页面样式环境下显示异常。
- **主题变量统一收口**：新增 `--pwa-update-*` 变量，提示背景、边框、阴影和按钮样式都跟随项目主题切换。
- **版本号同步启用**：当前版本已升级至 `0.0.135`，更新日志、包元数据和 README 已保持一致。

### v0.0.125 更新摘要

- **模型配置弹窗补回上下文窗口**：恢复 `上下文窗口 (Token)` 配置项及提交、编辑回填和重置逻辑，模型配置与后端字段重新对齐。
- **并发治理弹窗样式统一**：并发治理弹窗按模型配置弹窗的结构和视觉样式重新整理，提升两处配置入口的一致性。
- **上下文阈值收口到全局设置**：模型配置页顶部的上下文阈值快捷入口已移除，统一由并发治理页面维护全局上下文设置，减少重复配置。
- **速度统计修正**：聊天 tps 改为只按可见答案 token 和答案生成耗时计算，不再把模型思考阶段计入每秒 token。
- **验证**：`npm run check`、`npm run lint`、`node tests/security-chat.test.js` 已通过。

- **关系明细不再叠出两个弹窗**：知识图谱关系卡片移除原生 `title`，只保留自定义信息浮层，避免浏览器默认 tooltip 和业务浮层同时出现。
- **实体节点补齐完整悬停信息**：左侧实体节点卡片现在支持悬停/聚焦显示完整实体名称、类型、关系数、可信度、描述和 ID。
- **关系浮层展示完整来源片段**：关系 tooltip 新增完整来源内容，并支持长文本换行和滚动查看。
- **验证**：新增前端渲染回归测试，覆盖单一 tooltip、实体完整信息和关系来源内容展示。

### v0.0.123 更新摘要

- **关闭思考仍保留流式体验**：普通聊天关闭思考时不再等结束后一次性回放正文，标准 `content` 会继续按 SSE 流式输出。
- **增量过滤可见思考脚手架**：新增服务端过滤器，遇到 `Analyze User Input` / `Draft` / `Check Constraints` 这类混进正文的思考脚手架时，只从 `Draft:` 后开始流式输出最终答案。
- **验证**：新增 no-think 流式过滤单测，覆盖脚手架隐藏和普通正文立即流出。

### v0.0.122 更新摘要

- **修正关闭思考后的速度虚高**：关闭聊天思考时，最终正文会一次性回放；现在不再按极短可见回放时间计算 `t/s`，避免出现 `144000 t/s` 这类异常数值。
- **服务端下发真实统计**：聊天流保存助手消息后，后端按请求总耗时计算 `costTime/tps`，写入消息记录并随 `message_saved` 返回前端。
- **前端避免二次覆盖**：收到服务端统计后，前端直接展示真实平均速度，并跳过成功后的 `/chat/stats` 估算覆盖。

### v0.0.121 更新摘要

- **聊天思考开关改为正向语义**：模型配置弹窗改为“聊天中开启思考”，打开表示普通聊天保留思考，关闭表示普通聊天隐藏思考过程并追加 `/no_think`。
- **字段统一为 `chat_thinking_enabled`**：数据库 schema、迁移、模型保存/更新、后台列表、可用模型接口、前端保存和编辑回填均使用正向字段；默认关闭，只有思考模型且开关打开时才保留思考。
- **真正隐藏流式思考内容**：普通聊天关闭思考时不再向 SSE 写出 `reasoning_content` 增量，并会在最终文本兜底阶段剥离 `<think>` 块和 `Analyze User Input` / `Draft` / `Check Constraints` 等可见思考脚手架。
- **验证**：新增聊天 no-think 单测，覆盖默认关闭、打开保留、流式 reasoning 丢弃和思考脚手架清理；本轮按要求未打开浏览器验证。

### v0.0.120 更新摘要

- **普通聊天思考改为模型级配置**：模型配置弹窗新增“聊天中开启思考”开关，只有同时标记“思考模型”时才生效；关闭时普通聊天追加 `/no_think`，不再需要全局强制关闭思考。
- **请求链路追加 `/no_think`**：`/api/chat` 在 RAG/视觉/MCP 上下文注入后、上下文预算裁剪前，对最后一条用户消息副本追加 `/no_think`；不会污染入库历史，也不会重复追加。
- **多模态与预算兼容**：文本+图片消息只改第一个文本 part，图片 part 保持不变；新增 token 会进入 `fitMessagesToContextBudget` 统一预算。
- **配置闭环**：新增 `chat_thinking_enabled` 数据库字段和迁移，模型列表/可用模型接口、前端保存、编辑回填、重置均已接入。
- **验证**：新增聊天 no-think 单测，`node tests/security-chat.test.js`（79/79）、`npm run lint`、`npm run check` 均通过；本轮按要求未打开浏览器验证。

### v0.0.119 更新摘要

- **新增「上下文窗口」配置**：模型管理弹窗新增「上下文窗口 (Token)」字段并补齐 `context_window_tokens` 数据列（此前 `getModelContextBudget` 读取的是不存在的死字段）；管理员可按离线模型实际 `n_ctx` 直接设定，无需用输入上限反推。
- **无界预算告警**：模型未配任何上下文上限时，大输入会节流告警提醒补配，避免超长上下文无感知直发上游。
- **输出上限口径统一**：Agent 调用不再写死 1200、改为尊重 `modelCfg.max_tokens`（推理型模型不再被截断空输出）；网页聊天有意不兜底 max_tokens 已补注释说明。
- **压缩阈值随模型收紧**：记忆压缩触发阈值改为 `min(全局, 模型输入预算)`，小上下文模型提前压缩、避免请求时硬丢历史（仅下调不上调，未配置模型行为不变）。
- **token 设置范围/关系校验**：模型接口对 `max_input_tokens`/`max_tokens`/`context_window_tokens` 新增统一校验（负数/非数字/超限一律 400），并要求输入/输出上限小于上下文窗口，防止异常值入库并被原样转发给上游。
- **验证**：新增上下文设置单测 8 项；`npm run check`、`npm run lint`（0 错误）与 `node tests/security.test.js`（214/214）通过。

### v0.0.118 更新摘要

- **修复推理型模型空正文**：用 Qwen3 等思考型模型时，公文写作非流式操作（全文润色/起草/审校）弹"AI 未返回有效内容"的问题已修复（Qwen2.5 等不受影响）。根因是思考耗尽 token 且接口只读 `content`。
- **四项修复**：对推理型模型（标志位或 Qwen3/QwQ/R1 名识别）追加 `/no_think` 软开关关闭思考；`max_tokens` 不再写死 2000、改为尊重模型配置；非流式解析复用 `extractModelText` 并剥离 `<think>` 块；空正文记录诊断日志并把 `finish_reason` 透传，截断时给出明确提示。
- **验证**：新增 AI 路由单测 5 项；`npm run check`、`npm run lint`（0 错误）与 `node tests/security.test.js`（206/206）通过。

### v0.0.117 更新摘要

- **导出功能独立成文件**：公文写作的 DOCX/红头 DOCX、Markdown、带版式文本、打印 PDF（HTML 打印）、纯文本与下载工具及统一入口 `exportOfficialWriting`，从 proofread/rewrite 切片集中抽到新文件 `apps-workbench-export.js`（20 个函数）；纯结构搬移，函数体哈希前后一致。
- **关注点边界**：剪贴板与"带入聊天"相关函数不属导出，保留在原处；`pdf` 类型为打开打印窗口另存，非生成 PDF。
- **验证**：7 切片顶层声明仍 226 个、无重复无遗漏；隔离上下文加载无报错、入口正常注册；服务启动 7 切片均 200、旧路径 404；`npm run check`、`npm run lint`（0 错误）、`node tests/security.test.js`（201/201）通过。

### v0.0.116 更新摘要

- **公文写作脚本安全拆分**：单文件 `apps-workbench.js`（3612 行）按工作流拆为 6 个全局脚本（core/editor/proofread/ai/rewrite/rag），沿用 `dag-*`/`mcp-*` 顺序加载范式；拆分为纯结构调整，函数实现零改动，切片拼接与原文件逐字节一致。
- **顺序加载与全局登记**：`app-workspaces.js` 按依赖顺序串行加载 6 个切片；ESLint 新增 `appsWorkbenchGlobals` 登记 129 个跨切片标识符，资产守卫脚本改为合并切片后校验。
- **加载验证**：隔离上下文拼接执行无加载时报错，入口 `window.*` 正常注册；服务启动确认 6 个切片均 200、旧路径 404。
- **文档与验证同步**：版本升级至 `v0.0.116`，README、更新日志和版本字段同步更新；`npm run check`、`npm run lint`（0 错误）与 `node tests/security.test.js`（201/201）通过。

### v0.0.115 更新摘要

- **模型转发收口**：聊天（`/api/chat`）、OpenAI 兼容（`/v1/chat/completions`）与公文写作（`/api/apps/official-writing/ai`）三处重复的上游 axios 转发统一为 `server/services/model-forwarder.js` 的 `forwardChatCompletion()`，SSRF/出网安全校验、受限 HTTP agent、禁用系统代理与超时口径集中维护，安全检查不可被某处遗漏。
- **行为等价**：仅抽取“发起请求并返回响应”这一层，URL 构建、payload 组装与流式/非流式消费仍由各路由处理，聊天 Responses→Chat Completions 自动降级等专有逻辑不变。
- **文档与验证同步**：版本升级至 `v0.0.115`，README、更新日志和版本字段同步更新；新增转发服务契约单测 3 项，`node tests/security.test.js`（201/201）、`npm run check` 与 ESLint（0 错误）通过。

### v0.0.114 更新摘要

- **公文写作 AI 调用封装**：公文写作的起草、润色、全文改写、审校、选区处理与逐句流式改写改为调用后端专用接口 `POST /api/apps/official-writing/ai`，提示词组装、模型权限/配额/上下文预算、上游转发与审计标签统一在后端完成；前端只传结构化参数并复用单一共享请求方法。
- **模型选择兜底**：公文写作不再强制先在聊天页选模型，后端按“显式选择 → 个人默认模型 → 系统默认模型”顺序回退，均无可用模型时返回清晰提示。
- **审校结果更稳健**：审校 JSON 的提取与字段校验收口到后端（兼容代码块/前后说明、过滤脏数据），无有效条目时退回整体意见。
- **接口口径以最新版为准**：公文写作 AI 当前统一走 `/api/apps/official-writing/ai`，不再由前端直连 `/v1/chat/completions`；历史摘要中的 `/api/chat/completions`、`/v1/chat/completions` 为当时实现记录。
- **文档与验证同步**：版本升级至 `v0.0.114`，README、更新日志和版本字段同步更新；`npm run check`、`node tests/security.test.js`（192/192）与 ESLint（0 错误）通过，新增公文写作服务单测 6 项。

### v0.0.111 更新摘要

- **API 接入总开关**：把第三方 API 接入的启停能力收口到系统设置，管理员可在后台统一开关，关闭后 API Key、OpenAI-compatible `/v1` 路由和模型列表同步失效。
- **前后端联动**：API 账号页新增总开关、状态徽标和关闭提示，创建 key 与相关入口会随开关自动禁用。
- **鉴权与安全**：服务端对 API Key 登录、`/auth/keys` 和 `/v1` 路由做双重拦截，避免旁路访问。
- **文档与验证**：版本升级至 `v0.0.111`，更新更新日志、README 和版本字段，并补齐安全回归测试。

### v0.0.110 更新摘要

- **首屏资源瘦身**：精简首屏脚本清单，将工作区、管理后台、知识库和图表相关脚本拆为按需加载，首页初始体积明显下降。
- **大 vendor 按需加载**：ECharts 改为图表渲染时动态加载，管理后台和工作区入口也改成进入功能后再补脚本。
- **RAG 兼容修复**：补回 `escapeRagHtml` 等兼容导出，并对知识库专题/标签接口的未授权访问做静默回退，避免控制台报错。
- **启动调度优化**：后台维护任务改为服务监听成功后延迟启动，同时补齐环境变量说明和缺失配置。
- **文档与验证同步**：版本升级至 `v0.0.110`，更新更新日志、README 和版本字段；`npm run check`、`npm run lint` 与既有测试结果保持通过。

### v0.0.109 更新摘要

- **公文写作布局收口**：顶部工具栏改为单行优先工作流，收敛为文种、规范库、材料来源、模式、AI指令、全文AI、编辑、面板和更多；资料栏/审改栏统一用顶部面板开关控制。
- **全文 AI 与选区工具分离**：顶部“起草/润色/改写/审校”明确为全文 AI，不再因选区存在而变成选区处理；选中文字后的浮动工具条增加“选区”标识，专门处理局部片段。
- **正文版式深度优化**：正文与对照模式改为版式化 surface 渲染，优化标题、一级标题、`（一）（二）` 条目、正文段落、收尾句、行距、首行缩进、落款和日期右对齐。
- **左右面板与审改栏去重**：移除重复的“审改中心”、右栏“收起”和顶部“保存版本”入口；对照模式不再自动隐藏左侧资料栏，右侧审改 tab 单行滚动。
- **大纲交互优化**：大纲条目默认保持紧凑，悬停或键盘聚焦时在条目内多行展开完整内容，并去除大纲区域水平滚动条。
- **AI 请求路径修复**：公文写作 AI 调用统一走 `/v1/chat/completions`，修复起草、润色、改写、审校按钮出现 `AI 请求失败（404）` 的问题。
- **文档与验证同步**：版本升级至 `v0.0.109`，README、使用手册、项目汇报和更新日志同步更新；`npm run check` 与 `git diff --check` 已通过。

### v0.0.108 更新摘要

- **多文档公文工作台**：公文写作从单文档升级为文档库，支持新建/打开/重命名/删除，每篇独立保存正文、版本、批注和发文要素，旧数据自动迁移。
- **发文要素结构化表单**：新增版头（密级、紧急程度、发文字号、签发人）与版记（抄送、印发机关、印发日期）字段，导出时自动排版。
- **流式改写 + 逐句接受**：选区“逐句改写”流式生成后以红绿对照逐句呈现，支持单句接受/保留再拼装应用，而非整段替换。
- **知识库检索接入**：左侧“历史/规范”标签接入知识库检索 `/api/rag/debug-query`，可按专题库范围检索并一键引用到正文或作为依据。
- **导出格式扩展**：新增 Markdown、带版式纯文本与套用单位红头模板的 DOCX 导出。
- **公文校对增强**：新增校对页，检查易错词、敏感/绝对化措辞、标点规范与标题文种一致性，可点击定位。
- **文档与验证同步**：版本升级至 `v0.0.108`，`npm run check` 与 `eslint`（0 告警）均已通过。

### v0.0.107 更新摘要

- **公文写作接入真实模型**：应用中心“公文写作”的起草、润色、局改、审校与选区工具由本地正则模拟改为调用 OpenAI 兼容补全接口 `/api/chat/completions`，模型取自聊天页选择器；审校模式按 JSON 逐条回填可处理的修改建议。
- **规范库联动检查**：党政机关公文格式校验发文字号/签发人/紧急程度，会议纪要规范校验会议要素与议定事项，单位内部规范校验落款单位，所选规范库真正驱动差异化检查。
- **引用判定模糊匹配**：材料引用从精确子串改为 2-gram 重叠率叠加关键数字命中，减少正文改写后被误判为“未引用”的误报。
- **存储与导出加固**：本地存储写入前预估体积并在超限时梯度精简，新增工作区备份导出；DOCX 补齐 `docProps` 元数据并按层级套用标题、落款右对齐等版式。
- **编辑可靠性与无障碍**：新增撤销/重做（含 `Ctrl+Z`/`Ctrl+Y`），审改栏补齐 ARIA 角色、方向键导航、`Esc` 收起与焦点环。
- **文档与验证同步**：版本升级至 `v0.0.107`，改动文件经 `eslint`（0 告警）与 `node --check` 语法校验通过。

### v0.0.106 更新摘要

- **统计查询合并与并行化**：`/api/admin/ops-summary` 将多个独立 COUNT/SUM 合并为单条标量子查询，模型主机本地性判定改为并行解析，降低运维统计端点的往返延迟与锁争用。
- **前端拖拽节流与轮询收口**：DAG 编辑器 `pointermove` 接入 `rafThrottle` 每帧最多一次，公告 60 秒轮询新增清理钩子并在登出时停止。
- **错误响应收口**：服务端 5xx 默认返回通用提示，不再依赖 `NODE_ENV`，避免 SQL 错误、内部路径与堆栈泄漏到响应。
- **数据库标识符加固**：数据库 MCP 标识符拼接在引号转义之外新增控制字符与长度校验，作为防御纵深。
- **工程化基线**：ESLint 补齐一批低误报规则，`package.json` 新增 `engines.node >= 20`，新增 GitHub Actions CI（`npm ci` + 审计 + 检查 + Lint + 安全测试）。
- **文档与验证同步**：版本升级至 `v0.0.106`，`npm run check`、`npm run lint`（含新规则）与安全测试 `189/189` 已通过。

### v0.0.105 更新摘要

- **知识图谱索引补齐**：为此前无索引的 `knowledge_entity_mentions` 和 `knowledge_relations` 表补齐索引，覆盖按用户、状态和源/目标实体的高频查询，使关系统计与图谱展开从全表扫描转为索引查找；索引经 `initSchema()` 幂等执行，新老库下次启动自动生效。
- **xlsx 依赖镜像化**：Excel 解析依赖从 `cdn.sheetjs.com` 直链切换为 npm 官方镜像 `@e965/xlsx@^0.20.3`，恢复版本锁定与 integrity 校验，消除 `npm ci`/Docker 构建对外部 CDN 的依赖。
- **uuid 漏洞清零**：`uuid` 升级至 `^11.1.1`，`npm audit` 由 1 条 moderate 告警降为 0 漏洞，同时保留 CommonJS `require()` 兼容。
- **文档与验证同步**：版本升级至 `v0.0.105`，同步更新 README、使用手册、项目汇报、更新日志和版本文件；`npm run check`、`npm run lint` 和安全测试 `189/189` 已通过。

### v0.0.104 更新摘要

- **应用中心入口沉淀**：新增面向业务场景的应用中心，当前承载“公文写作”工作台，常用业务应用不再继续挤在左侧主导航中。
- **公文写作工作台重构**：顶部收敛为文种、规范库、材料来源、修改要求、正文/对照模式和 AI 快捷动作；中间保留正文编辑主空间，左侧固定资料库，右侧统一审改栏。
- **正文/对照模式清晰分工**：正文模式用于正式写作，对照模式用于原文和正文稿并排审改；移除独立“审校模式”，`审校`改为打开右侧规范检查的动作按钮。
- **审改栏去重与适配**：右侧只保留修改建议、批注、版本、规范检查和材料引用，移除重复的 AI 助手入口；Tab 按钮自适应换行，不再出现横向滚动条。
- **版本对比体验优化**：版本面板新增历史版本模块，版本卡片展示名称、类型和时间，历史版本列表与差异结果区域分离。
- **文档与验证同步**：版本升级至 `v0.0.104`，同步更新 README、使用手册、项目汇报、更新日志和版本文件；`node --check client\chat\apps-workbench.js`、`npm run check`、`npm run lint` 已通过，本轮按要求未启动浏览器。

### v0.0.103 更新摘要

- **角色与规范库升级**：原“指令中心”改造为“角色与规范库”，统一沉淀角色、输出规范、任务方法和工作流节点提示。
- **三入口套用规范**：聊天可将规范写入当前会话系统提示词，自由任务可追加到补充说明，工作流可写入或创建大模型节点。
- **规范资产结构化**：规范支持说明、类型、适用入口和关键词筛选；后端 `prompts` 接口同步支持按类型、入口和关键词过滤。
- **工具归属可见**：超级管理员查看其它用户 MCP 服务或工具时可看到所属用户/全局来源，自由任务工具目录也保留工具归属信息。
- **旧库启动兼容**：旧数据库缺少 `prompts.type`、`target_surfaces` 等新字段时，会在 schema 建索引前补列，避免 `no such column: type` 启动失败。
- **文档与验证同步**：版本升级至 `v0.0.103`，同步更新 README、使用手册、项目汇报、更新日志和版本文件；`npm run check`、`npm run lint`、角色与规范库/权限定向回归、数据库初始化和本地 HTTP 静态资源检查已通过。

### v0.0.102 更新摘要

- **第三方代码补全兼容**：OpenAI-compatible `/v1/chat/completions` 兼容 `prompt`、`input`、`prefix`、`suffix` 风格的代码补全请求，避免空 `messages` 被直接拒绝。
- **补全参数透传补齐**：聊天补全转发继续保留 `stop`、`top_p`、`presence_penalty`、`frequency_penalty` 和 `max_completion_tokens` 等常见参数，减少第三方客户端行为差异。
- **工具策略只读态收口**：普通管理员仍可查看工具策略页面，但只读卡片不再显示“编辑”按钮；内置 `admin` 继续负责全局工具启停和治理策略修改。
- **登录落点修复**：用户从登录页完成登录后进入主页面，不再回到上次停留的工作区；已有登录态刷新页面时仍保留工作区恢复能力。
- **接口路径口径保持**：Base URL 和 `/v1` 接入说明不做调整，现有 OpenAI-compatible 接入方式继续可用。
- **文档与验证同步**：版本升级至 `v0.0.102`，同步更新 README、使用手册、项目汇报、更新日志和版本文件；`node scripts\check_syntax.js`、`node tests\security-chat.test.js`、指定文件 ESLint 和 `git diff --check` 已通过。

### v0.0.101 更新摘要

- **知识库范围按需显示**：普通聊天输入区只有打开“知识库”后才显示专题库和标签选择，关闭时保持输入区简洁。
- **专题库和标签二级联动**：选择某个专题库后，标签选择只展示该专题库内已有标签，像一级/二级菜单一样避免跨专题库误选。
- **范围显示与实际检索一致**：聊天底部可用资料提示、回答顶部命中提示、知识库列表和召回测试统一使用当前专题库/标签范围，选中专题库时不会再查其它知识库。
- **知识库整理闭环**：工作台补齐“新建专题库”“新建标签”，文档表格只展示归属信息；整理弹窗通过下拉框和复选框选择已创建的专题库/标签，不再人工填写名称。
- **旧库启动兼容**：继续兼容旧数据库缺少 `knowledge_docs.collection_id` 的情况，避免 schema 初始化阶段出现 `no such column: collection_id`。
- **文档与验证同步**：版本升级至 `v0.0.101`，同步更新 README、使用手册、项目汇报、更新日志和版本文件；`node tests\security-rag.test.js`（57/57）、`npm run check`、指定文件 ESLint、数据库初始化检查和语法检查已通过。

### v0.0.100 更新摘要

- **专题库分组管理**：知识库新增专题库/集合，上传资料时可选择已有专题库或新建专题库，文档列表也可调整归属，适合按业务域、项目或部门管理不断增长的资料。
- **标签范围筛选**：文档支持多标签，知识库列表、上传弹窗、召回调试和普通聊天都可以按标签进一步缩小引用范围。
- **聊天按需引用知识库**：普通聊天新增知识库范围选择，默认仍可使用全部资料，但可切换为只引用指定专题库和标签，减少无关资料参与召回。
- **调试与 Graph-RAG 同步收口**：RAG 调试、FTS/LIKE 兜底、向量候选合并、最近分块兜底和 Graph-RAG 查询统一尊重专题库/标签作用域。
- **召回缓存隔离**：缓存键纳入专题库和标签范围，避免全库召回结果与局部范围召回结果互相命中。
- **旧库迁移兼容**：集合索引改由迁移阶段在补齐 `collection_id` 后创建，旧数据库升级时不再因缺少列而启动失败。
- **文档与验证同步**：版本升级至 `v0.0.100`，同步更新 README、使用手册、项目汇报、更新日志和版本文件；`npm run check`、`npm run lint`、`node tests\security-rag.test.js`（56/56）和 `node tests\security-chat.test.js`（51/51）已通过，本轮按要求未打开浏览器验证。

### v0.0.99 更新摘要

- **聊天输入区状态轻量化**：主页普通对话中，知识库和工具箱开启提示移动到对应按钮右侧，不再占用输入框中部空间。
- **提示文案简化**：知识库/工具箱提示收敛为“4 份资料可用”“27 个工具可用”等计数式短文案，并使用更小的紧凑标签样式。
- **知识库关键词兜底**：Embedding/向量服务鉴权失败、限流或短时不可用时，索引仍保留文本分片，聊天召回自动回退到 FTS/LIKE 候选和关键词评分。
- **工具箱限流提示修复**：普通聊天工具箱调用区分工具规划模型限流和工具执行失败，上游 `429` 不再以 `Request failed with status code 429` 原样展示。
- **向量模型边界说明**：知识库基础可用性不再完全依赖向量服务，但高质量语义召回仍建议配置可用的 Embedding 模型；对话模型不应直接作为向量模型使用，除非 embeddings 接口明确返回向量数组。
- **文档与验证同步**：版本升级至 `v0.0.99`，同步更新 README、项目汇报、更新日志和版本文件；`npm run check`、`npm run lint`、`node tests\security-rag.test.js`、`node tests\security-mcp.test.js` 和 `node tests\security-chat.test.js` 已通过。

### v0.0.97 更新摘要

- **工具箱命名与页面减重**：用户侧“能力库”统一调整为“工具箱”，导航保持三字菜单节奏；工具箱页面移除重复引导、健康提示和“带入聊天”按钮，工具是否参与聊天改由输入区工具箱开关控制。
- **工具列表简化**：普通工具弹窗聚焦工具名称、用途和启用状态，不再把风险等级、审批和适用说明暴露给普通用户，治理配置改由系统设置承接。
- **全局工具策略收口**：系统设置中的“工具策略”只管理系统工具和全局工具服务，不展示用户个人数据库连接、报表目录、消息通知或个人外部服务；普通管理员可查看，内置系统管理员可修改。
- **工具策略布局优化**：全局工具策略页改为紧凑的工具包列表和详情表格，左侧列表内部滚动，右侧表格按内容收缩，避免页面超长、右侧空白和无意义滚动条。
- **模型权限与删除修复**：私人模型只允许所有者测试，全局模型允许管理员测试；无权测试的按钮不再显示。模型删除改为软删除并清理默认引用，避免历史引用导致删除接口报错。
- **文档与验证同步**：版本升级至 `v0.0.97`，同步更新 README、使用手册、项目汇报、更新日志和版本文件；`node tests\security-auth.test.js`、`node tests\security-mcp.test.js`、`node tests\security-chat.test.js` 与 `npm run check` 已通过。

### v0.0.96 更新摘要

- **知识库顶部减重**：移除“三步用好知识库”“可以开始提问”等常驻引导和快捷卡片，避免与顶部召回测试、知识图谱和聊天知识库入口重复。
- **上传弹窗化**：点击“上传文档”打开弹窗，弹窗内支持拖拽上传或点击选择文件；页面内不再嵌入拖拽条，上传完成后自动关闭弹窗并刷新列表。
- **概览与质量诊断合并去重**：概览条统一承载文档、就绪、失败、分块、队列、评分、反馈、实体和关系等关键指标；质量诊断只在异常、停用、空分块或问题文档存在时显示。
- **知识库表格节奏统一**：知识库文档表格继续每页 15 条，表格底部到分页分割线的距离调整为与其它数据表一致。
- **知识图谱顶部工具合并**：知识图谱工作台将统计摘要、实体搜索、筛选、搜索按钮、图谱查询输入框和“查询图谱”按钮合并到同一行，查询结果继续独立显示在下方。
- **文档与验证同步**：版本升级至 `v0.0.96`，同步更新 README、项目汇报、更新日志和版本文件；`node tests\security-chat.test.js`（51/51）、`npm run check` 与 `npm run lint` 已通过。

### v0.0.95 更新摘要

- **任务记录分页收口**：智能体任务记录表格默认每页显示 15 条数据，前端请求、后端路由默认值和服务层兜底值保持一致，分页控件结构与样式不变。
- **DAG 任务详情去重**：工作流任务存在 DAG 节点时，详情页只展示工作流节点执行明细，不再重复铺开通用执行步骤和工具统计区域。
- **节点结果可读化补齐**：DAG 节点卡片优先展示结构化查询表格、图表摘要或 Markdown 正文，原始输入输出继续保留在折叠项中用于排查。
- **自由任务模板保存前置**：快速任务标题区新增“保存为模板”入口，用户填写目标和参数后可直接保存；模板与计划面板内保留“保存当前任务”用于模板库管理。
- **文档与验证同步**：版本升级至 `v0.0.95`，同步更新 README、项目汇报、更新日志和版本文件；`node tests\security-agent.test.js`（34/34）与 `npm run check` 已通过。

### v0.0.94 更新摘要

- **能力库工具级治理**：能力包下的单个工具可独立启停，并配置风险等级、审批要求和适用说明；聊天、自由任务和工作流都会尊重同一套工具治理结果。
- **数据库能力安全升级**：数据库连接新增表/集合白名单、字段白名单、敏感字段脱敏、查询超时和 SQL 成本估计提示，`db.group_count` 在前端明确展示为分布统计/图表前置工具。
- **配置诊断闭环**：报表文件能力支持目录可读性和预览文件诊断，IM 通知支持测试发送和最近投递状态，外部能力服务支持健康检查 URL、认证模式、超时、工具 Schema 校验和示例提示词。
- **能力库使用范围收口**：能力库页面文案从“聊天使用”扩展为聊天、自由任务和工作流共用，减少能力入口和任务入口之间的割裂。
- **文档与验证同步**：版本升级至 `v0.0.94`，同步更新 README、更新日志和版本文件；按用户要求未启动浏览器验证，使用 `npm run check`、`npm run lint` 和 `node tests\security-mcp.test.js` 确认。

### v0.0.93 更新摘要

- **自由任务定位收口**：保留原普通智能体能力，但用户入口改为“自由任务 / 快速任务”，用于分析、排查、临时处理和探索性目标拆解。
- **工作流生产入口强化**：工作流编排明确承载编排、发布、版本、计划任务和审计复用，企业生产任务默认推荐使用已发布工作流运行。
- **自由任务孵化工作流**：任务详情新增“生成工作流草稿”，可把本次自由任务的工具步骤转成可检查、可保存、可发布的工作流草稿。
- **模板与任务记录打通**：自由任务模板聚焦目标、参数和上下文复用；工作流模板聚焦步骤和节点复用；统一任务记录支持按“自由任务 / 工作流任务”筛选。
- **文档与验证同步**：版本升级至 `v0.0.93`，同步更新 README、使用手册、项目汇报和更新日志；按用户要求未再启动浏览器验证，使用静态检查、lint 和后端回归测试确认。

### v0.0.92 更新摘要

- **知识图谱质量治理**：知识图谱概览新增质量分、待确认关系、孤立实体、低可信关系、缺来源关系和疑似重复实体建议，帮助用户优先处理影响 Graph-RAG 可信度的问题。
- **待确认关系审核闭环**：低可信共现关系自动进入 `pending`，不会直接参与正式图谱召回；用户可在图谱工作台确认后转为 `active`，也可继续编辑或删除。
- **图谱查询与来源可见**：新增 `/rag/graph/query` 和前端图谱查询面板，关系卡片展示状态、来源文档和来源分块片段，方便排查职责、依赖、归属、影响等关系路径。
- **智能体图谱工具**：智能体工具新增 `knowledge.graph.query`，可在当前用户权限内查询知识图谱关系路径，用于更可靠地回答关系型问题。
- **文档与验证同步**：版本升级至 `v0.0.92`，同步更新 README、使用手册、项目汇报和更新日志；新增图谱治理回归用例，`node scripts\check_syntax.js` 与 `node tests\security-rag.test.js`（51/51）已通过。

### v0.0.91 更新摘要

- **知识库质量与缓存治理**：RAG 召回缓存新增知识库版本域，文档、图谱或召回参数变化后自动避开陈旧缓存；质量报告新增质量分、就绪率、反馈有用率、陈旧文档和图谱覆盖信号。
- **能力库健康评分**：能力治理接口新增健康分、健康等级、活跃率、调用失败率、异常/未检查服务统计和治理建议，能力库工作台同步展示关键指标。
- **智能体预检增强**：智能体运行前预检新增就绪度评分、输入 Token 估算、MCP 异常/未检查服务提醒和审批等待风险，让用户在运行前看到可处理的问题。
- **文档与验证同步**：版本升级至 `v0.0.91`，同步更新 README、使用手册、项目汇报和更新日志；本轮不打开浏览器验证，使用静态检查与后端回归测试确认。

### v0.0.90 更新摘要

- **知识库傻瓜式闭环**：知识库工作台新增三步引导、拖拽上传、质量诊断和“试问一下 / 打开聊天知识库”双动作；可用资料就绪后可一键回到聊天并自动点亮知识库。
- **知识来源可见**：普通聊天命中知识库后，回答上方会保留可引用资料数量和最多 3 个来源文件名，让用户能确认回答是否依据内部资料。
- **能力库使用门槛降低**：能力库新增三步引导、系统/个人能力卡片用途说明、配置表单中文提示和动作示例；示例问题可直接带回聊天并自动打开能力库。
- **能力调用状态中文化**：聊天中的能力库调用会展示“某服务 / 执行只读 SQL、生成图表、查询表格数据”等中文动作名，不再只暴露 `db.*` 等技术工具名。
- **拆分与编码治理**：`agent-runtime.js`、RAG/Chat/MCP 安全测试继续按领域拆分；新增拆分文件逐个检查中文注释、提示和编码，保持中文可读。
- **验证同步**：`node tests\security-chat.test.js`、`node tests\security-rag.test.js`、`node tests\security-mcp.test.js` 和 `npm run check` 已通过，并完成本地浏览器闭环验证。

### v0.0.89 更新摘要

- **DAG 向导运行时兼容修复**：统计图向导和数据库参数辅助统一显式传递当前工具列表，修复拆分后打开统计图模板时 `currentTools is not defined` 的报错。
- **数据库辅助入口一致**：普通参数弹窗、数据库连接下拉、读取表/字段辅助面板和侧栏输入摘要共用同一份工具上下文，避免不同入口看到的数据库连接不一致。
- **大模型节点结果可读化**：DAG 节点卡片的大模型结果改为紧凑 Markdown 布局，标题、列表和正文保持左对齐，运行详情里不再默认铺开 JSON 包装。
- **执行步骤隐藏 LLM JSON 包装**：`完成 DAG 节点：大模型处理` 步骤会优先展示 `content/text/markdown/answer` 中的模型正文，原始 JSON 仍可在“查看原始数据”中展开排查。
- **版本同步与回归验证**：新增向导工具列表传递和 LLM 输出可读化回归断言；`npm run check` 与定向 `tests/security-agent.test.js` 用例已通过。

### v0.0.88 更新摘要

- **LLM 节点顺序门禁**：工作流大模型节点可以作为起始节点，但必须在提示词中引用 `{{goal}}` 或 `{{inputs.*}}`；如果要处理上游数据，则必须连接对应数据、检索或工具节点，避免首节点没有数据输入。
- **运行时兜底增强**：前端画布校验、保存校验、运行预检和 DAG 运行时都接入 LLM 输入来源检查，直接提交 JSON 或旧任务恢复也会被阻断。
- **统计图模板补齐大模型节点**：统计图向导生成链路升级为“分组统计 → 大模型处理 → 生成统计图”，LLM 默认带模型、提示词、输出格式和 Token 配置；图表节点继续直接消费结构化查询结果，保持图表稳定。
- **回归验证同步**：补齐 LLM 起始节点校验、DAG 数据库路由和测试脚本装配断言；`npm run check`、`node tests\security-agent.test.js`、`node tests\security-mcp.test.js` 已通过。
- **版本同步**：应用版本升级至 `v0.0.88`，`package.json`、`package-lock.json`、README、使用手册和项目汇报同步更新。

### v0.0.87 更新摘要

- **监控页资源说明细化**：系统监控页对新拆分脚本与样式增加更细的资源名称兜底，能更直观地区分聊天脚本、聊天样式、公共样式和第三方资源。
- **版本同步**：应用版本升级至 `v0.0.87`，`package.json`、`package-lock.json`、README 和项目汇报同步更新。

### v0.0.86 更新摘要

- **前后端模块化拆分**：聊天、管理端、DAG、工作流、RAG、统计与样式继续按领域拆分成独立脚本/样式块，`partials/scripts.html` 重新编排加载顺序，降低大文件耦合。
- **运行时兼容修复**：修正 DAG 转义函数命名冲突、图谱平移事件、工具栏 `fitToContent` 依赖注入和工作流 JSON 读取等拆分后报错点，保证原有入口继续可用。
- **后端与测试治理**：内置 MCP 与安全测试继续按领域拆分，方便后续扩展、排查和维护。
- **版本同步**：应用版本升级至 `v0.0.86`，`package.json`、`package-lock.json`、README 和项目汇报同步更新。

### v0.0.85 更新摘要

- **任务详情精简**：智能体任务详情移除“任务时间轴”，保留进度、结果、工作流节点、工具统计和执行步骤，让详情弹窗更聚焦。
- **执行步骤排版修正**：任务执行步骤正文中的 Markdown 标题统一左对齐，和段落、列表、表格保持一致。
- **任务列悬浮信息补全**：智能体任务列表的任务列悬浮展示任务、状态、创建/开始/完成时间、模型、模式、步骤数、工具数、错误数和目标。
- **悬浮鼠标样式收口**：任务列保留自定义悬浮信息，但鼠标样式改回普通箭头，并移除原生 `title` 提示，避免双重提示。
- **版本同步**：应用版本升级至 `v0.0.85`，`package.json`、`package-lock.json`、README 和项目汇报同步更新。

### v0.0.84 更新摘要

- **版本历史补齐**：更新日志恢复 `v0.0.83` 条目，版本记录现在保持 `v0.0.84 → v0.0.83 → v0.0.82` 连续。
- **当前版本同步**：`package.json`、`package-lock.json`、README、项目汇报和运行时版本读取统一同步到 `v0.0.84`。
- **回归验证对齐**：补齐工作流前端静态断言对应实现，`npm run check`、`npm run lint` 和完整安全回归 `160/160` 已通过。

### v0.0.83 更新摘要

- **工作流编排页重构**：工作流页顶部收口为“当前画布 + 已保存工作流 + 生命周期状态”，右侧新建/加载/版本/删除按钮取消外层容器框；工具栏按“节点 / 模板 / 操作 / 发布 / 运行”分组，发布和运行入口同处画布工具栏。
- **LLM 节点成为运行配置中心**：新增 `agent.llm` 大模型节点，空画布自动补齐默认 LLM 节点；工作流运行模型和最大步骤数从 LLM 节点读取，保存/校验/运行要求至少保留一个已配置模型的 LLM 节点。
- **模型与权限边界收紧**：工作流 LLM 节点只展示全局模型和当前用户自己的私有模型；新增统一权限层级工具，区分内置 `admin` 系统管理员、普通管理员和普通用户，前后端门禁与文案同步改用权限层级语义。
- **数据库 MCP 工具抽象**：DAG 工具下拉将数据库 MCP 收敛为“列出数据表 / 查看表结构 / 只读 SQL 查询 / 分组统计 / 统计数据表数量”等通用动作，数据库连接改为参数面板里的数据源选择，并在运行时按 `connectionId` 路由到真实 MCP 工具。
- **能力库数据盘点增强**：数据库 MCP 新增 `db.count_tables`，MongoDB 新增 `db.count_collections`；普通对话中明确询问表/集合清单或数量时，规划器失败也会确定性回退到对应工具，不再误判为不需要能力库。
- **DAG 参数编辑体验提升**：节点参数迁入更大的弹窗工作台，常用工具参数补齐中文标签、说明和占位提示；任务补充说明改为多行文本，统计图向导补充 schema 字段说明。
- **回归验证**：新增权限层级、工作流 LLM 节点、预览/发布版运行、数据库表/集合计数和聊天 MCP 缺工具提示测试；`npm run check`、`npm run lint` 与完整安全测试套件均已通过。

### v0.0.82 更新摘要

- **刷新后图表不再丢失**：普通对话中通过 SSE 实时显示的图表 spec 会在 assistant 消息保存前合并为 `pivot-echart` 代码块，刷新页面后历史消息可通过现有 Markdown 图表渲染链路恢复。
- **重复图表去重**：后端记录并去重同轮 `chart` SSE 事件，自动图表链路取消二次发射，前端也用 `pendingChartKeys` 兜底忽略重复 chart 事件，避免同一张图实时显示两份。
- **持久化兼容保护**：如果助手正文已包含明确的 `pivot-echart` / `pivot-chart` 图表块，不再额外追加 SSE 图表，避免刷新后的正文重复。
- **回归测试补齐**：新增图表 spec 持久化回归用例，覆盖追加、去重和已有图表块保护；本轮 `npm run check`、目标 ESLint 和安全测试套件均已验证通过。

### v0.0.81 更新摘要

- **图表 SSE 直达前端**：普通对话中 `viz.build_chart` 和自动图表生成的结果不再通过模型"照抄代码块"传递，改为 SSE `chart` 事件绕过模型文本生成直接发送前端渲染，从根源消除"模型回复代码而非真正图表"的问题。
- **图表意图检测扩展**：新增 `统计分析|数据对比|数据汇总|分布情况|占比|排名` 等关键词，更精准地识别隐式图表请求并触发工具调度。
- **规划器工具调度强化**：数据库/数据查询请求优先选用工具而非返回 `action=none`；规划器跳过时注入可用工具提示防止主模型自行生成代码。
- **中文思考约束全覆盖**：补上 Agent 流式路径的中文思考规则；聊天系统提示词新增 `【重要工具规则】` 块明确禁止生成 Python/JS 图表代码；所有模型交互路径统一使用 `【重要语言规则】` 结构化约束。
- **文档与测试**：CHANGELOG、`package.json` 同步至 `v0.0.81`；安全回归测试 `141/141` 全部通过。

### v0.0.80 更新摘要

- **工作流运行闭环**：DAG 模式统一为“工作流”，新增运行工作流选择器、新建工作流弹窗、工作流说明保存、运行目标自动推断和运行前有效性校验，避免空画布或临时 JSON 被误提交。
- **运行输入与结果可视化**：自动扫描 `{{inputs.*}}` 生成运行输入表单，提交时合并为 `dagInputs`；运行详情新增 DAG 迷你拓扑状态图，依赖和节点状态更直观。
- **统计图模板与数据库 MCP**：新增 `db.group_count` 只读分组统计工具，支持 SQLite/PostgreSQL/MySQL/SQL Server；DAG 编辑器新增统计图模板向导，可从数据库表和字段生成“分组统计 -> 生成统计图”工作流。
- **结构化数据引用修复**：`{{nodes.xxx.output.rows}}` 会自动回退读取 MCP `structuredContent.rows`，图表节点可以直接消费结构化数据行。
- **思考过程中文约束与并发配置**：Agent 规划、流式工具调用、最终答案合成提示词和运行步骤标题完成中文化；普通聊天请求也会要求可见 `reasoning_content` / `<think>` / `<thought>` 思考内容默认使用中文；DAG 节点并发通过 `AGENT_DAG_NODE_CONCURRENCY` 配置。
- **文档与测试**：README、CHANGELOG、`.env.example`、`package.json` 与 `package-lock.json` 同步至 `v0.0.80`，补充 `db.group_count` 和结构化结果引用的安全回归用例。

### v0.0.79 更新摘要

- **Agent 提示词中文化**：将 Agent 运行时核心系统提示词从英文迁移为中文，Schema `thought` 字段标注为「简短推理（中文）」，运行模式标签、上下文指导、流式提示、最终答案合成提示及兜底文案全面汉化，确保大模型思考过程与最终输出一致使用中文。
- **回归验证**：`npm run check`、`npm run lint` 与 `node tests/security.test.js` 安全回归通过。

### v0.0.78 更新摘要

- **智能体工作流编排 UX 全面优化**：经过用户体验评审后的 10 项重点改进——新节点智能推断默认工具、画布节点坐标持久化（手动布局不再丢失）、节点属性字段 tooltip 说明、键盘快捷键（`Delete`/`Escape`）；保存/发布前主动校验门禁（循环依赖/未选工具时弹出确认）；工作流删除后一键撤销恢复（30 天内可找回）；统计图模板替换前检测未保存更改并提醒。
- **运行时输入可视化面板**：自动扫描 DAG 节点中所有 `{{inputs.*}}` 变量引用，动态生成表单字段，用户无需手写 JSON 即可填写运行时参数。
- **DAG 运行结果可视化**：运行详情中渲染迷你拓扑图，节点按状态着色（绿完成/蓝运行/红错误/黄跳过/灰待执行），依赖边一目了然。
- **DAG 节点并发可配**：互不依赖节点并行数从硬编码 4 改为环境变量 `AGENT_DAG_NODE_CONCURRENCY`（默认 4），运行时状态 API 暴露该值。
- **回归验证**：`npm run check`、`npm run lint` 与 `node tests/security.test.js` 安全回归通过。

### v0.0.77 更新摘要

- **工作流图表工具去重**：修复智能体工作流「选择工具」下拉中「图表与展示」分类下 `viz.build_chart` / `viz.build_table` 重复显示的问题。可视化工具同时作为内置智能体工具与「图表生成」内置 MCP 能力注册，工具目录在 `agent-tool-catalog.js` 合并内置工具与缓存 MCP 工具时按裸工具名去重，同名只保留内置工具；外部 MCP 工具（带 `mcp.<id>.` 服务前缀）不受影响，工具执行链路不变。
- **回归验证**：`npm run check`、`npm run lint` 与完整 `node tests/security.test.js`（`139/139`）通过。

### v0.0.76 更新摘要

- **普通用户私网出口默认收紧**：个人 MCP 外部端点、内置 IM Webhook 与数据库 MCP 连接默认禁止普通用户配置 loopback、link-local、局域网或内部主机地址，数据库 MCP 私网限制默认开启。
- **可信代理来源识别**：新增 `TRUSTED_PROXY_IPS`，仅当直连来源命中受信任 IP/CIDR 时才采信 `X-Forwarded-For`，访问日志同步使用同一套客户端 IP 解析逻辑。
- **DNS rebinding 防护闭环**：模型调用、MCP JSON-RPC、RAG Embedding、模型探测、健康检查、标题生成、记忆压缩和告警 Webhook 等 HTTP 出口统一使用安全 lookup Agent。
- **HTML 清洗兜底加固**：`PivotSafeHtml.sanitizeHtml` 在 DOMPurify 未加载时退回文本转义，不再返回原始 HTML。
- **回归验证**：新增/调整相关安全测试，`npm run check` 与完整 `node tests/security.test.js`（`139/139`）通过。

### v0.0.75 更新摘要

- **安全复查修复**：记忆压缩出站补齐运行时 SSRF 守卫（闭合最后一个 DNS rebinding 缺口）；修复 MongoDB SSL 开关被静默忽略（勾选 SSL 仍明文连接）与 MySQL 自签名开关被丢弃的问题，四种数据库 TLS 行为统一；用户上传工作簿解析新增单表 5 万行上限。
- **依赖安全**：`xlsx` 升级至 SheetJS 官方修复版 `0.20.3`，消除原型污染与 ReDoS 高危漏洞（用户上传文件解析攻击面）。
- **前端体验**：智能体任务过滤文本框改为防抖触发，避免逐键请求风暴与响应乱序覆盖。
- **复查与验证**：以并行子代理对安全/服务端/前端/依赖四维度做全面深入复查，确认上一轮重构无回归，安全回归 `135/135` 通过。

### v0.0.74 更新摘要

- **安全加固**：外部 MCP 调用与 IM Webhook 推送新增出站 SSRF 防护（调用时 DNS 解析，阻断 loopback/link-local/云元数据与 DNS rebinding）；数据库连接默认校验 TLS 证书并提供「信任自签名」显式开关；访问日志对 URL 中的 token、api_key 等敏感参数脱敏。
- **性能优化**：报表 MCP 与 PDF/图片读取的同步文件读全部改为异步，消除大文件读阻塞事件循环；监控指标对未匹配路由归入 `unmatched` 桶防止内存无界增长；GPU 监控定时器补齐 `.unref()`。
- **工程重构**：`agent-runtime.js`（2797→约 1571 行）与 `routes/chat.js` 的 `/chat` 处理器按领域拆分为多个模块，前端 `rag.js` 拆出格式化/图谱布局/渲染/交互模块，行为保持不变。
- **前端统一**：HTML 转义统一委托 `PivotSafeHtml`（含 `"` 与 `'`）；裸 `fetch` 全部改用带 CSRF 与 401 自动刷新的 `apiFetch`。
- **依赖安全**：非破坏性升级 axios、qs、brace-expansion、ip-address、express-rate-limit 等 6 项依赖漏洞。
- **回归验证**：57 个改动/新增 JS 文件语法通过，服务端完整加载，安全回归 `135/135` 通过。

### v0.0.73 更新摘要

- **知识图谱关系地图增强**：关系地图改为中文图谱化展示，支持拖拽平移、滚轮缩放、放大/缩小/1:1 复位，并修复节点点击被拖拽逻辑吞掉的问题。
- **完整信息悬浮提示**：关系地图节点名称截断时可通过悬浮提示查看完整实体信息；右侧关系明细卡片悬浮可查看完整起点、关系、终点、来源、可信度和描述。
- **校准流程弹窗化**：实体校准从页面底部嵌入区迁移为弹窗，关系校准也统一使用弹窗，图谱浏览区域不再被表单挤占。
- **关系明细与顶部布局优化**：关系明细卡片更紧凑；实体列表可信度完整显示；知识图谱顶部标题行对齐知识库页面，统计标签与搜索筛选合并为同一行。
- **系统监控滚动修复**：慢查询与异常告警区域补齐内部滚动，长列表不再被外层布局截断。

### v0.0.72 更新摘要

- **登录页公告公开展示**：公告新增“登录页展示”开关和公开只读接口，未登录用户可查看超级管理员发布的全员公开公告，普通定向公告不会被公开泄露。
- **公告布局与入口优化**：登录页公告移到左侧独立面板；主页公告入口移到左侧系统名称旁，公告中心从品牌区附近展开，避免与输入区浮动按钮冲突。
- **公告管理表格增强**：公告管理新增“登录页”独立列，标题列只展示标题；同时保留序号、详情、撤下、编辑、删除等权限化操作。
- **不再提示语义收紧**：普通公告点击“不再提示”后会记录当前用户已读并从站内公告入口、横幅和列表隐藏；强制确认公告确认前不允许隐藏。
- **回归验证**：`npm run check`、`npm run lint`、`node tests/security.test.js` 已通过，安全回归 `135/135`。

### v0.0.71 更新摘要

- **系统公告能力**：新增公告表、用户阅读确认状态表和公告 API，支持类型、优先级、发布窗口、草稿/发布/归档、软删除和审计记录。
- **定向投放**：管理员可按全员、单位、角色或指定用户投放公告，用户只会看到命中自身范围且处于有效期内的公告。
- **用户端通知体验**：聊天页新增公告横幅、公告中心和强制确认弹窗，支持已读、确认和当前用户隐藏。
- **后台公告管理**：系统设置新增“公告管理”页，支持搜索、状态筛选、新建、编辑、撤下、删除、确认要求和投放范围配置。
- **回归验证**：公告路由测试覆盖创建、定向可见性、非目标用户隔离、确认/隐藏状态落库和软删除审计链路。

### v0.0.70 更新摘要

- **部署构建修复**：`docker-compose.yml` 移除新版 Compose 已废弃的 `version` 字段，并补齐 `build.context` 与 `build.dockerfile`，`docker compose build --no-cache` 可直接基于当前目录构建 `pivot` 镜像。
- **模型端点本地识别优化**：系统监控在 Pivot 运行于 Docker/容器环境时，会把 `llama-server`、`ollama` 等 Docker 服务名识别为本地模型端点；同时支持显式开关和本机域名/IP 别名识别。
- **RAG 上下文预算裁剪修复**：上下文预算器会识别 `PIVOT_RAG_CONTEXT_BEGIN/END` 标记，user-role 知识库注入消息也会按 RAG 生成型上下文优先裁剪，不再误当普通历史消息。
- **回归验证**：新增 Docker 服务名本地识别与 user-role RAG 预算裁剪测试，`node tests/security.test.js` 已通过 `134/134`。

### v0.0.69 更新摘要

- **在线更新程序移除**：删除系统设置中的在线更新入口、前端脚本、后端 updater API/监控服务、`updater/` sidecar、Compose `online-update` profile 与 Docker socket 挂载。
- **设置页滚动条修复**：修复系统设置缩放画布记住旧 tab 高度的问题，短页面不再因为历史高度或 1-2px 测量误差误出现垂直滚动条。
- **代码质量收敛**：知识图谱关系编辑改用 `window.prompt`，设置页缩放脚本显式使用 `window` 浏览器全局对象；`npm run check` 与 `npm run lint` 均已通过。
- **文档与版本同步**：README、CHANGELOG、`.env.example`、`package.json`、`package-lock.json` 和检查脚本同步记录本轮清理与版本升级。

### v0.0.68 更新摘要

- **知识图谱与 Graph-RAG**：新增实体、提及、关系存储与索引服务，知识库召回会融合图谱邻居关系并追加图谱上下文，调试结果可查看图谱增强依据。
- **图谱工作台升级**：知识库新增知识图谱入口，支持实体搜索、类型筛选、局部关系图、关系列表、实体编辑、实体合并、关系编辑删除和单文档图谱重建。
- **全屏与刷新保持**：知识图谱页面改为真正全屏承载，样式、字体、字号、搜索框、下拉框和按钮对齐知识库页面；刷新后继续停留在知识图谱页面。
- **管理界面适配**：系统设置工作区新增比例缩放画布，模型配置弹窗改为内容区滚动与底部操作栏固定，审计、监控、附件预览等管理弹窗补齐视口边界和滚动兜底。

### v0.0.67 更新摘要

- **聊天元数据实时补齐**：模型端点熔断、上游异常等失败消息会立即显示耗时、Token 和速率，不再需要刷新页面后才显示完整元数据。
- **失败消息统计一致**：后端在错误消息保存事件中回传 `tokenCount`，前端同步刷新 footer 并写回统计，保证刷新前后一致。
- **文档与版本同步**：应用版本升级至 `v0.0.67`，同步更新 README、CHANGELOG、`package.json`、`package-lock.json` 和前端版本兜底值。

### v0.0.64 更新摘要

- **Markdown 渲染更稳**：聊天回复如果被外层 `markdown`、`text/plain` 或 `plain` 代码围栏整体包住，会自动去壳后按 Markdown 展示；真实代码块仍保持代码样式。
- **横向滚动与列表修复**：代码块、表格和误包裹的 prose 内容限制在消息气泡内部滚动或换行，普通无序列表恢复 bullet，只有 checkbox 任务列表隐藏项目符号。
- **非标准流式兜底**：后端兼容上游“假流式”或完整 JSON 响应，前端会把单次大块 `content` 平滑拆分回放，减少整段突然刷出的观感。
- **文档与版本同步**：应用版本升级至 `v0.0.64`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG。

### v0.0.63 更新摘要

- **上下文压缩阈值可配置**：上下文压缩阈值从固定 `MEMORY_THRESHOLD` 改为 `app_settings.memory_threshold` 运行时设置，管理员可在模型配置页直接填写 `12K`、`1M` 或纯数字并保存，保存后自动压缩、手动压缩和上下文用量展示即时使用新阈值。
- **设置入口收紧**：模型配置页的“上下文阈值”控件改为紧凑显示在“添加模型”按钮旁边，不再占用单独的大块区域。
- **文档与版本同步**：应用版本升级至 `v0.0.63`，同步更新 `package.json`、`package-lock.json`、README 与 CHANGELOG。

### v0.0.62 更新摘要

- **内置管理员账号自愈与修复**：系统在每次启动或预置数据填充时，都会自动对内置 `admin` 账号的角色、启用状态和软删除状态进行安全核查。一旦检测到受损或异常，将全自动触发修复，重新将其还原为正常的管理员角色与 active 状态。
- **Node.js v24.x 运行兼容调优**：原生依赖 `better-sqlite3` 由 `^9.4.3` 升级至 **`^11.3.0`**。完美在 Node v24 和 Windows / Linux 下流畅工作，且下载时自动匹配新 ABI 预编译包，免除了全新的本地编译开销和文件占用冲突问题。
- **安全回归测试覆盖**：在 `tests/security.test.js` 中新增了 `seed repair restores built-in admin role and login status` 测试用例，对内置管理员受损自愈逻辑进行了多场景闭环回归测试。
- **文档与版本同步**：应用版本升级至 `v0.0.62`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG；所有回归测试均已通过。

### v0.0.61 更新摘要

- **工作流库与版本治理**：智能体工作流编排支持保存到工作流库、加载、发布、版本列表、差异对比、历史回滚和删除；计划任务可引用已发布版本，避免周期任务被草稿变动影响。
- **画布编辑体验升级**：节点设置改为右侧抽屉，JSON 高级视图改为弹窗；工作流名称、工作流库和右侧操作按钮重新对齐，页面更紧凑。
- **节点配置补齐**：节点支持失败策略、重试次数、超时、工具参数模板、输入 schema 提示、变量插入和失败节点单独重跑。
- **工具选择器中文化**：节点工具下拉按知识与会话、数据库、报表与文件、图表与展示、数据处理、文档处理、格式转换、消息通知、系统诊断和外部能力分组，显示中文名称、用途说明、来源标签和内部 ID。
- **运行前校验增强**：工作流编排模式下显示当前画布状态，非 DAG 模式隐藏工作流输入；空工作流、未选工具节点和非法 JSON 会在运行前阻断。
- **模板工作流闭环**：DAG 模板会保存工作流快照、运行输入、工作流引用和版本信息，应用模板时同步恢复画布与输入。
- **文档与版本同步**：应用版本升级至 `v0.0.61`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、README、使用手册与 CHANGELOG。

### v0.0.60 更新摘要

- **长会话自动压缩即时生效**：达到上下文阈值时，会先完成压缩并重新读取消息，再组装当前模型请求上下文，避免本轮请求仍携带旧的超长历史。
- **上下文摘要顺序修正**：长期摘要会排在近期消息之前，保持历史摘要到当前对话的时间线顺序。
- **手动压缩提示优化**：重复触发压缩时会明确提示压缩进行中，不再表现为“没有可压缩内容”。
- **刷新后停留当前页面**：刷新会恢复当前主工作区，覆盖聊天、知识库、能力库、智能体、工作流编排、手册、设置和打印/导出页面。
- **页内位置恢复**：聊天页会恢复当前会话，设置页会恢复当前子 tab，打印/导出页会恢复对应会话预览；普通用户恢复到管理员 tab 时会自动回退到可访问页面。
- **会话消息复制修复**：消息复制按钮会捕获剪贴板写入异常，并在剪贴板 API 不可用、非安全上下文或权限失败时自动回退到 textarea 复制。
- **重新生成知识库命中修复**：已命中知识库的会话点击重新生成时，会复用最近一条用户问题作为 RAG 检索词，不再因为重新生成请求本身没有新输入而显示未命中。
- **文档与版本同步**：应用版本升级至 `v0.0.60`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。

### v0.0.59 更新摘要

- **知识库会话回答链路修复**：启用知识库且召回命中后，RAG 内容会以带强约束的上下文插入到当前问题之前，不再作为末尾 `system` 消息被部分兼容模型忽略。
- **知识库回答约束增强**：模型会被明确要求优先依据知识库、冲突时以知识库为准，资料不足时说明“知识库中未找到足够依据”，减少脱离资料回答。
- **知识库命中状态可见**：聊天流新增命中/未命中提示，用户能直接确认本轮是否真正使用了知识库。
- **监控命中率口径修正**：系统监控中的“命中率”展示知识库召回命中比例，“缓存命中率”单独展示缓存复用比例，避免命中文档但监控显示 0% 的误解。
- **监控统计口径巡检**：模型端点状态保留在数据概览页面；系统监控“索引分片”改为当前可召回分片数，Prometheus HTTP 直方图 bucket 恢复正确累计输出。
- **审计动作口径修正**：发送按钮点击不再把浏览器事件误传成 `regenerate=true`，后端也只接受显式 true，避免首次发送被记为“重新生成回答”。
- **文档与版本同步**：应用版本升级至 `v0.0.59`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。

### v0.0.58 更新摘要

- **附件上传链路加固**：图片或附件发送前固化附件快照，发送后会话框立即展示用户消息与附件卡片，修复上传图片分析后需要刷新才显示用户消息的问题。
- **会话归属与上传竞态治理**：上传必须绑定当前用户拥有的会话，切换会话会清空未发送附件，上传批次锁定初始会话，避免附件串到错误会话。
- **附件 URL 与视觉解析兼容**：上传响应、附件库、管理员附件列表、历史补 token 统一编码附件 URL，并兼容旧式空格/括号文件名，模型视觉解析与能力判断同步支持。
- **文档与版本同步**：应用版本升级至 `v0.0.58`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。

### v0.0.57 更新摘要

- **开放注册后台开关**：内置 `admin` 可在“系统设置 > 用户管理”右上角直接开启或关闭开放注册；设置写入数据库并即时生效，`.env` 中的 `ALLOW_PUBLIC_REGISTRATION` 仅作为首次默认值。
- **注册与密码提示优化**：注册、管理员添加用户、重置密码和个人改密统一返回 400 输入错误；密码规则明确为“至少 8 位，并同时包含字母和数字”，注册页密码框内直接展示规则，提交时提示缺少长度、字母或数字的具体原因。
- **用户管理体验增强**：开放注册开关回到用户操作区紧凑展示，用户列表分页回退、导入失败、保存失败和重置密码失败会优先展示具体后端错误。
- **上下文压缩与用量刷新**：上下文用量圆环可手动触发压缩，压缩状态、完成提示、会话重载和实时 token 用量刷新联动，模型调用超时和取消错误更明确。
- **审计与运营表格优化**：审计日志列表和导出补齐显示名字段，用量明细、API Key 和第三方调用记录表格继续优化列宽、Token 展示和空态提示。
- **文档与版本同步**：应用版本升级至 `v0.0.57`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。

### v0.0.56 更新摘要

- **聊天消息同步修复**：流式回复保存完成后会同步回填用户消息与助手消息的持久化 ID，并刷新当前会话上下文用量；如果当前会话 DOM 中缺少刚发送或刚生成的消息，会自动重新加载当前会话，避免用户消息需要刷新页面才显示。
- **流式代码块滚动修复**：回复生成过程中会同时保持聊天列表和最新代码块贴近底部；当用户主动向上滚动查看历史时不会强制抢回视口，代码块内容会跟随最新增量显示。
- **上下文压缩交互补齐**：输入框上下文圆环改为可点击按钮，新增 `GET /api/sessions/:id/context` 查询实时用量和 `POST /api/sessions/:id/compact` 手动压缩接口，压缩中有忙碌态，完成后刷新用量并在当前会话重新加载压缩结果。
- **自动压缩策略调整**：后台压缩不再等待“活跃消息超过 10 条”，默认在活跃上下文 token 超过 `MEMORY_THRESHOLD`（当前 12K）且存在可归档早期消息时触发；`MEMORY_MIN_MESSAGES_TO_COMPRESS` 默认降为 `1`，`MEMORY_SUMMARY_KEEP_COUNT` 可配置保留最近消息数量，更适合单条超长输入的场景。
- **文档与版本同步**：应用版本升级至 `v0.0.56`，同步更新 `package.json`、`package-lock.json`、前端版本兜底值、Service Worker vendor 缓存版本、README 与 CHANGELOG。

### v0.0.55 更新摘要

- **能力库全链路稳定性增强**：模型调用能力前新增规划与工具筛选加固，“查数据并生成图表”等复合任务会先完成查询/取数，再衔接图表生成；同时确认 `pivot-echart` 图表渲染路径可稳定展示结构化图表结果。
- **知识库检索链路优化**：全文检索命中与兜底候选改为合并排序，空库/无候选时不再误报向量配置失败，空文档会明确失败并提示未解析出可索引文本，召回摘要同步反映当前用户的个人检索配置。
- **知识库索引稳定性增强**：文档入库索引使用独立的向量请求超时，默认 120 秒，并将底层 timeout 转换为可读的检索配置排查提示。
- **知识库格式扩展**：在 `txt`、`md`、`pdf` 基础上，新增 `doc`、`docx`、`xls`、`xlsx`、`csv`、`json`、`html`、`htm` 文本抽取与索引支持，上传后会进入统一解析、分块和召回链路。
- **产品文案收口**：能力库与知识库页面继续统一“能力库 / 能力服务 / 检索配置 / 召回测试”等用户侧表达，减少底层实现名暴露。
- **工程质量收口**：补齐前端 lint 环境声明并清理未使用变量，`npm test` 已纳入 lint；智能体任务列表与会话侧栏加载减少不必要的统计查询。

### v0.0.54 更新摘要

- **能力库工作台升级**：MCP 页面升级为“能力库”，系统能力与个人能力卡片化展示；工具列表改为弹窗查看，启停使用椭圆开关，去除“内置”、多余“MCP”和底层实现名，并扩展报表文件、文档解析、数据处理、格式转换等能力。
- **智能体工作台重构**：高级设置参数前置到任务面板，工作流编排从 DAG 独立成完整页面入口；模板与计划、能力与结果弹窗重排为更高效的大尺寸布局。
- **任务记录表格化**：任务历史改为统一数据表格，支持详情弹窗、删除操作、分页、状态/搜索过滤、输入/输出/总 Token 分列与 K/M/B 压缩显示。
- **全局分页统一**：新增 `renderWorkspacePagination(...)`，智能体和知识库分页复用统一样式，系统设置相关表格分页控件同步收敛。

### v0.0.53 更新摘要

- **智能体任务历史可视化**：任务详情面板新增水平时间轴（每步按 `duration_ms` 比例分配色块，按 step 类型着色，失败步骤红色覆盖，悬停显示工具名/耗时/失败标记）与工具调用统计（按 `tool_name` 聚合次数/总耗时/平均耗时/失败次数，调用次数占比进度条排序）。
- **零新增 API**：纯前端基于已有 `getRunDetailForUser` 返回的 `steps` 数据计算，不改后端。
- **验证**：`npm run check` 全部 103 文件通过，`node tests/security.test.js` 维持 `100/100`。

### v0.0.52 更新摘要

- **流式 function calling 实时演示面板**：在 `agent-runtime` 流式分支启用（`AGENT_STREAMING_TOOLS=true`）后，每次累加器更新通过 `agent.streaming` SSE 事件推送 `content / partialToolCalls / finishReason` 增量；100ms 或 ≥120 字符增量节流，避免事件风暴。
- **前端实时渲染**：任务详情顶部插入 `.agent-streaming-panel`，含脉冲指示器、当前 step / finish_reason、累加文本与每个 partial tool call 的 arguments 字符数和预览；任务终态 5 秒后淡出。
- **验证**：新增 1 项 SSE 隔离单测；`npm test` 通过 `100/100`，`npm run check` 全部 103 文件通过。

### v0.0.51 更新摘要

- **DAG 编辑器缩放/平移**：滚轮以光标为锚点缩放（0.3x–2.5x），空白处拖动整图平移，工具栏新增"适配画布"一键重置视角；与节点拖拽互不干扰。
- **小地图**：画布右下角固定 140x90 缩略浮窗，显示所有节点和当前视口框；点击跳转视口中心，方便大型 DAG 定位。
- **验证**：`npm run check` 全部 103 文件通过，`node tests/security.test.js` 维持 `99/99`。

### v0.0.50 更新摘要

- **`auto-escalate` 成本升级策略**：路由策略新增第 6 项，先用最便宜模型尝试，合成最终答案后启发式判定置信度（空输出/过短/异常 finishReason/低置信短语），不达标时升级到更强模型重新合成；前端模板下拉同步增加该选项。
- **路由工具增强**：`server/services/model-router.js` 新增 `assessConfidence` 与 `pickEscalationModel` 两个工具函数，支持中英文低置信短语识别。
- **验证**：新增 3 项单测，`npm test` 通过 `99/99`，`npm run check` 全部 103 文件通过。

### v0.0.49 更新摘要

- **agent-runtime 接入流式 function calling**：在 `runAgent` 主循环新增可选的流式 `tool_calls` 协议分支，配合 v0.0.48 的累加器与 `callModelStreamingWithTools` API；每轮规划与工具调用都会写入 `agent_steps`，审计完整保留。
- **环境变量开关**：默认 `AGENT_STREAMING_TOOLS=false`，所有现有任务行为零改动；置为 `true` 才启用流式分支，任何流式失败自动回退到旧回合制 JSON 协议，确保任务能完成。
- **DAG 任务不受影响**：`run_mode = 'dag'` 永远走原有图调度，不会被流式工具替换。
- **验证**：`npm run check` 全部 103 文件通过，`node tests/security.test.js` 维持 `96/96`。

### v0.0.48 更新摘要

- **流式 function calling 基础设施**：新增 `server/services/streaming-tools.js` 纯函数累加器，按 OpenAI streaming tool_calls 协议累加多工具 `arguments` 与 assistant `content`；`finalize()` 返回结构化结果（含解析后的参数与 parseError 降级字段），兼容 legacy `function_call` delta。
- **agent-model 新增 `callModelStreamingWithTools` API**：基于 `axios stream` + `createSseEventParser` 实现流式调用，复用现有全局/端点并发保护与失败统计；当前**不替换** `callModelText`，agent-runtime 主循环维持回合制 JSON，未来可通过环境变量渐进启用。
- **验证**：新增 7 项 streaming-tools 单测，`npm test` 通过 `96/96`，`npm run check` 全部 103 文件通过。

### v0.0.47 更新摘要

- **智能体 DAG 可视化编辑器**：新增 `client/chat/agents-dag-editor.js`（纯原生 SVG + 拖拽，零依赖），节点可拖动、出端口拖到入端口即创建依赖；工具栏提供"添加节点 / 自动布局 / 从 JSON 同步"；选中节点后弹出详情面板编辑标题、工具、条件、输入 JSON 与依赖；与原 `#agent-dag-spec` textarea 双向同步，JSON 视图改为折叠 details，普通用户不再面对原始 JSON。
- **CSS 与集成**：`styles/workspaces/agent.css` 末尾追加约 180 行编辑器样式（网格画布背景、节点/边/端口/选中态、详情面板栅格表单、依赖胶囊），兼容暗色模式；`agents.js` 在 `loadAgentTools()` 后自动挂载编辑器。
- **验证**：`npm run check` 全部 102 文件通过，`node tests/security.test.js` 维持 `89/89`。

### v0.0.46 更新摘要

- **智能体模型路由策略**：新增 `server/services/model-router.js`，提供 5 种策略 —— `fixed`（固定）、`auto-vision`（视觉优先）、`auto-context`（上下文匹配）、`auto-cost`（成本优先）、`auto-load`（负载均衡）；智能体任务启动时按 `model_router` 字段自动选模，命中时写入 `chosen_model_id` 并在步骤记录中标注路由理由，回退安全。
- **DB 扩展与前端入口**：`agent_templates` 与 `agent_runs` 新增 `model_router` 字段（默认 `fixed`，对旧任务零影响）；智能体工作台"MCP 审批"旁新增"模型路由"下拉，模板加载/保存自动透传策略选择。
- **接口**：新增 `/api/agents/model-routers` 公开策略元数据，供前端下拉填充。
- **验证**：新增 5 项单测，`npm test` 通过 `89/89`，`npm run check` 全部 101 文件通过。

### v0.0.45 更新摘要

- **RAG 召回测试可视化**：调试弹窗新增关键词高亮、得分进度条、按文件聚合的"命中 / 峰值 / 均值"汇总条；`/api/rag/debug-query` 接口返回检索耗时。
- **会话 PDF 导出**：新增 `/api/sessions/:id/print` 打印友好视图（带 CSP nonce），用户在浏览器内按 Ctrl/Cmd+P 即可导出 PDF；会话菜单新增"打印 / 导出 PDF"入口，无需引入服务端重型依赖。
- **验证**：`npm run check` 全部 100 文件通过，`node tests/security.test.js` 通过 `84/84`。

### v0.0.44 更新摘要

- **`window.Pivot` 全局工具命名空间**：新增 `client/chat/pivot-core.js`，集中提供节流、防抖、rafThrottle、LruCache、formatBytes / formatNumber、chooseStreamInterval 等小工具，向前兼容现有 `window.*` 全局函数，新代码可统一从 `window.Pivot` 调用。
- **流式 Markdown 自适应节流**：聊天流式渲染按累计内容长度阶梯式调整刷新间隔（80 / 140 / 220 / 320ms），长回答中 `marked.parse` 的重排开销显著下降，无明显视觉延迟。
- **验证**：`npm run check` 全部通过，`node tests/security.test.js` 维持 `84/84`。

### v0.0.43 更新摘要

- **LRU / TtlCache 工具**：新增 `server/cache.js`，提供带容量上限和 TTL 的 LRU 与懒清理 TtlCache；服务端 `dirSizeCache` 改用 LRU，避免内网长期常驻部署的目录尺寸缓存无限增长。
- **MCP 调用日志脱敏**：新增 `redactSecrets` / `maskSecretString`，MCP 调用日志的 input/output 在写入审计表前自动屏蔽 `api_key`、`Bearer`、`sk-*`、JWT 等敏感字段，避免落入合规导出包。
- **后台任务超时与并发治理**：新增 `withTimeout` / `KeyedConcurrencyGuard` / `TimeoutError`；`compressMemory` 接入超时与会话级去重，全局并发上限可调；GPU 监控、模型端点监控、智能体任务恢复等启动失败改为带日志告警，不再静默吞错。
- **MCP 审批服务端二次校验**：移除 `agent-runtime` 中的 `metadata.approval === 'all_mcp_approved'` 死分支，只承认通过 `approveAgentTool` 写入的工具白名单。
- **智能体运行时拆分**：从 1978 行的 `agent-runtime.js` 拆出 `agent-validators.js`（常量与所有 normalize* 函数），主文件减少约 200 行，对外导出表完全保持。
- **回归测试加固**：新增 9 项基础设施单测，`npm test` 通过 `84/84`。

### v0.0.41 更新摘要

- **普通用户使用帮助**：新增独立 [Pivot 使用帮助](使用帮助.md)，按登录、对话、附件、知识库、MCP、智能体、模型配置、API Key、状态排查、常见问题和安全建议组织，聚焦普通用户操作指导。
- **前端帮助入口**：左下角用户区新增“帮助”入口，在应用内工作区打开同一份帮助内容，用户不需要切换到新标签页查找文档。
- **Docker 部署可用性**：服务端新增 `/manual` 与嵌入版 `/manual?embed=1` 渲染页面，直接读取根目录 `使用帮助.md`；检查脚本会校验帮助文件、前端入口和 `.dockerignore`，避免 Docker 镜像部署后帮助缺失。
- **侧栏用户区优化**：左下角用户名称、帮助、设置和退出保持紧凑单行布局；系统管理员标识改为小盾牌图标，仅内置 `admin` 账号显示，长名称保留悬浮完整提示。
- **文档与版本同步**：版本升级至 `v0.0.41`，`package.json`、`package-lock.json`、前端版本兜底值、README 与 CHANGELOG 已同步；`npm run check` 已通过。

### v0.0.40 更新摘要

- **监控与模型状态优化**：GPU 与并发保护区的利用率按比例正确显示，模型端点状态只保留当前启用配置，删除和历史残留端点不再占位。
- **MCP 工作台优化**：已缓存工具改为紧凑卡片网格，MCP 调用记录按服务归属展示，类型下拉顺序和空态布局都更顺手。
- **智能体工作台修复**：智能体模型选择与主页面一致，修正模型文案乱码，补齐高风险审批、任务重跑约束、模板/计划删除、DAG 续跑和导出内容。
- **模型弹窗紧凑化**：新增与编辑模型弹窗压缩了标题、说明、输入框、行距和按钮高度，更适合密集配置。
- **知识库与智能体复核**：确认知识库上传、索引、调试检索、质量报告，以及智能体运行、预检、模板、计划、产物、知识库工具调用链路完整可用。
- **文档与版本同步**：版本升级至 `v0.0.40`；`npm run check`、`node tests/security.test.js` 已通过。

### v0.0.39 更新摘要

- **权限与输入工具修复**：完成超级管理员、管理员、普通用户三类权限复核；普通用户和管理员均可在聊天输入框点击启用知识库与 MCP，按钮改为点击变色选中，不再使用复选框视觉。
- **MCP 模块升级**：新增报表与数据文件、可视化图表、报告编排、局域网消息通知四类内置 MCP；数据库 MCP 增强局域网连接诊断，MCP 治理区压缩为紧凑状态条。
- **组合式报告工作流**：支持按“数据库/文件取数 → 图表/表格展示 → 固定格式报告 → 内网消息通知”拆分为独立 MCP server 后自由组合，适合数据表分析和固定报告生成。
- **界面与监控优化**：修复左侧面板宽度随标题字数变化的问题；GPU 区域只展示 Pivot 部署服务器本机 GPU 与并发保护，同服务器模型可通过本机别名识别为本地端点。
- **文档与版本同步**：版本升级至 `v0.0.39`；`npm test` 已通过，安全测试 `73/73`。

### v0.0.37 更新摘要

- **主工作区与监控页优化**：搜索、设置、MCP、知识库和智能体保持右侧主工作区展示；系统监控页改为明确区域布局，慢查询与异常告警完整显示并减少不必要的纵向滚动。
- **权限模型收口**：普通用户和非内置 `admin` 管理员可使用自己的 MCP、知识库、搜索、系统设置和智能体配置；内置用户名 `admin` 保留全局/共享/审计能力。
- **MCP、知识库、智能体治理增强**：新增 MCP 调用日志与治理概览、知识库质量报告、智能体任务预检，并记录智能体触发的 MCP 调用来源。
- **局域网数据库 MCP 适配**：数据库 MCP 默认允许个人连接局域网数据库；如需恢复旧限制，可设置 `MCP_RESTRICT_PRIVATE_DATABASE_HOSTS_TO_ADMIN=true`。
- **文档与版本同步**：版本升级至 `v0.0.37`；`npm test` 已通过，安全测试 `64/64`。

### v0.0.36 更新摘要

- **智能体模板展示优化**：优化了模板库中时间相关数据的可读性，解决了长日期截断问题。
- **系统设置界面精简**：移除了设置面板冗余的 Logo 和标题，取消了侧边栏分类标签，提升了视觉清爽度。
- **文档与版本同步**：版本升级至 `v0.0.36`；`npm run verify` 已通过。

### v0.0.35 重点能力

- **对话分支 / Fork**：可从任意消息分叉出新会话，复制该消息之前的上下文，适合方案推演、改写和审查分路线探索。
- **智能体结果版本管理**：沉淀结果支持版本、备注、对比和回滚，方便把智能体输出作为可维护的内部资产。
- **慢查询与异常告警**：记录慢 SQL、模型端点慢响应和 RAG 慢检索，系统监控页可配置 webhook 告警。
- **智能体 DAG 编排**：新增 DAG 运行模式，可用 JSON 定义依赖节点、条件跳过和并行工具调用。
- **插件化能力市场**：内置工具、MCP 服务和数据库连接会抽象为可启停能力包，普通对话和智能体会按能力包状态过滤可用工具。

### v0.0.34 重点能力

- **侧栏轻量化**：搜索入口改为与知识库、MCP、智能体一致的侧栏按钮，移除会话列表上方常驻搜索、归档和批量工具条。
- **会话搜索弹窗升级**：搜索弹窗支持标题、消息内容和 `#标签` 检索，当前/归档视图等高，批量标签操作迁移到弹窗内。
- **会话结果交互统一**：搜索结果标签显示在标题前方，标题与消息条数同一行，悬停显示三点菜单并复用主会话操作。
- **侧栏底部与工作区细节**：管理员标识、设置和退出恢复同排布局；知识库、MCP、智能体和系统设置保持主工作区展示与统一关闭按钮样式。

### v0.0.33 重点能力

- **局域网部署优化**：根路径页面复用预加载模板，智能体队列改为数据库锁抢占，支持多实例协同、锁超时恢复和运行状态统计。
- **监控与维护性增强**：修正 Prometheus 直方图累计 bucket 输出，统一 logger 导出，抽出智能体模型调用层和正整数环境变量解析工具，并复用会话消息查询 SQL。
- **实时事件推送**：新增 `/api/events` SSE 通道，智能体任务状态、步骤变化、审批和通知会主动推送；断线或受限内网环境会自动回退轮询。
- **附件在线预览**：聊天消息附件和附件库支持统一预览窗，图片、PDF、文本、Markdown、CSV、JSON 可直接查看。
- **会话标签治理**：支持标签汇总、多标签筛选、标签重命名/删除，以及会话批量加标、移除标签、替换标签；默认会话列表样式保持不变，点击“批量”后才进入选择模式。
- **设置页工作区化**：系统设置改为与知识库、MCP、智能体一致的右侧主区域视图，顶部提供“返回对话”，不再以遮罩弹窗占据页面。
- **合规审计包**：管理员可导出 `pivot_compliance_audit.zip`，集中包含会话清单、审计日志、模型用量明细和模型费用统计，适合局域网离线留存。
- **模型费用统计**：模型配置增加输入/输出百万 Token 单价与币种，用量页和导出报表同步展示估算成本。
- **安全与验证**：新增文本完整性检查并收紧前端 CSP；当前 `npm run verify` 已通过，安全测试通过 `64/64`。

## 近期主要升级（v0.0.43 → v0.0.111）
- **API 接入总开关与管理员控制收口（v0.0.111）**：管理员可在系统设置中统一启停第三方 API 接入；关闭后 API Key 登录、`/auth/keys` 创建和 OpenAI-compatible `/v1` 路由同步受限，前端会禁用创建入口并显示当前状态。
- **聊天首屏资源瘦身与 RAG 兼容修复（v0.0.110）**：首屏脚本清单精简并把工作区、管理后台和 ECharts 改为按需加载；补回 RAG 兼容导出并静默处理知识库专题/标签未授权访问；后台维护任务延迟到服务启动后再执行，同时补齐环境变量说明。
- **公文写作版式与布局深度优化（v0.0.109）**：顶部工具栏单行优先并改用“AI指令 / 全文AI / 面板”分组；全文 AI 与选区浮动工具明确分工；正文和对照模式按公文版式渲染标题、段落、行距、缩进、落款和日期；右侧审改栏与顶部入口去重；对照模式保留资料栏；大纲悬停展开完整内容且无水平滚动条；修复公文写作 AI 请求 404。
- **公文写作功能扩展（v0.0.108）**：多文档工作台（新建/打开/重命名/删除，每篇独立保存）；发文要素结构化表单（版头版记，导出自动排版）；AI 流式改写 + 逐句红绿对比接受；左侧历史/规范接入知识库检索并一键引用；导出扩展 Markdown / 带版式文本 / 红头 DOCX；校对检查（易错词、敏感词、标点规范、标题文种一致性）。
- **公文写作接入真实模型与可靠性增强（v0.0.107）**：公文写作工作台的起草/润色/局改/审校与选区工具改为调用模型补全接口，审校按 JSON 回填建议；规范库联动差异化检查；材料引用改为模糊匹配；本地存储容量保护与备份导出；DOCX 补 `docProps` 元数据与版式；新增撤销/重做与无障碍（ARIA 角色、方向键、`Esc`、焦点环）。
- **统计查询合并、错误收口与工程化基线（v0.0.106）**：运维统计端点多查询合并为单条标量子查询、模型主机解析并行化；DAG 拖拽节流与公告轮询清理；服务端 5xx 错误统一收口避免泄漏；数据库 MCP 标识符增加控制字符校验；ESLint 补齐低误报规则、新增 `engines` 声明与 GitHub Actions CI。
- **知识图谱索引、依赖镜像化与漏洞清零（v0.0.105）**：为无索引的知识图谱关系与提及表补齐用户、状态和实体维度索引，高频查询由全表扫描转为索引查找；Excel 解析依赖切换为 npm 官方镜像 `@e965/xlsx`，`uuid` 升级至 `^11` 使 `npm audit` 归零，并保留 CommonJS 兼容。
- **应用中心与公文写作工作台（v0.0.104）**：新增应用中心承载具体业务场景；公文写作工作台形成“顶部发起任务、中间正文编辑、左侧资料库、右侧审改结果”的布局，支持正文/对照模式、AI 起草润色局改审校、批注、版本对比、规范检查和材料引用。
- **角色与规范库、工具归属可见与旧库迁移兼容（v0.0.103）**：原“指令中心”升级为服务聊天、自由任务和工作流的角色与规范库；MCP 服务和工具展示所属用户/全局来源；旧数据库升级时会先补齐 `prompts` 新字段再建索引，避免启动失败。
- **第三方代码补全兼容与登录落点修复（v0.0.102）**：OpenAI-compatible 聊天补全接口兼容 `prompt`/`suffix` 风格的代码补全请求；工具策略只读卡片不再显示编辑按钮；登录表单成功后统一进入主页面，已有登录态刷新仍可恢复工作区。
- **知识库范围选择与专题库标签联动（v0.0.101）**：聊天输入区仅在启用知识库后显示范围选择；专题库作为一级范围，标签作为二级范围跟随过滤；知识库列表、召回测试、聊天提示和实际检索统一使用同一作用域，并补齐专题库/标签创建与整理弹窗选择闭环。
- **知识库专题库与标签范围检索（v0.0.100）**：知识库新增专题库/集合和文档多标签，上传、列表、召回调试和普通聊天都可按范围引用资料；RAG 召回缓存、关键词兜底和 Graph-RAG 查询同步尊重专题库/标签作用域，避免知识库增长后默认全库检索造成浪费和串域。
- **聊天输入状态与知识库兜底（v0.0.99）**：主页普通对话中的知识库/工具箱开启提示移动到按钮右侧并压缩文案；知识库在向量服务失败时保留关键词分片并回退 FTS/LIKE 检索；工具箱区分规划模型限流和工具执行失败，避免原始 429 错误打扰用户。
- **工具策略卡片化治理（v0.0.98）**：系统设置中的“工具策略”去除左侧工具包滚动列表，系统/全局工具直接按卡片平铺展示；每页显示 18 个工具，接入统一分页；编辑弹窗居中显示并只保留风险等级、审批要求和适用说明等治理字段。
- **工具箱命名与全局工具策略收口（v0.0.97）**：用户侧“能力库”统一调整为“工具箱”，工具卡片和工具弹窗减重；系统设置新增全局工具策略边界，只管理系统工具和全局工具服务，不接管用户个人连接；模型测试权限和模型软删除链路同步修复。
- **知识库工作台体验减重（v0.0.96）**：知识库顶部移除重复引导，上传文档改为弹窗化，概览与质量诊断指标去重，知识图谱顶部搜索、筛选和查询工具合并到同一行。
- **智能体任务记录与自由任务模板入口收口（v0.0.95）**：任务记录默认 15 条分页，DAG 详情去除重复执行明细，自由任务“保存为模板”入口前置到快速任务主面板。
- **工具级治理、数据库安全与配置诊断（v0.0.94）**：工具包支持单工具启停、风险等级和审批要求，数据库连接补齐表/字段白名单、敏感字段脱敏和查询安全提示，报表、IM 和外部工具服务支持配置诊断。
- **自由任务定位与工作流孵化闭环（v0.0.93）**：普通智能体入口收口为“自由任务 / 快速任务”，工作流入口强调编排、发布、版本、计划任务和审计复用；自由任务可一键生成工作流草稿，模板体系和任务记录按自由任务/工作流任务分层展示。
- **知识图谱质量治理与 Graph-RAG 查询升级（v0.0.92）**：知识图谱新增质量分、待确认关系、重复实体建议、状态/类型/可信度筛选、来源片段和查询调试；低可信关系需确认后才参与正式召回，智能体也可通过 `knowledge.graph.query` 查询关系路径。
- **知识库、能力库与智能体治理体验增强（v0.0.91）**：知识库新增召回缓存版本域和质量信号评分；能力库新增健康分、调用失败率和治理建议；智能体预检新增就绪度、输入 Token 估算和 MCP 风险提示。
- **知识库与能力库傻瓜式闭环升级（v0.0.90）**：知识库新增三步引导、拖拽上传、试问结果判断、来源可见和“打开聊天知识库”一键回聊天；能力库新增三步引导、动作示例、配置提示和中文动作名，普通聊天会保留知识库/能力库状态追踪；同时拆分大文件和安全测试并逐个检查中文编码。
- **工作流向导兼容与大模型结果展示优化（v0.0.89）**：统计图向导、数据库参数辅助和侧栏摘要统一传递当前工具列表，修复拆分后 `currentTools is not defined` 报错；大模型节点卡片和执行步骤默认展示 Markdown 正文，原始 JSON 收进折叠明细，运行结果更适合直接阅读。
- **工作流 LLM 节点编排约束与统计图模板补齐（v0.0.88）**：大模型节点新增输入来源门禁，起始 LLM 必须引用 `{{goal}}` / `{{inputs.*}}`，处理上游数据时必须连接依赖；统计图模板升级为“分组统计 → 大模型处理 → 生成统计图”，并补齐运行时、预检和回归测试兜底。
- **监控页资源说明细化（v0.0.87）**：系统监控页对 `.js` / `.css` 资源新增自动命名兜底，并补充新拆分脚本、样式入口与公共样式的资源说明，方便快速定位具体模块。
- **前后端模块化拆分与兼容修复（v0.0.86）**：聊天、管理端、DAG、工作流、RAG、统计与样式继续拆分为独立模块，加载顺序重新编排；同时修正 DAG 转义命名冲突、图谱平移、工具栏依赖注入和工作流 JSON 读取等拆分后兼容问题。
- **智能体任务记录体验精简（v0.0.85）**：任务详情移除时间轴模块，执行步骤标题左对齐；任务列表悬浮补齐完整任务上下文，并取消问号鼠标样式与原生标题提示。
- **文档与版本治理（v0.0.84）**：补齐 `v0.0.83` 历史记录，当前版本、package 元数据、README、项目汇报和运行时版本读取统一同步到 `v0.0.84`；安全回归 `160/160` 通过。
- **工作流编排与权限层级收口（v0.0.83）**：工作流页顶部、工具栏、发布/运行入口和已保存工作流区域整体重构；新增 `agent.llm` 大模型节点并将运行模型/最大步骤迁入节点配置；统一 admin/manager/user 权限层级，收紧私有模型可见性；数据库 MCP 在 DAG 中收敛为通用动作 + 数据源参数 + 运行时 `connectionId` 路由，并新增表/集合计数能力。
- **普通对话图表刷新持久化（v0.0.82）**：SSE 实时图表会同步写入 assistant 消息正文，刷新后仍能恢复渲染；后端和前端双重去重，避免自动图表重复显示。
- **普通对话图表确定性修复（v0.0.81）**：图表结果通过 SSE 直达前端，绕过模型文本生成链路，彻底解决”模型回复代码而非真正图表”的问题；扩展意图检测、强化规划器调度、中文思考约束全覆盖。
- **工作流运行与数据库统计闭环（v0.0.80）**：DAG 模式统一为”工作流”，新增运行工作流选择器、新建工作流弹窗、运行目标自动推断、运行输入可视化表单、DAG 运行状态拓扑图、统计图模板向导和 `db.group_count` 数据库 MCP 分组统计工具；同时修复 `{{nodes.xxx.output.rows}}` 结构化数据引用，补齐 `AGENT_DAG_NODE_CONCURRENCY` 配置说明和安全回归覆盖。
- **Agent 提示词中文化（v0.0.79）**：Agent 运行时核心系统提示词从英文迁移为中文，Schema `thought` 字段标注为「简短推理（中文）」，运行模式标签、上下文指导、流式提示、最终答案合成提示及兜底文案全面汉化，确保大模型思考过程与最终输出一致使用中文。
- **工作流编排 UX 全面优化（v0.0.78）**：经过体验评审的 10 项改进——节点智能推断默认工具、坐标持久化、键盘快捷键（Delete/Escape）、保存/发布前校验门禁、删除后一键撤销恢复、运行时输入可视化面板、DAG 运行结果迷你拓扑图、并发节点数环境变量可配等。
- **工作流图表工具去重（v0.0.77）**：修复智能体工作流「选择工具」下拉中图表与展示分类下工具重复显示；工具目录合并内置与 MCP 缓存工具时按裸工具名去重。
- **私网出口与代理信任加固（v0.0.76）**：普通用户的个人 MCP、IM Webhook 和数据库 MCP 私网目标默认受限；`TRUSTED_PROXY_IPS` 控制 `X-Forwarded-For` 可信来源；HTTP 出口统一安全 lookup Agent 闭合 DNS rebinding/TOCTOU 风险；DOMPurify 缺失时 HTML 清洗退回文本转义，并补齐对应安全回归测试。
- **安全复查修复与依赖加固（v0.0.75）**：记忆压缩出站补齐运行时 SSRF 守卫，修复 MongoDB/MySQL 连接 TLS 校验（SSL 静默明文、自签名开关丢弃），用户上传工作簿解析加单表行数上限；`xlsx` 升级至官方修复版 `0.20.3` 消除原型污染与 ReDoS；智能体任务过滤防抖；并以并行子代理多维度复查确认无回归。
- **安全加固与工程重构（v0.0.74）**：外部 MCP/IM Webhook 出站 SSRF 防护（含 DNS rebinding 阻断）、数据库连接默认校验 TLS 证书、访问日志敏感参数脱敏；聊天热路径同步文件读改异步、监控指标内存增长收敛、GPU 监控定时器 unref；`agent-runtime.js` 与 `/chat` 处理器及前端 `rag.js` 按领域拆分；HTML 转义统一到 `PivotSafeHtml`、裸 `fetch` 收敛到 `apiFetch`；非破坏性修复 6 项依赖漏洞。
- **知识图谱交互体验（v0.0.73）**：关系地图支持中文图谱化关系、拖拽平移、滚轮缩放、节点与关系明细完整信息提示、校准弹窗、紧凑关系明细、顶部标题对齐，以及统计标签与搜索筛选同一行显示；系统监控慢查询与异常告警区域补齐内部滚动。
- **公告中心与登录页公告（v0.0.72）**：新增登录页公开公告展示、左侧独立登录页公告面板、主页品牌区公告入口、公告管理“登录页”独立列，并收紧“不再提示”当前用户隐藏语义。
- **系统公告基础能力（v0.0.71）**：新增系统公告数据模型、公告 API、用户阅读/确认状态、站内公告横幅、公告中心、强制确认弹窗和后台公告管理页，支持全员、单位、角色和指定用户投放。
- **部署构建与本地模型端点识别（v0.0.70）**：Compose 配置补齐镜像构建入口并移除废弃 `version` 字段；系统监控可将同服务器 Docker 服务名模型端点识别为本地，并修复 user-role RAG 上下文在预算裁剪中被误当普通历史的问题。

- **在线更新程序移除（v0.0.69）**：彻底移除系统设置在线更新页、updater 后端接口、自动轮询服务、sidecar 程序、Compose profile、Docker socket 挂载和相关环境变量说明，部署面更收敛。
- **设置页滚动与 lint 修复（v0.0.69）**：设置页缩放画布重新测量前清理旧高度并加入像素容差，减少短页面误出现滚动条；前端浏览器全局调用统一显式挂到 `window`，`npm run lint` 已通过。
- **知识图谱与 Graph-RAG（v0.0.68）**：知识库新增实体、提及、关系图谱索引，召回链路融合图谱邻居关系；知识图谱工作台支持全屏查看、实体搜索筛选、关系治理、实体合并、文档图谱重建，并在刷新后保持图谱页面。
- **管理界面与弹窗适配（v0.0.68）**：设置页在主工作区内使用比例缩放画布保持宽屏管理布局，模型配置弹窗固定底部操作栏并让内容区独立滚动，附件预览、监控热点接口、审计表格和会话搜索结果补齐小窗口滚动边界。
- **聊天元数据实时补齐（v0.0.67）**：失败消息收到 `assistant_error` 后立即刷新耗时、Token 和速率，并把统计写回后端，避免刷新前后显示不一致。
- **Markdown 与流式输出体验修复（v0.0.64）**：聊天消息会自动去掉整段 `markdown` / `text/plain` 外层围栏，修复普通列表 bullet、代码块/表格局部滚动和 prose 内容换行；后端兼容非标准流式 JSON，前端对单次大块增量做平滑拆分，减少回答一次性跳出的情况。
- **上下文压缩阈值可配置（v0.0.63）**：上下文压缩阈值改为管理员可在模型配置页调整的运行时设置，支持 `12K`、`1M` 等 Token 表达，`.env` 中的 `MEMORY_THRESHOLD` 仅作为首次初始化默认值；设置入口收紧到“添加模型”按钮旁边，减少模型管理页空间占用。
- **管理员自愈与 Windows 兼容运行（v0.0.62）**：新增内置 `admin` 账号在系统启动与数据预置时的多维度自愈修复机制，自动检查其角色、启用与软删除状态并在异常时触发全自动修复还原；将 `better-sqlite3` 核心依赖升级到 `^11.3.0`，原生适配 Windows 与 Node.js v24.x 运行，直接拉取预编译包免除本地 node-gyp 编译失败障碍；补齐账号自愈专项安全回归测试。
- **智能体工作流编排完整化（v0.0.61）**：工作流库、发布版本、版本差异、历史回滚、计划任务引用发布版、DAG 模板恢复画布、运行前空工作流校验、节点抽屉、JSON 弹窗、工具参数模板、变量插入和失败节点单独重跑形成闭环；节点工具选择器改为中文名称、分类分组和描述提示，减少直接暴露 `db.*`、`report.*` 等内部工具 ID。
- **长会话压缩与刷新状态恢复（v0.0.60）**：自动压缩会在当前请求前完成并重新组装上下文，长期摘要排在近期消息之前；刷新后会恢复当前主工作区、聊天会话、设置子 tab 与打印/导出预览，避免从设置、知识库、工具箱或智能体页面刷新后回到聊天首页；会话消息复制按钮补齐剪贴板失败回退，重新生成会复用最近用户问题进行知识库检索。
- **知识库会话遵循增强（v0.0.59）**：普通对话启用知识库且召回命中后，RAG 上下文会紧贴当前问题注入，并带有优先依据知识库、冲突时以知识库为准、资料不足时明确说明的规则；前端同步展示命中/未命中状态，监控页区分召回命中率与缓存命中率，便于排查知识库是否真正参与回答。
- **附件上传与图片消息同步（v0.0.58）**：发送图片或文件时，前端立即保留并渲染用户消息附件快照；后端强制校验上传会话归属，附件 URL 统一编码并兼容历史未编码链接，减少刷新后才显示、附件串会话和模型侧无法解析图片的情况。

- **聊天与上下文治理（v0.0.56）**：修复流式回复后用户消息需要刷新才显示、代码块生成时停留在顶部、上下文圆环用量不刷新等问题；补齐手动压缩入口和上下文查询/压缩接口，自动压缩改为超过 `MEMORY_THRESHOLD` 后按 token 压力触发，不再等待活跃消息超过 10 条。

- **智能体模型路由（v0.0.46 / v0.0.50）**：6 种策略 `fixed / auto-vision / auto-context / auto-cost / auto-load / auto-escalate`，可在智能体模板与单次任务里选择；`auto-escalate` 会先用最便宜模型尝试，启发式判定低置信后自动升级到更强模型重新合成。
- **DAG 可视化编辑器（v0.0.47 / v0.0.51）**：原生 SVG + 拖拽，节点可拖动、出端口拖到入端口即创建依赖；工具栏提供添加节点 / 自动布局 / 适配画布 / 从 JSON 同步；详情面板编辑标题、工具、条件、输入参数与依赖；滚轮缩放、空白平移与右下角小地图便于浏览大型流程；JSON 视图折叠保留为专家模式入口。
- **流式 function calling（v0.0.48 / v0.0.49）**：基础设施层新增 `streaming-tools.js` 累加器和 `callModelStreamingWithTools` API，按 OpenAI tools 协议增量解析多工具调用；agent-runtime 可通过 `AGENT_STREAMING_TOOLS=true` 启用流式分支，**默认关闭以兼容现有任务**，任何流式失败自动回退到旧回合制 JSON 协议，并在 agent_steps 留下 control 步骤记录。
- **RAG 调试可视化与会话 PDF 导出（v0.0.45）**：召回测试弹窗新增关键词高亮、得分进度条、按源文件聚合的命中汇总、检索耗时；新增 `/api/sessions/:id/print` 打印友好视图，浏览器内 Ctrl/Cmd+P 即可导出 PDF。
- **基础设施收口（v0.0.43）**：新增 `LruCache` / `TtlCache` 替换无上限 Map；统一 `redactSecrets` 在 MCP 调用日志、审计导出前自动屏蔽 `api_key`、`Bearer`、`sk-*`、JWT；后台启动失败 catch 改为带日志告警；`compressMemory` 接入超时与会话级去重；移除智能体审批 `all_mcp_approved` 短路死分支；`agent-runtime.js` 拆出 `agent-validators.js` 便于继续维护。
- **前端体验（v0.0.44）**：新增 `window.Pivot` 全局工具命名空间（节流/防抖/LruCache/格式化）；聊天流式渲染按累计内容长度阶梯式自适应（80→320ms），长回答的 marked.parse 重排开销显著下降。

## 近期基础能力

- 内置应用级 SQLite 热备份：维护服务默认每 24 小时调用 `db.backup()` 生成东八区毫秒级时间戳备份到 `data/backups/`，并按保留天数和版本数量滚动清理。
- 后台维护闭环增强：软删除附件、知识库源文件、知识库分块、FTS 索引和历史消息会在保留期后被物理清理，并配套执行 `PRAGMA optimize` 与增量 vacuum。
- 第三方 API 网关补齐向量转发：持有 Pivot API Key 的客户端可通过 OpenAI-compatible `/v1/embeddings` 调用当前用户可用的 RAG 向量模型。
- 知识库中文文件名修复：上传入口统一规范化文件名，保留正常中文并修复 latin1 mojibake，避免知识库列表中文档名称显示乱码。
- 前端 CSP 治理推进：`client/chat` 清除内联事件，模型、提示词、附件、API Key、会话菜单和工作台操作均采用事件委托；DAG 编辑器、会话打印视图等新增页面均通过 `res.locals.cspNonce` 注入脚本。
- 会话列表采用游标分页，减少创建、置顶或更新会话时滚动列表出现重复或跳页。
- PWA 更新机制收紧：Service Worker 只缓存稳定 vendor 资源，业务页面、脚本、API 与版本清单交回服务端和浏览器处理。
- 用户删除聊天消息、会话、附件、知识库文档和任务记录时采用软删除，保留审计日志、用量统计、用量明细和资产元数据。
- 第三方 API Key 调用新增请求与响应留痕，仅内置 `admin` 超级管理员可在 API 接入管理页查看和检索。
- 管理端数据表、Token 单位、模型配置布局、审计报表时间筛选和 API Key 输入/输出 Token 统计持续统一。
- 通用缓存与超时基础设施（v0.0.43）：`server/cache.js` 的 `LruCache` / `TtlCache` 支持容量与 TTL 上限，`dirSizeCache` 等关键缓存已切换避免长期运行内存膨胀；`server/services/concurrency.js` 新增 `withTimeout` / `KeyedConcurrencyGuard` 用于后台异步任务保护，`compressMemory` 后台压缩接入超时与会话级去重。
- 敏感信息脱敏（v0.0.43）：`server/security.js` 的 `redactSecrets` / `maskSecretString` 自动屏蔽 `api_key`、`Bearer`、`sk-*`、JWT 等敏感字段；MCP 调用日志的 input/output 写入审计前统一脱敏。
- 前端 Pivot 命名空间（v0.0.44）：`client/chat/pivot-core.js` 集中提供节流、防抖、`rafThrottle`、LruCache、formatBytes / formatNumber、chooseStreamInterval 等小工具，新代码可统一从 `window.Pivot` 调用。

## 核心特性

### 1. 对话与多模型

- **多模型接入**：支持 OpenAI-compatible Chat Completions / Responses 风格的上游模型接入，可配置 Base URL、API Key、模型 ID、温度、最大输出 Token、每日额度和端点并发。
- **第三方 API 流式输出**：对外的 OpenAI-compatible `/v1/chat/completions` 与 `/v1/completions` 支持流式，调用方在请求体携带 `stream: true` 即按 SSE（`text/event-stream`）真流式逐块返回，与标准 OpenAI 客户端/SDK 用法一致；不带或 `stream: false` 时返回一次性 JSON。示例：`curl -N -H "Authorization: Bearer <API_KEY>" -H "Content-Type: application/json" -d '{"model":"<模型ID>","stream":true,"messages":[{"role":"user","content":"你好"}]}' http://<host>/v1/chat/completions`。
- **模型可见性**：支持全局模型、部门可见模型和用户私有模型；用户私有模型只对所有者可见，自主任务、工作流和计划任务不会越权使用其他用户私有模型。
- **模型能力标识**：模型配置、模型列表、聊天下拉和自主任务模型选择器展示文本、视觉、推理等能力图标。
- **模型探测**：支持一键获取上游服务模型列表，减少手工配置成本。
- **模型端点保护**：支持端点并发上限、健康检查 URL、排队与熔断保护；用户排队时会看到前方等待数、当前生成占用数和最长等待时间。
- **Token 统计**：网页聊天、自主任务、工作流、RAG embedding 和第三方 API 调用统一计入模型每日额度、后台报表和 Prometheus 指标。
- **模型路由策略**：自主任务支持 6 种路由策略（`fixed` / `auto-vision` / `auto-context` / `auto-cost` / `auto-load` / `auto-escalate`），可在任务启动或模板中选择；命中替换时会写入 `agent_runs.chosen_model_id` 并在步骤记录中标注路由理由，路由失败安全回退到原模型。

### 2. 自动化任务与流程

- **统一任务运行**：从左侧“自动化”进入“任务运行”标签，集中展示自主任务、工作流任务和计划运行记录；需要发起探索性工作时点击“新建任务”。
- **目标拆解**：自主任务可把复杂目标拆解为多步骤任务，按需调用知识库、会话检索、最近会话、模型列表、系统健康和工具库工具。
- **运行模式**：自主任务支持标准、深度和审查模式，规划提示会根据模式调整检索深度、证据约束和风险表达。
- **工具范围控制**：支持仅内置工具、内置 + 工具库、工具白名单、工具包启停和管理员工具隔离。
- **MCP 审批**：高风险 MCP 调用可挂起等待用户批准或拒绝，数据库只读 MCP 可按策略安全执行；服务端只承认通过 `approveAgentTool` 写入的 `approvedTools` 白名单，杜绝路由层意外绕过审批策略（v0.0.43 收口）。
- **预算与重试**：支持 Token 预算、失败重试、任务运行超时、单次工具调用超时和运行心跳检测。
- **任务审计**：任务详情保留目标、类型、模型、步骤、工具输入输出、错误信息、Token 用量、耗时和最终结果；MCP 调用日志的 input/output 写入审计前自动脱敏。
- **自动化资产中心**：左侧“自动化”通过顶部标签统一承载任务运行、工作流与计划任务，可搜索、查看状态、运行或进入编辑器；工作流画布只在新建或编辑时打开。
- **工作流编排**：工作流编排支持可视化定义节点依赖、条件执行、并行工具调用和聚合节点；画布支持拖拽节点、端口连线、自动布局、适配画布、滚轮缩放、空白平移、小地图、草稿保存、工作流库、发布、版本对比和历史回滚，JSON 高级视图保留为弹窗入口。
- **任务孵化**：自主任务详情可生成工作流草稿，把一次性探索中的工具步骤转成可检查、可保存、可发布的流程资产。
- **节点工具选择**：节点工具选择器按知识与会话、数据库、报表与文件、图表与展示、数据处理、文档处理、格式转换、消息通知、系统诊断和外部能力分组，显示中文名称、用途说明、来源标签、审批提示和内部工具 ID。
- **流式 function calling（可选）**：v0.0.48 / v0.0.49 在 `runAgent` 引入按 OpenAI tools 协议的流式分支，通过环境变量 `AGENT_STREAMING_TOOLS=true` 启用；任何流式失败自动回退到旧回合制 JSON 协议，DAG 任务永远走原图调度。
- **模板库**：自主任务模板保存常用目标、运行参数、上下文来源和模型路由；工作流模板复用步骤、节点和依赖结构。
- **计划任务**：自主任务支持临时计划；企业生产周期任务建议使用已发布工作流承接每日、每周和手动计划。
- **通知中心**：任务完成、失败、停止、等待审批、计划入队和结果沉淀均会生成用户级通知。
- **结果沉淀**：任务最终答案或错误摘要可保存为结果资产，并支持版本备注、差异对比和回滚。
- **断点续跑**：失败或中断任务可基于上一轮状态、错误和最近步骤继续执行，避免只能从头重跑。

### 3. 工具库与 MCP 工具接入

用户侧统一使用“工具库”命名，底层继续兼容 MCP 协议。聊天、自主任务和工作流是否调用工具库，由对应任务的工具开关或工具范围控制决定；工具卡片本身不再提供重复的“带入聊天”动作。

- **外部 MCP Server**：支持配置个人或全局 MCP Server，保存服务后可刷新工具缓存，并供普通对话、自主任务和工作流调用。
- **数据库 MCP Server**：内置数据库 MCP 预设，通过可视化表单填写类型、主机、端口、数据库名、用户名、密码、Schema、SSL 和最大返回行数。
- **局域网数据库连接**：默认允许用户连接自己的局域网数据库；如需恢复旧限制，可在服务端环境变量中设置 `MCP_RESTRICT_PRIVATE_DATABASE_HOSTS_TO_ADMIN=true`。
- **报表与数据文件 MCP**：可配置局域网目录或共享目录，只负责列出、摘要读取、抽样查询和对比 CSV、Excel、JSON、Markdown 等报表/数据文件。
- **可视化图表 MCP**：从上一步返回的数据行生成 `pivot-chart` 或 `pivot-table` 结构化展示块，用于普通对话和报告结果中的图表、表格展示。
- **报告编排 MCP**：接收摘要、指标、表格和图表块，按固定章节模板组装 Markdown 报告，适合数据库表分析、月报、巡检报告等固定格式输出。
- **局域网消息通知 MCP**：对接内网即时聊天工具的 Webhook/API，按白名单用户或群组发送文本/Markdown 通知，不依赖微信、钉钉等外部平台。
- **组合式流程**：数据库、报表文件、可视化、报告编排和消息通知彼此独立，未来可按业务需要自由组合，而不是把完整流程固化在一个 MCP server 中。
- **统一数据库入口**：数据库类型作为连接字段选择，不再按数据库种类拆成多个工具入口。
- **支持数据库类型**：PostgreSQL、MySQL/MariaDB、SQL Server、SQLite 和 MongoDB。
- **只读安全边界**：SQL 数据库提供表列表、结构查看和只读查询；MongoDB 提供集合列表、样本读取和聚合读取。
- **SQL 限制**：只允许单语句读取类 SQL，例如 `SELECT`、`WITH`、`SHOW`、`DESCRIBE`、`DESC`、`EXPLAIN`，写入、DDL 和管理类关键字会被拦截。
- **SQLite 路径约束**：SQLite 文件路径限制在允许目录内，并以只读方式打开。
- **连接测试**：新增测试连接按钮，保存或编辑前可确认数据库连接是否可用。
- **工具缓存**：刷新服务后展示已缓存可调用工具，工具名称和说明尽量使用中文语义。
- **工具包市场**：内置工具、外部 MCP Server 和数据库 MCP 连接会同步为工具包，可按用户权限启停，并影响普通对话、自主任务工具列表和 MCP 工具调用。
- **工具策略**：系统设置中的“工具策略”只管理系统工具和全局工具服务；页面按工具卡片平铺展示，每页 18 个工具，卡片启停开关用于控制工具可用状态。
- **治理权限边界**：普通管理员可以查看工具策略但卡片不显示“编辑”入口；内置系统管理员可以修改全局工具启停、风险等级和审批要求，并通过卡片“编辑”打开居中的治理弹窗。个人工具仍按所有者权限、数据库只读限制、审批策略和运行时安全检查生效。

### 4. 应用中心业务工作台

- **公文写作**：支持多文档公文起草、润色、局部改写、审校、批注、版本对比和多格式导出。
- **数据分析**：支持表格数据导入、字段画像、查询、透视、图表、数据比对、智能分析和结果导出。
- **法规查询**：支持共享法规制度库、法规文档上传、版本管理、条文级检索、文档下载、归档治理和基于命中条文的 AI 问答。
- **应用恢复**：刷新页面后会尽量恢复到当前应用工作区，减少长流程操作中断。
## 插件化能力市场使用说明

当前能力市场是内置工具包管理，不是联网下载插件商店。系统会自动把以下能力同步成可管理包：

- 内置自主任务工具：知识库检索、会话检索、最近会话、知识库列表、模型列表、系统健康、模型运行态等。
- 已配置并可见的外部 MCP Server。
- 数据库 MCP 连接，例如 PostgreSQL、MySQL/MariaDB、SQL Server、SQLite 和 MongoDB。

使用方式：

1. 进入左侧 **自动化**，切换到 **任务运行**，点击 **新建任务**。
2. 展开 **能力与结果**。
3. 在 **能力清单** 上方查看能力包，并切换启用或停用。
4. 停用后，该能力会从自主任务可用工具、普通对话 MCP 工具候选和 `/api/mcp/tools/call` 调用中被过滤或拒绝。
5. 新增或刷新 MCP Server、数据库连接后，重新打开或刷新任务中心即可同步看到对应工具包。

权限规则：

- 全局内置工具包只有内置 `admin` 可以启停。
- 用户自己的 MCP Server 和数据库连接工具包可由拥有者启停。
- 管理员专用内置能力普通用户不可见。

相关接口：

- `GET /api/capabilities/packages`：查看当前用户可见能力包。
- `PUT /api/capabilities/packages/:key`：启用或停用能力包，请求体示例为 `{ "enabled": false }`。

### 4. RAG 知识库与中文检索

- **正式工作台**：知识库作为正式功能接入左侧工具入口，整合文档列表、RAG 配置、召回测试、删除审计、批量重建、批量删除、失败重试和上传文档。
- **专题库与标签范围**：知识库支持专题库/集合分组和文档多标签，上传、列表筛选、召回测试和聊天引用均可按范围缩小候选资料；专题库和标签需要先创建后使用，选择专题库后标签会联动过滤，默认仍可使用全部知识库。
- **普通对话接入**：聊天输入区提供知识库开关，用户可按本轮问题决定是否检索私有文档。
- **智能体接入**：智能体可按目标调用知识库检索、知识库文档列表和相关上下文。
- **索引状态**：文档记录状态、分块数、已索引分块、进度百分比、错误信息和处理时间；服务重启后会恢复处理中任务。
- **召回测试**：可临时调整相似度阈值、Top K 和候选数量，展示命中文档、分块、相似度分数和命中状态；测试期间按钮禁用并显示加载状态。
- **召回可视化（v0.0.45）**：调试弹窗新增关键词高亮、按相对最高分占比的得分进度条、按源文件聚合的"命中数 / 峰值 / 均值"汇总条以及 `/api/rag/debug-query` 返回的检索耗时，便于普通用户和管理员快速排查检索质量。
- **反馈闭环**：召回结果可提交"有用/无用"反馈，便于分析低质量文档和低命中查询。
- **中文 ngram**：索引时生成中文 1-3 gram token，改善 `unicode61` 对中文短词、单字和词组召回不足的问题。
- **候选预过滤**：`knowledge_chunks_fts` 通过触发器同步分片检索内容，减少向量相似度计算候选数量。
- **结果缓存**：RAG 检索结果支持 TTL 缓存，同一用户重复问题可减少 embedding 调用和数据库排序开销。
- **Embedding 配置**：管理员可维护系统默认向量配置，普通用户可维护个人配置；云端、本机和局域网向量服务均通过 HTTP 接入。
- **知识图谱抽取定位**：当前实体和关系由规则、模式匹配和启发式质量信号生成，适合作为 Graph-RAG 辅助线索；正式知识问答前建议确认低可信关系、合并重复实体，并结合来源文档校验。
- **兼容端点**：支持 OpenAI compatible `/v1/embeddings` 以及 Ollama 常用 `/api/embed`、`/api/embeddings`。

### 5. 效率工具集

- **FTS5 全文搜索系统**：集成 SQLite FTS5 毫秒级检索引擎，支持海量历史消息的关键词搜索，并通过触发器同步索引。
- **角色与规范库**：沉淀常用角色、输出规范、任务方法和工作流节点提示，统一服务聊天、自主任务与工作流编排。
- **会话管理**：支持多轮会话、置顶、归档、多标签、搜索、导出 Markdown、打印 / 导出 PDF（v0.0.45 新增 `/api/sessions/:id/print` 视图，浏览器内 Ctrl/Cmd+P 即可生成 PDF）和软删除审计。
- **重新回答**：支持一键丢弃错误输出，并结合最新上下文重新生成回答。
- **多模态附件**：聊天会话支持上传图片、PDF、Word、Excel、TXT、Markdown、CSV 等文件，并尽量在上传阶段抽取可读文本。
- **PWA 应用**：支持作为独立应用安装到桌面或移动端，稳定 vendor 资源可缓存，业务页面保持及时更新。

### 6. 系统运营监控

- **实时运营面板**：展示系统活跃度、用量趋势、模型调用、Token 消耗和核心资源状态。
- **健康检查**：`/api/health` 返回不包含路径的轻量存活状态；登录后访问 `/api/health/details` 可获取缓存的数据库、目录、内存和磁盘诊断。
- **维护状态**：系统监控面板展示最近维护任务成功时间、处理条数和错误信息。
- **RAG 指标**：暴露 RAG 检索次数、缓存命中、候选数量、命中数量、Top Score 和检索/入库耗时。
- **Prometheus 指标**：便于接入企业监控体系；默认要求 `Authorization: Bearer <METRICS_TOKEN>`，未配置令牌时接口保持关闭，完全可信的隔离局域网可通过显式开关允许匿名采集。
- **GPU 监控**：支持读取 Pivot 所在服务器的 GPU 使用情况；远端模型 GPU 负载需通过远端健康接口或模型端点监控补充。
- **端点状态**：展示模型端点健康探针、并发、排队、失败次数和熔断状态。

### 7. 安全、存储与审计

- **超级管理员边界**：只有内置用户名 `admin` 是超级管理员；普通管理员不能创建全局模型、全局 Prompt、全局 MCP 服务，也不能管理其他管理员账号。
- **资产确权隔离**：上传文件按用户和会话路径隔离存储，附件访问令牌绑定用户、会话和文件路径，并带过期时间。
- **软删除治理**：聊天、附件、知识库文档和智能体任务均采用软删除策略；内置 `admin` 可查看删除审计入口。
- **全链路审计日志**：记录敏感操作、IP 地址、用户、对象和操作详情，支持筛选与导出。
- **第三方 API 留痕**：API Key 调用记录请求内容、响应内容、输入 Token、输出 Token、状态和模型。
- **CSP 与 XSS 防护**：Helmet 与 CSP 配置深度加固，前端尽量采用事件委托减少内联脚本。
- **SSRF 防护**：出站请求默认拦截本机、内网、云元数据地址和受限主机别名，可通过配置显式允许本地模型主机。
- **文档解析安全上限**：DOCX/XLSX ZIP 解析内置 entry 数量、单文件解压大小和总解压大小限制，降低压缩炸弹风险。
- **日志脱敏**：生产日志自动切换高性能 JSON 模式，并对 API Key、密码等敏感字段脱敏。
- **灾备方案**：内置数据库字段迁移逻辑与定时快照备份脚本。

## 多模态与附件上传

聊天会话支持直接多选上传图片和文档，当前上传队列最多保留 5 个附件。系统会尽量在上传阶段完成文档文本抽取，并把可读内容随用户问题一并发送给模型。

### 支持格式

- 图片：`png`、`jpg`、`jpeg`、`gif`、`webp`、`bmp`
- 文档：`pdf`、`txt`、`md`、`csv`、`doc`、`docx`、`xls`、`xlsx`

### 处理策略

- PDF 优先抽取文本；扫描 PDF 无文本时会尝试渲染页面为图片交给视觉模型。
- Word 和 Excel 通过 `word-extractor` 与 `xlsx` 解析，兼容新旧 Office 格式。
- 加密文件需要提供密码；无法解密时会在上传阶段提示。
- 图片会被限制大小、像素和输出体积，避免超大图造成 OOM 风险。
- 当前多模态模型按“每次最多 1 张图片”发送；多余图片会被跳过并提示。
- 模型配置中可显式开启“视觉输入（图片/扫描件）”。未开启的模型收到图片或扫描件兜底图片时会提前提示该模型不具备视觉能力。

## 远端模型监控与控制

系统监控页中的本机 GPU 指标只代表 Pivot 所在服务器。如果大模型部署在其他服务器，本机 `nvidia-smi` 无法反映远端 GPU 负载。远端模型建议通过模型配置中的以下字段进行保护：

- **端点并发上限**：限制 Pivot 对同一个模型端点同时发起的请求数量。局域网多模态模型如果一次只可靠处理一张图片，建议设置为 `1`。
- **远端健康检查 URL**：填写远端模型服务提供的健康接口，例如 `http://192.168.1.20:8000/health`。

系统会在“系统监控 > 模型端点状态”中展示远端端点的健康探针、并发、排队、失败次数和熔断状态。模型配置中的端点并发填 `0` 时会使用 `MODEL_ENDPOINT_DEFAULT_CONCURRENCY`。连续失败时会短暂熔断，避免持续压垮远端模型服务。

如果大模型服务和 Pivot 部署在同一台服务器，但模型地址使用宿主机内网 IP、反向代理域名或容器访问域名，建议把这些本机别名加入 `PIVOT_LOCAL_MODEL_HOSTS` 或 `PIVOT_ADVERTISE_HOSTS`，例如：

```bash
PIVOT_LOCAL_MODEL_HOSTS=192.168.31.10,pivot.local,host.docker.internal
PIVOT_ADVERTISE_HOSTS=192.168.31.10,pivot.example.com
```

这样系统监控的“模型端点状态”会把这些端点标记为本地；“GPU 与并发保护”区域始终只代表 Pivot 部署服务器的本机 GPU，不展示远端模型列表。

## 部署指南

### 1. 镜像打包

在联网环境中构建镜像并导出：

```bash
docker build -t pivot:latest .
docker save -o pivot.tar pivot:latest
```

### 2. 离线部署

将 `pivot.tar` 和 `docker-compose.yml` 拷贝至目标服务器：

```bash
docker load -i pivot.tar
docker network create ai-bridge
docker compose up -d
```

### 3. 升级已有部署

如果目标服务器已经运行过旧版本，建议显式重建并重启容器，确保运行中的容器使用新镜像：

```bash
docker compose down
docker build -t pivot .
docker compose up -d
```

如果是离线包部署，需先在目标服务器加载新镜像，再重建容器：

```bash
docker load -i pivot.tar
docker compose down
docker compose up -d
```

本项目启动时会自动执行数据库迁移。升级后可用以下命令确认容器内代码和数据库字段是否已更新：

```bash
docker exec pivot node -e "const { db } = require('/app/server/db'); console.log(db.prepare('PRAGMA table_info(models)').all().map(c=>c.name))"
docker logs pivot --tail=200
```

如果字段没有出现，通常说明容器仍在运行旧镜像、没有重建容器，或 `/app/data` 挂载目录权限导致 SQLite 迁移失败。

## 快速启动

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境

复制模板并按需修改：

```bash
copy .env.example .env
```

Linux / macOS：

```bash
cp .env.example .env
```

`.env.example` 是配置模板，实际服务启动时读取 `.env`。生产部署修改 `.env` 后需要重启服务，新的登录有效期需要重新登录后才会体现在 Cookie 中。
开放注册的 `.env` 值只作为首次初始化默认值；系统运行后请由内置 `admin` 在“系统设置 > 用户管理”页面切换，设置会写入数据库并即时生效。
局域网部署时，通常可以保持 `PUBLIC_URL` 为空，`CORS_ORIGIN` 只填写实际会访问本系统的内网地址；如果是纯 HTTP 内网环境，`COOKIE_SECURE` 保持 `false` 即可。若模型服务、MCP 服务或反向代理使用内网主机名，把它们补到 `PIVOT_LOCAL_MODEL_HOSTS` 和 `MODEL_URL_ALLOWLIST` 里，避免被当成外网地址拦截。

无公网 IPv4/IPv6、无域名和无证书不影响隔离局域网部署，但客户端与服务器之间仍需存在私有 IP、IPv6 字面量或可解析局域网主机名中的至少一种可达地址。监控接入应配置 `METRICS_TOKEN`；未配置时 `/api/metrics` 默认关闭。只有完全可信且隔离的局域网才考虑设置 `METRICS_ALLOW_UNAUTHENTICATED_LAN=true`。

### 3. 启动服务

```bash
npm start
```

开发模式：

```bash
npm run dev
```

默认访问：

```text
http://localhost:3000
```

首次初始化会确保内置 `admin` 用户存在。生产环境请务必修改默认密码，并配置强随机 `JWT_SECRET`。

## 关键环境变量

```env
# 基础服务
PORT=3000
DATA_DIR=./data
JWT_SECRET=change-me

# 登录态有效期
ACCESS_TOKEN_EXPIRES_MINUTES=480
REFRESH_TOKEN_EXPIRES_DAYS=30

# 账号注册策略：首次初始化开放注册默认值，后续可在用户管理页面开关
ALLOW_PUBLIC_REGISTRATION=false

# 全局 AI 并发与排队
MAX_CONCURRENT_AI_REQUESTS=1
MAX_AI_QUEUE_SIZE=20
AI_QUEUE_TIMEOUT_MS=300000

# 模型端点默认并发上限（模型里填 0 时使用）
MODEL_ENDPOINT_DEFAULT_CONCURRENCY=1
MODEL_ENDPOINT_QUEUE_TIMEOUT_MS=300000

# 本地模型主机白名单
PIVOT_LOCAL_MODEL_HOSTS=localhost,127.0.0.1
PIVOT_TRUST_CONTAINER_LOCAL_HOSTS=false

# 智能体运行治理
AGENT_MAX_CONCURRENT_RUNS=2
AGENT_DAG_NODE_CONCURRENCY=4
AGENT_RUN_TIMEOUT_MS=900000
AGENT_TOOL_TIMEOUT_MS=120000
AGENT_STALE_RUNNING_MINUTES=30
# 是否启用流式 function calling 分支（v0.0.49）；失败自动回退到旧回合制 JSON
AGENT_STREAMING_TOOLS=false

# 后台记忆压缩（v0.0.43）
# 自动压缩触发阈值与保留策略（v0.0.56）
# 仅作为初始化默认值；后续可由管理员在系统设置中调整，保存后即时生效。
MEMORY_THRESHOLD=12000
MEMORY_MIN_MESSAGES_TO_COMPRESS=1
MEMORY_SUMMARY_KEEP_COUNT=6
# 超时（毫秒）与全局并发上限，会话级去重默认开启
MEMORY_COMPRESSION_TIMEOUT_MS=180000
MEMORY_COMPRESSION_MAX_CONCURRENT=2

# 数据库 MCP
MCP_SQLITE_ROOTS=
MCP_RESTRICT_PRIVATE_DATABASE_HOSTS_TO_ADMIN=false
MCP_DATABASE_CONNECT_TIMEOUT_MS=10000
MCP_DATABASE_TEST_TIMEOUT_MS=10000

# RAG 检索与缓存
EMBEDDING_MODE=http
EMBEDDING_API_URL=
EMBEDDING_API_KEY=
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_REQUEST_TIMEOUT_MS=30000
RAG_INDEX_EMBEDDING_TIMEOUT_MS=120000
RAG_CANDIDATE_LIMIT=300
RAG_CACHE_TTL_MS=300000
RAG_CACHE_MAX_ITEMS=500
RAG_SCORE_THRESHOLD=0.4

# 慢查询与异常告警
PIVOT_SLOW_SQL_MS=500
PIVOT_SLOW_MODEL_MS=30000
PIVOT_SLOW_RAG_MS=3000
PIVOT_ALERT_WEBHOOK_URL=
PIVOT_ALERT_WEBHOOK_TIMEOUT_MS=5000

# 后台维护
AUDIT_LOG_RETENTION_DAYS=180
API_CALL_LOG_RETENTION_DAYS=30
STORAGE_GC_RETENTION_DAYS=30
STORAGE_GC_BATCH_SIZE=100
SQLITE_INCREMENTAL_VACUUM_PAGES=200
DB_BACKUP_DIR=
DB_BACKUP_RETENTION_DAYS=7
DB_BACKUP_MAX_VERSIONS=7

# 本机 GPU 动态保护，仅代表 Pivot 所在服务器
GPU_MONITOR_INTERVAL_MS=15000
GPU_CONCURRENT_MIN=1
GPU_CONCURRENT_MAX=4
GPU_VRAM_REJECT_THRESHOLD=0.97
```

## 验证命令

```bash
npm run check
npm run lint
node tests/security.test.js
```

完整验证：

```bash
npm run verify
```

数据库初始化 smoke test：

```bash
node -e "require('./server/db'); console.log('db init ok')"
```

外部链路配置体检（默认只读 `.env` 和 SQLite 配置，不请求外部服务）：

```bash
npm run check:external
```

真实外部链路验收（会请求已配置的模型监控 URL、Embedding、外部 MCP 健康检查和数据库只读连通测试）：

```bash
npm run check:external -- --live
```

`npm run verify` 主要验证代码、接口和本地安全回归；模型补全、Embedding、外部 MCP、数据库和 IM Webhook 的真实端到端效果仍依赖 `.env`、数据库内配置和外部服务可用性。为避免误耗额度或误发生产群，模型实际补全、IM 发送和告警投递建议在目标环境中按业务账号手动验收。

## 数据库与迁移

项目启动时会自动执行 SQLite schema 初始化和迁移。历史数据库会自动补齐新增字段与索引，包括：

- 用户、消息、附件和会话的软删除与审计字段。
- 模型能力、端点并发、每日额度、输入 Token 上限和私有模型字段。
- 模型输入/输出百万 Token 单价和费用币种字段。
- 知识库文档状态、分块、FTS 索引、反馈和 RAG 配置字段。
- 法规文档、法规版本、法规条文、法规条文 FTS 索引和归档字段。
- MCP 服务、数据库 MCP 连接、工具缓存、刷新状态和能力包字段。
- 智能体任务治理字段、DAG 节点、模板、计划、通知、结果沉淀和结果版本表。
- 慢查询、慢模型响应、慢 RAG 检索和异常告警事件表。

迁移逻辑遵循幂等设计，重复启动不会重复创建字段。若旧数据库启动失败，优先检查日志中的缺失字段、迁移顺序和数据目录权限。

## 目录结构

- `server/`：后端核心程序，包含 Express 路由、SQLite schema、服务层、模型适配、RAG、MCP、智能体运行时和安全中间件。
  - `server/cache.js`：通用 `LruCache` / `TtlCache`（v0.0.43）。
  - `server/security.js`：URL 防 SSRF、文件路径校验、`redactSecrets` 脱敏（v0.0.43）。
  - `server/services/concurrency.js`：信号量、`withTimeout`、`KeyedConcurrencyGuard`（v0.0.43）。
  - `server/services/model-router.js`：模型路由 6 策略、`assessConfidence`、`pickEscalationModel`（v0.0.46 / v0.0.50）。
  - `server/services/streaming-tools.js`：OpenAI tools 流式累加器与辅助函数（v0.0.48）。
  - `server/services/regulations.js`：法规库文档、版本、条文解析、全文检索和 AI 上下文服务（v0.0.168）。
  - `server/services/agent-runtime.js` + `agent-validators.js`：智能体运行时与拆分出的常量/规范化（v0.0.43 拆分）。
- `client/`：前端静态资源，包含聊天页、管理端、工作台、PWA 和本地渲染资源。
  - `client/chat/pivot-core.js`：`window.Pivot` 全局工具命名空间（v0.0.44）。
  - `client/chat/agents-dag-editor.js`：智能体 DAG 可视化编辑器（v0.0.47 / v0.0.51 缩放与小地图）。
  - `client/chat/apps-workbench-regulations.js`：法规查询工作台，承载法规检索、详情、AI 问答和管理员维护入口（v0.0.168）。
  - `client/chat/safe-html.js`：HTML 转义与 DOMPurify 适配。
- `data/`：SQLite 数据库及默认备份目录。
- `uploads/`：用户附件隔离存储目录。
- `scripts/`：语法检查、数据库备份、模型下载等辅助脚本。
- `tests/`：安全、迁移、RAG、MCP、智能体、模型路由、流式工具累加器和系统边界测试。

## 版本记录

详细变更请查看 [CHANGELOG.md](CHANGELOG.md)。

**当前版本**：v0.0.242（修复旧数据库自动化迁移启动顺序，提高新建任务弹窗可用高度，并将使用帮助统一到“自动化 → 任务运行”的当前入口；详细变更请查看 [CHANGELOG.md](CHANGELOG.md)。）
