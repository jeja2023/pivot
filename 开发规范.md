# Pivot 开发规范

本规范适用于 Pivot 项目的所有后续开发、修复、重构、页面调整、接口扩展、文档处理能力建设和交付验收。任何新增功能都必须先阅读并遵守本文件；当需求、实现习惯或临时代码与本规范冲突时，以本规范为准。

## 1. 基本原则

1. 不破坏现有功能。修改前先理解现有调用链、数据结构、样式体系和权限边界。
2. 不引入重复体系。已有工具、服务、样式、组件、接口模式可以满足需求时，必须复用。
3. 不做无关重构。只修改本次需求相关文件，除非现有结构会直接阻碍正确实现。
4. 不提交临时代码。禁止遗留调试按钮、测试页面、未使用变量、无效注释、死代码和硬编码测试数据。
5. 不绕过安全边界。文件上传、路径处理、权限校验、模型调用、数据库写入、日志输出必须按现有安全策略执行。
6. 不让 UI 自成一套。所有页面必须使用全局统一样式、组件语义和交互模式。

## 2. 开发前流程

每次编码前必须完成以下检查：

1. 阅读本规范和 [DESIGN.md](docs/design/DESIGN.md)。
2. 用搜索确认相关模块、接口、样式、工具函数和既有实现。
3. 明确本次改动影响范围，包括前端页面、后端路由、数据库、文件上传、模型调用、权限、日志和测试。
4. 如果涉及 UI，先确认全局样式来源和同类页面写法。
5. 如果涉及文档处理、OCR、PDF、知识库或法规查询，先确认是否应复用统一文档处理底座。
6. 如果现有需求描述不完整，优先选择与项目现有模式一致的保守方案。

## 3. 代码组织与架构

1. 后端路由只负责请求解析、鉴权、参数校验和响应组装；业务逻辑应下沉到 `server/services/` 或已有对应服务模块。
2. 数据库结构变更必须同时考虑初始化、迁移、默认值、兼容旧数据和失败回滚。
3. 文件处理能力必须集中封装，禁止在多个业务路由里复制解析、转换、OCR、PDF 处理逻辑。
4. 通用能力优先放入清晰命名的共享模块，避免页面级脚本和路由文件膨胀。
5. 新增配置必须有默认值、说明、边界校验和缺失时的安全行为。
6. 新增依赖必须有明确理由，优先使用项目已有依赖；不得为了少量逻辑引入大型库。
7. 异步任务必须有状态、错误信息、重试或失败处理，不得只依赖前端等待。

### 3.1 文件拆分与大文件治理

1. 不按固定代码行数做机械限制，但必须避免出现职责混杂、持续膨胀、难以阅读和难以维护的大文件。
2. 当一个文件同时承担多个业务职责、多个页面区域、多个接口流程、多个状态模型或多个导出能力时，必须按功能边界拆分。
3. 后端优先按路由、服务、数据访问、任务处理、适配器、导出器拆分；路由文件不得承载大量业务规则。
4. 前端优先按工作区、页面区域、状态管理、事件绑定、渲染函数、弹窗、表格、工具栏拆分；页面脚本不得无限追加功能。
5. 样式优先按全局样式、工作区共享样式、应用模块样式拆分；禁止把多个无关页面样式堆入一个私有 CSS 文件。
6. 拆分后的模块必须命名清晰、职责单一、导入关系明确，不能为了拆分制造碎片化和循环依赖。
7. 修改已有大文件时，新增功能应优先落到新模块或已有合适模块；如必须在大文件中接线，只保留薄入口和组合逻辑。
8. 评审和验收时，以“是否容易定位、理解、测试和复用”为判断标准，不以单一行数作为唯一标准。

## 4. 接口与路由规范

1. 新接口必须保持统一响应结构，成功和失败格式应与相邻模块一致。
2. 所有输入参数必须校验类型、长度、枚举范围和必填项。
3. 文件路径必须使用服务端生成的受控路径，禁止信任前端传入的任意路径。
4. 上传接口必须限制文件类型、大小、数量和业务场景。
5. 批量接口必须有数量上限和清晰的部分失败结果。
6. 长耗时能力必须设计为任务接口，不得让普通请求长时间阻塞。
7. 错误信息应便于用户理解，但不得泄露密钥、绝对敏感路径、SQL、Token 或内部堆栈。

## 5. 前端 UI 统一规范

前端样式统一是硬性验收项。新增或修改页面时，必须复用全局样式体系，禁止为单个页面另起一套按钮、输入框、表格、弹窗、分页、主题色和字号体系。

### 5.1 全局样式来源

必须优先复用以下文件中的变量、类名和布局模式：

1. `client/common/styles/theme.css`
2. `client/chat/styles/workspaces/shared.css`
3. `client/chat/styles/admin/admin-tables-models.css`
4. 相关页面已经使用的共享脚本和渲染工具，例如分页渲染函数。

如确实需要新增通用样式，必须加入全局或共享样式文件，并保证不会破坏已有页面。

### 5.2 按钮规范

1. 主按钮必须使用 `.btn-primary`。
2. 次按钮必须使用 `.btn-secondary`。
3. 危险操作必须使用 `.btn-danger` 或项目已有危险按钮样式。
4. 图标按钮应使用现有图标体系和统一尺寸，不得手写一套相同语义的按钮样式。
5. 禁止新增页面私有的主色按钮、渐变按钮、特殊圆角按钮和临时 hover 颜色。

### 5.3 表单控件规范

1. 输入框、搜索框、下拉框、文本域必须使用 `.form-input` 或同体系全局类。
2. 表单字体、字号、边框、圆角、背景、placeholder、focus 状态必须与全局样式一致。
3. 文件选择按钮必须使用 `client/common/styles/theme.css` 中的 `::file-selector-button` 全局样式。
4. 表单错误、禁用、加载中状态必须清晰可见，并与现有页面一致。

### 5.4 字体与字号规范

1. 页面字体必须继承全局字体设置。
2. 正文字号、表格字号、按钮字号、弹窗字号必须沿用同类页面标准。
3. 禁止在局部页面随意设置大标题、超小字体、负字距或视口宽度缩放字体。
4. 文本必须在移动端和桌面端都不溢出、不遮挡、不挤压关键控件。

### 5.5 弹窗规范

1. 弹窗必须使用 `.modal-overlay`、`.workspace-modal`、`.workspace-modal-header`、`.workspace-modal-body` 等现有结构或同体系样式。
2. 弹窗标题、关闭按钮、底部操作区、滚动区域必须与现有工作区弹窗一致。
3. 禁止在页面里实现私有遮罩、私有弹窗动画和私有关闭交互。
4. 危险确认弹窗必须清晰说明影响范围，并使用统一危险按钮样式。

### 5.6 表格与分页规范

1. 数据表格必须使用 `.table-container`、`.data-table`、`.compact-table` 或已有工作区表格样式。
2. 表头、行高、边框、空状态、加载状态、操作列必须与同类页面一致。
3. 分页必须使用 `.workspace-pagination` 或已有统一分页渲染函数。
4. 禁止在单个页面自定义表格颜色、分页按钮、页码布局和操作列样式。
5. 应用中心任务列表（OCR、PDF、批量处理、导入类工具）必须参照法规查询的数据表格布局；表头、完整边框、单元格行高、状态徽标、操作列和 hover/选中状态保持一致。
6. 表格单元格只显示当前列核心信息，文件类型、大小、语言、引擎参数等辅助信息不得作为第二行常驻文本撑高行高；辅助信息放到 title、详情弹窗或查看页。
7. 操作列只放明确操作按钮，例如查看、下载、重试、取消；同一任务的结果查看和文件下载入口不得散落到表格外的临时区域。

### 5.7 颜色与主题规范

1. 颜色必须使用全局 CSS 变量，例如 `--primary`、`--text-main`、`--text-muted`、`--border`、`--surface`。
2. 禁止硬编码与主题冲突的主色、背景色、阴影和边框色。
3. 新增状态色必须复用已有语义，确实缺失时才扩展全局变量。
4. 页面必须支持现有主题，不得出现局部区域颜色体系割裂。

### 5.8 页面布局规范

1. 页面布局必须贴合应用型工具界面，优先清晰、稳定、高效，不做营销页式大图英雄区。
2. 工具类页面应提供明确的列表区、工作区、操作区、结果区和状态反馈。
3. 固定格式控件必须有稳定尺寸，避免 hover、加载、长文本导致布局跳动。
4. 禁止卡片套卡片，禁止用大量装饰性渐变、光斑、漂浮色块制造视觉噪音。
5. 任务型工具页主页面只保留创建/筛选区和任务表格；预览、识别文本、处理结果、输出清单和复核操作必须进入查看弹窗、详情页或独立结果页，禁止直接在主页面长区块堆叠。

## 6. 文档处理、OCR 与 PDF 专项规范

文档处理能力必须建设为共享底座，再由具体应用复用。OCR 识别应用、PDF 工具应用、知识库导入、法规导入、聊天附件解析不得各自实现一套解析链路。

### 6.1 能力边界

1. 普通文本型 PDF、Word、Excel、TXT 等文件优先走文本抽取。
2. 扫描 PDF、图片、无文本层 PDF 才进入 OCR 流程。
3. OCR 输出必须包含纯文本、页码、块、行、坐标、置信度、识别引擎、语言和处理参数。
4. PDF 工具必须区分结构化处理、视觉预览、文本抽取、OCR、导出结果，不能混成单个不可复用函数。
5. 知识库和法规查询使用 OCR 结果时，必须保留来源文件、页码和可追溯片段。

### 6.2 OCR 服务策略

1. Pivot 主项目只保留 HTTP OCR 客户端和统一协议，不内置 PaddleOCR、Tesseract 或具体模型运行时。
2. PP-OCR、PaddleOCR-VL、Tesseract、商业 OCR API 等具体引擎只在独立 OCR 服务项目内部选择和升级。
3. 业务层只能调用文档处理底座的统一 OCR 接口，不得直接绑定外部服务内部命令、SDK 或模型目录。
4. Pivot 侧配置只维护服务地址、健康检查超时、单页请求大小、任务超时、并发、页数限制和置信度阈值。
5. OCR 服务必须提供健康检查和识别接口，返回文本、坐标、置信度和语言，失败时返回清晰错误。

### 6.3 OCR 应用规范

1. OCR 应用应提供上传、任务列表、识别进度、原图预览、识别框叠加、文本编辑、低置信度标记、导出和发送到业务模块。
2. 原图或 PDF 版式展示应以原始页面预览为准，识别文本通过坐标叠加或旁侧编辑区呈现。
3. 可搜索 PDF 应通过原页面图像或原 PDF 外观加透明文本层实现，保证视觉上接近原件。
4. DOCX、Markdown、HTML 导出只能承诺尽量还原阅读顺序和结构，不得承诺复杂版面像素级一致。
5. 识别结果被人工修改后，必须保存修订状态和更新时间。
6. OCR 任务列表必须复用全局数据表格和分页；主页面不直接展开原图预览、识别文本、置信度明细和复核区，点击查看后进入弹窗或详情页。
7. OCR 表格列只展示任务核心信息（序号、文件、识别引擎、状态、进度、更新时间、操作），文件大小、语言、引擎参数等辅助信息进入悬浮提示或详情。

### 6.4 PDF 工具应用规范

1. PDF 工具应用应优先提供拆分、合并、旋转、删除页、重排、提取文本、PDF 转图片、图片转 PDF、生成可搜索 PDF。
2. 后续能力可扩展压缩、水印、页码、加密解密、表格提取和版面导出。
3. 每个处理任务必须生成独立输出文件，不得覆盖原文件。
4. 批量处理必须有任务状态、失败明细和可下载结果。
5. 涉及 OCR 的 PDF 工具必须调用 OCR 底座，不得重复识别实现。
6. PDF 工具任务列表必须复用全局数据表格和分页；主页面不直接铺开拆分、合并、转换、提取文本等处理结果清单。
7. PDF 处理输出必须提供下载入口；单输出可在表格操作列下载，多输出在查看弹窗或详情页中列出并下载。

### 6.5 知识库与法规查询集成规范

1. 文本型文件直接抽取并入库。
2. 空文本 PDF、扫描件、图片类材料必须提示或自动进入 OCR 流程。
3. 入库前必须能识别 OCR 低置信度内容，并允许用户复核或确认继续。
4. 检索结果必须能回溯到文件、页码、片段，必要时展示原页预览。
5. 法规类文档必须保留标题、条款层级、页码和来源信息，不得只保存无结构长文本。

## 7. 安全、日志与数据规范

1. 密钥、Token、连接串、用户隐私、上传文件内容不得写入普通日志。
2. 日志应记录操作结果、任务状态和必要错误摘要，避免输出完整大文本。
3. 上传文件必须校验扩展名、MIME、大小和业务场景。
4. 生成文件必须放在受控目录，下载必须通过受控接口。
5. 临时文件必须有清理策略。
6. 数据库写入必须使用参数化查询或现有安全封装。
7. 权限相关接口必须复用现有鉴权中间件和角色判断。

## 8. 注释、命名、提示与日志

1. 注释必须使用中文，且只解释复杂逻辑、边界条件和非显然决策，不描述显而易见的赋值。
2. 用户可见文案必须使用中文，包括按钮文字、占位提示、空状态、加载状态、成功提示、失败提示、确认弹窗和表单校验提示。
3. 业务日志必须使用中文描述操作结果、任务状态和错误摘要，禁止新增英文日志正文。
4. 错误提示必须使用中文，并说明用户下一步能做什么。
5. 代码命名必须表达业务含义，避免 `data2`、`newList`、`tempFn` 这类含糊名称；代码标识符、协议字段、第三方 API 名称、命令、路径、文件名、包名、CSS 类名可以保留必要英文。
6. 技术名词可以保留英文原名，但必须放在中文语境中，例如“调用 PaddleOCR 识别失败”，不得只写 `OCR failed`、`Loading...`、`Success`、`Error` 这类英文提示。
7. 调试信息、临时提示、TODO、FIXME、控制台输出不得使用英文凑合提交；确需保留的开发说明也必须写中文。
8. 中文文案必须保持 UTF-8 正常显示，禁止提交乱码。

## 9. 测试与验收

默认校验入口：

1. 通用检查：`npm run check`
2. 完整测试：`npm test`
3. 交付验收：`npm run verify`

按改动范围补充以下验证：

1. 后端接口变更必须验证成功、失败、权限不足、参数非法和边界数据。
2. 文件处理变更必须验证不同格式、空文件、大文件、异常文件和清理逻辑。
3. OCR/PDF 变更必须验证文本型 PDF、扫描 PDF、图片、低置信度、失败重试和导出文件。
4. UI 变更必须验证桌面端和移动端布局，确保无重叠、无溢出、无私有样式体系。
5. 知识库和法规查询变更必须验证导入、索引、检索、来源回溯和失败提示。

如果因环境限制无法运行某项检查，必须在交付说明中明确说明未运行原因和剩余风险。

## 10. 自动化检查与提交门禁

1. 项目必须提供开发规范自动检查脚本，当前入口为 `npm run check:standards`。
2. `npm run check` 必须包含 `npm run check:standards`，确保常规检查会覆盖开发规范门禁。
3. 项目必须提供 Git 提交门禁，当前仓库门禁文件为 `.githooks/pre-commit`。
4. 本地首次启用提交门禁时，必须运行 `npm run hooks:install`，将 Git 的 `core.hooksPath` 指向 `.githooks`。
5. pre-commit 门禁必须至少运行暂存区开发规范检查和文本完整性检查。
6. 自动检查重点覆盖硬性规范，包括规范文档存在、中文注释/中文提示/中文日志、公共 CDN 禁止、前端新增代码复用全局 UI 样式、禁止新增内联样式和硬编码颜色、路由层避免新增重业务逻辑。
7. 自动检查不能替代人工评审；涉及产品体验、架构边界、数据安全和复杂业务兼容时，仍必须按本规范人工确认。
8. 如果自动检查误报，应优先优化检查脚本或补充明确豁免说明，禁止直接绕过门禁提交。
## 11. 提交前检查清单

提交或交付前必须逐项确认：

1. 是否遵守本规范和 docs/design/DESIGN.md。
2. 是否复用了现有服务、样式、工具函数和接口模式。
3. 是否没有新增无关重构和临时代码。
4. 是否没有硬编码颜色、字号、路径、密钥和测试数据。
5. 是否没有破坏全局 UI 样式统一性。
6. 是否处理了异常、空状态、加载状态和权限失败。
7. 是否保留了文档处理结果的来源和可追溯信息。
8. 是否避免新增英文注释、英文用户提示和英文业务日志。
9. 是否避免继续扩大已有大文件，新增逻辑是否已按功能边界拆分。
10. 是否运行了必要测试或说明了无法运行的原因。

## 12. 冲突处理优先级

当不同要求发生冲突时，按以下顺序处理：

1. 用户明确需求。
2. 本开发规范。
3. docs/design/DESIGN.md。
4. 现有业务逻辑和数据兼容性。
5. 现有代码风格和目录结构。
6. 新方案的可维护性和扩展性。

若冲突会导致数据丢失、安全风险或大范围行为变化，必须先暂停实现并说明风险。
