# 智枢 (Pivot AI) - 打包与推送脚本中文说明

为了彻底解决 Windows 命令提示符（CMD）在不同语言编码环境下读取 UTF-8 中文字符时产生的乱码和命令执行中断错误，我们对项目根目录下的两个脚本进行了深入优化，移除了脚本内部的直接中文字符。

本说明文档使用简体中文为您提供脚本的作用和详细注释，以作参考。

---

## 1. 📂 打包成安装包.bat
**存放路径**：[打包成安装包.bat](file:///E:/pivot/打包成安装包.bat)  
**主要功能**：一键安装项目依赖并执行 Electron 桌面端打包，生成 `.exe` 安装程序。

### 脚本中文注释与执行逻辑说明：
* `@echo off`：关闭脚本命令的逐行回显，保证控制台界面整洁。
* **Step 1: Install dependencies**：第一步，自动运行 `npm install` 检查并安装项目所需的全部 Node.js 依赖包（如 `electron`，`electron-builder`等）。
* **Step 2: Build and package installer**：第二步，执行编译打包命令 `npm run dist:win`（即调用项目自带的 `scripts/package_desktop.js`），编译生成 Windows 的可执行安装程序。
* **输出路径**：打包成功后，安装包会保存在项目根目录的 `dist-electron-remote` 文件夹中。常见安装文件为 `Pivot Setup 0.0.180.exe`。

---

## 2. 🚀 推送至GitHub.bat
**存放路径**：[推送至GitHub.bat](file:///E:/pivot/推送至GitHub.bat)  
**主要功能**：一键将本地所有的代码修改提交并推送至 GitHub 远程仓库的 `main` 分支。

### 脚本中文注释与执行逻辑说明：
* **Step 1: Git Status**：第一步，调用 `git status` 展示当前工作区中有哪些文件被修改或新建了。
* **Step 2: Git Add and Commit**：第二步，自动执行 `git add .` 将所有修改加入暂存区，并执行 `git commit -m "Update project"` 自动将修改提交到本地仓库。
* **Step 3: Git Push**：第三步，执行 `git push origin main`，将本地所有最新提交推送到 GitHub 的 `main` 分支。

---

## 3. 🧹 清理建议
打包或推送任务完成后，您可以随时直接将这两个 `.bat` 脚本以及本说明文档删除，这不会改变您的任何项目核心代码。
