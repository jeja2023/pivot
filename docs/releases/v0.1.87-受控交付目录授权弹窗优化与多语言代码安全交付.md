# v0.1.87 发布记录：受控交付目录授权弹窗优化与多语言代码安全交付

## 版本概述

**发布日期**：2026-09-07  
**版本号**：v0.1.87  
**适用范围**：Pivot 全场景智能协同中枢系统（Web 端 / Windows & Linux 桌面客户端）

在 v0.1.86 完成桌面端企业配置锁定与极简交互治理后，本版本重点解决**桌面端受控文件交付目录授权弹窗的视觉美化与体验重构**，并全面打通**多语言代码安全交付与受控原子落盘**全链路。

---

## 核心更新特性

### 1. 桌面受控交付目录授权弹窗现代化重构

- **彻底告别原生 Win32 弹窗**：替换原先调用操作系统 `dialog.showMessageBox` 弹出的粗糙系统消息框，设计并上线专属无边框 Electron 模态对话框（`desktop/delivery-grant.html` + `desktop/delivery-grant-preload.js`）。
- **统一 Pivot 品牌与视觉动效**：采用 Pivot Emerald 翠绿渐变主色调，配备入场 Pop-in 弹性动效的成功对勾徽章；增加专属已授权目录高亮胶囊卡片（`dir-pill`），并标明「受控写入」合规标识。
- **结构化格式标签芯片（Chips）展示**：将原先占满屏幕的多达 32 种格式长字符串重构为标签芯片排版，精选高频格式优先呈现，其余格式自动折叠为 `+N 种格式` 悬停提示气泡，直观整洁。
- **全链路深色模式与快捷交互**：完美适配操作系统或客户端的深色模式（Dark Mode）；支持无边框标题栏拖拽与右上角快捷关闭；主按钮「确定」自动聚焦，支持 `Enter` 与 `Esc` 键极速确认关闭。
- **无缝状态联动与优雅降级**：弹窗左下角提供「查看交付状态」快捷跳转入口，方便直接唤起受控交付状态监控页面；在无图形或极端异常环境下，保留格式化后的原生消息框优雅回退机制。

### 2. 多语言代码资产安全受控交付

- **全面扩展交付格式矩阵**：支持受控输出主流编程语言与配置文件（`py`、`js`、`ts`、`jsx`、`tsx`、`java`、`c`、`cpp`、`go`、`rs`、`sql`、`sh`、`json`、`yaml` 等）。
- **前端代码交付一键落盘**：新增 `client/chat/code-delivery.js`，在聊天与智能体代码产物中提供一键保存到本地受控目录的直观交互。
- **服务端防穿透与格式归一化**：在 `server/services/agent-path-safety.js` 与 `desktop/delivery/output-grants.js` 统一受控目录格式解析校验，提供向后兼容格式归一化，严守安全沙箱边界。

### 3. 受控文件交付状态窗口极简交互与目录管理治理

- **顶栏标识精简收敛**：移除顶部标题栏中多余的「企业受控通道」小徽标，使对话框标题更加清爽聚焦。
- **运行状态展示去重与不换行保障**：重构第一张 KPI 卡片展示，彻底去除重复渲染的状态文字与标签，通过 `white-space: nowrap` 布局彻底解决「空闲待命」跨行折叠为两行的不良排版。
- **受控目录防重添加机制**：接入排重校验，当用户选择已授权目录时友好提示无需重复添加；并在本地持久化存储加载与保存时自动清理历史重复目录，确保列表中同路径目录永远唯一。
- **受控目录删除与授权撤销能力**：在受控输出目录列表中为每条记录增设「删除」操作按钮，并配置两步确认（防止误触），支持从服务端和本机无缝撤销目录写入授权。

---

## 变更文件列表

| 模块 | 影响文件 | 变更说明 |
| :--- | :--- | :--- |
| **桌面客户端** | `desktop/delivery-status.html` | 顶栏徽标精简、状态去重不换行、目录项增加删除按钮与防误触 |
| **桌面客户端** | `desktop/delivery-status-preload.js` | 状态窗口上下文隔离桥增加 `revokeDirectory` 方法 |
| **桌面客户端** | `desktop/delivery-grant.html` (新建) | 现代化受控目录授权成功独立对话框界面 |
| **桌面客户端** | `desktop/delivery-grant-preload.js` (新建) | 交付授权弹窗专属安全上下文隔离桥 |
| **桌面客户端** | `desktop/delivery/status-window.js` | 增加授权窗口管理、撤销 IPC 注册及状态联动 |
| **桌面客户端** | `desktop/delivery/controller.js` | 接入现代化授权弹窗、重复拦截提示与撤销回调 |
| **桌面客户端** | `desktop/delivery/output-grants.js` | 格式归一化、存储加载自动排重与目录查找 |
| **桌面客户端** | `desktop/delivery/executor.js` | 授权前增加目录排重校验，防止重复添加相同受控目录 |
| **Web 聊天** | `client/chat/code-delivery.js` (新建) | 代码交付前端交互与保存到本机能力 |
| **服务端** | `server/services/agent-path-safety.js` | 代码与文档交付格式白名单及安全路径校验 |
| **服务端** | `server/services/agent-code-renditions.js` (新建) | 代码产物多格式转换服务 |
| **测试与质量** | `tests/desktop-delivery.test.js` | 新增授权弹窗、重复目录拦截与目录撤销测试用例 |
| **版本元数据** | `CHANGELOG.md`, `package.json`, `README.md` | 同步更新至 v0.1.87 版本 |
