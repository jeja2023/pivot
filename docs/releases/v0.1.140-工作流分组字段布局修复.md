# v0.1.140 工作流分组字段布局修复

- **发布日期**：2026-09-18
- **修复范围**：`data.group_summary` 的参数向导。

## 修复内容

分组字段的专用控件现在同时依据节点原始工具名、工具目录名称、工具标题、节点标题、Schema 描述和数组初始值识别。即使工具缓存不完整，也会显示标签式多字段编辑器，而不会把空数组显示成 `[]` 文本框。

“分组字段”和“指标字段”在桌面宽度下并排显示。分组字段内已经添加的字段以横向标签展示；用户可在同一行输入字段名并点击“添加字段”。
