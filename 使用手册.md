# 智枢（Pivot） 用户使用手册


## 1. 登录与界面

### 1.1 登录系统

1. 打开管理员提供的访问地址。
2. 输入用户名和密码。
3. 登录成功后会进入主页面；如果只是刷新已经登录的页面，系统可能恢复当前工作区。
4. 首次登录后，建议进入左下角用户菜单检查个人信息，并按管理员要求修改密码。

如果无法登录，先确认：

- 用户名、密码是否正确。
- 浏览器是否能访问服务器地址。
- 账号是否被管理员停用。
- 系统服务是否正在运行。

### 1.2 主界面区域

- 左侧栏：会话列表、搜索、知识库、工具箱、自由任务、系统设置等入口。
- 中间区域：当前对话或工作台内容。
- 输入区：发送问题、上传附件、启用知识库或工具箱工具。
- 右上角模型选择器：选择本轮对话使用的模型。
- 左下角用户区：账号信息、管理员标识、设置和退出登录。

普通用户通常只看到自己的会话、知识库、工具箱、自由任务、工作流和个人模型。管理员会看到更多管理入口。内置用户名 `admin` 是超级管理员，拥有全局配置和审计能力。

## 2. 公告与通知

公告用于发布系统维护、安全提醒、知识库变更、使用规范等重要信息。公告分为登录页公开公告和登录后的站内公告，两者用途不同。

### 2.1 登录页公告

登录页公告会在未登录状态下显示，用于提示所有访问者都需要提前知道的信息，例如系统维护、登录说明或紧急通知。

注意事项：

- 登录页公告只展示管理员设置为“登录页展示”的全员公告。
- 普通定向公告不会出现在登录页，避免把单位、角色或指定用户公告公开给未登录访问者。
- 登录后在主页点击“不再提示”只影响当前用户的站内公告，不会关闭登录页公告。
- 如果不希望登录页继续展示某条公告，需要管理员在公告管理中关闭“登录页展示”、撤下公告或设置结束时间。

### 2.2 主页公告入口

登录后，公告入口位于左侧系统名称“智枢”旁边。入口出现时，表示当前账号有有效公告可查看；如果有未读公告，会显示红点。

你可以：

- 点击公告入口打开公告中心。
- 查看公告类型、优先级、发布时间和内容。
- 对普通公告点击“不再提示”，让它不再出现在当前账号的站内公告入口、横幅和公告中心。
- 对需要确认的公告点击“确认”，表示已知晓该通知。

### 2.3 横幅和强制确认

重要公告可能会在页面顶部显示横幅。需要确认的公告会弹出确认窗口，用户必须点击“确认”后才算完成阅读确认。

规则说明：

- 普通公告可以“不再提示”。
- 需要确认的公告在确认前不会提供“不再提示”按钮。
- “不再提示”只对当前登录用户生效，不会影响其他用户。
- 公告是否继续有效，由公告状态、开始时间、结束时间和投放范围共同决定。

### 2.4 管理员发布公告

管理员可以在系统设置中进入“公告管理”。公告管理页支持搜索、状态筛选、新建、编辑、撤下、删除和查看详情。

新建公告时常见字段包括：

- 标题：公告列表和横幅中显示的标题。
- 内容：公告正文。
- 类型：系统、安全、知识库、普通公告。
- 优先级：低、普通、重要、紧急。
- 投放范围：全员、单位、角色或指定用户。
- 开始时间和结束时间：控制公告生效窗口，可留空表示立即生效或长期有效。
- 状态：草稿、已发布、已归档。
- 需要确认：开启后用户必须点击确认。
- 登录页展示：开启后公告会显示在登录页，但必须是全员公告。

公告管理表格会显示序号、标题、类型、优先级、范围、登录页、状态、确认、更新时间和操作。“登录页”列用“是/否”标记该公告是否会公开展示在登录页。

### 2.5 公告权限边界

权限规则如下：

- 超级管理员 `admin` 可以发布全员、单位、角色、指定用户公告，也可以设置“登录页展示”。
- 普通管理员通常只能管理自己创建、面向本单位的公告。
- 登录页公告必须面向全员投放，普通管理员不能发布登录页公告。
- 删除公告采用软删除方式，便于审计和追踪。
- 用户的已读、确认和不再提示状态按用户单独记录。

## 3. 日常对话

### 3.1 新建对话

1. 在左侧会话区点击新建会话。
2. 在右上角选择模型。
3. 在输入框输入问题。
4. 点击发送按钮，等待模型回复。

建议在问题中明确说明背景、目标、输出格式和限制条件。例如：

```text
请根据下面的会议记录，整理一份不超过 500 字的行动项清单，按负责人分组。
```

### 3.2 选择模型

模型选择器会优先显示当前可用和默认模型。不同模型能力可能不同：

- 文本模型：适合问答、总结、代码、写作和分析。
- 视觉模型：可以处理图片或扫描件。
- 推理模型：适合复杂分析、规划和审查，响应时间可能更长。
- 个人模型：只对本人可见。
- 全局模型：由管理员配置，按权限对用户开放。

如果模型不可用，可能是模型被停用、端点异常、额度不足或当前账号无权限。

### 3.3 上传附件

在输入区点击附件按钮，可以上传图片或文档。常见支持格式包括：

- 图片：`png`、`jpg`、`jpeg`、`gif`、`webp`、`bmp`
- 文档：`pdf`、`txt`、`md`、`csv`、`doc`、`docx`、`xls`、`xlsx`

使用建议：

- 文本型 PDF、Word、Excel 会尽量抽取文本后发送给模型。
- 扫描 PDF 和图片需要选择支持视觉输入的模型。
- 单次附件数量不要过多，优先上传与当前问题直接相关的文件。
- 加密文档需要按提示输入密码。

### 3.4 使用知识库回答

如果问题需要引用内部资料：

1. 在输入区打开“知识库”开关。
2. 如资料较多，可选择本轮要引用的专题库和标签范围；选择某个专题库后，标签列表只显示该专题库内已有标签。
3. 输入问题。
4. 发送后，系统会先按范围检索你的知识库，再把相关内容交给模型回答。

适合使用知识库的场景：

- 查询制度、手册、历史报告。
- 根据公司内部资料回答问题。
- 需要减少模型凭空回答的情况。

不适合使用知识库的场景：

- 纯创作、闲聊、翻译。
- 问题与已上传资料无关。
- 资料还没有索引完成。

### 3.5 使用工具箱工具

如果问题需要调用外部工具、数据库、报表文件或通知能力：

1. 在输入区打开“工具箱”工具开关。
2. 首次使用时阅读风险提示并确认。
3. 描述要查询或处理的内容。
4. 系统会按可用工具执行，并在回答中返回结果。

示例：

```text
使用数据库连接查询本月销售额最高的 10 个客户，并生成一张汇总表。
```

如果工具需要审批，任务会暂停，等待你或管理员确认。

### 3.6 套用角色与规范

如果需要固定本轮对话的角色、输出格式或处理方法，可以在输入区点击“套用角色与规范”。

常见用法：

- 选择角色类规范，让本轮会话按指定身份或职责回答。
- 选择输出规范，让回答统一成表格、清单、报告或审查意见。
- 选择任务方法，让模型按固定分析步骤处理问题。

套用后，规范会写入当前会话的系统提示词，只影响当前会话后续回答。管理员可在系统设置的“角色与规范库”中维护可复用规范，并设置它适用于聊天、自由任务或工作流。

### 3.7 管理会话

常用操作：

- 重命名会话：在会话菜单中修改标题。
- 搜索会话：点击搜索入口，按关键词、消息内容或标签检索。
- 标签管理：给项目、部门或主题相关会话添加标签。
- 归档会话：把不常用会话移出默认列表。
- 删除会话：删除后会进入软删除记录，管理员可按审计规则查看。
- 导出会话：需要留档时导出为 Markdown。
- 打印 / 导出 PDF：在主工作区打开打印预览，点击右侧“打印 / 导出为 PDF”后按浏览器打印对话框保存为 PDF。

## 4. 应用中心与公文写作

应用中心用于承载面向具体业务场景的工作台。当前内置“公文写作”，适合起草通知、请示、报告、函、会议纪要等公文。它支持多文档管理、发文要素结构化、全文 AI 起草/润色/改写/审校、选区润色/压缩/扩写/逐句改写、知识库检索参考、批注、版本对比、规范与校对检查、材料引用，以及多种格式导出。

### 4.1 打开应用中心

1. 在主界面打开“应用中心”。
2. 点击“公文写作”应用卡片。
3. 进入公文写作页面后，刷新浏览器仍会停留在该页面，不会自动退回应用中心首页。

### 4.2 公文写作页面布局

公文写作页面按正式写作流程组织：

- 顶部工具栏：选择文种、规范库、材料来源和正文/对照模式，填写“AI指令”，并执行“全文AI”的起草、润色、改写、审校，以及撤销/重做、资料栏/审改栏开关和更多导出操作。
- 左侧资料库：顶部是“我的公文”文档列表，下方可查看大纲、材料、模板、历史稿件和规范条文，其中“历史”“规范”支持知识库检索；大纲条目默认紧凑显示，鼠标悬停或键盘聚焦时会在条目内展开完整内容。
- 中间编辑区：正文模式用于正式写作，按公文版式渲染标题、段落、行距、缩进、落款和日期；对照模式用于左侧原文、右侧正文稿并排审改。
- 右侧审改栏：集中处理修改建议、发文要素、批注、版本、规范检查、校对和材料引用；版本保存入口位于“版本”面板。

### 4.3 多文档管理

左侧“我的公文”是文档库，每篇公文独立保存正文、原文、版本、批注、建议和发文要素：

- 新建：点击“新建公文”创建一篇空白公文并切换过去。
- 打开：点击列表中的公文标题即可切换，标题自动取自正文首行。
- 重命名 / 删除：每条公文右侧提供“改名”“删除”；删除有内容的公文会二次确认，删空后会自动保留一篇空白公文。
- 旧版本数据会在首次进入时自动迁移为文档库中的第一篇。

### 4.4 发文要素（版头与版记）

在右侧审改栏“发文要素”页可填写党政公文要素，导出时自动排版：

- 版头：密级与保密期限、紧急程度、发文字号、签发人。
- 版记：抄送机关、印发机关、印发日期。
- 这些字段仅在导出时参与排版，不影响正文编辑区；其中“印发机关”同时作为红头模板的机关名称。

### 4.5 起草和修改公文

公文写作的 AI 动作会调用模型生成内容，请先在聊天页选择一个可用模型，再回到公文写作工作台使用。常用流程：

1. 选择文种，例如通知、请示、报告或函。
2. 选择规范库。不同规范库会触发不同的检查规则，例如“党政机关公文格式”会额外校验发文字号、签发人和紧急程度，“会议纪要规范”会校验会议要素和议定事项。
3. 在“AI指令”中填写语气、结构、字数、重点或限制，这些要求会一并发送给模型。
4. 如有原文材料，在对照模式中粘贴到“原文”区域。
5. 点击顶部“全文AI”动作按钮（处理期间按钮会显示加载状态并暂时禁用）：
   - 起草：调用模型根据文种和材料生成结构化正文稿。
   - 润色：调用模型优化当前正文稿整体表达。
   - 改写：对当前正文稿进行整体改写，保留事实信息和必要结构。
   - 审校：调用模型逐条给出格式与表达问题，回填到右侧“修改建议”；同时左侧规范检查会刷新本地识别出的风险。
6. 选中正文或原文片段时会出现带“选区”标识的浮动工具条，可只针对选区执行润色、压缩、扩写、公文语气、逐句改写或转为批注。顶部全文 AI 与选区工具互不混用。
7. 误操作可用顶部“撤销”“重做”按钮恢复，也支持 `Ctrl+Z` / `Ctrl+Y`，覆盖建议替换、模板套用、载入版本和同步原文等操作。

### 4.6 流式改写与逐句对比接受

选中片段后点击浮动工具条“逐句改写”：

1. 模型会流式生成改写内容，可实时看到生成过程。
2. 生成完成后按句子做红绿对比：红色为原文、绿色为改写。
3. 可对每句选择“接受改写”或“保留原文”，也可一键“全部接受”或“全部保留原文”。
4. 点击“应用所选”后，按逐句选择拼装结果替换原选区；按 `Esc` 或“取消”可放弃。

### 4.7 知识库检索与参考起草

左侧资料库的“历史”“规范”标签接入知识库检索：

1. 输入关键词，可选择专题库范围后点击“检索”。
2. 结果显示来源与相关度，点击“引用到正文”会带来源前缀插入光标处，点击“作为依据”会追加到“AI指令”。
3. 历史公文与规范条文均存放在知识库中，可在知识库模块按需建立“历史公文”等专题库后检索。

### 4.8 审改、批注、版本与校对

右侧审改栏只展示结果类内容：

- 修改建议：模型生成的建议，可替换选区、插入下方、转为批注、生成版本、接受或拒绝。
- 批注：可针对原文或正文稿添加局部意见。
- 版本：保存当前正文稿，选择当前原文、当前正文稿或历史版本进行对比。
- 规范检查：查看系统按所选规范库识别出的格式、日期、待补充和事实依据风险。
- 校对：检查易错词、敏感与绝对化措辞、标点规范（连续标点、英文标点、引号括号成对）以及标题与文种一致性，问题可点击定位到正文。
- 材料引用：查看正文稿与原文材料之间的引用关系，支持对改写后的引用做相近度模糊匹配。

### 4.9 导出与备份

提示词相关操作位于顶部“更多”菜单，包括复制提示词和带入聊天。导出选项也在“更多”菜单中：

- 导出文本：纯文本正文。
- 导出 DOCX：按标题、主送机关、层级标题和落款日期套用版式，并写入发文要素的版头与版记。
- 导出红头 DOCX：套用单位红头模板（红色机关名称 + 红色分隔线），需先在“发文要素”填写印发机关。
- 导出 Markdown：标题与层级用 # 分级，发文要素以引用块呈现。
- 导出版式文本：标题居中、落款右对齐、版记分区的纯文本。
- 导出 PDF：打开打印窗口另存为 PDF。
- 导出备份：导出完整工作区 JSON；当内容较多接近本地存储上限时，系统会自动精简自动草稿与历史版本并提示，可先备份再清理。
- 送审版、领导修改版、定稿版：快速保存为对应阶段版本。

## 5. 知识库

知识库用于存放个人或团队资料，让模型在回答前先检索可信内容。

### 5.1 上传文档

1. 点击左侧“知识库”。
2. 点击上传文档。
3. 选择文件。
4. 根据资料用途选择已创建的专题库和标签。
5. 等待系统解析、分块和索引。
6. 文档状态变为就绪后即可检索。

上传建议：

- 一个文件只放一个主题，便于召回。
- 文件名尽量清晰，例如 `2026年销售制度.md`。
- 专题库适合放业务域、项目、部门这类大范围分类；标签适合标记系统名、阶段、地区、年份或用途。
- 新专题库和新标签建议先在知识库工作台创建，再在上传或整理文档时选择使用。
- 大文件上传后需要等待索引完成。
- 扫描件效果取决于 OCR 或视觉模型能力。

### 5.2 查看文档状态

文档列表会显示状态、分块数量、索引进度、错误信息和处理时间。

常见状态：

- 处理中：正在解析或索引。
- 就绪：可以被检索。
- 失败：解析或索引失败，需要查看错误并重试。
- 停用：暂时不参与检索。

### 5.3 按专题库和标签管理

当知识库资料越来越多时，建议先做范围管理，再让模型检索：

- 用专题库管理较大的资料边界，例如项目、部门、产品线或制度体系。
- 用标签管理更细的筛选条件，例如 `2026`、`合同`、`上线前`、`FAQ`。
- 专题库和标签需要先创建后使用；整理文档时通过下拉框选择专题库，通过复选框选择标签，不需要手工输入名称。
- 文档列表中可按专题库或标签筛选，表格中会展示每个文档所属的专题库和标签；需要调整时使用整理文档弹窗。
- 选择某个专题库后，标签选项会自动缩小到该专题库内的标签，避免跨专题库误选。
- 在普通聊天或召回测试中选择专题库/标签后，本轮只会从对应范围中召回资料，不会再引用其它专题库。
- 不确定资料范围时保持“全部”，确认问题属于某个业务域时再缩小范围。

### 5.4 调试检索效果

在知识库工作台中使用召回测试：

1. 输入测试问题。
2. 选择要测试的专题库和标签范围；标签会跟随专题库过滤。
3. 可临时调整相似度阈值、Top K 和候选数量。
4. 查看命中文档、分块内容和相似度分数。
5. 对结果标记“有用”或“无用”。

结果区会展示：

- 检索耗时。
- 按文件聚合的命中汇总。
- 每个候选的得分进度条。
- 关键词高亮。

如果没有命中：

- 检查文档是否就绪。
- 换成文档中更接近原文的关键词。
- 降低相似度阈值。
- 确认问题确实属于已上传资料范围，且没有选错专题库或标签。

### 5.5 批量维护

知识库支持：

- 批量重建索引。
- 批量删除文档。
- 对失败文档重试。
- 查看删除审计。
- 查看质量报告。

质量报告会展示质量分、就绪率、反馈有用率、陈旧文档、空分块文档和知识图谱覆盖情况。分数偏低时，优先处理失败或停用文档，重新索引长期未更新的就绪文档，再用召回测试确认命中是否符合预期。

删除通常采用软删除，方便审计和追踪。

### 5.6 查看知识图谱

知识图谱用于查看知识库中抽取出的实体节点和关系。

当前知识图谱更偏工程化辅助能力：实体和关系主要由规则、模式匹配和启发式质量信号抽取，适合帮助 Graph-RAG 找到关系线索，不建议直接当作高精度语义知识图谱使用。用于正式问答前，建议先确认低可信关系、合并重复实体，并回到来源文档核对关键结论。

在知识库页面点击“知识图谱”后，可以：

- 在左侧实体节点列表中搜索、筛选并选择实体。
- 通过“全部质量 / 孤立实体 / 低可信”筛选，优先找到需要补充资料或校准的实体。
- 在中间关系地图中查看中心实体和邻居节点。
- 在关系地图空白区域拖动平移，使用滚轮或右上角 `- / + / 1:1` 控件缩放和复位。
- 点击关系地图节点切换中心实体。
- 鼠标悬停节点时查看完整实体信息，包含名称、类型、关系数、实体 ID 等。
- 在右侧关系明细中按状态、关系类型和最低可信度筛选关系，查看来源、关系类型、可信度、来源片段和描述。
- 对“待确认”关系点击“确认”，确认后该关系才会作为正式图谱关系参与后续 Graph-RAG 召回；不准确的关系可以编辑或删除。
- 点击“校准实体”打开实体校准弹窗，保存实体信息或合并重复实体。
- 在顶部“查询图谱”中输入职责、依赖、归属或影响类问题，查看命中的实体和关系路径，用于排查图谱是否能支撑回答。

如果关系较多，右侧关系明细会以紧凑卡片展示，便于同屏查看更多关系。图谱概览中的质量分、待确认关系和孤立实体数量越低，通常代表图谱越适合参与正式问答；如果质量分偏低，建议先确认低可信关系、合并重复实体，并补充包含“负责、依赖、归属、包含、影响”等关系描述的资料。

## 6. 工具箱

工具箱用于把模型、自由任务和工作流连接到可调用工具，例如数据库、文件报表、图表生成、报告编排、文档解析、数据处理、格式转换和内网通知。系统底层兼容 MCP，但界面上会以“工具”的方式呈现。

### 6.1 系统工具

点击左侧“工具箱”后，上方会显示系统工具。系统工具由系统集成，通常无需填写名称或地址。

常见系统工具：

- 图表生成：把表格数据生成可直接渲染的柱状图、折线图、饼图和表格展示。
- 报告编排：把摘要、表格、图表和指标块组织成固定格式报告。
- 文档解析：从文档文本中提取大纲、键值信息，并按段落切分为可分析片段。
- 数据处理：对表格行进行字段画像、筛选、聚合统计和字段标准化处理。
- 格式转换：在 Markdown 表格、JSON 和规范化文本之间进行轻量转换。

每张系统工具卡片右上角有开关。打开后，聊天、自由任务和工作流可以按权限调用该工具；关闭后，该工具不会被用于后续任务。点击“工具”可以在弹窗中查看工具名称、说明和刷新工具缓存。

### 6.2 个人连接

个人连接需要你连接或配置后才会生效，保存后供当前用户调用。

常见个人连接：

- 数据库连接：连接 MySQL、PostgreSQL、SQL Server、SQLite、MongoDB 等数据库。
- 报表文件：连接报表和数据文件目录，读取 CSV、Excel、JSON、Markdown 等文件。
- 消息通知：对接局域网 Webhook 或消息 API，把任务摘要、提醒和结果发送给指定目标。
- 已配置连接：已经保存成功的数据库或外部服务会以独立卡片显示，可以编辑、查看工具、停用或删除。

配置个人连接的一般流程：

1. 点击对应工具卡片上的“配置”。
2. 填写连接信息或目录/API 参数。
3. 保存前按需测试连接。
4. 保存后在个人连接区域查看已配置服务卡片。
5. 点击“工具”确认工具列表已加载。

### 6.3 数据库连接使用建议

数据库连接主要用于只读查询。

配置时注意：

- 使用只读账号。
- 填写正确的主机、端口、数据库名、用户名和密码。
- 设置最大返回行数，避免一次查询过多数据。
- 生产库建议配置允许访问的表/集合白名单。
- 涉及个人信息或敏感业务字段时，配置字段白名单和敏感字段脱敏。
- 按需填写行级权限提示，例如“只查询当前部门数据”。
- 保存前先测试连接。

使用时建议明确限制查询范围：

```text
查询 2026 年 5 月销售订单总额，按部门汇总，只返回前 20 行。
```

### 6.4 查看工具与状态

工具箱页面会显示：

- 系统工具卡片：启停状态、工具数量和工具弹窗。
- 个人连接卡片：配置入口、已配置连接、启停、编辑、工具和删除操作。
- 工具弹窗：工具名称、说明、刷新工具按钮和单个工具的启用状态。

如果某个连接没有工具或工具加载失败，先进入工具弹窗刷新，再检查连接状态、权限和网络。

### 6.5 配置诊断与测试

保存连接后，可以在编辑弹窗中使用诊断或测试按钮：

- 数据库连接：测试连接，并确认表白名单、敏感字段和查询超时等治理配置。
- 报表文件：诊断目录是否可读，并预览可读取文件。
- IM 通知：发送测试消息，查看最近投递状态和失败提示。
- 外部能力服务：配置健康检查 URL、调用超时、认证模式和工具 Schema 校验，诊断服务是否可访问。

### 6.6 工具策略

管理员可以在系统设置中进入“工具策略”。这个页面只用于集中管理系统工具和全局工具服务，用户自己的连接和个人外部服务不在这里治理：

- 工具以卡片形式直接平铺展示，每页显示 18 个工具，底部使用统一分页控件翻页。
- 每张卡片右上角的轨道开关用于启用或停用工具。
- 内置系统管理员可以点击卡片上的“编辑”，打开页面居中的治理弹窗，修改风险等级、审批要求和适用说明。
- 用户自己配置的数据库连接、报表目录、消息通知和个人外部服务不在这里管理。
- 普通管理员可以查看工具策略，但只读卡片不显示“编辑”入口；内置系统管理员可以修改工具启停和治理策略。
- 个人连接仍由当前用户自己配置、启停和删除，并继续受到数据库只读限制、审批策略和运行时安全检查保护。

### 6.7 常见工具箱问题

- 工具列表为空：先在工具弹窗中刷新工具，确认服务或连接可访问。
- 调用失败：检查服务地址、账号、网络和权限。
- 数据库无结果：检查 SQL 条件、表名、Schema、最大返回行数，以及表/字段白名单是否过窄。
- 字段被显示为“已脱敏”：说明该字段被配置为敏感字段，系统会在返回给模型前做脱敏。
- 一直等待审批：说明工具风险较高，需要用户或管理员批准。

## 7. 自由任务与工作流编排

自由任务用于处理探索性、多步骤任务，例如资料调研、临时分析、数据排查和一次性报告生成。它保留目标拆解、工具调用、预检、审批和结果沉淀能力，但产品定位更偏“快速处理”和“流程孵化”。

工作流编排用于承载稳定流程，例如标准报表、周期取数、审批前检查、内网通知和可审计的生产任务。企业部署中，建议用自由任务先探索和验证步骤，再将可复用流程沉淀为工作流并发布运行。

### 7.1 创建自由任务

1. 点击左侧“自由任务”。
2. 输入任务目标。
3. 选择模型。
4. 选择运行模式。
5. 在“模型路由”下拉框中选择路由策略（默认“固定模型”等同旧行为）。
6. 根据需要打开“允许工具箱工具”，并调整工具审批、Token 预算、失败重试、上下文来源和补充说明。
7. 如需统一角色、输出格式或任务方法，点击“套用规范”，选择适用于自由任务的规范；内容会追加到补充说明。
8. 点击运行。

运行前系统会做预检，并展示就绪度评分、预计输入 Token、高风险工具数量、异常工具服务和未检查工具服务。如果评分偏低，先收窄工具范围、提高 Token 预算、刷新工具箱工具或修复异常连接，再启动任务。

“模型路由”可选项：

- 固定模型：始终使用所选模型。
- 视觉优先：含图片时优先选支持视觉的模型。
- 上下文匹配：选能容纳输入且窗口最小的模型。
- 成本优先：选价格最低的可用模型。
- 负载均衡：选当前活跃占比最低的模型。
- 成本升级：先用便宜模型，置信不足时升级到更强模型。

目标应尽量具体：

```text
根据知识库中的项目资料，整理本周项目风险清单，按风险等级排序，并给出处理建议。
```

### 7.2 自由任务模式与工作流入口

- 标准模式：适合一般任务。
- 深度模式：适合需要更多检索、分析和反复验证的任务。
- 审查模式：适合检查风险、漏洞、合规问题或遗漏。

如果不确定，先使用标准模式。

自由任务页面提供“工作流编排”入口，用于打开完整画布页面：

- 拖动节点，右侧端口拖到目标节点即创建依赖。
- 工具栏支持添加节点、自动布局、适配画布、JSON 视图、从 JSON 同步、保存草稿、保存工作流、发布版本和运行发布版本。
- 选中节点后会从右侧抽屉编辑标题、工具、条件、失败策略、重试次数、超时、输入和依赖。
- 工具选择器会按知识与会话、数据库、报表与文件、图表与展示、数据处理、文档处理、格式转换、消息通知、系统诊断和外部能力分组，并显示中文名称、用途说明、来源标签和内部 ID。
- 输入参数区域可套用工具参数模板，也可以插入任务目标或上游节点输出变量。
- 需要统一大模型节点提示时，可点击“套用规范”；系统会优先写入已有大模型节点，没有合适节点时创建新的大模型节点。
- 大模型节点可以作为起始节点，但提示词必须引用 `{{goal}}` 或 `{{inputs.*}}`；如果要分析数据库查询、知识检索、报表解析或其他工具结果，应先连接这些上游节点，再在提示词中引用对应的 `{{nodes.节点ID.output}}`。
- 如果大模型节点既没有上游依赖，也没有引用工作流目标或运行输入，保存、预检或运行时会提示“缺少上游输入”，避免模型节点空跑。
- 统计图模板会默认生成“分组统计 → 大模型处理 → 生成统计图”三段流程：大模型节点负责解释统计结果，图表节点仍直接读取结构化行数据，保证图表稳定渲染。
- 配置数据库节点或统计图模板时，参数弹窗、数据库连接下拉、读取表/字段辅助和侧栏摘要会使用当前工具库中的同一份连接信息，减少连接切换后不同入口显示不一致的问题。
- 运行详情会优先把大模型节点结果显示为可读 Markdown 正文；如果需要排查完整返回对象，可展开“查看原始数据”或“节点输出”查看原始 JSON。
- 工作流库支持加载、发布、查看版本、对比差异、回滚历史版本和删除工作流。
- 已发布工作流可用于计划任务和审计复用，适合企业生产任务。
- 支持滚轮缩放、空白处平移和右下角小地图。
- JSON 视图以弹窗保留，供专家用户直接编辑。

自由任务完成后，任务详情中可以点击“生成工作流草稿”。系统会把本次工具执行步骤转换为顺序工作流节点，并补充最终大模型总结节点；进入画布后需要检查节点、参数和依赖，再保存、发布或配置计划运行。

### 7.3 工具范围

自由任务可以调用系统工具、知识库工具和工具箱工具。你可以限制工具范围：

- 不使用工具：只让模型根据输入内容回答。
- 仅系统工具：使用会话、知识库、模型状态等系统工具。
- 系统工具 + 工具箱：允许调用数据库、报表文件、图表、报告编排、消息通知等工具。
- 白名单工具：只允许选中的工具。

如果任务涉及敏感数据库或外部系统，建议只开放必要工具。

### 7.4 审批与风险确认

当自由任务准备调用高风险工具箱工具时，任务可能进入“等待审批”状态。

你可以：

- 查看工具名称、输入参数和风险说明。
- 批准执行。
- 拒绝执行。
- 停止任务。

不要批准看不懂或超出任务范围的工具调用。

### 7.5 查看任务记录与结果

任务记录会以数据表格展示，便于扫描和筛选。后台保留统一任务记录，前端可以按“全部类型 / 自由任务 / 工作流任务”筛选。表格通常包含：

- 序号、任务、类型、模型、模式、步骤、工具、错误。
- 输入 Token、输出 Token、总 Token。
- 创建时间、状态和操作。

顶部统计会展示 7 日任务数、成功率、失败数和 Token 用量；Token 会按 K / M / B 压缩显示。你可以按状态、类型筛选，也可以用搜索框查找任务。

点击“详情”后，可以在弹窗中查看：

- 任务时间轴：按耗时占比展示每一步，失败步骤标红，悬停显示工具名与毫秒数。
- 工具调用统计：按工具名聚合次数、总耗时与失败次数。
- 每一步执行过程。
- 工具输入和输出。
- 错误信息。
- Token 用量。
- 最终答案。
- 工作流节点轨迹。

启用流式 function calling 时，详情顶部会显示实时生成面板。

任务完成后，可以把结果保存为产物，后续支持查看、备注、对比和回滚。

### 7.6 模板和计划任务

自由任务面板提供三个常用配置入口：

- 工作流编排：打开完整工作流画布页面。
- 自由任务模板与计划：管理目标模板、临时计划执行、计划队列和通知中心。
- 能力与结果：查看能力清单、可用工具和沉淀结果。

常用自由任务可以保存为模板：

1. 配好目标、模型、模式、模型路由和工具范围。
2. 保存为个人模板或共享模板。
3. 下次直接套用，系统会恢复目标、参数、上下文来源、模型路由和工具范围。

工作流模板用于复用步骤、节点、依赖结构和运行输入。稳定流程建议在工作流编排中保存、发布，再由已发布版本承接计划任务。

周期性任务可以配置为计划：

- 每日任务。
- 每周任务。
- 手动触发任务。

无效模板或计划可以删除，避免列表堆积。

### 7.7 失败后处理

任务失败时：

- 先查看错误步骤。
- 如果是临时网络或模型异常，可以重试。
- 如果是工具参数错误，修改目标或工具配置后再运行。
- 如果支持断点续跑，可以从失败上下文继续。

运行中的任务不能直接重跑，需要先停止或等待完成。

## 8. 模型配置

模型配置一般由管理员完成。普通用户如果被允许，也可以维护自己的个人模型。

### 8.1 新增模型

1. 进入系统设置或模型管理。
2. 点击添加模型。
3. 填写接口地址 Endpoint。
4. 填写 API Key，如不需要可留空。
5. 填写模型展示名称。
6. 填写模型标识 Model ID。
7. 设置保存范围：个人模型或全局模型。
8. 按需设置可见部门、额度、温度、价格、Token 上限、健康检查 URL 和并发上限。
9. 标记模型能力，例如视觉输入、思考模型。
10. 测试连接。
11. 保存。

### 8.2 编辑模型

编辑时常见调整：

- 修改模型名称，便于用户识别。
- 修改 Endpoint 或 Model ID。
- 更新 API Key。
- 调整每日额度。
- 设置端点并发上限。
- 设置远端健康检查 URL。
- 开启或关闭视觉、推理能力标识。
- 调整可见部门。

保存后，用户侧模型选择器和自由任务模型选择会同步更新。

### 8.3 模型不可用排查

如果模型调用失败：

- 先点击测试连接。
- 检查 Endpoint 是否以正确路径结尾，例如 `/v1`。
- 检查 API Key 是否有效。
- 检查 Model ID 是否与上游服务一致。
- 检查并发限制是否过低。
- 查看系统监控中的模型端点状态。
- 确认当前用户是否有模型权限。

## 9. 第三方 API Key

系统可提供 OpenAI-compatible API 给外部程序调用。

### 9.1 创建 API Key

1. 进入 API 接入或系统设置中的相关入口。
2. 创建新的 API Key。
3. 设置名称、权限和有效期。
4. 保存后复制密钥。

密钥通常只完整显示一次，请妥善保管。

### 9.2 使用 API Key

外部程序可以按 OpenAI-compatible 格式调用系统接口。具体地址和模型名以管理员配置为准。

使用建议：

- 给不同系统分别创建不同 Key。
- 不要把 Key 写入公开仓库。
- 代码补全类客户端如果通过聊天补全接口发送 `prompt`/`suffix` 风格请求，系统会兼容转换为可用的聊天消息。
- 不再使用时及时停用或删除。
- 管理员可查看调用记录和 Token 用量。

## 10. 状态与问题排查

如果你的账号可以看到系统状态或监控入口，可以用它辅助判断模型、知识库和工具是否正常。普通用户不需要维护这些指标，只需要知道异常时看哪里、该向谁反馈。

### 10.1 常见状态

- 活跃用户和调用量。
- 模型 Token 使用量。
- 模型端点健康状态。
- 并发、排队和熔断状态。
- RAG 检索耗时和命中情况。
- 慢查询和异常告警。
- Pivot 所在服务器的 GPU 与显存情况。

注意：GPU 区域只代表 Pivot 部署服务器本机 GPU。如果模型部署在远端服务器，需要结合远端健康检查或模型端点状态判断。

### 10.2 模型端点状态

模型端点状态只显示当前仍启用的模型配置。已删除或停用的模型不会继续显示。

重点关注：

- 健康检查是否成功。
- 当前并发是否达到上限。
- 队列是否持续增长。
- 是否进入熔断状态。
- 失败次数是否异常。

### 10.3 异常处理建议

- 排队过多：提高端点并发、增加模型实例或降低任务量。
- 端点熔断：检查上游模型服务是否可用。
- Token 消耗异常：检查是否有批量任务或第三方 API 大量调用。
- RAG 很慢：检查文档数量、召回参数和服务器负载。
- 外部链路未生效：先确认 `.env`、模型、Embedding、工具箱、数据库连接和 IM Webhook 配置完整；管理员可运行 `npm run check:external` 查看配置体检，目标环境验收时再运行 `npm run check:external -- --live` 做真实连通测试。

## 11. 推荐工作流

### 11.1 普通问答

1. 选择合适模型。
2. 直接提问。
3. 需要引用内部资料时打开知识库。
4. 需要查询数据库、读取报表文件或生成图表时打开工具箱工具。

### 11.2 内部资料问答

1. 上传资料到知识库。
2. 等待文档状态变为就绪。
3. 用召回测试验证能否命中。
4. 在对话中打开知识库提问。

### 11.3 数据分析报告

1. 配置数据库连接或报表文件工具。
2. 在对话或自由任务中启用工具箱工具。
3. 明确查询范围和输出格式。
4. 需要图表时调用图表生成能力。
5. 需要固定格式时调用报告编排能力。

### 11.4 周期性任务

1. 在自由任务中写清任务目标，先试运行一次。
2. 选择模型、模式和工具范围。
3. 如果流程稳定，生成工作流草稿并在画布中检查节点。
4. 保存并发布工作流。
5. 用已发布工作流配置每日或每周计划。
6. 定期检查通知和产物。

## 12. 常见问题

### 12.1 模型回复慢

可能原因：

- 模型本身推理慢。
- 当前端点排队。
- 附件或知识库上下文较大。
- 自由任务或工作流步骤较多。

处理方式：

- 换用更快模型。
- 减少附件和上下文。
- 查看系统监控中的端点并发和队列。
- 等待当前任务完成。

### 12.2 知识库回答不准确

处理方式：

- 检查文档是否就绪。
- 用召回测试查看命中文档。
- 调整问题关键词。
- 上传更清晰、更聚焦的资料。
- 调整 RAG 参数后重试。

### 12.3 工具箱无法调用

处理方式：

- 检查工具卡片右上角开关是否启用。
- 在工具弹窗中刷新工具缓存。
- 查看对应工具卡片的状态和连接配置。
- 检查连接地址、账号和网络。
- 数据库连接使用只读账号并确认表权限。

### 12.4 自由任务卡在等待审批

说明自由任务准备调用需要确认的工具。

处理方式：

- 打开任务详情。
- 查看工具和参数。
- 确认合理后批准。
- 不合理则拒绝或停止任务。

### 12.5 附件无法读取

可能原因：

- 文件格式不支持。
- 文件过大。
- 文档加密但密码错误。
- 扫描件需要视觉模型。
- 图片过大被压缩或跳过。

处理方式：

- 换成 PDF、TXT、DOCX、XLSX、CSV 等常见格式。
- 拆分大文件。
- 选择支持视觉输入的模型。
- 重新上传清晰文件。

### 12.6 看不到某个模型或工具

可能原因：

- 当前账号无权限。
- 模型或工具已停用。
- 只对指定部门可见。
- 个人资源属于其他用户。

处理方式：

- 联系管理员确认权限。
- 检查模型或工具是否启用。
- 刷新页面后再试。

### 12.7 主页不再提示后，登录页公告还显示吗

会继续显示。

主页“不再提示”只记录当前登录用户对站内公告的隐藏状态。登录页公告是公开展示入口，不依赖某个已登录用户的已读或隐藏状态。

如果需要关闭登录页展示，需要管理员在公告管理中执行以下任一操作：

- 编辑公告并取消“登录页展示”。
- 将公告撤下或归档。
- 设置结束时间，让公告过期。

## 13. 使用安全建议

- 不要在普通对话中粘贴不必要的密码、密钥或身份证件信息。
- 上传内部资料前确认资料范围和保密要求。
- 使用工具箱查询数据库时优先使用只读账号。
- 批准自由任务工具调用前，先确认工具和参数是否符合任务目标。
- 第三方 API Key 不要共享给无关人员。
- 离职、项目结束或系统交接时，及时停用账号、计划任务和 API Key。

## 14. 快速检查清单

新用户第一次使用前：

- 已能登录系统。
- 已知道默认可用模型。
- 已能新建一次对话。
- 已了解知识库和工具箱工具开关的用途。
- 已知道遇到模型不可用时联系谁。
