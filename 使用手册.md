# 智枢（Pivot） 用户使用手册

本文主要面向 智枢（Pivot） 的普通用户。内容以“怎么使用”为主，不展开技术实现。

## 1. 登录与界面

### 1.1 登录系统

1. 打开管理员提供的访问地址。
2. 输入用户名和密码。
3. 首次登录后，建议进入左下角用户菜单检查个人信息，并按管理员要求修改密码。

如果无法登录，先确认：

- 用户名、密码是否正确。
- 浏览器是否能访问服务器地址。
- 账号是否被管理员停用。
- 系统服务是否正在运行。

### 1.2 主界面区域

- 左侧栏：会话列表、搜索、知识库、MCP、智能体、系统设置等入口。
- 中间区域：当前对话或工作台内容。
- 输入区：发送问题、上传附件、启用知识库或 MCP。
- 右上角模型选择器：选择本轮对话使用的模型。
- 左下角用户区：账号信息、管理员标识、设置和退出登录。

普通用户通常只看到自己的会话、知识库、MCP、智能体和个人模型。管理员会看到更多管理入口。内置用户名 `admin` 是超级管理员，拥有全局配置和审计能力。

## 2. 日常对话

### 2.1 新建对话

1. 在左侧会话区点击新建会话。
2. 在右上角选择模型。
3. 在输入框输入问题。
4. 点击发送按钮，等待模型回复。

建议在问题中明确说明背景、目标、输出格式和限制条件。例如：

```text
请根据下面的会议记录，整理一份不超过 500 字的行动项清单，按负责人分组。
```

### 2.2 选择模型

模型选择器会优先显示当前可用和默认模型。不同模型能力可能不同：

- 文本模型：适合问答、总结、代码、写作和分析。
- 视觉模型：可以处理图片或扫描件。
- 推理模型：适合复杂分析、规划和审查，响应时间可能更长。
- 个人模型：只对本人可见。
- 全局模型：由管理员配置，按权限对用户开放。

如果模型不可用，可能是模型被停用、端点异常、额度不足或当前账号无权限。

### 2.3 上传附件

在输入区点击附件按钮，可以上传图片或文档。常见支持格式包括：

- 图片：`png`、`jpg`、`jpeg`、`gif`、`webp`、`bmp`
- 文档：`pdf`、`txt`、`md`、`csv`、`doc`、`docx`、`xls`、`xlsx`

使用建议：

- 文本型 PDF、Word、Excel 会尽量抽取文本后发送给模型。
- 扫描 PDF 和图片需要选择支持视觉输入的模型。
- 单次附件数量不要过多，优先上传与当前问题直接相关的文件。
- 加密文档需要按提示输入密码。

### 2.4 使用知识库回答

如果问题需要引用内部资料：

1. 在输入区打开“知识库”开关。
2. 输入问题。
3. 发送后，系统会先检索你的知识库，再把相关内容交给模型回答。

适合使用知识库的场景：

- 查询制度、手册、历史报告。
- 根据公司内部资料回答问题。
- 需要减少模型凭空回答的情况。

不适合使用知识库的场景：

- 纯创作、闲聊、翻译。
- 问题与已上传资料无关。
- 资料还没有索引完成。

### 2.5 使用 MCP 工具

如果问题需要调用外部工具、数据库、报表文件或通知能力：

1. 在输入区打开“MCP”开关。
2. 首次使用时阅读风险提示并确认。
3. 描述要查询或处理的内容。
4. 系统会按可用工具执行，并在回答中返回结果。

示例：

```text
使用数据库 MCP 查询本月销售额最高的 10 个客户，并生成一张汇总表。
```

如果工具需要审批，任务会暂停，等待你或管理员确认。

### 2.6 管理会话

常用操作：

- 重命名会话：在会话菜单中修改标题。
- 搜索会话：点击搜索入口，按关键词、消息内容或标签检索。
- 标签管理：给项目、部门或主题相关会话添加标签。
- 归档会话：把不常用会话移出默认列表。
- 删除会话：删除后会进入软删除记录，管理员可按审计规则查看。
- 导出会话：需要留档时导出为 Markdown。
- 打印 / 导出 PDF：从会话菜单点击“打印 / 导出 PDF”，会在新标签页打开打印友好视图，按浏览器自带的打印对话框可直接保存为 PDF。

## 3. 知识库

知识库用于存放个人或团队资料，让模型在回答前先检索可信内容。

### 3.1 上传文档

1. 点击左侧“知识库”。
2. 点击上传文档。
3. 选择文件。
4. 等待系统解析、分块和索引。
5. 文档状态变为就绪后即可检索。

上传建议：

- 一个文件只放一个主题，便于召回。
- 文件名尽量清晰，例如 `2026年销售制度.md`。
- 大文件上传后需要等待索引完成。
- 扫描件效果取决于 OCR 或视觉模型能力。

### 3.2 查看文档状态

文档列表会显示状态、分块数量、索引进度、错误信息和处理时间。

常见状态：

- 处理中：正在解析或索引。
- 就绪：可以被检索。
- 失败：解析或索引失败，需要查看错误并重试。
- 停用：暂时不参与检索。

### 3.3 调试检索效果

在知识库工作台中使用召回测试：

1. 输入测试问题。
2. 可临时调整相似度阈值、Top K 和候选数量。
3. 查看命中文档、分块内容和相似度分数。
4. 对结果标记“有用”或“无用”。

结果区会同步展示：

- 检索耗时（毫秒），便于判断响应是否过慢。
- 按文件聚合的命中汇总条，包含命中数、峰值得分和均值。
- 每个候选分块的得分进度条，方便快速对比相似度梯度。
- 关键词高亮：分块文本中匹配到的关键词会以黄底高亮。

如果没有命中：

- 检查文档是否就绪。
- 换成文档中更接近原文的关键词。
- 降低相似度阈值。
- 确认问题确实属于已上传资料范围。

### 3.4 批量维护

知识库支持：

- 批量重建索引。
- 批量删除文档。
- 对失败文档重试。
- 查看删除审计。
- 查看质量报告。

删除通常采用软删除，方便审计和追踪。

## 4. MCP 工作台

MCP 用于把模型连接到外部工具，例如数据库、文件报表、图表生成、报告编排和内网通知。

### 4.1 新增 MCP

1. 点击左侧“MCP”。
2. 在类型下拉框选择 MCP 类型。
3. 填写名称、连接地址或配置参数。
4. 保存配置。
5. 点击测试连接或刷新工具。
6. 确认工具列表已加载。

常见类型：

- 外部 MCP Server：连接已有 MCP 服务。
- 数据库 MCP Server：连接 MySQL、PostgreSQL、SQL Server、SQLite、MongoDB 等。
- 报表与数据文件 MCP：读取内网目录中的 CSV、Excel、JSON、Markdown 等文件。
- 可视化图表 MCP：把数据生成图表或表格展示块。
- 报告编排 MCP：按固定结构生成报告。
- 局域网消息通知 MCP：向内网消息系统发送通知。

### 4.2 数据库 MCP 使用建议

数据库 MCP 主要用于只读查询。

配置时注意：

- 使用只读账号。
- 填写正确的主机、端口、数据库名、用户名和密码。
- 设置最大返回行数，避免一次查询过多数据。
- 保存前先测试连接。

使用时建议明确限制查询范围：

```text
查询 2026 年 5 月销售订单总额，按部门汇总，只返回前 20 行。
```

### 4.3 查看工具与调用记录

每个 MCP 服务卡片内会显示：

- 当前服务状态。
- 已缓存的可调用工具。
- 最近调用记录。
- 异常或未检查状态。

如果某个 MCP 没有调用记录，会在该 MCP 区域显示空态提示。全局治理区只展示汇总统计。

### 4.4 常见 MCP 问题

- 工具列表为空：先刷新工具，确认 MCP 服务可访问。
- 调用失败：检查服务地址、账号、网络和权限。
- 数据库无结果：检查 SQL 条件、表名、Schema 和最大返回行数。
- 一直等待审批：说明工具风险较高，需要用户或管理员批准。

## 5. 智能体

智能体用于处理多步骤任务，例如资料调研、报告生成、数据分析、流程审查和周期性任务。

### 5.1 创建智能体任务

1. 点击左侧“智能体”。
2. 输入任务目标。
3. 选择模型。
4. 选择运行模式。
5. 在“模型路由”下拉框中选择路由策略（默认“固定模型”等同旧行为）。
6. 根据需要启用知识库、MCP 或指定工具范围。
7. 点击运行。

“模型路由”可选项说明：

- 固定模型：始终使用所选模型，行为与历史版本一致。
- 视觉优先：当输入包含图片时优先选支持视觉的模型，否则按上下文匹配。
- 上下文匹配：在能容纳输入的模型中选窗口最小的，节约大窗口配额。
- 成本优先：在能容纳输入的模型中选输入加输出单价之和最低的。
- 负载均衡：在端点活跃占比最低的候选中分发，降低排队等待。
- 成本升级：先用最便宜模型尝试，如果输出过短、出现“不确定/无法判断”等低置信短语，自动升级到更强模型再合成一次。

目标应尽量具体：

```text
根据知识库中的项目资料，整理本周项目风险清单，按风险等级排序，并给出处理建议。
```

### 5.2 运行模式

- 标准模式：适合一般任务。
- 深度模式：适合需要更多检索、分析和反复验证的任务。
- 审查模式：适合检查风险、漏洞、合规问题或遗漏。
- DAG 编排模式：适合结构化流程、并行步骤和依赖关系明确的任务，可在画布上拖拽节点、连接依赖。

如果不确定，先使用标准模式。

DAG 模式的画布操作：

- 工具栏支持“+ 添加节点”、“自动布局”、“适配画布”、“从 JSON 同步”四个动作。
- 拖动节点可调整位置；从节点右侧端口拖到目标节点左侧端口可创建依赖。
- 选中节点会在下方详情面板编辑标题、工具、条件、输入参数和依赖。
- 滚轮缩放、空白处拖动平移、右下角小地图点击跳转视口，便于浏览大型流程。
- JSON 视图折叠在“JSON 视图（高级）”下，专家用户可直接编辑。

### 5.3 工具范围

智能体可以调用内置工具、知识库工具和 MCP 工具。你可以限制工具范围：

- 不使用工具：只让模型根据输入内容回答。
- 仅内置工具：使用会话、知识库、模型状态等系统能力。
- 内置工具 + MCP：允许调用外部工具。
- 白名单工具：只允许选中的工具。

如果任务涉及敏感数据库或外部系统，建议只开放必要工具。

### 5.4 审批与风险确认

当智能体准备调用高风险 MCP 工具时，任务可能进入“等待审批”状态。

你可以：

- 查看工具名称、输入参数和风险说明。
- 批准执行。
- 拒绝执行。
- 停止任务。

不要批准看不懂或超出任务范围的工具调用。

### 5.5 查看任务结果

任务详情中可以查看：

- 任务时间轴：水平色块视图展示每一步的耗时占比和类型（规划、工具、控制、审批），失败步骤会用红色标出，悬停可看到工具名和毫秒数。
- 工具调用统计：按工具名聚合的调用次数、总耗时、平均耗时和失败次数，按调用次数排名。
- 每一步执行过程。
- 工具输入和输出。
- 错误信息。
- Token 用量。
- 最终答案。
- DAG 节点轨迹。

如果启用了流式 function calling（管理员通过环境变量开启），任务详情顶部会显示“流式生成”面板，实时展示当前正在累加的文本和工具调用增量。

任务完成后，可以把结果保存为产物，后续支持查看、备注、对比和回滚。

### 5.6 模板和计划任务

常用任务可以保存为模板：

1. 配好目标、模型、模式、模型路由和工具范围。
2. 保存为个人模板或共享模板。
3. 下次直接套用，模型路由策略会一并恢复。

周期性任务可以配置为计划：

- 每日任务。
- 每周任务。
- 手动触发任务。

无效模板或计划可以删除，避免列表堆积。

### 5.7 失败后处理

任务失败时：

- 先查看错误步骤。
- 如果是临时网络或模型异常，可以重试。
- 如果是工具参数错误，修改目标或工具配置后再运行。
- 如果支持断点续跑，可以从失败上下文继续。

运行中的任务不能直接重跑，需要先停止或等待完成。

## 6. 模型配置

模型配置一般由管理员完成。普通用户如果被允许，也可以维护自己的个人模型。

### 6.1 新增模型

1. 进入系统设置或模型管理。
2. 点击添加模型。
3. 填写接口地址 Endpoint。
4. 填写 API Key，如不需要可留空。
5. 填写模型展示名称。
6. 填写模型标识 Model ID。
7. 设置保存范围：个人模型或全局模型。
8. 按需设置可见部门、额度、温度、价格、Token 上限、健康检查 URL 和并发上限。
9. 标记模型能力，例如视觉输入、思考模型。
10. 测试连接。
11. 保存。

### 6.2 编辑模型

编辑时常见调整：

- 修改模型名称，便于用户识别。
- 修改 Endpoint 或 Model ID。
- 更新 API Key。
- 调整每日额度。
- 设置端点并发上限。
- 设置远端健康检查 URL。
- 开启或关闭视觉、推理能力标识。
- 调整可见部门。

保存后，用户侧模型选择器和智能体模型选择会同步更新。

### 6.3 模型不可用排查

如果模型调用失败：

- 先点击测试连接。
- 检查 Endpoint 是否以正确路径结尾，例如 `/v1`。
- 检查 API Key 是否有效。
- 检查 Model ID 是否与上游服务一致。
- 检查并发限制是否过低。
- 查看系统监控中的模型端点状态。
- 确认当前用户是否有模型权限。

## 7. 第三方 API Key

系统可提供 OpenAI-compatible API 给外部程序调用。

### 7.1 创建 API Key

1. 进入 API 接入或系统设置中的相关入口。
2. 创建新的 API Key。
3. 设置名称、权限和有效期。
4. 保存后复制密钥。

密钥通常只完整显示一次，请妥善保管。

### 7.2 使用 API Key

外部程序可以按 OpenAI-compatible 格式调用系统接口。具体地址和模型名以管理员配置为准。

使用建议：

- 给不同系统分别创建不同 Key。
- 不要把 Key 写入公开仓库。
- 不再使用时及时停用或删除。
- 管理员可查看调用记录和 Token 用量。

## 8. 状态与问题排查

如果你的账号可以看到系统状态或监控入口，可以用它辅助判断模型、知识库和工具是否正常。普通用户不需要维护这些指标，只需要知道异常时看哪里、该向谁反馈。

### 8.1 常见状态

- 活跃用户和调用量。
- 模型 Token 使用量。
- 模型端点健康状态。
- 并发、排队和熔断状态。
- RAG 检索耗时和命中情况。
- 慢查询和异常告警。
- Pivot 所在服务器的 GPU 与显存情况。

注意：GPU 区域只代表 Pivot 部署服务器本机 GPU。如果模型部署在远端服务器，需要结合远端健康检查或模型端点状态判断。

### 8.2 模型端点状态

模型端点状态只显示当前仍启用的模型配置。已删除或停用的模型不会继续显示。

重点关注：

- 健康检查是否成功。
- 当前并发是否达到上限。
- 队列是否持续增长。
- 是否进入熔断状态。
- 失败次数是否异常。

### 8.3 异常处理建议

- 排队过多：提高端点并发、增加模型实例或降低任务量。
- 端点熔断：检查上游模型服务是否可用。
- Token 消耗异常：检查是否有批量任务或第三方 API 大量调用。
- RAG 很慢：检查文档数量、召回参数和服务器负载。

## 9. 推荐工作流

### 9.1 普通问答

1. 选择合适模型。
2. 直接提问。
3. 需要引用内部资料时打开知识库。
4. 需要查询数据库或文件时打开 MCP。

### 9.2 内部资料问答

1. 上传资料到知识库。
2. 等待文档状态变为就绪。
3. 用召回测试验证能否命中。
4. 在对话中打开知识库提问。

### 9.3 数据分析报告

1. 配置数据库 MCP 或报表文件 MCP。
2. 在对话或智能体中启用 MCP。
3. 明确查询范围和输出格式。
4. 需要图表时调用可视化图表 MCP。
5. 需要固定格式时调用报告编排 MCP。

### 9.4 周期性任务

1. 在智能体中写清任务目标。
2. 选择模型、模式和工具范围。
3. 试运行一次。
4. 保存为模板。
5. 配置每日或每周计划。
6. 定期检查通知和产物。

## 10. 常见问题

### 10.1 模型回复慢

可能原因：

- 模型本身推理慢。
- 当前端点排队。
- 附件或知识库上下文较大。
- 智能体任务步骤较多。

处理方式：

- 换用更快模型。
- 减少附件和上下文。
- 查看系统监控中的端点并发和队列。
- 等待当前任务完成。

### 10.2 知识库回答不准确

处理方式：

- 检查文档是否就绪。
- 用召回测试查看命中文档。
- 调整问题关键词。
- 上传更清晰、更聚焦的资料。
- 调整 RAG 参数后重试。

### 10.3 MCP 无法调用

处理方式：

- 检查 MCP 服务是否启用。
- 刷新工具缓存。
- 查看对应 MCP 卡片中的调用记录。
- 检查连接地址、账号和网络。
- 数据库 MCP 使用只读账号并确认表权限。

### 10.4 智能体卡在等待审批

说明智能体准备调用需要确认的工具。

处理方式：

- 打开任务详情。
- 查看工具和参数。
- 确认合理后批准。
- 不合理则拒绝或停止任务。

### 10.5 附件无法读取

可能原因：

- 文件格式不支持。
- 文件过大。
- 文档加密但密码错误。
- 扫描件需要视觉模型。
- 图片过大被压缩或跳过。

处理方式：

- 换成 PDF、TXT、DOCX、XLSX、CSV 等常见格式。
- 拆分大文件。
- 选择支持视觉输入的模型。
- 重新上传清晰文件。

### 10.6 看不到某个模型或工具

可能原因：

- 当前账号无权限。
- 模型或 MCP 已停用。
- 只对指定部门可见。
- 个人资源属于其他用户。

处理方式：

- 联系管理员确认权限。
- 检查模型或 MCP 是否启用。
- 刷新页面后再试。

## 11. 使用安全建议

- 不要在普通对话中粘贴不必要的密码、密钥或身份证件信息。
- 上传内部资料前确认资料范围和保密要求。
- 使用 MCP 查询数据库时优先使用只读账号。
- 批准智能体工具调用前，先确认工具和参数是否符合任务目标。
- 第三方 API Key 不要共享给无关人员。
- 离职、项目结束或系统交接时，及时停用账号、计划任务和 API Key。

## 12. 快速检查清单

新用户第一次使用前：

- 已能登录系统。
- 已知道默认可用模型。
- 已能新建一次对话。
- 已了解知识库和 MCP 开关的用途。
- 已知道遇到模型不可用时联系谁。
