# 智枢（Pivot）使用帮助

智枢（Pivot）是面向组织内部的全场景智能协同与业务自动化中枢平台。它以统一的智能工作入口连接对话、专业应用、知识库、工具库和自动化流程，覆盖从信息理解、内容生产、数据分析到任务执行、流程编排和结果沉淀的完整工作链路，帮助组织把分散的知识、能力与业务流程转化为可协同、可复用、可持续优化的智能生产力。

智枢适合辅助完成材料总结、内容起草、表格分析、制度查询、图片文字识别、资料检索、多步骤任务和周期性流程等工作。使用过程中，系统会在当前账号的权限范围内提供模型、资料和工具；需要确认的操作会等待用户决定，重要事实、数字和正式成果仍应由人员复核。

### 核心能力

- **对话与内容处理**：通过自然语言完成问答、总结、改写、翻译、方案整理和材料分析，并可结合附件继续处理。
- **专业应用**：使用公文写作、数据分析、法规查询、文字识别和 PDF 工具处理步骤明确的业务工作。
- **知识库**：管理单位或个人资料，按专题库和标签缩小范围，让回答和自动化流程优先参考可靠的内部依据。
- **工具库**：在授权范围内使用数据源、文件处理、图表、报告和消息等能力，让智枢不仅提供建议，也能协助完成实际处理步骤。
- **自动化执行**：把稳定的处理步骤编排为可重复使用的工作流，并通过计划任务按设定时间自动执行，集中查看运行状态和结果。

### 选择合适的工作方式

- **普通会话**：适合即时问答、写作、总结和一次性处理，是大多数工作的首选入口。
- **应用**：适合公文、表格、法规、文字识别和 PDF 等已有明确操作流程的工作。
- **工作流**：适合步骤已经稳定、需要多人复用或反复运行的流程。
- **计划任务**：适合需要按固定时间自动运行，并持续查看结果和异常的工作。

本手册面向普通用户，按照实际页面说明从登录、对话和材料处理，到工作流、计划任务、知识库和工具使用的完整操作方法。不同账号看到的内容可能不同；如果某个菜单、模型、资料或工具没有显示，通常表示当前账号暂未获得相应权限，请联系系统管理员。

## 1. 快速开始

### 1.1 登录和退出

1. 打开单位提供的智枢访问入口。
2. 输入用户名和密码。
3. 点击“进入系统”。
4. 使用结束后，点击左侧底部的“退出”。

如果登录失败，请先检查用户名和密码是否正确，再确认当前设备能够访问单位提供的入口。仍无法登录时，把页面提示告知系统管理员。

### 1.2 认识左侧导航

首页左侧导航采用单层结构，不需要展开二级菜单。上方固定菜单按以下顺序排列：

- **搜索**：查找会话、工作流和相关运行记录。
- **应用**：打开公文写作、数据分析、法规查询、文字识别和 PDF 工具。
- **知识库**：管理资料，并检查资料是否能够被准确找到。
- **工具库**：查看和管理可用的数据源、工具及连接。
- **自动化**：进入自动化工作区。工作区内的“工作流”标签用于管理可复用流程，“计划任务”标签用于管理定时运行。

左侧栏中部是“最近会话”，用于快速回到最近打开的对话；底部是账号操作区：

- **帮助**：打开本使用手册。
- **设置**：普通账号进入“个人设置”，具备管理权限的账号会看到“系统设置”；具体标签按账号权限显示。
- **退出**：安全退出当前账号。

点击左上角的“+”可以随时发起新对话。

### 1.3 完成第一次对话

1. 点击左上角“+”新建会话。
2. 在输入框中写下问题或工作要求。
3. 在输入框右侧查看当前模型名称；需要更换时，点击模型名称选择其他模型。
4. 点击最右侧的发送箭头，或按 `Enter` 发送。
5. 需要在输入框中换行时，按 `Shift+Enter`。

模型名称位于发送箭头左侧。这里仅显示当前模型名称，点击后可以查看并切换可用模型。

### 1.4 在手机或窄屏设备上使用

窄屏下左侧栏默认收起。点击页面顶部的菜单按钮可以打开侧栏，选择页面后侧栏会收起；也可以点击侧栏右上角的关闭按钮。

如果按钮或内容显示不完整，建议先关闭侧栏，并保持浏览器使用默认缩放比例。

## 2. 会话与搜索

### 2.1 怎样写出清楚的要求

建议在输入中写清四件事：

- **背景**：这项工作与什么有关。
- **目标**：希望智枢完成什么。
- **材料**：需要参考哪些内容。
- **结果形式**：希望得到清单、表格、报告、邮件还是其他形式。

示例：

```text
请根据下面的会议记录整理行动项，按负责人分组，标明完成时间，并把未明确责任人的事项单独列出。
```

对于重要任务，可以补充字数、语气、时间范围、必须包含的内容和不能出现的内容。

### 2.2 选择模型

点击输入框右侧、发送箭头左侧的模型名称，可以选择当前账号可用的模型。

选择时可参考模型列表中的说明：

- 日常问答、总结和写作可选择响应较快的通用模型。
- 图片、截图或扫描件应选择能够阅读图片的模型。
- 复杂分析、规划和审查可选择更擅长推理的模型。

如果显示“暂无可用模型”，请联系系统管理员为当前账号开通模型。

### 2.3 上传附件与文件夹

点击输入框左下角的“+”按钮打开选项菜单，选择“文件和文件夹”，可以添加单个文件或选择整个文件夹上传；也可以直接将文件或文件夹拖入输入框，或在输入框中直接粘贴剪贴板中的图片与文件。支持图片、PDF、Word、Excel 和文本文件。

使用建议：

- 只上传与当前问题直接相关的材料。
- 支持上传整个文件夹，系统会自动保留有效文件夹层级。
- 文件名和目录尽量能够说明内容和日期。
- 扫描件或图片需要选择能够阅读图片的模型。
- 文件较大时可以先拆分，再分批发送。
- 加密文件请根据页面提示输入正确密码。
- 发送前检查附件是否已正确添加并解析。

### 2.4 让知识库参与回答

当问题需要参考单位内部资料时：

1. 点击输入框左下角的“+”按钮，点击“知识库”开启知识库参与模式。
2. 点击知识库右侧的“›”图标可展开筛选面板，按需搜索和选择专题库、标签，缩小资料检索范围。
3. 输入问题并发送。
4. 根据回答中的资料线索核对原文。

制度查询、手册问答、历史报告总结和基于内部材料起草内容，通常适合使用知识库。纯创作、翻译或闲聊通常不需要开启。

### 2.5 让工具库参与回答

当问题需要读取数据、生成图表、整理报告或发送通知时：

1. 点击输入框左下角的“+”按钮，点击“工具”开启工具库调用模式。
2. 点击工具右侧的“›”图标可展开工具配置面板，支持“模型自动选择”或“手动限制范围”（可搜索、全选或单独勾选允许调用的工具）。
3. 首次使用时阅读页面提示并确认。
4. 写清要处理的数据、范围和期望结果。
5. 发送后查看智枢准备使用的工具和处理结果。

工具调用需要确认时，请先核对工具名称、处理内容和数据范围。看不懂或超出当前工作范围时，应拒绝或停止。

### 2.6 普通会话的连续执行

普通会话自动连续 Agent 默认关闭。管理员在“系统设置 → 全局参数 → 智能体后台”启用后，包含分析、查询、整理、生成、执行等明确任务意图的问题才会进入连续 Agent；短且无任务意图的闲聊仍使用普通模型流。需要多轮规划、调用工具、读取资料或等待确认的问题，会在后台继续执行。较长消息会按照模型上下文预算处理，超出预算、超时或检测到重复调用时会尽量保留部分结果并提示限制。你可以离开当前会话、刷新页面，或在 Web 与 Electron 客户端之间切换，任务不会因为页面断开而丢失。

管理员可以在“系统设置 → 全局参数 → 智能体后台”关闭“普通会话自动连续 Agent”。关闭后普通会话恢复原有模型流式生成，不影响手工创建的智能体任务、工作流或计划任务。

执行期间，聊天中会显示规划、工具调用、等待确认和完成等状态。根据页面提供的操作，可以查看详情、停止任务、批准或拒绝高风险操作，也可以继续可恢复的中断任务。最终答案或失败原因会写回原会话，重新打开会话即可查看。

点击已有回答的“重新生成”时，系统会复用原来的用户问题创建新的连续 Agent，不会重复添加一条用户消息。图片附件会在服务端完成归属和安全检查后交给支持视觉的模型处理。

### 2.7 查看和压缩会话内容

输入区会显示当前会话的内容用量。对话很长时，可以点击该位置进行整理，减少较早内容对后续交流的影响。

整理前，建议先确认重要结论、数字和待办事项已经写入当前会话，或另行保存。

### 2.8 管理会话

最近会话显示在左侧导航下方。常用操作包括：

- 点击会话标题继续对话。
- 修改会话标题，方便以后查找。
- 添加标签，按项目、部门或主题整理。
- 归档暂时不用的会话。
- 删除不再需要的会话。
- 导出或打印需要留存的内容。

发起新对话不会删除旧会话，之后仍可从最近会话或搜索中打开。

### 2.9 使用全局搜索

1. 点击左侧“搜索”。
2. 输入标题、关键词或内容片段。
3. 默认搜索会话；需要时切换到“工作流”。
4. 点击结果，直接打开对应内容。

搜索不到时，可以缩短关键词、检查所选类别，或确认当前账号是否有权查看目标内容。

## 3. 应用中心

点击左侧“应用”进入应用中心。应用中心用于处理有明确业务步骤的工作，不需要在普通会话中反复说明流程。

### 3.1 公文写作

公文写作适合起草和修改通知、请示、报告、函、会议纪要、通报和意见。

常用流程：

1. 打开“公文写作”，新建一篇公文。
2. 选择文种和规范。
3. 填写写作要求，并添加原始材料。
4. 使用起草、润色、改写或审校。
5. 对建议逐条接受、拒绝、替换或转为批注。
6. 检查正文、发文要素和引用材料。
7. 保存阶段内容并导出需要的文件。

选中正文中的一段文字后，可以进行润色、压缩、扩写、正式化表达或按要求改写。重要公文导出前应由人员再次复核事实、数字、称谓和日期。

### 3.2 数据分析

数据分析适合处理 Excel、CSV 和其他表格数据。

常用流程：

1. 打开“数据分析”，导入数据文件。
2. 查看字段信息和数据预览，确认表头是否正确。
3. 使用查询、透视、图表或数据比对完成分析。
4. 在智能分析中写清问题、范围和关注指标。
5. 核对结果后导出表格、图片或报告材料。

对于只有几百行但包含大段文本的表格，不要把整列内容粘贴到普通 AI 输入框。进入“智能分析”的“全量语义分析任务”，选择文本字段，填写逐条分析要求，并启动任务。系统会按模型上下文预算自动分批处理全部记录，超长单元格也会继续分块，不做抽样；模型端点允许时会有限并行处理多个批次，并持续按已完成批次数更新进度。任务支持查看进度、取消、失败重试，完成后在当前区域显示全量汇总报告，并在历史记录中保存结果。

使用建议：

- 上传前尽量去掉合并单元格、空白表头和无关备注行。
- 编号、单号和账号通常不适合求和或平均。
- 金额、数量和日期格式不一致时，先整理数据再分析。
- 大表先缩小范围，避免一次展示过多内容。
- 长文本全量分析应使用全量语义分析任务；批次处理预算越小越稳但模型调用次数越多，任务完成前不要删除原数据集。
- 重要结论应回到原始数据核对。

### 3.3 法规查询

法规查询适合查找法律法规、制度文件和具体条文。

1. 打开“法规查询”。
2. 按关键词、分类或适用范围筛选文档。
3. 打开文档详情，核对当前状态、正文和来源文件。
4. 在条文检索中输入问题，查看命中的具体条文。
5. 使用问答功能获得解释，并回到条文原文复核。

法规问答适合作为检索和理解辅助，不能替代正式的法务意见。处理重要事项时，应以有效文件和单位正式意见为准。

### 3.4 文字识别

文字识别适合从图片或扫描 PDF 中提取文字。

1. 上传待识别文件。
2. 选择需要处理的页面。
3. 开始识别并查看结果。
4. 重点复核页面提示的疑似错误内容。
5. 确认后导出文字或结构化结果。

文件越清晰、页面越端正，识别效果通常越好。印章、手写内容、复杂表格和模糊扫描件需要重点人工核对。

### 3.5 PDF 工具

PDF 工具可以合并、拆分、旋转、删除和调整页面顺序，也可以导出页面图片或文字。

操作前先确认文件顺序和页面范围，导出后打开结果文件检查页数、方向和内容是否完整。

## 4. 知识库

知识库用于保存个人或团队资料，让会话和自动化流程能够先查资料再回答。

### 4.1 上传资料

1. 点击左侧“知识库”。
2. 点击“上传文档”。
3. 选择专题库和标签。
4. 选择文件或把文件拖入上传区。
5. 检查待上传清单后开始上传。
6. 等待文档状态变为“就绪”。

上传建议：

- 一个文件尽量围绕一个主题。
- 文件名包含清晰的主题和日期。
- 同一制度的不同内容应避免重复上传。
- 扫描件上传后重点检查文字是否能够被正确识别。
- 资料有变化时及时替换或停用旧文件。

### 4.2 理解文档状态

- **处理中**：正在读取资料，请稍后再用。
- **就绪**：可以参与检索和回答。
- **失败**：未能完成处理，可按页面提示重试。

文档列表中的“启用”开关是独立设置。关闭后文档仍会保留在知识库中，但不会参与新的检索；需要时可以重新启用。

问题回答不准确时，先确认相关文档是否已经就绪。

### 4.2.1 共享专题库

专题库所有者可以在共享设置中选择本单位、指定个人或（管理员）全体成员。共享给其他账号后，接收者只能读取专题库、文档、分块和检索结果；不能上传、编辑、启停、调整归属、删除文档，也不能修改专题库共享设置。页面中的“只读”标识表示当前账号没有管理权限。

### 4.3 使用专题库和标签

专题库适合按业务、部门、项目或制度体系划分资料；标签适合标记年份、地区、阶段和用途。

资料较多时，先选专题库，再选择标签。对话中缩小范围通常能减少无关资料干扰。

### 4.4 检查资料能否被找到

知识库页面中的“召回测试”用于检查一个问题能找到哪些资料。

1. 输入一个真实问题。
2. 选择专题库和标签范围。
3. 开始测试。
4. 查看命中的文档、内容片段、匹配分数和排序位置。
5. 根据测试结果调整关键词、专题库、标签或检索范围，再重新测试。

没有找到合适资料时，可以换用更贴近原文的关键词、检查资料范围，或确认相关文档是否已就绪。测试结果只用于检查检索效果，正式回答仍应回到来源文档核对。

### 4.5 使用知识图谱

知识图谱用于查看人物、机构、事项及它们之间的关系。可以搜索实体、点击关系和查看来源资料。

图谱适合发现线索，不应直接作为最终依据。涉及重要结论时，请回到来源文档核对。

## 5. 工具库

工具库集中管理数据源、处理工具和输出动作。会话和自动化流程可以按权限使用这里的能力。

### 5.1 认识工具库

常见内容包括：

- **数据管理**：进入数据分析，上传和管理数据集。
- **我的电脑**：在桌面客户端中选择允许使用的本机文件或数据范围。
- **数据源**：读取获准使用的业务数据和报表。
- **处理工具**：生成图表、整理报告、解析文档和处理数据。
- **输出动作**：把结果保存、导出或发送到单位内部渠道。

只使用与当前工作有关、来源可信且已经获得授权的内容。

### 5.2 使用个人连接

如果单位允许个人配置连接：

1. 在工具库中选择相应卡片。
2. 点击“配置”。
3. 按页面提示填写连接信息。
4. 先进行连接测试。
5. 保存后刷新工具列表。

连接信息不清楚时，不要猜测填写，请向数据提供方或系统管理员确认。

### 5.2.1 使用共享只读数据库能力

数据库连接所有者可以在分享设置中选择单位或指定个人。接收者能在工具库中查看共享服务器和工具列表，但只会看到治理允许的只读数据库工具；不能编辑、刷新、诊断、停用或删除连接，也不能调用写入类工具。需要写入或管理操作时，应联系连接所有者或系统管理员。

### 5.3 在会话或自动化流程中使用工具

- 普通会话：打开输入区的工具库按钮后再发送问题。
- 自动化流程：在工作流步骤中选择需要的工具；计划任务会按关联工作流的配置执行。

描述处理目标时，应写清数据范围、筛选条件、时间范围和期望结果，避免使用“查一下”“全部处理”等过于宽泛的表达。

### 5.4 工具调用注意事项

- 首次使用陌生工具时，先用少量数据测试。
- 看到确认请求时，先检查操作内容和范围。
- 不要处理无权查看的数据。
- 不要把重要结果直接发送给他人，先人工核对。
- 工具结果为空时，先检查条件和数据范围，不要立即重复大量运行。

## 6. 自动化

自动化工作区用于运行一次性多步骤任务、管理可重复使用的工作流和维护计划任务。

### 6.1 查看自动化资产

点击左侧“自动化”后，使用顶部标签切换不同内容：

- **运行记录与新建任务**：新建一次性多步骤任务，并集中查看自主任务、工作流任务和计划运行记录。
- **工作流**：已经保存的处理流程及其状态。
- **计划任务**：按照设定时间运行的流程。

可以从列表中编辑、运行、查看历史或创建计划。只有在新建或编辑工作流时，才会进入可视化编排页面。

工作流发布后，工作流列表或工作流详情中的“自动启动”用于管理该工作流的事件触发方式；自动化页面顶部的“凭据库”用于集中管理多个工作流可以引用的受控凭据。计划任务只负责按时间安排运行，不包含事件触发器或凭据管理。

### 6.1.1 运行能力与诊断

自动化首页的“能力与结果”区域包含“运行环境”，用于管理任务所需的运行能力和常驻实例：

- **技能**：可以注册个人技能或导入技能包。注册后的技能会出现在“新建任务”的“执行技能”下拉框中；停用技能不会删除历史运行记录。
- **运行资源**：浏览器和数据处理资源由服务端统一维护。只有管理员可以同步资源，普通用户可以查看当前是否就绪。
- **常驻实例**：可以查看当前账号的上下文常驻实例、最近访问时间和过期时间，也可以驱逐单个实例或清理过期实例；超级管理员可切换到全部用户范围。

打开某条任务的“详情”后，展开“运行诊断”，可以查看上下文变化、运行状态快照、资源使用情况和任务控制消息。这些信息用于解释任务为什么等待、恢复或失败，一般不需要手工修改。

### 6.2 新建一次性任务

1. 在自动化首页点击“新建任务”。
2. 直接输入明确的任务目标，或使用“风险总结”“待办整理”“资料检查”快速填入常用目标。快捷按钮只负责填写建议内容，不会立即运行。
3. 选择模型，并根据任务复杂度选择标准、深度或审查模式；需要调用工具库时保持对应开关开启。
4. 根据需要展开高级设置，选择工具审批、模型路由、失败重试、上下文来源和补充说明。
5. 检查配置后启动任务；需要以后复用时，点击弹窗左下角“保存为模板”并填写模板名称。

“最大执行轮次”通常保持留空即可。系统会按模式自动设置：标准模式 30 轮，深度模式 50 轮，审计模式 60 轮。只有在确实需要更严格控制执行时间时才填写固定值，填写值不能超过当前模式上限；达到上限、预算或超时后系统会优先基于已有结果生成部分总结，并提示结果可能不完整。

“单次任务模型用量上限”累计计算模型读取内容和生成内容的用量，留空或填写 `0` 表示不限。桌面端它与“上下文来源”并排显示，手机等窄屏设备会自动上下排列。该上限适合成本敏感或输入规模明确的任务，不建议设置得过低。

保存任务模板时，留空的最大执行轮次会继续保存为“自动”，应用模板后仍按运行模式选择 20 或 50 轮，不会恢复为固定 10 轮。

### 6.3 创建工作流

1. 点击“新建工作流”。
2. 添加需要的处理步骤。
3. 按实际顺序连接步骤。
4. 为每一步填写输入、工具和失败处理方式。
5. 保存草稿并进行试运行。
6. 检查结果无误后发布。

工作流应尽量做到每一步职责单一、输入来源明确、结果可以核对。复杂流程可以先保存草稿并试运行，再根据验证结果整理为正式工作流。

新建工作流时，添加第一个节点后画布会自动把节点放到合适的可视区域并放大显示，方便直接开始配置；后续添加节点不会自动打乱已经调整好的位置。节点列表默认展示最常用的输入、模型、条件、审批、输出、检索、报表和图表节点，需要代码、循环、网络请求、浏览器或数据处理等较少使用的能力时，再展开“高级节点”或使用搜索。

点击“配置参数”后，向导会优先展示完成当前步骤所需的主要字段；不常修改的模型参数、超时、并发和其他技术选项会放在“更多设置”中。条件节点只会显示当前条件需要的输入，延时填写秒数即可。完成配置后点击“应用到画布”，再点击页面顶部“保存”才能保存工作流。

点击节点配置中的“应用到画布”后，窗口会关闭并提示参数已经写入画布；这一步只更新当前编辑内容，仍需点击顶部“保存”才会持久化。重新打开工作流或节点配置时，系统会恢复已保存的手动参数、输入引用和输出契约。如果画布刷新失败，页面会保留参数并显示警告，应在确认画布内容后再保存。

预设节点较多时，可以直接在节点列表内部上下滚动，画布位置不会因此移动。配置只读数据库查询节点时，可先点击“读取数据表”，选择数据表后再点击“读取字段”。按钮下方会显示请求是否已经触发、读取进度、成功数量或具体失败原因。

在筛选条件区域，可以选择“全部满足 AND”或“任一满足 OR”来决定多个条件的组合方式。读取到字段类型后，日期、日期时间和时间字段会显示对应的选择控件；选择“介于”时填写起止值，选择“属于列表”时用英文逗号分隔多个值。

对于日期和日期时间字段，筛选运算符还支持“当天”“早于当天”和“晚于当天”，系统会在查询执行时按数据库当天日期动态比较，不需要每天手动修改日期。纯时间字段没有日期关系，因此不提供这些选项。

数据库读取请求由智枢服务端发起，不是由当前浏览器直接连接数据库。连接地址填写 `localhost` 时，它表示智枢服务端或容器所在设备；如果数据库位于用户自己的电脑，应使用服务端能够访问且单位安全策略允许的地址。页面提示安全策略拦截、连接超时、拒绝连接或认证失败时，应按提示检查地址、端口、网络和账号权限，不要反复点击按钮。

工作流名称可以在编排页面标题旁点击编辑图标修改。节点名称应先完整输入，再按 Enter 或点击其他位置提交；输入过程中不会逐字保存，也不需要每输入一个字就重新点击编辑框。名称和节点修改完成后，仍需按页面保存流程持久化工作流。

### 6.3.1 设置输出模式和结果契约

在大模型节点或工作流输出节点中，可以在“输出模式”选择：

- **格式化文本**：适合报告、说明和需要阅读的结果。
- **纯文本**：适合继续拼接、发送到其他文本处理步骤。
- **结构化数据**：适合让后续节点按字段继续处理。

选择“结构化数据”后，点击“设置字段”可以定义字段名称、类型、说明和必填状态，也可以继续添加嵌套对象或数组项。右侧会显示输出预览和示例校验结果；字段定义完成后，后续节点的输入建议会出现可直接选择的字段路径，例如 `{{nodes.extract.output.customer.name}}`。

工作流输出节点还可以选择增强交付方式：

- **默认结果**：保留原始结构化结果。
- **表格**：把数组或包含行数据的结果按列展示，适合查看清单和统计结果。
- **文件产物**：展示文件名称和可下载引用，适合传递报告、导出文件或其他生成文件。

建议先试运行并检查输出预览，再发布工作流。表格数据和文件引用仍然会保留在结果中，便于后续节点继续使用。

### 6.3.2 新闻内容校对工作流

需要审核网站新闻时，建议按“只读数据库查询 → 富文本内容校对 → 工作流输出”编排：

1. 数据库查询节点只返回待审核新闻的编号、标题和正文，并把结果传给校对节点的 `records` 输入，优先使用 `{{nodes.查询节点.output.structuredContent.rows}}`。
2. 校对节点的“校对模型”字段只能选择当前账号可用的文本模型；`records` 支持结构化结果、记录数组和上游变量，不需要手工拼接普通字符串。
3. 校对节点会去除 HTML 标签、脚本、样式、实体和无关空白，保留标题与正文段落，再逐条检查错别字、漏字、标点和明显表述问题。
4. 多条新闻会分别输出记录状态、问题字段、原文片段、修改建议和原因；没有发现问题时显示“未发现明确的文字问题”，校对未完成时不会误报为通过。
5. 长正文会自动分块并保留少量相邻上下文。查询节点如果返回结果已被上游限制或截断，工作流会显示“不完整”及原因，需要先调大查询/模型输出限制或缩小查询范围。
6. 可在工作流输出节点选择“表格”查看问题清单，并保存 Markdown 校对报告作为结果产物，避免页面摘要过短造成误解。

自由任务适合先验证查询字段和校对要求；将符合“新闻/内容校对”目标的自由任务转换为工作流后，系统会自动补齐上述三个节点和数据引用。发布前请先试运行并抽查原文，确认无误后再创建计划任务。

### 6.4 运行和发布工作流

运行详情会优先展示任务状态、最终结果和总耗时。每个执行步骤默认使用中文标题和可读摘要；数据库、文件等工具返回的结构化内容会整理成表格或列表，较长结果只展示必要部分。需要排查问题时，可以展开“查看原始数据”核对完整信息。

工具调用过程中的结束原因、工具名称和参数都会使用中文说明。页面中的“任务规划”表示系统正在安排后续步骤，“调用工具”表示正在使用已授权的数据源或能力；工具内部标识仅在原始数据中保留，不影响普通查看。

- **保存**：保留当前编辑内容。
- **试运行**：用实际或示例输入检查流程。
- **发布**：把经过确认的流程作为可正式运行的版本。
- **查看历史**：比较之前保存的内容，需要时恢复。

修改已在使用的工作流时，建议先保存为草稿并试运行，不要直接覆盖仍在执行的重要流程。

### 6.4.1 运行共享工作流

共享工作流接收者只能看到所有者已发布的版本，不能编辑、保存、发布、回滚、删除或创建计划。首次运行带有模型、工具或凭据依赖的共享工作流时，点击“配置依赖”，为每项依赖选择当前账号可用的等价资源后确认；系统保存的是授权引用，凭据明文不会展示。

所有者发布新版本后，旧的依赖映射会标记为过期并阻断运行。重新打开“配置依赖”并确认新版本映射后，才能继续执行。

### 6.5 创建计划任务

1. 在自动化工作区切换到“计划任务”标签。
2. 点击“新建计划”，选择已经发布的工作流。
3. 选择每日、每周或“按间隔”执行；按间隔模式可设置每 5 至 1440 分钟运行一次，满足一天多次执行。
4. 填写运行所需内容。
5. 保存并检查下次运行时间。

计划创建后，可以暂停、恢复、立即运行或删除。建议定期查看计划运行记录，确认结果是否仍符合业务需要。

计划任务只能关联已经发布的工作流。正在编辑的草稿、未发布版本或无权访问的工作流不会进入可选列表；修改流程后，应先保存并发布新版本，再检查计划的下次执行时间。

## 7. 设置与长期记忆

### 7.1 打开设置

点击左侧底部“设置”。普通账号会进入“个人设置”，具备管理权限的账号会进入“系统设置”；页面标题和标签数量会随账号权限变化。

普通账号通常可以看到以下标签：

- **用量统计**：查看消息、模型和近期使用情况的汇总。
- **用量明细**：按时间、模型和消息查看详细用量，并按页面提供的入口导出。
- **模型配置**：管理当前账号可以使用或维护的模型配置；模型是否可见以页面权限为准。
- **长期记忆**：查看、筛选、修正和删除跨会话保留的信息。
- **附件库**：搜索和管理已上传的文件，查看文件与会话的关联关系。
- **外部调用**：单位开放相关能力时，可按页面提示创建和管理个人调用凭据。
- **账号安全**：修改当前登录密码。

具备管理权限的账号还可能看到数据概览、系统监控、全局参数、工具策略、公告管理、用户管理、审计日志和审计报表等标签。如果某项设置没有显示，表示该选项由单位统一管理或当前账号没有权限。

### 7.2 使用长期记忆

长期记忆可以帮助新会话继续参考你的偏好、项目事实、长期决定和历史片段。它不会替代当前对话中的明确要求，重要项目变化后应及时检查旧内容。

1. 在“个人设置”或“系统设置”中打开“长期记忆”标签。
2. 开启长期记忆。
3. 在后续对话中正常工作，系统会根据内容形成可复用记忆。
4. 使用搜索和类型筛选检查记忆内容，修正、禁用或删除不准确的信息。
5. 需要留存时使用“导出”；发现重复内容时先查看“合并建议”再处理。

不要在对话中主动提供密码、密钥、银行卡号和证件号码等敏感信息。重要项目变化后，应及时检查旧记忆是否仍然有效。

## 8. 常见问题

### 8.1 看不到模型、工具、资料或菜单

当前账号可能没有相应权限，或者相关内容已经停用。刷新页面后仍未出现时，请联系系统管理员。

### 8.2 模型回复较慢

复杂问题、附件较多、参考资料较多或任务正在排队时，等待时间会增加。可以减少无关附件、缩小资料范围，或选择更适合日常问答的模型。

### 8.3 附件无法读取

检查文件是否损坏、是否加密、大小是否超出页面提示，以及扫描件是否使用了能够阅读图片的模型。必要时把大文件拆分或转换为常见格式。

### 8.4 知识库回答不准确

确认文档已经就绪，缩小专题库和标签范围，用“召回测试”检查实际命中的资料，并换用更贴近原文的关键词。

### 8.5 工具无法调用

确认输入区的工具库按钮已经开启，所需工具处于可用状态，并检查选择的数据范围。仍失败时，把页面提示和操作时间告知系统管理员。

### 8.6 自动化流程一直等待确认

打开对应的工作流运行详情，查看待确认操作。确认内容合理后批准；内容不清楚、范围过大或可能涉及敏感数据时拒绝。

### 8.7 找不到之前的会话

先查看左侧最近会话，再使用“搜索”按标题或内容关键词查找。仍找不到时，确认会话是否被归档或删除。

### 8.8 手机端看不到左侧菜单

点击页面顶部的菜单按钮打开侧栏。进入目标页面后，侧栏会自动收起，避免遮挡内容。

### 8.9 页面显示异常

先刷新页面。如果刚完成系统维护仍显示旧内容，可以关闭并重新打开页面。问题持续存在时，记录出现问题的页面、操作步骤和提示文字，再联系系统管理员。

## 9. 使用建议

- 重要事实、数字和结论应回到来源资料核对。
- 涉及法规、财务、人事、安全和对外发布的内容必须人工复核。
- 只上传工作所需资料，不在会话中提供密码等敏感信息。
- 工具调用和自动化流程确认前先检查范围，不批准看不懂的操作。
- 工作流先小范围试运行，确认结果后再扩大范围。
- 可重复的成熟流程再保存为自动化，避免过早固化错误步骤。
- 使用结束后及时保存需要留存的结果，并安全退出账号。
