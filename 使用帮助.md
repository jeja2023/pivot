# 智枢（Pivot）使用帮助

适用版本：`v0.0.156`  
更新时间：2026-06-24

智枢（Pivot）是一套面向内网、私有化和离线化场景的 AI 工作平台。它把日常对话、知识库检索、工具箱调用、应用中心、自由任务、工作流编排、第三方 API、系统监控和权限治理集中到一个入口中，让用户可以在可控、可审计的环境里使用大模型。

本帮助面向普通用户、业务管理员和系统管理员。普通用户重点阅读登录、对话、知识库、工具箱、应用中心和常见问题；管理员还应阅读模型配置、全局参数、API Key、工具策略和系统状态章节。

## 1. 登录与界面

### 1.1 登录系统

1. 打开管理员提供的访问地址。
2. 输入用户名和密码。
3. 登录成功后进入主界面。
4. 首次登录后，建议在左下角用户区检查账号信息，并按单位要求修改密码。

如果无法登录，先确认：

- 用户名和密码是否正确。
- 浏览器是否能访问服务器地址。
- 账号是否被管理员停用。
- 系统服务是否正在运行。
- 如果开启了开放注册或单位账号管理，请确认注册入口和账号审核状态。

### 1.2 主界面区域

- 左侧栏：会话列表、会话搜索、应用中心、知识库、工具箱、自由任务、工作流和系统设置等入口。
- 中间区域：当前对话、工作台或设置页面。
- 输入区：输入问题、上传附件、启用知识库或工具箱。
- 右上角模型选择器：选择本轮对话使用的模型。
- 左下角用户区：账号、管理员标识、帮助、设置和退出登录。

普通用户通常只看到自己有权限使用的模型、会话、知识库、工具箱连接、自由任务和工作流。内置用户名 `admin` 是超级管理员，拥有全局配置、审计和治理能力。

### 1.3 帮助入口

点击左下角“帮助”可以在应用内打开本帮助。直接访问 `/manual` 也可以独立查看同一份内容。为兼容旧链接，浏览器路径仍保留 `/manual`，但页面标题和前端入口统一显示为“帮助”。

## 2. 公告与通知

公告用于发布系统维护、安全提醒、知识库变更和使用规范。公告分为登录页公告和登录后的站内公告。

### 2.1 登录页公告

登录页公告会在未登录状态下展示，适合提示维护窗口、登录说明或紧急通知。

注意事项：

- 登录页公告只展示管理员设置为“登录页展示”的全员公告。
- 定向到单位、角色或指定用户的公告不会公开显示在登录页。
- 登录后点击“不再提示”只影响当前用户的站内公告，不会关闭登录页公告。
- 如需关闭登录页公告，需要管理员撤下公告、取消“登录页展示”或设置结束时间。

### 2.2 站内公告

登录后，公告入口位于左侧系统名称旁。入口出现红点时，表示当前账号有未读公告。

用户可以：

- 打开公告中心查看公告内容。
- 对普通公告点击“不再提示”。
- 对需要确认的公告点击“确认”。

需要确认的公告在确认前不会提供“不再提示”按钮。公告的可见范围由状态、开始时间、结束时间和投放范围共同决定。

### 2.3 管理员发布公告

管理员可在系统设置中进入“公告管理”，支持新建、编辑、撤下、删除、搜索和查看详情。常用字段包括标题、内容、类型、优先级、投放范围、开始时间、结束时间、状态、是否需要确认和是否登录页展示。

权限边界：

- 超级管理员 `admin` 可以发布全员、单位、角色、指定用户公告，也可以设置登录页公告。
- 普通管理员通常只能管理自己创建且面向本单位的公告。
- 登录页公告必须面向全员。
- 删除公告采用软删除，便于审计追踪。

## 3. 日常对话

### 3.1 新建对话

1. 点击左侧新建会话。
2. 在右上角选择模型。
3. 在输入框中输入问题。
4. 点击发送，等待模型回复。

建议把背景、目标、输出格式和限制写清楚，例如：

```text
请根据下面的会议记录，整理一份不超过 500 字的行动项清单，按负责人分组。
```

### 3.2 选择模型

模型选择器会展示当前用户可用的模型。不同模型能力不同：

- 文本模型：适合问答、总结、写作、代码和常规分析。
- 视觉模型：可以处理图片、截图和扫描件。
- 推理模型：适合复杂分析、规划和审查，响应可能更慢。
- 个人模型：只对创建者可见。
- 全局或部门模型：由管理员配置并按权限开放。

如果模型不可用，可能是模型被停用、端点异常、额度用尽、当前账号无权限或端点正在熔断排队。

### 3.3 思考模型与普通聊天

部分推理模型会产生思考过程。系统当前按模型配置决定普通聊天是否保留思考内容。管理员在模型配置中可设置“聊天中开启思考”：

- 开启：普通聊天可能展示推理模型返回的思考内容。
- 关闭：普通聊天会隐藏思考过程，并尽量只显示最终答案。

工具型场景（例如公文写作、部分应用中心 AI 调用）通常会自动关闭思考过程，以避免把思维链混入正式内容。

### 3.4 上传附件

点击输入区附件按钮可上传图片或文档。常见支持格式包括：

- 图片：`png`、`jpg`、`jpeg`、`gif`、`webp`、`bmp`
- 文档：`pdf`、`txt`、`md`、`csv`、`doc`、`docx`、`xls`、`xlsx`

使用建议：

- 文本型 PDF、Word、Excel 会尽量抽取文本后发送给模型。
- 扫描 PDF 和图片需要选择支持视觉输入的模型。
- 单轮附件不要过多，优先上传与问题直接相关的材料。
- 加密文档需要按提示输入密码。
- 大文件可能受管理员配置的上传大小、图片大小和上下文长度限制。

### 3.5 使用知识库回答

如果问题需要引用内部资料：

1. 在输入区打开“知识库”开关。
2. 可选择专题库和标签范围；选择某个专题库后，标签会联动缩小到该专题库内。
3. 输入问题并发送。
4. 系统会先在指定范围内检索资料，再把相关内容交给模型回答。

适合使用知识库的场景：制度查询、手册问答、历史报告总结、根据内部资料生成说明。不适合纯创作、闲聊、翻译或资料尚未索引完成的场景。

如果向量服务临时不可用，系统仍会尽量使用文本分片、FTS/LIKE 和关键词评分兜底，但高质量语义召回仍建议配置可用的 Embedding 模型。

### 3.6 使用工具箱

如果问题需要查询数据库、读取报表、生成图表、编排报告或发送通知：

1. 在输入区打开“工具箱”开关。
2. 首次使用时阅读风险提示并确认。
3. 描述要查询或处理的内容。
4. 系统会规划可用工具并返回结果。

示例：

```text
使用数据库连接查询 2026 年 5 月销售额最高的 10 个客户，并生成一张汇总表。
```

如果工具需要审批，任务会暂停等待确认。不要批准看不懂、超出任务范围或可能访问敏感数据的工具调用。

### 3.7 套用角色与规范

如果需要固定本轮对话的角色、输出格式或处理方法，可以在输入区点击“套用角色与规范”。

常见用法：

- 角色类规范：让模型按指定身份回答。
- 输出规范：把回答固定为表格、清单、报告或审查意见。
- 任务方法：让模型按固定分析步骤处理。

套用后，规范会写入当前会话的系统提示词，只影响当前会话后续回答。管理员可在系统设置的“角色与规范库”中维护可复用规范，并设置适用于聊天、自由任务或工作流。

### 3.8 管理会话

常用操作：

- 重命名会话：在会话菜单中修改标题。
- 搜索会话：按标题、消息内容或标签检索。
- 标签管理：给项目、部门或主题相关会话添加标签。
- 归档会话：把不常用会话移出默认列表。
- 删除会话：删除后进入软删除记录，按审计规则保留。
- 导出会话：导出为 Markdown。
- 打印 / 导出 PDF：打开打印预览后，通过浏览器打印对话框保存为 PDF。

## 4. 应用中心

应用中心承载面向具体业务场景的工作台。当前内置两个应用：

- 公文写作：辅助起草、润色、局部修改、审校、版本对比和导出公文。
- 数据分析：上传表格数据，完成字段画像、数据查询、数据透视、图表生成、数据比对和智能分析。

### 4.1 打开应用中心

1. 点击左侧“应用中心”。
2. 点击应用卡片进入对应工作台。
3. 刷新页面后，系统会尽量恢复到当前应用页面。
4. 点击顶部“返回应用中心”可回到应用卡片列表。

## 5. 公文写作

公文写作适合处理通知、请示、报告、函、会议纪要、通报、意见和其他公文。它围绕正式写作流程组织：顶部发起 AI 操作，左侧管理资料与文档，中间编辑正文，右侧集中处理审改结果。

### 5.1 页面布局

- 顶部工具栏：选择文种、规范库、材料来源、正文模式或对照模式，填写“AI指令”，执行“全文AI”的起草、润色、改写和审校。
- 左侧资料栏：管理“我的公文”，查看大纲、材料、模板、历史和规范。
- 中间编辑区：正文模式用于正式写作；对照模式用于原文和正文稿并排审改。
- 右侧审改栏：修改建议、发文要素、批注、版本、规范检查、校对和材料引用。
- 更多菜单：导出文本、复制提示词、带入聊天、导出 DOCX、导出红头 DOCX、导出 Markdown、导出版式文本、导出 PDF、导出备份、保存阶段版本和清空。

### 5.2 多文档管理

左侧“我的公文”是文档库，每篇公文独立保存正文、原文、版本、批注、建议和发文要素。

- 新建：点击“新建公文”。
- 打开：点击文档标题切换。
- 重命名：点击文档右侧“改名”。
- 删除：点击“删除”，有内容的公文会二次确认。
- 自动迁移：旧版本单文档数据首次进入时会迁移为文档库中的第一篇。

### 5.3 起草与修改

常用流程：

1. 选择文种，例如通知、请示、报告或函。
2. 选择规范库，例如通用公文规范、党政机关公文格式、会议纪要规范或单位内部规范。
3. 在“AI指令”中填写语气、结构、字数、重点和限制。
4. 如有原始材料，在对照模式中粘贴到“原文”。
5. 点击“全文AI”中的起草、润色、改写或审校。

公文写作 AI 调用由后端统一处理提示词、模型权限、配额、上下文预算、上游转发和审计。系统会按“显式选择模型 → 个人默认模型 → 系统默认模型”的顺序兜底；均不可用时会给出明确提示。

当前起草、润色、改写和选区处理支持流式生成，生成期间会先显示占位建议卡片，完成后再恢复替换、插入等操作按钮。审校需要解析结构化 JSON，因此保持完整返回后再展示。

### 5.4 选区工具与逐句改写

选中正文或原文片段后会出现浮动工具条：

- 润色：优化局部表达。
- 压缩：缩短选区内容。
- 扩写：补充细节。
- 公文语气：转换为更正式的公文表达。
- 逐句改写：流式生成改写结果，并按句子做红绿对比。
- 批注：把选区转为批注意见。

逐句改写完成后，可以逐句选择“接受改写”或“保留原文”，也可以全部接受或全部保留。点击“应用所选”后，系统会把选择结果替换原选区。

### 5.5 发文要素

右侧“发文要素”用于填写导出时参与排版的版头和版记：

- 版头：密级与保密期限、紧急程度、发文字号、签发人。
- 版记：抄送机关、印发机关、印发日期。

这些字段不直接改变正文编辑区，但会用于 DOCX、红头 DOCX 和带版式文本导出。其中“印发机关”会作为红头模板机关名称。

### 5.6 审改、批注、版本与校对

右侧审改栏的各页用途如下：

- 修改建议：展示 AI 或系统生成的建议，可替换、插入、转批注、生成版本、接受或拒绝。
- 批注：对原文或正文稿添加局部意见。
- 版本：保存当前正文稿，选择基准和目标后对比差异；点击历史版本可载入正文稿。
- 规范检查：按所选规范库检查标题、日期、待补充项、事实依据和公文要素风险。
- 校对：检查易错词、敏感与绝对化措辞、标点规范、标题与文种一致性。
- 材料引用：查看正文稿与原文材料之间的引用关系，支持对改写后的引用做模糊匹配。

误操作时可用顶部“撤销”“重做”，也支持 `Ctrl+Z` 和 `Ctrl+Y`。

### 5.7 历史与规范检索

左侧“历史”和“规范”标签接入知识库检索：

1. 输入关键词。
2. 选择专题库范围。
3. 点击“检索”。
4. 对结果点击“引用到正文”或“作为依据”。

建议把历史公文、制度条文和单位规范先上传到知识库，并按专题库/标签整理，便于公文写作中快速引用。

### 5.8 导出与备份

“更多”菜单支持：

- 导出文本：导出纯文本正文。
- 导出 DOCX：按公文结构生成 Word 文件。
- 导出红头 DOCX：带红色机关名称和分隔线。
- 导出 Markdown：按标题层级输出 Markdown。
- 导出版式文本：保留标题居中、落款右对齐等文本版式。
- 导出 PDF：打开打印窗口后另存为 PDF。
- 导出备份：导出完整工作区 JSON。
- 送审版、领导修改版、定稿版：快速保存阶段版本。

当本地内容较多接近存储上限时，系统会优先精简可重建的自动草稿和历史版本，保留正文、原文、批注和建议。重要工作建议定期导出备份。

## 6. 数据分析

数据分析工作台面向表格类数据处理。它使用 SQLite 保存数据集元信息，使用 Parquet 保存原始数据，使用 DuckDB 执行查询、画像、透视、图表和导出。

### 6.1 导入数据集

进入“应用中心 > 数据分析”后，可在“数据总览”中导入数据：

- 上传 Excel / CSV：支持 CSV、XLS、XLSX。
- 从数据库导入：选择已配置的数据库连接，输入只读 SQL 或表名，导入为新的数据集。
- 刷新列表：同步当前数据集列表。

CSV 会优先走 DuckDB 原生导入路径，减少大文件导入内存占用。Excel 文件会解析后同样生成字段画像。

导入后，数据集表格会显示序号、数据集名称、原始文件名、数据大小、文件类型、导入时间和操作。

### 6.2 数据总览

数据总览用于管理数据集和查看基础信息：

- 查看数据集列表。
- 打开详情和预览数据。
- 导出原始数据集。
- 删除不再使用的数据集。
- 查看字段画像、行数、列数和基础统计。

字段画像会识别字段类型、填充率、去重数、Top 值、数值统计和日期形态。成绩、金额、数量等数值指标会尽量按数值处理；学生编号、ID、单号、账号等编号字段会按维度处理，避免被误当作可求和指标。

### 6.3 图表生成

“图表生成”适合快速做可视化：

1. 选择分析数据集。
2. 选择分类字段、数值字段和可选分组字段。
3. 选择聚合方式：求和、计数、平均、最小或最大。
4. 选择柱状图、折线图、面积图或饼图。
5. 设置显示前几位、排序和配色。
6. 点击“生成图表”。

图表生成后可保存为图片，也可导出图表数据 CSV。生成记录会进入“历史记录”。

### 6.4 数据比对

“数据比对”用于比较两个数据集的差异：

1. 选择左侧数据集和右侧数据集。
2. 分别选择主键字段。
3. 选择对比字段。
4. 点击“开始比对”。

结果会展示新增、删除、变化和一致情况，并提供导出结果入口。适合月度数据核对、导入前后差异确认和系统迁移校验。

### 6.5 数据查询

“数据查询”提供两种模式：

- 可视化查询：通过筛选条件、条件关系、排序字段、排序方式和限制行数快速查询。
- SQL 查询（高级）：编写只读 SQL 查询。

SQL 查询规则：

- 表名固定为 `data`。
- 列名使用数据集字段名。
- 只支持读取类查询，例如 `SELECT` 或 `WITH`。
- 系统会拦截写入、DDL、外部文件访问和多语句等高风险操作。
- 查询结果会受行数上限保护。

示例：

```sql
SELECT 部门, SUM(金额) AS 总金额
FROM data
GROUP BY 部门
ORDER BY 总金额 DESC
LIMIT 20
```

### 6.6 数据透视

“数据透视”用于生成交叉汇总表：

1. 选择分析数据集。
2. 选择行维度、列维度和值字段。
3. 选择聚合方式：求和、计数、平均、最大或最小。
4. 设置排序、占比显示、行 Top N、列 Top N 和空值名称。
5. 可点击“推荐配置”让系统选择合适字段。
6. 点击“开始透视”。
7. 需要留档时点击“导出 CSV”。

透视结果会展示轻量摘要、Top 行项、Top 列项和交叉表。当前版本优化了透视区布局，常用操作集中在配置区，减少滚动寻找按钮。

### 6.7 智能分析

“智能分析”用于让模型基于当前数据集给出解释、建议或报告摘要。

使用方式：

1. 选择分析数据集。
2. 在输入框写清要分析的问题。
3. 如只需要普通解释，直接点击“生成建议”。
4. 如需要模型自行查询数据或生成图表，勾选“深度分析（可查询数据 / 生成图表）”。
5. 查看最终回答、执行过程和生成图表。

普通智能分析会流式输出回答。深度分析会使用 ReAct 工具调用方式，模型可以调用只读 SQL 查询和图表生成工具；完成后会展示最终回答、执行步骤、成功/失败状态和图表。当前版本已优化深度分析结果宽度和错误摘要，SQL 类型错误会显示更易理解的短提示。

### 6.8 历史记录与导出

“历史记录”会记录图表、数据透视、比对、查询和导出。图表记录可重新打开查看。

数据分析支持多种导出方式：

- 数据集导出：CSV、XLSX、Parquet。
- 查询结果导出：CSV。
- 透视结果导出：CSV。
- 图表数据导出：CSV。
- 图表图片下载：PNG。
- 比对结果导出：CSV。

CSV 会加入 UTF-8 BOM，方便 Windows Excel 正确打开中文。

### 6.9 数据分析使用建议

- 上传前尽量清理表头，避免多行表头、合并单元格和备注行。
- 编号、单号、账号等字段不要拿来求和或平均。
- 金额、成绩、数量等字段如果被识别为文本，可在查询中使用 `TRY_CAST`。
- 大表查询优先限制行数、使用 Top N，并减少无关字段。
- 深度分析适合探索问题，不要把未核验的 AI 结论直接作为最终报表结论。

## 7. 知识库

知识库用于存放个人或团队资料，让模型回答前先检索可信内容。

### 7.1 上传文档

1. 点击左侧“知识库”。
2. 点击“上传文档”。
3. 选择文件。
4. 选择已创建的专题库和标签。
5. 等待系统解析、分块和索引。
6. 文档状态变为“就绪”后即可检索。

上传建议：

- 一个文件只放一个主题。
- 文件名尽量清晰，例如 `2026年销售制度.md`。
- 专题库适合业务域、项目、部门或制度体系。
- 标签适合年份、地区、阶段、用途或系统名。
- 大文件上传后需要等待索引完成。
- 扫描件效果取决于 OCR 或视觉模型能力。

### 7.2 文档状态

常见状态：

- 处理中：正在解析或索引。
- 就绪：可以被检索。
- 失败：解析或索引失败，需要查看错误并重试。
- 停用：暂时不参与检索。

文档列表会显示分块数量、索引进度、错误信息和处理时间。

### 7.3 专题库和标签

当资料越来越多时，建议先做范围管理：

- 先创建专题库，再上传或整理文档。
- 文档可以有多个标签。
- 整理文档时通过下拉框选择专题库，通过复选框选择标签。
- 选择专题库后，标签列表只显示该专题库内已有标签。
- 普通聊天、召回测试和公文写作检索都会尊重当前专题库/标签范围。

不确定范围时保持“全部”；确认问题属于某个业务域时再缩小范围。

### 7.4 召回测试

在知识库工作台中使用召回测试：

1. 输入测试问题。
2. 选择专题库和标签范围。
3. 可调整相似度阈值、Top K 和候选数量。
4. 查看命中文档、分块内容、得分和关键词高亮。
5. 对结果标记“有用”或“无用”。

如果没有命中，优先检查文档是否就绪、范围是否选错、问题关键词是否贴近原文、阈值是否过高。

### 7.5 批量维护和质量报告

知识库支持：

- 批量重建索引。
- 批量删除文档。
- 对失败文档重试。
- 查看删除审计。
- 查看质量报告。

质量报告会展示质量分、就绪率、反馈有用率、陈旧文档、空分块文档和知识图谱覆盖情况。分数偏低时，优先处理失败或停用文档，重新索引长期未更新的资料，再用召回测试验证命中效果。

### 7.6 知识图谱与 Graph-RAG

知识图谱用于查看知识库中抽取出的实体和关系。当前实体和关系主要由规则、模式匹配和启发式质量信号生成，适合作为 Graph-RAG 辅助线索，不建议直接当作高精度语义知识图谱。

在知识图谱页面可以：

- 搜索和筛选实体。
- 查看中心实体和邻居节点。
- 拖动画布、滚轮缩放、复位视图。
- 点击节点切换中心实体。
- 查看关系来源、类型、可信度、片段和描述。
- 确认、编辑或删除关系。
- 合并重复实体。
- 输入图谱查询问题，查看命中的实体和关系路径。

正式问答前，建议先确认低可信关系、合并重复实体，并回到来源文档核对关键结论。

## 8. 工具箱

工具箱把模型、聊天、自由任务和工作流连接到可调用工具，例如数据库、报表文件、图表生成、报告编排、文档解析、数据处理、格式转换和内网通知。底层兼容 MCP，界面统一称为“工具箱”。

### 8.1 系统工具

系统工具由平台内置，通常无需填写服务地址。常见工具包括：

- 图表生成：把表格数据生成柱状图、折线图、饼图或表格展示。
- 报告编排：把摘要、表格、图表和指标块组织成报告。
- 文档解析：提取文档大纲、键值信息和段落片段。
- 数据处理：字段画像、筛选、聚合统计和字段标准化。
- 格式转换：在 Markdown 表格、JSON 和规范文本之间转换。

工具卡片右上角开关控制该工具是否可用于后续任务。

### 8.2 个人连接

个人连接由当前用户配置和使用。常见类型：

- 数据库连接：MySQL、PostgreSQL、SQL Server、SQLite、MongoDB。
- 报表文件：连接局域网目录或共享目录，读取 CSV、Excel、JSON、Markdown 等文件。
- IM 通知：对接内网 Webhook 或消息 API。
- 外部工具服务：连接技术同事提供的 MCP 服务。

配置流程：

1. 点击对应工具卡片“配置”。
2. 填写连接信息。
3. 保存前按需测试连接或配置诊断。
4. 保存后在个人连接区域查看卡片。
5. 点击“工具”刷新并确认工具列表。

### 8.3 数据库连接安全建议

数据库连接默认用于只读查询。建议：

- 使用只读账号。
- 设置最大返回行数。
- 生产库配置表/集合白名单。
- 配置字段白名单和敏感字段脱敏。
- 按需填写行级权限提示。
- 设置查询超时。
- 保存前先测试连接。

SQL 数据库只允许单语句读取类 SQL，例如 `SELECT`、`WITH`、`SHOW`、`DESCRIBE`、`DESC`、`EXPLAIN`。写入、DDL 和管理类关键字会被拦截。MongoDB 仅提供只读样本读取和聚合读取。

### 8.4 工具策略

管理员可以在系统设置中进入“工具策略”。该页面只管理系统工具和全局工具服务，不接管用户个人连接。

- 工具以卡片形式展示。
- 卡片右上角开关用于启停工具。
- 内置系统管理员可以编辑风险等级、审批要求和适用说明。
- 普通管理员可以查看，但只读卡片不显示编辑入口。
- 用户个人数据库连接、报表目录、消息通知和个人外部服务仍由所有者自己管理。

### 8.5 常见工具箱问题

- 工具列表为空：进入工具弹窗刷新工具，确认服务可访问。
- 调用失败：检查服务地址、账号、网络和权限。
- 数据库无结果：检查 SQL 条件、表名、Schema、返回行数和白名单。
- 字段显示为“已脱敏”：说明字段被配置为敏感字段。
- 等待审批：说明工具风险较高，需要用户或管理员批准。

## 9. 自由任务与工作流

自由任务用于探索性、多步骤任务，例如资料调研、临时分析、数据排查和一次性报告生成。工作流用于稳定流程，例如标准报表、周期取数、审批前检查、内网通知和可审计的生产任务。

建议流程：先用自由任务探索和验证步骤，再把可复用流程沉淀为工作流并发布运行。

### 9.1 创建自由任务

1. 点击左侧“自由任务”。
2. 输入任务目标。
3. 选择模型。
4. 选择运行模式。
5. 选择模型路由策略。
6. 按需开启工具箱工具、审批、Token 预算、失败重试、上下文来源和补充说明。
7. 如需统一格式，点击“套用规范”。
8. 点击运行。

运行前系统会做预检，展示就绪度、预计输入 Token、高风险工具数量、异常工具服务和未检查服务。评分偏低时，先收窄工具范围、提高预算、刷新工具或修复异常连接。

### 9.2 运行模式与模型路由

运行模式：

- 标准模式：适合一般任务。
- 深度模式：适合需要更多检索、分析和验证的任务。
- 审查模式：适合检查风险、漏洞、合规问题或遗漏。

模型路由：

- 固定模型：始终使用所选模型。
- 视觉优先：含图片时优先选择视觉模型。
- 上下文匹配：选择能容纳输入且窗口合适的模型。
- 成本优先：选择价格更低的可用模型。
- 负载均衡：选择当前负载较低的模型。
- 成本升级：先用便宜模型，置信不足时升级到更强模型。

### 9.3 工具范围和审批

自由任务可以限制工具范围：

- 不使用工具：只根据输入回答。
- 仅系统工具：使用会话、知识库、模型状态等系统工具。
- 系统工具 + 工具箱：允许数据库、报表、图表、报告、通知等工具。
- 白名单工具：只允许指定工具。

当任务准备调用高风险工具时，可能进入等待审批。审批前应查看工具名称、输入参数和风险说明。不合理时拒绝或停止任务。

### 9.4 查看任务记录

任务记录支持按全部类型、自由任务和工作流任务筛选。表格通常显示任务、类型、模型、模式、步骤、工具、错误、Token 用量、创建时间、状态和操作。

点击详情可查看：

- 任务时间轴。
- 工具调用统计。
- 每一步执行过程。
- 工具输入和输出。
- 错误信息。
- Token 用量。
- 最终答案。
- 工作流节点轨迹。

任务完成后，可以把结果保存为产物，并支持备注、版本、差异对比和回滚。

### 9.5 工作流编排

工作流编排提供可视化画布：

- 添加节点、拖拽节点、端口连线。
- 自动布局、适配画布、滚轮缩放、空白平移和小地图。
- 右侧抽屉编辑节点标题、工具、条件、失败策略、重试、超时、输入和依赖。
- 工具选择器按知识与会话、数据库、报表与文件、图表与展示、数据处理、文档处理、格式转换、消息通知、系统诊断和外部能力分组。
- 输入参数可插入任务目标或上游节点输出变量。
- 工作流库支持保存、发布、版本对比、历史回滚和删除。
- 已发布工作流可用于计划任务和审计复用。

大模型节点可以作为起始节点，但提示词必须引用 `{{goal}}` 或 `{{inputs.*}}`。如果要分析上游工具结果，应连接对应节点，并在提示词中引用 `{{nodes.节点ID.output}}`。系统会在保存、预检或运行时拦截缺少输入的大模型节点。

自由任务完成后，可在详情中点击“生成工作流草稿”，把本次工具步骤转换为可检查、可保存、可发布的流程资产。

### 9.6 模板和计划任务

自由任务模板保存目标、运行参数、上下文来源、模型路由和工具范围。工作流模板保存步骤、节点、依赖结构和运行输入。

周期任务建议使用已发布工作流承接每日、每周或手动计划。计划任务运行后会产生通知和任务记录，便于追踪审计。

## 10. 模型配置

模型配置一般由管理员完成。普通用户如果被授权，也可以维护个人模型。

### 10.1 新增模型

1. 进入系统设置或模型管理。
2. 点击添加模型。
3. 填写 Endpoint、API Key、展示名称和 Model ID。
4. 设置保存范围：个人模型或全局模型。
5. 按需设置可见部门、每日额度、温度、最大输出 Token、上下文窗口、健康检查 URL 和并发上限。
6. 标记模型能力，例如文本、视觉、推理。
7. 设置是否在聊天中开启思考。
8. 测试连接并保存。

### 10.2 上下文窗口和输出限制

模型配置中的关键 Token 参数：

- 上下文窗口：模型实际可容纳的总 Token。
- 最大输入 Token：单次请求允许注入的输入上限。
- 最大输出 Token：发送给上游模型的输出上限。

如果部署的是长上下文模型，应让这些配置与实际 `n_ctx` 或服务端配置一致。输入和输出上限应小于上下文窗口，避免请求被上游拒绝。

### 10.3 模型不可用排查

如果模型调用失败：

- 点击测试连接。
- 检查 Endpoint 是否正确，例如是否需要 `/v1`。
- 检查 API Key 和 Model ID。
- 检查当前用户是否有模型权限。
- 检查每日额度是否用完。
- 检查端点并发、排队和熔断状态。
- 查看系统监控中的模型端点状态。

## 11. 全局参数

全局参数用于调整系统运行期限制和资源保护策略。普通管理员可查看，只有内置 `admin` 超级管理员可以保存修改。

### 11.1 打开全局参数

1. 进入系统设置。
2. 点击“全局参数”。
3. 按需调整请求队列、上下文窗口、上传与图片、知识库与 RAG、自由任务与后台任务参数。
4. 点击“保存参数”。

保存后，后续请求会读取新的运行时配置。

### 11.2 调整建议

调整大上下文相关参数时，建议按顺序处理：

1. 先确认模型自身上下文窗口与实际部署一致。
2. 再调整全局上下文窗口和预留输出 Token。
3. 根据服务器资源逐步放宽附件、图片、知识库上传和抽取长度。
4. 最后调整 RAG Top K、候选数量、分片大小和 RAG 上下文占比，并用召回测试验证质量。

全局参数不是无限放开。系统仍会参考模型窗口、资源保护上限、上传大小、图片大小、RAG 候选数量和知识库抽取长度，避免单次请求拖垮服务。

## 12. 第三方 API Key

系统可以提供 OpenAI-compatible API 给外部程序调用。

### 12.1 创建 API Key

1. 进入 API 接入或系统设置中的相关入口。
2. 创建新的 API Key。
3. 设置名称、权限和有效期。
4. 保存后复制密钥。

密钥通常只完整显示一次，请妥善保管。

### 12.2 调用接口

外部程序可以按 OpenAI-compatible 格式调用：

- `/v1/chat/completions`
- `/v1/completions`
- `/v1/models`

流式输出：请求体中携带 `stream: true` 时，系统会按 SSE 真流式返回；不带或为 `false` 时返回一次性 JSON。

代码补全类客户端如果发送 `prompt`、`input`、`prefix`、`suffix` 风格请求，系统会兼容转换为可用的聊天消息。

使用建议：

- 不同系统分别创建不同 Key。
- 不要把 Key 写入公开仓库。
- 不再使用时及时停用或删除。
- 管理员可查看调用记录和 Token 用量。

## 13. 状态与问题排查

如果账号可以看到系统状态或监控入口，可以辅助判断模型、知识库和工具是否正常。

### 13.1 常见状态

- 活跃用户和调用量。
- 模型 Token 使用量。
- 模型端点健康状态。
- 并发、排队和熔断。
- RAG 检索耗时和命中情况。
- 慢查询和异常告警。
- Pivot 所在服务器的 GPU 和显存。

GPU 区域只代表 Pivot 部署服务器本机 GPU。如果模型部署在远端服务器，需要结合远端健康检查或模型端点状态判断。

### 13.2 常见异常处理

- 排队过多：提高端点并发、增加模型实例或降低任务量。
- 端点熔断：检查上游模型服务是否可用。
- Token 消耗异常：检查批量任务或第三方 API 调用。
- RAG 很慢：检查文档数量、召回参数和服务器负载。
- 工具箱连接失败：检查服务地址、账号、网络、权限和白名单。
- 数据分析 SQL 失败：检查字段类型、字段名双引号、是否需要 `TRY_CAST`，以及是否误把编号字段当作数值。

管理员可运行配置体检命令检查外部链路：

```bash
npm run check:external
```

目标环境验收时，可在确认允许真实访问外部服务后运行：

```bash
npm run check:external -- --live
```

## 14. 推荐工作流

### 14.1 普通问答

1. 选择模型。
2. 直接提问。
3. 需要内部资料时打开知识库。
4. 需要数据库、报表或图表时打开工具箱。

### 14.2 内部资料问答

1. 上传资料到知识库。
2. 等待文档就绪。
3. 用召回测试验证命中。
4. 在对话中打开知识库并选择范围。
5. 提问并核对来源。

### 14.3 公文起草

1. 打开应用中心的公文写作。
2. 新建公文。
3. 选择文种和规范库。
4. 粘贴材料或检索历史/规范。
5. 使用起草、润色、改写和审校。
6. 在审改栏处理建议、批注、版本和校对。
7. 导出 DOCX、红头 DOCX、Markdown、PDF 或备份。

### 14.4 数据分析报告

1. 打开应用中心的数据分析。
2. 上传 Excel/CSV 或从数据库导入。
3. 查看字段画像和数据预览。
4. 用查询、透视、图表和比对完成分析。
5. 用智能分析生成解释和建议。
6. 导出 CSV、XLSX、Parquet、PNG 或报告材料。

### 14.5 周期性任务

1. 用自由任务写清目标并试运行。
2. 调整模型、模式和工具范围。
3. 生成工作流草稿。
4. 在画布中检查节点、参数和依赖。
5. 保存并发布工作流。
6. 配置计划任务。
7. 定期检查通知、任务记录和结果产物。

## 15. 常见问题

### 15.1 模型回复慢

可能原因：模型本身推理慢、端点排队、附件或知识库上下文较大、自由任务步骤较多。

处理方式：换用更快模型，减少附件和上下文，查看端点并发和队列，等待当前任务完成。

### 15.2 知识库回答不准确

处理方式：检查文档是否就绪，用召回测试查看命中文档，调整问题关键词，缩小专题库/标签范围，上传更清晰的资料，必要时调整 RAG 参数。

### 15.3 工具箱无法调用

处理方式：检查工具卡片开关，刷新工具缓存，查看连接配置，检查地址、账号、网络、白名单和权限。

### 15.4 自由任务等待审批

说明任务准备调用需要确认的工具。打开任务详情，查看工具和参数，合理则批准，不合理则拒绝或停止任务。

### 15.5 附件无法读取

可能原因：格式不支持、文件过大、文档加密、扫描件需要视觉模型、图片过大被压缩或跳过。

处理方式：换成常见格式，拆分大文件，输入正确密码，选择视觉模型，或联系管理员确认全局参数限制。

### 15.6 看不到某个模型或工具

可能原因：账号无权限、模型或工具停用、仅对指定部门可见、个人资源属于其他用户。

处理方式：联系管理员确认权限，检查启停状态，刷新页面后再试。

### 15.7 数据分析结果和预期不一致

处理方式：检查字段类型和字段画像，确认编号字段没有被当作指标，金额/成绩/数量是否能转成数值，透视和图表的聚合方式是否正确，查询是否限制了行数或 Top N。

### 15.8 首页“不再提示”后登录页公告还显示吗

会继续显示。主页“不再提示”只记录当前登录用户对站内公告的隐藏状态。登录页公告是公开入口，不依赖某个已登录用户的已读或隐藏状态。

如需关闭登录页展示，管理员需要取消“登录页展示”、撤下公告、归档公告或设置结束时间。

## 16. 使用安全建议

- 不要在普通对话中粘贴不必要的密码、密钥、身份证件或敏感个人信息。
- 上传内部资料前确认保密范围。
- 使用知识库回答重要问题时核对来源。
- 使用数据库工具时优先使用只读账号。
- 批准工具调用前确认工具和参数符合任务目标。
- 第三方 API Key 不要共享给无关人员。
- 项目结束、人员离职或系统交接时，及时停用账号、计划任务和 API Key。
- 数据分析和智能分析的 AI 结论应经过人工复核后再用于正式汇报。

## 17. 新用户快速检查清单

第一次使用前确认：

- 已能登录系统。
- 已知道默认可用模型。
- 已能新建一次对话。
- 已了解帮助入口在哪里。
- 已了解知识库和工具箱开关的用途。
- 已知道如何上传附件。
- 已知道应用中心包含公文写作和数据分析。
- 已知道遇到模型不可用或权限不足时联系谁。