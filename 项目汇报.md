# Pivot（智枢）项目汇报

> AI 智能中枢管理系统 · 当前版本 v0.0.215

---

## 本次版本更新（v0.0.215）

- API 接入管理页的外部调用说明从“聊天/向量”补齐为聊天对话、代码补全和向量生成三类接口。
- 接入指南明确展示 `POST /v1/chat/completions`、`POST /v1/completions` 和 `POST /v1/embeddings`，其中代码补全标注兼容 `prompt`、`input`、`prefix`、`suffix` 请求字段。
- 可用模型说明同步补充 `/v1/completions` 代码补全入口，并将用户侧“聊天补全”统一修正为“聊天对话”，避免和代码补全混淆。
- CHANGELOG、README、项目汇报、`package.json` 和 `package-lock.json` 已同步启用 `0.0.215`；服务端重启后 `/chat` 静态模板会读取新的接入指南文案。

---

## 本次版本更新（v0.0.214）

- 将上一版本之后的 remote 本机 MCP 执行、普通对话工具调度、工具箱治理体验、测试补充和发布文档统一归档为 `v0.0.214`。
- 本机 SQLite 与本机报表目录继续通过桌面端/本地助手反向执行，服务器只下发 `mcp.0.*` 任务并接收结构化结果，不主动读取用户电脑路径。
- 普通对话查询本机目录文件清单时会确定性调用 `mcp.0.reports.list_files`，并对桌面端桥、授权状态、旧客户端接口、工具治理过滤和路径错误给出更明确提示。
- 工具箱首页补齐本机授权后的工具数量、工具弹窗、刷新同步和治理展示；聊天统计文案统一为 `Tokens` 与 `t/s`。
- README、使用帮助、项目汇报、工具箱设计方案、CHANGELOG、`package.json` 和 `package-lock.json` 已同步启用 `0.0.214`。

---

## 本次版本更新（v0.0.213）

- 修复 remote 模式桌面端本机执行器未初始化本地运行目录的问题，避免打包态服务模块默认向 `resources/app.asar/logs` 写日志并触发 `ENOTDIR`。
- 本机授权执行器固定读取桌面端用户数据目录下的 `local-authorizations.json`，确保授权中心展示与实际本机工具执行使用同一份授权记录。
- 桌面端 IPC、前端任务回传和服务端桥接层增加受控错误细节透传，后续可从服务端日志直接定位打包态、本机路径和权限问题。
- 已验证当前授权的 `D:\法律` 报表目录可列出 3 个 Markdown 文件，`node tests\security-mcp.test.js` 已通过 31/31。

---

## 本次版本更新（v0.0.212）

- 修复打包桌面端列本机报表目录时提前加载 Excel 解析库导致的 asar 路径异常，普通目录清单只执行文件扫描。
- 服务端接收本机桥接任务结果时二次归一化路径错误状态码，`ENOTDIR`、路径不存在和权限不足不再统一显示为 HTTP 500。
- 新增本机桥接执行错误归一化回归测试，`node tests\security-mcp.test.js` 已通过 31/31。

---

## 本次版本更新（v0.0.211）

- 修复 remote 桌面端本机目录查询时把授权根目录名误当作文件名过滤条件的问题，“查询本机法律目录下有哪些文件”会列出授权根目录内容。
- 桌面端本机只读执行器改为结构化回传状态码和错误码，服务端可区分路径不存在、路径不是目录、权限不足等本机执行异常，避免笼统显示 HTTP 500。
- `mcp.0.*` 本机虚拟工具调用日志改为不绑定真实 MCP 服务 ID，避免 `server_id=0` 外键失败导致日志写入重试。
- 前端聊天消息统计与本机执行器诊断改为中文展示，避免普通用户看到 `Tokens`、`t/s` 和内部执行接口名。
- 当前已验证 `D:\\法律` 授权目录可列出 3 个 Markdown 文件，`node tests\security-mcp.test.js` 和 `npm run check` 已通过。

---

## 本次版本更新（v0.0.210）

- 修复 remote 桌面端聊天页本机执行器长轮询对 `currentUser` 初始化时序的依赖，避免心跳已注册但本机任务无人领取。
- “查询本机某目录下有哪些文件”等请求确定性走 `mcp.0.reports.list_files` 后，执行失败会按本机工具执行链路提示，不再退回规划模型并误报“工具规划失败”。
- 新增本机目录工具失败路径回归测试，当前 `node tests\security-mcp.test.js` 已通过 30/30。

---

## 本次版本更新（v0.0.209）

- 普通对话页新增桌面端本机执行器主动心跳，减少只打开聊天页时没有本机工具心跳的问题。
- 聊天请求会携带桌面端桥接诊断，服务端可明确提示普通浏览器打开、旧客户端缺少执行接口、未授权报表目录或心跳注册失败。
- 应用版本启用 `0.0.209`，用于刷新聊天页静态资源缓存。

---

## 本次版本更新（v0.0.208）

- 修复 remote 桌面端普通对话页未稳定加载本机执行器桥接脚本的问题，聊天页现在会默认启动本机执行器心跳。
- 普通对话启用工具箱后，发送消息前会主动同步桌面端本机授权摘要，让 `mcp.0.db.*` 与 `mcp.0.reports.*` 能进入本轮工具上下文。
- “查询本机某目录下有哪些文件”等请求会确定性路由到 `mcp.0.reports.list_files`，避免误用数据库工具或退回普通命令建议。
- 工具箱诊断提示细化为心跳、授权和工具治理三个层级，便于排查客户端是否已打开、报表目录是否授权、工具是否被禁用。
- README、项目汇报、工具箱设计方案和 CHANGELOG 已同步当前口径；`package.json` 和 `package-lock.json` 同步启用 `0.0.208`。

---

## 本次版本更新（v0.0.207）

- remote 模式下保持服务器统一业务能力，生产客户端仍可继续使用全局大模型、文字识别、PDF 工具、法规查询、文档处理、工具策略、审计和自动更新。
- 新增桌面端本机资源反向执行通道：授权后的本机 SQLite 与本机报表目录不由服务器主动访问，而是由桌面端领取任务后在用户电脑本机只读执行。
- 授权且桌面端在线后，工具箱会动态暴露 `mcp.0.db.*` 和 `mcp.0.reports.*`；SQLite 仅支持只读 SQL，报表目录仅允许授权根目录内的文件列出、摘要和表格查询。
- 桌面端授权中心刷新、授权成功和撤销授权后会立即同步本机执行器状态，减少工具列表延迟；完整本机路径继续只保存在用户电脑本机授权文件中。
- README、使用帮助、项目汇报、工具箱设计方案和 CHANGELOG 已同步当前口径；`package.json` 和 `package-lock.json` 同步启用 `0.0.207`。
- 本轮已通过 `node tests\security-mcp.test.js`（26/26）和 `npm run check`。

---

## 本次版本更新（v0.0.205）

- 将当前工作区 `v0.0.197` 至 `v0.0.204` 的全部修改和优化统一归档为新的发布口径，便于按 `v0.0.205` 打包、部署、验收和追踪问题。
- 工具箱与数据分析、本机授权中心、文档处理/OCR/PDF、桌面客户端自动更新、离线局域网 HTTP 更新和部署文档调整均已纳入本轮汇总说明。
- README、使用帮助、项目汇报和 CHANGELOG 已新增 `v0.0.205` 摘要；下方继续保留逐版本明细，方便回看每一轮能力边界。
- `package.json` 和 `package-lock.json` 同步启用 `0.0.205`，保持包版本、运行版本和文档版本一致。

---

## 本次版本更新（v0.0.204）

- 桌面客户端自动更新支持无互联网局域网 HTTP 场景，管理员可显式开启 `autoUpdate.allowInsecureHttp=true`。
- 非本机 HTTP 更新源必须配置 `autoUpdate.allowedOrigins`，客户端只接受白名单内网地址的 `/downloads/` 更新清单。
- `config.example.json` 已调整为 `Production LAN` 示例，展示内网 HTTP `remoteUrl`、`path=/downloads/` 和来源白名单组合。
- 远程部署说明、downloads 目录说明、Docker 客户端下载说明、README、使用帮助和 CHANGELOG 已同步当前口径，应用版本启用 v0.0.204。

---

## 本次版本更新（v0.0.203）

- 桌面客户端自动更新默认接入生产站点 `/downloads/` 目录，开启 `autoUpdate.enabled` 后可读取 `latest.yml` 并下载新版本。
- `npm run dist:win` 成功后会自动复制安装器、`.blockmap` 和 `latest.yml` 到 `downloads/`，同时生成 `Pivot-Setup.exe` 作为手工下载入口。
- 生产更新源继续要求 HTTPS，HTTP 仅保留给显式放开的 loopback 开发测试，避免明文更新源被误用。
- 远程模式配置新增 `autoUpdate.path`，默认 `/downloads/`；需要独立 CDN 时仍可填写 `autoUpdate.url` 覆盖。
- README、使用帮助、项目汇报、远程部署说明、downloads 目录说明和 CHANGELOG 已同步当前口径，应用版本启用 v0.0.203。

---

## 本次版本更新（v0.0.202）

- 工具箱“我的电脑”动作区现在可以打开本机授权中心，连接本机数据库和授权本机报表目录不再只是禁用说明。
- 桌面客户端新增受控本机授权桥，可选择本机 SQLite 文件或本机报表目录，并支持撤销当前设备授权。
- 授权状态会回写工具箱首页，显示待授权、已授权或需客户端；网页端没有桌面客户端/本地助手时仍保持不可直接读取本机资源。
- 完整本机路径只保存在桌面客户端本机配置中，页面和服务器只接触资源名称、设备名、授权时间和路径摘要。
- 本版本只完成授权中心和状态记录，本机查询、目录扫描与结果回传仍作为后续只读执行器阶段接入。
- README、使用帮助、项目汇报、设计方案和 CHANGELOG 已同步当前口径，应用版本启用 v0.0.202。

---

## 一、项目是什么

**Pivot（智枢）** 是一款面向**私有化、离线化、企业内网**场景的全栈 AI 对话、自由任务与工作流编排平台。

它把"接入大模型、检索知识库、调用工具、运行自由任务、编排工作流、治理权限、审计监控"这些原本分散的能力，整合为一个**可控、安全、可审计**的统一 AI 工作入口，部署在企业自己的服务器或局域网中，数据不出内网。

- **技术形态**：Node.js + Express + SQLite 单体应用，支持 Docker 离线打包部署。
- **交付形态**：Web 应用 + PWA（可安装到桌面/移动端）+ OpenAI 兼容 API 网关。
- **定位**：企业内部的"AI 中枢"，而非面向公网的对话产品。

---

## 二、核心能力

| 能力域 | 说明 |
|--------|------|
| **多模型接入** | 接入任意 OpenAI 兼容模型，支持全局/部门/私有模型可见性、能力标识（文本/视觉/推理）、每日额度、费用统计、端点并发与熔断保护、模型上下文窗口配置，及 6 种自由任务模型路由策略。 |
| **自由任务与工作流编排** | 自由任务用于分析、排查、临时处理和流程孵化；工作流编排用于节点编排、发布版本、计划任务、审计复用和企业生产任务运行。 |
| **角色与规范库** | 原“指令中心”升级为可复用规范资产库，支持角色、输出规范、任务方法和工作流节点提示分类管理，并可套用到聊天、自由任务和工作流三个入口。 |
| **应用中心、公文写作与文档处理** | 应用中心承载面向具体业务场景的工作台；公文写作支持正文/对照模式、全文 AI 起草润色改写审校、选区工具、版式化正文渲染、左侧资料库、右侧审改栏、批注、版本对比、规范检查和材料引用；文字识别和 PDF 工具已接入共享文档处理底座，支持 OCR 复核、多格式导出、结果分发、PDF 页面处理和可搜索 PDF。 |
| **知识库 / RAG** | 文档上传与索引、专题库/集合分组、文档多标签、专题库到标签二级联动、中文 ngram 检索、范围化召回测试、结果缓存、反馈闭环；支持知识图谱与 Graph-RAG（实体/关系融合召回）。知识图谱当前按规则/启发式抽取实体关系，定位为可治理的 Graph-RAG 辅助线索，并可按专题库/标签缩小引用范围。 |
| **工具箱（MCP）** | 用户侧统一以“工具箱”呈现，底层兼容 MCP 协议；以“数据来源 + 处理工具 + 输出动作”组织能力，可接入外部工具、服务器可访问数据库（PG/MySQL/SQL Server/SQLite/MongoDB 只读）、服务器可访问报表目录、导入数据文件、图表生成、报告编排和内网消息通知；桌面客户端或本地助手授权后可执行本机数据库/报表目录操作；支持系统工具策略、个人连接隔离、工具所属用户/全局来源可见、数据库白名单脱敏和配置诊断，可组合成聊天、自由任务与工作流工具。 |
| **API 网关** | 提供 OpenAI 兼容 `/v1/chat/completions`、`/v1/embeddings` 接口，第三方系统可凭 API Key 接入，并留痕审计；代码补全类客户端的 `prompt`/`suffix` 风格请求可在聊天补全接口中兼容处理。 |
| **运营监控** | 实时运营面板、健康检查、RAG 指标、Prometheus 指标、GPU 监控、端点状态、慢查询与异常告警（webhook），并提供全局参数页动态治理请求队列、上下文、上传、图片、知识库和 RAG 资源限制。 |
| **安全与审计** | 超级管理员边界、资产隔离、软删除治理、全链路审计日志、SSRF/XSS/CSP 防护、敏感信息脱敏、合规审计包导出、定时数据库备份。 |

### 6 种自由任务模型路由策略

自由任务执行时，可在**该用户有权限的模型池内**按任务特征自动挑选最合适的模型，而非固定指定。路由不会越权、失败自动回退到原模型，并记录选择理由。

| 策略 | 名称 | 选择逻辑 | 适用场景 |
|------|------|----------|----------|
| `fixed` | 固定模型 | 始终用指定模型（默认，等同旧行为） | 已明确知道该用哪个模型 |
| `auto-vision` | 视觉优先 | 输入含图片时选支持视觉的模型，否则按上下文兜底 | 图文混合任务 |
| `auto-context` | 上下文匹配 | 在能容纳输入的模型中选窗口最小但够用的 | 节约大窗口模型配额 |
| `auto-cost` | 成本优先 | 在够用模型中选输入+输出单价之和最低的 | 成本敏感任务 |
| `auto-load` | 负载均衡 | 选当前活跃占比最低的端点 | 降低长任务排队等待 |
| `auto-escalate` | 成本升级 | 先用最便宜模型出结果，置信不足时自动升级到更强模型重做 | 成本与质量自动权衡 |

> `auto-escalate` 通过启发式判断输出置信度（空/过短/异常结束/低置信短语），不达标时升级到单价更高或窗口更大的模型重新合成，实现"简单任务省钱、难任务才升级"。

---

## 三、能做到什么

- **统一管理多个大模型**：在一个平台接入云端、本机、局域网的多种模型，按部门和用户分配可见范围与额度，并自动统计 Token 用量与成本。
- **基于私有资料回答**：把企业内部文档导入知识库，让模型优先依据内部资料作答；资料规模增长后，可按专题库和标签只引用相关资料，标签会跟随专题库联动过滤，并可视化验证是否真正命中。
- **承载具体业务写作场景**：通过应用中心进入公文写作工作台，在同一页面完成材料整理、正文起草、对照审改、版式化正文编辑、规范检查、批注和版本对比。
- **让 AI 自主完成多步任务**：自由任务可拆解目标、调用工具、查询数据库、生成图表与报告；成熟步骤可生成工作流草稿，再通过可视化工作流编排固化为业务流程（如"取数 → 大模型解读 → 图表 → 报告 → 内网通知"）。
- **对接企业现有系统**：通过 OpenAI 兼容 API，让其他业务系统直接调用平台的模型与向量能力。
- **全程可审计可监控**：所有敏感操作、模型调用、工具调用均留痕，管理员可导出合规审计包，运维可接入企业监控体系。

---

## 四、解决哪些问题

1. **数据安全与合规**——避免企业数据流向公网 AI 服务；私有化离线部署，全链路审计，满足内网管控与合规留存要求。
2. **模型资源碎片化**——统一纳管多模型与多端点，提供并发保护、熔断、额度、成本治理和运行时全局参数配置，避免资源滥用和服务雪崩。
3. **知识难以被 AI 利用**——通过 RAG 与知识图谱，把企业文档转化为模型可检索、可追溯的知识来源，降低"幻觉"。
4. **AI 落地停留在聊天**——自由任务与工作流编排把 AI 从"对话"推进到"自动完成业务任务"，并把临时任务沉淀为可复用、可发布、可审计的流程资产。
5. **权限与治理缺位**——三级权限模型（超级管理员/管理员/普通用户）、工具策略、个人连接隔离、资产隔离，让 AI 能力在组织内可控分发。

---

## 五、技术亮点

- **离线友好**：纯内网可运行，Docker 镜像可离线打包导入，启动自动迁移数据库。
- **安全前置**：内置 SSRF 防护、文档解析炸弹防护、TLS 校验、日志脱敏、CSP 加固，工具箱支持工具策略、个人连接隔离和数据库表/字段边界，持续以安全回归测试把关。
- **动态资源治理**：在 128K 等大上下文自部署模型场景下，上传、图片、附件上下文、知识库抽取、RAG Top K、候选数量、分片大小和 RAG 上下文占比采用“模型窗口 + 资源保护 + 管理员配置”策略；管理员可在全局参数页查看，内置 `admin` 超级管理员可保存修改，常规运营调整无需反复修改 `.env` 并重启。
- **工程化扎实**：模块化拆分、统一缓存与超时治理、幂等迁移、开发规范自动门禁、语法/lint/安全三重校验（`npm run verify`）。
- **验收口径清晰**：本地回归用于证明代码、接口和安全边界；真实模型、Embedding、外部 MCP、数据库和 IM Webhook 通过 `npm run check:external` / `-- --live` 与目标环境手工验收共同确认。
- **持续迭代**：v0.0.43 → v0.0.126 累计落地基础设施收口、模型路由、流式 function calling、工作流编排、知识图谱、公告中心、安全加固、普通对话图表确定性渲染、工作流资产化、权限层级收口、任务记录体验精简、前后端模块化拆分与运行时兼容修复、监控页资源说明细化、工作流 LLM 节点编排门禁与统计图模板补齐、工作流向导兼容修复、大模型运行结果可读化、知识库/工具箱普通用户使用闭环、知识库质量评分、工具箱健康评分、自由任务预检就绪度、知识图谱质量治理、待确认关系审核、Graph-RAG 查询调试、自由任务定位与工作流孵化闭环、工具级治理、数据库安全边界和配置诊断、智能体任务记录 15 条分页、DAG 详情去重与自由任务模板保存入口前置、知识库工作台顶部减重、上传弹窗化、质量诊断按需显示、知识图谱顶部工具区合并、工具箱命名收口、工具策略边界、模型测试权限和模型软删除修复、工具策略卡片平铺、18 个工具分页和居中治理弹窗体验收口、聊天输入状态轻量化、知识库关键词兜底、工具箱限流提示修复、知识库专题库/标签范围检索、知识库范围选择和专题库标签二级联动、第三方代码补全兼容、工具策略只读态收口、登录落点修复、角色与规范库升级、工具归属可见、旧数据库迁移兼容、应用中心与公文写作工作台体验升级、知识图谱索引补齐与依赖治理、统计查询合并与错误响应收口和数据库标识符加固及工程化基线（ESLint 规则、engines 声明、CI）、公文写作工作台接入真实模型与规范库联动和编辑可靠性增强、公文写作六项功能扩展（多文档工作台、发文要素结构化表单、AI 流式逐句改写、知识库检索接入、多格式导出、公文校对增强）、公文写作版式布局与 AI 操作边界深度优化、聊天思考开关与流式输出修复、上下文窗口配置补齐，以及 128K 上下文后的动态资源限制与全局参数治理，并在 v0.0.193 固化开发规范、中文提示日志门禁、Git pre-commit 检查和文档处理底座/OCR/PDF 工具分阶段方案；v0.0.194 将 DESIGN.md 重构为项目级设计规范，拆分公文写作设计方案，确认工具箱数据接入与本机能力方案为根目录设计文档，并统一“服务器可访问数据库”命名；v0.0.195 完成文档处理底座落地、文字识别与 PDF 工具应用中心接入、OCR/PDF 导出分发治理、管理员配置统计和全局样式统一；v0.0.196 完成 PaddleOCR 3.x/PP-OCRv6 升级、OCR 中文识别乱码修复、文字识别与 PDF 工具表格/分页/弹窗化收口、PDF 下载入口补齐，以及生产离线部署模型外部挂载与环境变量对齐；v0.0.197 完成工具箱数据来源重组、服务器可访问数据库/报表目录命名落地、SQLite 数据集导入、已导入数据集入口和服务器侧诊断提示增强；v0.0.198 完成 OCR 项目彻底外置、HTTP 对接协议固化、独立 OCR 项目搭建指南和 PaddleOCR-VL 选型说明；v0.0.199 完成工具箱已导入数据集摘要入口、轻量统计接口和首页文件列表收口；v0.0.200 补齐文字识别和 PDF 工具任务删除入口、受控产物清理及 PDF 多输出打包下载；v0.0.201 完成工具箱数据管理单入口、本机授权入口独立呈现、admin 多用户工具折叠摘要、数据来源卡片层级收口和宽扁卡片可读性修复；v0.0.202 完成桌面端/本地助手授权中心、工具箱本机授权入口和本机路径安全摘要；v0.0.203 完成桌面客户端自动更新接入 downloads 发布目录、打包产物自动归档和生产 HTTPS 更新源配置收口；v0.0.204 支持离线局域网 HTTP 自动更新，并通过显式开关和来源白名单控制风险；v0.0.205 将本轮全部修改和优化统一归档为当前发布版本，并同步包版本、README、使用帮助、项目汇报和更新日志；v0.0.206 完成工具箱页面前端样式、布局和本机授权入口视觉治理；v0.0.207 在 remote 模式下启用桌面端本机资源反向执行通道，将授权后的本机 SQLite 与报表目录暴露为 `mcp.0.*` 只读工具，同时保持服务器统一业务能力。

---

*文档更新于 2026/07/10 · 详细变更见 [CHANGELOG.md](CHANGELOG.md) · 使用说明见 [使用帮助.md](使用帮助.md)*
