# Pivot（智枢）项目汇报

> AI 智能中枢管理系统 · 当前版本 v0.0.85

---

## 一、项目是什么

**Pivot（智枢）** 是一款面向**私有化、离线化、企业内网**场景的全栈 AI 对话与智能体工作平台。

它把"接入大模型、检索知识库、调用工具、运行智能体、治理权限、审计监控"这些原本分散的能力，整合为一个**可控、安全、可审计**的统一 AI 工作入口，部署在企业自己的服务器或局域网中，数据不出内网。

- **技术形态**：Node.js + Express + SQLite 单体应用，支持 Docker 离线打包部署。
- **交付形态**：Web 应用 + PWA（可安装到桌面/移动端）+ OpenAI 兼容 API 网关。
- **定位**：企业内部的"AI 中枢"，而非面向公网的对话产品。

---

## 二、核心能力

| 能力域 | 说明 |
|--------|------|
| **多模型接入** | 接入任意 OpenAI 兼容模型，支持全局/部门/私有模型可见性、能力标识（文本/视觉/推理）、每日额度、费用统计、端点并发与熔断保护，及 6 种智能体模型路由策略。 |
| **智能体工作台** | 目标拆解、多步骤任务、工具调用、DAG 可视化编排、工作流库与版本治理、计划任务、断点续跑、结果沉淀与回滚、完整任务审计。 |
| **知识库 / RAG** | 文档上传与索引、中文 ngram 检索、召回测试可视化、结果缓存、反馈闭环；支持知识图谱与 Graph-RAG（实体/关系融合召回）。 |
| **能力库（MCP）** | 兼容 MCP 协议，接入外部工具、数据库（PG/MySQL/SQL Server/SQLite/MongoDB 只读）、报表文件、图表生成、报告编排、内网消息通知，可组合成业务流程。 |
| **API 网关** | 提供 OpenAI 兼容 `/v1/chat/completions`、`/v1/embeddings` 接口，第三方系统可凭 API Key 接入，并留痕审计。 |
| **运营监控** | 实时运营面板、健康检查、RAG 指标、Prometheus 指标、GPU 监控、端点状态、慢查询与异常告警（webhook）。 |
| **安全与审计** | 超级管理员边界、资产隔离、软删除治理、全链路审计日志、SSRF/XSS/CSP 防护、敏感信息脱敏、合规审计包导出、定时数据库备份。 |

### 6 种智能体模型路由策略

智能体执行任务时，可在**该用户有权限的模型池内**按任务特征自动挑选最合适的模型，而非固定指定。路由不会越权、失败自动回退到原模型，并记录选择理由。

| 策略 | 名称 | 选择逻辑 | 适用场景 |
|------|------|----------|----------|
| `fixed` | 固定模型 | 始终用指定模型（默认，等同旧行为） | 已明确知道该用哪个模型 |
| `auto-vision` | 视觉优先 | 输入含图片时选支持视觉的模型，否则按上下文兜底 | 图文混合任务 |
| `auto-context` | 上下文匹配 | 在能容纳输入的模型中选窗口最小但够用的 | 节约大窗口模型配额 |
| `auto-cost` | 成本优先 | 在够用模型中选输入+输出单价之和最低的 | 成本敏感任务 |
| `auto-load` | 负载均衡 | 选当前活跃占比最低的端点 | 降低长任务排队等待 |
| `auto-escalate` | 成本升级 | 先用最便宜模型出结果，置信不足时自动升级到更强模型重做 | 成本与质量自动权衡 |

> `auto-escalate` 通过启发式判断输出置信度（空/过短/异常结束/低置信短语），不达标时升级到单价更高或窗口更大的模型重新合成，实现"简单任务省钱、难任务才升级"。

---

## 三、能做到什么

- **统一管理多个大模型**：在一个平台接入云端、本机、局域网的多种模型，按部门和用户分配可见范围与额度，并自动统计 Token 用量与成本。
- **基于私有资料回答**：把企业内部文档导入知识库，让模型优先依据内部资料作答，并可视化验证是否真正命中。
- **让 AI 自主完成多步任务**：智能体可拆解目标、调用工具、查询数据库、生成图表与报告，并通过可视化工作流编排固定的业务流程（如"取数 → 图表 → 报告 → 内网通知"）。
- **对接企业现有系统**：通过 OpenAI 兼容 API，让其他业务系统直接调用平台的模型与向量能力。
- **全程可审计可监控**：所有敏感操作、模型调用、工具调用均留痕，管理员可导出合规审计包，运维可接入企业监控体系。

---

## 四、解决哪些问题

1. **数据安全与合规**——避免企业数据流向公网 AI 服务；私有化离线部署，全链路审计，满足内网管控与合规留存要求。
2. **模型资源碎片化**——统一纳管多模型与多端点，提供并发保护、熔断、额度与成本治理，避免资源滥用和服务雪崩。
3. **知识难以被 AI 利用**——通过 RAG 与知识图谱，把企业文档转化为模型可检索、可追溯的知识来源，降低"幻觉"。
4. **AI 落地停留在聊天**——智能体与工作流编排把 AI 从"对话"推进到"自动完成业务任务"，并可沉淀为可复用资产。
5. **权限与治理缺位**——三级权限模型（超级管理员/管理员/普通用户）、能力包启停、资产隔离，让 AI 能力在组织内可控分发。

---

## 五、技术亮点

- **离线友好**：纯内网可运行，Docker 镜像可离线打包导入，启动自动迁移数据库。
- **安全前置**：内置 SSRF 防护、文档解析炸弹防护、TLS 校验、日志脱敏、CSP 加固，持续以安全回归测试把关（160/160 通过）。
- **工程化扎实**：模块化拆分、统一缓存与超时治理、幂等迁移、语法/lint/安全三重校验（`npm run verify`）。
- **持续迭代**：v0.0.43 → v0.0.85 累计落地基础设施收口、模型路由、流式 function calling、工作流编排、知识图谱、公告中心、安全加固、普通对话图表确定性渲染、工作流资产化、权限层级收口与智能体任务记录体验精简等多轮升级。

---

*文档生成于 2026/06/05 · 详细变更见 [CHANGELOG.md](CHANGELOG.md) · 使用说明见 [使用手册.md](使用手册.md)*
