# Pivot PPT 外部模板导入与复用产品级优化改造方案

- **文档状态**：已实施，待组织试点验收
- **编制日期**：2026-09-25
- **实施版本**：v0.1.178
- **适用范围**：外部 PPTX 模板导入、创建、AI 生成、编辑、预览和导出
- **关联文档**：[PPT 制作应用产品级方案](./PPT制作应用产品级方案.md)

## 1. 执行摘要

当前应用允许管理员导入 `.pptx`，但结果只是轻量主题模板：提取比例、少量颜色与字体，将外部布局名称映射为内置布局。原母版的精确版式、图片、装饰、动画没有成为可复用内容。空白新建能够绑定导入模板；AI 生成对非内置模板仍会回退到内置模板。主题色提取表达式存在转义缺陷，实际颜色可能落入默认值。

本次目标是：**上传已有 PPTX 后，能够预览并校对提取结果，再用导入模板生成可编辑新稿；品牌样式、布局槽位及受控图片在编辑器、预览和导出中保持一致。** 不承诺任意第三方 PPTX 无损导入或 PowerPoint 级动画时间轴往返编辑。

采用“结构化可编辑 + 复杂装饰保真降级”：常见母版、占位符和素材转为 Pivot 模板对象；不能安全转换的装饰经确认后固化为不可编辑背景。降级必须可见、可追踪、可在发布前审核。

## 2. 现状与差距

| 环节 | 当前实现 | 差距 |
|---|---|---|
| 导入入口 | 管理员通过 `/apps/presentations/templates/import` 导入 JSON/PPTX，进入审核流程 | 缺少预检、逐项报告和原稿对照 |
| PPTX 解析 | `presentation-pptx-template-import.js` 扫描 ZIP/XML，读取首个主题/母版及布局名称 | 不解析多母版关系；用正则处理 XML，主题色提取存在缺陷 |
| 模板模型 | `theme`、`layouts` 的名称/类别/槽位标签、`brandControls` | 不保存真实占位符坐标、装饰层、布局样式和素材引用 |
| 新建及套版 | 空白新建绑定主题；模板切换主要更新背景色和字体 | 不实例化真实布局；旧稿重新套版语义不清 |
| AI 生成 | `presentation-ai.js` 基于内置模板提示并规范化结果 | 非内置模板 ID 静默回退；生成坐标与所选布局脱节 |
| 图片与字体 | 文稿元素支持受控素材，模板仅有 Logo/字体扩展 | 外部图片未入素材库；字体名不等于可用或获准嵌入 |
| 动画与导出 | IR 支持部分入场动画/转场；PPTX 导出处理转场 | 外部动画忽略，元素入场动画未在 PPTX 导出中实现同等语义 |
| 版本 | 文稿保存模板 ID、版本和摘要 | 缺少完整布局/素材快照，旧稿结果需进一步隔离于模板更新 |

本方案深化现有产品方案的“外部 PPTX 模板导入策略”，不替换整套 PPT 应用架构。

## 3. 产品定义

### 3.1 用户旅程

1. 模板管理员上传 `.pptx`；系统进行安全扫描、结构解析和兼容性预检，不立即发布。
2. 导入预览展示母版/布局缩略图、主题、素材、可编辑槽位、缺失字体和不支持项。管理员选择“优先可编辑”或“优先还原外观”，逐布局校对。
3. 系统保存不可变模板草稿版本；经审核后按组织/部门权限发布。普通用户只看到有权使用的已发布模板。
4. 用户在空白创建、AI 生成或材料转 PPT 时选模板。内容先形成结构化提纲，再填充匹配的布局槽位；无法容纳时提示拆页或换布局。
5. 编辑器区分锁定品牌/装饰层与可编辑内容层；导出前报告文本溢出、缺失素材、字体替代和不支持动画。

### 3.2 支持矩阵

| 对象 | 产品级首期目标 | 降级或后续目标 |
|---|---|---|
| 比例、主题色、字体 | 正确解析关联主题和颜色引用，提供字体替代预览 | 字体缺失时显式标记，不承诺像素级一致 |
| 背景、Logo、装饰图 | 提取为租户受控素材，保留层级、裁剪及透明度 | 复杂装饰可经确认转为锁定背景并标明不可编辑 |
| 母版与布局 | 支持多母版、多布局及文本/图片占位符实际几何 | 不支持的对象转静态元素或进入人工修复清单 |
| 原稿内容图片 | 默认仅提取母版/布局素材，可选择示例页作样张 | 不自动把原稿所有照片复用到新稿，避免版权和语义误用 |
| 页面转场、简单入场 | 预览与导出均有明确映射时才导入 | 复杂时间轴、触发器和媒体联动列为不支持 |
| SmartArt、OLE、宏、ActiveX | 不执行、不作为可编辑对象导入 | 可安全预览时经确认转静态图，否则跳过或阻断发布 |
| `.ppt`、`.potx`、`.pptm` | 首期只接收 `.pptx`；旧 Pivot JSON 模板包继续兼容 | 其他格式另立专项，不通过更改扩展名接收 |

“外观优先”不等于无损导入：静态背景可以保留视觉，却不能编辑。界面和报告必须明确区分保真程度与可编辑范围。

## 4. 目标设计

### 4.1 版本化模板定义

在现有 `theme/layouts/brandControls` 基础上演进模板定义，旧模板仍可读。结构示意如下，具体字段和限额需经 schema 评审：

```json
{
  "schemaVersion": "2.0",
  "theme": { "colors": {}, "fonts": {}, "fontAssets": {}, "chartPalette": [] },
  "masters": [{ "id": "master_1", "background": {}, "decorations": [], "brandControls": {} }],
  "layouts": [{
    "id": "layout_1", "name": "标题与正文", "masterId": "master_1",
    "category": "内容", "previewAssetRef": "artifact-cas://0123456789abcdef",
    "placeholders": [{
      "id": "title", "role": "title", "kind": "text",
      "bounds": { "x": 80, "y": 60, "width": 1120, "height": 100 },
      "style": {}, "required": true,
      "constraints": { "maxLines": 2, "overflow": "shrink-or-split" }
    }],
    "decorations": [], "defaultTransition": { "type": "none" }
  }],
  "assetRefs": [],
  "importReport": { "sourceFormat": "pptx", "warnings": [] }
}
```

坐标归一化到现有画布；`masterId` 建立继承关系。素材引用须使用现有 CAS 机制，校验租户、类型与发布权限。来源文件以受控对象保存摘要和导入参数，用于审计或重新解析，不在运行时执行嵌入内容。

模板版本不可变。文稿创建时固化模板版本、摘要、解析后的布局及素材引用；更新模板产生新版本，下架仅禁止新建，不能改变旧稿的编辑和导出。素材回收必须考虑模板版本及历史文稿引用。旧版 `slots` 可迁移为标准布局的默认几何，标注“非原稿精确布局”。

### 4.2 导入流水线

1. **入口隔离**：限制文件大小、数量、解压体积/比例、XML 大小及耗时；校验 OOXML 类型、ZIP 路径、重复条目及外部关系。大文件解析和预览在受限任务进程执行，可取消。
2. **结构解析**：采用现有 `@xmldom/xmldom` 等结构化 XML API，沿 `_rels` 关联 presentation → master → layout → theme/media；处理命名空间、颜色与样式继承、图片裁剪、透明度和坐标换算，不再用正则解析 XML 结构。
3. **资产入库**：图片/字体白名单和真实内容校验，拒绝外链抓取、宏和 ActiveX；图片存入受控素材库，按哈希去重并记录来源。失败时回收本任务新增且未被引用的资产。
4. **模板转换**：识别占位符角色及几何，保留可编辑对象；其余按策略降级或阻断。对象状态为 `imported/approximated/rasterized/omitted/blocked`，附原因和位置。
5. **预览审核**：生成代表页，展示原稿与转换结果及差异；允许调整槽位、替换素材/字体后提交审核。阻断项修复前不得发布。

兼容性报告随模板版本持久化，包含母版/布局/对象定位、影响程度、建议操作及预览。PPTX 导入接口返回任务 ID，单独查询进度/报告；现有同步 JSON 模板包路径继续兼容。

### 4.3 创建、AI 与编辑

- 服务端按用户权限和具体版本解析模板；AI 提示、提纲校验、页面生成及结果规范化使用同一上下文。模板不可用时明确报错并重选，禁止静默回退。
- AI 先生成语义角色与内容，再由确定性布局填充器选择 `layoutId` 和占位符，执行字数/行数约束、图像适配、溢出检测和拆页。AI 不直接决定品牌层和最终坐标。
- 空白创建实例化模板封面；新增页实例化选中布局。旧稿切换提供预览和两个明确操作：“仅换主题”或“重新套版”；保留手工锁定元素并报告冲突。
- 编辑器展示锁定品牌层及可替换文本/图片槽；保存仍经过 `normalizePresentation`、素材归属和服务端品牌约束，不能信任客户端锁定标记。
- 预览、PNG/PDF/PPTX 使用同一实例化 IR。PPTX 尽量输出原生可编辑文本、形状和图片；静态降级保持原样。动画必须经浏览器预览和 PPTX 导出双端测试后才能标为支持。

### 4.4 权限与治理

导入、修改、审核、发布沿用管理员及组织/部门权限；模板预览、导入报告、源文件和派生素材采用同一租户边界。发布时校验所有引用素材具有适当共享权限；私有资产不可被模板发布隐式开放。记录上传、解析、校对、审核、发布、创建和导出审计事件；不对外暴露绝对路径或原始嵌入对象。

## 5. 模块改造清单

| 模块 | 主要改造 |
|---|---|
| `server/services/presentations/presentation-pptx-template-import.js` | 修复提色；结构化 OOXML 解析、多母版/关系、素材映射与报告 |
| `server/services/presentations/presentation-service.js` | 模板 v2、不可变版本快照、资产权限及生命周期、创建实例化 |
| `server/services/presentations/presentation-schema.js` | 占位符/装饰/布局快照校验、旧版兼容、数量与引用上限 |
| `server/services/presentations/presentation-ai.js`、应用路由 | 解析所选模板版本、生成填槽、禁止回退 |
| `server/services/presentations/presentation-renderer.js` | 统一实例化 IR，明确原生与降级对象及动画差异 |
| `client/chat/apps-workbench-presentations*.js` | 预检、报告、校对、创建选模、编辑器槽位与套版预览 |
| `server/db/migrations/` | 版本快照、导入任务/报告、来源和资产引用的兼容迁移 |
| `tests/presentation-*` | 导入、安全、权限、AI、视觉/导出与性能回归 |

现有 `presentation_templates.definition_json` 可先承载 v2 定义；是否单独建立版本/任务表，应按快照大小、查询模式和现有数据库约束评审。不能仅更新摘要并覆盖旧版定义。

## 6. 分期交付

| 阶段 | 交付内容 | 完成门槛 |
|---|---|---|
| P0 正确性 | 修复主题色提取；真实 PPTX 样本；AI 解析自定义模板；逐项报告；上传安全基线 | 新稿的模板 ID/版本、主题与所选一致；不支持项可定位 |
| P1 布局与素材 | v2 快照、多母版/占位符几何、母版图片入库、预览与发布前检查、空白创建实例化 | 封面和内容页编辑、预览、导出一致；旧版不漂移 |
| P2 AI 与套版 | 语义内容填槽、确定性排版、旧稿换主题/重新套版、溢出与冲突处理 | AI 无内置模板回退，品牌元素不被覆盖，异常可修复 |
| P3 动画与复杂对象 | 常见转场/入场映射、静态降级、PPTX 导出一致性 | 仅对预览及 PowerPoint/WPS 实测通过的动画宣称支持 |

先以获授权的真实组织模板试点，确认解析难度和资产授权后排期。阶段可独立上线，新能力以配置开关灰度。准备迁移回滚：关闭 v2 新建/导入不删除已有快照，旧版读取路径保持可用。

## 7. 测试与验收

### 7.1 样本和测试

- 建立经授权、脱敏的 PPTX 样本：16:9/4:3、多母版、主题引用色、中文/英文字体、背景图/Logo、透明图、占位符、转场、简单动画、图表和不支持对象。
- 单元测试覆盖关系解析、主题继承、坐标、图片、字体替代、资产权限、旧 schema 及错误报告；负例覆盖 ZIP 炸弹、路径穿越、外部关系、畸形 XML、重复关系和跨租户引用。
- 集成测试覆盖上传→预检→校对→审核→发布→AI/空白创建→编辑→导出→模板升级/下架；中断或失败不留下可见半成品。
- 固定字体环境逐页做视觉回归，机器差异评分加人工复核；检查文本溢出、缺图、层级及字体替换。
- 实际使用 PowerPoint/WPS 打开导出的 PPTX，核对可编辑对象及与预览/PDF 的差异。

### 7.2 建议验收指标

以下为试点目标，**不是当前已达成事实**；基线测试后按模板复杂度分层设定阈值。

| 指标 | 试点目标 |
|---|---|
| 可解释性 | 所有不支持/降级对象都有定位、原因和处置方式，无静默丢失 |
| 主题与布局 | 支持的颜色、字体、占位符逐项核对；缺失字体显式标记 |
| 生成 | 100% 使用请求的可访问模板版本；无未报告的越界/溢出 |
| 历史稳定性 | 模板升级/下架后旧稿预览和导出保持不变 |
| 安全权限 | 恶意包及跨租户引用被拒绝；宏/ActiveX 不执行；失败不泄漏资源 |
| 导出兼容 | 代表样本在 PowerPoint/WPS 可打开；承诺可编辑的对象可编辑；降级有说明 |

## 8. 风险与待决策项

1. **保真与可编辑冲突**：按布局/对象转换或静态降级；不得全稿栅格化后仍声称可编辑。
2. **版权与字体许可**：上传者确认图片/字体使用权；默认不嵌入无授权字体，替代结果须预览。
3. **OOXML 复杂性**：按真实模板统计覆盖率，不用单个样本的解析成功代表产品可用。
4. **存储成本**：不可变版本增加资源占用，使用内容寻址去重和有引用感知的回收。
5. **动画承诺**：P3 前只标注“可在应用内重新设置动画”，不宣称原动画已继承。
6. **环境差异**：不同 PowerPoint/WPS 版本与字体环境可能渲染不同，以代表环境实测约束承诺。

**立项需确认**：首批是否聚焦企业汇报类静态母版；试点组织和合法样本；可接受的静态降级比例；来源文件的保留期；目标 PowerPoint/WPS 版本。这些决定直接影响工期及验收范围。

## 9. 实施记录

本方案已在 2026-09-25 完成实现，核心交付如下：

- 使用关系驱动的 OOXML 解析替代正则扫描，支持主题、母版、布局、占位符、背景、受控图片、常见转场和简单入场动画的提取；外部关系、宏、ActiveX、嵌入对象和复杂动画均进入可定位兼容性报告。
- 模板定义升级为 v2，保存母版、布局几何、装饰层、素材引用、动画映射和不可变模板版本快照；源 PPTX/模板包也通过 CAS 留存，可下载审计并重新解析为新版本。
- 导入前预检、导入模式选择、兼容性报告、模板缩略预览、发布前资源/对比度/阻断项检查、模板审核流程均已接入工作台。
- PPTX 正式导入采用持久化任务：上传后返回任务 ID，源包进入 CAS，再由内存受限且无数据库/网络权限的 Worker 解析；客户端轮询任务状态。排队阶段可以取消，解析阶段会原子完成或失败并回收任务引用，避免出现可见半成品。
- 空白创建、材料转 PPT、AI 创建、继续生成、页面改写、套版和新增页面均按选定模板版本处理；AI 不再静默回退到内置模板。
- 已实现“主题替换”和“重新套版”两种操作。重新套版会应用槽位几何、样式、母版装饰、背景、页面转场及可映射入场动画。
- PPTX 导出保持原生文本、形状、图片、表格和图表；常见入场动画使用 PowerPoint 可识别的时间线结构写入导出包。

已执行并通过：PPT 导入/布局/动画回归测试、工作台契约测试、迁移覆盖检查、SQL 基线检查、开发规范检查、ESLint、CSS 构建和 Office 包验证。已使用本机 PowerPoint COM 只读打开含页面转场和淡入动画的导出样本，确认其识别 1 个动画序列。当前环境未安装 WPS 或 LibreOffice，因此这两个客户端的人工兼容性检查仍须在目标交付环境执行。
