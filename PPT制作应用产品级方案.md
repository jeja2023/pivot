# Pivot PPT 制作应用产品级方案

**文档状态**：产品化实现完成并完成产品级补齐（v0.1.167）
**适用项目**：Pivot 智枢 AI 智能中枢管理系统
**编制日期**：2026-09-22
**目标版本**：PPT 应用产品级补齐（v0.1.167）
**文档目的**：明确 PPT 制作应用的产品定位、功能边界、模板体系、AI 生成链路、编辑器设计、导出方案、系统集成、数据模型、权限审计、测试验收和分阶段实施计划。

---

## 1. 执行摘要

### 1.1 结论

在当前 Pivot 项目中开发 PPT 制作应用是可行的，而且不需要另起一套独立系统。推荐将其作为“应用中心”的新应用接入，以现有的 AI 模型路由、用户认证、文件处理、数据分析、Artifact 产物、版本管理、下载交付和 Electron 桌面能力为基础，新增一套面向幻灯片的结构化文档模型和可视化编辑器。

产品第一阶段不追求复刻 PowerPoint 的全部能力，而是聚焦于：

> AI 生成结构化演示文稿 → 套用模板 → 在线调整 → 数据和图表增强 → 导出 PPTX/PDF。

### 1.2 推荐的产品形态

产品名称暂定为“PPT 制作”或“智能演示文稿”。应用中心提供入口，用户可以从以下任一方式开始：

1. 输入主题、受众、时长和风格，由 AI 生成完整初稿；
2. 选择模板后手工创建演示文稿；
3. 上传 Word、PDF、Markdown、表格或粘贴材料，由 AI 转换成 PPT；
4. 从数据分析应用生成图表后，直接插入 PPT；
5. 打开历史演示文稿继续编辑、复制或导出。

### 1.3 核心设计原则

- 内容与版式分离：AI 生成内容结构，模板负责视觉规范，编辑器负责最终调整。
- 结构化优先：保存为可校验的 PPT IR（中间表示），不能只保存截图或 HTML。
- 模板可控：第一阶段使用 Pivot 自有模板格式，后续再兼容外部 `.pptx` 模板。
- 导出可追溯：编辑稿、渲染稿、下载交付都接入现有 Artifact 链路。
- 人机协同：AI 负责提案和批量修改，用户拥有最终确认权。
- 渐进式建设：先实现高频汇报场景，再扩展动画、协作和高级 PowerPoint 能力。

---

## 2. 当前项目基础与差距

### 2.1 可以直接复用的能力

当前项目已经具备较适合本应用的基础设施：

| 现有能力 | 项目位置或表现 | 对 PPT 应用的价值 |
|---|---|---|
| 应用中心注册与工作台 | `client/chat/apps-workbench-core.js` | 新增应用入口、应用卡片、打开和关闭机制 |
| 应用后端路由 | `server/routes/apps/index.js` 及 `server/routes/apps/*` | 新增 PPT 专属 API，沿用认证、异常和审计规范 |
| AI 调用和模型路由 | 应用路由及模型服务 | 复用模型选择、配额、上下文预算、流式输出和用量统计 |
| 数据分析和图表 | 数据分析应用、ECharts 资源 | 生成或复用柱状图、折线图、饼图等 PPT 图表 |
| 文件处理 | 文档、PDF、OCR、上传服务 | 读取用户材料、图片、PDF 和文档内容 |
| Artifact 版本链路 | `server/services/agent-artifacts.js` | 保存演示文稿源稿、版本、标题、备注和历史 |
| 二进制产物与渲染链路 | `server/services/agent-artifact-renditions.js` | 保存 PPTX、PDF、预览图等不可变渲染产物 |
| 下载交付和审计 | Artifact Delivery 相关服务与迁移 | 实现短时下载令牌、设备绑定和交付记录 |
| Electron 桌面端 | `desktop/`、打包配置 | 支持本地文件、桌面下载和后续离线能力 |
| CJK 字体资源 | `assets/fonts/cjk/` | 提高中文预览和渲染稳定性 |
| 现有依赖 | `package.json` | 已有文档、PDF、表格和图像处理基础；需补充 PPTX 生成/解析方案 |

### 2.2 主要新增能力

当前项目尚未形成完整的 PPT 专用能力，主要差距包括：

1. PPT 文档数据模型：页面、元素、母版、主题、布局、备注和资源引用。
2. 画布编辑器：选中、拖拽、缩放、对齐、层级、文字编辑和页面管理。
3. 模板系统：主题令牌、布局槽位、模板包、预览图和模板版本。
4. AI 幻灯片生成协议：模型必须返回结构化页面 JSON，而不是只返回 Markdown。
5. 版式校验：溢出、遮挡、低对比度、图片缺失、字体缺失和内容过密检测。
6. PPTX 导出：将 PPT IR 映射为可编辑的 PowerPoint 内容。
7. 预览渲染：将源稿渲染为缩略图、逐页预览和 PDF。
8. PPT 专属权限：模板发布、模板管理、文稿共享和导出权限。

### 2.3 技术判断

建议采用“结构化 IR + 浏览器编辑器 + 服务端导出”的方式。浏览器端负责交互和即时预览，服务端负责统一生成 PPTX/PDF、字体处理、资源整理和审计。这样可以避免不同浏览器产生不同导出结果，也便于桌面端、远程模式和未来批量生成保持一致。

不建议第一阶段直接把 PowerPoint 文件当作编辑器的内部数据源。任意 `.pptx` 文件内部包含主题、母版、占位符、关系文件、字体、图表、动画和嵌入对象，直接解析会显著扩大项目风险。第一阶段应先使用受控的 Pivot 模板格式；外部 `.pptx` 导入作为后续专项能力。

---

## 3. 产品定位、目标与边界

### 3.1 产品定位

Pivot PPT 制作应用定位为面向组织内部办公和知识工作的“AI 演示文稿工作台”，重点服务于：

- 工作汇报和项目汇报；
- 政策、制度、方案和培训材料讲解；
- 经营分析、数据汇报和会议演示；
- 从已有文档、知识库和数据快速生成演示稿；
- 统一单位或部门的品牌视觉模板。

### 3.2 产品目标

#### 业务目标

- 降低从材料到 PPT 初稿的时间成本。
- 提高组织内部 PPT 的结构完整性、视觉统一性和事实可追溯性。
- 将 Pivot 的知识库、数据分析和 Agent 产物能力转化为可交付的办公成果。
- 建立模板、素材和演示文稿资产的长期沉淀机制。

#### 用户目标

- 5 分钟内生成一份可编辑的 PPT 初稿。
- 用户可以快速替换模板、调整页面顺序和修改文字。
- 数据、图片、引用和来源能够保留上下文。
- 导出的 PPTX 在 PowerPoint、WPS 等常见软件中可正常打开。

#### 工程目标

- 复用现有认证、权限、审计、Artifact 和交付体系。
- PPT 文稿保存可版本化，渲染产物不可变且可校验。
- 预览和导出过程具备超时、取消、重试和错误提示能力。
- 模板和文稿数据具有明确版本，不因模板更新而破坏历史文稿。

### 3.3 MVP 范围

MVP 必须包含：

- 应用中心入口；
- 新建、打开、复制、重命名和删除演示文稿；
- AI 根据主题生成大纲和页面初稿；
- 6 至 10 个标准布局；
- 6 至 10 套内置主题模板；
- 文本、图片、形状、表格和图表的基本编辑；
- 页面缩略图、排序、复制和删除；
- 自动保存和版本记录；
- PPTX、PDF、PNG 预览导出；
- 权限校验、操作审计和错误恢复。

### 3.4 MVP 不包含

以下功能不作为第一阶段硬性范围：

- 与 PowerPoint 完全一致的动画时间轴；
- 复杂的音视频剪辑；
- 任意第三方 `.pptx` 模板的无损导入；
- 多人实时协同编辑；
- 复杂 SmartArt、宏、VBA 和嵌入式 OLE 对象；
- 完整的演讲者视图、提词器和远程投屏控制；
- 跨文稿全局搜索和资产市场。

---

## 4. 用户角色与典型场景

### 4.1 用户角色

| 角色 | 主要需求 | 关键权限 |
|---|---|---|
| 普通用户 | 快速生成和编辑 PPT | 创建、编辑、预览、导出自己的文稿 |
| 部门用户 | 使用部门模板和数据素材 | 使用部门模板，查看部门共享文稿 |
| 模板管理员 | 维护主题、模板和素材 | 创建、审核、发布、下架模板 |
| 审核人员 | 检查内容、格式和敏感信息 | 查看指定文稿、添加意见、导出审核稿 |
| 系统管理员 | 配置能力和治理风险 | 配置模型、限制资源、审计和清理 |

### 4.2 典型场景

#### 场景 A：AI 生成项目汇报

用户输入项目主题、汇报对象、汇报时长、已有材料和风格要求，系统生成 10 页左右的汇报初稿。用户选择模板，修改关键数字和图片，导出 PPTX。

#### 场景 B：材料转换为 PPT

用户上传 Word、PDF 或 Markdown，系统识别标题层级、事实、数据和结论，将内容拆分为章节页、正文页和总结页，并标识无法确认的内容。

#### 场景 C：数据分析结果进入 PPT

用户在数据分析应用中完成数据清洗和图表生成，将图表作为可追溯对象插入 PPT，保留数据集、查询条件和生成时间。

#### 场景 D：部门模板制作

模板管理员上传 Logo、品牌色、字体和版式规范，制作部门模板并发布。普通用户只能选择已发布模板，不能修改受控的品牌元素。

#### 场景 E：演示文稿版本回退

用户修改后发现页面布局被破坏，可以查看版本列表、对比差异、恢复某个版本，再重新生成导出产物。

---

## 5. 产品信息架构与用户流程

### 5.1 应用入口

在 `PIVOT_APP_REGISTRY` 中新增：

```js
{
    id: 'presentations',
    name: 'PPT 制作',
    category: '办公创作',
    description: '使用 AI、模板和数据快速制作可编辑演示文稿。',
    icon: 'presentation',
    tags: ['AI 生成', '模板设计', 'PPTX 导出'],
    status: 'available',
    openMode: 'inline'
}
```

前端建议新增模块：

```text
client/chat/apps-workbench-presentations.js
client/chat/apps-presentations/
  presentation-state.js
  presentation-api.js
  presentation-editor.js
  presentation-canvas.js
  presentation-template-panel.js
  presentation-ai-panel.js
  presentation-assets.js
  presentation-export.js
  presentation-validation.js
```

### 5.2 页面结构

应用建议分成四个主要区域：

1. **文稿库**：最近编辑、我的文稿、共享文稿、模板入口。
2. **创建向导**：主题、材料、受众、时长、页数、风格和模板选择。
3. **编辑工作区**：页面缩略图、中心画布、右侧属性和 AI 助手。
4. **导出与交付**：预览、质量检查、格式选择、下载和版本记录。

### 5.3 新建流程

```text
进入 PPT 制作
    ↓
选择“AI 生成 / 空白创建 / 材料转 PPT / 从模板创建”
    ↓
填写主题、受众、时长、页数、语言和风格
    ↓
选择模板和内容来源
    ↓
AI 生成大纲
    ↓
用户确认或调整大纲
    ↓
生成页面草稿
    ↓
版式校验与问题提示
    ↓
进入编辑器
    ↓
自动保存、预览、导出
```

### 5.4 编辑器布局

推荐采用桌面优先、窄屏可用的三栏布局：

| 区域 | 内容 |
|---|---|
| 左侧 220px | 页面缩略图、章节分组、添加页面、页面排序 |
| 中央自适应 | 16:9 画布、缩放、标尺、网格、参考线 |
| 右侧 320px | 页面属性、元素属性、主题、布局、素材和 AI 操作 |
| 顶部工具栏 | 撤销、重做、保存状态、预览、检查、导出 |

编辑器必须提供清晰的状态反馈：未保存、保存中、已保存、导出中、导出成功、导出失败。长时间操作不能只显示一个无期限的 loading。

---

## 6. 主题、模板与布局体系

### 6.1 三层模型

主题模板不能只保存为一张背景图片，建议拆成三层：

#### Theme：视觉规范

负责整套文稿的全局设计令牌：

- 主色、辅助色、强调色、文本色、背景色；
- 标题、正文、注释和数字字体；
- 字号层级、行距、段距和默认对齐方式；
- 图表颜色序列；
- 页面边距、圆角、阴影和线条；
- 页眉、页脚、页码和 Logo 规则。

#### Layout：页面结构

负责页面内容槽位和布局约束：

- 标题页；
- 章节过渡页；
- 标题加正文；
- 双栏图文；
- 三栏卡片；
- 大图页；
- 数据图表页；
- 表格页；
- 引用页；
- 总结与行动计划页。

布局只定义槽位和约束，不绑定具体内容。AI 生成的是 `layoutId + slots`，而不是任意 HTML。

#### Template：可交付模板

模板由 Theme、多个 Layout、素材和元数据组成，提供用户可以直接选择的完整视觉方案。

### 6.2 模板包格式

建议定义 `.pivot-ppt-template` 逻辑包，物理上可采用 ZIP：

```text
template.pivot-ppt-template
├─ manifest.json
├─ theme.json
├─ layouts/
│  ├─ cover.json
│  ├─ section.json
│  ├─ title-content.json
│  └─ chart.json
├─ assets/
│  ├─ logo.png
│  ├─ cover.jpg
│  └─ decoration.svg
├─ fonts/
└─ preview.png
```

`manifest.json` 至少包含：

```json
{
  "schemaVersion": "1.0",
  "templateId": "business-blue",
  "name": "商务蓝汇报",
  "description": "适用于项目、经营和工作汇报",
  "ownerType": "system",
  "status": "published",
  "tags": ["商务", "汇报", "数据"],
  "aspectRatio": "16:9",
  "defaultTheme": "theme.json",
  "layouts": ["cover", "section", "title-content", "chart"],
  "preview": "preview.png",
  "templateVersion": 1
}
```

### 6.3 模板管理流程

模板状态建议为：

```text
草稿 → 待审核 → 已发布 → 已下架
```

模板发布前必须检查：

- 所有引用的素材存在；
- 字体可用或已标记替代字体；
- 所有布局能通过渲染测试；
- 颜色对比度满足可读性要求；
- 主题色数量、字号层级和页面比例合法；
- 预览图和元数据完整；
- 没有指向本地绝对路径的资源；
- 模板不包含恶意脚本或可执行内容。

### 6.4 模板权限

模板应按租户、组织、部门和个人划分可见范围：

| 范围 | 说明 |
|---|---|
| 系统模板 | 产品内置，由系统管理员维护 |
| 组织模板 | 组织内所有用户可用 |
| 部门模板 | 指定部门可用 |
| 个人模板 | 仅创建者可用 |
| 共享模板 | 明确授权给指定用户或群组 |

历史文稿必须保存模板版本号和模板快照摘要。模板后续更新不能改变历史文稿的视觉结果。

### 6.5 外部 PPTX 模板导入策略

建议分三阶段：

1. **第一阶段**：只支持 Pivot 结构化模板。
2. **第二阶段**：支持导入 PPTX 的主题色、字体、背景和部分母版信息，导入后生成 Pivot 模板并提示人工校验。
3. **第三阶段**：支持更多占位符、图表和母版映射，但明确不承诺动画、宏和复杂嵌入对象无损导入。

---

## 7. 功能需求

### 7.1 文稿库

#### 必须支持

- 最近编辑列表；
- 按标题、创建者、更新时间和模板筛选；
- 新建、重命名、复制、归档和删除；
- 打开历史版本；
- 显示文稿状态：草稿、生成中、导出中、已完成、失败；
- 显示最后保存时间和最近导出格式。

#### 后续支持

- 标签和收藏；
- 部门共享空间；
- 文稿全文搜索；
- 文稿封面自定义；
- 从聊天记录或 Agent 任务产物一键打开。

### 7.2 AI 创建

创建向导应收集：

- 主题或标题；
- 目标受众；
- 演示目的；
- 演示时长；
- 目标页数；
- 语言；
- 风格；
- 是否需要数据图表；
- 内容来源；
- 是否保留原文引用；
- 禁用或必须出现的内容。

AI 生成流程分为两步：

1. 先生成大纲，用户确认章节和页面数量；
2. 再根据大纲生成具体页面。

这样可以避免模型一次性生成几十页内容后难以修改，也可以降低生成失败时的重试成本。

### 7.3 AI 改写和局部操作

选中页面或元素后，支持以下操作：

- 精简文字；
- 扩写说明；
- 改成正式、公文、汇报或培训语气；
- 转成要点；
- 调整页面结构；
- 自动补充结论；
- 根据数据生成图表；
- 为图片生成说明文字；
- 将一页拆成两页；
- 将多页压缩成一页；
- 检查事实、数字和引用。

AI 修改必须以补丁或结构化变更返回，不能直接覆盖整个文稿。用户确认后再写入正式版本。

### 7.4 页面和元素编辑

MVP 元素类型：

- 文本框；
- 富文本段落；
- 图片；
- 形状；
- 线条；
- 表格；
- 图表；
- 图标；
- 页码和页脚；
- 引用标记。

MVP 编辑能力：

- 选择、移动、缩放、旋转；
- 复制、粘贴、删除；
- 上移、下移、置顶、置底；
- 对齐、平均分布、组合和取消组合；
- 画布缩放和适应窗口；
- 文本字号、颜色、对齐、行距和列表；
- 图片裁剪、替换和适应方式；
- 表格行列增删；
- 图表类型和数据绑定修改；
- 页面复制、排序和删除；
- 撤销和重做。

### 7.5 预览与质量检查

导出前显示检查结果：

| 检查项 | 示例问题 |
|---|---|
| 文本溢出 | 标题或正文超过文本框边界 |
| 元素遮挡 | 图片覆盖正文或元素重叠 |
| 内容过密 | 一页文字过多，字号低于下限 |
| 图片资源 | 图片缺失、加载失败或分辨率过低 |
| 字体 | 字体不存在或回退后版式变化 |
| 可读性 | 文字与背景对比度不足 |
| 数据一致性 | 图表数据和正文结论不一致 |
| 占位符 | 仍存在“请填写”“XXX”等待替换内容 |
| 引用 | 外部材料存在但没有来源标注 |
| 页面结构 | 页面没有标题或内容槽位为空 |

检查分为三类：阻断问题、建议修复、提示信息。用户可以选择“忽略并导出”，但阻断问题应记录到审计事件。

### 7.6 导出

MVP 支持：

- `.pptx`：主交付格式；
- `.pdf`：固定版式交付；
- `.png`：单页或整套预览图；
- `.json`：结构化源稿备份，仅供系统恢复和高级用户使用。

导出选项：

- 16:9 或 4:3；
- 是否包含备注；
- 是否包含页码；
- 是否嵌入字体或使用替代字体；
- 图片质量；
- 是否显示引用和来源；
- 是否附带生成说明和版本号。

---

## 8. PPT IR 数据模型

### 8.1 顶层对象

推荐使用版本化的 JSON IR：

```json
{
  "schemaVersion": "1.0",
  "presentationId": "pres_01HXXX",
  "title": "项目建设汇报",
  "aspectRatio": "16:9",
  "width": 1280,
  "height": 720,
  "template": {
    "id": "business-blue",
    "version": 3,
    "snapshotDigest": "sha256:..."
  },
  "theme": {},
  "slides": [],
  "assets": [],
  "sources": [],
  "notes": {},
  "metadata": {
    "createdBy": 1001,
    "createdAt": "2026-09-22T00:00:00.000Z",
    "updatedAt": "2026-09-22T00:00:00.000Z"
  }
}
```

### 8.2 页面对象

```json
{
  "id": "slide_001",
  "index": 0,
  "type": "cover",
  "layoutId": "cover",
  "sectionId": "section_01",
  "background": {
    "fill": "#F7F9FC",
    "imageAssetId": null,
    "opacity": 1
  },
  "elements": [],
  "speakerNotes": "本页介绍汇报背景。",
  "sources": [],
  "validation": {
    "status": "passed",
    "issues": []
  }
}
```

### 8.3 元素对象

所有元素统一使用边界框和变换属性：

```json
{
  "id": "element_title_001",
  "type": "text",
  "x": 80,
  "y": 60,
  "width": 1120,
  "height": 100,
  "rotation": 0,
  "zIndex": 10,
  "locked": false,
  "visible": true,
  "style": {
    "fontFamily": "Microsoft YaHei",
    "fontSize": 36,
    "fontWeight": 700,
    "color": "#1F2937",
    "align": "left",
    "verticalAlign": "middle",
    "lineHeight": 1.2
  },
  "content": {
    "text": "项目建设目标"
  },
  "sourceRefs": []
}
```

### 8.4 图表对象

图表不建议只保存为图片。应保存数据、配置和渲染结果：

```json
{
  "id": "chart_001",
  "type": "chart",
  "chartType": "bar",
  "data": {
    "columns": ["季度", "完成率"],
    "rows": [
      ["Q1", 0.72],
      ["Q2", 0.86]
    ]
  },
  "binding": {
    "datasetId": "dataset_001",
    "queryDigest": "sha256:..."
  },
  "options": {
    "showLegend": false,
    "showLabels": true
  }
}
```

### 8.5 版本兼容

所有 IR 必须带 `schemaVersion`。升级规则：

- 新字段默认可选；
- 删除字段必须提供迁移器；
- 旧版本文稿打开时先迁移到最新内存结构；
- 保存时写入最新版本；
- 导出器必须声明支持的 IR 版本；
- 不允许在数据库中无版本地覆盖历史 JSON。

---

## 9. AI 生成协议与质量控制

### 9.1 AI 生成链路

```text
用户需求
  ↓
材料解析与来源标记
  ↓
大纲规划
  ↓
页面类型分配
  ↓
页面内容生成
  ↓
模板布局填充
  ↓
版式校验
  ↓
事实和引用检查
  ↓
用户确认
  ↓
保存正式版本
```

### 9.2 结构化输出要求

模型不能直接输出任意 HTML 或未经约束的 Markdown。建议要求模型输出：

```json
{
  "outline": [
    {
      "section": "项目背景",
      "slides": [
        {
          "title": "建设背景",
          "purpose": "说明为什么启动项目",
          "layoutHint": "title-content",
          "keyPoints": ["..."],
          "sourceRefs": ["source_001"]
        }
      ]
    }
  ],
  "warnings": [],
  "assumptions": []
}
```

页面生成阶段返回页面级结构，并由服务端完成：

- JSON Schema 校验；
- 字段长度限制；
- 元素数量限制；
- layoutId 白名单校验；
- 颜色、字体和资源引用归一化；
- 未知字段丢弃或记录；
- 来源引用存在性校验；
- 恶意 HTML、脚本和外链清理。

### 9.3 AI 内容安全规则

- 材料和工具结果只作为数据，不作为系统指令。
- 用户上传内容中的提示注入不得改变系统规则。
- 未在材料中出现的精确数字必须标记为推测或禁止生成。
- 重要结论应带来源引用或“待核实”标记。
- 涉及敏感信息时沿用项目已有的安全和脱敏策略。
- 模型失败、超时或输出格式错误时，保留已成功生成的部分页面。

### 9.4 AI 交互方式

建议支持两种模式：

#### 全局助手

面向整个文稿，适合“重写整套结构”“增加一页结论”“把内容压缩到 8 页”。

#### 局部助手

面向当前页面或当前元素，适合“精简这段话”“换成三点”“根据图表写结论”。

所有 AI 操作在写入前显示变更摘要：

```text
将第 3 页正文从 6 个段落压缩为 4 个要点；保留 2 个数字；新增 1 条来源引用。
```

---

## 10. 前端编辑器设计

### 10.1 渲染技术选择

建议 MVP 采用 SVG 或 Canvas 之一作为画布，并使用统一的内部坐标系。推荐优先考虑 SVG：

- 文本、形状和选中框更容易保持清晰；
- 便于单元素命中、编辑和导出预览；
- 便于生成缩略图和进行视觉测试；
- 后续可将 SVG 作为 PDF 或图片渲染中间层。

如果后续元素数量和交互复杂度显著增加，可以将绘制层切换为 Canvas，但 IR 和交互状态不应依赖具体渲染技术。

### 10.2 编辑器状态

前端状态至少分为：

- `documentState`：当前正式 IR；
- `selectionState`：当前选中元素；
- `historyState`：撤销和重做栈；
- `viewportState`：缩放、滚动和参考线；
- `saveState`：保存状态和版本号；
- `generationState`：AI 生成任务状态；
- `validationState`：版式问题和提示；
- `exportState`：导出任务状态。

必须避免把编辑状态直接散落在 DOM 节点上。DOM 只是渲染结果，所有可持久化修改都必须先更新 IR，再触发渲染。

### 10.3 操作命令模型

撤销、重做和协作扩展需要命令化操作：

```json
{
  "command": "update-element",
  "slideId": "slide_001",
  "elementId": "element_title_001",
  "patch": {
    "content.text": "新的标题",
    "style.fontSize": 34
  }
}
```

命令应具备：

- 可验证；
- 可序列化；
- 可撤销；
- 可记录审计；
- 可批量合并；
- 可在保存失败后重放。

### 10.4 自动排版策略

布局引擎至少实现：

- 文本自适应：优先增高、换行、缩小字号，最后才截断；
- 要点数量过多时自动拆页；
- 图文布局中的图片裁剪策略；
- 卡片等宽和等间距；
- 标题和正文的最小间距；
- 页边距安全区；
- 元素重叠检测；
- 模板槽位空缺检测。

自动排版不能悄悄破坏用户的手工调整。对用户已锁定或手工移动的元素，应降低自动重排优先级。

---

## 11. 后端模块与接口设计

### 11.1 建议新增服务模块

```text
server/services/presentations/
  presentation-service.js
  presentation-schema.js
  presentation-templates.js
  presentation-layout.js
  presentation-validation.js
  presentation-ai.js
  presentation-renderer.js
  presentation-export.js
  presentation-assets.js
```

建议新增路由：

```text
server/routes/apps/presentations.js
```

再由 `server/routes/apps/index.js` 挂载。

### 11.2 文稿接口

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/apps/presentations` | 查询当前用户可见文稿 |
| POST | `/apps/presentations` | 创建空白或初始文稿 |
| GET | `/apps/presentations/:id` | 获取文稿及当前版本 |
| PATCH | `/apps/presentations/:id` | 修改标题、状态和元数据 |
| PUT | `/apps/presentations/:id/content` | 保存完整 IR 版本 |
| POST | `/apps/presentations/:id/duplicate` | 复制文稿 |
| DELETE | `/apps/presentations/:id` | 软删除文稿 |
| GET | `/apps/presentations/:id/versions` | 查询版本 |
| GET | `/apps/presentations/:id/versions/:version` | 获取指定版本 |
| POST | `/apps/presentations/:id/rollback` | 从历史版本创建新版本 |
| GET | `/apps/presentations/:id/validation` | 获取检查结果 |

### 11.3 AI 接口

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/apps/presentations/ai/outline` | 生成大纲 |
| POST | `/apps/presentations/ai/slides` | 根据大纲生成页面 |
| POST | `/apps/presentations/:id/ai/continue` | 继续补写页面 |
| POST | `/apps/presentations/:id/ai/rewrite` | 生成结构化修改建议 |
| POST | `/apps/presentations/:id/ai/validate` | 内容和事实检查 |

AI 接口需要支持请求幂等键、取消、超时、模型记录、用量记录和失败重试。

### 11.4 模板接口

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/apps/presentations/templates` | 查询可用模板 |
| POST | `/apps/presentations/templates` | 创建模板草稿 |
| GET | `/apps/presentations/templates/:id` | 获取模板详情 |
| PUT | `/apps/presentations/templates/:id` | 修改模板 |
| POST | `/apps/presentations/templates/:id/publish` | 发布模板 |
| POST | `/apps/presentations/templates/:id/unpublish` | 下架模板 |
| POST | `/apps/presentations/templates/import` | 导入模板包 |
| GET | `/apps/presentations/templates/:id/preview` | 获取预览图 |

模板管理接口必须实施管理员或模板管理能力检查，不能只依赖前端隐藏按钮。

### 11.5 导出接口

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/apps/presentations/:id/render` | 生成预览图或 PDF |
| POST | `/apps/presentations/:id/export` | 创建 PPTX/PDF/PNG 导出任务 |
| GET | `/apps/presentations/:id/exports` | 查询导出历史 |
| GET | `/apps/presentations/exports/:exportId` | 查询导出状态 |
| POST | `/apps/presentations/exports/:exportId/cancel` | 取消导出 |
| POST | `/apps/presentations/exports/:exportId/download-intent` | 创建下载意图 |

导出结果不建议直接从业务路由裸返回文件。应接入现有 Artifact → Rendition → Delivery 交付链路。

---

## 12. 数据库和持久化设计

### 12.1 推荐新增表

表名可根据项目数据库命名规范调整：

#### `presentation_documents`

保存文稿主信息：

- `id`；
- `tenant_id`；
- `owner_id`；
- `title`；
- `status`；
- `template_id`；
- `template_version`；
- `aspect_ratio`；
- `current_version_id`；
- `cover_object_id`；
- `created_at`、`updated_at`、`deleted_at`。

#### `presentation_versions`

保存结构化 IR：

- `id`；
- `presentation_id`；
- `version`；
- `schema_version`；
- `content_json` 或 `ir_object_id`；
- `content_digest`；
- `note`；
- `created_by`；
- `created_at`。

大文稿建议优先使用对象存储/CAS 保存正文，数据库保存摘要、引用和元数据，避免单行 JSON 无限膨胀。

#### `presentation_templates`

保存模板元数据、可见范围、状态和版本。

#### `presentation_template_versions`

保存模板包、主题、布局、资源清单和版本摘要。

#### `presentation_assets`

保存图片、字体、图标、图表快照的引用，不直接信任客户端提供的存储路径。

#### `presentation_exports`

保存导出任务、格式、渲染器版本、状态、错误和对应 rendition。

### 12.2 与现有 Artifact 体系的关系

推荐采用以下映射：

```text
presentation_documents       文稿业务实体
presentation_versions        文稿编辑版本
agent_artifacts              可交付的逻辑产物
agent_artifact_objects       IR、图片和二进制内容
agent_artifact_renditions    PPTX、PDF、PNG 等不可变渲染结果
agent_artifact_delivery_*    下载交付和审计
```

建议一份正式文稿至少对应一个独立 Artifact，导出格式作为该 Artifact 的不同 rendition。AI 生成的草稿可以先保存为草稿版本，用户确认后再标记为正式版本。

### 12.3 保存策略

- 编辑器本地先维护未提交操作队列；
- 典型情况下 1 至 3 秒防抖保存；
- 页面切换、导出、关闭前主动 flush；
- 保存请求携带 `baseVersion`，防止覆盖其他版本；
- 冲突时返回版本冲突，而不是静默覆盖；
- 大型图片和字体使用对象存储/CAS，不能全部内嵌 JSON；
- 自动保存失败时保留本地草稿，并提供恢复入口。

---

## 13. 导出、渲染与兼容性方案

### 13.1 导出分层

建议把导出分成三个层次：

1. **预览渲染**：快速生成缩略图和页面预览，服务编辑器。
2. **固定版式渲染**：生成 PDF/PNG，追求视觉一致性。
3. **可编辑 PPTX 导出**：将文本、形状、图片、表格和图表映射为 PowerPoint 原生对象。

### 13.2 PPTX 导出要求

导出器至少要处理：

- 页面尺寸和比例；
- 背景色和背景图；
- 文本字体、字号、粗细、颜色、对齐和换行；
- 图片裁剪和透明度；
- 形状、线条和圆角；
- 表格行列、边框和填充；
- 图表或图表快照；
- 页码、页脚和 Logo；
- 备注和来源信息；
- 中文字体回退；
- 图片资源嵌入；
- 导出前资源完整性检查。

### 13.3 导出工具选型原则

当前项目的依赖中已有文档、PDF、表格和图像处理能力，但没有明确的 PPTX 专用生成依赖。选型应重点比较：

- 是否支持原生文本、形状、图片、表格和图表；
- 是否支持中文字体和字体回退；
- 是否能在 Node.js 和 Electron 打包环境运行；
- 是否支持服务端无头运行；
- 是否允许商业或组织内部使用；
- 是否能生成稳定的 OOXML；
- 是否便于审计、升级和锁定版本；
- 是否能处理图片、字体和临时文件安全问题。

依赖引入前必须经过项目已有的依赖覆盖、许可证和安全检查脚本。不能因为浏览器端预览正常，就默认 PPTX 导出结果兼容 PowerPoint 或 WPS。

### 13.4 兼容性验证矩阵

至少验证：

| 环境 | 验证内容 |
|---|---|
| Microsoft PowerPoint | 打开、编辑、保存、重新打开 |
| WPS Office | 打开、文字和图表显示、导出 PDF |
| LibreOffice | 基本打开和 PDF 渲染 |
| Pivot 预览 | 与导出 PDF 视觉差异 |
| Windows 桌面端 | 中文字体、文件保存、下载权限 |
| 远程部署 | 服务端字体、临时目录和大文件下载 |

### 13.5 导出失败处理

错误信息必须面向用户可理解，并保留技术追踪 ID：

- 字体缺失：提示替代字体并允许继续；
- 图片过大：提示压缩或自动压缩；
- 页面结构非法：定位到具体页面和元素；
- 渲染超时：可重试并保留任务状态；
- 资源对象失效：提供重新上传入口；
- 导出器异常：不产生半成品下载链接。

---

## 14. 权限、安全与审计

### 14.1 访问控制

每次读取或修改都必须验证：

- 当前用户身份；
- 租户归属；
- 文稿所有权或共享关系；
- 模板可见范围；
- 导出权限；
- 素材访问权限；
- 管理员能力和部门范围。

不能只根据客户端传来的 `presentationId`、`templateId` 或对象存储 key 访问资源。

### 14.2 文件安全

- 校验 MIME、扩展名、文件大小和图片真实格式；
- 对 SVG、HTML、Office 文件和压缩包做内容安全处理；
- 禁止模板包执行脚本；
- 解压 ZIP 时防止路径穿越；
- 下载使用短时、一次性、设备绑定令牌；
- 不将服务器绝对路径暴露给客户端；
- 对导出临时目录进行生命周期清理；
- 记录文件摘要，防止下载内容被替换。

### 14.3 审计事件

建议记录：

- 创建、复制、删除、恢复文稿；
- AI 生成大纲和页面；
- AI 改写和用户确认；
- 模板创建、审核、发布和下架；
- 上传和删除素材；
- 导出、下载、失败和取消；
- 权限变更和共享变更；
- 忽略阻断级质量问题后导出。

审计内容保存必要的摘要、操作者、时间、资源 ID、结果和 trace ID，不保存不必要的原始敏感材料。

### 14.4 内容治理

- 生成内容必须区分原文事实、模型推断和用户输入；
- 数字、日期、政策名称和组织名称应支持引用标记；
- 对上传材料执行已有的安全、脱敏和内容审查策略；
- 导出前支持敏感信息扫描；
- 删除文稿采用软删除和延迟清理，符合现有数据治理规则。

---

## 15. 非功能需求

### 15.1 性能目标

MVP 建议目标：

- 20 页以内文稿首次打开时间不超过 3 秒；
- 普通页面操作反馈不超过 100ms；
- 自动保存请求 P95 不超过 2 秒；
- 20 页预览渲染 P95 不超过 15 秒；
- 20 页普通 PPTX 导出 P95 不超过 30 秒；
- 单个图片资源默认不超过 20MB；
- 单份文稿默认不超过 100MB；
- 单次 AI 生成页数和输入材料大小可配置限流。

具体指标应在压测后根据部署环境校准，不能把本地开发机的结果直接作为生产承诺。

### 15.2 可靠性

- 生成任务可取消、可重试、可恢复；
- 导出任务幂等；
- 同一 IR、同一格式、同一渲染器版本应可复用渲染结果；
- 导出失败不影响源稿和历史版本；
- 数据库保存失败不应丢失浏览器端未提交草稿；
- 版本和导出结果必须能通过摘要校验。

### 15.3 可观测性

每个 AI、渲染和导出任务都应包含：

- `requestId`；
- `traceId`；
- `presentationId`；
- `versionId`；
- `templateId` 和版本；
- 模型标识和调用次数；
- 输入输出大小；
- 渲染器版本；
- 耗时、状态和错误码；
- 操作用户和租户。

### 15.4 国际化和中文支持

第一阶段以中文为主，但 IR 不应把中文写死：

- 文本编码统一 UTF-8；
- 字体按主题配置；
- 文本方向和语言属性可扩展；
- 日期、数字和图表格式可本地化；
- 预留英文和中英双语模板字段。

---

## 16. 测试策略与验收标准

### 16.1 单元测试

覆盖：

- IR Schema 校验；
- 模板包解析和路径安全；
- 主题合并和默认值；
- 布局计算；
- 文本溢出检测；
- 元素重叠检测；
- AI 输出归一化；
- 版本号和并发保存；
- 导出任务幂等；
- 权限判断；
- 下载令牌和摘要校验。

### 16.2 集成测试

至少覆盖：

1. 创建文稿 → 保存版本 → 查询文稿；
2. AI 生成大纲 → 确认 → 生成页面；
3. 上传图片 → 引用图片 → 导出 PPTX；
4. 数据分析图表 → 插入 PPT → 生成 PDF；
5. 模板发布 → 普通用户可见 → 模板下架后不可新建；
6. 版本冲突 → 返回冲突 → 用户选择保留或恢复；
7. 导出失败 → 重试 → 成功下载；
8. 无权访问他人文稿、模板和素材时返回一致错误。

### 16.3 端到端测试

使用 Playwright 或项目现有 E2E 体系验证：

- 应用入口和工作台打开；
- 新建向导；
- 页面选择和编辑；
- 撤销/重做；
- 自动保存；
- 模板切换；
- 预览检查；
- 导出和下载；
- 刷新后恢复文稿；
- 小屏和桌面端布局。

### 16.4 视觉回归测试

内置模板必须有基准页面：

- 首次渲染截图；
- 关键字体和颜色检查；
- 文字溢出检查；
- 页面尺寸检查；
- PDF 与浏览器预览差异检查；
- PowerPoint/WPS 实机抽样检查。

### 16.5 验收标准

MVP 验收至少满足：

- 用户可以从应用中心进入 PPT 制作应用；
- 用户可以基于模板创建并保存 20 页以内文稿；
- AI 能生成可校验的大纲和页面结构；
- 所有页面可以预览、排序、复制和删除；
- 文本、图片、表格、图表可以基础编辑；
- 自动保存和版本恢复可用；
- PPTX 在 PowerPoint 和 WPS 中可打开；
- PDF 与应用预览不存在严重错位；
- 导出结果经过 Artifact 交付链路；
- 无权用户无法读取或下载文稿和素材；
- 关键操作有审计记录；
- 现有项目检查、测试和打包流程不被破坏。

---

## 17. 分阶段实施计划

### 阶段 0：技术预研与方案冻结

**目标**：确定 IR、模板包、画布技术和导出器。

交付物：

- IR Schema 1.0；
- 模板包规范；
- 画布技术验证；
- PPTX/PDF 导出器技术验证；
- 字体和中文兼容性验证；
- 依赖许可证和打包验证；
- 关键风险清单。

退出条件：能够用一个最小 IR 生成一页预览、PDF 和可打开的 PPTX。

### 阶段 1：MVP 数据闭环

**目标**：先打通文稿保存、模板、预览和导出。

交付物：

- 应用入口；
- 文稿库；
- 空白创建；
- 主题和布局；
- 文本、图片、形状基础编辑；
- 自动保存和版本；
- 预览；
- PPTX/PDF/PNG 导出；
- 基础权限和审计。

退出条件：用户不依赖 AI 也能制作并导出一份基本 PPT。

### 阶段 2：AI 生成和材料转换

**目标**：实现产品差异化能力。

交付物：

- AI 创建向导；
- 大纲确认；
- 页面批量生成；
- 文档/PDF/表格材料转换；
- 页面级 AI 改写；
- 来源引用；
- 生成任务取消和重试；
- 事实和结构检查。

退出条件：用户可以从主题或材料在合理时间内生成可编辑初稿。

### 阶段 3：数据、模板和治理增强

**目标**：建立组织级使用能力。

交付物：

- 数据分析图表绑定；
- 部门模板管理；
- 模板审核与发布；
- 模板包导入导出；
- 品牌 Logo、字体和页脚控制；
- 敏感信息检查；
- 模板和素材统计。

退出条件：组织可以维护自己的品牌模板并进行规模化使用。

### 阶段 4：高级编辑和协作

**目标**：提升专业制作能力。

可选交付物：

- 多人协同和评论；
- 演讲者备注和提词器；
- 动画和转场；
- PPTX 模板导入；
- 高级表格、SmartArt 和嵌入对象；
- 远程演示和投屏；
- 模板市场和素材市场。

---

## 18. 运营指标与产品指标

### 18.1 使用指标

- PPT 应用日活、周活和月活；
- 新建文稿数量；
- AI 生成使用率；
- AI 大纲确认率；
- 生成初稿到首次导出的转化率；
- 模板使用次数和模板复用率；
- 数据图表插入次数；
- 平均文稿页数；
- 人均导出次数。

### 18.2 质量指标

- AI 生成成功率；
- AI 输出 Schema 校验通过率；
- 自动排版失败率；
- 导出成功率；
- PowerPoint/WPS 打开失败率；
- 文本溢出率；
- 用户手工大幅调整比例；
- 导出后重新编辑比例。

### 18.3 性能指标

- 首次打开耗时；
- 自动保存 P95；
- 预览渲染 P95；
- PPTX 导出 P95；
- 导出任务超时率；
- 前端错误率；
- 资源加载失败率。

### 18.4 体验指标

- 用户完成首份 PPT 的平均时间；
- 从创建到导出的步骤数；
- 模板选择放弃率；
- 导出失败后的重试成功率；
- 用户反馈中的“内容有用”“版式可用”“导出可用”评分。

---

## 19. 主要风险与应对

| 风险 | 影响 | 应对方案 |
|---|---|---|
| PPTX 导出兼容性不足 | 高 | 先做小范围元素白名单，建立 PowerPoint/WPS 回归样本 |
| 中文字体缺失造成错位 | 高 | 统一字体策略、字体回退、服务端字体打包和导出检查 |
| AI 页面内容过密 | 高 | 先生成大纲，限制页面内容，加入溢出和拆页规则 |
| 任意 PPTX 模板解析复杂 | 高 | 第一阶段使用自有模板包，外部导入单独立项 |
| 编辑器状态复杂 | 高 | IR 与 DOM 分离，使用命令模型和版本化状态 |
| 大图片导致性能下降 | 中 | 图片压缩、缩略图、懒加载和资源上限 |
| 模型输出注入或恶意 HTML | 高 | 结构化协议、Schema 校验、白名单和安全清洗 |
| 并发保存覆盖数据 | 高 | baseVersion、乐观锁、冲突提示和草稿恢复 |
| 导出临时文件泄露 | 高 | 临时目录隔离、短时令牌、生命周期清理和审计 |
| 新依赖影响桌面打包 | 中 | 在阶段 0 验证 Electron、远程模式、Windows 和 Linux 打包 |
| 模板版本更新破坏历史文稿 | 中 | 保存模板版本和快照摘要，历史文稿绑定固定版本 |
| 过早开发实时协作 | 中 | MVP 先做单用户可靠编辑，协作后置 |

---

## 20. 推荐的首批内置模板

第一批建议提供 8 套模板，每套至少包含封面、章节页、正文页、双栏页、图表页和总结页：

1. 商务蓝：项目和经营汇报；
2. 科技深色：技术方案和产品发布；
3. 政务简洁：政策、制度和工作汇报；
4. 党建红色：党建、宣传和会议材料；
5. 教学培训：课程、培训和知识讲解；
6. 数据分析：指标、趋势和经营分析；
7. 极简白：高密度信息和正式汇报；
8. 绿色公益：环保、公益和社会项目。

每套模板应提供三类信息：

- 适用场景；
- 不适用场景；
- 推荐页数和内容密度。

AI 选模板时不要只根据用户输入的“商务”“简洁”等标签，还应结合受众、演示目的、页数、是否包含数据图表以及组织品牌要求。

---

## 21. 推荐开发顺序

建议按以下顺序实施：

1. 冻结 IR Schema 和模板包格式；
2. 完成空白文稿保存、读取和版本；
3. 完成模板、布局和基础画布；
4. 完成文本、图片、形状和页面操作；
5. 打通预览、PDF 和 PNG；
6. 接入 PPTX 导出器并建立兼容性样本；
7. 接入 Artifact、Rendition 和 Delivery；
8. 增加 AI 大纲和页面生成；
9. 接入文档、PDF、表格和数据分析来源；
10. 增加模板管理、权限和治理；
11. 做性能、视觉回归和桌面端打包验证；
12. 小范围试用后再扩展高级能力。

不建议先从“AI 一键生成整套 PPT”开始，因为如果 IR、模板和导出基础不稳定，AI 生成的结果无法可靠编辑、保存和交付。

---

## 22. 方案决策清单

正式开发前需要确认以下事项：

- PPT 应用的正式名称；
- 首批用户是个人用户、部门用户还是组织级用户；
- 是否必须支持 4:3；
- 首发是否要求 PowerPoint 和 WPS 双重兼容；
- 是否允许用户上传字体；
- 是否需要从已有 `.pptx` 导入模板；
- PPTX 导出采用服务端还是桌面端优先；
- 是否保存原始材料和引用关系；
- 是否允许共享文稿；
- 是否需要审核后才能导出；
- 默认单份文稿页数和文件大小上限；
- 是否需要组织品牌模板强制锁定 Logo、页脚和字体；
- 是否要求离线制作和离线导出；
- 是否将 PPT 作为 Agent 可调用的正式产物类型。

如果没有额外要求，建议默认决策为：首发中文、16:9、单用户编辑、组织内模板、服务端导出、保留来源、支持 PPTX/PDF/PNG，不支持任意 PPTX 模板无损导入。

---

## 23. 最终建议

Pivot 的 PPT 制作应用应当被设计为一个“结构化演示文稿生产系统”，而不是单纯的图片排版工具或聊天式 PPT 生成器。

最关键的工程决策有三个：

1. 以版本化 PPT IR 作为唯一事实源；
2. 以 Theme、Layout、Template 三层模型管理主题模板；
3. 以 Artifact → Rendition → Delivery 作为保存、渲染和下载链路。

只要先把这三个基础层建立起来，AI 生成、模板切换、数据图表、PDF/PPTX 导出、版本恢复、权限审计和后续多人协作都可以在同一套架构上持续演进。

从产品投入产出比看，建议优先完成“模板化 AI PPT 工作台”，而不是一开始追求 PowerPoint 全功能替代。该路线最符合 Pivot 当前的应用中心架构，也能最快验证用户价值和导出质量。

---

## 24. v0.1.164 实施回执

本方案的 MVP 已于 `v0.1.164` 实施完成。实现采用“结构化 PPT IR + 浏览器工作台 + 服务端受控导出”的架构，源码、迁移、测试和发布记录均已纳入仓库。

| 方案要求 | 实现状态 | 对应交付 |
|---|---|---|
| 应用中心入口与工作台 | 已完成 | `presentations` 应用注册、文稿库与编辑工作区 |
| 6 至 10 套主题与布局 | 已完成 | 8 套内置主题、10 类标准布局、组织模板创建/提交审核/审核发布及受校验模板包导入/导出 |
| 版本化 PPT IR | 已完成 | `presentation-schema.js`、`presentation_documents`、`presentation_versions` |
| 文本、图片、形状、表格、图表编辑 | 已完成 | 画布选择/拖拽、属性编辑、图片上传、表格/图表 JSON 编辑、数据分析图表插入 |
| 页面管理与撤销恢复 | 已完成 | 页面新增、复制、排序、删除、撤销/重做和服务端版本恢复 |
| AI 大纲与页面生成 | 已完成 | AI 双阶段接口、结构化协议、Schema 校验、材料来源边界 |
| 材料转换和数据协同 | 已完成 | Word/PDF/表格/Markdown 文本提取、数据分析图表绑定 |
| 预览和质量检查 | 已完成 | PNG 页面预览渲染与溢出、遮挡、低对比度、占位符、内容密度、敏感信息检查 |
| PPTX / PDF / PNG 导出 | 已完成 | 同一 IR 服务端渲染，PPTX、PDF、PNG 二进制测试覆盖 |
| Artifact / CAS / Delivery | 已完成 | 独立 Artifact、受控 CAS 素材、不可变 rendition、短时下载令牌和交付审计 |
| 权限和审计 | 已完成 | 租户与所有者双重过滤、管理员模板操作、上传校验和业务审计 |
| 数据库迁移 | 已完成 | `202609220001_presentation_workbench_foundation` |
| 测试与发布文档 | 已完成 | `presentation-workbench.test.js`、更新日志、使用帮助、生产升级说明和发布记录 |

本方案第 3.4、17.4 所列的高级能力在 v0.1.164 首发时明确排除，未计入当时的完成判定。后续实现仍必须以 PPT IR、主题模板三层模型和 Artifact 交付链为兼容边界，不得以浏览器自由 HTML 或未审计文件路径替代。

## 25. 产品级补齐记录（v0.1.167）

在 v0.1.164 MVP 基础上，以下计划任务已完成并纳入代码、迁移、自动化测试与发布记录：

| 任务 | 状态 | 说明 |
|---|---|---|
| AI 继续生成、改写与内容检查 | 已完成 | 结构化接口、取消、幂等、失败状态、前端操作与变更确认均已闭环。 |
| 模板审核、部门范围与统计 | 已完成 | 草稿 → 待审核 → 已发布/下架，支持组织/部门范围、品牌控制及管理员统计。 |
| 自定义字体与素材库 | 已完成 | TTF/OTF/WOFF/WOFF2 经过签名检查、受控 CAS 保存、主题应用、浏览器加载与 PDF 嵌入；素材库支持图片、字体、音频、视频和附件。 |
| 外部 PPTX 模板导入 | 已完成（受限） | 提取主题色、中文/拉丁字体、背景、比例和布局；宏、ActiveX、嵌入对象、媒体、原生图表和复杂动画均会提示并隔离。 |
| AI、媒体、图示与动画 | 已完成（受控） | 支持流程图、音视频、附件、元素入口动画和页面转场；浏览器演示/远程展示播放受控动画，PPTX 写入页面转场并导出原生媒体卡片。 |
| 宏/VBA 治理 | 已完成（静态审计） | bas、vba、pptm、ppsm 作为受控附件处理，静态检查 Shell、网络、文件系统和自动执行入口；服务端与浏览器绝不执行宏。 |
| 评论、协作者与实时协作 | 已完成 | 评论线程、角色权限、Presence、SSE 推送、断线轮询回退和乐观锁冲突提示均已实现。 |
| 共享文稿库 | 已完成 | 支持共享可见性、标题/全文/标签/收藏筛选、个人收藏和编辑者安全续用既有受控素材。 |
| 演讲者备注与提词器 | 已完成 | 逐页备注、PPTX 备注、计时演示者模式与键盘翻页均已实现。 |
| 远程演示与投屏 | 已完成 | 所有者/管理员创建短时令牌展示会话，控制端翻页，展示端只读取脱敏 IR 和会话允许的受控素材。 |
| 视觉、性能与运营观测 | 已完成 | 8 套模板 PNG SHA-256 快照；20 页 10 次本机基准 PDF P95 88ms、PPTX P95 20ms；Prometheus 和管理员看板记录操作结果与耗时。 |
| Office 兼容性矩阵 | 已完成（可执行） | npm run verify:presentation:office 验证 OOXML、备注、转场、包结构并探测 PowerPoint/WPS/LibreOffice；本机已探测到 PowerPoint，WPS 与 LibreOffice 未安装。 |

### 已冻结的安全与兼容性边界

- 不实现任意 Office 二进制对象、ActiveX、VBA 或宏的服务器/浏览器执行；相关文件只可作为静态审计附件。
- 不承诺第三方 PPTX 的复杂母版、SmartArt、嵌入对象、动画时间轴和媒体逐字节无损还原；导入器保留可控主题信息并返回兼容性提示。
- 已安装客户端的真实打开、编辑、另存与重新打开验证必须在对应软件所在目标机执行；仓库已提供环境探测、OOXML 验收和人工步骤清单，不能将不存在的软件记作通过。

v0.1.164 的历史回执表示首发边界；本节记录 v0.1.167 的产品级补齐完成状态和长期安全边界。
